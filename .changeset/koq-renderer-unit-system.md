---
"@itwin/imodel-components-react": patch
---

`KoqPropertyValueRenderer` now re-renders and re-formats its value when the active unit system, quantity formats, or formats provider formats change, so it no longer shows an outdated formatted value.
