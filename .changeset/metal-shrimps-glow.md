---
"@itwin/components-react": minor
---

Added `maxSize` property to `InputEditorSizeParams` interface of `@itwin/components-react` package (using TypeScript declaration merging) to allow max sizing for tool settings input properties.
