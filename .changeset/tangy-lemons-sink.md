---
"@itwin/components-react": patch
---

Property grid no longer renders a text editor for empty array and struct properties. Editors for such properties are rendered only when a custom editor is registered for them.
