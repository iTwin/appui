var _a, _b, _c;
import { j as jsxRuntimeExports } from "./jsx-runtime-_iMjpMZ4.js";
import { I as InitializerDecorator, A as AppUiDecorator } from "./Decorators-CXFXT6hr.js";
import { r as reactExports } from "./index-DlkhCVTf.js";
import { i as IModelApp, v as ConditionalStringValue, aR as Icon$1, az as IconHelper, aq as Orientation, aU as BeEvent, cQ as combineReducers$1, bp as UiError, ak as ConditionalBooleanValue, b1 as ContextMenuItem, b0 as ContextSubMenu, bd as BeUiEvent, L as Logger, cR as GlobalContextMenu, cS as UiEvent, cT as getCssVariableAsNumber, cU as NotifyMessageDetails, cV as OutputMessagePriority, cW as OutputMessageType, cX as create, bo as fn, cY as K, cZ as lodashExports, cI as Point, b4 as Rectangle, c_ as useStoreWithEqualityFn, c$ as createStore, d0 as shallow$1, ck as assert, d1 as useRefs, c as classnames, O as IconButton, aJ as DropdownMenu, aN as SvgMoreVertical, aK as MenuItem$1, d2 as SvgDockLeft, d3 as SvgDockRight, d4 as SvgDockTop, d5 as SvgDockBottom, d6 as useRefEffect, cJ as Timer, d7 as SvgPin, d8 as SvgPinHollow, d9 as SvgWindowPopout, da as HorizontalAlignment, db as SvgWindowMinimize, dc as SvgWindowMaximize, cE as useResizeObserver, a2 as Icon$2, dd as SvgDragHandleVertical, de as SvgMore, c1 as Popup, df as TransitionGroup, dg as CSSTransition, c2 as RelativePosition, cF as useRefState, K as Key_enum, cp as Guid, dh as Size, c9 as Div, di as shimExports, aS as BadgeUtilities, dj as MenuExtraContent, dk as Surface, dl as InternalToolbarComponent, ar as Direction, at as ToolbarPanelAlignment, dm as MessageRenderer$1, dn as connect, dp as ThemeProvider, dq as ThemeProvider$1, dr as SnapMode, ds as MessageBoxValue, dt as MessageBoxType, du as DialogButtonType, c7 as MessageSeverity, dv as MessageBoxIconType, dw as MessageBox, dx as MessageContainer, dy as OutputMessageAlert, dz as ActivityMessageDetails, n as SvgStatusSuccess, dA as SvgInfo, o as SvgStatusWarning, m as SvgStatusError, aE as SvgClose, dB as SvgInfoCircularHollow, dC as CubeNavigationAid, dD as DrawingNavigationAid, dE as SpatialViewState, dF as OrthographicViewState, dG as DrawingViewState, dH as SheetViewState, dI as ScrollView, dJ as FlexWrapContainer, cK as SvgPlaceholder, dK as SvgDocument, dL as SearchBox, dM as ViewportComponentEvents, b$ as SvgChevronLeft, c0 as SvgChevronRight, bq as ProgressRadial, cB as calculateToolbarOpacity, cC as calculateBoxShadowOpacity, cD as calculateBackdropFilterBlur, cw as getToolbarBackgroundColor, cx as getToolbarBoxShadow, cy as getToolbarBackdropFilter, cz as TOOLBAR_BOX_SHADOW_OPACITY_DEFAULT, cA as TOOLBAR_BACKDROP_FILTER_BLUR_DEFAULT, dN as svgCubeFacesTop, dO as svgCubeFacesBottom, dP as svgCubeFacesLeft, dQ as svgCubeFacesRight, dR as svgCubeFacesFront, dS as svgCubeFacesRear, dT as svgCubeFacesIsoLeft, dU as svgCubeFacesIsoRight, dV as StandardViewId, dW as Panel$1, dX as Columns, dY as GroupColumn, dZ as GroupTool, cH as ToolbarItemUtilities, a6 as usePackageTranslation, d_ as abstract, d$ as InteractiveTool, e0 as focusIntoContainer, cj as BentleyStatus, e1 as LocalStateStorage, e2 as $44d7e150ebc754d2$export$e926676385687eaf, e3 as NonIdealState, e4 as SvgError, e5 as SvgProgressBackwardCircular, Y as Text, d as BadgeType, e6 as useSelector, e7 as UiStateStorageStatus, e8 as ProcessDetector, e9 as ToolbarPopupAutoHideContext, ea as useToaster, eb as ProgressLinear, ec as DivWithOutsideClick, ed as FocusTrap, ee as PropertyValueRendererManager, aw as ToolbarOpacitySetting, ef as EditorContainer, j as PropertyValueFormat, eg as Tool, eh as svgRemove, ei as ParseAndRunResult, I as Input, ej as Listbox, ek as ListboxItem, el as FilteredText, em as UiLayoutDataProvider, bw as PropertyRecord, af as Provider, en as createStore$1, eo as EmphasizeElements, ep as FeatureAppearance, eq as svgSettings, er as SettingsContainer, es as Centered, et as svgViewLayouts, B as Button, c4 as IconInput, aL as SvgCheckmark, ca as SvgRemove, eu as ContextMenu, ev as AngleDescription, ew as LengthDescription, ex as Dialog$1, ey as UiIModelComponents, aI as UiAdmin, ez as SettingsManager, ac as getObjectClassName } from "./DefaultToolSettingsProvider-Do4qbEAN.js";
import { r as reactDomExports } from "./index-Cm_5MPU1.js";
import "./_commonjsHelpers-LQfde5yM.js";
import "./getPrototypeOf-BmmMfuHC.js";
import "./iframe-kR_u1aqe.js";
import "../sb-preview/runtime.js";
import "./index-B47T7vRo.js";
class PropsHelper {
  /** Get spec for returning a string. Could be a simple string of a 'StringGetter' method used to return the string. */
  static getStringSpec(explicitValue, stringKey) {
    if (explicitValue) {
      return explicitValue;
    }
    const outValue = stringKey ? IModelApp.localization.getLocalizedString(stringKey) : "";
    return outValue;
  }
  /** Get the display string. */
  static getStringFromSpec(spec) {
    let label = "";
    if (typeof spec === "string")
      label = spec;
    else if (spec instanceof ConditionalStringValue)
      label = spec.value;
    else
      label = spec();
    return label;
  }
  /** Get JSX element that defines an icon. If iconSpec is a string, then a web-font icon class is used otherwise a ReactNode holding an SVG icon is assumed.  */
  static getIcon(iconSpec) {
    if (iconSpec instanceof ConditionalStringValue)
      return /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec: iconSpec.value });
    return iconSpec ? /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec }) : void 0;
  }
  /** returns true if the two objects are the same using a shallow compare of each property */
  static isShallowEqual(newObj, prevObj) {
    for (const key in newObj) {
      if (newObj[key] !== prevObj[key])
        return false;
    }
    return true;
  }
  /** @deprecated in 4.0 These abstract props types are obsolete. */
  static getAbstractPropsForReactIcon(iconSpec, internalData) {
    if (!iconSpec || !reactExports.isValidElement(iconSpec))
      return {};
    if (!internalData)
      internalData = /* @__PURE__ */ new Map();
    const icon = IconHelper.getIconData(iconSpec, internalData);
    return icon === "" ? { icon } : { icon, internalData };
  }
}
try {
  PropsHelper.displayName = "PropsHelper";
  PropsHelper.__docgenInfo = { "description": "A set of helper methods for various props", "displayName": "PropsHelper", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class ItemDefBase {
  constructor(itemProps) {
    this._label = "";
    this._tooltip = "";
    this._description = "";
    this.isPressed = false;
    this._isActive = false;
    ItemDefBase.initializeDef(this, itemProps);
  }
  get isActive() {
    return this._isActive;
  }
  set isActive(v) {
    this._isActive = v;
  }
  static initializeDef(me, itemProps) {
    me.isHidden = itemProps.isHidden;
    me.isDisabled = itemProps.isDisabled;
    me.isPressed = itemProps.isPressed !== void 0 ? itemProps.isPressed : false;
    me.isActive = itemProps.isActive !== void 0 ? itemProps.isActive : false;
    me.badgeType = itemProps.badgeType;
    if (itemProps.applicationData !== void 0)
      me.applicationData = itemProps.applicationData;
    if (itemProps.iconSpec)
      me.iconSpec = itemProps.iconSpec;
    if (itemProps.icon)
      me.iconSpec = itemProps.icon;
    me._label = PropsHelper.getStringSpec(itemProps.label, itemProps.labelKey);
    me._tooltip = PropsHelper.getStringSpec(
      itemProps.tooltip,
      itemProps.tooltipKey
    );
    me._description = PropsHelper.getStringSpec(
      itemProps.description,
      itemProps.descriptionKey
    );
  }
  get trayId() {
    return void 0;
  }
  /** Get the label string */
  get rawLabel() {
    return this._label;
  }
  /** Get the label string */
  get label() {
    return PropsHelper.getStringFromSpec(this._label);
  }
  /** Set the label.
   * @param v A string or a function to get the string.
   */
  setLabel(v) {
    this._label = v;
  }
  /** Get the tooltip string */
  get tooltip() {
    return PropsHelper.getStringFromSpec(this._tooltip);
  }
  /** Set the tooltip.
   * @param v A string or a function to get the string.
   */
  setTooltip(v) {
    this._tooltip = v;
  }
  /** Get the description string */
  get description() {
    return PropsHelper.getStringFromSpec(this._description);
  }
  /** Set the description.
   * @param v A string or a function to get the string.
   */
  setDescription(v) {
    this._description = v;
  }
}
const _ActionButtonItemDef = class _ActionButtonItemDef extends ItemDefBase {
  constructor(itemProps, onItemExecuted) {
    super(itemProps);
    this.handleSizeKnown = (size) => {
      this.size = size;
    };
    this.execute = this.execute.bind(this);
    this._onItemExecuted = onItemExecuted;
  }
  /** Called when the action button is invoked by a click or touch */
  execute() {
    if (this._commandHandler && this._commandHandler.execute) {
      if (this._commandHandler.getCommandArgs)
        this._commandHandler.execute(this._commandHandler.getCommandArgs());
      else
        this._commandHandler.execute(this._commandHandler.parameters);
    }
    if (this._onItemExecuted)
      this._onItemExecuted(this);
  }
  /** Determines the dimension in a given orientation */
  getDimension(orientation) {
    let dimension = _ActionButtonItemDef.defaultButtonSize;
    if (this.size)
      dimension = orientation === Orientation.Horizontal ? this.size.width : this.size.height;
    return dimension;
  }
};
_ActionButtonItemDef.defaultButtonSize = 42;
let ActionButtonItemDef = _ActionButtonItemDef;
try {
  ActionButtonItemDef.displayName = "ActionButtonItemDef";
  ActionButtonItemDef.__docgenInfo = { "description": "Abstract base class that is used by classes to execute an action when pressed.", "displayName": "ActionButtonItemDef", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ActionButtonItemDef.defaultButtonSize.displayName = "ActionButtonItemDef.defaultButtonSize";
  ActionButtonItemDef.defaultButtonSize.__docgenInfo = { "description": "The default button size for all action buttons", "displayName": "ActionButtonItemDef.defaultButtonSize", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const _CommandItemDef = class _CommandItemDef extends ActionButtonItemDef {
  constructor(commandItemProps, onItemExecuted) {
    super(commandItemProps, onItemExecuted);
    this.commandId = "";
    if (commandItemProps.execute) {
      this._commandHandler = {
        execute: commandItemProps.execute,
        parameters: commandItemProps.parameters,
        getCommandArgs: commandItemProps.getCommandArgs
      };
    }
    if (commandItemProps.commandId)
      this.commandId = commandItemProps.commandId;
    else {
      _CommandItemDef._sId++;
      this.commandId = _CommandItemDef.commandIdPrefix + _CommandItemDef._sId;
    }
  }
  get id() {
    return this.commandId;
  }
};
_CommandItemDef._sId = 0;
_CommandItemDef.commandIdPrefix = "Command-";
let CommandItemDef = _CommandItemDef;
class InternalBackstageManager {
  constructor() {
    this._isOpen = false;
    this.onToggled = new BeEvent();
  }
  get isOpen() {
    return this._isOpen;
  }
  setIsOpen(isOpen) {
    if (isOpen === this._isOpen)
      return;
    this._isOpen = isOpen;
    this.onToggled.raiseEvent({
      isOpen
    });
  }
  open() {
    this.setIsOpen(true);
  }
  close() {
    this.setIsOpen(false);
  }
  toggle() {
    this.setIsOpen(!this.isOpen);
  }
  /** Get CommandItemDef that will toggle display of Backstage and allow iconSpec to be overridden */
  getBackstageToggleCommand(overrideIconSpec) {
    return new CommandItemDef({
      commandId: "UiFramework.openBackstage",
      iconSpec: overrideIconSpec ? overrideIconSpec : "icon-home",
      labelKey: "UiFramework:commands.openBackstage",
      execute: () => {
        UiFramework.backstage.toggle();
      }
    });
  }
}
try {
  InternalBackstageManager.displayName = "InternalBackstageManager";
  InternalBackstageManager.__docgenInfo = { "description": "Controls backstage.", "displayName": "InternalBackstageManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class BackstageManager {
  constructor() {
    this.internal = new InternalBackstageManager();
  }
  /**
   * Override internal implementation for a mock
   * @internal For tests only.
   */
  mockInternal(internal) {
    this.internal = internal;
  }
  /** Event raised when backstage is opened or closed. */
  get onToggled() {
    return this.internal.onToggled;
  }
  get isOpen() {
    return this.internal.isOpen;
  }
  open() {
    return this.internal.open();
  }
  close() {
    return this.internal.close();
  }
  toggle() {
    return this.internal.toggle();
  }
  getBackstageToggleCommand(overrideIconSpec) {
    return this.internal.getBackstageToggleCommand(overrideIconSpec);
  }
  /** Get CommandItemDef that will toggle display of Backstage and allow iconSpec to be overridden
   */
  static getBackstageToggleCommand(overrideIconSpec) {
    return UiFramework.backstage.getBackstageToggleCommand(overrideIconSpec);
  }
}
const useIsBackstageOpen = (manager) => {
  const [isOpen, setIsOpen] = reactExports.useState(manager.isOpen);
  reactExports.useEffect(() => {
    const handleToggled = (args) => {
      setIsOpen(args.isOpen);
    };
    setIsOpen(manager.isOpen);
    manager.onToggled.addListener(handleToggled);
    return () => {
      manager.onToggled.removeListener(handleToggled);
    };
  }, [manager]);
  return isOpen;
};
const useBackstageManager = () => {
  const [manager] = reactExports.useState(UiFramework.backstage);
  return manager;
};
try {
  BackstageManager.displayName = "BackstageManager";
  BackstageManager.__docgenInfo = { "description": "Controls backstage.", "displayName": "BackstageManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useIsBackstageOpen.displayName = "useIsBackstageOpen";
  useIsBackstageOpen.__docgenInfo = { "description": "Hook that returns isOpen flag of the backstage.", "displayName": "useIsBackstageOpen", "props": { "onToggled": { "defaultValue": null, "description": "Event activated when the backstage is toggled.", "name": "onToggled", "required": true, "type": { "name": "BeEvent<(args: BackstageToggledArgs) => void>" } }, "isOpen": { "defaultValue": null, "description": "State of the backstage.", "name": "isOpen", "required": true, "type": { "name": "boolean" } }, "open": { "defaultValue": null, "description": "Display the backstage.", "name": "open", "required": true, "type": { "name": "() => void" } }, "close": { "defaultValue": null, "description": "Hides the backstage.", "name": "close", "required": true, "type": { "name": "() => void" } }, "toggle": { "defaultValue": null, "description": "Toggle the backstage.", "name": "toggle", "required": true, "type": { "name": "() => void" } }, "getBackstageToggleCommand": { "defaultValue": null, "description": "Creates a CommandItemDef that toggle the backstage.\n@param overrideIconSpec Icon to replace the default 'home'.", "name": "getBackstageToggleCommand", "required": true, "type": { "name": "(overrideIconSpec?: IconSpec) => CommandItemDef" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useBackstageManager.displayName = "useBackstageManager";
  useBackstageManager.__docgenInfo = { "description": "Hook that returns backstage manager.", "displayName": "useBackstageManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const combineReducers = combineReducers$1;
var SessionStateActionId = /* @__PURE__ */ ((SessionStateActionId2) => {
  SessionStateActionId2["SetNumItemsSelected"] = "sessionstate:set-num-items-selected";
  SessionStateActionId2["SetAvailableSelectionScopes"] = "sessionstate:set-available-selection-scopes";
  SessionStateActionId2["SetSelectionScope"] = "sessionstate:set-selection-scope";
  SessionStateActionId2["SetActiveIModelId"] = "sessionstate:set-active-imodelid";
  SessionStateActionId2["SetIModelConnection"] = "sessionstate:set-imodel-connection";
  SessionStateActionId2["SetDefaultIModelViewportControlId"] = "sessionstate:set-default-viewportid";
  SessionStateActionId2["SetDefaultViewId"] = "sessionstate:set-default-viewid";
  SessionStateActionId2["SetDefaultViewState"] = "sessionstate:set-default-view-state";
  SessionStateActionId2["UpdateCursorMenu"] = "sessionstate:update-cursor-menu";
  return SessionStateActionId2;
})(SessionStateActionId || {});
const defaultSelectionScope = {
  id: "element",
  label: "Element"
};
const initialState$1 = {
  /** number of selected items in Presentation Selection */
  numItemsSelected: 0,
  /** initialize to only support "Element" scope, this will be overwritten when iModelConnection is established */
  availableSelectionScopes: [defaultSelectionScope],
  /** initialize to active selection scope to "Element", this will be overwritten when iModelConnection is established */
  activeSelectionScope: defaultSelectionScope.id,
  /** set to iModelId if an iModel is active else it is an empty string, so initialize to empty string */
  iModelId: "",
  defaultIModelViewportControlId: void 0,
  defaultViewId: void 0,
  defaultViewState: void 0,
  iModelConnection: void 0,
  cursorMenuData: void 0,
  // @deprecated in 4.11.x use {@link SessionState.cursorMenuPayload} instead
  cursorMenuPayload: void 0
};
function SessionStateReducer(state = initialState$1, action) {
  switch (action.type) {
    case "sessionstate:set-num-items-selected": {
      if (void 0 !== action.payload)
        return { ...state, numItemsSelected: action.payload };
      else
        return { ...state, numItemsSelected: 0 };
    }
    case "sessionstate:set-available-selection-scopes": {
      const payloadArray = [];
      action.payload.forEach((scope) => payloadArray.push(scope));
      if (void 0 !== action.payload)
        return { ...state, availableSelectionScopes: payloadArray };
      else
        return { ...state, availableSelectionScopes: [defaultSelectionScope] };
    }
    case "sessionstate:set-selection-scope": {
      if (void 0 !== action.payload)
        return { ...state, activeSelectionScope: action.payload };
      else
        return { ...state, activeSelectionScope: defaultSelectionScope.id };
    }
    case "sessionstate:set-active-imodelid": {
      if (void 0 !== action.payload)
        return { ...state, iModelId: action.payload };
      else
        return { ...state, iModelId: "" };
    }
    case "sessionstate:set-default-viewportid": {
      return { ...state, defaultIModelViewportControlId: action.payload };
    }
    case "sessionstate:set-default-viewid": {
      return { ...state, defaultViewId: action.payload };
    }
    case "sessionstate:set-default-view-state": {
      return { ...state, defaultViewState: action.payload };
    }
    case "sessionstate:set-imodel-connection": {
      return { ...state, iModelConnection: action.payload };
    }
    case "sessionstate:update-cursor-menu": {
      return {
        ...state,
        cursorMenuPayload: action.payload,
        // eslint-disable-next-line deprecation/deprecation
        cursorMenuData: action.payload
      };
    }
  }
  return state;
}
class MenuItem extends ItemDefBase {
  /** onSelection is an optional parameter typically supplied to allow menu parent to close context menu when a menu item is selected. */
  constructor(props, onSelection) {
    super(props);
    this._id = "";
    this._id = props.id;
    this._submenu = new Array();
    this._onSelection = onSelection;
    if (props.item) {
      this._actionItem = new CommandItemDef(props.item);
      if (!this.iconSpec)
        this.iconSpec = this._actionItem.iconSpec;
      if (!this.label)
        this.setLabel(this._actionItem.label);
      if (!this.badgeType)
        this.badgeType = this._actionItem.badgeType;
      if (!this.isDisabled)
        this.isDisabled = this._actionItem.isDisabled;
    } else if (props.submenu) {
      props.submenu.forEach((childProps) => {
        const childItem = new MenuItem(childProps, onSelection);
        this._submenu.push(childItem);
      });
    } else {
      throw new UiError(
        UiFramework.loggerCategory(this),
        `Either 'item' or 'submenu' must be specified for '${props.id}'.`
      );
    }
    this.iconRightSpec = props.iconRight;
  }
  get id() {
    return this._id;
  }
  get submenu() {
    return this._submenu;
  }
  get actionItem() {
    return this._actionItem;
  }
  itemPicked() {
    setTimeout(() => {
      if (this._actionItem)
        this._actionItem.execute();
    });
    if (this._onSelection)
      this._onSelection();
  }
}
class MenuItemHelpers {
  static createMenuItems(itemPropsList, onSelection) {
    const menuItems = new Array();
    itemPropsList.forEach((itemProps) => {
      menuItems.push(new MenuItem(itemProps, onSelection));
    });
    return menuItems;
  }
  static createMenuItemNodes(itemList) {
    const itemNodes = [];
    itemList.forEach((item, index) => {
      const reactItem = this.createMenuItemNode(item, index);
      if (reactItem)
        itemNodes.push(reactItem);
    });
    return itemNodes;
  }
  static createMenuItemNode(item, index) {
    let node = null;
    const label = item.label;
    const iconSpec = item.iconSpec;
    const iconRightSpec = item.iconRightSpec;
    const badgeType = item.badgeType;
    const isDisabled = ConditionalBooleanValue.getValue(
      item.isDisabled
    );
    if (item.actionItem) {
      const sel = () => item.itemPicked();
      node = /* @__PURE__ */ jsxRuntimeExports.jsx(
        ContextMenuItem,
        {
          onSelect: sel,
          icon: iconSpec,
          iconRight: iconRightSpec,
          badgeType,
          disabled: isDisabled,
          children: label
        },
        index
      );
    } else {
      if (item.submenu && item.submenu.length > 0) {
        const items = this.createMenuItemNodes(item.submenu);
        node = /* @__PURE__ */ jsxRuntimeExports.jsx(
          ContextSubMenu,
          {
            icon: iconSpec,
            label,
            badgeType,
            disabled: isDisabled,
            children: items
          },
          index
        );
      }
    }
    return node;
  }
}
try {
  MenuItem.displayName = "MenuItem";
  MenuItem.__docgenInfo = { "description": "Menu Item", "displayName": "MenuItem", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MenuItemHelpers.displayName = "MenuItemHelpers";
  MenuItemHelpers.__docgenInfo = { "description": "Menu Item helper methods", "displayName": "MenuItemHelpers", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class UiSyncEvent extends BeUiEvent {
}
class InternalSyncUiEventDispatcher {
  constructor() {
    this._eventIds = /* @__PURE__ */ new Set();
    this._eventIdAdded = false;
    this._uiSyncEvent = new UiSyncEvent();
    this._timeoutPeriod = 100;
    this._secondaryTimeoutPeriod = this._timeoutPeriod / 2;
  }
  /** @internal - used for testing only */
  /* istanbul ignore next */
  setTimeoutPeriod(period) {
    this._timeoutPeriod = period;
    this._secondaryTimeoutPeriod = Math.floor(this._timeoutPeriod / 2);
    if (this._secondaryTimeoutPeriod < 1)
      this._secondaryTimeoutPeriod = 1;
    if (this._syncEventTimerId) {
      window.clearTimeout(this._syncEventTimerId);
      this._syncEventTimerId = void 0;
    }
    if (this._eventIds)
      this._eventIds.clear();
    this._eventIdAdded = false;
  }
  /** The current timeout period */
  get timeoutPeriod() {
    return this._timeoutPeriod;
  }
  /** Return set of event ids that will be sent to listeners/. */
  get syncEventIds() {
    return this._eventIds;
  }
  /** Return UiSyncEvent so callers can register an event callback. */
  get onSyncUiEvent() {
    return this._uiSyncEvent;
  }
  /** Immediately trigger sync event processing. */
  dispatchImmediateSyncUiEvent(eventId) {
    const eventIds = /* @__PURE__ */ new Set();
    eventIds.add(eventId.toLowerCase());
    this.onSyncUiEvent.emit({ eventIds });
  }
  /** Save eventId in Set for processing. */
  dispatchSyncUiEvent(eventId) {
    if (0 === this._timeoutPeriod) {
      return;
    }
    this.syncEventIds.add(eventId.toLowerCase());
    if (!this._syncEventTimerId) {
      this._syncEventTimerId = window.setTimeout(() => {
        this.checkForAdditionalIds();
      }, this._timeoutPeriod);
    } else {
      this._eventIdAdded = true;
    }
  }
  /** Save multiple eventIds in Set for processing. */
  dispatchSyncUiEvents(eventIds) {
    if (0 === this._timeoutPeriod) {
      return;
    }
    eventIds.forEach((id) => this.syncEventIds.add(id.toLowerCase()));
    if (!this._syncEventTimerId) {
      this._syncEventTimerId = window.setTimeout(() => {
        this.checkForAdditionalIds();
      }, this._timeoutPeriod);
    } else {
      this._eventIdAdded = true;
    }
  }
  /** Trigger registered event processing when timer has expired and no addition eventId are added. */
  checkForAdditionalIds() {
    if (!this._eventIdAdded) {
      if (this._syncEventTimerId) {
        window.clearTimeout(this._syncEventTimerId);
        this._syncEventTimerId = void 0;
      }
      this._eventIdAdded = false;
      if (this.syncEventIds.size > 0) {
        const eventIds = /* @__PURE__ */ new Set();
        this.syncEventIds.forEach((value) => eventIds.add(value));
        this._eventIds.clear();
        this.onSyncUiEvent.emit({ eventIds });
      }
      return;
    }
    if (this._syncEventTimerId) {
      window.clearTimeout(this._syncEventTimerId);
      this._syncEventTimerId = void 0;
    }
    this._eventIdAdded = false;
    this._syncEventTimerId = window.setTimeout(() => {
      this.checkForAdditionalIds();
    }, this._secondaryTimeoutPeriod);
  }
  /** Checks to see if an eventId of interest is contained in the set of eventIds */
  hasEventOfInterest(eventIds, idsOfInterest) {
    if (idsOfInterest.length > 0 && idsOfInterest.some(
      (value) => eventIds.has(value.toLowerCase())
    ))
      return true;
    return false;
  }
}
var SyncUiEventId = /* @__PURE__ */ ((SyncUiEventId2) => {
  SyncUiEventId2["AccuDrawCompassModeChanged"] = "accudrawcompassmodechanged";
  SyncUiEventId2["AccuDrawRotationChanged"] = "accudrawrotationchanged";
  SyncUiEventId2["ActiveContentChanged"] = "activecontentchanged";
  SyncUiEventId2["ActiveViewportChanged"] = "activeviewportchanged";
  SyncUiEventId2["BackstageEvent"] = "backstageevent";
  SyncUiEventId2["ContentLayoutActivated"] = "contentlayoutactivated";
  SyncUiEventId2["ContentControlActivated"] = "contentcontrolactivated";
  SyncUiEventId2["FrontstageActivating"] = "frontstageactivating";
  SyncUiEventId2["FrontstageReady"] = "frontstageready";
  SyncUiEventId2["ModalFrontstageChanged"] = "modalfrontstagechanged";
  SyncUiEventId2["ModalDialogChanged"] = "modaldialogchanged";
  SyncUiEventId2["NavigationAidActivated"] = "navigationaidactivated";
  SyncUiEventId2["ToolActivated"] = "toolactivated";
  SyncUiEventId2["WidgetStateChanged"] = "widgetstatechanged";
  SyncUiEventId2["SelectionSetChanged"] = "selectionsetchanged";
  SyncUiEventId2["SettingsProvidersChanged"] = "settingsproviderschanged";
  SyncUiEventId2["ViewStateChanged"] = "viewstatechanged";
  SyncUiEventId2["UiStateStorageChanged"] = "uistatestoragechanged";
  SyncUiEventId2["ShowHideManagerSettingChange"] = "show-hide-setting-change";
  SyncUiEventId2["FeatureOverridesChanged"] = "featureoverrideschanged";
  SyncUiEventId2["ViewedModelsChanged"] = "viewedmodelschanged";
  return SyncUiEventId2;
})(SyncUiEventId || {});
const _SyncUiEventDispatcher = class _SyncUiEventDispatcher {
  /** @internal - used for testing only */
  /* istanbul ignore next */
  static setTimeoutPeriod(period) {
    _SyncUiEventDispatcher._uiEventDispatcher.setTimeoutPeriod(period);
  }
  /** Return set of event ids that will be sent to listeners/. */
  static get syncEventIds() {
    return _SyncUiEventDispatcher._uiEventDispatcher.syncEventIds;
  }
  /** Return SyncUiEvent so callers can register an event callback. */
  static get onSyncUiEvent() {
    return _SyncUiEventDispatcher._uiEventDispatcher.onSyncUiEvent;
  }
  /** Immediately trigger sync event processing. */
  static dispatchImmediateSyncUiEvent(eventId) {
    _SyncUiEventDispatcher._uiEventDispatcher.dispatchImmediateSyncUiEvent(
      eventId
    );
  }
  /** Save eventId in Set for processing. */
  static dispatchSyncUiEvent(eventId) {
    if (0 === _SyncUiEventDispatcher._uiEventDispatcher.timeoutPeriod) {
      Logger.logInfo(
        UiFramework.loggerCategory(this),
        `[dispatchSyncUiEvent] not processed because timeoutPeriod=0`
      );
      return;
    }
    _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(eventId);
  }
  /** Save multiple eventIds in Set for processing. */
  static dispatchSyncUiEvents(eventIds) {
    if (0 === _SyncUiEventDispatcher._uiEventDispatcher.timeoutPeriod) {
      Logger.logInfo(
        UiFramework.loggerCategory(this),
        `[dispatchSyncUiEvents] not processed because _timeoutPeriod=0`
      );
    }
    _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvents(eventIds);
  }
  /** Checks to see if an eventId of interest is contained in the set of eventIds */
  static hasEventOfInterest(eventIds, idsOfInterest) {
    return _SyncUiEventDispatcher._uiEventDispatcher.hasEventOfInterest(
      eventIds,
      idsOfInterest
    );
  }
  // istanbul ignore next
  static _dispatchViewChange() {
    _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
      "viewstatechanged"
      /* ViewStateChanged */
    );
  }
  // istanbul ignore next
  static _dispatchFeatureOverridesChange() {
    _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
      "featureoverrideschanged"
      /* FeatureOverridesChanged */
    );
  }
  // istanbul ignore next
  static _dispatchViewedModelsChanged() {
    _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
      "viewedmodelschanged"
      /* ViewedModelsChanged */
    );
  }
  /** Initializes the Monitoring of Events that trigger dispatching sync events */
  static initialize() {
    this._unregisterFuncs.forEach((unregister) => unregister());
    this._unregisterFuncs = [];
    this._unregisterFuncs.push(
      UiFramework.frontstages.onContentControlActivatedEvent.addListener(() => {
        _SyncUiEventDispatcher.dispatchSyncUiEvent(
          "contentcontrolactivated"
          /* ContentControlActivated */
        );
      }),
      UiFramework.frontstages.onContentLayoutActivatedEvent.addListener(() => {
        _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
          "contentlayoutactivated"
          /* ContentLayoutActivated */
        );
      }),
      UiFramework.frontstages.onFrontstageActivatedEvent.addListener(() => {
        _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
          "frontstageactivating"
          /* FrontstageActivating */
        );
      }),
      UiFramework.frontstages.onFrontstageReadyEvent.addListener(() => {
        _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
          "frontstageready"
          /* FrontstageReady */
        );
      }),
      UiFramework.frontstages.onModalFrontstageChangedEvent.addListener(() => {
        _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
          "modalfrontstagechanged"
          /* ModalFrontstageChanged */
        );
      }),
      UiFramework.frontstages.onNavigationAidActivatedEvent.addListener(() => {
        _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
          "navigationaidactivated"
          /* NavigationAidActivated */
        );
      }),
      UiFramework.frontstages.onToolActivatedEvent.addListener(() => {
        _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
          "toolactivated"
          /* ToolActivated */
        );
      }),
      UiFramework.frontstages.onWidgetStateChangedEvent.addListener(() => {
        _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
          "widgetstatechanged"
          /* WidgetStateChanged */
        );
      }),
      UiFramework.backstage.onToggled.addListener(() => {
        _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
          "backstageevent"
          /* BackstageEvent */
        );
      }),
      UiFramework.content.onActiveContentChangedEvent.addListener(() => {
        _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
          "activecontentchanged"
          /* ActiveContentChanged */
        );
      }),
      IModelApp.viewManager.onSelectedViewportChanged.addListener((args) => {
        _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
          "activeviewportchanged"
          /* ActiveViewportChanged */
        );
        if (void 0 === args.previous) {
          void IModelApp.toolAdmin.startDefaultTool();
        } else {
          args.previous.onViewChanged.removeListener(
            _SyncUiEventDispatcher._dispatchViewChange
          );
          args.previous.onFeatureOverridesChanged.removeListener(
            _SyncUiEventDispatcher._dispatchFeatureOverridesChange
          );
          args.previous.onViewedModelsChanged.removeListener(
            _SyncUiEventDispatcher._dispatchViewedModelsChanged
          );
        }
        if (args.current) {
          args.current.onViewChanged.addListener(
            _SyncUiEventDispatcher._dispatchViewChange
          );
          args.current.onFeatureOverridesChanged.addListener(
            _SyncUiEventDispatcher._dispatchFeatureOverridesChange
          );
          args.current.onViewedModelsChanged.addListener(
            _SyncUiEventDispatcher._dispatchViewedModelsChanged
          );
        }
      })
    );
  }
  /** This should be called by IModelApp when the active IModelConnection is closed. */
  static clearConnectionEvents(iModelConnection) {
    if (this._iModelConnection !== iModelConnection)
      return;
    this._connectionUnregisterFuncs.forEach((unregister) => unregister());
    this._connectionUnregisterFuncs = [];
  }
  /** This should be called by IModelApp when the active IModelConnection is established. */
  static initializeConnectionEvents(iModelConnection) {
    this._iModelConnection && this.clearConnectionEvents(this._iModelConnection);
    this._iModelConnection = iModelConnection;
    this._connectionUnregisterFuncs.push(
      iModelConnection.selectionSet.onChanged.addListener(() => {
        _SyncUiEventDispatcher._uiEventDispatcher.dispatchSyncUiEvent(
          "selectionsetchanged"
          /* SelectionSetChanged */
        );
      }),
      iModelConnection.selectionSet.onChanged.addListener((ev) => {
        const numSelected = ev.set.elements.size;
        UiFramework.dispatchActionToStore(
          SessionStateActionId.SetNumItemsSelected,
          numSelected
        );
      })
    );
  }
};
_SyncUiEventDispatcher._uiEventDispatcher = new InternalSyncUiEventDispatcher();
_SyncUiEventDispatcher._unregisterFuncs = new Array();
_SyncUiEventDispatcher._connectionUnregisterFuncs = new Array();
let SyncUiEventDispatcher = _SyncUiEventDispatcher;
class CursorPopupMenu extends reactExports.PureComponent {
  constructor() {
    super(...arguments);
    this._isMounted = false;
    this.state = {
      menuX: 0,
      menuY: 0,
      menuVisible: false,
      items: void 0
    };
    this._handleSyncUiEvent = (args) => {
      if (!this._isMounted)
        return;
      if (SyncUiEventDispatcher.hasEventOfInterest(args.eventIds, [
        SessionStateActionId.UpdateCursorMenu
      ])) {
        const menuData = UiFramework.getCursorMenuData();
        if (menuData && this._hostChildWindowId === menuData.childWindowId) {
          this.setState({
            menuVisible: menuData.items && menuData.items.length > 0,
            items: menuData.items,
            menuX: menuData.position.x,
            menuY: menuData.position.y
          });
        } else {
          this.setState({ menuVisible: false, items: void 0 });
        }
      }
    };
    this._handleRefSet = (popupDiv) => {
      const parentWindow = (popupDiv == null ? void 0 : popupDiv.ownerDocument.defaultView) ?? void 0;
      this._hostChildWindowId = UiFramework.childWindows.findId(parentWindow);
      Logger.logInfo(
        UiFramework.loggerCategory(UiFramework),
        `Cursor Menu for ${this._hostChildWindowId ?? "main"} window`
      );
    };
    this._hideContextMenu = () => {
      this.setState({ menuVisible: false });
    };
    this._itemPicked = () => {
      this._hideContextMenu();
    };
  }
  componentDidMount() {
    this._isMounted = true;
    SyncUiEventDispatcher.onSyncUiEvent.addListener(this._handleSyncUiEvent);
  }
  componentWillUnmount() {
    this._isMounted = false;
    SyncUiEventDispatcher.onSyncUiEvent.removeListener(this._handleSyncUiEvent);
  }
  render() {
    const { menuX, menuY, items, menuVisible } = this.state;
    const onClose = this._hideContextMenu;
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-cursor-menu-container-div", ref: this._handleRefSet, children: items && items.length > 0 && menuVisible && /* @__PURE__ */ jsxRuntimeExports.jsx(
      GlobalContextMenu,
      {
        className: this.props.className,
        style: this.props.style,
        identifier: "cursor-popup-menu",
        x: menuX,
        y: menuY,
        opened: menuVisible,
        onEsc: onClose,
        onOutsideClick: onClose,
        edgeLimit: false,
        autoflip: true,
        children: MenuItemHelpers.createMenuItemNodes(
          MenuItemHelpers.createMenuItems(items, this._itemPicked)
        )
      }
    ) });
  }
}
try {
  CursorPopupMenu.displayName = "CursorPopupMenu";
  CursorPopupMenu.__docgenInfo = { "description": "Popup Menu to show at cursor typically used by tools to provide a right-click context menu.", "displayName": "CursorPopupMenu", "props": { "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class DialogChangedEvent extends UiEvent {
}
const ZINDEX_DEFAULT = 12e3;
const _DialogManagerBase = class _DialogManagerBase {
  constructor(onDialogChangedEvent) {
    this._dialogs = new Array();
    this._onDialogChangedEvent = onDialogChangedEvent;
  }
  /** Initialize the modeless dialog manager */
  static initialize() {
    _DialogManagerBase._topZIndex = _DialogManagerBase.getDialogZIndexDefault();
  }
  static get topZIndex() {
    return _DialogManagerBase._topZIndex;
  }
  static set topZIndex(zIndex) {
    _DialogManagerBase._topZIndex = zIndex;
  }
  get dialogs() {
    return this._dialogs;
  }
  get onDialogChangedEvent() {
    return this._onDialogChangedEvent;
  }
  static getDialogZIndexDefault() {
    const variable = "--uicore-z-index-dialog";
    const value = getCssVariableAsNumber(variable);
    if (!isNaN(value))
      return value;
    Logger.logError(
      UiFramework.loggerCategory(this),
      `'${variable}' CSS variable not found`
    );
    return ZINDEX_DEFAULT;
  }
  /**
   * Triggers opening a dialog.
   * @param dialog Dialog React component.
   * @param id The unique Id the identifies the dialog.
   * @param parentDocument Optional document required when displaying a dialog in a child popup window.
   */
  openDialog(dialog2, id, parentDocument) {
    if (!id)
      id = `Dialog-${++_DialogManagerBase._sId}`;
    const owningDoc = parentDocument ?? document;
    this.pushDialog({ reactNode: dialog2, id, parentDocument: owningDoc });
  }
  /** @internal */
  pushDialog(dialogInfo) {
    this._dialogs.push(dialogInfo);
    this.emitDialogChangedEvent();
  }
  closeDialog(dialog2) {
    let targetDialog = dialog2;
    if (!dialog2)
      targetDialog = this.activeDialog;
    this.removeDialog(targetDialog);
    this.emitDialogChangedEvent();
  }
  /** @internal */
  closeAll() {
    this._dialogs = [];
    this.emitDialogChangedEvent();
  }
  /** @internal */
  removeDialog(dialog2) {
    const index = this._dialogs.findIndex((dialogInfo) => {
      return dialog2 === dialogInfo.reactNode;
    });
    if (index >= 0)
      this._dialogs.splice(index, 1);
    if (this._dialogs.length < 1)
      _DialogManagerBase.topZIndex = _DialogManagerBase.getDialogZIndexDefault();
  }
  emitDialogChangedEvent() {
    this._onDialogChangedEvent.emit({
      dialogCount: this.dialogCount,
      activeDialog: this.activeDialog
    });
  }
  update() {
    this.emitDialogChangedEvent();
  }
  get activeDialog() {
    if (this._dialogs.length > 0)
      return this._dialogs[this._dialogs.length - 1].reactNode;
    return void 0;
  }
  get dialogCount() {
    return this._dialogs.length;
  }
};
_DialogManagerBase._sId = 0;
_DialogManagerBase._topZIndex = ZINDEX_DEFAULT;
let DialogManagerBase = _DialogManagerBase;
class DialogRendererBase extends reactExports.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      parentDocument: null
    };
    this._handleRefSet = (popupDiv) => {
      this.setState({ parentDocument: (popupDiv == null ? void 0 : popupDiv.ownerDocument) ?? null });
    };
    this._handleDialogChangedEvent = (_args) => {
      this.forceUpdate();
    };
  }
  render() {
    if (this.props.dialogManager.dialogCount <= 0)
      return null;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: "appui-react-dialog-render-container",
        ref: this._handleRefSet,
        children: this.state.parentDocument && this.props.dialogManager.dialogs.filter((info) => info.parentDocument === this.state.parentDocument).map((dialogInfo) => {
          return /* @__PURE__ */ jsxRuntimeExports.jsx(reactExports.Fragment, { children: dialogInfo.reactNode }, dialogInfo.id);
        })
      }
    );
  }
  componentDidMount() {
    this.props.dialogManager.onDialogChangedEvent.addListener(
      this._handleDialogChangedEvent
    );
  }
  componentWillUnmount() {
    this.props.dialogManager.onDialogChangedEvent.removeListener(
      this._handleDialogChangedEvent
    );
  }
}
try {
  DialogChangedEvent.displayName = "DialogChangedEvent";
  DialogChangedEvent.__docgenInfo = { "description": "Dialog Changed Event class.", "displayName": "DialogChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  DialogManagerBase.displayName = "DialogManagerBase";
  DialogManagerBase.__docgenInfo = { "description": "Dialog Manager class.", "displayName": "DialogManagerBase", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  DialogRendererBase.displayName = "DialogRendererBase";
  DialogRendererBase.__docgenInfo = { "description": "DialogRenderer React component.", "displayName": "DialogRendererBase", "props": { "dialogManager": { "defaultValue": null, "description": "", "name": "dialogManager", "required": true, "type": { "name": "DialogManagerBase" } }, "style": { "defaultValue": null, "description": "", "name": "style", "required": false, "type": { "name": "CSSProperties" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class ModalDialogChangedEvent extends DialogChangedEvent {
}
class ModelessDialogChangedEvent extends DialogChangedEvent {
}
const _InternalModalDialogManager = class _InternalModalDialogManager {
  /** Get the array of modal dialogs */
  static get dialogs() {
    return _InternalModalDialogManager.dialogManager.dialogs;
  }
  /** Open a modal dialog
   * @param dialog The Dialog to open
   * @param id The id of the Dialog. If one is not provided, an id is generated.
   * @param parentDocument The Document used to determine the owning window.
   */
  static open(dialog2, id, parentDocument = document) {
    _InternalModalDialogManager.dialogManager.openDialog(
      dialog2,
      id,
      parentDocument
    );
  }
  /** Close a modal dialog
   * @param dialog The Dialog to open. If one is not specified, the active dialog will be closed.
   */
  static close(dialog2) {
    _InternalModalDialogManager.dialogManager.closeDialog(dialog2);
  }
  /** @internal */
  static closeAll() {
    _InternalModalDialogManager.dialogManager.closeAll();
  }
  /** Update the dialogs */
  static update() {
    _InternalModalDialogManager.dialogManager.update();
  }
  /** Get the active modal dialog */
  static get active() {
    return _InternalModalDialogManager.dialogManager.activeDialog;
  }
  /** Get the count of modal dialogs */
  static get count() {
    return _InternalModalDialogManager.dialogManager.dialogCount;
  }
};
_InternalModalDialogManager.onModalDialogChangedEvent = new ModalDialogChangedEvent();
_InternalModalDialogManager.dialogManager = new DialogManagerBase(_InternalModalDialogManager.onModalDialogChangedEvent);
let InternalModalDialogManager = _InternalModalDialogManager;
try {
  InternalModalDialogManager.displayName = "InternalModalDialogManager";
  InternalModalDialogManager.__docgenInfo = { "description": "Modal Dialog Manager class displays and manages multiple modal dialogs", "displayName": "InternalModalDialogManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalModalDialogManager.onModalDialogChangedEvent.displayName = "InternalModalDialogManager.onModalDialogChangedEvent";
  InternalModalDialogManager.onModalDialogChangedEvent.__docgenInfo = { "description": "Modal Dialog Changed Event class.", "displayName": "InternalModalDialogManager.onModalDialogChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalModalDialogManager.dialogManager.displayName = "InternalModalDialogManager.dialogManager";
  InternalModalDialogManager.dialogManager.__docgenInfo = { "description": "Dialog Manager class.", "displayName": "InternalModalDialogManager.dialogManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalModalDialogManager.dialogs.displayName = "InternalModalDialogManager.dialogs";
  InternalModalDialogManager.dialogs.__docgenInfo = { "description": "Get the array of modal dialogs", "displayName": "InternalModalDialogManager.dialogs", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalModalDialogManager.active.displayName = "InternalModalDialogManager.active";
  InternalModalDialogManager.active.__docgenInfo = { "description": "Get the active modal dialog", "displayName": "InternalModalDialogManager.active", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalModalDialogManager.count.displayName = "InternalModalDialogManager.count";
  InternalModalDialogManager.count.__docgenInfo = { "description": "Get the count of modal dialogs", "displayName": "InternalModalDialogManager.count", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class ModalDialogRenderer extends reactExports.PureComponent {
  constructor(props) {
    super(props);
  }
  render() {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      DialogRendererBase,
      {
        ...this.props,
        dialogManager: InternalModalDialogManager.dialogManager
      }
    );
  }
}
try {
  ModalDialogRenderer.displayName = "ModalDialogRenderer";
  ModalDialogRenderer.__docgenInfo = { "description": "ModalDialogRenderer React component renders modal dialogs", "displayName": "ModalDialogRenderer", "props": { "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const _InternalModelessDialogManager = class _InternalModelessDialogManager {
  /** Get the array of modeless dialogs */
  static get dialogs() {
    return _InternalModelessDialogManager.dialogManager.dialogs;
  }
  /** Initialize the modeless dialog manager
   * @internal
   */
  static initialize() {
    DialogManagerBase.initialize();
  }
  /** Open a modeless dialog
   * @param dialog The Dialog to open
   * @param id The id of the Dialog to open
   */
  static open(dialog2, id, parentDocument = document) {
    const dialogInfo = _InternalModelessDialogManager._dialogMap.get(id);
    if (dialogInfo) {
      const message = `Dialog with id of '${id}' already opened`;
      Logger.logInfo(
        UiFramework.loggerCategory(this),
        `openDialog: ${message}`
      );
      IModelApp.notifications.outputMessage(
        new NotifyMessageDetails(
          OutputMessagePriority.Info,
          message,
          void 0,
          OutputMessageType.Toast
        )
      );
      return;
    }
    _InternalModelessDialogManager._dialogMap.set(id, {
      reactNode: dialog2,
      zIndex: DialogManagerBase.topZIndex,
      parentDocument
    });
    _InternalModelessDialogManager._idArray.push(id);
    _InternalModelessDialogManager.dialogManager.openDialog(
      dialog2,
      id,
      parentDocument
    );
  }
  /** Close a modeless dialog
   * @param id The id of the Dialog to close.
   */
  static close(id) {
    const dialogInfo = _InternalModelessDialogManager._dialogMap.get(id);
    if (dialogInfo) {
      _InternalModelessDialogManager.dialogManager.removeDialog(
        dialogInfo.reactNode
      );
      _InternalModelessDialogManager._dialogMap.delete(id);
      const index = _InternalModelessDialogManager._idArray.indexOf(id);
      if (index >= 0)
        _InternalModelessDialogManager._idArray.splice(index, 1);
      if (_InternalModelessDialogManager.active === void 0)
        DialogManagerBase.topZIndex = DialogManagerBase.getDialogZIndexDefault();
      this.update();
    } else {
      Logger.logError(
        UiFramework.loggerCategory(this),
        `closeDialog: Could not find dialog with id of '${id}'`
      );
    }
  }
  /** @internal */
  static closeAll() {
    _InternalModelessDialogManager.dialogManager.closeAll();
    _InternalModelessDialogManager._dialogMap.clear();
    _InternalModelessDialogManager._idArray = [];
  }
  /** Update the dialogs */
  static update() {
    _InternalModelessDialogManager.dialogManager.update();
  }
  /** Get the active modeless dialog */
  static get active() {
    if (_InternalModelessDialogManager._idArray.length > 0) {
      const id = _InternalModelessDialogManager._idArray[_InternalModelessDialogManager._idArray.length - 1];
      const dialogInfo = _InternalModelessDialogManager._dialogMap.get(id);
      if (dialogInfo)
        return dialogInfo.reactNode;
    }
    return void 0;
  }
  /** Get the count of modeless dialogs */
  static get count() {
    return _InternalModelessDialogManager.dialogManager.dialogCount;
  }
  /** Handle a pointer down event on a modeless dialog */
  static handlePointerDownEvent(_event, id, updateFunc) {
    const dialogInfo = _InternalModelessDialogManager._dialogMap.get(id);
    if (dialogInfo && dialogInfo.reactNode !== _InternalModelessDialogManager.active) {
      DialogManagerBase.topZIndex += 1;
      dialogInfo.zIndex = DialogManagerBase.topZIndex;
      _InternalModelessDialogManager._idArray.splice(
        _InternalModelessDialogManager._idArray.indexOf(id),
        1
      );
      _InternalModelessDialogManager._idArray.push(id);
      updateFunc();
      this.update();
    }
  }
  /** Get the z-index for a modeless dialog */
  static getZIndex(id) {
    let zIndex = DialogManagerBase.getDialogZIndexDefault();
    const dialogInfo = _InternalModelessDialogManager._dialogMap.get(id);
    if (dialogInfo)
      zIndex = dialogInfo.zIndex;
    return zIndex;
  }
  static getInfo(id) {
    return _InternalModelessDialogManager._dialogMap.get(id);
  }
};
_InternalModelessDialogManager.onModelessDialogChangedEvent = new ModelessDialogChangedEvent();
_InternalModelessDialogManager.dialogManager = new DialogManagerBase(
  _InternalModelessDialogManager.onModelessDialogChangedEvent
);
_InternalModelessDialogManager._dialogMap = /* @__PURE__ */ new Map();
_InternalModelessDialogManager._idArray = new Array();
let InternalModelessDialogManager = _InternalModelessDialogManager;
try {
  InternalModelessDialogManager.displayName = "InternalModelessDialogManager";
  InternalModelessDialogManager.__docgenInfo = { "description": "Modeless Dialog Manager class displays and manages multiple modeless dialogs", "displayName": "InternalModelessDialogManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalModelessDialogManager.onModelessDialogChangedEvent.displayName = "InternalModelessDialogManager.onModelessDialogChangedEvent";
  InternalModelessDialogManager.onModelessDialogChangedEvent.__docgenInfo = { "description": "Modeless Dialog Changed Event class.", "displayName": "InternalModelessDialogManager.onModelessDialogChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalModelessDialogManager.dialogManager.displayName = "InternalModelessDialogManager.dialogManager";
  InternalModelessDialogManager.dialogManager.__docgenInfo = { "description": "Dialog Manager class.", "displayName": "InternalModelessDialogManager.dialogManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalModelessDialogManager.dialogs.displayName = "InternalModelessDialogManager.dialogs";
  InternalModelessDialogManager.dialogs.__docgenInfo = { "description": "Get the array of modeless dialogs", "displayName": "InternalModelessDialogManager.dialogs", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalModelessDialogManager.active.displayName = "InternalModelessDialogManager.active";
  InternalModelessDialogManager.active.__docgenInfo = { "description": "Get the active modeless dialog", "displayName": "InternalModelessDialogManager.active", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalModelessDialogManager.count.displayName = "InternalModelessDialogManager.count";
  InternalModelessDialogManager.count.__docgenInfo = { "description": "Get the count of modeless dialogs", "displayName": "InternalModelessDialogManager.count", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class ModelessDialogRenderer extends reactExports.PureComponent {
  constructor(props) {
    super(props);
  }
  render() {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      DialogRendererBase,
      {
        ...this.props,
        dialogManager: InternalModelessDialogManager.dialogManager
      }
    );
  }
}
try {
  ModelessDialogRenderer.displayName = "ModelessDialogRenderer";
  ModelessDialogRenderer.__docgenInfo = { "description": "ModelessDialogRenderer React component renders modeless dialogs.", "displayName": "ModelessDialogRenderer", "props": { "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetContentManager(props) {
  const setContentContainer = useContainersStore((state) => state.setContainer);
  const saveTransientStateRef = reactExports.useRef(
    new BeEvent()
  );
  const restoreTransientStateRef = reactExports.useRef(
    new BeEvent()
  );
  const setContainer = reactExports.useCallback(
    (tabId, container) => {
      container === null && saveTransientStateRef.current.raiseEvent(tabId);
      setContentContainer(tabId, container);
    },
    [setContentContainer]
  );
  const widgetContentManagerContextValue = reactExports.useMemo(
    () => ({
      setContainer,
      onSaveTransientState: saveTransientStateRef.current,
      onRestoreTransientState: restoreTransientStateRef.current
    }),
    [setContainer]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    WidgetContentManagerContext.Provider,
    {
      value: widgetContentManagerContextValue,
      children: props.children
    }
  );
}
const useContainersStore = create((set) => ({
  containers: {},
  setContainer: (tabId, container) => {
    set(
      (state) => fn(state, (draft) => {
        draft.containers[tabId] = K(container);
      })
    );
  }
}));
const WidgetContentManagerContext = reactExports.createContext(void 0);
WidgetContentManagerContext.displayName = "nz:WidgetContentManagerContext";
try {
  nz:
    WidgetContentManagerContext.displayName = "nz:WidgetContentManagerContext";
  nz:
    WidgetContentManagerContext.__docgenInfo = { "description": "", "displayName": "nz:WidgetContentManagerContext", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function isTabDropTargetState(state) {
  return state.type === "tab";
}
function isPanelDropTargetState(state) {
  return state.type === "panel";
}
function isSectionDropTargetState(state) {
  return state.type === "section";
}
function isWidgetDropTargetState(state) {
  return state.type === "widget";
}
function isWindowDropTargetState(state) {
  return state.type === "window";
}
function isWidgetDragDropTargetState(state) {
  if (state.type === "floatingWidget")
    return false;
  return true;
}
function isTabDragDropTargetState(state) {
  if (state.type === "window")
    return false;
  return true;
}
function useDragTab(args) {
  const { tabId, onDrag, onDragEnd } = args;
  const item = reactExports.useMemo(() => {
    return {
      type: "tab",
      id: tabId
    };
  }, [tabId]);
  const handleDrag = reactExports.useCallback(
    (_item, info) => {
      const dragBy = info.lastPointerPosition.getOffsetTo(info.pointerPosition);
      onDrag && onDrag(dragBy);
    },
    [onDrag]
  );
  const handleDragEnd = reactExports.useCallback(
    (_item, info, target) => {
      if (!onDragEnd)
        return;
      let tabTarget;
      if (target && isTabDragDropTargetState(target)) {
        tabTarget = target;
      } else {
        const tabInfo = info;
        const newFloatingWidgetId = getUniqueId();
        const size = tabInfo.widgetSize;
        tabTarget = {
          type: "floatingWidget",
          newFloatingWidgetId,
          size
        };
      }
      onDragEnd(tabTarget);
    },
    [onDragEnd]
  );
  const onDragStart = useDragItem({
    item,
    onDrag: handleDrag,
    onDragEnd: handleDragEnd
  });
  const handleDragStart = reactExports.useCallback(
    ({
      initialPointerPosition,
      pointerPosition,
      widgetSize
    }) => {
      onDragStart({
        initialPointerPosition,
        pointerPosition,
        lastPointerPosition: initialPointerPosition,
        widgetSize
      });
    },
    [onDragStart]
  );
  return handleDragStart;
}
function useDragWidget(args) {
  const { widgetId, onDragStart, onDrag, onDragEnd } = args;
  const dragManager = reactExports.useContext(DragManagerContext);
  const widgetItem = reactExports.useMemo(() => {
    return {
      type: "widget",
      id: widgetId
    };
  }, [widgetId]);
  const handleDragStart = reactExports.useCallback(
    (_item, info) => {
      onDragStart && onDragStart(
        (id) => {
          dragManager.handleDragUpdate({
            type: "widget",
            id
          });
        },
        info.initialPointerPosition,
        info.pointerPosition
      );
    },
    [dragManager, onDragStart]
  );
  const handleDrag = reactExports.useCallback(
    (_item, info) => {
      const dragBy = info.lastPointerPosition.getOffsetTo(info.pointerPosition);
      onDrag && onDrag(dragBy);
    },
    [onDrag]
  );
  const handleDragEnd = reactExports.useCallback(
    (_item, _info, target) => {
      if (!onDragEnd)
        return;
      let widgetTarget;
      if (target && isWidgetDragDropTargetState(target)) {
        widgetTarget = target;
      } else {
        widgetTarget = {
          type: "window"
        };
      }
      onDragEnd(widgetTarget);
    },
    [onDragEnd]
  );
  const isDragItem = reactExports.useCallback((item, dragged) => {
    return !!item && defaultIsDragItem(item, dragged);
  }, []);
  const onItemDragStart = useDragItem({
    item: widgetItem,
    isDragItem,
    onDragStart: handleDragStart,
    onDrag: handleDrag,
    onDragEnd: handleDragEnd
  });
  const handleWidgetDragStart = reactExports.useCallback(
    ({ initialPointerPosition, pointerPosition }) => {
      onItemDragStart({
        initialPointerPosition,
        pointerPosition,
        lastPointerPosition: initialPointerPosition
      });
    },
    [onItemDragStart]
  );
  return handleWidgetDragStart;
}
function useDragPanelGrip(args) {
  const { side, onDrag, onDragEnd } = args;
  const gripItem = reactExports.useMemo(() => {
    return {
      type: "panelGrip",
      id: side
    };
  }, [side]);
  const handleDrag = reactExports.useCallback(
    (_item, info) => {
      onDrag && onDrag(info.pointerPosition, info.lastPointerPosition);
    },
    [onDrag]
  );
  const handleDragEnd = reactExports.useCallback(() => {
    onDragEnd && onDragEnd();
  }, [onDragEnd]);
  const onItemDragStart = useDragItem({
    item: gripItem,
    onDrag: handleDrag,
    onDragEnd: handleDragEnd
  });
  const handleDragStart = reactExports.useCallback(
    ({ initialPointerPosition }) => {
      onItemDragStart({
        initialPointerPosition,
        pointerPosition: initialPointerPosition,
        lastPointerPosition: initialPointerPosition
      });
    },
    [onItemDragStart]
  );
  return handleDragStart;
}
function useDragResizeHandle(args) {
  const { handle, onDrag, widgetId } = args;
  const resizeHandleItem = reactExports.useMemo(() => {
    return {
      type: "resizeHandle",
      id: handle,
      widgetId
    };
  }, [handle, widgetId]);
  const isDragItem = reactExports.useCallback((item, draggedItem) => {
    return !!item && isResizeHandleDragItem(draggedItem) && defaultIsDragItem(item, draggedItem) && item.widgetId === draggedItem.widgetId;
  }, []);
  const handleDrag = reactExports.useCallback(
    (_item, info) => {
      onDrag && onDrag(info.pointerPosition);
    },
    [onDrag]
  );
  const onItemDragStart = useDragItem({
    item: resizeHandleItem,
    isDragItem,
    onDrag: handleDrag
  });
  const handleDragStart = reactExports.useCallback(
    ({ initialPointerPosition }) => {
      onItemDragStart({
        initialPointerPosition,
        pointerPosition: initialPointerPosition,
        lastPointerPosition: initialPointerPosition
      });
    },
    [onItemDragStart]
  );
  return handleDragStart;
}
function useDragToolSettings(args) {
  const { newFloatingWidgetId } = args;
  const item = reactExports.useMemo(() => {
    return {
      type: "widget",
      id: newFloatingWidgetId
    };
  }, [newFloatingWidgetId]);
  const onDragStart = useDragItem({
    item
  });
  const handleDragStart = reactExports.useCallback(
    ({ initialPointerPosition }) => {
      onDragStart({
        initialPointerPosition,
        pointerPosition: initialPointerPosition,
        lastPointerPosition: initialPointerPosition
      });
    },
    [onDragStart]
  );
  return handleDragStart;
}
function useTarget(target) {
  const dragManager = reactExports.useContext(DragManagerContext);
  const [targeted, setTargeted] = reactExports.useState(false);
  const targetedRef = reactExports.useRef(false);
  const onTargeted = reactExports.useCallback(
    (doTarget) => {
      const isTargeted = dragManager.isTargeted(target);
      if (doTarget && !isTargeted) {
        dragManager.handleTargetChanged(target);
      } else if (!doTarget && isTargeted) {
        dragManager.handleTargetChanged(void 0);
      }
      setTargeted(doTarget);
    },
    [dragManager, target]
  );
  const ref = reactExports.useRef(null);
  reactExports.useEffect(() => {
    return dragManager.onDrag.addListener((_item, info) => {
      const targetedElement = document.elementFromPoint(
        info.pointerPosition.x,
        info.pointerPosition.y
      );
      const newTargeted = targetedElement === ref.current;
      newTargeted !== targetedRef.current && onTargeted(newTargeted);
      targetedRef.current = newTargeted;
    });
  }, [onTargeted, dragManager]);
  reactExports.useEffect(() => {
    return dragManager.onDragEnd.addListener(() => {
      targetedRef.current && onTargeted(false);
      targetedRef.current = false;
    });
  }, [onTargeted, dragManager]);
  return [ref, targeted];
}
function usePanelTarget(args) {
  const { side, newWidgetId } = args;
  const target = reactExports.useMemo(
    () => ({
      type: "panel",
      side,
      newWidgetId
    }),
    [side, newWidgetId]
  );
  return useTarget(target);
}
function defaultIsDragItem(item, dragged) {
  return dragged.type === item.type && dragged.id === item.id;
}
function useDragItem(args) {
  const dragManager = reactExports.useContext(DragManagerContext);
  const { item, isDragItem, onDragStart, onDrag, onDragEnd } = args;
  const handleDragStart = reactExports.useCallback(
    (info) => {
      item && dragManager.handleDragStart({
        item,
        info
      });
    },
    [dragManager, item]
  );
  reactExports.useEffect(() => {
    return dragManager.onDragStart.addListener((draggedItem, info, target) => {
      const handleEvent = isDragItem ? isDragItem(item, draggedItem) : defaultIsDragItem(item, draggedItem);
      if (!handleEvent)
        return;
      onDragStart && onDragStart(draggedItem, info, target);
    });
  }, [dragManager, onDragStart, item, isDragItem]);
  reactExports.useEffect(() => {
    return dragManager.onDrag.addListener((draggedItem, info, target) => {
      const handleEvent = isDragItem ? isDragItem(item, draggedItem) : defaultIsDragItem(item, draggedItem);
      if (!handleEvent)
        return;
      onDrag && onDrag(draggedItem, info, target);
    });
  }, [dragManager, onDrag, item, isDragItem]);
  reactExports.useEffect(() => {
    return dragManager.onDragEnd.addListener((draggedItem, info, target) => {
      const handleEvent = isDragItem ? isDragItem(item, draggedItem) : defaultIsDragItem(item, draggedItem);
      if (!handleEvent)
        return;
      onDragEnd && onDragEnd(draggedItem, info, target);
    });
  }, [dragManager, onDragEnd, item, isDragItem]);
  return handleDragStart;
}
function useDraggedItem() {
  var _a2;
  const dragManager = reactExports.useContext(DragManagerContext);
  const [draggedItem, setDraggedItem] = reactExports.useState(
    (_a2 = dragManager.draggedItem) == null ? void 0 : _a2.item
  );
  reactExports.useEffect(() => {
    return dragManager.onDragStart.addListener((item) => {
      setDraggedItem(item);
    });
  }, [dragManager]);
  reactExports.useEffect(() => {
    return dragManager.onDragUpdate.addListener((item) => {
      setDraggedItem(item);
    });
  }, [dragManager]);
  reactExports.useEffect(() => {
    return dragManager.onDragEnd.addListener(() => {
      setDraggedItem(void 0);
    });
  }, [dragManager]);
  return draggedItem;
}
function useIsDraggedItem(item) {
  const draggedItem = useDraggedItem();
  return !!draggedItem && draggedItem.id === item.id && draggedItem.type === item.type;
}
function useDraggedItemId(type) {
  const draggedItem = useDraggedItem();
  if (draggedItem && draggedItem.type === type) {
    return draggedItem.id;
  }
  return void 0;
}
function DragProvider(props) {
  const dragManager = reactExports.useRef(new DragManager());
  reactExports.useEffect(() => {
    const mouseMove = (e) => {
      dragManager.current.handleDrag(e.clientX, e.clientY);
    };
    document.addEventListener("mousemove", mouseMove);
    return () => {
      document.removeEventListener("mousemove", mouseMove);
    };
  }, []);
  reactExports.useEffect(() => {
    const mouseUp = () => {
      dragManager.current.handleDragEnd();
    };
    document.addEventListener("mouseup", mouseUp);
    return () => {
      document.removeEventListener("mouseup", mouseUp);
    };
  }, []);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(DragManagerContext.Provider, { value: dragManager.current, children: /* @__PURE__ */ jsxRuntimeExports.jsx(DraggedWidgetIdProvider, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(DraggedPanelSideProvider, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(DraggedResizeHandleProvider, { children: props.children }) }) }) });
}
function DraggedWidgetIdProvider(props) {
  const draggedWidgetId = useDraggedItemId("widget");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(DraggedWidgetIdContext.Provider, { value: draggedWidgetId, children: props.children });
}
function DraggedPanelSideProvider(props) {
  const draggedSide = useDraggedItemId("panelGrip");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(DraggedPanelSideContext.Provider, { value: draggedSide, children: props.children });
}
function DraggedResizeHandleProvider(props) {
  const draggedHandle = useDraggedItemId("resizeHandle");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(DraggedResizeHandleContext.Provider, { value: draggedHandle, children: props.children });
}
function isResizeHandleDragItem(item) {
  return item.type === "resizeHandle";
}
class DragManager {
  constructor() {
    this._onDragStartEmitter = new BeEvent();
    this._onDragUpdateEmitter = new BeEvent();
    this._onDragEmitter = new BeEvent();
    this._onDragEndEmitter = new BeEvent();
    this._onTargetChangedEmitter = new BeEvent();
  }
  get draggedItem() {
    return this._dragged;
  }
  isTargeted(target) {
    if (!this._dragged)
      return false;
    if (!this._dragged.target)
      return false;
    return lodashExports.isEqual(this._dragged.target, target);
  }
  get onDragStart() {
    return this._onDragStartEmitter;
  }
  get onDragUpdate() {
    return this._onDragUpdateEmitter;
  }
  get onDrag() {
    return this._onDragEmitter;
  }
  get onDragEnd() {
    return this._onDragEndEmitter;
  }
  get onTargetChanged() {
    return this._onTargetChangedEmitter;
  }
  handleDragStart({ item, info }) {
    this._dragged = {
      item,
      info,
      target: void 0
    };
    this._onDragStartEmitter.raiseEvent(
      this._dragged.item,
      this._dragged.info,
      this._dragged.target
    );
  }
  handleDragUpdate(item) {
    if (!this._dragged)
      return;
    this._dragged.item = item;
    this._onDragUpdateEmitter.raiseEvent(
      this._dragged.item,
      this._dragged.info,
      this._dragged.target
    );
  }
  handleDrag(x, y) {
    if (!this._dragged)
      return;
    this._dragged.info.lastPointerPosition = this._dragged.info.pointerPosition;
    this._dragged.info.pointerPosition = new Point(x, y);
    this._onDragEmitter.raiseEvent(
      this._dragged.item,
      this._dragged.info,
      this._dragged.target
    );
  }
  handleDragEnd() {
    if (!this._dragged)
      return;
    const item = this._dragged.item;
    const info = this._dragged.info;
    const target = this._dragged.target;
    this._dragged = void 0;
    this._onDragEndEmitter.raiseEvent(item, info, target);
    target && this._onTargetChangedEmitter.raiseEvent(void 0);
  }
  handleTargetChanged(target) {
    if (!this._dragged)
      return;
    this._dragged.target = target;
    this._onTargetChangedEmitter.raiseEvent(target);
  }
}
const DragManagerContext = reactExports.createContext(null);
DragManagerContext.displayName = "nz:DragManagerContext";
const DraggedWidgetIdContext = reactExports.createContext(void 0);
DraggedWidgetIdContext.displayName = "nz:DraggedWidgetIdContext";
const DraggedPanelSideContext = reactExports.createContext(void 0);
DraggedPanelSideContext.displayName = "nz:DraggedPanelSideContext";
const DraggedResizeHandleContext = reactExports.createContext(void 0);
DraggedResizeHandleContext.displayName = "nz:DraggedResizeHandleContext";
function useTargeted() {
  const dragManager = reactExports.useContext(DragManagerContext);
  const [targeted, setTargeted] = reactExports.useState();
  reactExports.useEffect(() => {
    return dragManager.onTargetChanged.addListener((t) => {
      setTargeted(t);
    });
  }, [dragManager]);
  return targeted;
}
try {
  nz:
    DragManagerContext.displayName = "nz:DragManagerContext";
  nz:
    DragManagerContext.__docgenInfo = { "description": "", "displayName": "nz:DragManagerContext", "props": { "tabId": { "defaultValue": null, "description": "", "name": "tabId", "required": true, "type": { "name": "string" } }, "onDrag": { "defaultValue": null, "description": "", "name": "onDrag", "required": false, "type": { "name": "((dragBy: XAndY) => void)" } }, "onDragEnd": { "defaultValue": null, "description": "", "name": "onDragEnd", "required": false, "type": { "name": "((target: TabDragDropTargetState) => void)" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function isFloatingWidgetLocation(location) {
  return "floatingWidgetId" in location;
}
function isPopoutWidgetLocation(location) {
  return "popoutWidgetId" in location;
}
function isPanelWidgetLocation(location) {
  return "side" in location;
}
function getWidgetLocation(state, id) {
  if (id in state.floatingWidgets.byId) {
    return {
      floatingWidgetId: id
    };
  }
  if (state.popoutWidgets) {
    if (id in state.popoutWidgets.byId) {
      return {
        popoutWidgetId: id
      };
    }
  }
  for (const side of panelSides) {
    const panel = state.panels[side];
    const index = panel.widgets.indexOf(id);
    if (index >= 0) {
      return {
        side,
        index
      };
    }
  }
  return void 0;
}
function isFloatingTabLocation(location) {
  return "floatingWidgetId" in location;
}
function isPopoutTabLocation(location) {
  return "popoutWidgetId" in location;
}
function isPanelTabLocation(location) {
  return "side" in location;
}
function getTabLocation(state, id) {
  let widgetId;
  for (const [, widget2] of Object.entries(state.widgets)) {
    const index = widget2.tabs.indexOf(id);
    if (index >= 0) {
      widgetId = widget2.id;
      break;
    }
  }
  if (!widgetId)
    return void 0;
  const location = getWidgetLocation(state, widgetId);
  if (!location)
    return void 0;
  if (isFloatingWidgetLocation(location))
    return {
      floatingWidgetId: widgetId,
      widgetId
    };
  if (isPopoutWidgetLocation(location))
    return {
      popoutWidgetId: widgetId,
      widgetId
    };
  return {
    side: location.side,
    widgetId
  };
}
function isFloatingWidgetRestoreState(state) {
  return "floatingWidget" in state;
}
function isPanelWidgetRestoreState(state) {
  return "side" in state;
}
function createTabState(id, args) {
  return {
    label: "",
    ...args,
    id,
    unloaded: false
  };
}
function createDraggedTabState(tabId, args) {
  return {
    home: {
      side: "left",
      widgetId: "",
      widgetIndex: 0
    },
    position: { x: 0, y: 0 },
    ...args,
    tabId
  };
}
function updateTabState(state, id, update) {
  if (!(id in state.tabs))
    throw new UiError(category, "Tab does not exist");
  return fn(state, (draft) => {
    const tab2 = draft.tabs[id];
    update(tab2);
  });
}
function updateSavedTabState(state, id, update) {
  return fn(state, (draft) => {
    const allIds = draft.savedTabs.allIds;
    const byId = draft.savedTabs.byId;
    let tab2 = byId[id];
    if (!tab2) {
      allIds.push(id);
      tab2 = byId[id] = { id };
    } else {
      const index = allIds.indexOf(id);
      allIds.splice(index, 1);
      allIds.push(id);
    }
    update(tab2);
  });
}
function addTab(state, id, tabArgs) {
  if (id in state.tabs)
    throw new UiError(category, "Tab already exists");
  const tab2 = {
    ...createTabState(id),
    ...tabArgs
  };
  return fn(state, (stateDraft) => {
    stateDraft.tabs[id] = K(tab2);
  });
}
function addTabToWidget(state, tabId, widgetId) {
  return insertTabToWidget(state, tabId, widgetId, Infinity);
}
function insertTabToWidget(state, tabId, widgetId, tabIndex) {
  if (!(tabId in state.tabs))
    throw new UiError(category, "Tab does not exist", void 0, () => ({
      tabId
    }));
  assertWidgetState(state, widgetId);
  const location = getTabLocation(state, tabId);
  if (location)
    throw new UiError(
      category,
      "Tab is already in a widget",
      void 0,
      () => ({ tabId, widgetId: location.widgetId })
    );
  return fn(state, (draft) => {
    const widget2 = getWidgetState$1(draft, widgetId);
    widget2.tabs.splice(tabIndex, 0, tabId);
  });
}
function removeTabFromWidget(state, tabId) {
  const location = getTabLocation(state, tabId);
  if (!location)
    return state;
  const widgetId = location.widgetId;
  const widget2 = getWidgetState$1(state, widgetId);
  const tabs = [...widget2.tabs];
  const tabIndex = tabs.indexOf(tabId);
  tabs.splice(tabIndex, 1);
  if (tabs.length === 0) {
    return removeWidget(state, widgetId);
  }
  if (tabId === widget2.activeTabId) {
    state = setWidgetActiveTabId(state, widget2.id, tabs[0]);
  }
  return updateWidgetState(state, widgetId, {
    tabs
  });
}
function removeTab(state, tabId) {
  if (!(tabId in state.tabs))
    throw new UiError(category, "Tab does not exist");
  state = removeTabFromWidget(state, tabId);
  return fn(state, (draft) => {
    var _a2;
    delete draft.tabs[tabId];
    if (((_a2 = draft.toolSettings) == null ? void 0 : _a2.tabId) === tabId) {
      draft.toolSettings = void 0;
    }
  });
}
const defaultHomeState = {
  side: "left",
  widgetId: "",
  widgetIndex: 0,
  tabIndex: 0
};
function addRemovedTab(state, tabId) {
  if (!(tabId in state.tabs))
    throw new UiError(category, "Tab does not exist", void 0, () => ({
      tabId
    }));
  const savedTab = state.savedTabs.byId[tabId];
  const home = (savedTab == null ? void 0 : savedTab.home) || defaultHomeState;
  const { tabIndex, widgetId } = home;
  if (widgetId in state.widgets) {
    return insertTabToWidget(state, tabId, widgetId, tabIndex);
  }
  if (isFloatingWidgetRestoreState(home)) {
    const nzBounds = Rectangle.createFromSize(state.size);
    const bounds = Rectangle.create(home.floatingWidget.bounds).containIn(
      nzBounds
    );
    return addFloatingWidget(state, widgetId, [tabId], {
      ...home.floatingWidget,
      bounds: bounds.toProps()
    });
  }
  const panel = state.panels[home.side];
  if (panel.widgets.length >= panel.maxWidgetCount) {
    const sectionIndex = Math.min(panel.maxWidgetCount - 1, home.widgetIndex);
    const sectionId = panel.widgets[sectionIndex];
    return insertTabToWidget(state, tabId, sectionId, home.tabIndex);
  }
  const newSectionId = getUniqueId();
  return insertPanelWidget(
    state,
    panel.side,
    newSectionId,
    [tabId],
    home.widgetIndex
  );
}
function createWidgetState(id, tabs, args) {
  if (tabs.length === 0)
    throw new UiError(category, "Widget must contain tabs");
  return {
    activeTabId: tabs[0],
    minimized: false,
    ...args,
    id,
    tabs
  };
}
function updateWidgetState(state, id, args) {
  return fn(state, (draft) => {
    const widget2 = getWidgetState$1(draft, id);
    draft.widgets[id] = {
      ...widget2,
      ...K(args)
    };
  });
}
function addWidgetState(state, id, tabs, args) {
  if (id in state.widgets)
    throw new UiError(category, "Widget already exists");
  const widget2 = createWidgetState(id, tabs, args);
  for (const tabId of widget2.tabs) {
    if (!(tabId in state.tabs))
      throw new UiError(category, "Tab does not exist", void 0, () => ({
        tabId
      }));
    const location = getTabLocation(state, tabId);
    if (location)
      throw new UiError(
        category,
        "Tab is already in a widget",
        void 0,
        () => ({ tabId, widgetId: location.widgetId })
      );
  }
  return fn(state, (draft) => {
    draft.widgets[id] = K(widget2);
  });
}
function removeWidget(state, id) {
  const location = getWidgetLocation(state, id);
  if (!location)
    throw new UiError(category, "Widget not found");
  if (isFloatingWidgetLocation(location))
    return removeFloatingWidget(state, id);
  if (isPopoutWidgetLocation(location))
    return removePopoutWidget(state, id);
  return removePanelWidget(state, id, location);
}
function removeWidgetState(state, id) {
  assertWidgetState(state, id);
  return fn(state, (draft) => {
    delete draft.widgets[id];
  });
}
function createFloatingWidgetState(id, args) {
  const bounds = toRectangleProps(args == null ? void 0 : args.bounds);
  return {
    home: {
      side: "left",
      widgetId: "",
      widgetIndex: 0
    },
    ...args,
    bounds,
    id
  };
}
function createPopoutWidgetState(id, args) {
  const bounds = toRectangleProps(args == null ? void 0 : args.bounds);
  const home = (args == null ? void 0 : args.home) ? args.home : {
    side: "left",
    widgetId: "",
    widgetIndex: 0
  };
  return {
    ...args,
    home,
    bounds,
    id
  };
}
function updateFloatingWidgetState(state, id, args) {
  if (!(id in state.floatingWidgets.byId))
    throw new UiError(category, "Floating widget does not exist");
  return fn(state, (draft) => {
    const floatingWidget2 = draft.floatingWidgets.byId[id];
    const { bounds, ...other } = args;
    draft.floatingWidgets.byId[id] = {
      ...floatingWidget2,
      ...other
    };
    if (bounds)
      setRectangleProps(floatingWidget2.bounds, bounds);
  });
}
function removeFloatingWidget(state, id) {
  if (!(id in state.floatingWidgets.byId))
    throw new UiError(category, "Floating widget does not exist");
  state = fn(state, (draft) => {
    delete draft.floatingWidgets.byId[id];
    const idIndex = draft.floatingWidgets.allIds.indexOf(id);
    draft.floatingWidgets.allIds.splice(idIndex, 1);
  });
  return removeWidgetState(state, id);
}
function removePopoutWidget(state, id) {
  if (!(id in state.popoutWidgets.byId))
    throw new UiError(category, "Popout widget does not exist");
  state = fn(state, (draft) => {
    delete draft.popoutWidgets.byId[id];
    const index = state.popoutWidgets.allIds.indexOf(id);
    draft.popoutWidgets.allIds.splice(index, 1);
  });
  return removeWidgetState(state, id);
}
function removePanelWidget(state, id, location) {
  location = location || findPanelWidget(state, id);
  if (!location)
    throw new UiError(category, "Panel widget not found");
  state = updatePanelState(state, location.side, (draft) => {
    draft.widgets.splice(location.index, 1);
  });
  const widgets = state.panels[location.side].widgets;
  const expandedWidget = widgets.find((widgetId) => {
    const widget2 = getWidgetState$1(state, widgetId);
    return !widget2.minimized;
  });
  if (!expandedWidget && widgets.length > 0) {
    const firstWidgetId = widgets[0];
    state = updateWidgetState(state, firstWidgetId, {
      minimized: false
    });
  }
  return removeWidgetState(state, id);
}
function findPanelWidget(state, id) {
  const location = getWidgetLocation(state, id);
  if (location && isPanelWidgetLocation(location))
    return location;
  return void 0;
}
function assertWidgetState(state, id) {
  if (!(id in state.widgets))
    throw new UiError(category, "Widget does not exist", void 0, () => ({
      id
    }));
}
function getWidgetState$1(state, id) {
  assertWidgetState(state, id);
  return state.widgets[id];
}
function setWidgetActiveTabId(state, widgetId, tabId) {
  const widget2 = getWidgetState$1(state, widgetId);
  if (!widget2.tabs.includes(tabId))
    throw new UiError(category, "Tab is not in a widget");
  state = updateWidgetState(state, widgetId, {
    activeTabId: tabId
  });
  const floatingWidget2 = state.floatingWidgets.byId[widgetId];
  if (floatingWidget2) {
    const activeTab = state.tabs[tabId];
    const preferredFloatingWidgetSize = Rectangle.create(
      floatingWidget2.bounds
    ).getSize();
    state = updateTabState(state, activeTab.id, (draft) => {
      initSizeProps(
        draft,
        "preferredFloatingWidgetSize",
        preferredFloatingWidgetSize
      );
    });
  }
  return state;
}
function getNewFloatingWidgetBounds(state) {
  const size = { height: 120, width: 200 };
  const initialPosition = new Point(360, 340);
  const nzBounds = Rectangle.createFromSize(state.size);
  const widgetsBounds = nzBounds.inset(20, 20, 20, 20);
  const offset = new Point(40, 40);
  let bounds = Rectangle.createFromSize(size);
  if (state.floatingWidgets.allIds.length === 0) {
    bounds = bounds.offset(initialPosition);
  } else {
    const widgetId = state.floatingWidgets.allIds[state.floatingWidgets.allIds.length - 1];
    const widget2 = state.floatingWidgets.byId[widgetId];
    const widgetBounds = Rectangle.create(widget2.bounds);
    const topLeft = widgetBounds.topLeft().offset(offset);
    bounds = bounds.offset(topLeft);
    const widgetBottomRight = new Point(
      widgetBounds.right,
      widgetBounds.bottom
    );
    const minBottomRight = widgetBottomRight.offset(offset);
    const x = Math.max(0, minBottomRight.x - bounds.right);
    const y = Math.max(0, minBottomRight.y - bounds.bottom);
    bounds = bounds.offset({ x, y });
  }
  if (bounds.bottom >= widgetsBounds.bottom) {
    bounds = bounds.setPosition({ x: bounds.left, y: widgetsBounds.top });
  }
  if (bounds.right >= widgetsBounds.right) {
    bounds = bounds.setPosition({
      x: widgetsBounds.left,
      y: widgetsBounds.top
    });
  }
  bounds = bounds.containIn(widgetsBounds);
  return bounds.toProps();
}
function addFloatingWidget(state, id, tabs, floatingWidgetArgs, widgetArgs) {
  if (id in state.floatingWidgets.byId)
    throw new UiError(category, "Floating widget already exists");
  state = addWidgetState(state, id, tabs, widgetArgs);
  let bounds = floatingWidgetArgs == null ? void 0 : floatingWidgetArgs.bounds;
  if (!bounds) {
    bounds = getNewFloatingWidgetBounds(state);
  }
  const tabId = tabs[0];
  const tab2 = state.tabs[tabId];
  const resizable = tab2.isFloatingWidgetResizable;
  const floatingWidget2 = createFloatingWidgetState(id, {
    bounds,
    resizable,
    ...floatingWidgetArgs
  });
  return fn(state, (stateDraft) => {
    stateDraft.floatingWidgets.byId[id] = floatingWidget2;
    stateDraft.floatingWidgets.allIds.push(id);
  });
}
function addPopoutWidget(state, id, tabs, popoutWidgetArgs, widgetArgs) {
  if (tabs.length !== 1)
    throw new UiError(
      category,
      "Popout widget should contain one tab only",
      void 0,
      () => ({ tabs })
    );
  const popoutWidget = createPopoutWidgetState(id, popoutWidgetArgs);
  state = addWidgetState(state, id, tabs, widgetArgs);
  return fn(state, (stateDraft) => {
    stateDraft.popoutWidgets.byId[id] = popoutWidget;
    stateDraft.popoutWidgets.allIds.push(id);
  });
}
function floatingWidgetBringToFront(state, floatingWidgetId) {
  return fn(state, (draft) => {
    const idIndex = draft.floatingWidgets.allIds.indexOf(floatingWidgetId);
    const spliced = draft.floatingWidgets.allIds.splice(idIndex, 1);
    draft.floatingWidgets.allIds.push(spliced[0]);
  });
}
const category = "appui-react:layout";
function toRectangleProps(rectangle) {
  if (rectangle) {
    return {
      bottom: rectangle.bottom,
      left: rectangle.left,
      right: rectangle.right,
      top: rectangle.top
    };
  }
  return new Rectangle().toProps();
}
function setRectangleProps(props, bounds) {
  props.left = bounds.left;
  props.right = bounds.right;
  props.top = bounds.top;
  props.bottom = bounds.bottom;
}
function setPointProps(props, point) {
  props.x = point.x;
  props.y = point.y;
}
function setSizeProps(props, size) {
  props.height = size.height;
  props.width = size.width;
}
function initSizeProps(obj, key, size) {
  if (obj[key] && size) {
    setSizeProps(obj[key], size);
    return;
  }
  obj[key] = size ? {
    height: size.height,
    width: size.width
  } : void 0;
}
function initRectangleProps(obj, key, rectangle) {
  const rect = rectangle;
  if (obj[key] && rect) {
    setRectangleProps(obj[key], rect);
    return;
  }
  obj[key] = rect ? {
    bottom: rect.bottom,
    left: rect.left,
    right: rect.right,
    top: rect.top
  } : void 0;
}
function isToolSettingsFloatingWidget(state, id) {
  var _a2;
  const widget2 = getWidgetState$1(state, id);
  const toolSettingsTabId = (_a2 = state.toolSettings) == null ? void 0 : _a2.tabId;
  return widget2.tabs.length === 1 && widget2.tabs[0] === toolSettingsTabId && id in state.floatingWidgets.byId;
}
function updateHomeOfToolSettingsWidget(state, id, home) {
  if (!isToolSettingsFloatingWidget(state, id))
    return state;
  return updateFloatingWidgetState(state, id, {
    home
  });
}
function createPanelState(side) {
  return {
    collapseOffset: 100,
    collapsed: false,
    maxSize: 600,
    minSize: 200,
    pinned: true,
    resizable: true,
    side,
    size: void 0,
    widgets: [],
    maxWidgetCount: 2,
    splitterPercent: 50
  };
}
function createVerticalPanelState(side, args) {
  return {
    ...createPanelState(side),
    ...args,
    side
  };
}
function createHorizontalPanelState(side, args) {
  return {
    ...createPanelState(side),
    minSize: 100,
    span: true,
    ...args,
    side
  };
}
function createPanelsState(args) {
  return {
    bottom: createHorizontalPanelState("bottom"),
    left: createVerticalPanelState("left"),
    right: createVerticalPanelState("right"),
    top: createHorizontalPanelState("top"),
    ...args
  };
}
function updatePanelState(state, side, update) {
  return fn(state, (draft) => {
    const panel = draft.panels[side];
    update(panel);
  });
}
function getPanelPixelSizeFromSpec(side, appSize, panelSize) {
  if (typeof panelSize === "number") {
    return panelSize;
  }
  const fullSize = isHorizontalPanelSide(side) ? appSize.height : appSize.width;
  return panelSize.percentage / 100 * fullSize;
}
function getPanelSize$1(preferredSizeSpec, side, minSizeSpec, maxSizeSpec, appSize) {
  if (preferredSizeSpec === void 0)
    return void 0;
  const maxSize = getPanelPixelSizeFromSpec(side, appSize, maxSizeSpec);
  const minSize = getPanelPixelSizeFromSpec(side, appSize, minSizeSpec);
  const preferredSize = getPanelPixelSizeFromSpec(
    side,
    appSize,
    preferredSizeSpec
  );
  return Math.min(Math.max(preferredSize, minSize), maxSize);
}
function addPanelWidget(state, side, id, tabs, widgetArgs) {
  return insertPanelWidget(state, side, id, tabs, Infinity, widgetArgs);
}
function insertPanelWidget(state, side, id, tabs, sectionIndex, widgetArgs) {
  const panel = state.panels[side];
  const maxWidgetCount = panel.maxWidgetCount;
  if (panel.widgets.length >= maxWidgetCount)
    throw new UiError(category, "Max widget count exceeded", void 0, () => ({
      maxWidgetCount
    }));
  state = addWidgetState(state, id, tabs, widgetArgs);
  return fn(state, (draft) => {
    const widgets = draft.panels[side].widgets;
    widgets.splice(sectionIndex, 0, id);
  });
}
function createNineZoneState(args) {
  return {
    draggedTab: void 0,
    floatingWidgets: {
      byId: {},
      allIds: []
    },
    popoutWidgets: {
      byId: {},
      allIds: []
    },
    panels: createPanelsState(),
    widgets: {},
    tabs: {},
    savedTabs: {
      allIds: [],
      byId: {}
    },
    toolSettings: void 0,
    size: {
      height: 0,
      width: 0
    },
    ...args
  };
}
const LayoutStoreContext = reactExports.createContext(
  void 0
);
LayoutStoreContext.displayName = "appui:LayoutStoreContext";
function createLayoutStore(args) {
  return createStore(() => createNineZoneState(args));
}
function useLayoutStore$1() {
  const store = reactExports.useContext(LayoutStoreContext);
  return store;
}
function useLayout(selector, multipleSlices = false) {
  const store = useLayoutStore$1();
  return useStoreWithEqualityFn(
    store,
    selector,
    multipleSlices ? shallow$1 : void 0
  );
}
try {
  appui:
    LayoutStoreContext.displayName = "appui:LayoutStoreContext";
  appui:
    LayoutStoreContext.__docgenInfo = { "description": "", "displayName": "appui:LayoutStoreContext", "props": { "draggedTab": { "defaultValue": null, "description": "", "name": "draggedTab", "required": false, "type": { "name": "DraggedTabState" } }, "floatingWidgets": { "defaultValue": null, "description": "", "name": "floatingWidgets", "required": false, "type": { "name": "FloatingWidgetsState" } }, "popoutWidgets": { "defaultValue": null, "description": "", "name": "popoutWidgets", "required": false, "type": { "name": "PopoutWidgetsState" } }, "panels": { "defaultValue": null, "description": "", "name": "panels", "required": false, "type": { "name": "PanelsState" } }, "tabs": { "defaultValue": null, "description": "", "name": "tabs", "required": false, "type": { "name": "TabsState" } }, "savedTabs": { "defaultValue": null, "description": "", "name": "savedTabs", "required": false, "type": { "name": "SavedTabsState" } }, "toolSettings": { "defaultValue": null, "description": "", "name": "toolSettings", "required": false, "type": { "name": "ToolSettingsState" } }, "widgets": { "defaultValue": null, "description": "", "name": "widgets", "required": false, "type": { "name": "WidgetsState" } }, "size": { "defaultValue": null, "description": "", "name": "size", "required": false, "type": { "name": "SizeProps" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function isHorizontalPanelState(state) {
  return isHorizontalPanelSide(state.side);
}
function getWidgetPanelSectionId(side, panelSectionIndex) {
  return 0 === panelSectionIndex ? `${side}Start` : `${side}End`;
}
function WidgetProvider(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetIdContext.Provider, { value: props.id, children: props.children });
}
const Widget = reactExports.forwardRef(
  function Widget2(props, forwardedRef) {
    const dispatch = reactExports.useContext(NineZoneDispatchContext);
    const side = reactExports.useContext(PanelSideContext);
    const id = reactExports.useContext(WidgetIdContext);
    const measureNz = reactExports.useContext(MeasureContext);
    const floatingWidgetId = useFloatingWidgetId();
    const { preferredFloatingWidgetSize, userSized } = useLayout((state) => {
      const widget2 = getWidgetState$1(state, id);
      const tab2 = state.tabs[widget2.activeTabId];
      return {
        preferredFloatingWidgetSize: tab2.preferredFloatingWidgetSize,
        userSized: tab2.userSized || tab2.isFloatingWidgetResizable && !!tab2.preferredFloatingWidgetSize
      };
    }, true);
    const elementRef = reactExports.useRef(null);
    const widgetId = floatingWidgetId === void 0 ? id : floatingWidgetId;
    const onDragStart = reactExports.useCallback(
      (updateId, initialPointerPosition, pointerPosition) => {
        assert(!!elementRef.current);
        if (floatingWidgetId !== void 0)
          return;
        const nzBounds = measureNz();
        let bounds = Rectangle.create(
          elementRef.current.getBoundingClientRect()
        );
        const size = restrainInitialWidgetSize(
          bounds.getSize(),
          nzBounds.getSize()
        );
        bounds = bounds.setSize(size);
        if (preferredFloatingWidgetSize) {
          bounds = bounds.setSize(preferredFloatingWidgetSize);
        }
        if (initialPointerPosition.x > bounds.right) {
          const offset = initialPointerPosition.x - bounds.right + 20;
          bounds = bounds.offsetX(offset);
        }
        const dragOffset = initialPointerPosition.getOffsetTo(pointerPosition);
        bounds = bounds.offset(dragOffset);
        bounds = bounds.offset({ x: -nzBounds.left, y: -nzBounds.top });
        const newFloatingWidgetId = getUniqueId();
        updateId(newFloatingWidgetId);
        dispatch({
          type: "PANEL_WIDGET_DRAG_START",
          newFloatingWidgetId,
          id,
          bounds,
          side,
          userSized
        });
      },
      [
        dispatch,
        floatingWidgetId,
        id,
        side,
        measureNz,
        preferredFloatingWidgetSize,
        userSized
      ]
    );
    useDragWidget({
      widgetId,
      onDragStart
    });
    reactExports.useEffect(() => {
      const listener = () => {
        floatingWidgetId && dispatch({
          type: "FLOATING_WIDGET_BRING_TO_FRONT",
          id: floatingWidgetId
        });
      };
      const element = elementRef.current;
      element == null ? void 0 : element.addEventListener("click", listener);
      return () => {
        element == null ? void 0 : element.removeEventListener("click", listener);
      };
    }, [dispatch, floatingWidgetId]);
    const measure = reactExports.useCallback(() => {
      if (!elementRef.current)
        return new Rectangle();
      const bounds = elementRef.current.getBoundingClientRect();
      return Rectangle.create(bounds);
    }, []);
    const widgetContextValue = reactExports.useMemo(
      () => ({
        measure
      }),
      [measure]
    );
    const ref = useRefs(forwardedRef, elementRef);
    const className = classnames("nz-widget-widget", props.className);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetContext.Provider, { value: widgetContextValue, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className,
        onMouseEnter: props.onMouseEnter,
        onMouseLeave: props.onMouseLeave,
        onTransitionEnd: props.onTransitionEnd,
        ref,
        style: props.style,
        "data-widget-id": props.widgetId,
        children: props.children
      }
    ) });
  }
);
const WidgetIdContext = reactExports.createContext(void 0);
WidgetIdContext.displayName = "nz:WidgetIdContext";
const WidgetContext = reactExports.createContext(null);
WidgetContext.displayName = "nz:WidgetContext";
const minWidth = 200;
const minHeight = 200;
function restrainInitialWidgetSize(size, nzSize) {
  const width = Math.max(Math.min(nzSize.width / 3, size.width), minWidth);
  const height = Math.max(Math.min(nzSize.height / 3, size.height), minHeight);
  return {
    width,
    height
  };
}
function useActiveTabId() {
  const id = reactExports.useContext(WidgetIdContext);
  return useLayout((state) => {
    const widget2 = getWidgetState$1(state, id);
    return widget2.activeTabId;
  });
}
try {
  nz:
    WidgetIdContext.displayName = "nz:WidgetIdContext";
  nz:
    WidgetIdContext.__docgenInfo = { "description": "", "displayName": "nz:WidgetIdContext", "props": { "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function useIsToolSettingsTab() {
  const activeTabId = useActiveTabId();
  const toolSettingsTabId = useLayout((state) => {
    var _a2;
    return (_a2 = state.toolSettings) == null ? void 0 : _a2.tabId;
  });
  return activeTabId === toolSettingsTabId;
}
const TabBarButton = reactExports.forwardRef(function TabBarButton2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { ref, styleType: "borderless", size: "small", ...props });
});
try {
  TabBarButton.displayName = "TabBarButton";
  TabBarButton.__docgenInfo = { "description": "", "displayName": "TabBarButton", "props": { "isActive": { "defaultValue": { value: "false" }, "description": "Button gets active style.", "name": "isActive", "required": false, "type": { "name": "boolean" } }, "label": { "defaultValue": null, "description": "Name of the button, shown in a tooltip and exposed to assistive technologies.", "name": "label", "required": false, "type": { "name": "ReactNode" } }, "labelProps": { "defaultValue": null, "description": "Props passed to the Tooltip that contains the `label`.\nCan be used for customizing the tooltip's `placement`, etc.", "name": "labelProps", "required": false, "type": { "name": 'Omit<Omit<Omit<Omit<DetailedHTMLProps<HTMLAttributes<HTMLDivElement>, HTMLDivElement>, "ref"> & { ...; }, "content" | ... 3 more ... | keyof TooltipOptions> & { ...; } & PortalProps & TooltipOptions & { ...; }, "ref">, "content" | ... 2 more ... | "ariaStrategy">' } }, "iconProps": { "defaultValue": null, "description": "Passes props to IconButton icon.", "name": "iconProps", "required": false, "type": { "name": "DetailedHTMLProps<HTMLAttributes<HTMLSpanElement>, HTMLSpanElement>" } }, "size": { "defaultValue": null, "description": "Modify size of the button.", "name": "size", "required": false, "type": { "name": "enum", "value": [{ "value": '"small"' }, { "value": '"large"' }] } }, "htmlDisabled": { "defaultValue": null, "description": "Built-in html `disabled` attribute", "name": "htmlDisabled", "required": false, "type": { "name": "boolean" } }, "styleType": { "defaultValue": { value: "'default'" }, "description": "Style of the button.\nUse 'borderless' to hide outline.", "name": "styleType", "required": false, "type": { "name": "enum", "value": [{ "value": '"default"' }, { "value": '"cta"' }, { "value": '"high-visibility"' }, { "value": '"borderless"' }] } }, "as": { "defaultValue": null, "description": "", "name": "as", "required": true, "type": { "name": '"symbol" | "object" | "map" | "filter" | "header" | "form" | "pattern" | "slot" | "style" | "title" | "button" | "image" | "text" | "time" | "article" | "dialog" | "figure" | "img" | ... 158 more ... | FunctionComponent<...>' } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const knownFeaturesObject = {
  activateDroppedTab: void 0,
  contentAlwaysMaxSize: void 0,
  enableMaximizedFloatingWidget: void 0,
  enableMaximizedPanelWidget: void 0,
  horizontalPanelAlignment: void 0,
  widgetActionDropdown: void 0,
  newToolbars: void 0
};
function trimToKnownFeaturesOnly(previewFeatures) {
  const knownFeatureKeys = Object.keys(knownFeaturesObject);
  const [knownFeatures, unknownFeatures] = Object.entries(
    previewFeatures
  ).reduce(
    ([known, unknown], [key, value]) => {
      if (knownFeatureKeys.includes(key)) {
        known[key] = value;
      } else {
        unknown.push(key);
      }
      return [known, unknown];
    },
    [{}, []]
  );
  if (Object.keys(unknownFeatures).length > 0) {
    Logger.logWarning(
      UiFramework.loggerCategory(trimToKnownFeaturesOnly),
      `Features used in "setPreviewFeatures" are unknown or no longer in preview`,
      {
        unknownFeatures
      }
    );
  }
  return knownFeatures;
}
const usePreviewFeaturesStore = create((set) => {
  return {
    previewFeatures: {},
    setPreviewFeatures: (newPreviewFeatures) => {
      const previewFeatures = trimToKnownFeaturesOnly(newPreviewFeatures);
      set({ previewFeatures });
    }
  };
});
function usePreviewFeatures() {
  return usePreviewFeaturesStore((state) => state.previewFeatures);
}
function PreviewFeaturesProvider({
  children,
  features
}) {
  const setPreviewFeatures = usePreviewFeaturesStore(
    (state) => state.setPreviewFeatures
  );
  reactExports.useEffect(() => {
    setPreviewFeatures(features ?? {});
  }, [features, setPreviewFeatures]);
  reactExports.useEffect(
    () => () => {
      setPreviewFeatures({});
    },
    [setPreviewFeatures]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(jsxRuntimeExports.Fragment, { children });
}
try {
  usePreviewFeatures.displayName = "usePreviewFeatures";
  usePreviewFeatures.__docgenInfo = { "description": "Hook to access the preview features set.", "displayName": "usePreviewFeatures", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  PreviewFeaturesProvider.displayName = "PreviewFeaturesProvider";
  PreviewFeaturesProvider.__docgenInfo = { "description": "Set which preview features are enabled. These features are not yet ready for production use nor have\na proper API defined yet.\nThe available set of features are defined in the [[PreviewFeatures]] interface.\n\nThis component should wrap the Provider component.\n\n```tsx\n<PreviewFeaturesProvider features={{ enableMaximizedFloatingWidget: true }}>\n <Provider store={UiFramework.store}>\n  [...]\n   <ConfigurableUIContent />\n  [/...]\n </Provider>\n</PreviewFeaturesProvider>\n```", "displayName": "PreviewFeaturesProvider", "props": { "features": { "defaultValue": null, "description": "", "name": "features", "required": false, "type": { "name": "PreviewFeatures" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function MoreButton(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    DropdownMenu,
    {
      placement: "bottom-end",
      menuItems: (onClose) => [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(WidgetActionDropdownContext.Provider, { value: { onClose }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(CloseOnPanelCollapse, {}),
          props.children
        ] }, 0)
      ],
      children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { height: "100%", display: "flex", marginInline: "0.25em" }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(TabBarButton, { label: "More actions", children: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgMoreVertical, {}) }) })
    }
  );
}
const WidgetActionDropdownContext = reactExports.createContext(void 0);
function CloseOnPanelCollapse() {
  const context = reactExports.useContext(WidgetActionDropdownContext);
  const side = reactExports.useContext(PanelSideContext);
  const { onClose } = context;
  const collapsed = useLayout((state) => {
    if (!side)
      return false;
    return state.panels[side].collapsed;
  });
  reactExports.useEffect(() => {
    if (collapsed)
      onClose();
  }, [collapsed, onClose]);
  return null;
}
const order = [
  "pin",
  "maximize",
  "popout",
  "horizontalAlign",
  "dock",
  "sendBack"
];
function useDropdownFeatures(features) {
  const { widgetActionDropdown } = usePreviewFeatures();
  const threshold = (widgetActionDropdown == null ? void 0 : widgetActionDropdown.threshold) ?? Infinity;
  const isDropdown = features.length > threshold;
  if (!isDropdown)
    return [features, false];
  const sorted = [...features].sort(
    (a, b) => order.indexOf(a) - order.indexOf(b)
  );
  return [sorted, true];
}
try {
  MoreButton.displayName = "MoreButton";
  MoreButton.__docgenInfo = { "description": "", "displayName": "MoreButton", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useDropdownFeatures.displayName = "useDropdownFeatures";
  useDropdownFeatures.__docgenInfo = { "description": "", "displayName": "useDropdownFeatures", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function ActionButton(props) {
  const dropdownContext = reactExports.useContext(WidgetActionDropdownContext);
  if (dropdownContext !== void 0) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(MenuItem$1, { icon: props.icon, onClick: props.onClick, ...props.menuProps, children: props.title });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    TabBarButton,
    {
      onClick: props.onClick,
      title: props.title,
      ...props.buttonProps,
      children: props.icon
    }
  );
}
try {
  ActionButton.displayName = "ActionButton";
  ActionButton.__docgenInfo = { "description": "", "displayName": "ActionButton", "props": { "title": { "defaultValue": null, "description": "", "name": "title", "required": false, "type": { "name": "string" } }, "icon": { "defaultValue": null, "description": "", "name": "icon", "required": true, "type": { "name": "Element" } }, "onClick": { "defaultValue": null, "description": "", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "menuProps": { "defaultValue": null, "description": "", "name": "menuProps", "required": false, "type": { "name": '(Omit<Omit<DetailedHTMLProps<HTMLAttributes<HTMLDivElement>, HTMLDivElement>, "ref"> & { ...; }, "value" | ... 13 more ... | "subMenuItems"> & { ...; } & Pick<...> & { ...; })' } }, "buttonProps": { "defaultValue": null, "description": "", "name": "buttonProps", "required": false, "type": { "name": '(Omit<Omit<DetailedHTMLProps<ButtonHTMLAttributes<HTMLButtonElement>, HTMLButtonElement>, "ref"> & { ...; }, "size" | ... 6 more ... | "iconProps"> & { ...; } & Omit<...> & { ...; })' } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const MaximizedWidgetContext = reactExports.createContext(
  {
    maximizedWidget: void 0,
    setMaximizedWidget: () => {
    }
  }
);
function MaximizedWidgetProvider({
  children
}) {
  const [maximizedWidget, setMaximizedWidget] = reactExports.useState(void 0);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    MaximizedWidgetContext.Provider,
    {
      value: { maximizedWidget, setMaximizedWidget },
      children
    }
  );
}
try {
  MaximizedWidgetProvider.displayName = "MaximizedWidgetProvider";
  MaximizedWidgetProvider.__docgenInfo = { "description": "Maximized widget feature provider.", "displayName": "MaximizedWidgetProvider", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MaximizedWidgetContext.displayName = "MaximizedWidgetContext";
  MaximizedWidgetContext.__docgenInfo = { "description": "Context containing configuration and state for maximized widget feature.", "displayName": "MaximizedWidgetContext", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function useIsMaximizedWidget() {
  const widgetId = reactExports.useContext(WidgetIdContext);
  const maximizedWidgetId = useMaximizedWidgetId();
  if (maximizedWidgetId === void 0)
    return false;
  return widgetId === maximizedWidgetId;
}
function useMaximizedPanelLayout(side) {
  const location = useMaximizedPanelWidgetLocation();
  return {
    "uifw-preview-enableMaximizedWidget_panel-layout": (location == null ? void 0 : location.side) === side
  };
}
function useMaximizedPanel(side) {
  const isMaximized = useIsMaximizedPanel(side);
  if (!isMaximized)
    return void 0;
  return {
    "uifw-preview-enableMaximizedWidget_panel": isMaximized
  };
}
function useMaximizedSection(widgetId) {
  const location = useMaximizedPanelWidgetLocation();
  if (!location)
    return void 0;
  return {
    "uifw-preview-enableMaximizedWidget_section": location.widgetId === widgetId,
    "uifw-preview-enableMaximizedWidget_section-sibling": location.widgetId !== widgetId
  };
}
function useIsMaximizedPanel(side) {
  const location = useMaximizedPanelWidgetLocation();
  return (location == null ? void 0 : location.side) === side;
}
function useMaximizedPanelWidgetLocation() {
  const location = useMaximizedWidgetLocation();
  if (!location)
    return void 0;
  if (!isPanelWidgetLocation(location))
    return void 0;
  return location;
}
function useMaximizedWidgetLocation() {
  const widgetId = useMaximizedWidgetId();
  const location = useLayout((state) => {
    if (!widgetId)
      return void 0;
    return getWidgetLocation(state, widgetId);
  }, true);
  if (!widgetId || !location)
    return void 0;
  return { ...location, widgetId };
}
function useMaximizedWidgetId() {
  const { maximizedWidget } = reactExports.useContext(MaximizedWidgetContext);
  const preview = usePreviewFeatures();
  const enabled = preview.enableMaximizedFloatingWidget || preview.enableMaximizedPanelWidget;
  if (!enabled)
    return void 0;
  return maximizedWidget;
}
function useMaximizedFloatingWidget() {
  const isMaximizedWidget = useIsMaximizedWidget();
  if (!isMaximizedWidget)
    return {
      style: {},
      classNames: {}
    };
  return {
    style: {
      transform: "unset",
      height: "unset",
      width: "unset",
      "max-height": "unset",
      "max-width": "unset"
    },
    classNames: { "uifw-preview-enableMaximizedWidget_floatingWidget": true }
  };
}
function useMaximizedWidgetTabBarHandle() {
  const isMaximizedWidget = useIsMaximizedWidget();
  return {
    "uifw-preview-enableMaximizedWidget_widget-tabBar": isMaximizedWidget
  };
}
try {
  useMaximizedPanelLayout.displayName = "useMaximizedPanelLayout";
  useMaximizedPanelLayout.__docgenInfo = { "description": "", "displayName": "useMaximizedPanelLayout", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useMaximizedPanel.displayName = "useMaximizedPanel";
  useMaximizedPanel.__docgenInfo = { "description": "", "displayName": "useMaximizedPanel", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useMaximizedSection.displayName = "useMaximizedSection";
  useMaximizedSection.__docgenInfo = { "description": "", "displayName": "useMaximizedSection", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const useActiveSendBackWidgetIdStore = create(() => void 0);
function getSendBackHomeState(state, widgetId) {
  const floatingWidget2 = state.floatingWidgets.byId[widgetId];
  const home = floatingWidget2.home;
  const panel = state.panels[home.side];
  const destinationWidgetId = home.widgetId ? home.widgetId : getWidgetPanelSectionId(panel.side, home.widgetIndex);
  let destinationWidget = state.widgets[destinationWidgetId];
  if (!destinationWidget && panel.widgets.length === panel.maxWidgetCount) {
    const id = panel.widgets[home.widgetIndex];
    destinationWidget = state.widgets[id];
  }
  if (destinationWidget) {
    return {
      side: home.side,
      widgetId: destinationWidget.id
    };
  }
  if (panel.widgets.length === 0) {
    return {
      side: home.side
    };
  }
  return {
    side: home.side,
    sectionIndex: destinationWidgetId.endsWith("End") ? 1 : 0
  };
}
function useSendBackHomeState() {
  const widgetId = useActiveSendBackWidgetIdStore((state) => state);
  return useLayout(
    (state) => widgetId ? getSendBackHomeState(state, widgetId) : void 0
  );
}
function Icon() {
  const id = useFloatingWidgetId();
  const home = useLayout((state) => state.floatingWidgets.byId[id].home);
  return home.side === "left" ? /* @__PURE__ */ jsxRuntimeExports.jsx(SvgDockLeft, {}) : home.side === "right" ? /* @__PURE__ */ jsxRuntimeExports.jsx(SvgDockRight, {}) : home.side === "top" ? /* @__PURE__ */ jsxRuntimeExports.jsx(SvgDockTop, {}) : /* @__PURE__ */ jsxRuntimeExports.jsx(SvgDockBottom, {});
}
function SendBack() {
  const id = useFloatingWidgetId();
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const title = useLabel("sendWidgetHomeTitle");
  const setActiveWidgetId = (newId) => useActiveSendBackWidgetIdStore.setState(newId);
  const onClick = () => {
    setActiveWidgetId(void 0);
    dispatch({
      type: "FLOATING_WIDGET_SEND_BACK",
      id
    });
  };
  const onMouseOver = () => {
    setActiveWidgetId(id);
  };
  const onFocus = () => {
    setActiveWidgetId(id);
  };
  const onMouseOut = () => {
    setActiveWidgetId(void 0);
  };
  const onBlur = () => {
    setActiveWidgetId(void 0);
  };
  const eventHandlers = { onMouseOver, onFocus, onMouseOut, onBlur };
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ActionButton,
    {
      icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, {}),
      title,
      onClick,
      buttonProps: eventHandlers,
      menuProps: eventHandlers
    }
  );
}
function useSendBack() {
  const maximizedWidget = useIsMaximizedWidget();
  const isToolSettings = useIsToolSettingsTab();
  const isFloatingWidget = !!useFloatingWidgetId();
  const canBeDocked = useWidgetAllowedToDock();
  return !maximizedWidget && isFloatingWidget && !isToolSettings && canBeDocked;
}
try {
  useActiveSendBackWidgetIdStore.displayName = "useActiveSendBackWidgetIdStore";
  useActiveSendBackWidgetIdStore.__docgenInfo = { "description": "", "displayName": "useActiveSendBackWidgetIdStore", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function PanelOutline() {
  const side = reactExports.useContext(PanelSideContext);
  const isHorizontal = isHorizontalPanelSide(side);
  const span = useLayout((state) => {
    const panel = state.panels[side];
    return isHorizontalPanelState(panel) ? panel.span : false;
  });
  const hidden = useHidden$3();
  const className = classnames(
    "nz-outline-panelOutline",
    hidden && "nz-hidden",
    `nz-${side}`,
    span && "nz-span"
  );
  const size = useSize$1();
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className,
      style: {
        width: isHorizontal ? void 0 : size,
        height: isHorizontal ? size : void 0
      }
    }
  );
}
function useHidden$3() {
  const side = reactExports.useContext(PanelSideContext);
  const targeted = useTargeted();
  const activeHomeState = useSendBackHomeState();
  return reactExports.useMemo(() => {
    if (activeHomeState) {
      if (activeHomeState.side === side && activeHomeState.widgetId === void 0 && activeHomeState.sectionIndex === void 0)
        return false;
    }
    if (!targeted)
      return true;
    if (!isPanelDropTargetState(targeted))
      return true;
    if (targeted.side !== side)
      return true;
    return false;
  }, [side, targeted, activeHomeState]);
}
function useSize$1() {
  const side = reactExports.useContext(PanelSideContext);
  return useLayout((state) => {
    const panel = state.panels[side];
    return panel.size !== void 0 ? panel.size : panel.minSize;
  });
}
function useCursor() {
  const type = reactExports.useContext(CursorTypeContext);
  reactExports.useEffect(() => {
    const cursor = type ? `nz-${type}` : void 0;
    cursor && document.body.classList.add(cursor);
    return () => {
      cursor && document.body.classList.remove(cursor);
    };
  }, [type]);
}
function getCursorClassName(type) {
  return `nz-widgetPanels-cursorOverlay_cursor nz-${type}`;
}
try {
  useCursor.displayName = "useCursor";
  useCursor.__docgenInfo = { "description": 'Controls cursor type of the application.\nI.e. when dragging a widget "grabbing" cursor should be displayed until user drops the widget.', "displayName": "useCursor", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  getCursorClassName.displayName = "getCursorClassName";
  getCursorClassName.__docgenInfo = { "description": "", "displayName": "getCursorClassName", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function useAllowedSideTarget(side) {
  const draggedWidgetId = reactExports.useContext(DraggedWidgetIdContext);
  return useLayout(
    (state) => isAllowedSideTarget(state, draggedWidgetId, side)
  );
}
function isAllowedSideTarget(state, draggedWidget, side) {
  const draggedTab = state.draggedTab;
  const tabsState = state.tabs;
  const widgetsState = state.widgets;
  let allowedPanelTargets;
  if (draggedTab) {
    const tab2 = tabsState[draggedTab.tabId];
    allowedPanelTargets = tab2.allowedPanelTargets;
  } else if (draggedWidget && draggedWidget in widgetsState) {
    const widget2 = widgetsState[draggedWidget];
    const activeTabId = widget2.activeTabId;
    const activeTab = tabsState[activeTabId];
    allowedPanelTargets = activeTab.allowedPanelTargets;
    widget2.tabs.forEach((tabId) => {
      const tab2 = tabsState[tabId];
      if (!allowedPanelTargets) {
        allowedPanelTargets = tab2.allowedPanelTargets;
      } else if (tab2.allowedPanelTargets !== void 0) {
        const tabPanelTargets = tab2.allowedPanelTargets;
        allowedPanelTargets = allowedPanelTargets.filter(
          (x) => tabPanelTargets.includes(x)
        );
      }
    });
  }
  if (allowedPanelTargets) {
    return allowedPanelTargets.includes(side);
  }
  return true;
}
try {
  useAllowedSideTarget.displayName = "useAllowedSideTarget";
  useAllowedSideTarget.__docgenInfo = { "description": "Check the docking side against allowed regions", "displayName": "useAllowedSideTarget", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function useAllowedPanelTarget() {
  const side = reactExports.useContext(PanelSideContext);
  return useAllowedSideTarget(side);
}
try {
  useAllowedPanelTarget.displayName = "useAllowedPanelTarget";
  useAllowedPanelTarget.__docgenInfo = { "description": "Check the docking side against allowed regions", "displayName": "useAllowedPanelTarget", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function SectionTarget(props) {
  const { sectionIndex } = props;
  const cursorType = reactExports.useContext(CursorTypeContext);
  const draggedTab = useLayout((state) => !!state.draggedTab);
  const draggedWidgetId = reactExports.useContext(DraggedWidgetIdContext);
  const direction = useTargetDirection();
  const [ref, targeted] = useTarget(
    useSectionTargetArgs(sectionIndex)
  );
  const allowedTarget = useAllowedPanelTarget();
  const hidden = !allowedTarget || !draggedTab && !draggedWidgetId;
  const className = classnames(
    "nz-target-sectionTarget",
    `nz-${direction}`,
    // istanbul ignore next
    targeted && "nz-targeted",
    hidden && "nz-hidden",
    // istanbul ignore next
    cursorType && getCursorClassName(cursorType)
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, ref, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: classnames(
          "nz-section",
          "nz-start",
          sectionIndex === 0 && "nz-target"
        )
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-border" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: classnames(
          "nz-section",
          "nz-end",
          sectionIndex === 1 && "nz-target"
        )
      }
    )
  ] });
}
function useSectionTargetArgs(sectionIndex) {
  const side = reactExports.useContext(PanelSideContext);
  const newWidgetId = reactExports.useMemo(() => getUniqueId(), []);
  return reactExports.useMemo(() => {
    return {
      type: "section",
      side,
      sectionIndex,
      newWidgetId
    };
  }, [side, sectionIndex, newWidgetId]);
}
function useTargetDirection() {
  const side = reactExports.useContext(PanelSideContext);
  if (isHorizontalPanelSide(side)) {
    return "horizontal";
  }
  return "vertical";
}
try {
  SectionTarget.displayName = "SectionTarget";
  SectionTarget.__docgenInfo = { "description": "", "displayName": "SectionTarget", "props": { "sectionIndex": { "defaultValue": null, "description": "", "name": "sectionIndex", "required": true, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }] } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useSectionTargetArgs.displayName = "useSectionTargetArgs";
  useSectionTargetArgs.__docgenInfo = { "description": "", "displayName": "useSectionTargetArgs", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function SectionOutline(props) {
  const hidden = useHidden$2(props.sectionIndex);
  const direction = useTargetDirection();
  const style = useSize(props.sectionIndex);
  const className = classnames(
    "nz-outline-sectionOutline",
    `nz-${props.sectionIndex}`,
    `nz-${direction}`,
    hidden && "nz-hidden",
    props.className
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className,
      style: {
        ...hidden ? {} : style,
        ...props.style
      }
    }
  );
}
function useHidden$2(sectionIndex) {
  const side = reactExports.useContext(PanelSideContext);
  const targeted = useTargeted();
  const activeHomeState = useSendBackHomeState();
  return reactExports.useMemo(() => {
    if (activeHomeState && activeHomeState.side === side && activeHomeState.sectionIndex === sectionIndex)
      return false;
    if (!targeted)
      return true;
    if (!isSectionDropTargetState(targeted))
      return true;
    if (targeted.sectionIndex !== sectionIndex)
      return true;
    if (targeted.side !== side)
      return true;
    return false;
  }, [targeted, side, sectionIndex, activeHomeState]);
}
function useSize(sectionIndex) {
  const side = reactExports.useContext(PanelSideContext);
  const splitterPercent = useLayout(
    (state) => state.panels[side].splitterPercent
  );
  return reactExports.useMemo(() => {
    let size = splitterPercent;
    if (!size)
      return void 0;
    if (sectionIndex === 1)
      size = 100 - size;
    const style = {};
    if (isHorizontalPanelSide(side))
      style.width = `${size}%`;
    else
      style.height = `${size}%`;
    return style;
  }, [side, splitterPercent, sectionIndex]);
}
try {
  SectionOutline.displayName = "SectionOutline";
  SectionOutline.__docgenInfo = { "description": "", "displayName": "SectionOutline", "props": { "sectionIndex": { "defaultValue": null, "description": "", "name": "sectionIndex", "required": true, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }] } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function TargetContainer(props) {
  const className = classnames(
    "nz-target-targetContainer",
    `nz-${props.direction}`,
    props.className
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className, style: props.style, children: props.children });
}
try {
  TargetContainer.displayName = "TargetContainer";
  TargetContainer.__docgenInfo = { "description": "", "displayName": "TargetContainer", "props": { "direction": { "defaultValue": null, "description": "", "name": "direction", "required": true, "type": { "name": "enum", "value": [{ "value": '"horizontal"' }, { "value": '"vertical"' }] } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function PanelTarget(props) {
  const { side } = props;
  const cursorType = reactExports.useContext(CursorTypeContext);
  const draggedWidgetId = reactExports.useContext(DraggedWidgetIdContext);
  const draggedTab = useLayout((state) => !!state.draggedTab);
  const allowedTarget = useAllowedPanelTarget();
  const newWidgetId = reactExports.useMemo(() => getUniqueId(), []);
  const [ref, targeted] = usePanelTarget({
    side,
    newWidgetId
  });
  const visible = (draggedTab || !!draggedWidgetId) && allowedTarget;
  const isHorizontal = isHorizontalPanelSide(side);
  const className = classnames(
    "nz-target-panelTarget",
    // istanbul ignore next
    isHorizontal ? "nz-horizontal" : "nz-vertical",
    // istanbul ignore next
    targeted && "nz-targeted",
    !visible && "nz-hidden",
    // istanbul ignore next
    cursorType && getCursorClassName(cursorType)
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className, ref });
}
try {
  PanelTarget.displayName = "PanelTarget";
  PanelTarget.__docgenInfo = { "description": "", "displayName": "PanelTarget", "props": { "side": { "defaultValue": null, "description": "", "name": "side", "required": true, "type": { "name": "enum", "value": [{ "value": '"top"' }, { "value": '"left"' }, { "value": '"right"' }, { "value": '"bottom"' }] } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function useAllowedWidgetTarget(widgetId) {
  const draggedWidget = reactExports.useContext(DraggedWidgetIdContext);
  return useLayout((state) => {
    const widgetLocation = getWidgetLocation(state, widgetId);
    if (!widgetLocation || isPopoutWidgetLocation(widgetLocation)) {
      return false;
    } else if (isFloatingWidgetLocation(widgetLocation)) {
      return true;
    }
    return isAllowedSideTarget(state, draggedWidget, widgetLocation.side);
  });
}
try {
  useAllowedWidgetTarget.displayName = "useAllowedWidgetTarget";
  useAllowedWidgetTarget.__docgenInfo = { "description": "Checks the proposed docking target to see if it's allowed by the dragged widget or tab", "displayName": "useAllowedWidgetTarget", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function MergeTarget(props) {
  const { widgetId } = props;
  const cursorType = reactExports.useContext(CursorTypeContext);
  const draggedWidgetId = reactExports.useContext(DraggedWidgetIdContext);
  const draggedTab = useLayout((state) => !!state.draggedTab);
  const [ref, targeted] = useTarget(useTargetArgs$2(widgetId));
  const allowedTarget = useAllowedWidgetTarget(widgetId);
  const hidden = !allowedTarget || !draggedTab && !draggedWidgetId || draggedWidgetId === widgetId;
  const className = classnames(
    "nz-target-mergeTarget",
    // istanbul ignore next
    targeted && "nz-targeted",
    hidden && "nz-hidden",
    // istanbul ignore next
    cursorType && getCursorClassName(cursorType)
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className, ref });
}
function useTargetArgs$2(widgetId) {
  return reactExports.useMemo(() => {
    return {
      type: "widget",
      widgetId
    };
  }, [widgetId]);
}
try {
  MergeTarget.displayName = "MergeTarget";
  MergeTarget.__docgenInfo = { "description": "", "displayName": "MergeTarget", "props": { "widgetId": { "defaultValue": null, "description": "", "name": "widgetId", "required": true, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function PanelTargets() {
  const side = reactExports.useContext(PanelSideContext);
  const widgets = useLayout((state) => state.panels[side].widgets, true);
  const span = useLayout((state) => {
    const panel = state.panels[side];
    return isHorizontalPanelState(panel) && panel.span;
  });
  const direction = useTargetDirection();
  const type = usePanelTargetsType();
  const className = classnames(
    "nz-target-panelTargets",
    `nz-${side}`,
    type === "two-widgets" && "nz-wide",
    span && "nz-span"
  );
  let targets;
  if (type === "no-panel") {
    targets = /* @__PURE__ */ jsxRuntimeExports.jsx(PanelTarget, { side });
  } else if (type === "single-widget") {
    targets = /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(SectionTarget, { sectionIndex: 0 }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(MergeTarget, { widgetId: widgets[0] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(SectionTarget, { sectionIndex: 1 })
    ] });
  } else if (type === "two-widgets") {
    targets = /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(MergeTarget, { widgetId: widgets[0] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(MergeTarget, { widgetId: widgets[1] })
    ] });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(TargetContainer, { className, direction, children: targets });
}
function usePanelTargetsType() {
  const side = reactExports.useContext(PanelSideContext);
  return useLayout((state) => {
    const panel = state.panels[side];
    if (panel.widgets.length === 0)
      return "no-panel";
    if (!panel.collapsed)
      return "hidden";
    if (panel.widgets.length === 2)
      return "two-widgets";
    assert(panel.widgets.length === 1);
    return "single-widget";
  });
}
function SectionTargets(props) {
  const direction = useTargetDirection();
  const { widgetId } = props;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(TargetContainer, { className: "nz-target-sectionTargets", direction, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(SectionTarget, { sectionIndex: 0 }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(MergeTarget, { widgetId }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(SectionTarget, { sectionIndex: 1 })
  ] });
}
try {
  SectionTargets.displayName = "SectionTargets";
  SectionTargets.__docgenInfo = { "description": "", "displayName": "SectionTargets", "props": { "widgetId": { "defaultValue": null, "description": "", "name": "widgetId", "required": true, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const usePointerCaptor = (onPointerDown, onPointerMove, onPointerUp) => {
  const dragManager = reactExports.useContext(DragManagerContext);
  const isDown = reactExports.useRef(false);
  reactExports.useEffect(() => {
    const mouseMove = (e) => {
      isDown.current && onPointerMove && onPointerMove(e, e);
    };
    document.addEventListener("mousemove", mouseMove);
    return () => {
      document.removeEventListener("mousemove", mouseMove);
    };
  }, [onPointerMove]);
  reactExports.useEffect(() => {
    const mouseUp = (e) => {
      isDown.current && onPointerUp && onPointerUp(e);
      isDown.current = false;
    };
    document.addEventListener("mouseup", mouseUp);
    return () => {
      document.removeEventListener("mouseup", mouseUp);
    };
  }, [onPointerUp]);
  const setRef = useRefEffect(
    (instance) => {
      let touchTarget = null;
      const mouseDown = (e) => {
        const isSecondaryButton = (e.button & 2) === 2;
        if (isSecondaryButton)
          return;
        onPointerDown && onPointerDown(e, e);
        isDown.current = true;
      };
      const touchMove = (e) => {
        if (e.touches.length !== 1)
          return;
        isDown.current && onPointerMove && onPointerMove(e.touches[0], e);
        isDown.current && dragManager.handleDrag(e.touches[0].clientX, e.touches[0].clientY);
      };
      const targetTouchMove = (e) => {
        e.cancelable && e.preventDefault();
        touchMove(e);
      };
      const documentTouchMove = (e) => {
        if (touchTarget === e.target)
          return;
        touchMove(e);
      };
      const touchEnd = (e) => {
        isDown.current && onPointerUp && onPointerUp(e);
        isDown.current && dragManager.handleDragEnd();
        isDown.current = false;
        touchTarget = null;
        if (e.target instanceof HTMLElement) {
          e.target.removeEventListener("touchmove", targetTouchMove);
          e.target.removeEventListener("touchend", touchEnd);
        }
        document.removeEventListener("touchmove", documentTouchMove);
        document.removeEventListener("touchend", documentTouchEnd);
      };
      const documentTouchEnd = (e) => {
        if (touchTarget === e.target)
          return;
        touchEnd(e);
      };
      const touchStart = (e) => {
        e.cancelable && e.preventDefault();
        if (e.touches.length !== 1)
          return;
        touchTarget = e.target;
        if (e.target instanceof HTMLElement) {
          e.target.addEventListener("touchmove", targetTouchMove);
          e.target.addEventListener("touchend", touchEnd);
        }
        document.addEventListener("touchmove", documentTouchMove);
        document.addEventListener("touchend", documentTouchEnd);
        onPointerDown && onPointerDown(e.touches[0], e);
        isDown.current = true;
      };
      instance && instance.addEventListener("mousedown", mouseDown);
      instance && instance.addEventListener("touchstart", touchStart);
      return () => {
        instance && instance.removeEventListener("mousedown", mouseDown);
        instance && instance.removeEventListener("touchstart", touchStart);
      };
    },
    [onPointerDown, onPointerMove, onPointerUp, dragManager]
  );
  return setRef;
};
try {
  usePointerCaptor.displayName = "usePointerCaptor";
  usePointerCaptor.__docgenInfo = { "description": "Captures mouse and touch events of an element. Used in drag or resize interactions.", "displayName": "usePointerCaptor", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetPanelGrip(props) {
  const side = reactExports.useContext(PanelSideContext);
  const { collapsed, pinned } = useLayout((state) => {
    const panel = state.panels[side];
    return {
      collapsed: panel.collapsed,
      pinned: panel.pinned
    };
  }, true);
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const [ref, resizing, active] = useResizeGrip();
  const className = classnames(
    "nz-widgetPanels-grip",
    `nz-${side}`,
    active && "nz-active",
    resizing && "nz-resizing",
    props.className
  );
  const resizeGripTitle = useLabel("resizeGripTitle");
  const isMaximizedPanel = !!useMaximizedPanel(side);
  if (isMaximizedPanel)
    return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, title: resizeGripTitle, style: props.style, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-line-grip", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-line-grip-detail" }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: "nz-handle",
        ref,
        onMouseOverCapture: () => {
          collapsed && !pinned && !resizing && dispatch({
            side,
            collapsed: false,
            type: "PANEL_SET_COLLAPSED"
          });
        }
      }
    )
  ] });
}
const useResizeGrip = () => {
  const widgetPanel = reactExports.useContext(WidgetPanelContext);
  const side = reactExports.useContext(PanelSideContext);
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const [resizing, setResizing] = reactExports.useState(false);
  const [active, setActive] = reactExports.useState(false);
  const initialPointerPosition = reactExports.useRef();
  const dragStartTimer = reactExports.useRef(new Timer(300));
  const ref = reactExports.useRef(null);
  const relativePosition = reactExports.useRef(new Point());
  const layoutStore = useLayoutStore$1();
  const panelStateRef = reactExports.useRef(layoutStore.getState().panels[side]);
  reactExports.useLayoutEffect(
    () => layoutStore.subscribe((state) => {
      panelStateRef.current = state.panels[side];
    }),
    [layoutStore, side]
  );
  const handleDoubleClick = reactExports.useCallback(() => {
    dispatch({
      type: "PANEL_TOGGLE_COLLAPSED",
      side
    });
  }, [dispatch, side]);
  const handleClick = useDoubleClick(handleDoubleClick);
  const onDrag = reactExports.useCallback(
    (pointerPosition, lastPointerPosition) => {
      if (!ref.current)
        return;
      const direction = side === "left" || side === "top" ? 1 : -1;
      const dragOffset = lastPointerPosition.getOffsetTo(pointerPosition);
      const dragBy = isHorizontalPanelSide(side) ? dragOffset.y : dragOffset.x;
      const resizeBy = direction * dragBy;
      const bounds = widgetPanel.getBounds();
      const outerEdge = bounds[side];
      const innerEdge = isHorizontalPanelSide(side) ? pointerPosition.y : pointerPosition.x;
      const size = (innerEdge - outerEdge) * direction;
      const panel = panelStateRef.current;
      if (resizeBy === 0)
        return;
      if (panel.collapsed) {
        if (size >= panel.collapseOffset) {
          dispatch({
            type: "PANEL_SET_COLLAPSED",
            side,
            collapsed: false
          });
          return;
        }
        return;
      }
      if (panel.size === void 0)
        return;
      const sizeDiff = size - panel.size;
      if (sizeDiff * resizeBy < 0)
        return;
      const collapseThreshold = Math.max(
        panel.minSize - panel.collapseOffset,
        0
      );
      if (size <= collapseThreshold) {
        dispatch({
          type: "PANEL_SET_COLLAPSED",
          side,
          collapsed: true
        });
        dispatch({
          type: "PANEL_SET_SIZE",
          side,
          size: panel.minSize
        });
        return;
      }
      dispatch({
        type: "PANEL_SET_SIZE",
        side,
        size
      });
    },
    [dispatch, side, widgetPanel]
  );
  const onDragEnd = reactExports.useCallback(() => {
    setResizing(false);
  }, []);
  const handlePanelGripDragStart = useDragPanelGrip({
    side,
    onDrag,
    onDragEnd
  });
  reactExports.useEffect(() => {
    const timer = dragStartTimer.current;
    timer.setOnExecute(() => {
      if (initialPointerPosition.current && ref.current) {
        relativePosition.current = Rectangle.create(
          ref.current.getBoundingClientRect()
        ).topLeft().getOffsetTo(initialPointerPosition.current);
        setResizing(true);
        handlePanelGripDragStart({
          initialPointerPosition: initialPointerPosition.current,
          pointerPosition: initialPointerPosition.current
        });
      }
      initialPointerPosition.current = void 0;
    });
    return () => {
      timer.setOnExecute(void 0);
    };
  }, [handlePanelGripDragStart]);
  const handleDragEnd = reactExports.useCallback(() => {
    initialPointerPosition.current = void 0;
    dragStartTimer.current.stop();
    handleClick();
    setActive(false);
  }, [handleClick]);
  const handlePointerDown = reactExports.useCallback((args) => {
    initialPointerPosition.current = new Point(args.clientX, args.clientY);
    dragStartTimer.current.start();
    setActive(true);
  }, []);
  const handlePointerMove = reactExports.useCallback(
    (args) => {
      if (!initialPointerPosition.current)
        return;
      const position = new Point(args.clientX, args.clientY);
      setResizing(true);
      handlePanelGripDragStart({
        initialPointerPosition: position,
        pointerPosition: position
      });
      onDrag(position, initialPointerPosition.current);
      initialPointerPosition.current = void 0;
      dragStartTimer.current.stop();
    },
    [handlePanelGripDragStart, onDrag]
  );
  const handlePointerCaptorRef = usePointerCaptor(
    handlePointerDown,
    handlePointerMove,
    handleDragEnd
  );
  const handleRef = reactExports.useCallback(
    (instance) => {
      ref.current = instance;
      handlePointerCaptorRef(instance);
    },
    [handlePointerCaptorRef]
  );
  return [handleRef, resizing, active];
};
function useDoubleClick(onDoubleClick) {
  const timer = reactExports.useRef(new Timer(300));
  const clickCount = reactExports.useRef(0);
  timer.current.setOnExecute(() => {
    clickCount.current = 0;
  });
  const handleClick = reactExports.useCallback(() => {
    timer.current.start();
    clickCount.current++;
    if (clickCount.current === 2) {
      onDoubleClick && onDoubleClick();
      clickCount.current = 0;
      timer.current.stop();
    }
  }, [onDoubleClick]);
  return handleClick;
}
try {
  WidgetPanelGrip.displayName = "WidgetPanelGrip";
  WidgetPanelGrip.__docgenInfo = { "description": "Resize grip of [[WidgetPanel]] component.", "displayName": "WidgetPanelGrip", "props": { "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useDoubleClick.displayName = "useDoubleClick";
  useDoubleClick.__docgenInfo = { "description": "", "displayName": "useDoubleClick", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function Dock() {
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const title = useLabel("dockToolSettingsTitle");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ActionButton,
    {
      icon: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgDockTop, {}),
      title,
      onClick: () => {
        dispatch({
          type: "TOOL_SETTINGS_DOCK"
        });
      }
    }
  );
}
function useDock() {
  const isToolSettings = useIsToolSettingsTab();
  const isMaximizedWidget = useIsMaximizedWidget();
  return isToolSettings && !isMaximizedWidget;
}
function usePanelWidgetId() {
  const side = reactExports.useContext(PanelSideContext);
  const widgetId = reactExports.useContext(WidgetIdContext);
  if (!side || widgetId === void 0)
    return void 0;
  return widgetId;
}
function useMainPanelWidgetId() {
  const side = reactExports.useContext(PanelSideContext);
  const widgetId = reactExports.useContext(WidgetIdContext);
  return useLayout((state) => {
    if (!side)
      return void 0;
    const widgets = state.panels[side].widgets;
    const mainWidget = isHorizontalPanelSide(side) ? widgets[widgets.length - 1] : widgets[0];
    return mainWidget === widgetId ? widgetId : void 0;
  });
}
function PinToggle() {
  const side = reactExports.useContext(PanelSideContext);
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const pinPanelTitle = useLabel("pinPanelTitle");
  const unpinPanelTitle = useLabel("unpinPanelTitle");
  const pinned = useLayout((state) => state.panels[side].pinned);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ActionButton,
    {
      icon: pinned ? /* @__PURE__ */ jsxRuntimeExports.jsx(SvgPin, {}) : /* @__PURE__ */ jsxRuntimeExports.jsx(SvgPinHollow, {}),
      title: pinned ? unpinPanelTitle : pinPanelTitle,
      onClick: () => {
        dispatch({
          side,
          type: "PANEL_TOGGLE_PINNED"
        });
      }
    }
  );
}
function usePinToggle() {
  const isMainPanelWidget = !!useMainPanelWidgetId();
  const isMaximizedWidget = useIsMaximizedWidget();
  return isMainPanelWidget && !isMaximizedWidget;
}
function PopoutToggle() {
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const activeTabId = useActiveTabId();
  const popoutTitle = useLabel("popoutActiveTab");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ActionButton,
    {
      icon: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgWindowPopout, {}),
      title: popoutTitle,
      onClick: () => {
        dispatch({
          id: activeTabId,
          type: "WIDGET_TAB_POPOUT"
        });
      }
    }
  );
}
function usePopoutToggle() {
  const tabId = useActiveTabId();
  return useLayout((state) => {
    const tab2 = state.tabs[tabId];
    return !!tab2.canPopout;
  });
}
const defaultAlignments = {
  top: HorizontalAlignment.Justify,
  bottom: HorizontalAlignment.Justify
};
const HorizontalPanelAlignContext = reactExports.createContext({
  enabled: false,
  alignments: defaultAlignments,
  setAlignment: () => {
  }
});
function PreviewHorizontalPanelAlignFeatureProvider({
  enabled,
  children
}) {
  const [alignments, setAlignments] = reactExports.useState({
    top: HorizontalAlignment.Justify,
    bottom: HorizontalAlignment.Justify
  });
  const setAlignment = reactExports.useCallback(
    (panel, alignment) => {
      if (!panel || !isHorizontalPanelSide(panel))
        return;
      setAlignments((prev) => ({
        ...prev,
        [panel]: alignment
      }));
    },
    []
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    HorizontalPanelAlignContext.Provider,
    {
      value: {
        enabled: !!enabled,
        alignments: enabled ? alignments : defaultAlignments,
        setAlignment
      },
      children
    }
  );
}
function SvgSide(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      width: "1rem",
      height: "1rem",
      viewBox: "0 0 16 16",
      fill: "var(--iui-color-icon-muted, currentColor)",
      ...props,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M0 2v12h16V2H0Zm11 7H1V3h10v6Zm4 4h-3V3h3v10Z" })
    }
  );
}
function SvgJustify(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      width: "1rem",
      height: "1rem",
      viewBox: "0 0 16 16",
      fill: "var(--iui-color-icon-muted, currentColor)",
      ...props,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M0 2v12h16V2H0Zm4 7H1V3h3v6Zm1 0V3h6v6H5Zm10 0h-3V3h3v6Z" })
    }
  );
}
function SvgCenter(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      width: "1rem",
      height: "1rem",
      viewBox: "0 0 16 16",
      fill: "var(--iui-color-icon-muted, currentColor)",
      ...props,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M0 2v12h16V2H0Zm4 11H1V3h3v10Zm1-4V3h6v6H5Zm10 4h-3V3h3v10Z" })
    }
  );
}
function getIcon(side, alignment) {
  const scaleY = side === "top" ? "-1" : "1";
  if (alignment === HorizontalAlignment.Justify)
    return /* @__PURE__ */ jsxRuntimeExports.jsx(SvgJustify, { transform: `scale(1, ${scaleY})` });
  if (alignment === HorizontalAlignment.Center)
    return /* @__PURE__ */ jsxRuntimeExports.jsx(SvgCenter, { transform: `scale(1, ${scaleY})` });
  const scaleX = alignment === HorizontalAlignment.Left ? "1" : "-1";
  return /* @__PURE__ */ jsxRuntimeExports.jsx(SvgSide, { transform: `scale(${scaleX}, ${scaleY})` });
}
function capitalize(s) {
  return s.charAt(0).toUpperCase() + s.slice(1);
}
function PreviewHorizontalPanelAlignButton() {
  const side = reactExports.useContext(PanelSideContext);
  const { alignments, setAlignment } = reactExports.useContext(
    HorizontalPanelAlignContext
  );
  const title = "Align panel";
  const getMenuItems = (onClose) => [
    HorizontalAlignment.Justify,
    HorizontalAlignment.Center,
    HorizontalAlignment.Left,
    HorizontalAlignment.Right
  ].map((align) => {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      MenuItem$1,
      {
        onClick: () => {
          setAlignment(side, align);
          onClose == null ? void 0 : onClose();
        },
        icon: getIcon(side, align),
        isSelected: alignments[side] === align,
        children: capitalize(align)
      },
      align
    );
  });
  const dropdownContext = reactExports.useContext(WidgetActionDropdownContext);
  if (dropdownContext !== void 0) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      MenuItem$1,
      {
        icon: getIcon(side, alignments[side]),
        subMenuItems: getMenuItems(),
        children: title
      }
    );
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(DropdownMenu, { menuItems: (close) => getMenuItems(close), children: /* @__PURE__ */ jsxRuntimeExports.jsx(TabBarButton, { title, children: getIcon(side, alignments[side]) }) });
}
function useHorizontalPanelAlignButton() {
  const side = reactExports.useContext(PanelSideContext);
  const { enabled } = reactExports.useContext(HorizontalPanelAlignContext);
  const isMainPanelWidget = !!useMainPanelWidgetId();
  const isMaximizedWidget = useIsMaximizedWidget();
  if (!side)
    return false;
  const isHorizontalPanel = isHorizontalPanelSide(side);
  return enabled && isHorizontalPanel && isMainPanelWidget && !isMaximizedWidget;
}
try {
  PreviewHorizontalPanelAlignFeatureProvider.displayName = "PreviewHorizontalPanelAlignFeatureProvider";
  PreviewHorizontalPanelAlignFeatureProvider.__docgenInfo = { "description": "Preview horizontal panel alignment feature provider.", "displayName": "PreviewHorizontalPanelAlignFeatureProvider", "props": { "enabled": { "defaultValue": null, "description": "", "name": "enabled", "required": false, "type": { "name": "boolean" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function MaximizeToggle() {
  const widgetId = reactExports.useContext(WidgetIdContext);
  const { maximizedWidget, setMaximizedWidget } = reactExports.useContext(
    MaximizedWidgetContext
  );
  const { id, title, iconSpec } = maximizedWidget === widgetId ? {
    id: void 0,
    title: "Restore",
    iconSpec: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgWindowMinimize, {})
  } : {
    id: widgetId,
    title: "Maximize",
    iconSpec: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgWindowMaximize, {})
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ActionButton,
    {
      icon: iconSpec,
      title,
      onClick: () => {
        setMaximizedWidget(id);
      }
    }
  );
}
function useMaximizeToggle() {
  const { enableMaximizedFloatingWidget, enableMaximizedPanelWidget } = usePreviewFeatures();
  const floatingWidgetId = useFloatingWidgetId();
  const panelWidgetId = usePanelWidgetId();
  if (enableMaximizedFloatingWidget && floatingWidgetId)
    return true;
  if (enableMaximizedPanelWidget && panelWidgetId)
    return true;
  return false;
}
function TabBarButtons() {
  const features = useWidgetFeatures();
  const [sortedFeatures, isDropdown] = useDropdownFeatures(features);
  const buttons2 = sortedFeatures.map((feature) => {
    switch (feature) {
      case "popout":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(PopoutToggle, {}, "popout");
      case "maximize":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(MaximizeToggle, {}, "maximize");
      case "sendBack":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(SendBack, {}, "sendBack");
      case "dock":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(Dock, {}, "dock");
      case "horizontalAlign":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(PreviewHorizontalPanelAlignButton, {}, "horizontalAlign");
      case "pin":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(PinToggle, {}, "pin");
    }
    return void 0;
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-widget-buttons", children: isDropdown ? /* @__PURE__ */ jsxRuntimeExports.jsx(MoreButton, { children: buttons2 }) : buttons2 });
}
function useWidgetFeatures() {
  const popoutToggle = usePopoutToggle();
  const maximizeToggle = useMaximizeToggle();
  const sendBack = useSendBack();
  const dock = useDock();
  const horizontalPanelAlignButton = useHorizontalPanelAlignButton();
  const pinToggle = usePinToggle();
  return [
    ...popoutToggle ? ["popout"] : [],
    ...maximizeToggle ? ["maximize"] : [],
    ...sendBack ? ["sendBack"] : [],
    ...dock ? ["dock"] : [],
    ...horizontalPanelAlignButton ? ["horizontalAlign"] : [],
    ...pinToggle ? ["pin"] : []
  ];
}
function DockedToolSettingsHandle(props) {
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const resizeObserverRef = useResizeObserver(props.onResize);
  const newFloatingWidgetId = reactExports.useMemo(() => getUniqueId(), []);
  const onDragStart = useDragToolSettings({ newFloatingWidgetId });
  const handleDragStart = reactExports.useCallback(
    (initialPointerPosition) => {
      onDragStart({
        initialPointerPosition,
        pointerPosition: initialPointerPosition
      });
      dispatch({
        type: "TOOL_SETTINGS_DRAG_START",
        newFloatingWidgetId
      });
    },
    [dispatch, newFloatingWidgetId, onDragStart]
  );
  const dragRef = useDrag(handleDragStart);
  const refs = useRefs(dragRef, resizeObserverRef);
  const title = useLabel("toolSettingsHandleTitle");
  const className = classnames("nz-toolSettings-handle", props.className);
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className, ref: refs, style: props.style, title, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$2, { size: "large", children: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgDragHandleVertical, {}) }) });
}
try {
  DockedToolSettingsHandle.displayName = "DockedToolSettingsHandle";
  DockedToolSettingsHandle.__docgenInfo = { "description": "Component that displays tool settings as a bar across the top of the content view.", "displayName": "DockedToolSettingsHandle", "props": { "onResize": { "defaultValue": null, "description": "", "name": "onResize", "required": false, "type": { "name": "((w: number) => void)" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const DockedToolSettingsOverflow = reactExports.forwardRef(function DockedToolSettingsOverflow2(props, ref) {
  const roRef = useResizeObserver(props.onResize);
  const refs = useRefs(roRef, ref);
  const className = classnames("nz-toolSettings-overflow", props.className);
  const moreToolSettingsTitle = useLabel("moreToolSettingsTitle");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    IconButton,
    {
      className,
      onClick: props.onClick,
      ref: refs,
      style: props.style,
      styleType: "borderless",
      label: moreToolSettingsTitle,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgMore, {})
    }
  );
});
try {
  DockedToolSettingsOverflow.displayName = "DockedToolSettingsOverflow";
  DockedToolSettingsOverflow.__docgenInfo = { "description": "Entry point to overflown tool settings of [[DockedToolSettings]] component.", "displayName": "DockedToolSettingsOverflow", "props": { "onClick": { "defaultValue": null, "description": "Function called when button is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "onResize": { "defaultValue": null, "description": "Function called when button is resized.", "name": "onResize", "required": false, "type": { "name": "((w: number) => void)" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function ToolSettingsOverflowPanel(props) {
  const className = classnames("nz-toolSettings-panel", props.className);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Popup,
    {
      className,
      isOpen: props.open,
      offset: 0,
      onClose: props.onClose,
      style: props.style,
      showShadow: true,
      target: props.target,
      children: props.children
    }
  );
}
try {
  ToolSettingsOverflowPanel.displayName = "ToolSettingsOverflowPanel";
  ToolSettingsOverflowPanel.__docgenInfo = { "description": "Displays overflown tool settings.", "displayName": "ToolSettingsOverflowPanel", "props": { "children": { "defaultValue": null, "description": "Panel content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "onClose": { "defaultValue": null, "description": "", "name": "onClose", "required": true, "type": { "name": "() => void" } }, "target": { "defaultValue": null, "description": "", "name": "target", "required": true, "type": { "name": "HTMLElement | undefined" } }, "open": { "defaultValue": null, "description": "", "name": "open", "required": true, "type": { "name": "boolean" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function onOverflowLabelAndEditorResize() {
}
function OverflowLabelAndEditor({ wrapper }) {
  assert(reactExports.isValidElement(wrapper));
  const entryValue = reactExports.useMemo(
    () => ({
      isOverflown: true,
      onResize: onOverflowLabelAndEditorResize
    }),
    []
  );
  const wrapperChildren = wrapper.props.children;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(jsxRuntimeExports.Fragment, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(DockedToolSettingsEntryContext.Provider, { value: entryValue, children: wrapperChildren && reactExports.Children.map(
    wrapperChildren,
    (child) => child
  ) }) });
}
function DockedToolSettings(props) {
  const [open, setOpen] = reactExports.useState(false);
  const animateToolSettings = reactExports.useContext(
    AnimateDockedToolSettingsContext
  );
  const ref = reactExports.useRef(null);
  const width = reactExports.useRef(void 0);
  const handleWidth = reactExports.useRef(void 0);
  const [
    overflown,
    handleContainerResize,
    handleOverflowResize,
    getOnEntryResize,
    handleGapResize
  ] = useOverflow$1(props.children);
  const onResize = reactExports.useCallback(() => {
    let gapSize = 0;
    if (ref.current) {
      gapSize = getGapVariable(ref.current);
      handleGapResize(gapSize);
    }
    width.current !== void 0 && handleWidth.current !== void 0 && handleContainerResize(width.current - handleWidth.current - gapSize);
  }, [handleContainerResize, handleGapResize]);
  const handleHandleResize = reactExports.useCallback(
    (w) => {
      handleWidth.current = w;
      onResize();
    },
    [onResize]
  );
  const handleResize = reactExports.useCallback(
    (w) => {
      width.current = w;
      onResize();
    },
    [onResize]
  );
  const resizeObserverRef = useResizeObserver(handleResize);
  const onOverflowClick = reactExports.useCallback(() => {
    setOpen((prev) => !prev);
  }, []);
  const handleOnClose = reactExports.useCallback(() => {
    setOpen(false);
  }, []);
  const targetRef = reactExports.useRef(null);
  const refs = useRefs(ref, resizeObserverRef);
  const children = reactExports.useMemo(
    () => reactExports.Children.toArray(props.children),
    [props.children]
  );
  const dockedChildren = children.reduce(
    (acc, child, index) => {
      const key = getChildKey(child, index);
      if (!overflown || overflown.indexOf(key) < 0) {
        acc.push([key, child]);
      }
      return acc;
    },
    []
  );
  const overflownChildren = overflown ? children.reduce((acc, child, index) => {
    const key = getChildKey(child, index);
    if (overflown.indexOf(key) >= 0) {
      acc.push([key, child]);
    }
    return acc;
  }, []) : [];
  const PanelContainer = props.panelContainer ? props.panelContainer : DefaultPanelContainer;
  const className = classnames("nz-toolSettings-docked", props.className);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      "data-toolsettings-provider": props.itemId,
      className,
      ref: refs,
      style: props.style,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(DockedToolSettingsHandle, { onResize: handleHandleResize }),
        dockedChildren.map(([key, child]) => {
          return /* @__PURE__ */ jsxRuntimeExports.jsx(
            TransitionGroup,
            {
              appear: animateToolSettings,
              enter: animateToolSettings,
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                CSSTransition,
                {
                  in: animateToolSettings,
                  timeout: 400,
                  classNames: {
                    appear: "toolsettings-appear",
                    enter: "toolsettings-appear",
                    appearActive: "toolsettings-appear-active",
                    enterActive: "toolsettings-appear-active"
                  },
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                    DockedToolSettingsEntry,
                    {
                      entryKey: key,
                      getOnResize: getOnEntryResize,
                      children: child
                    },
                    key
                  )
                }
              )
            },
            key
          );
        }),
        (!overflown || overflown.length > 0) && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            DockedToolSettingsOverflow,
            {
              onClick: onOverflowClick,
              onResize: handleOverflowResize,
              ref: targetRef
            }
          ),
          overflownChildren.length > 0 && open && targetRef.current && /* @__PURE__ */ jsxRuntimeExports.jsx(
            ToolSettingsOverflowPanel,
            {
              onClose: handleOnClose,
              open: true,
              target: targetRef.current,
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(PanelContainer, { children: overflownChildren.map(([key, child]) => {
                return /* @__PURE__ */ jsxRuntimeExports.jsx(OverflowLabelAndEditor, { wrapper: child }, key);
              }) })
            }
          )
        ] })
      ]
    }
  );
}
function DockedToolSettingsEntry({
  children,
  entryKey,
  getOnResize
}) {
  const onResize = reactExports.useMemo(
    () => getOnResize(entryKey),
    [getOnResize, entryKey]
  );
  const entry = reactExports.useMemo(
    () => ({
      isOverflown: false,
      onResize
    }),
    [onResize]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(DockedToolSettingsEntryContext.Provider, { value: entry, children });
}
function getChildKey(child, index) {
  if (reactExports.isValidElement(child) && child.key !== null) {
    return child.key.toString();
  }
  return index.toString();
}
function getOverflown(width, docked, overflowWidth, activeIndex, gapSize = 0) {
  let settingsWidth = 0;
  let firstItem = true;
  const getGapSize = () => {
    if (firstItem) {
      firstItem = false;
      return 0;
    }
    return gapSize;
  };
  if (activeIndex !== void 0 && docked.length > activeIndex) {
    const activeWidth = docked[activeIndex];
    settingsWidth += activeWidth[1] + getGapSize();
  }
  let i = 0;
  for (; i < docked.length; i++) {
    if (i === activeIndex)
      continue;
    const itemWidth = docked[i][1];
    const newSettingsWidth = settingsWidth + itemWidth + getGapSize();
    if (newSettingsWidth > width) {
      settingsWidth += overflowWidth + getGapSize();
      break;
    }
    settingsWidth = newSettingsWidth;
  }
  let j = i;
  if (j < docked.length) {
    for (; j > 0; j--) {
      if (j === activeIndex)
        continue;
      if (settingsWidth <= width)
        break;
      const itemWidth = docked[j][1];
      settingsWidth -= itemWidth + gapSize;
    }
  }
  const overflown = new Array();
  for (i = j; i < docked.length; i++) {
    if (i === activeIndex)
      continue;
    overflown.push(docked[i][0]);
  }
  return overflown;
}
function useOverflow$1(children, activeChildIndex) {
  const [overflown, setOverflown] = reactExports.useState();
  const entryWidths = reactExports.useRef(/* @__PURE__ */ new Map());
  const width = reactExports.useRef(void 0);
  const overflowWidth = reactExports.useRef(void 0);
  const gapSize = reactExports.useRef(0);
  const calculateOverflow = reactExports.useCallback(() => {
    const widths = verifiedMapEntries$1(entryWidths.current);
    if (width.current === void 0 || widths === void 0 || overflowWidth.current === void 0) {
      setOverflown(void 0);
      return;
    }
    const newOverflown = getOverflown(
      width.current,
      [...widths.entries()],
      overflowWidth.current,
      activeChildIndex,
      gapSize.current
    );
    setOverflown((prevOverflown) => {
      return eqlOverflown(prevOverflown, newOverflown) ? prevOverflown : newOverflown;
    });
  }, [activeChildIndex]);
  reactExports.useLayoutEffect(() => {
    const newEntryWidths = /* @__PURE__ */ new Map();
    const array = reactExports.Children.toArray(children);
    for (let i = 0; i < array.length; i++) {
      const child = array[i];
      const key = getChildKey(child, i);
      const lastW = entryWidths.current.get(key);
      newEntryWidths.set(key, lastW);
    }
    entryWidths.current = newEntryWidths;
    calculateOverflow();
  }, [children, calculateOverflow]);
  const handleContainerResize = reactExports.useCallback(
    (w) => {
      const calculate = width.current !== w;
      width.current = w;
      calculate && calculateOverflow();
    },
    [calculateOverflow]
  );
  const handleOverflowResize = reactExports.useCallback(
    (w) => {
      const calculate = overflowWidth.current !== w;
      overflowWidth.current = w;
      calculate && calculateOverflow();
    },
    [calculateOverflow]
  );
  const handleEntryResize = reactExports.useCallback(
    (key) => (w) => {
      const oldW = entryWidths.current.get(key);
      if (oldW === w)
        return;
      entryWidths.current.set(key, w);
      calculateOverflow();
    },
    [calculateOverflow]
  );
  const handleGapResize = reactExports.useCallback(
    (w) => {
      if (gapSize.current === w)
        return;
      gapSize.current = w;
      calculateOverflow();
    },
    [calculateOverflow]
  );
  return [
    overflown,
    handleContainerResize,
    handleOverflowResize,
    handleEntryResize,
    handleGapResize
  ];
}
const DockedToolSettingsEntryContext = reactExports.createContext(null);
DockedToolSettingsEntryContext.displayName = "nz:DockedToolSettingsEntryContext";
function useToolSettingsEntry() {
  return reactExports.useContext(DockedToolSettingsEntryContext);
}
function verifiedMapEntries$1(map) {
  for (const [, val] of map) {
    if (val === void 0)
      return void 0;
  }
  return map;
}
function DefaultPanelContainer(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-toolSettings-docked_container", children: props.children });
}
function eqlOverflown(prev, value) {
  if (!prev)
    return false;
  if (prev.length !== value.length)
    return false;
  for (let i = 0; i < prev.length; i++) {
    const p = prev[i];
    const v = value[i];
    if (p !== v)
      return false;
  }
  return true;
}
function getGapVariable(htmlElement) {
  const gap = getCssVariableAsNumber("gap", htmlElement);
  if (isNaN(gap))
    return 0;
  return gap;
}
try {
  nz:
    DockedToolSettingsEntryContext.displayName = "nz:DockedToolSettingsEntryContext";
  nz:
    DockedToolSettingsEntryContext.__docgenInfo = { "description": "Component that displays tool settings as a bar across the top of the content view.", "displayName": "nz:DockedToolSettingsEntryContext", "props": { "children": { "defaultValue": null, "description": "Tool settings content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "panelContainer": { "defaultValue": null, "description": "Container for overflown entries.", "name": "panelContainer", "required": false, "type": { "name": "ComponentType<{ children?: ReactNode; }>" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetMenu(props) {
  const className = classnames("nz-widget-menu", props.className);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Popup,
    {
      className,
      isOpen: props.open,
      offset: 0,
      onClose: props.onClose,
      position: RelativePosition.BottomLeft,
      style: props.style,
      showShadow: false,
      target: props.target,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-widget-menu_tabs", children: props.children })
    }
  );
}
try {
  WidgetMenu.displayName = "WidgetMenu";
  WidgetMenu.__docgenInfo = { "description": "", "displayName": "WidgetMenu", "props": { "open": { "defaultValue": null, "description": "", "name": "open", "required": false, "type": { "name": "boolean" } }, "onClose": { "defaultValue": null, "description": "", "name": "onClose", "required": false, "type": { "name": "(() => void)" } }, "target": { "defaultValue": null, "description": "", "name": "target", "required": false, "type": { "name": "HTMLElement" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetOverflow(props) {
  const [open, setOpen] = reactExports.useState(false);
  const ref = reactExports.useRef(null);
  const [targetRef, target] = useRefState();
  const resizeObserverRef = useResizeObserver(props.onResize);
  const refs = useRefs(ref, resizeObserverRef);
  const handleClick = reactExports.useCallback((e) => {
    e.preventDefault();
    setOpen((prev) => !prev);
  }, []);
  const handleClose = reactExports.useCallback(() => {
    setOpen(false);
  }, []);
  usePanelPopup(handleClose);
  const className = classnames(
    "nz-widget-overflow",
    props.hidden && "nz-hidden"
  );
  const overflowContext = reactExports.useMemo(() => {
    return {
      close: handleClose
    };
  }, [handleClose]);
  const moreWidgetsTitle = useLabel("moreWidgetsTitle");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, ref: refs, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: "nz-button",
        onClick: handleClick,
        ref: targetRef,
        role: "button",
        tabIndex: -1,
        title: moreWidgetsTitle,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-icon" })
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetOverflowContext.Provider, { value: overflowContext, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      WidgetMenu,
      {
        children: props.children,
        open,
        onClose: handleClose,
        target
      }
    ) })
  ] });
}
const WidgetOverflowContext = reactExports.createContext(void 0);
WidgetOverflowContext.displayName = "nz:WidgetOverflowContext";
function usePanelPopup(onClose) {
  const side = reactExports.useContext(PanelSideContext);
  const collapsed = useLayout(
    (state) => side ? state.panels[side].collapsed : false
  );
  reactExports.useEffect(() => {
    if (collapsed) {
      onClose();
    }
  }, [collapsed, onClose]);
}
try {
  nz:
    WidgetOverflowContext.displayName = "nz:WidgetOverflowContext";
  nz:
    WidgetOverflowContext.__docgenInfo = { "description": "", "displayName": "nz:WidgetOverflowContext", "props": { "hidden": { "defaultValue": null, "description": "", "name": "hidden", "required": false, "type": { "name": "boolean" } }, "onResize": { "defaultValue": null, "description": "", "name": "onResize", "required": false, "type": { "name": "((w: number) => void)" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function TabTarget() {
  const cursorType = reactExports.useContext(CursorTypeContext);
  const draggedTab = useLayout((state) => !!state.draggedTab);
  const draggedWidgetId = reactExports.useContext(DraggedWidgetIdContext);
  const widgetId = reactExports.useContext(WidgetIdContext);
  const tabIndex = useTabIndex();
  const [ref, targeted] = useTarget(
    useTargetArgs$1(widgetId, tabIndex)
  );
  const allowedTarget = useAllowedWidgetTarget(widgetId);
  const hidden = !allowedTarget || !draggedTab && !draggedWidgetId || draggedWidgetId === widgetId;
  const className = classnames(
    "nz-target-tabTarget",
    hidden && "nz-hidden",
    // istanbul ignore next
    targeted && "nz-targeted",
    // istanbul ignore next
    cursorType && getCursorClassName(cursorType)
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className, ref });
}
function useTabIndex() {
  const widgetId = reactExports.useContext(WidgetIdContext);
  const tabId = reactExports.useContext(TabIdContext);
  return useLayout((state) => {
    const widget2 = getWidgetState$1(state, widgetId);
    return widget2.tabs.findIndex((id) => id === tabId);
  });
}
function useTargetArgs$1(widgetId, tabIndex) {
  return reactExports.useMemo(() => {
    return {
      type: "tab",
      widgetId,
      tabIndex
    };
  }, [widgetId, tabIndex]);
}
function WidgetMenuTab(props) {
  const id = reactExports.useContext(TabIdContext);
  const overflowContext = reactExports.useContext(WidgetOverflowContext);
  const showWidgetIcon = reactExports.useContext(ShowWidgetIconContext);
  const { label, iconSpec } = useLayout((state) => {
    const tab2 = state.tabs[id];
    return {
      label: tab2.label,
      iconSpec: tab2.iconSpec
    };
  }, true);
  const closeOverflow = reactExports.useCallback(() => {
    overflowContext.close();
  }, [overflowContext]);
  const ref = useTabInteractions({
    onDragStart: closeOverflow,
    onClick: closeOverflow,
    onDoubleClick: closeOverflow
  });
  const activeTabId = useActiveTabId();
  const active = activeTabId === id;
  const className = classnames(
    "nz-widget-menuTab",
    !showWidgetIcon && "nz-no-icon",
    props.className
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, ref, title: label, children: [
    props.badge && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-badge", children: props.badge }),
    showWidgetIcon && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-icon", children: iconSpec && /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: label }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: classnames(
          "nz-checkmark",
          // istanbul ignore next
          !active && "nz-hidden"
        )
      }
    )
  ] });
}
try {
  WidgetMenuTab.displayName = "WidgetMenuTab";
  WidgetMenuTab.__docgenInfo = { "description": "", "displayName": "WidgetMenuTab", "props": { "badge": { "defaultValue": null, "description": "", "name": "badge", "required": false, "type": { "name": "ReactNode" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetTabProvider({
  id,
  first,
  firstInactive,
  last,
  showOnlyTabIcon
}) {
  const tabNode = reactExports.useContext(TabNodeContext);
  const position = reactExports.useMemo(
    () => ({
      first,
      firstInactive,
      last
    }),
    [first, firstInactive, last]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(TabIdContext.Provider, { value: id, children: /* @__PURE__ */ jsxRuntimeExports.jsx(TabPositionContext.Provider, { value: position, children: /* @__PURE__ */ jsxRuntimeExports.jsx(IconOnlyOnWidgetTabContext.Provider, { value: !!showOnlyTabIcon, children: tabNode }) }) });
}
function WidgetTab(props) {
  const widgetOverflow = reactExports.useContext(WidgetOverflowContext);
  const overflown = !!widgetOverflow;
  if (overflown)
    return /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetMenuTab, { ...props });
  return /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetTabComponent, { ...props });
}
function WidgetTabComponent(props) {
  const id = reactExports.useContext(TabIdContext);
  const { first, firstInactive, last } = reactExports.useContext(TabPositionContext);
  const widgetTabsEntryContext = reactExports.useContext(WidgetTabsEntryContext);
  const side = reactExports.useContext(PanelSideContext);
  const widgetId = reactExports.useContext(WidgetIdContext);
  const showIconOnly = reactExports.useContext(IconOnlyOnWidgetTabContext);
  const showWidgetIcon = reactExports.useContext(ShowWidgetIconContext);
  const iconSpec = useLayout((state) => state.tabs[id].iconSpec);
  const label = useLayout((state) => state.tabs[id].label);
  const activeTabId = useLayout(
    (state) => getWidgetState$1(state, widgetId).activeTabId
  );
  const minimized = useLayout(
    (state) => getWidgetState$1(state, widgetId).minimized
  );
  const maximizedWidget = useIsMaximizedWidget();
  const resizeObserverRef = useResizeObserver(
    widgetTabsEntryContext == null ? void 0 : widgetTabsEntryContext.onResize
  );
  const pointerCaptorRef = useTabInteractions({ clickOnly: maximizedWidget });
  const refs = useRefs(resizeObserverRef, pointerCaptorRef);
  const active = activeTabId === id;
  const className = classnames(
    "nz-widget-tab",
    active && "nz-active",
    void 0 === side && minimized && "nz-minimized",
    first && "nz-first",
    last && "nz-last",
    firstInactive && "nz-first-inactive",
    props.className
  );
  const showLabel = showIconOnly && !iconSpec || showWidgetIcon && !showIconOnly || !showWidgetIcon;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      "data-item-id": id,
      "data-item-type": "widget-tab",
      className,
      ref: refs,
      role: "tab",
      style: props.style,
      title: label,
      tabIndex: 0,
      children: [
        (showWidgetIcon || showIconOnly) && iconSpec && /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec }),
        showLabel && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: label }),
        props.badge && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-badge", children: props.badge }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(TabTarget, {})
      ]
    }
  );
}
function useTabInteractions({
  onClick,
  onDoubleClick,
  onDragStart,
  clickOnly
}) {
  const id = reactExports.useContext(TabIdContext);
  const widgetContext = reactExports.useContext(WidgetContext);
  const measure = reactExports.useContext(MeasureContext);
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const side = reactExports.useContext(PanelSideContext);
  const widgetId = reactExports.useContext(WidgetIdContext);
  const widgetTabsEntryContext = reactExports.useContext(WidgetTabsEntryContext);
  const floatingWidgetId = useFloatingWidgetId();
  const layoutStore = useLayoutStore$1();
  const tabRef = reactExports.useRef(layoutStore.getState().tabs[id]);
  const clickCount = reactExports.useRef(0);
  const doubleClickTimer = reactExports.useRef(new Timer(300));
  const initialPointerPosition = reactExports.useRef();
  const overflown = !widgetTabsEntryContext;
  reactExports.useEffect(
    () => layoutStore.subscribe((state) => {
      tabRef.current = state.tabs[id];
    }),
    [layoutStore, id]
  );
  const handleClick = reactExports.useCallback(() => {
    dispatch({
      type: "WIDGET_TAB_CLICK",
      side,
      widgetId,
      id
    });
    onClick == null ? void 0 : onClick();
  }, [dispatch, widgetId, id, side, onClick]);
  const handleDoubleClick = reactExports.useCallback(() => {
    if (clickOnly)
      return;
    dispatch({
      type: "WIDGET_TAB_DOUBLE_CLICK",
      side,
      widgetId,
      floatingWidgetId,
      id
    });
    onDoubleClick == null ? void 0 : onDoubleClick();
  }, [
    clickOnly,
    dispatch,
    floatingWidgetId,
    widgetId,
    id,
    side,
    onDoubleClick
  ]);
  const handleDragTabStart = useDragTab({
    tabId: id
  });
  const handleDragStart = reactExports.useCallback(
    (pointerPosition) => {
      if (clickOnly)
        return;
      assert(!!ref.current);
      assert(!!initialPointerPosition.current);
      const nzBounds = measure();
      const nzOffset = new Point(-nzBounds.left, -nzBounds.top);
      let bounds = Rectangle.create(ref.current.getBoundingClientRect());
      bounds = bounds.offset(nzOffset);
      const tab2 = tabRef.current;
      const userSized = tab2.userSized || tab2.isFloatingWidgetResizable && /* istanbul ignore next */
      !!tab2.preferredFloatingWidgetSize;
      let position = bounds.topLeft();
      const widgetBounds = widgetContext.measure();
      const widgetSize = restrainInitialWidgetSize(
        widgetBounds.getSize(),
        nzBounds.getSize()
      );
      if (overflown) {
        position = initialPointerPosition.current.offset(nzOffset);
        position = position.offset({ x: -7, y: -7 });
      }
      const dragOffset = initialPointerPosition.current.getOffsetTo(pointerPosition);
      position = position.offset(dragOffset);
      handleDragTabStart({
        initialPointerPosition: Point.create(initialPointerPosition.current),
        pointerPosition,
        widgetSize
      });
      dispatch({
        type: "WIDGET_TAB_DRAG_START",
        floatingWidgetId,
        side,
        widgetId,
        id,
        position,
        userSized
      });
      onDragStart == null ? void 0 : onDragStart();
      initialPointerPosition.current = void 0;
    },
    [
      clickOnly,
      measure,
      widgetContext,
      handleDragTabStart,
      dispatch,
      floatingWidgetId,
      side,
      widgetId,
      id,
      onDragStart,
      overflown
    ]
  );
  const handlePointerDown = reactExports.useCallback(
    (args, e) => {
      e.type === "touchstart" && floatingWidgetId && dispatch({
        type: "FLOATING_WIDGET_BRING_TO_FRONT",
        id: floatingWidgetId
      });
      initialPointerPosition.current = new Point(args.clientX, args.clientY);
    },
    [dispatch, floatingWidgetId]
  );
  const handlePointerMove = reactExports.useCallback(
    (args) => {
      if (!initialPointerPosition.current)
        return;
      const pointerPosition = new Point(args.clientX, args.clientY);
      const distance = initialPointerPosition.current.getDistanceTo(pointerPosition);
      if (distance < 10)
        return;
      handleDragStart(pointerPosition);
    },
    [handleDragStart]
  );
  const handlePointerUp = reactExports.useCallback(() => {
    clickCount.current++;
    initialPointerPosition.current = void 0;
    doubleClickTimer.current.start();
  }, []);
  const pointerCaptorRef = usePointerCaptor(
    handlePointerDown,
    handlePointerMove,
    handlePointerUp
  );
  const ref = reactExports.useRef();
  const refs = useRefs(pointerCaptorRef, ref);
  reactExports.useEffect(() => {
    const timer = doubleClickTimer.current;
    timer.setOnExecute(() => {
      if (clickCount.current === 1)
        handleClick();
      else
        handleDoubleClick();
      clickCount.current = 0;
    });
    const keydown = (e) => {
      if (e.key === " " || e.key === Key_enum.Key.Enter.valueOf()) {
        handleClick();
      }
    };
    const instance = ref.current;
    instance && instance.addEventListener("keydown", keydown);
    return () => {
      timer.setOnExecute(void 0);
      instance && instance.removeEventListener("keydown", keydown);
    };
  }, [handleClick, handleDoubleClick]);
  return refs;
}
const TabPositionContext = reactExports.createContext(
  void 0
);
TabPositionContext.displayName = "nz:TabPositionContext";
const IconOnlyOnWidgetTabContext = reactExports.createContext(false);
IconOnlyOnWidgetTabContext.displayName = "nz:IconOnlyOnWidgetTabContext";
try {
  nz:
    TabPositionContext.displayName = "nz:TabPositionContext";
  nz:
    TabPositionContext.__docgenInfo = { "description": "", "displayName": "nz:TabPositionContext", "props": { "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "showOnlyTabIcon": { "defaultValue": null, "description": "", "name": "showOnlyTabIcon", "required": false, "type": { "name": "boolean" } }, "first": { "defaultValue": null, "description": "", "name": "first", "required": false, "type": { "name": "boolean" } }, "last": { "defaultValue": null, "description": "", "name": "last", "required": false, "type": { "name": "boolean" } }, "firstInactive": { "defaultValue": null, "description": "", "name": "firstInactive", "required": false, "type": { "name": "boolean" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function TabOutline() {
  const hidden = useHidden$1();
  const className = classnames("nz-outline-tabOutline", hidden && "nz-hidden");
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className });
}
function useHidden$1() {
  const widgetId = reactExports.useContext(WidgetIdContext);
  const targeted = useTargeted();
  const activeHomeState = useSendBackHomeState();
  return reactExports.useMemo(() => {
    if ((activeHomeState == null ? void 0 : activeHomeState.widgetId) === widgetId)
      return false;
    if (!targeted)
      return true;
    if (!isWidgetDropTargetState(targeted))
      return true;
    if (targeted.widgetId !== widgetId)
      return true;
    return false;
  }, [targeted, widgetId, activeHomeState]);
}
function TitleBarTarget() {
  const cursorType = reactExports.useContext(CursorTypeContext);
  const draggedWidgetId = reactExports.useContext(DraggedWidgetIdContext);
  const widgetId = reactExports.useContext(WidgetIdContext);
  const activeHomeState = useSendBackHomeState();
  const draggedTab = useLayout((state) => !!state.draggedTab);
  const [ref] = useTarget(useTargetArgs(widgetId));
  const allowedTarget = useAllowedWidgetTarget(widgetId);
  const hidden = !allowedTarget || (!draggedTab && !draggedWidgetId || draggedWidgetId === widgetId) && !((activeHomeState == null ? void 0 : activeHomeState.widgetId) === widgetId);
  const className = classnames(
    "nz-target-titleBarTarget",
    hidden && "nz-hidden",
    // istanbul ignore next
    cursorType && getCursorClassName(cursorType)
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className, ref, children: /* @__PURE__ */ jsxRuntimeExports.jsx(TabOutline, {}) });
}
function useTargetArgs(widgetId) {
  return reactExports.useMemo(() => {
    return {
      type: "widget",
      widgetId
    };
  }, [widgetId]);
}
function WidgetTabs() {
  const side = reactExports.useContext(PanelSideContext);
  const widgetId = reactExports.useContext(WidgetIdContext);
  const showWidgetIcon = reactExports.useContext(ShowWidgetIconContext);
  const tabIds = useLayout((state) => getWidgetState$1(state, widgetId).tabs);
  const activeTabId = useLayout(
    (state) => getWidgetState$1(state, widgetId).activeTabId
  );
  const minimized = useLayout(
    (state) => getWidgetState$1(state, widgetId).minimized
  );
  const [showOnlyTabIcon, setShowOnlyTabIcon] = reactExports.useState(false);
  const activeTabIndex = tabIds.findIndex((id) => id === activeTabId);
  const children = reactExports.useMemo(() => {
    return tabIds.map((id, index, array) => {
      const firstInactive = activeTabIndex + 1 === index;
      return /* @__PURE__ */ jsxRuntimeExports.jsx(
        WidgetTabProvider,
        {
          id,
          first: index === 0,
          firstInactive,
          last: index === array.length - 1,
          showOnlyTabIcon: showOnlyTabIcon && showWidgetIcon
        },
        id
      );
    });
  }, [tabIds, activeTabIndex, showOnlyTabIcon, showWidgetIcon]);
  const [overflown, handleResize, handleOverflowResize, handleEntryResize] = useOverflow$1(children, activeTabIndex);
  const horizontal = side && isHorizontalPanelSide(side);
  const handleContainerResize = reactExports.useCallback(
    (w) => {
      if (showWidgetIcon)
        setShowOnlyTabIcon(tabIds.length * 158 > w);
      handleResize && handleResize(w);
    },
    [handleResize, showWidgetIcon, tabIds]
  );
  const ref = useResizeObserver(handleContainerResize);
  const childrenArray = reactExports.useMemo(
    () => reactExports.Children.toArray(children),
    [children]
  );
  const tabChildren = childrenArray.reduce(
    (acc, child, index) => {
      const key = getChildKey(child, index);
      if (!overflown) {
        acc.push([key, child]);
        return acc;
      }
      if (horizontal && minimized)
        return acc;
      overflown.indexOf(key) < 0 && acc.push([key, child]);
      return acc;
    },
    []
  );
  const panelChildren = tabChildren.length !== childrenArray.length ? childrenArray.map((child, index) => {
    const key = getChildKey(child, index);
    return [key, child];
  }) : [];
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "nz-widget-tabs", ref, role: "tablist", children: [
    tabChildren.map(([key, child], index, array) => {
      return /* @__PURE__ */ jsxRuntimeExports.jsx(
        WidgetTabsEntryProvider,
        {
          children: child,
          id: key,
          lastNotOverflown: index === array.length - 1 && panelChildren.length > 0,
          getOnResize: handleEntryResize
        },
        key
      );
    }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(TitleBarTarget, {}),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      WidgetOverflow,
      {
        hidden: overflown && panelChildren.length === 0,
        onResize: handleOverflowResize,
        children: panelChildren.map(([key, child]) => {
          return /* @__PURE__ */ jsxRuntimeExports.jsx(reactExports.Fragment, { children: child }, key);
        })
      }
    )
  ] });
}
const WidgetTabsEntryContext = reactExports.createContext(void 0);
WidgetTabsEntryContext.displayName = "nz:WidgetTabsEntryContext";
function WidgetTabsEntryProvider(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    WidgetTabsEntryContext.Provider,
    {
      value: {
        lastNotOverflown: props.lastNotOverflown,
        onResize: props.getOnResize(props.id)
      },
      children: props.children
    }
  );
}
try {
  nz:
    WidgetTabsEntryContext.displayName = "nz:WidgetTabsEntryContext";
  nz:
    WidgetTabsEntryContext.__docgenInfo = { "description": "", "displayName": "nz:WidgetTabsEntryContext", "props": { "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "getOnResize": { "defaultValue": null, "description": "", "name": "getOnResize", "required": true, "type": { "name": "(id: string) => (w: number) => void" } }, "lastNotOverflown": { "defaultValue": null, "description": "", "name": "lastNotOverflown", "required": true, "type": { "name": "boolean" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetTabBar(props) {
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const id = reactExports.useContext(WidgetIdContext);
  const floatingWidgetId = useFloatingWidgetId();
  const widgetId = floatingWidgetId === void 0 ? id : floatingWidgetId;
  const handleDoubleClick = reactExports.useCallback(() => {
    floatingWidgetId && dispatch({
      type: "FLOATING_WIDGET_CLEAR_USER_SIZED",
      id: floatingWidgetId
    });
  }, [dispatch, floatingWidgetId]);
  const handleActionAreaClick = useDoubleClick(handleDoubleClick);
  const onDrag = reactExports.useCallback(
    (dragBy) => {
      floatingWidgetId !== void 0 && dispatch({
        type: "WIDGET_DRAG",
        dragBy,
        floatingWidgetId
      });
    },
    [dispatch, floatingWidgetId]
  );
  const onDragEnd = reactExports.useCallback(
    (target) => {
      floatingWidgetId !== void 0 && handleActionAreaClick();
      floatingWidgetId !== void 0 && dispatch({
        type: "WIDGET_DRAG_END",
        floatingWidgetId,
        target
      });
    },
    [dispatch, floatingWidgetId, handleActionAreaClick]
  );
  const handleWidgetDragStart = useDragWidget({
    widgetId,
    onDrag,
    onDragEnd
  });
  const containerRef = reactExports.useRef(null);
  const handleDragStart = reactExports.useCallback(
    (initialPointerPosition, pointerPosition) => {
      handleWidgetDragStart({
        initialPointerPosition,
        pointerPosition
      });
    },
    [handleWidgetDragStart]
  );
  const handleTouchStart = reactExports.useCallback(() => {
    floatingWidgetId && dispatch({
      type: "FLOATING_WIDGET_BRING_TO_FRONT",
      id: floatingWidgetId
    });
  }, [dispatch, floatingWidgetId]);
  const ref = useDrag(
    handleDragStart,
    void 0,
    void 0,
    handleTouchStart,
    handleDoubleClick
  );
  const maximizedWidgetHandle = useMaximizedWidgetTabBarHandle();
  const className = classnames(
    "nz-widget-tabBar",
    props.separator && "nz-separator"
  );
  const handleClassName = classnames("nz-handle", maximizedWidgetHandle);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { ref: containerRef, className, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: handleClassName, ref }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetTabs, {}),
    /* @__PURE__ */ jsxRuntimeExports.jsx(TabBarButtons, {})
  ] });
}
function useDrag(onDragStart, onDrag, onDragEnd, onTouchStart, onDoubleClick) {
  const doubleClickTimer = reactExports.useRef(new Timer(300));
  const clickCount = reactExports.useRef(0);
  const initialPointerPosition = reactExports.useRef();
  reactExports.useEffect(() => {
    const handleExecute = () => {
      if (clickCount.current === 2)
        onDoubleClick && onDoubleClick();
      clickCount.current = 0;
    };
    const timer = doubleClickTimer.current;
    timer.setOnExecute(handleExecute);
    return () => {
      timer.setOnExecute(void 0);
    };
  }, [onDoubleClick]);
  const handlePointerDown = reactExports.useCallback(
    (args, e) => {
      initialPointerPosition.current = new Point(args.clientX, args.clientY);
      e.type === "touchstart" && onTouchStart && onTouchStart();
    },
    [onTouchStart]
  );
  const handlePointerMove = reactExports.useCallback(
    (args) => {
      if (initialPointerPosition.current) {
        onDragStart && onDragStart(
          initialPointerPosition.current,
          new Point(args.clientX, args.clientY)
        );
        initialPointerPosition.current = void 0;
        return;
      }
      onDrag && onDrag(new Point(args.clientX, args.clientY));
    },
    [onDragStart, onDrag]
  );
  const handlePointerUp = reactExports.useCallback(() => {
    clickCount.current++;
    doubleClickTimer.current.start();
    initialPointerPosition.current = void 0;
    onDragEnd && onDragEnd();
  }, [onDragEnd]);
  const ref = usePointerCaptor(
    handlePointerDown,
    handlePointerMove,
    handlePointerUp
  );
  return ref;
}
try {
  WidgetTabBar.displayName = "WidgetTabBar";
  WidgetTabBar.__docgenInfo = { "description": "", "displayName": "WidgetTabBar", "props": { "separator": { "defaultValue": null, "description": "", "name": "separator", "required": false, "type": { "name": "boolean" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useDrag.displayName = "useDrag";
  useDrag.__docgenInfo = { "description": "Hook to control drag interactions.\nStarts drag interaction after pointer moves or after timeout.", "displayName": "useDrag", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetOutline() {
  const hidden = useHidden();
  const className = classnames(
    "nz-outline-widgetOutline",
    hidden && "nz-hidden"
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className });
}
function useHidden() {
  const widgetId = reactExports.useContext(WidgetIdContext);
  const targeted = useTargeted();
  const activeHomeState = useSendBackHomeState();
  return reactExports.useMemo(() => {
    if ((activeHomeState == null ? void 0 : activeHomeState.widgetId) === widgetId)
      return false;
    if (!targeted)
      return true;
    if (isWidgetDropTargetState(targeted) && targeted.widgetId === widgetId)
      return false;
    if (isTabDropTargetState(targeted) && targeted.widgetId === widgetId)
      return false;
    return true;
  }, [targeted, widgetId, activeHomeState]);
}
function WidgetTarget() {
  const widgetId = reactExports.useContext(WidgetIdContext);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(TargetContainer, { className: "nz-target-widgetTarget", direction: "horizontal", children: /* @__PURE__ */ jsxRuntimeExports.jsx(MergeTarget, { widgetId }) });
}
const PanelWidget = reactExports.forwardRef(
  function PanelWidget2({ widgetId }, ref) {
    const side = reactExports.useContext(PanelSideContext);
    const widgetsLength = useLayout((state) => {
      const panel = state.panels[side];
      return panel.widgets.length;
    });
    const minimized = useLayout((state) => {
      const widget2 = getWidgetState$1(state, widgetId);
      return widget2.minimized;
    });
    const horizontal = isHorizontalPanelSide(side);
    const mode = useMode(widgetId);
    const borders = useBorders(widgetId);
    const showTarget = widgetsLength !== 1;
    const className = classnames(
      "nz-widget-panelWidget",
      horizontal && "nz-horizontal",
      `nz-${mode}`,
      borders
    );
    const content = reactExports.useMemo(
      () => /* @__PURE__ */ jsxRuntimeExports.jsxs(WidgetContentContainer, { children: [
        showTarget && /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetTarget, {}),
        /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetOutline, {})
      ] }),
      [showTarget]
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetProvider, { id: widgetId, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Widget, { className, ref, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        WidgetTabBar,
        {
          separator: isHorizontalPanelSide(side) ? true : !minimized
        }
      ),
      content
    ] }) });
  }
);
function findFillWidget(state, side) {
  const panelWidgets = state.panels[side].widgets;
  return panelWidgets.find((widgetId) => {
    const widget2 = getWidgetState$1(state, widgetId);
    if (widget2.minimized)
      return false;
    const tabId = widget2.activeTabId;
    const tab2 = state.tabs[tabId];
    if (!tab2.preferredPanelWidgetSize)
      return true;
    return false;
  });
}
function useMode(widgetId) {
  const side = reactExports.useContext(PanelSideContext);
  return useLayout((state) => {
    const fillWidget = findFillWidget(state, side);
    if (!fillWidget) {
      const panel = state.panels[side];
      for (let i = panel.widgets.length - 1; i >= 0; i--) {
        const wId = panel.widgets[i];
        const w = getWidgetState$1(state, wId);
        if (w.minimized)
          continue;
        if (wId === widgetId)
          return "fill";
        break;
      }
    }
    const widget2 = getWidgetState$1(state, widgetId);
    if (widget2.minimized)
      return "minimized";
    const tabId = widget2.activeTabId;
    const tab2 = state.tabs[tabId];
    return tab2.preferredPanelWidgetSize ? "fit" : "fill";
  });
}
function useBorders(widgetId) {
  const side = reactExports.useContext(PanelSideContext);
  return useLayout((state) => {
    const panels = state.panels;
    const panel = panels[side];
    const toolSettings = state.toolSettings;
    const isHorizontal = isHorizontalPanelSide(panel.side);
    const isVertical = !isHorizontal;
    const isFirst = panel.widgets[0] === widgetId;
    const isLast = panel.widgets[panel.widgets.length - 1] === widgetId;
    const isTopMostPanelBorder = panel.side === "top" || isVertical && !panels.top.span || isVertical && panels.top.span && panels.top.collapsed || isVertical && panels.top.widgets.length === 0;
    let top = true;
    let bottom = true;
    let left = true;
    let right = true;
    if (panel.side === "bottom") {
      bottom = false;
    }
    if (isVertical && isLast) {
      bottom = false;
    }
    if (isTopMostPanelBorder && (toolSettings == null ? void 0 : toolSettings.type) === "docked") {
      top = false;
    }
    if (isVertical && !isFirst) {
      top = false;
    }
    if (isVertical && panels.top.span && !panels.top.collapsed && panels.top.widgets.length > 0) {
      top = false;
    }
    if (isHorizontal && !isFirst) {
      left = false;
    }
    if (isHorizontalPanelState(panel) && !panel.span && isFirst && !panels.left.collapsed && panels.left.widgets.length > 0) {
      left = false;
    }
    if (isHorizontalPanelState(panel) && !panel.span && isLast && !panels.right.collapsed && panels.right.widgets.length > 0) {
      right = false;
    }
    return {
      "nz-border-top": top,
      "nz-border-bottom": bottom,
      "nz-border-left": left,
      "nz-border-right": right
    };
  }, true);
}
try {
  useMode.displayName = "useMode";
  useMode.__docgenInfo = { "description": "", "displayName": "useMode", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useBorders.displayName = "useBorders";
  useBorders.__docgenInfo = { "description": "", "displayName": "useBorders", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  PanelWidget.displayName = "PanelWidget";
  PanelWidget.__docgenInfo = { "description": "", "displayName": "PanelWidget", "props": { "widgetId": { "defaultValue": null, "description": "", "name": "widgetId", "required": true, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function PanelSplitter() {
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const side = reactExports.useContext(PanelSideContext);
  const containerRef = reactExports.useRef(null);
  const splitterProcessingActiveRef = reactExports.useRef(false);
  const isHorizontal = isHorizontalPanelSide(side);
  const getPercentage = reactExports.useCallback(
    (min, max, current) => {
      const range = max - min;
      const adjusted = Math.max(min, Math.min(max, current));
      if (adjusted === min)
        return 0;
      if (adjusted === max)
        return 100;
      const percent = (adjusted - min) * 100 / range;
      return percent;
    },
    []
  );
  const updatePanelSize = reactExports.useCallback(
    (event) => {
      if (!containerRef.current)
        return;
      const parentPanel = containerRef.current.closest(
        ".nz-widgetPanels-panel"
      );
      const sectionToResize = containerRef.current.parentElement;
      if (parentPanel && sectionToResize) {
        const rect = parentPanel.getBoundingClientRect();
        const percent = getPercentage(
          isHorizontal ? rect.left : rect.top,
          isHorizontal ? rect.right : rect.bottom,
          isHorizontal ? event.clientX : event.clientY
        );
        dispatch({
          type: "PANEL_SET_SPLITTER_VALUE",
          side,
          percent
        });
      }
    },
    [getPercentage, isHorizontal, side, dispatch]
  );
  const handlePointerMove = reactExports.useCallback(
    (event) => {
      if (splitterProcessingActiveRef.current) {
        event.preventDefault();
        event.stopPropagation();
        updatePanelSize(event);
      }
    },
    [updatePanelSize]
  );
  const handlePointerUp = reactExports.useCallback(
    (event) => {
      var _a2, _b2;
      updatePanelSize(event);
      event.preventDefault();
      event.stopPropagation();
      (_a2 = containerRef.current) == null ? void 0 : _a2.ownerDocument.removeEventListener(
        "pointermove",
        handlePointerMove
      );
      (_b2 = containerRef.current) == null ? void 0 : _b2.ownerDocument.removeEventListener(
        "pointerup",
        handlePointerUp
      );
    },
    [handlePointerMove, updatePanelSize]
  );
  const handlePointerDownOnSplitter = reactExports.useCallback(
    (event) => {
      var _a2, _b2;
      if (containerRef.current) {
        (_a2 = containerRef.current) == null ? void 0 : _a2.ownerDocument.addEventListener(
          "pointermove",
          handlePointerMove
        );
        (_b2 = containerRef.current) == null ? void 0 : _b2.ownerDocument.addEventListener(
          "pointerup",
          handlePointerUp
        );
        splitterProcessingActiveRef.current = true;
        event.preventDefault();
        event.stopPropagation();
      }
    },
    [handlePointerMove, handlePointerUp]
  );
  const className = isHorizontal ? "nz-horizontal-panel-splitter" : "nz-vertical-panel-splitter";
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      ref: containerRef,
      className,
      onPointerDown: handlePointerDownOnSplitter
    }
  );
}
function PanelSections() {
  const side = reactExports.useContext(PanelSideContext);
  const widgets = useLayout((state) => {
    return state.panels[side].widgets;
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsx(jsxRuntimeExports.Fragment, { children: widgets.map((widgetId, index, array) => {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      PanelSection,
      {
        widgetId,
        index,
        sectionLength: array.length
      },
      widgetId
    );
  }) });
}
function PanelSection({ widgetId, index, sectionLength }) {
  const side = reactExports.useContext(PanelSideContext);
  const splitterPercent = useLayout((state) => {
    return state.panels[side].splitterPercent ?? 50;
  });
  const maximizedSection = useMaximizedSection(widgetId);
  const isMaximizedPanel = !!useMaximizedPanel(side);
  const isHorizontal = isHorizontalPanelSide(side);
  const last = index === sectionLength - 1;
  const className = classnames(
    "nz-widgetPanels-panelSections_section",
    `nz-panel-section-${index}`,
    isHorizontal ? "nz-widgetPanels-horizontal" : "nz-widgetPanels-vertical",
    last && 0 === index && "nz-panel-section-full-size",
    maximizedSection
  );
  const hasSplitter = !last && 0 === index && !isMaximizedPanel;
  const panelStyle = hasSplitter ? getSplitterStyle(isHorizontal, splitterPercent) : void 0;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, style: panelStyle, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(PanelWidget, { widgetId }),
    hasSplitter && /* @__PURE__ */ jsxRuntimeExports.jsx(PanelSplitter, {})
  ] });
}
function getSplitterStyle(isHorizontal, splitterPercent) {
  if (isHorizontal) {
    return {
      width: `${splitterPercent}%`
    };
  }
  return {
    height: `${splitterPercent}%`
  };
}
function useAnimatePanel() {
  const side = reactExports.useContext(PanelSideContext);
  const draggedPanelSide = reactExports.useContext(DraggedPanelSideContext);
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const captured = draggedPanelSide === side;
  const [contentSize, setContentSize] = reactExports.useState();
  const [prepareTransition, setPrepareTransition] = reactExports.useState(false);
  const [transition, setTransition] = reactExports.useState();
  const [panelSize, setPanelSize] = reactExports.useState();
  const [initializing, setInitializing] = reactExports.useState(false);
  const horizontal = isHorizontalPanelSide(side);
  const animateFrom = reactExports.useRef();
  const animateTo = reactExports.useRef(0);
  const maxPanelSize = reactExports.useRef();
  const collapsing = reactExports.useRef();
  const ref = reactExports.useRef(null);
  const panel = useLayout((state) => {
    const p = state.panels[side];
    return {
      collapsed: p.collapsed,
      size: p.size,
      minSize: p.minSize
    };
  }, true);
  const [prevCollapsed, setPrevCollapsed] = reactExports.useState(panel.collapsed);
  if (prevCollapsed !== panel.collapsed) {
    setPrevCollapsed(panel.collapsed);
    let from = animateFrom.current;
    if (from === void 0 && ref.current) {
      const bounds = ref.current.getBoundingClientRect();
      from = getPanelSize(horizontal, bounds);
    }
    animateFrom.current = from;
    setPanelSize(void 0);
    setContentSize(void 0);
    setTransition(void 0);
    setPrepareTransition(true);
    if (panel.collapsed) {
      collapsing.current = "collapsing";
      maxPanelSize.current = from;
    } else {
      collapsing.current = "expanding";
    }
  }
  const [prevSize, setPrevSize] = reactExports.useState(panel.size);
  if (prevSize !== panel.size) {
    setPrevSize(panel.size);
    if (initializing) {
      maxPanelSize.current = void 0;
      setPanelSize(void 0);
      setContentSize(void 0);
      setTransition(void 0);
      setPrepareTransition(true);
    } else if (collapsing.current === "collapsing")
      ;
    else if (!captured && ref.current && prevSize !== void 0) {
      animateFrom.current = getPanelSize(
        horizontal,
        ref.current.getBoundingClientRect()
      );
      setPanelSize(void 0);
      setContentSize(void 0);
      setTransition(void 0);
      setPrepareTransition(true);
    } else {
      setPanelSize(void 0);
      setContentSize(void 0);
      setTransition(void 0);
      animateFrom.current = void 0;
      collapsing.current = void 0;
    }
  }
  reactExports.useLayoutEffect(() => {
    if (panel.size !== void 0 || panel.collapsed)
      return;
    assert(!!ref.current);
    const bounds = ref.current.getBoundingClientRect();
    const newSize = getPanelSize(horizontal, bounds);
    dispatch({
      type: "PANEL_INITIALIZE",
      side,
      size: newSize
    });
    setInitializing(true);
  }, [panel.size, panel.collapsed]);
  reactExports.useLayoutEffect(() => {
    if (!prepareTransition)
      return;
    setPrepareTransition(false);
    assert(!!ref.current);
    animateTo.current = getPanelSize(
      horizontal,
      ref.current.getBoundingClientRect()
    );
    if (animateFrom.current === animateTo.current) {
      maxPanelSize.current = void 0;
      animateFrom.current = void 0;
      collapsing.current = void 0;
      setPanelSize(void 0);
      setContentSize(void 0);
      setTransition(void 0);
      return;
    }
    if (collapsing.current === "expanding" && maxPanelSize.current === void 0) {
      maxPanelSize.current = animateTo.current;
    }
    setPanelSize(animateFrom.current);
    setContentSize(maxPanelSize.current);
    setTransition("init");
  });
  reactExports.useLayoutEffect(() => {
    if (transition !== "init")
      return;
    const handle = window.requestAnimationFrame(() => {
      animateFrom.current = void 0;
      setTransition("transition");
      setPanelSize(animateTo.current);
    });
    return () => {
      window.cancelAnimationFrame(handle);
    };
  });
  reactExports.useEffect(() => {
    setInitializing(false);
  }, [initializing]);
  const handleTransitionEnd = reactExports.useCallback(
    (e) => {
      if (e.target !== ref.current)
        return;
      maxPanelSize.current = void 0;
      collapsing.current = void 0;
      animateFrom.current = void 0;
      setPanelSize(void 0);
      setContentSize(void 0);
      setTransition(void 0);
    },
    []
  );
  const size = reactExports.useMemo(() => {
    if (panelSize !== void 0)
      return panelSize;
    return panel.collapsed ? 0 : panel.size ?? panel.minSize;
  }, [panel.size, panel.collapsed, panel.minSize, panelSize]);
  return { ref, size, handleTransitionEnd, contentSize, transition };
}
function getPanelSize(horizontal, size) {
  return horizontal ? size.height : size.width;
}
function WidgetPanelProvider({ side }) {
  const hasWidgets = useLayout(
    (state) => state.panels[side].widgets.length > 0
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(PanelSideContext.Provider, { value: side, children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "nz-widgetPanels_panelContainer", children: [
    hasWidgets && /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanel, {}),
    /* @__PURE__ */ jsxRuntimeExports.jsx(PanelTargets, {}),
    /* @__PURE__ */ jsxRuntimeExports.jsx(PanelOutline, {})
  ] }) });
}
function WidgetPanel() {
  const side = reactExports.useContext(PanelSideContext);
  const draggedPanelSide = reactExports.useContext(DraggedPanelSideContext);
  const maximizedPanel = useMaximizedPanel(side);
  const spanTop = useLayout((state) => state.panels.top.span);
  const spanBottom = useLayout((state) => state.panels.bottom.span);
  const panel = useLayout((state) => {
    const p = state.panels[side];
    const span = isHorizontalPanelState(p) ? p.span : false;
    return {
      collapsed: p.collapsed,
      widgets: p.widgets,
      resizable: p.resizable,
      span
    };
  }, true);
  const {
    handleTransitionEnd,
    size,
    contentSize,
    transition,
    ref: panelRef
  } = useAnimatePanel();
  const isHorizontal = isHorizontalPanelSide(side);
  const style = reactExports.useMemo(() => {
    if (isHorizontal)
      return {
        height: `${size}px`
      };
    return {
      width: `${size}px`
    };
  }, [size, isHorizontal]);
  const contentStyle = reactExports.useMemo(() => {
    if (contentSize === void 0)
      return void 0;
    if (isHorizontal)
      return {
        minHeight: `${contentSize}px`
      };
    return {
      minWidth: `${contentSize}px`
    };
  }, [contentSize, isHorizontal]);
  const elementRef = reactExports.useRef(null);
  const getBounds = reactExports.useCallback(() => {
    assert(!!elementRef.current);
    return elementRef.current.getBoundingClientRect();
  }, []);
  const widgetPanel = reactExports.useMemo(() => {
    return {
      getBounds
    };
  }, [getBounds]);
  const captured = draggedPanelSide === side;
  const className = classnames(
    "nz-widgetPanels-panel",
    `nz-${side}`,
    panel.collapsed && "nz-collapsed",
    captured && "nz-captured",
    panel.span && "nz-span",
    spanTop && "nz-span-top",
    spanBottom && "nz-span-bottom",
    transition && `nz-${transition}`,
    maximizedPanel
  );
  const singleSection = panel.widgets.length === 1;
  const showSectionTargets = singleSection && !panel.collapsed;
  const ref = useRefs(elementRef, panelRef);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelContext.Provider, { value: widgetPanel, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className,
      ref,
      style: maximizedPanel ? {} : style,
      onTransitionEnd: handleTransitionEnd,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "nz-content", style: maximizedPanel ? {} : contentStyle, children: [
          singleSection && /* @__PURE__ */ jsxRuntimeExports.jsx(SectionOutline, { sectionIndex: 0 }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(PanelSections, {}),
          singleSection && /* @__PURE__ */ jsxRuntimeExports.jsx(SectionOutline, { sectionIndex: 1 })
        ] }),
        showSectionTargets && /* @__PURE__ */ jsxRuntimeExports.jsx(SectionTargets, { widgetId: panel.widgets[0] }),
        panel.resizable && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-grip-container", children: /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelGrip, { className: "nz-grip" }) })
      ]
    }
  ) });
}
const PanelSideContext = reactExports.createContext(
  void 0
);
PanelSideContext.displayName = "nz:PanelSideContext";
const WidgetPanelContext = reactExports.createContext(void 0);
WidgetPanelContext.displayName = "nz:WidgetPanelContext";
const isHorizontalPanelSide = (side) => {
  return side === "top" || side === "bottom";
};
const panelSides = ["left", "right", "top", "bottom"];
try {
  nz:
    PanelSideContext.displayName = "nz:PanelSideContext";
  nz:
    PanelSideContext.__docgenInfo = { "description": "Widget panel component is a side panel with multiple widgets.", "displayName": "nz:PanelSideContext", "props": { "side": { "defaultValue": null, "description": "", "name": "side", "required": true, "type": { "name": "enum", "value": [{ "value": '"top"' }, { "value": '"left"' }, { "value": '"right"' }, { "value": '"bottom"' }] } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetContentContainer(props) {
  const widgetId = reactExports.useContext(WidgetIdContext);
  const widgetContentManager = reactExports.useContext(WidgetContentManagerContext);
  const side = reactExports.useContext(PanelSideContext);
  const { minimized, activeTabId } = useLayout((state) => {
    const widget2 = getWidgetState$1(state, widgetId);
    return { minimized: widget2.minimized, activeTabId: widget2.activeTabId };
  }, true);
  const ref = reactExports.useCallback(
    (instance) => {
      if (!widgetContentManager)
        return;
      widgetContentManager.setContainer(activeTabId, instance);
    },
    [widgetContentManager, activeTabId]
  );
  const className = classnames(
    "nz-widget-contentContainer",
    void 0 === side && minimized && "nz-minimized"
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-content", ref }),
    props.children
  ] });
}
try {
  WidgetContentContainer.displayName = "WidgetContentContainer";
  WidgetContentContainer.__docgenInfo = { "description": "", "displayName": "WidgetContentContainer", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function FloatingWidgetProvider(props) {
  const floatingWidget2 = reactExports.useContext(FloatingWidgetNodeContext);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetProvider, { id: props.id, children: floatingWidget2 });
}
function FloatingWidget$1(props) {
  const uiIsVisible = reactExports.useContext(UiIsVisibleContext);
  const id = useFloatingWidgetId();
  const {
    autoSized,
    bounds,
    hideWithUiWhenFloating,
    isToolSettingsTab,
    minimized,
    resizable
  } = useFloatingWidgetState();
  const hideFloatingWidget = !uiIsVisible && hideWithUiWhenFloating;
  const item = reactExports.useMemo(
    () => ({
      id,
      type: "widget"
    }),
    [id]
  );
  const dragged = useIsDraggedItem(item);
  const ref = useHandleAutoSize(dragged);
  const maximizedWidget = useMaximizedFloatingWidget();
  const className = classnames(
    "nz-widget-floatingWidget",
    dragged && "nz-dragged",
    isToolSettingsTab && "nz-floating-toolSettings",
    minimized && "nz-minimized",
    hideFloatingWidget && "nz-hidden",
    maximizedWidget.classNames
  );
  const style = reactExports.useMemo(() => {
    const boundsRect = Rectangle.create(bounds);
    const { height, width } = boundsRect.getSize();
    const position = boundsRect.topLeft();
    return {
      transform: `translate(${position.x}px, ${position.y}px)`,
      height: minimized || autoSized ? void 0 : height,
      width: autoSized ? void 0 : width,
      maxHeight: autoSized ? "60%" : void 0,
      maxWidth: autoSized ? "60%" : void 0
    };
  }, [autoSized, bounds, minimized]);
  const content = reactExports.useMemo(
    () => /* @__PURE__ */ jsxRuntimeExports.jsxs(WidgetContentContainer, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetTarget, {}),
      /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetOutline, {})
    ] }),
    []
  );
  const handles = reactExports.useMemo(
    () => resizable && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingWidgetHandle, { handle: "left" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingWidgetHandle, { handle: "top" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingWidgetHandle, { handle: "right" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingWidgetHandle, { handle: "bottom" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingWidgetHandle, { handle: "topLeft" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingWidgetHandle, { handle: "topRight" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingWidgetHandle, { handle: "bottomLeft" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingWidgetHandle, { handle: "bottomRight" })
    ] }),
    [resizable]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    Widget,
    {
      className,
      widgetId: id,
      style: {
        ...style,
        ...maximizedWidget.style
      },
      ref,
      onMouseEnter: props.onMouseEnter,
      onMouseLeave: props.onMouseLeave,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetTabBar, { separator: !minimized }),
        content,
        handles
      ]
    }
  );
}
function useFloatingWidgetState() {
  const id = useFloatingWidgetId();
  return useLayout((state) => {
    var _a2;
    const widget2 = getWidgetState$1(state, id);
    const floatingWidget2 = state.floatingWidgets.byId[id];
    const tabs = widget2.tabs;
    const activeTabId = widget2.activeTabId;
    const activeTab = state.tabs[activeTabId];
    const userSized = floatingWidget2.userSized;
    const singleTab = 1 === tabs.length;
    const toolSettingsTabId = (_a2 = state.toolSettings) == null ? void 0 : _a2.tabId;
    const isToolSettingsTab = widget2.tabs[0] === toolSettingsTabId;
    const resizable = !!floatingWidget2.resizable && !isToolSettingsTab;
    const autoSized = singleTab && !userSized;
    return {
      autoSized,
      bounds: floatingWidget2.bounds,
      hideWithUiWhenFloating: activeTab.hideWithUiWhenFloating,
      isToolSettingsTab,
      minimized: widget2.minimized,
      resizable
    };
  }, true);
}
function useHandleAutoSize(dragged) {
  const dragManager = reactExports.useContext(DragManagerContext);
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const measureNz = reactExports.useContext(MeasureContext);
  const id = useFloatingWidgetId();
  const userSized = useLayout(
    (state) => state.floatingWidgets.byId[id].userSized
  );
  const maximizedWidget = useIsMaximizedWidget();
  const updatePosition = reactExports.useRef(true);
  const ref = reactExports.useRef(null);
  reactExports.useLayoutEffect(() => {
    if (!updatePosition.current)
      return;
    if (!dragged)
      return;
    if (maximizedWidget)
      return;
    if (!dragManager.draggedItem)
      return;
    if (!ref.current)
      return;
    let bounds = Rectangle.create(ref.current.getBoundingClientRect());
    const nzBounds = measureNz();
    const pointerPosition = dragManager.draggedItem.info.pointerPosition;
    if (bounds.containsPoint(pointerPosition))
      return;
    if (pointerPosition.x > bounds.right) {
      const offset = pointerPosition.x - bounds.right + 20;
      bounds = bounds.offsetX(offset);
    }
    bounds = bounds.offset({ x: -nzBounds.left, y: -nzBounds.top });
    dispatch({
      type: "FLOATING_WIDGET_SET_BOUNDS",
      id,
      bounds: bounds.toProps()
    });
    dispatch({
      type: "FLOATING_WIDGET_SET_USER_SIZED",
      id,
      userSized: true
    });
    updatePosition.current = false;
  }, [dragged, dragManager, dispatch, id, measureNz, maximizedWidget]);
  const handleResize = reactExports.useCallback(() => {
    if (!ref.current)
      return;
    if (dragged)
      return;
    if (maximizedWidget)
      return;
    if (userSized)
      return;
    let bounds = Rectangle.create(ref.current.getBoundingClientRect());
    const nzBounds = measureNz();
    bounds = bounds.offset({ x: -nzBounds.left, y: -nzBounds.top });
    dispatch({
      type: "FLOATING_WIDGET_SET_BOUNDS",
      id,
      bounds
    });
  }, [dispatch, dragged, id, userSized, measureNz, maximizedWidget]);
  const roRef = useResizeObserver(handleResize);
  const refs = useRefs(ref, roRef);
  return refs;
}
function FloatingWidgetHandle(props) {
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const id = useFloatingWidgetId();
  const { handle } = props;
  const relativePosition = reactExports.useRef(new Point());
  const onDrag = reactExports.useCallback(
    (pointerPosition) => {
      assert(!!ref.current);
      const bounds = Rectangle.create(ref.current.getBoundingClientRect());
      const newRelativePosition = bounds.topLeft().getOffsetTo(pointerPosition);
      const offset = relativePosition.current.getOffsetTo(newRelativePosition);
      const resizeBy = getResizeBy(handle, offset);
      dispatch({
        type: "FLOATING_WIDGET_RESIZE",
        id,
        resizeBy
      });
    },
    [dispatch, handle, id]
  );
  const handleDragStart = useDragResizeHandle({
    handle,
    widgetId: id,
    onDrag
  });
  const handlePointerDown = reactExports.useCallback(
    (args) => {
      assert(!!ref.current);
      const bounds = Rectangle.create(ref.current.getBoundingClientRect());
      const initialPointerPosition = new Point(args.clientX, args.clientY);
      relativePosition.current = bounds.topLeft().getOffsetTo(initialPointerPosition);
      handleDragStart({
        initialPointerPosition,
        pointerPosition: initialPointerPosition
      });
      dispatch({
        type: "FLOATING_WIDGET_BRING_TO_FRONT",
        id
      });
    },
    [dispatch, handleDragStart, id]
  );
  const pointerCaptorRef = usePointerCaptor(handlePointerDown);
  const ref = reactExports.useRef(null);
  const refs = useRefs(ref, pointerCaptorRef);
  const isMaximized = useIsMaximizedWidget();
  const className = classnames(
    "nz-widget-floatingWidget_handle",
    `nz-${handle}`
  );
  if (isMaximized)
    return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className, ref: refs });
}
function getResizeBy(handle, offset) {
  switch (handle) {
    case "left":
      return new Rectangle(-offset.x);
    case "top":
      return new Rectangle(0, -offset.y);
    case "right":
      return new Rectangle(0, 0, offset.x);
    case "bottom":
      return new Rectangle(0, 0, 0, offset.y);
    case "topLeft":
      return new Rectangle(-offset.x, -offset.y);
    case "topRight":
      return new Rectangle(0, -offset.y, offset.x);
    case "bottomLeft":
      return new Rectangle(-offset.x, 0, 0, offset.y);
    case "bottomRight":
      return new Rectangle(0, 0, offset.x, offset.y);
  }
}
function useFloatingWidgetId() {
  const widgetId = reactExports.useContext(WidgetIdContext);
  return useLayout((state) => {
    if (!widgetId)
      return void 0;
    const floatingWidget2 = state.floatingWidgets.byId[widgetId];
    if (!floatingWidget2)
      return void 0;
    return widgetId;
  });
}
function useWidgetAllowedToDock() {
  const id = useFloatingWidgetId();
  return useLayout((state) => {
    var _a2;
    if (id) {
      const widget2 = getWidgetState$1(state, id);
      const activeTab = state.tabs[widget2.activeTabId];
      if (((_a2 = activeTab.allowedPanelTargets) == null ? void 0 : _a2.length) === 0) {
        return false;
      }
    }
    return true;
  });
}
try {
  FloatingWidgetProvider.displayName = "FloatingWidgetProvider";
  FloatingWidgetProvider.__docgenInfo = { "description": "", "displayName": "FloatingWidgetProvider", "props": { "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  FloatingWidget$1.displayName = "FloatingWidget";
  FloatingWidget$1.__docgenInfo = { "description": "", "displayName": "FloatingWidget", "props": { "onMouseEnter": { "defaultValue": null, "description": "", "name": "onMouseEnter", "required": false, "type": { "name": "((event: MouseEvent<HTMLElement, MouseEvent>) => void)" } }, "onMouseLeave": { "defaultValue": null, "description": "", "name": "onMouseLeave", "required": false, "type": { "name": "((event: MouseEvent<HTMLElement, MouseEvent>) => void)" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function NineZone(props) {
  const { children, ...providerProps } = props;
  const measurerRef = reactExports.useRef(null);
  const measure = reactExports.useCallback(() => {
    assert(!!measurerRef.current);
    return Rectangle.create(measurerRef.current.getBoundingClientRect());
  }, []);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(NineZoneProvider, { measure, ...providerProps, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Measurer, { ref: measurerRef }),
    children
  ] });
}
const tab = /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetTab, {});
const floatingWidget = /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingWidget$1, {});
function NineZoneProvider(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutStoreContext.Provider, { value: props.layout, children: /* @__PURE__ */ jsxRuntimeExports.jsx(NineZoneDispatchContext.Provider, { value: props.dispatch, children: /* @__PURE__ */ jsxRuntimeExports.jsx(NineZoneLabelsContext.Provider, { value: props.labels, children: /* @__PURE__ */ jsxRuntimeExports.jsx(UiIsVisibleContext.Provider, { value: !!props.uiIsVisible, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ShowWidgetIconContext.Provider, { value: !!props.showWidgetIcon, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    AutoCollapseUnpinnedPanelsContext.Provider,
    {
      value: !!props.autoCollapseUnpinnedPanels,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetContentNodeContext.Provider, { value: props.widgetContent, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        ToolSettingsNodeContext.Provider,
        {
          value: props.toolSettingsContent,
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(TabNodeContext.Provider, { value: props.tab || tab, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            FloatingWidgetNodeContext.Provider,
            {
              value: props.floatingWidget || floatingWidget,
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                AnimateDockedToolSettingsContext.Provider,
                {
                  value: !!props.animateDockedToolSettings,
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(DragProvider, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(CursorTypeProvider, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetContentManager, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(MeasureContext.Provider, { value: props.measure, children: props.children }) }) }) })
                }
              )
            }
          ) })
        }
      ) })
    }
  ) }) }) }) }) });
}
const NineZoneDispatchContext = reactExports.createContext(
  null
);
NineZoneDispatchContext.displayName = "nz:NineZoneDispatchContext";
const NineZoneLabelsContext = reactExports.createContext(void 0);
NineZoneLabelsContext.displayName = "nz:NineZoneLabelsContext";
const CursorTypeContext = reactExports.createContext(
  void 0
);
CursorTypeContext.displayName = "nz:CursorTypeContext";
const WidgetContentNodeContext = reactExports.createContext(void 0);
WidgetContentNodeContext.displayName = "nz:WidgetContentNodeContext";
const ShowWidgetIconContext = reactExports.createContext(false);
ShowWidgetIconContext.displayName = "nz:ShowWidgetIconContext";
const AutoCollapseUnpinnedPanelsContext = reactExports.createContext(false);
AutoCollapseUnpinnedPanelsContext.displayName = "nz:AutoCollapseUnpinnedPanelsContext";
const AnimateDockedToolSettingsContext = reactExports.createContext(false);
AnimateDockedToolSettingsContext.displayName = "nz:AnimateDockedToolSettingsContext";
const ToolSettingsNodeContext = reactExports.createContext(void 0);
ToolSettingsNodeContext.displayName = "nz:ToolSettingsNodeContext";
const TabNodeContext = reactExports.createContext(void 0);
TabNodeContext.displayName = "nz:TabNodeContext";
const FloatingWidgetNodeContext = reactExports.createContext(void 0);
FloatingWidgetNodeContext.displayName = "nz:FloatingWidgetNodeContext";
const MeasureContext = reactExports.createContext(null);
MeasureContext.displayName = "nz:MeasureContext";
const UiIsVisibleContext = reactExports.createContext(false);
UiIsVisibleContext.displayName = "nz:UiIsVisibleContext";
function CursorTypeProvider(props) {
  const draggedTab = useLayout((state) => !!state.draggedTab);
  const draggedWidgetId = reactExports.useContext(DraggedWidgetIdContext);
  const draggedPanelSide = reactExports.useContext(DraggedPanelSideContext);
  const draggedResizeHandle = reactExports.useContext(DraggedResizeHandleContext);
  let type;
  if (draggedTab || draggedWidgetId)
    type = "grabbing";
  else if (draggedPanelSide)
    type = sideToCursorType(draggedPanelSide);
  else if (draggedResizeHandle)
    type = handleToCursorType(draggedResizeHandle);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(CursorTypeContext.Provider, { value: type, children: props.children });
}
const Measurer = reactExports.forwardRef(function Measurer2(_, ref) {
  const size = reactExports.useRef({});
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const handleResize = reactExports.useCallback(
    (width, height) => {
      if (size.current.width === width && size.current.height === height)
        return;
      size.current.height = height;
      size.current.width = width;
      dispatch({
        type: "RESIZE",
        size: {
          height,
          width
        }
      });
    },
    [dispatch]
  );
  const roRef = useResizeObserver(handleResize);
  const refs = useRefs(ref, roRef);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      ref: refs,
      style: {
        position: "absolute",
        width: "100%",
        height: "100%",
        top: "0",
        left: "0",
        visibility: "hidden"
      }
    }
  );
});
function sideToCursorType(side) {
  switch (side) {
    case "bottom":
    case "top":
      return "ns-resize";
    case "left":
    case "right":
      return "ew-resize";
  }
}
function handleToCursorType(handle) {
  switch (handle) {
    case "bottom":
    case "top":
      return "ns-resize";
    case "left":
    case "right":
      return "ew-resize";
    case "topLeft":
    case "bottomRight":
      return "nwse-resize";
    case "topRight":
    case "bottomLeft":
      return "nesw-resize";
  }
}
function getUniqueId() {
  return Guid.createValue();
}
function useLabel(labelKey) {
  const labels = reactExports.useContext(NineZoneLabelsContext);
  return labels == null ? void 0 : labels[labelKey];
}
try {
  nz:
    NineZoneDispatchContext.displayName = "nz:NineZoneDispatchContext";
  nz:
    NineZoneDispatchContext.__docgenInfo = { "description": "", "displayName": "nz:NineZoneDispatchContext", "props": { "dispatch": { "defaultValue": null, "description": "", "name": "dispatch", "required": true, "type": { "name": "NineZoneDispatch" } }, "layout": { "defaultValue": null, "description": "", "name": "layout", "required": true, "type": { "name": "LayoutStore" } }, "labels": { "defaultValue": null, "description": "", "name": "labels", "required": false, "type": { "name": "NineZoneLabels" } }, "toolSettingsContent": { "defaultValue": null, "description": "", "name": "toolSettingsContent", "required": false, "type": { "name": "ReactNode" } }, "widgetContent": { "defaultValue": null, "description": "", "name": "widgetContent", "required": false, "type": { "name": "ReactNode" } }, "tab": { "defaultValue": null, "description": "", "name": "tab", "required": false, "type": { "name": "ReactNode" } }, "floatingWidget": { "defaultValue": null, "description": "", "name": "floatingWidget", "required": false, "type": { "name": "ReactNode" } }, "showWidgetIcon": { "defaultValue": null, "description": "", "name": "showWidgetIcon", "required": false, "type": { "name": "boolean" } }, "autoCollapseUnpinnedPanels": { "defaultValue": null, "description": "", "name": "autoCollapseUnpinnedPanels", "required": false, "type": { "name": "boolean" } }, "animateDockedToolSettings": { "defaultValue": null, "description": "", "name": "animateDockedToolSettings", "required": false, "type": { "name": "boolean" } }, "uiIsVisible": { "defaultValue": null, "description": "", "name": "uiIsVisible", "required": false, "type": { "name": "boolean" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetContentRenderers() {
  const widgetContent2 = reactExports.useContext(WidgetContentNodeContext);
  const toolSettingsContent2 = reactExports.useContext(ToolSettingsNodeContext);
  const tabIds = useLayout((state) => {
    return Object.keys(state.tabs);
  }, true);
  const toolSettingsTabId = useLayout((state) => {
    var _a2;
    return (_a2 = state.toolSettings) == null ? void 0 : _a2.tabId;
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsx(jsxRuntimeExports.Fragment, { children: tabIds.map((tabId) => {
    const children = tabId === toolSettingsTabId ? toolSettingsContent2 : widgetContent2;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetContentRenderer, { tabId, children }, tabId);
  }) });
}
function WidgetContentRenderer(props) {
  const renderTo = useContainersStore((state) => state.containers[props.tabId]);
  const unloaded = useLayout((state) => !!state.tabs[props.tabId].unloaded);
  const widgetContentManager = reactExports.useContext(WidgetContentManagerContext);
  const container = reactExports.useRef(void 0);
  if (!container.current) {
    container.current = document.createElement("div");
    container.current.classList.add("nz-widget-contentRenderer");
  }
  reactExports.useLayoutEffect(() => {
    if (!widgetContentManager)
      return;
    const parent = renderTo;
    if (parent) {
      while (parent.firstChild) {
        parent.removeChild(parent.firstChild);
      }
      parent.appendChild(container.current);
      widgetContentManager.onRestoreTransientState.raiseEvent(props.tabId);
    }
    return () => {
      for (const child of (parent == null ? void 0 : parent.children) || []) {
        if (child === container.current) {
          parent.removeChild(child);
          return;
        }
      }
    };
  }, [renderTo, widgetContentManager, props.tabId]);
  return reactDomExports.createPortal(
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        style: { height: "100%", width: "100%" },
        id: `content-container:${props.tabId}`,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(TabIdContext.Provider, { value: props.tabId, children: unloaded ? null : props.children })
      }
    ),
    container.current
  );
}
const TabIdContext = reactExports.createContext(
  void 0
);
TabIdContext.displayName = "nz:TabIdContext";
function useTabTransientState(tabId, onSave, onRestore) {
  const widgetContentManager = reactExports.useContext(WidgetContentManagerContext);
  reactExports.useEffect(() => {
    if (!widgetContentManager)
      return;
    return widgetContentManager.onSaveTransientState.addListener((id) => {
      tabId === id && onSave && onSave();
    });
  }, [widgetContentManager, onSave, tabId]);
  reactExports.useEffect(() => {
    if (!widgetContentManager)
      return;
    return widgetContentManager.onRestoreTransientState.addListener((id) => {
      tabId === id && onRestore && onRestore();
    });
  }, [widgetContentManager, onRestore, tabId]);
}
try {
  nz:
    TabIdContext.displayName = "nz:TabIdContext";
  nz:
    TabIdContext.__docgenInfo = { "description": "", "displayName": "nz:TabIdContext", "props": { "tabId": { "defaultValue": null, "description": "", "name": "tabId", "required": true, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const _Tooltip = class _Tooltip extends reactExports.PureComponent {
  constructor() {
    super(...arguments);
    this._lastSize = new Size();
    this._tooltip = reactExports.createRef();
  }
  render() {
    const className = classnames("nz-popup-tooltip", this.props.className);
    const style = {
      ...this.props.style,
      left: this.props.position.x,
      top: this.props.position.y
    };
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, ref: this._tooltip, style, children: [
      this.props.icon !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: this.props.icon }) : void 0,
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-content", children: this.props.children })
    ] });
  }
  componentDidMount() {
    this.onSizeChanged();
  }
  componentDidUpdate() {
    this.onSizeChanged();
  }
  onSizeChanged() {
    const tooltip = this._tooltip.current;
    const rect = tooltip.getBoundingClientRect();
    const size = {
      height: rect.height,
      width: rect.width
    };
    if (this._lastSize.equals(size))
      return;
    this._lastSize = Size.create(size);
    this.props.onSizeChanged && this.props.onSizeChanged(size);
  }
};
_Tooltip.defaultProps = {
  position: {
    x: 0,
    y: 0
  }
};
let Tooltip = _Tooltip;
const offsetAndContainInContainer = (tooltipBounds, containerSize, offset = new Point(20, 20)) => {
  let newBounds = Rectangle.create(tooltipBounds).offset(offset);
  const containerBounds = Rectangle.createFromSize(containerSize);
  if (containerBounds.contains(newBounds))
    return newBounds.topLeft();
  newBounds = newBounds.containIn(containerBounds);
  return newBounds.topLeft();
};
try {
  Tooltip.displayName = "Tooltip";
  Tooltip.__docgenInfo = { "description": "Positionable tooltip component.", "displayName": "Tooltip", "props": { "children": { "defaultValue": null, "description": "Tooltip content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "icon": { "defaultValue": null, "description": "Tooltip icon.", "name": "icon", "required": false, "type": { "name": "ReactNode" } }, "onSizeChanged": { "defaultValue": null, "description": "Function called when the bounds of the tooltip changes.", "name": "onSizeChanged", "required": false, "type": { "name": "((size: SizeProps) => void)" } }, "position": { "defaultValue": { value: "{\n      x: 0,\n      y: 0,\n    }" }, "description": "Position of the tooltip.", "name": "position", "required": false, "type": { "name": "XAndY" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  offsetAndContainInContainer.displayName = "offsetAndContainInContainer";
  offsetAndContainInContainer.__docgenInfo = { "description": "Function to apply offset and contain tooltip bounds in container.", "displayName": "offsetAndContainInContainer", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function Dialog(props) {
  const className = classnames("nz-footer-dialog-dialog", props.className);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, style: props.style, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: props.titleBar }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: props.children })
  ] });
}
try {
  Dialog.displayName = "Dialog";
  Dialog.__docgenInfo = { "description": "Dialog used in footer indicators.", "displayName": "Dialog", "props": { "children": { "defaultValue": null, "description": "Dialog content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "titleBar": { "defaultValue": null, "description": "Dialog title bar. See [[TitleBar]]", "name": "titleBar", "required": false, "type": { "name": "ReactNode" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function TitleBarButton(props) {
  const className = classnames("nz-footer-dialog-button", props.className);
  return (
    // eslint-disable-next-line jsx-a11y/click-events-have-key-events
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className,
        onClick: props.onClick,
        style: props.style,
        title: props.title,
        role: "button",
        tabIndex: -1,
        children: props.children
      }
    )
  );
}
try {
  TitleBarButton.displayName = "TitleBarButton";
  TitleBarButton.__docgenInfo = { "description": "Button used in [[TitleBar]] component.", "displayName": "TitleBarButton", "props": { "children": { "defaultValue": null, "description": "Button content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "onClick": { "defaultValue": null, "description": "Function called when button is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "title": { "defaultValue": null, "description": "Button title.", "name": "title", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function StatusBarDialogTitleBarButton(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(TitleBarButton, { ...props });
}
try {
  StatusBarDialogTitleBarButton.displayName = "StatusBarDialogTitleBarButton";
  StatusBarDialogTitleBarButton.__docgenInfo = { "description": "Dialog component used in a [[StatusBarDialog]] component.", "displayName": "StatusBarDialogTitleBarButton", "props": { "children": { "defaultValue": null, "description": "Button content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "onClick": { "defaultValue": null, "description": "Function called when button is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "title": { "defaultValue": null, "description": "Button title.", "name": "title", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function TitleBar(props) {
  const className = classnames("nz-footer-dialog-titleBar", props.className);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, style: props.style, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "nz-title", children: props.title }),
    props.children
  ] });
}
try {
  TitleBar.displayName = "TitleBar";
  TitleBar.__docgenInfo = { "description": "Title bar of [[Dialog]] component.", "displayName": "TitleBar", "props": { "children": { "defaultValue": null, "description": "Title bar buttons. I.e. [[TitleBarButton]]", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "title": { "defaultValue": null, "description": "Title bar title.", "name": "title", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function StatusBarDialogTitleBar(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(TitleBar, { ...props });
}
try {
  StatusBarDialogTitleBar.displayName = "StatusBarDialogTitleBar";
  StatusBarDialogTitleBar.__docgenInfo = { "description": "Dialog component used in a [[StatusBarDialog]] component.", "displayName": "StatusBarDialogTitleBar", "props": { "children": { "defaultValue": null, "description": "Title bar buttons.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "title": { "defaultValue": null, "description": "Title bar title.", "name": "title", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function StatusBarDialog(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { ...props });
}
((StatusBarDialog2) => {
  StatusBarDialog2.TitleBar = StatusBarDialogTitleBar;
  StatusBarDialog2.TitleBarButton = StatusBarDialogTitleBarButton;
})(StatusBarDialog || (StatusBarDialog = {}));
try {
  StatusBarDialog.displayName = "StatusBarDialog";
  StatusBarDialog.__docgenInfo = { "description": "Dialog component used in a [[StatusBarIndicator]] component.\nComponents used in a [[StatusBarDialog]].", "displayName": "StatusBarDialog", "props": { "children": { "defaultValue": null, "description": "Dialog content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "titleBar": { "defaultValue": null, "description": "Dialog title bar. See [[StatusBarDialog.TitleBar]]", "name": "titleBar", "required": false, "type": { "name": "ReactNode" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  StatusBarDialog.TitleBar.displayName = "StatusBarDialog.TitleBar";
  StatusBarDialog.TitleBar.__docgenInfo = { "description": "Title bar of a [[StatusBarDialog]].", "displayName": "StatusBarDialog.TitleBar", "props": { "children": { "defaultValue": null, "description": "Title bar buttons.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "title": { "defaultValue": null, "description": "Title bar title.", "name": "title", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  StatusBarDialog.TitleBarButton.displayName = "StatusBarDialog.TitleBarButton";
  StatusBarDialog.TitleBarButton.__docgenInfo = { "description": "Title bar button of a [[StatusBarDialog]].", "displayName": "StatusBarDialog.TitleBarButton", "props": { "children": { "defaultValue": null, "description": "Button content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "onClick": { "defaultValue": null, "description": "Function called when button is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "title": { "defaultValue": null, "description": "Button title.", "name": "title", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class CursorPopupUpdatePositionEvent extends UiEvent {
}
class CursorPopupFadeOutEvent extends UiEvent {
}
class CursorPopupsChangedEvent extends UiEvent {
}
const _CursorPopupManager = class _CursorPopupManager {
  /** @internal */
  static clearPopups() {
    this._popups.length = 0;
    _CursorPopupManager._emitPopupsChangedEvent();
  }
  static get popups() {
    return this._popups;
  }
  static get popupCount() {
    return this._popups.length;
  }
  static _emitPopupsChangedEvent() {
    _CursorPopupManager.onCursorPopupsChangedEvent.emit({});
  }
  static pushPopup(popupInfo) {
    _CursorPopupManager._popups.push(popupInfo);
    _CursorPopupManager._emitPopupsChangedEvent();
  }
  /** Called to open popup with a new set of properties
   */
  static open(id, content, pt, offset, relativePosition, priority = 0, options) {
    const popupInfo = _CursorPopupManager._popups.find(
      (info) => id === info.id
    );
    if (popupInfo) {
      popupInfo.content = content;
      popupInfo.offset = Point.create(offset);
      popupInfo.relativePosition = relativePosition;
      popupInfo.renderRelativePosition = relativePosition;
      popupInfo.priority = priority;
      popupInfo.options = options;
    } else {
      const newPopupInfo = {
        id,
        content,
        offset: Point.create(offset),
        relativePosition,
        options,
        renderRelativePosition: relativePosition,
        priority
      };
      _CursorPopupManager.pushPopup(newPopupInfo);
    }
    _CursorPopupManager.updatePosition(pt);
  }
  /** Called to update popup with a new set of properties
   */
  static update(id, content, pt, offset, relativePosition, priority = 0) {
    const popupInfo = _CursorPopupManager._popups.find(
      (info) => id === info.id
    );
    if (popupInfo) {
      popupInfo.content = content;
      popupInfo.offset = Point.create(offset);
      popupInfo.relativePosition = relativePosition;
      popupInfo.renderRelativePosition = relativePosition;
      popupInfo.priority = priority;
    } else {
      Logger.logError(
        UiFramework.loggerCategory(this),
        `update: Could not find popup with id of '${id}'`
      );
    }
    _CursorPopupManager.updatePosition(pt);
  }
  /** Called to move the open popup to new location
   */
  static updatePosition(pt) {
    _CursorPopupManager.resetPopupsRenderRelativePosition(Point.create(pt));
    _CursorPopupManager.onCursorPopupUpdatePositionEvent.emit({ pt });
  }
  /** Called when tool wants to close the popup
   */
  static close(id, apply, fadeOut) {
    const popupInfo = _CursorPopupManager._popups.find(
      (info) => id === info.id
    );
    if (popupInfo) {
      if (popupInfo.options) {
        if (apply && popupInfo.options.onApply)
          popupInfo.options.onApply();
        if (popupInfo.options.onClose)
          popupInfo.options.onClose();
      }
      if (fadeOut) {
        _CursorPopupManager.onCursorPopupFadeOutEvent.emit({ id });
        setTimeout(() => {
          _CursorPopupManager.removePopup(id);
        }, CursorPopup.fadeOutTime);
      } else {
        _CursorPopupManager.removePopup(id);
      }
    } else {
      Logger.logError(
        UiFramework.loggerCategory(this),
        `close: Could not find popup with id of '${id}'`
      );
    }
  }
  static removePopup(id) {
    const index = _CursorPopupManager._popups.findIndex(
      (popupInfo) => id === popupInfo.id
    );
    if (index >= 0) {
      _CursorPopupManager._popups.splice(index, 1);
      _CursorPopupManager._emitPopupsChangedEvent();
    }
  }
  static resetPopupsRenderRelativePosition(pt) {
    _CursorPopupManager.popups.forEach((popupInfo) => {
      popupInfo.renderRelativePosition = popupInfo.relativePosition;
      const flipped = _CursorPopupManager.validateRenderRelativePosition(
        pt,
        popupInfo
      );
      if (flipped) {
        _CursorPopupManager.validateRenderRelativePosition(pt, popupInfo);
      }
    });
  }
  static validateRenderRelativePosition(pt, popupInfo) {
    const popupRect = CursorPopup.getPopupRect(
      pt,
      Point.create(popupInfo.offset),
      popupInfo.popupSize,
      popupInfo.renderRelativePosition
    );
    const { outPos, flipped } = _CursorPopupManager.autoFlip(
      popupInfo.renderRelativePosition,
      popupRect,
      window.innerWidth,
      window.innerHeight
    );
    if (flipped)
      popupInfo.renderRelativePosition = outPos;
    return flipped;
  }
  static autoFlip(inPos, rect, windowWidth, windowHeight) {
    let flipped = false;
    let outPos = inPos;
    if (rect.right > windowWidth) {
      flipped = true;
      switch (inPos) {
        case RelativePosition.Top:
        case RelativePosition.TopRight:
          outPos = RelativePosition.TopLeft;
          break;
        case RelativePosition.Right:
          outPos = RelativePosition.Left;
          break;
        case RelativePosition.Bottom:
        case RelativePosition.BottomRight:
          outPos = RelativePosition.BottomLeft;
          break;
      }
    }
    if (rect.left < 0) {
      flipped = true;
      switch (inPos) {
        case RelativePosition.Top:
        case RelativePosition.TopLeft:
          outPos = RelativePosition.TopRight;
          break;
        case RelativePosition.Left:
          outPos = RelativePosition.Right;
          break;
        case RelativePosition.Bottom:
        case RelativePosition.BottomLeft:
          outPos = RelativePosition.BottomRight;
          break;
      }
    }
    if (rect.bottom > windowHeight) {
      flipped = true;
      switch (inPos) {
        case RelativePosition.Left:
        case RelativePosition.BottomLeft:
          outPos = RelativePosition.TopLeft;
          break;
        case RelativePosition.Bottom:
          outPos = RelativePosition.Top;
          break;
        case RelativePosition.Right:
        case RelativePosition.BottomRight:
          outPos = RelativePosition.TopRight;
          break;
      }
    }
    if (rect.top < 0) {
      flipped = true;
      switch (inPos) {
        case RelativePosition.Left:
        case RelativePosition.TopLeft:
          outPos = RelativePosition.BottomLeft;
          break;
        case RelativePosition.Top:
          outPos = RelativePosition.Bottom;
          break;
        case RelativePosition.Right:
        case RelativePosition.TopRight:
          outPos = RelativePosition.BottomRight;
          break;
      }
    }
    return { outPos, flipped };
  }
};
_CursorPopupManager._popups = new Array();
_CursorPopupManager.onCursorPopupUpdatePositionEvent = new CursorPopupUpdatePositionEvent();
_CursorPopupManager.onCursorPopupFadeOutEvent = new CursorPopupFadeOutEvent();
_CursorPopupManager.onCursorPopupsChangedEvent = new CursorPopupsChangedEvent();
let CursorPopupManager = _CursorPopupManager;
class CursorPopupRenderer extends reactExports.Component {
  constructor(props) {
    super(props);
    this._handlePopupChangedEvent = (_args) => {
      this.forceUpdate();
    };
    this._handleCursorPopupUpdatePositionEvent = (args) => {
      this.setState({ pt: Point.create(args.pt) });
    };
    this.state = {
      pt: new Point()
    };
  }
  render() {
    if (CursorPopupManager.popupCount <= 0)
      return null;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(jsxRuntimeExports.Fragment, { children: this.renderPositions() });
  }
  _handleSizeKnown(popupInfo, size) {
    popupInfo.popupSize = Size.create(size);
  }
  renderPositions() {
    const positions = new Array();
    let renderedPosition;
    const begin = RelativePosition.Left;
    const end = RelativePosition.BottomRight;
    for (let position = begin; position <= end; position++) {
      renderedPosition = this.renderRelativePosition(position);
      if (renderedPosition)
        positions.push(renderedPosition);
    }
    return positions;
  }
  renderRelativePosition(relativePosition) {
    const filteredInfo = CursorPopupManager.popups.filter(
      (popupInfo) => popupInfo.renderRelativePosition === relativePosition
    );
    if (filteredInfo.length > 0) {
      let totalDimension = 0;
      const ascending = relativePosition === RelativePosition.Right || relativePosition === RelativePosition.BottomRight || relativePosition === RelativePosition.Bottom || relativePosition === RelativePosition.BottomLeft;
      const positionPopups = filteredInfo.sort(
        (a, b) => ascending ? a.priority - b.priority : b.priority - a.priority
      ).map((popupInfo, index) => {
        const title = popupInfo.options !== void 0 ? popupInfo.options.title : "";
        const shadow = popupInfo.options !== void 0 ? popupInfo.options.shadow : false;
        let offset = popupInfo.offset;
        if (index > 0)
          offset = this.adjustOffset(
            offset,
            popupInfo.renderRelativePosition,
            totalDimension
          );
        if (popupInfo.popupSize)
          totalDimension += this.getDimension(
            popupInfo.popupSize,
            relativePosition
          );
        return /* @__PURE__ */ jsxRuntimeExports.jsx(
          CursorPopup,
          {
            id: popupInfo.id,
            content: popupInfo.content,
            pt: this.state.pt,
            offset,
            relativePosition: popupInfo.renderRelativePosition,
            title,
            shadow,
            onSizeKnown: (size) => this._handleSizeKnown(popupInfo, size)
          },
          popupInfo.id
        );
      });
      return /* @__PURE__ */ jsxRuntimeExports.jsx(reactExports.Fragment, { children: positionPopups }, relativePosition.toString());
    }
    return null;
  }
  getDimension(popupSize, relativePosition) {
    let dimension = 0;
    switch (relativePosition) {
      case RelativePosition.Top:
      case RelativePosition.Bottom:
      case RelativePosition.TopLeft:
      case RelativePosition.TopRight:
      case RelativePosition.BottomLeft:
      case RelativePosition.BottomRight:
        dimension = popupSize.height;
        break;
      case RelativePosition.Left:
      case RelativePosition.Right:
        dimension = popupSize.width;
        break;
    }
    return dimension;
  }
  adjustOffset(offset, relativePosition, dimension) {
    let outOffset = Point.create(offset.toProps());
    switch (relativePosition) {
      case RelativePosition.Top:
      case RelativePosition.Bottom:
      case RelativePosition.TopLeft:
      case RelativePosition.TopRight:
      case RelativePosition.BottomLeft:
      case RelativePosition.BottomRight:
        outOffset = Point.create({ x: offset.x, y: offset.y + dimension });
        break;
      case RelativePosition.Left:
      case RelativePosition.Right:
        outOffset = Point.create({ x: offset.x + dimension, y: offset.y });
        break;
    }
    return outOffset;
  }
  componentDidMount() {
    CursorPopupManager.onCursorPopupsChangedEvent.addListener(
      this._handlePopupChangedEvent
    );
    CursorPopupManager.onCursorPopupUpdatePositionEvent.addListener(
      this._handleCursorPopupUpdatePositionEvent
    );
  }
  componentWillUnmount() {
    CursorPopupManager.onCursorPopupsChangedEvent.removeListener(
      this._handlePopupChangedEvent
    );
    CursorPopupManager.onCursorPopupUpdatePositionEvent.removeListener(
      this._handleCursorPopupUpdatePositionEvent
    );
  }
}
try {
  CursorPopupUpdatePositionEvent.displayName = "CursorPopupUpdatePositionEvent";
  CursorPopupUpdatePositionEvent.__docgenInfo = { "description": "CursorPopup Update Position Event class.", "displayName": "CursorPopupUpdatePositionEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  CursorPopupFadeOutEvent.displayName = "CursorPopupFadeOutEvent";
  CursorPopupFadeOutEvent.__docgenInfo = { "description": "CursorPopup FadeOut Event class.", "displayName": "CursorPopupFadeOutEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  CursorPopupManager.displayName = "CursorPopupManager";
  CursorPopupManager.__docgenInfo = { "description": "CursorPopup component", "displayName": "CursorPopupManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  CursorPopupManager.onCursorPopupUpdatePositionEvent.displayName = "CursorPopupManager.onCursorPopupUpdatePositionEvent";
  CursorPopupManager.onCursorPopupUpdatePositionEvent.__docgenInfo = { "description": "CursorPopup Update Position Event class.", "displayName": "CursorPopupManager.onCursorPopupUpdatePositionEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  CursorPopupManager.onCursorPopupFadeOutEvent.displayName = "CursorPopupManager.onCursorPopupFadeOutEvent";
  CursorPopupManager.onCursorPopupFadeOutEvent.__docgenInfo = { "description": "CursorPopup FadeOut Event class.", "displayName": "CursorPopupManager.onCursorPopupFadeOutEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  CursorPopupManager.onCursorPopupsChangedEvent.displayName = "CursorPopupManager.onCursorPopupsChangedEvent";
  CursorPopupManager.onCursorPopupsChangedEvent.__docgenInfo = { "description": "Cursor Popups Changed Event class.", "displayName": "CursorPopupManager.onCursorPopupsChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  CursorPopupRenderer.displayName = "CursorPopupRenderer";
  CursorPopupRenderer.__docgenInfo = { "description": "CursorPopupRenderer React component.", "displayName": "CursorPopupRenderer", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
var CursorPopupShow = /* @__PURE__ */ ((CursorPopupShow2) => {
  CursorPopupShow2[CursorPopupShow2["Open"] = 0] = "Open";
  CursorPopupShow2[CursorPopupShow2["FadeOut"] = 1] = "FadeOut";
  return CursorPopupShow2;
})(CursorPopupShow || {});
const _CursorPopup = class _CursorPopup extends reactExports.Component {
  /** @internal */
  constructor(props) {
    super(props);
    this._isMounted = false;
    this._handleCursorPopupFadeOutEvent = (args) => {
      if (this.props.id === args.id) {
        if (this._isMounted)
          this.setState({
            showPopup: 1
            /* FadeOut */
          });
      }
    };
    this.state = {
      showPopup: 0,
      size: new Size(-1, -1)
    };
  }
  componentDidMount() {
    this._isMounted = true;
    CursorPopupManager.onCursorPopupFadeOutEvent.addListener(
      this._handleCursorPopupFadeOutEvent
    );
  }
  componentWillUnmount() {
    this._isMounted = false;
    CursorPopupManager.onCursorPopupFadeOutEvent.removeListener(
      this._handleCursorPopupFadeOutEvent
    );
  }
  /** @internal */
  static getPopupRect(pt, offset, popupSize, relativePosition) {
    const popupRect = { top: 0, left: 0, right: 0, bottom: 0 };
    if (popupSize === void 0)
      return popupRect;
    if (typeof relativePosition === "string") {
      const placement = relativePosition.split("-");
      const [mainPlacement, subPlacement] = placement;
      switch (mainPlacement) {
        case "top":
          popupRect.left = pt.x - popupSize.width / 2 - offset.x;
          popupRect.top = pt.y - offset.y + popupSize.height;
          break;
        case "left":
          popupRect.left = pt.x - popupSize.width - offset.x;
          popupRect.top = pt.y + popupSize.height / 2 - offset.y;
          break;
        case "right":
          popupRect.left = pt.x + offset.x;
          popupRect.top = pt.y + popupSize.height / 2 - offset.y;
          break;
        case "bottom":
          popupRect.left = pt.x - popupSize.width / 2 - offset.x;
          popupRect.top = pt.y + offset.y;
          break;
      }
      if (subPlacement === "start") {
        switch (mainPlacement) {
          case "top":
          case "bottom":
            popupRect.left = pt.x - offset.x;
            break;
          case "left":
          case "right":
            popupRect.top = pt.y - offset.y;
            break;
        }
      } else if (subPlacement === "end") {
        switch (mainPlacement) {
          case "top":
          case "bottom":
            popupRect.left = pt.x - popupSize.width - offset.x;
            break;
          case "left":
          case "right":
            popupRect.top = popupSize.height + pt.y - offset.y;
            break;
        }
      }
      popupRect.right = popupRect.left + popupSize.width;
      popupRect.bottom = popupRect.top + popupSize.height;
    } else {
      switch (relativePosition) {
        case RelativePosition.Top:
          popupRect.bottom = pt.y - offset.y;
          popupRect.left = pt.x - popupSize.width / 2;
          popupRect.top = popupRect.bottom - popupSize.height;
          popupRect.right = popupRect.left + popupSize.width;
          break;
        case RelativePosition.Left:
          popupRect.right = pt.x - offset.x;
          popupRect.top = pt.y - popupSize.height / 2;
          popupRect.left = popupRect.right - popupSize.width;
          popupRect.bottom = popupRect.top + popupSize.height;
          break;
        case RelativePosition.Right:
          popupRect.left = pt.x + offset.x;
          popupRect.top = pt.y - popupSize.height / 2;
          popupRect.right = popupRect.left + popupSize.width;
          popupRect.bottom = popupRect.top + popupSize.height;
          break;
        case RelativePosition.Bottom:
          popupRect.top = pt.y + offset.y;
          popupRect.left = pt.x - popupSize.width / 2;
          popupRect.bottom = popupRect.top + popupSize.height;
          popupRect.right = popupRect.left + popupSize.width;
          break;
        case RelativePosition.TopLeft:
          popupRect.bottom = pt.y - offset.y;
          popupRect.right = pt.x - offset.x;
          popupRect.top = popupRect.bottom - popupSize.height;
          popupRect.left = popupRect.right - popupSize.width;
          break;
        case RelativePosition.TopRight:
          popupRect.bottom = pt.y - offset.y;
          popupRect.left = pt.x + offset.x;
          popupRect.top = popupRect.bottom - popupSize.height;
          popupRect.right = popupRect.left + popupSize.width;
          break;
        case RelativePosition.BottomLeft:
          popupRect.top = pt.y + offset.y;
          popupRect.right = pt.x - offset.x;
          popupRect.bottom = popupRect.top + popupSize.height;
          popupRect.left = popupRect.right - popupSize.width;
          break;
        case RelativePosition.BottomRight:
          popupRect.top = pt.y + offset.y;
          popupRect.left = pt.x + offset.x;
          popupRect.bottom = popupRect.top + popupSize.height;
          popupRect.right = popupRect.left + popupSize.width;
          break;
      }
    }
    return popupRect;
  }
  setDivRef(div) {
    if (div) {
      const rect = div.getBoundingClientRect();
      const newSize = new Size(rect.width, rect.height);
      if (!this.state.size.equals(newSize)) {
        if (this.props.onSizeKnown)
          this.props.onSizeKnown(newSize);
        if (this._isMounted)
          this.setState({ size: newSize });
      }
    }
  }
  /** @internal */
  render() {
    const popupRect = _CursorPopup.getPopupRect(
      this.props.pt,
      this.props.offset,
      this.state.size,
      // eslint-disable-next-line deprecation/deprecation
      this.props.placement || this.props.relativePosition
    );
    const positioningStyle = {
      left: popupRect.left,
      top: popupRect.top
    };
    const classNames = classnames(
      "uifw-cursorpopup",
      this.props.shadow && "core-popup-shadow",
      this.state.showPopup === 1 && "uifw-cursorpopup-fadeOut"
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: classNames,
        ref: (e) => this.setDivRef(e),
        style: positioningStyle,
        children: [
          this.props.title && /* @__PURE__ */ jsxRuntimeExports.jsx(
            StatusBarDialog.TitleBar,
            {
              title: this.props.title,
              className: "uifw-cursorpopup-title"
            }
          ),
          this.props.content
        ]
      }
    );
  }
};
_CursorPopup.fadeOutTime = 500;
let CursorPopup = _CursorPopup;
function CursorPopupContent(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Div, { ...props, mainClassName: "uifw-cursorpopup-content" });
}
try {
  CursorPopupContent.displayName = "CursorPopupContent";
  CursorPopupContent.__docgenInfo = { "description": "CursorPopup content with padding", "displayName": "CursorPopupContent", "props": { "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  CursorPopupShow.displayName = "CursorPopupShow";
  CursorPopupShow.__docgenInfo = { "description": "Enum for showing CursorPopup", "displayName": "CursorPopupShow", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  CursorPopup.displayName = "CursorPopup";
  CursorPopup.__docgenInfo = { "description": "CursorPopup React component", "displayName": "CursorPopup", "props": { "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "content": { "defaultValue": null, "description": "", "name": "content", "required": true, "type": { "name": "ReactNode" } }, "pt": { "defaultValue": null, "description": "", "name": "pt", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "offset": { "defaultValue": null, "description": "", "name": "offset", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "title": { "defaultValue": null, "description": "", "name": "title", "required": false, "type": { "name": "string" } }, "shadow": { "defaultValue": null, "description": "", "name": "shadow", "required": false, "type": { "name": "boolean" } }, "onSizeKnown": { "defaultValue": null, "description": "Function called when size is known.", "name": "onSizeKnown", "required": false, "type": { "name": "((size: SizeProps) => void)" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } }, "placement": { "defaultValue": null, "description": "", "name": "placement", "required": false, "type": { "name": "enum", "value": [{ "value": '"top"' }, { "value": '"left"' }, { "value": '"right"' }, { "value": '"bottom"' }, { "value": '"top-start"' }, { "value": '"top-end"' }, { "value": '"left-start"' }, { "value": '"left-end"' }, { "value": '"right-start"' }, { "value": '"right-end"' }, { "value": '"bottom-start"' }, { "value": '"bottom-end"' }] } }, "relativePosition": { "defaultValue": null, "description": "", "name": "relativePosition", "required": false, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }, { "value": "2" }, { "value": "3" }, { "value": "4" }, { "value": "5" }, { "value": "6" }, { "value": "7" }, { "value": "8" }, { "value": "9" }] } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class PositionPopup extends reactExports.PureComponent {
  constructor(props) {
    super(props);
  }
  render() {
    const { point, className, style, onSizeKnown, ...props } = this.props;
    const divStyle = {
      ...style,
      top: point.y,
      left: point.x
    };
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        ...props,
        className: classnames("uifw-position-popup", className),
        style: divStyle,
        ref: (e) => this.setDivRef(e),
        children: this.props.children
      }
    );
  }
  setDivRef(div) {
    if (div) {
      const rect = div.getBoundingClientRect();
      const size = new Size(rect.width, rect.height);
      if (this.props.onSizeKnown)
        this.props.onSizeKnown(size);
    }
  }
}
function PositionPopupContent(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Div, { ...props, mainClassName: "uifw-position-popup-content" });
}
try {
  PositionPopupContent.displayName = "PositionPopupContent";
  PositionPopupContent.__docgenInfo = { "description": "PositionPopup content with padding", "displayName": "PositionPopupContent", "props": { "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  PositionPopup.displayName = "PositionPopup";
  PositionPopup.__docgenInfo = { "description": "Popup component at screen position", "displayName": "PositionPopup", "props": { "point": { "defaultValue": null, "description": "Center point", "name": "point", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "onSizeKnown": { "defaultValue": null, "description": "Function called when size is known.", "name": "onSizeKnown", "required": false, "type": { "name": "((size: SizeProps) => void)" } }, "children": { "defaultValue": null, "description": "Content", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function isToolbarActionItem(item) {
  return "execute" in item;
}
function isToolbarGroupItem(item) {
  return "items" in item;
}
function isToolbarCustomItem(item) {
  return !isToolbarActionItem(item) && !isToolbarGroupItem(item);
}
function toUIAToolbarItem(item) {
  if (isToolbarCustomItem(item)) {
    const customItem = {
      ...item,
      badgeType: item.badge,
      isCustom: true,
      icon: item.icon,
      panelContentNode: item.panelContent
    };
    return customItem;
  }
  if (isToolbarGroupItem(item)) {
    return {
      ...item,
      items: item.items.map(toUIAToolbarItem),
      badgeType: item.badge
    };
  }
  return {
    ...item,
    badgeType: item.badge
  };
}
function isConditionalValue(conditionalProp) {
  return typeof conditionalProp === "object" && conditionalProp && "syncEventIds" in conditionalProp && "refresh" in conditionalProp && "value" in conditionalProp;
}
function useConditionalValue(conditionalProp) {
  const subscribe = reactExports.useCallback(
    (onStoreChange) => {
      if (isConditionalValue(conditionalProp)) {
        return SyncUiEventDispatcher.onSyncUiEvent.addListener(
          ({ eventIds }) => {
            if (!conditionalProp.syncEventIds.some((id) => eventIds.has(id)))
              return;
            conditionalProp.refresh();
            onStoreChange();
          }
        );
      }
      return () => {
      };
    },
    [conditionalProp]
  );
  const getSnapshot = reactExports.useCallback(() => {
    if (isConditionalValue(conditionalProp))
      return conditionalProp.value;
    return conditionalProp;
  }, [conditionalProp]);
  return shimExports.useSyncExternalStore(subscribe, getSnapshot);
}
try {
  useConditionalValue.displayName = "useConditionalValue";
  useConditionalValue.__docgenInfo = { "description": "", "displayName": "useConditionalValue", "props": { "syncEventIds": { "defaultValue": null, "description": "", "name": "syncEventIds", "required": true, "type": { "name": "string[]" } }, "refresh": { "defaultValue": null, "description": "", "name": "refresh", "required": true, "type": { "name": "() => void" } }, "value": { "defaultValue": null, "description": "", "name": "value", "required": true, "type": { "name": "T" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function Badge({ badge }) {
  const badgeRenderer = BadgeUtilities.getComponentForBadgeType(badge);
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-toolbar-group-badge", children: badgeRenderer });
}
try {
  Badge.displayName = "Badge";
  Badge.__docgenInfo = { "description": "", "displayName": "Badge", "props": { "badge": { "defaultValue": null, "description": "Describes badge. Renders no badge if not specified.", "name": "badge", "required": false, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }, { "value": "2" }] } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const Item = reactExports.forwardRef(
  function Item2(props, ref) {
    const { item, ...other } = props;
    const label = useConditionalValue(item.label);
    const isDisabled = useConditionalValue(item.isDisabled);
    const isHidden = useConditionalValue(item.isHidden);
    const iconSpec = useConditionalValue(item.icon);
    const labelProps = useLabelProps();
    if (isHidden)
      return null;
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      IconButton,
      {
        className: props.className,
        styleType: "borderless",
        disabled: isDisabled,
        isActive: item.isActive,
        label,
        labelProps,
        style: props.style,
        ref,
        ...other,
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { badge: item.badge }),
          props.children
        ]
      }
    );
  }
);
function useExpandsTo() {
  const context = reactExports.useContext(ToolbarContext);
  const { expandsTo } = context;
  return expandsTo;
}
function useLabelProps() {
  const context = reactExports.useContext(ToolbarContext);
  const [internalVisible, setInternalVisible] = reactExports.useState(false);
  const visible = (context == null ? void 0 : context.popoverOpen) ? false : internalVisible;
  const placement = useExpandsTo();
  return {
    placement,
    visible,
    onVisibleChange: setInternalVisible
  };
}
try {
  Item.displayName = "Item";
  Item.__docgenInfo = { "description": "", "displayName": "Item", "props": { "size": { "defaultValue": null, "description": "Modify size of the button.", "name": "size", "required": false, "type": { "name": "enum", "value": [{ "value": '"small"' }, { "value": '"large"' }] } }, "label": { "defaultValue": null, "description": "Name of the button, shown in a tooltip and exposed to assistive technologies.", "name": "label", "required": false, "type": { "name": "ReactNode" } }, "isActive": { "defaultValue": { value: "false" }, "description": "Button gets active style.", "name": "isActive", "required": false, "type": { "name": "boolean" } }, "as": { "defaultValue": null, "description": "", "name": "as", "required": false, "type": { "name": "enum", "value": [{ "value": '"button"' }] } }, "item": { "defaultValue": null, "description": "", "name": "item", "required": true, "type": { "name": "ToolbarItem" } }, "htmlDisabled": { "defaultValue": null, "description": "Built-in html `disabled` attribute", "name": "htmlDisabled", "required": false, "type": { "name": "boolean" } }, "styleType": { "defaultValue": { value: "'default'" }, "description": "Style of the button.\nUse 'borderless' to hide outline.", "name": "styleType", "required": false, "type": { "name": "enum", "value": [{ "value": '"default"' }, { "value": '"cta"' }, { "value": '"high-visibility"' }, { "value": '"borderless"' }] } }, "labelProps": { "defaultValue": null, "description": "Props passed to the Tooltip that contains the `label`.\nCan be used for customizing the tooltip's `placement`, etc.", "name": "labelProps", "required": false, "type": { "name": 'Omit<Omit<Omit<Omit<DetailedHTMLProps<HTMLAttributes<HTMLDivElement>, HTMLDivElement>, "ref"> & { ...; }, "content" | ... 3 more ... | keyof TooltipOptions> & { ...; } & PortalProps & TooltipOptions & { ...; }, "ref">, "content" | ... 2 more ... | "ariaStrategy">' } }, "iconProps": { "defaultValue": null, "description": "Passes props to IconButton icon.", "name": "iconProps", "required": false, "type": { "name": "DetailedHTMLProps<HTMLAttributes<HTMLSpanElement>, HTMLSpanElement>" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function ExpandIndicator() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-toolbar-group-expandIndicator" });
}
const GroupItem = reactExports.forwardRef(
  function GroupItem2({ item }, ref) {
    const placement = usePopoverPlacement();
    const context = reactExports.useContext(ToolbarContext);
    const overflowContext = reactExports.useContext(ToolGroupOverflowContext);
    if (overflowContext) {
      return /* @__PURE__ */ jsxRuntimeExports.jsx(GroupMenuItem, { item, onClose: overflowContext.onClose });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      DropdownMenu,
      {
        menuItems: (close) => {
          return toMenuItems(item, close);
        },
        placement,
        onVisibleChange: (newVisible) => {
          context == null ? void 0 : context.setPopoverOpen(newVisible);
        },
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(Item, { item, ref, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ExpandIndicator, {}) })
      }
    );
  }
);
function usePopoverPlacement() {
  const context = reactExports.useContext(ToolbarContext);
  if (!context)
    return void 0;
  return `${context.expandsTo}-${context.panelAlignment}`;
}
function GroupMenuItem({ item, onClose }) {
  const iconSpec = useConditionalValue(item.icon);
  const label = useConditionalValue(item.label);
  const isDisabled = useConditionalValue(item.isDisabled);
  const isHidden = useConditionalValue(item.isHidden);
  const isActive = useConditionalValue(item.isActive);
  if (isHidden) {
    return null;
  }
  const subMenuItems = isDisabled ? void 0 : toMenuItems(item, onClose);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    MenuItem$1,
    {
      startIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec }),
      disabled: isDisabled,
      subMenuItems,
      onClick: () => {
        if (isToolbarActionItem(item)) {
          item.execute();
          onClose == null ? void 0 : onClose();
        }
      },
      isSelected: isActive,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { badge: item.badge }),
        label
      ]
    }
  );
}
function toMenuItems(item, onClose) {
  if (isToolbarGroupItem(item)) {
    return item.items.map((groupItem) => {
      return /* @__PURE__ */ jsxRuntimeExports.jsx(GroupMenuItem, { item: groupItem, onClose }, groupItem.id);
    });
  }
  if (isToolbarCustomItem(item)) {
    return [
      /* @__PURE__ */ jsxRuntimeExports.jsx(MenuExtraContent, { children: item.panelContent }, item.id)
    ];
  }
  return [];
}
try {
  GroupMenuItem.displayName = "GroupMenuItem";
  GroupMenuItem.__docgenInfo = { "description": "", "displayName": "GroupMenuItem", "props": { "item": { "defaultValue": null, "description": "", "name": "item", "required": true, "type": { "name": "ToolbarItem" } }, "onClose": { "defaultValue": null, "description": "", "name": "onClose", "required": false, "type": { "name": "(() => void)" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  GroupItem.displayName = "GroupItem";
  GroupItem.__docgenInfo = { "description": "", "displayName": "GroupItem", "props": { "item": { "defaultValue": null, "description": "", "name": "item", "required": true, "type": { "name": "ToolbarGroupItem" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const ToolGroupOverflowContext = reactExports.createContext(void 0);
const OverflowButton = reactExports.forwardRef(function OverflowButton2(props, ref) {
  const placement = usePopoverPlacement();
  const labelProps = useLabelProps();
  const context = reactExports.useContext(ToolbarContext);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    DropdownMenu,
    {
      menuItems: (close) => {
        return [
          /* @__PURE__ */ jsxRuntimeExports.jsx(OverflowMenu, { onClose: close, children: props.children }, 0)
        ];
      },
      placement,
      onVisibleChange: (newVisible) => {
        context == null ? void 0 : context.setPopoverOpen(newVisible);
      },
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        IconButton,
        {
          ref,
          label: "More",
          labelProps,
          styleType: "borderless",
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgMore, {})
        }
      )
    }
  );
});
function OverflowMenu({ children, onClose }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ToolGroupOverflowContext.Provider, { value: { onClose }, children });
}
try {
  OverflowButton.displayName = "OverflowButton";
  OverflowButton.__docgenInfo = { "description": "", "displayName": "OverflowButton", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const ActionItem = reactExports.forwardRef(
  function ActionItem2({ item }, ref) {
    const overflowContext = reactExports.useContext(ToolGroupOverflowContext);
    if (overflowContext) {
      return /* @__PURE__ */ jsxRuntimeExports.jsx(GroupMenuItem, { item, onClose: overflowContext.onClose });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Item,
      {
        ref,
        item,
        onClick: () => {
          item.execute();
        }
      }
    );
  }
);
try {
  ActionItem.displayName = "ActionItem";
  ActionItem.__docgenInfo = { "description": "", "displayName": "ActionItem", "props": { "item": { "defaultValue": null, "description": "", "name": "item", "required": true, "type": { "name": "ToolbarActionItem" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const CustomItem = reactExports.forwardRef(
  function CustomItem2({ item }, ref) {
    const placement = usePopoverPlacement();
    const context = reactExports.useContext(ToolbarContext);
    const overflowContext = reactExports.useContext(ToolGroupOverflowContext);
    if (overflowContext) {
      return /* @__PURE__ */ jsxRuntimeExports.jsx(GroupMenuItem, { item, onClose: overflowContext.onClose });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      DropdownMenu,
      {
        menuItems: () => {
          return [
            /* @__PURE__ */ jsxRuntimeExports.jsx(MenuExtraContent, { children: item.panelContent }, 0)
          ];
        },
        placement,
        onVisibleChange: (newVisible) => {
          context == null ? void 0 : context.setPopoverOpen(newVisible);
        },
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(Item, { ref, item, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ExpandIndicator, {}) })
      }
    );
  }
);
try {
  CustomItem.displayName = "CustomItem";
  CustomItem.__docgenInfo = { "description": "", "displayName": "CustomItem", "props": { "item": { "defaultValue": null, "description": "", "name": "item", "required": true, "type": { "name": "ToolbarCustomItem" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const useOverflow = (allItems, orientation, getItemSize, getOverflowSize) => {
  const [visible, setVisible] = reactExports.useState(allItems.length);
  const [calculate, setCalculate] = reactExports.useState(false);
  const containerSizeRef = reactExports.useRef();
  const componentSizeRef = reactExports.useRef();
  const [prevOrientation, setPrevOrientation] = reactExports.useState(orientation);
  if (prevOrientation !== orientation) {
    setPrevOrientation(orientation);
    setCalculate(true);
  }
  reactExports.useLayoutEffect(() => {
    setCalculate(false);
    if (!calculate)
      return;
    const containerSize = containerSizeRef.current ? getLength(containerSizeRef.current, orientation) : void 0;
    if (containerSize === void 0)
      return;
    const componentSize = componentSizeRef.current ? getLength(componentSizeRef.current, orientation) : void 0;
    if (componentSize === void 0)
      return;
    const itemSizes = allItems.map((item) => {
      const size = getItemSize(item);
      return getLength(size, orientation);
    });
    const overflowSize = getLength(getOverflowSize(), orientation);
    const otherSize = itemSizes.reduce((acc, size) => acc + size, 0);
    if (otherSize < containerSize) {
      setVisible(itemSizes.length);
      return;
    }
    let totalSize = overflowSize;
    if (totalSize >= containerSize) {
      setVisible(0);
      return;
    }
    let newVisible = 0;
    for (const size of itemSizes) {
      totalSize += size;
      if (totalSize >= containerSize) {
        break;
      }
      newVisible++;
    }
    setVisible(newVisible);
  }, [calculate, allItems, getItemSize, getOverflowSize, orientation]);
  const containerRoRef = useResizeObserver(
    reactExports.useCallback(
      (width, height) => {
        const length = containerSizeRef.current ? getLength(containerSizeRef.current, orientation) : void 0;
        containerSizeRef.current = { width, height };
        const newLength = getLength(containerSizeRef.current, orientation);
        if (newLength === length)
          return;
        setCalculate(true);
      },
      [orientation]
    )
  );
  const componentRoRef = useResizeObserver(
    reactExports.useCallback(
      (width, height) => {
        const length = componentSizeRef.current ? getLength(componentSizeRef.current, orientation) : void 0;
        componentSizeRef.current = { width, height };
        const newLength = getLength(componentSizeRef.current, orientation);
        if (newLength === length)
          return;
        setCalculate(true);
      },
      [orientation]
    )
  );
  const overflow = calculate || visible < allItems.length;
  const visibleItems = calculate ? allItems.length : visible;
  return [containerRoRef, componentRoRef, visibleItems, overflow];
};
function getLength(size, orientation) {
  return orientation === "horizontal" ? size.width : size.height;
}
function ToolGroup({ children, className, ...props }) {
  const context = reactExports.useContext(ToolbarContext);
  const expandsTo = (context == null ? void 0 : context.expandsTo) ?? "bottom";
  const panelAlignment = (context == null ? void 0 : context.panelAlignment) ?? "start";
  const orientation = getOrientation(expandsTo);
  const childrenArray = reactExports.Children.toArray(children);
  const keyToChildMap = childrenArray.reduce(
    (acc, child, index) => {
      const childKey = getChildKey(child, index);
      acc.set(childKey, child);
      return acc;
    },
    /* @__PURE__ */ new Map()
  );
  const childrenKeys = Array.from(keyToChildMap.keys());
  const itemRefs = reactExports.useRef(/* @__PURE__ */ new Map());
  const overflowRef = reactExports.useRef(null);
  const getItemSize = reactExports.useCallback((item) => {
    const element = itemRefs.current.get(item);
    if (!element)
      return { width: 0, height: 0 };
    return element.getBoundingClientRect();
  }, []);
  const getOverflowSize = reactExports.useCallback(() => {
    const element = overflowRef.current;
    if (!element)
      return { width: 0, height: 0 };
    return element.getBoundingClientRect();
  }, []);
  const [containerRef, componentRef, visibleCount, renderOverflow] = useOverflow(childrenKeys, orientation, getItemSize, getOverflowSize);
  const keyToChildEntries = Array.from(keyToChildMap.entries());
  const visibleChildren = keyToChildEntries.slice(0, visibleCount);
  const overflown = keyToChildEntries.slice(visibleCount);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className: classnames(
        "uifw-toolbar-group-toolGroup_container",
        `uifw-${orientation}`,
        `uifw-${panelAlignment}`,
        className
      ),
      ref: containerRef,
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Surface,
        {
          className: classnames(
            "uifw-toolbar-group-toolGroup",
            `uifw-${orientation}`
          ),
          ref: componentRef,
          ...props,
          children: [
            visibleChildren.map(([item, child]) => {
              if (!reactExports.isValidElement(child))
                return child;
              return reactExports.cloneElement(child, {
                ref: (el) => {
                  if (!el) {
                    itemRefs.current.delete(item);
                    return;
                  }
                  itemRefs.current.set(item, el);
                }
              });
            }),
            renderOverflow && /* @__PURE__ */ jsxRuntimeExports.jsx(OverflowButton, { ref: overflowRef, children: overflown.map(([_, child]) => child) })
          ]
        }
      )
    }
  );
}
ToolGroup.ActionItem = ActionItem;
ToolGroup.GroupItem = GroupItem;
ToolGroup.CustomItem = CustomItem;
function getOrientation(expandsTo) {
  switch (expandsTo) {
    case "left":
    case "right":
      return "vertical";
    case "top":
    case "bottom":
      return "horizontal";
  }
}
try {
  ToolGroup.displayName = "ToolGroup";
  ToolGroup.__docgenInfo = { "description": "", "displayName": "ToolGroup", "props": { "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function Toolbar$1({
  items,
  expandsTo = "bottom",
  panelAlignment = "start"
}) {
  const [popoverOpen, setPopoverOpen] = reactExports.useState(false);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ToolbarContext.Provider,
    {
      value: {
        expandsTo,
        panelAlignment,
        popoverOpen,
        setPopoverOpen
      },
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(ToolGroup, { children: items.map((item) => {
        if (isToolbarActionItem(item)) {
          return /* @__PURE__ */ jsxRuntimeExports.jsx(ToolGroup.ActionItem, { item }, item.id);
        }
        if (isToolbarGroupItem(item)) {
          return /* @__PURE__ */ jsxRuntimeExports.jsx(ToolGroup.GroupItem, { item }, item.id);
        }
        if (isToolbarCustomItem(item)) {
          return /* @__PURE__ */ jsxRuntimeExports.jsx(ToolGroup.CustomItem, { item }, item.id);
        }
        return null;
      }) })
    }
  );
}
const ToolbarContext = reactExports.createContext(void 0);
ToolbarContext.displayName = "uifw:ToolbarContext";
try {
  uifw:
    ToolbarContext.displayName = "uifw:ToolbarContext";
  uifw:
    ToolbarContext.__docgenInfo = { "description": "", "displayName": "uifw:ToolbarContext", "props": { "items": { "defaultValue": null, "description": "Definitions for items of the toolbar.", "name": "items", "required": true, "type": { "name": "ToolbarItem[]" } }, "expandsTo": { "defaultValue": { value: "bottom" }, "description": "Describes direction to which the panels are expanded. Orientation of the toolbar is determined based on this prop. Defaults to `bottom`.", "name": "expandsTo", "required": false, "type": { "name": "enum", "value": [{ "value": '"top"' }, { "value": '"left"' }, { "value": '"right"' }, { "value": '"bottom"' }] } }, "panelAlignment": { "defaultValue": { value: "start" }, "description": "Describes how panels are aligned. Defaults to `start`.", "name": "panelAlignment", "required": false, "type": { "name": "enum", "value": [{ "value": '"start"' }, { "value": '"end"' }] } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function Toolbar(props) {
  const previewFeatures = usePreviewFeatures();
  if (previewFeatures.newToolbars) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(NewToolbar, { ...props });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(OriginalToolbar, { ...props });
}
function NewToolbar(props) {
  const expandsTo = toDirection(props.expandsTo);
  const panelAlignment = toPanelAlignment(props.panelAlignment);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Toolbar$1,
    {
      expandsTo,
      panelAlignment,
      items: props.items
    }
  );
}
function OriginalToolbar(props) {
  const { items, ...other } = props;
  const uiaItems = reactExports.useMemo(() => {
    return items.map((item) => toUIAToolbarItem(item));
  }, [items]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    InternalToolbarComponent,
    {
      items: uiaItems,
      syncUiEvent: SyncUiEventDispatcher.onSyncUiEvent,
      ...other
    }
  );
}
function toDirection(direction) {
  switch (direction) {
    case Direction.Bottom:
      return "bottom";
    case Direction.Left:
      return "left";
    case Direction.Right:
      return "right";
    case Direction.Top:
      return "top";
  }
  return void 0;
}
function toPanelAlignment(alignment) {
  switch (alignment) {
    case ToolbarPanelAlignment.End:
      return "end";
    case ToolbarPanelAlignment.Start:
      return "start";
  }
  return void 0;
}
try {
  Toolbar.displayName = "Toolbar";
  Toolbar.__docgenInfo = { "description": "Component that displays toolbar items.", "displayName": "Toolbar", "props": { "expandsTo": { "defaultValue": null, "description": "Describes to which direction the popup panels are expanded, also defines the orientation of the toolbar (Top/Bottom will create an horizontal toolbar, Left/Right will create a vertical toolbar). Defaults to: [[Direction.Bottom]]", "name": "expandsTo", "required": false, "type": { "name": "enum", "value": [{ "value": "1" }, { "value": "2" }, { "value": "3" }, { "value": "4" }] } }, "enableOverflow": { "defaultValue": null, "description": "Describes if items that do not fit available space should move to an expandable panel. Defaults to: false", "name": "enableOverflow", "required": false, "type": { "name": "boolean | OverflowToolbarOptions" } }, "items": { "defaultValue": null, "description": "Definitions for items of the toolbar. Items are expected to be already sorted by group and item.", "name": "items", "required": true, "type": { "name": "ToolbarItem[]" } }, "panelAlignment": { "defaultValue": null, "description": "Describes how expanded panels are aligned. Defaults to: [[ToolbarPanelAlignment.Start]]", "name": "panelAlignment", "required": false, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }] } }, "useDragInteraction": { "defaultValue": null, "description": "Use Drag Interaction to open popups with nest action buttons", "name": "useDragInteraction", "required": false, "type": { "name": "boolean" } }, "toolbarOpacitySetting": { "defaultValue": null, "description": "Determines whether to use mouse proximity to alter the opacity of the toolbar", "name": "toolbarOpacitySetting", "required": false, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }, { "value": "2" }] } }, "onItemExecuted": { "defaultValue": null, "description": "Optional function to call on any item execution", "name": "onItemExecuted", "required": false, "type": { "name": "OnItemExecutedFunc" } }, "onKeyDown": { "defaultValue": null, "description": "Optional function to call on any KeyDown events processed by toolbar", "name": "onKeyDown", "required": false, "type": { "name": "((e: KeyboardEvent<Element>) => void)" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function ToolbarWithOverflow(props) {
  const { overflowExpandsTo, ...toolbarProps } = props;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Toolbar, { enableOverflow: { overflowExpandsTo }, ...toolbarProps });
}
try {
  ToolbarWithOverflow.displayName = "ToolbarWithOverflow";
  ToolbarWithOverflow.__docgenInfo = { "description": "Component that displays toolbar items, displaying only the elements that can fit in the available space,\nand put the other items into a single panel.", "displayName": "ToolbarWithOverflow", "props": { "expandsTo": { "defaultValue": null, "description": "Describes to which direction the popup panels are expanded, also defines the orientation of the toolbar (Top/Bottom will create an horizontal toolbar, Left/Right will create a vertical toolbar). Defaults to: [[Direction.Bottom]]", "name": "expandsTo", "required": false, "type": { "name": "enum", "value": [{ "value": "1" }, { "value": "2" }, { "value": "3" }, { "value": "4" }] } }, "overflowExpandsTo": { "defaultValue": null, "description": "Describes to which direction the overflow popup panels are expanded. Defaults to: [[Direction.Right]]", "name": "overflowExpandsTo", "required": false, "type": { "name": "enum", "value": [{ "value": "1" }, { "value": "2" }, { "value": "3" }, { "value": "4" }] } }, "items": { "defaultValue": null, "description": "Definitions for items of the toolbar. Items are expected to be already sorted by group and item.", "name": "items", "required": true, "type": { "name": "ToolbarItem[]" } }, "panelAlignment": { "defaultValue": null, "description": "Describes how expanded panels are aligned. Defaults to: [[ToolbarPanelAlignment.Start]]", "name": "panelAlignment", "required": false, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }] } }, "useDragInteraction": { "defaultValue": null, "description": "Use Drag Interaction to open popups with nest action buttons", "name": "useDragInteraction", "required": false, "type": { "name": "boolean" } }, "toolbarOpacitySetting": { "defaultValue": null, "description": "Determines whether to use mouse proximity to alter the opacity of the toolbar", "name": "toolbarOpacitySetting", "required": false, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }, { "value": "2" }] } }, "onItemExecuted": { "defaultValue": null, "description": "Optional function to call on any item execution", "name": "onItemExecuted", "required": false, "type": { "name": "OnItemExecutedFunc" } }, "onKeyDown": { "defaultValue": null, "description": "Optional function to call on any KeyDown events processed by toolbar", "name": "onKeyDown", "required": false, "type": { "name": "((e: KeyboardEvent<Element>) => void)" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function mapToRelativePosition(input) {
  if (typeof input === "string")
    return placementToRelativePositionMap[input];
  return input;
}
function mapToPlacement(input) {
  if (!input)
    return "top-end";
  if (typeof input === "string")
    return input;
  return relativePositionToPlacementMap[input];
}
const placementToRelativePositionMap = {
  left: RelativePosition.Left,
  top: RelativePosition.Top,
  right: RelativePosition.Right,
  bottom: RelativePosition.Bottom,
  "left-start": RelativePosition.LeftTop,
  "left-end": RelativePosition.Left,
  "right-start": RelativePosition.RightTop,
  "right-end": RelativePosition.Right,
  "top-start": RelativePosition.TopLeft,
  "top-end": RelativePosition.TopRight,
  "bottom-start": RelativePosition.BottomLeft,
  "bottom-end": RelativePosition.BottomRight
};
const relativePositionToPlacementMap = {
  [RelativePosition.Left]: "left",
  [RelativePosition.Top]: "top",
  [RelativePosition.Right]: "right",
  [RelativePosition.Bottom]: "bottom",
  [RelativePosition.LeftTop]: "left-start",
  [RelativePosition.RightTop]: "right-start",
  [RelativePosition.TopLeft]: "top-start",
  [RelativePosition.TopRight]: "top-end",
  [RelativePosition.BottomLeft]: "bottom-start",
  [RelativePosition.BottomRight]: "bottom-end"
};
class CursorUpdatedEvent extends UiEvent {
}
const _CursorInformation = class _CursorInformation {
  /** The cursor position. */
  static get cursorPosition() {
    return this._cursorPosition;
  }
  static set cursorPosition(pt) {
    this._cursorPosition = Point.create(pt);
  }
  /** Gets the cursor X position. */
  static get cursorX() {
    return this._cursorPosition.x;
  }
  /** Gets the cursor Y position. */
  static get cursorY() {
    return this._cursorPosition.y;
  }
  /** Gets the general cursor movement direction. */
  static get cursorDirection() {
    return this._cursorDirection;
  }
  /** Handles the mouse movement.  Sets the cursor position and direction and emits onCursorUpdatedEvent. */
  static handleMouseMove(point) {
    const oldPt = _CursorInformation.cursorPosition;
    const direction = this._determineMostFrequentDirection(
      this._cursorDirections,
      this._cursorPosition,
      Point.create(point)
    );
    this.cursorPosition = point;
    this._cursorDirection = direction;
    this.onCursorUpdatedEvent.emit({ oldPt, newPt: point, direction });
  }
  static _determineMostFrequentDirection(cursorDirections, oldPt, newPt) {
    const cursorDirection = this._determineCursorDirection(oldPt, newPt);
    cursorDirections.push(cursorDirection);
    if (cursorDirections.length > 10)
      cursorDirections.shift();
    return this._mostFrequent(cursorDirections);
  }
  static _mostFrequent(array) {
    const hashMap = /* @__PURE__ */ new Map();
    let maxCount = 0, mostFrequent = 0;
    array.forEach((value) => {
      const key = value;
      let frequency = hashMap.get(key);
      if (frequency !== void 0)
        frequency++;
      else
        frequency = 1;
      hashMap.set(key, frequency);
      if (frequency > maxCount) {
        mostFrequent = key;
        maxCount = frequency;
      }
    });
    return mostFrequent;
  }
  static _determineCursorDirection(oldPt, newPt) {
    let directionParts = 0;
    if (newPt.x < oldPt.x)
      directionParts |= 256;
    else if (newPt.x > oldPt.x)
      directionParts |= 16;
    if (newPt.y < oldPt.y)
      directionParts |= 4096;
    else if (newPt.y > oldPt.y)
      directionParts |= 1;
    const direction = directionParts;
    return direction;
  }
  /** Gets the relative position based on the cursor direction. */
  static getRelativePositionFromCursorDirection(cursorDirection) {
    let relativePosition = RelativePosition.BottomRight;
    switch (cursorDirection) {
      case 4096:
        relativePosition = RelativePosition.Top;
        break;
      case 256:
        relativePosition = RelativePosition.Left;
        break;
      case 16:
        relativePosition = RelativePosition.Right;
        break;
      case 1:
        relativePosition = RelativePosition.Bottom;
        break;
      case 4352:
        relativePosition = RelativePosition.TopLeft;
        break;
      case 4112:
        relativePosition = RelativePosition.TopRight;
        break;
      case 257:
        relativePosition = RelativePosition.BottomLeft;
        break;
      case 17:
        relativePosition = RelativePosition.BottomRight;
        break;
    }
    return relativePosition;
  }
  /** @internal - for testing */
  static clearCursorDirections() {
    this._cursorDirections.length = 0;
  }
};
_CursorInformation._cursorPosition = new Point();
_CursorInformation._cursorDirection = 17;
_CursorInformation._cursorDirections = new Array();
_CursorInformation.onCursorUpdatedEvent = new CursorUpdatedEvent();
let CursorInformation = _CursorInformation;
class ElementTooltipChangedEvent extends UiEvent {
}
const _ElementTooltip = class _ElementTooltip extends reactExports.Component {
  constructor(props) {
    super(props);
    this._size = {
      height: 0,
      width: 0
    };
    this.state = {
      message: "",
      isVisible: false,
      position: {
        x: 0,
        y: 0
      }
    };
    this._handleElementTooltipChangedEvent = (args) => {
      this._element = args.el;
      this._position = args.pt;
      this.setState({
        isVisible: args.isTooltipVisible,
        message: args.message
      });
      this.updatePosition();
    };
    this._handleSizeChanged = (size) => {
      this._size = size;
      this.updatePosition();
    };
  }
  static get onElementTooltipChangedEvent() {
    return _ElementTooltip._elementTooltipChangedEvent;
  }
  static get isTooltipVisible() {
    return _ElementTooltip._isTooltipVisible;
  }
  static showTooltip(el, message, pt, options) {
    if (_ElementTooltip._isTooltipHalted)
      return;
    _ElementTooltip._isTooltipVisible = true;
    _ElementTooltip.onElementTooltipChangedEvent.emit({
      isTooltipVisible: true,
      el,
      message,
      pt,
      options
    });
    el.ownerDocument.addEventListener(
      "mousemove",
      _ElementTooltip._handleMouseMove
    );
  }
  static hideTooltip() {
    _ElementTooltip._isTooltipVisible = false;
    _ElementTooltip.onElementTooltipChangedEvent.emit({
      isTooltipVisible: false,
      message: ""
    });
  }
  // istanbul ignore next
  static get isTooltipHalted() {
    return _ElementTooltip._isTooltipHalted;
  }
  // istanbul ignore next
  static set isTooltipHalted(halt) {
    _ElementTooltip._isTooltipHalted = halt;
    if (halt && _ElementTooltip._isTooltipVisible)
      _ElementTooltip.hideTooltip();
  }
  render() {
    if (!this.state.isVisible)
      return null;
    const className = classnames("uifw-element-tooltip", this.props.className);
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-element-tooltip-container", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      Tooltip,
      {
        className,
        style: this.props.style,
        position: this.state.position,
        onSizeChanged: this._handleSizeChanged,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(MessageRenderer$1, { message: this.state.message })
      }
    ) });
  }
  componentDidMount() {
    _ElementTooltip.onElementTooltipChangedEvent.addListener(
      this._handleElementTooltipChangedEvent
    );
  }
  componentWillUnmount() {
    _ElementTooltip.onElementTooltipChangedEvent.removeListener(
      this._handleElementTooltipChangedEvent
    );
  }
  static _handleMouseMove(event) {
    const el = event.currentTarget;
    if (el && _ElementTooltip._isTooltipHalted || !_ElementTooltip._isTooltipVisible) {
      el.removeEventListener("mousemove", _ElementTooltip._handleMouseMove);
      return;
    }
    const hoveredElement = el ? el.elementFromPoint(event.clientX, event.clientY) : void 0;
    if (hoveredElement && hoveredElement.localName !== "canvas") {
      _ElementTooltip.onElementTooltipChangedEvent.emit({
        isTooltipVisible: false,
        message: ""
      });
      el.removeEventListener("mousemove", _ElementTooltip._handleMouseMove);
      return;
    }
  }
  updatePosition() {
    this.setState((prevState) => {
      if (!this._element)
        return null;
      if (!this._position)
        return null;
      const containerBounds = Rectangle.create(
        this._element.getBoundingClientRect()
      );
      const relativeBounds = Rectangle.createFromSize(this._size).offset(
        this._position
      );
      const adjustedPosition = offsetAndContainInContainer(
        relativeBounds,
        containerBounds.getSize(),
        { x: 8, y: 8 }
      );
      const position = adjustedPosition.offset(containerBounds.topLeft());
      if (position.equals(prevState.position))
        return null;
      return {
        position
      };
    });
  }
};
_ElementTooltip._elementTooltipChangedEvent = new ElementTooltipChangedEvent();
let ElementTooltip = _ElementTooltip;
try {
  ElementTooltipChangedEvent.displayName = "ElementTooltipChangedEvent";
  ElementTooltipChangedEvent.__docgenInfo = { "description": "ElementTooltip Changed Event class.", "displayName": "ElementTooltipChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ElementTooltip.displayName = "ElementTooltip";
  ElementTooltip.__docgenInfo = { "description": "ElementTooltip React component.", "displayName": "ElementTooltip", "props": { "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ElementTooltip._elementTooltipChangedEvent.displayName = "ElementTooltip._elementTooltipChangedEvent";
  ElementTooltip._elementTooltipChangedEvent.__docgenInfo = { "description": "ElementTooltip Changed Event class.", "displayName": "ElementTooltip._elementTooltipChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class KeyboardShortcutMenuEvent extends UiEvent {
}
const _KeyboardShortcutMenu = class _KeyboardShortcutMenu extends reactExports.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      menuVisible: false,
      menuX: 0,
      menuY: 0
    };
    this._handleKeyboardShortcutMenuEvent = (state) => {
      this.setState(state);
    };
    this._hideContextMenu = () => {
      this.setState({ menuVisible: false, shortcuts: void 0 });
    };
    this._itemPicked = (shortcut) => {
      this._hideContextMenu();
      shortcut.itemPicked();
    };
  }
  componentDidMount() {
    _KeyboardShortcutMenu.onKeyboardShortcutMenuEvent.addListener(
      this._handleKeyboardShortcutMenuEvent
    );
  }
  componentWillUnmount() {
    _KeyboardShortcutMenu.onKeyboardShortcutMenuEvent.removeListener(
      this._handleKeyboardShortcutMenuEvent
    );
  }
  render() {
    const { shortcuts, menuX, menuY, menuVisible } = this.state;
    const onClose = this._hideContextMenu;
    if (menuVisible) {
      const items = this.getShortcutMenuItems(shortcuts);
      return /* @__PURE__ */ jsxRuntimeExports.jsx(
        GlobalContextMenu,
        {
          className: this.props.className,
          style: this.props.style,
          identifier: "keyboard-shortcuts",
          x: menuX,
          y: menuY,
          opened: menuVisible,
          onEsc: onClose,
          onOutsideClick: onClose,
          edgeLimit: false,
          autoflip: true,
          ignoreNextKeyUp: true,
          children: items
        }
      );
    }
    return null;
  }
  getShortcutMenuItems(shortcuts) {
    const items = [];
    if (shortcuts) {
      shortcuts.forEach((shortcut, index) => {
        const item = this.getShortcutMenuItem(shortcut, index);
        if (item)
          items.push(item);
      });
    }
    return items;
  }
  getShortcutMenuItem(shortcut, index) {
    const shortcutKey = shortcut.key;
    const isHidden = ConditionalBooleanValue.getValue(shortcut.isHidden);
    if (shortcutKey !== shortcut.keyMapKey || shortcut.isFunctionKey || shortcut.isSpecialKey || isHidden)
      return null;
    let node = null;
    let label = shortcut.label;
    const iconSpec = shortcut.iconSpec;
    label = `~${shortcutKey.toLocaleUpperCase()} ${label}`;
    if (shortcut.shortcutContainer.areKeyboardShortcutsAvailable()) {
      const shortcuts = shortcut.shortcutContainer.getAvailableKeyboardShortcuts();
      const items = this.getShortcutMenuItems(shortcuts);
      node = /* @__PURE__ */ jsxRuntimeExports.jsx(
        ContextSubMenu,
        {
          icon: iconSpec,
          label,
          disabled: shortcut.isDisabled,
          children: items
        },
        index
      );
    } else {
      const sel = () => this._itemPicked(shortcut);
      node = /* @__PURE__ */ jsxRuntimeExports.jsx(
        ContextMenuItem,
        {
          onSelect: sel,
          icon: iconSpec,
          disabled: shortcut.isDisabled,
          children: label
        },
        index
      );
    }
    return node;
  }
};
_KeyboardShortcutMenu.onKeyboardShortcutMenuEvent = new KeyboardShortcutMenuEvent();
let KeyboardShortcutMenu = _KeyboardShortcutMenu;
try {
  KeyboardShortcutMenuEvent.displayName = "KeyboardShortcutMenuEvent";
  KeyboardShortcutMenuEvent.__docgenInfo = { "description": "KeyboardShortcut Menu Event class.", "displayName": "KeyboardShortcutMenuEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  KeyboardShortcutMenu.displayName = "KeyboardShortcutMenu";
  KeyboardShortcutMenu.__docgenInfo = { "description": "React component that displays a context menu at the cursor containing keyboard shortcuts.", "displayName": "KeyboardShortcutMenu", "props": { "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  KeyboardShortcutMenu.onKeyboardShortcutMenuEvent.displayName = "KeyboardShortcutMenu.onKeyboardShortcutMenuEvent";
  KeyboardShortcutMenu.onKeyboardShortcutMenuEvent.__docgenInfo = { "description": "KeyboardShortcut Menu Event class.", "displayName": "KeyboardShortcutMenu.onKeyboardShortcutMenuEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const SYSTEM_PREFERRED_COLOR_THEME = "SYSTEM_PREFERRED";
var ColorTheme = /* @__PURE__ */ ((ColorTheme2) => {
  ColorTheme2["Light"] = "light";
  ColorTheme2["Dark"] = "dark";
  ColorTheme2[ColorTheme2["System"] = SYSTEM_PREFERRED_COLOR_THEME] = "System";
  ColorTheme2["Inherit"] = "inherit";
  ColorTheme2["HighContrastLight"] = "high-contrast-light";
  ColorTheme2["HighContrastDark"] = "high-contrast-dark";
  return ColorTheme2;
})(ColorTheme || {});
const colorThemeToThemeTypeMap = {
  [
    "light"
    /* Light */
  ]: "light",
  [
    "high-contrast-light"
    /* HighContrastLight */
  ]: "light",
  [
    "dark"
    /* Dark */
  ]: "dark",
  [
    "high-contrast-dark"
    /* HighContrastDark */
  ]: "dark",
  [ColorTheme.System]: "os",
  os: "os"
  // handle "os" for backwards compatibility
};
const highContrastColorThemes = [
  "high-contrast-dark",
  "high-contrast-light"
  /* HighContrastLight */
];
const WIDGET_OPACITY_DEFAULT = 0.9;
const TOOLBAR_OPACITY_DEFAULT = 0.5;
function mapStateToProps(state) {
  const frameworkState = state[UiFramework.frameworkStateKey];
  if (!frameworkState)
    return void 0;
  return {
    theme: frameworkState.configurableUiState.theme,
    widgetOpacity: frameworkState.configurableUiState.widgetOpacity,
    toolbarOpacity: frameworkState.configurableUiState.toolbarOpacity
  };
}
class ThemeManagerComponent extends reactExports.Component {
  constructor() {
    super(...arguments);
    this._setTheme = (theme) => {
      document.documentElement.classList.add("theme-transition");
      document.documentElement.setAttribute("data-theme", theme);
      window.setTimeout(
        () => document.documentElement.classList.remove("theme-transition"),
        1e3
      );
    };
    this._setWidgetOpacity = (opacity) => {
      const currentWidgetOpacity = document.documentElement.style.getPropertyValue("--buic-widget-opacity");
      if (currentWidgetOpacity !== opacity.toString()) {
        setTimeout(
          () => document.documentElement.style.setProperty(
            "--buic-widget-opacity",
            opacity.toString()
          )
        );
      }
    };
    this._setToolbarOpacity = (opacity) => {
      const currentToolbarOpacity = document.documentElement.style.getPropertyValue("--buic-toolbar-opacity");
      if (currentToolbarOpacity !== opacity.toString()) {
        setTimeout(() => {
          document.documentElement.style.setProperty(
            "--buic-toolbar-opacity",
            opacity.toString()
          );
        });
      }
    };
  }
  componentDidMount() {
    this._setTheme(this.props.theme);
    this._setWidgetOpacity(this.props.widgetOpacity);
    this._setToolbarOpacity(this.props.toolbarOpacity);
  }
  componentDidUpdate(prevProps) {
    if (this.props.theme !== prevProps.theme)
      this._setTheme(this.props.theme);
    if (this.props.widgetOpacity !== prevProps.widgetOpacity)
      this._setWidgetOpacity(this.props.widgetOpacity);
    if (this.props.toolbarOpacity !== prevProps.toolbarOpacity)
      this._setToolbarOpacity(this.props.toolbarOpacity);
  }
  render() {
    const theme = colorThemeToThemeTypeMap[this.props.theme];
    const highContrast = highContrastColorThemes.includes(this.props.theme);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ThemeProvider,
      {
        className: "uifw-root",
        theme: theme ?? "inherit",
        themeOptions: { highContrast },
        "data-root-container": "appui-root-id",
        includeCss: true,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          ThemeProvider$1,
          {
            style: { height: "100%" },
            theme: theme ?? "inherit",
            themeOptions: { highContrast },
            children: this.props.children
          }
        )
      }
    );
  }
}
const ThemeManager = connect(mapStateToProps)(ThemeManagerComponent);
try {
  SYSTEM_PREFERRED_COLOR_THEME.displayName = "SYSTEM_PREFERRED_COLOR_THEME";
  SYSTEM_PREFERRED_COLOR_THEME.__docgenInfo = { "description": "System preferred color theme.", "displayName": "SYSTEM_PREFERRED_COLOR_THEME", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ColorTheme.displayName = "ColorTheme";
  ColorTheme.__docgenInfo = { "description": "Enum for the Color Theme string.", "displayName": "ColorTheme", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ColorTheme.Light.displayName = "ColorTheme.Light";
  ColorTheme.Light.__docgenInfo = { "description": "Will use iTwinUI `light` theme.", "displayName": "ColorTheme.Light", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ColorTheme.Dark.displayName = "ColorTheme.Dark";
  ColorTheme.Dark.__docgenInfo = { "description": "Will use iTwinUI `dark` theme.", "displayName": "ColorTheme.Dark", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ColorTheme.System.displayName = "ColorTheme.System";
  ColorTheme.System.__docgenInfo = { "description": "Will use iTwinUI `os` theme.", "displayName": "ColorTheme.System", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ColorTheme.Inherit.displayName = "ColorTheme.Inherit";
  ColorTheme.Inherit.__docgenInfo = { "description": "Will use iTwinUI wrapping `ThemeProvider` if provided, or default to 'Light'", "displayName": "ColorTheme.Inherit", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ColorTheme.HighContrastLight.displayName = "ColorTheme.HighContrastLight";
  ColorTheme.HighContrastLight.__docgenInfo = { "description": "Will use iTwinUI `light` theme with `highContrast` set to `true`", "displayName": "ColorTheme.HighContrastLight", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ColorTheme.HighContrastDark.displayName = "ColorTheme.HighContrastDark";
  ColorTheme.HighContrastDark.__docgenInfo = { "description": "Will use iTwinUI `dark` theme with `highContrast` set to `true`", "displayName": "ColorTheme.HighContrastDark", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  WIDGET_OPACITY_DEFAULT.displayName = "WIDGET_OPACITY_DEFAULT";
  WIDGET_OPACITY_DEFAULT.__docgenInfo = { "description": "The default widget opacity.", "displayName": "WIDGET_OPACITY_DEFAULT", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  TOOLBAR_OPACITY_DEFAULT.displayName = "TOOLBAR_OPACITY_DEFAULT";
  TOOLBAR_OPACITY_DEFAULT.__docgenInfo = { "description": "The default widget opacity.", "displayName": "TOOLBAR_OPACITY_DEFAULT", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ThemeManager.displayName = "ThemeManager";
  ThemeManager.__docgenInfo = { "description": "ThemeManager handles setting color themes and element opacity management. Note that this component will\naffect the entire application by setting the data-theme attribute to the html element.\nIt also sets an iTwinUI `ThemeProvider` element locally, so all elements\nwithin the AppUI tree will have the same theme, and should be using iTwinUI 2.x or later.\nA `ColorTheme` enum values will configure iTwinUI `ThemeProvider` accordingly.\nAny other string will only apply the `data-theme` attribute to the `html` element\nand `ThemeProvider` theme will be set to `inherit`, in this case the application is\nresponsible for setting the theme by overriding iTwinUI css variables.\n\nThis React component is Redux connected and should wrap [[ConfigurableUiContent]].\n\n```tsx\n<Provider store={UiFramework.store}>\n <ThemeManager>\n   <ConfigurableUiContent />\n </ThemeManager>\n</Provider>\n```", "displayName": "ThemeManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
var ConfigurableUiActionId = /* @__PURE__ */ ((ConfigurableUiActionId2) => {
  ConfigurableUiActionId2["SetSnapMode"] = "configurableui:set_snapmode";
  ConfigurableUiActionId2["SetTheme"] = "configurableui:set_theme";
  ConfigurableUiActionId2["SetToolPrompt"] = "configurableui:set_toolprompt";
  ConfigurableUiActionId2["SetWidgetOpacity"] = "configurableui:set_widget_opacity";
  ConfigurableUiActionId2["SetDragInteraction"] = "configurableui:set-drag-interaction";
  ConfigurableUiActionId2["SetShowWidgetIcon"] = "configurableui:set-show-widget-icon";
  ConfigurableUiActionId2["AutoCollapseUnpinnedPanels"] = "configurableui:set-auto-collapse-unpinned-panels";
  ConfigurableUiActionId2["SetViewOverlayDisplay"] = "configurableui:set-view-overlay-display";
  ConfigurableUiActionId2["AnimateToolSettings"] = "configurableui:set-animate-tool-settings";
  ConfigurableUiActionId2["UseToolAsToolSettingsLabel"] = "configurableui:set-use-tool-as-tool-settings-label";
  ConfigurableUiActionId2["SetToolbarOpacity"] = "configurableui:set-toolbar-opacity";
  return ConfigurableUiActionId2;
})(ConfigurableUiActionId || {});
const initialState = {
  snapMode: SnapMode.NearestKeypoint,
  toolPrompt: "",
  theme: SYSTEM_PREFERRED_COLOR_THEME,
  widgetOpacity: WIDGET_OPACITY_DEFAULT,
  useDragInteraction: false,
  showWidgetIcon: true,
  autoCollapseUnpinnedPanels: false,
  viewOverlayDisplay: true,
  animateToolSettings: false,
  useToolAsToolSettingsLabel: false,
  toolbarOpacity: TOOLBAR_OPACITY_DEFAULT
};
function ConfigurableUiReducer(state = initialState, action) {
  const outState = state;
  switch (action.type) {
    case "configurableui:set_snapmode": {
      return { ...state, snapMode: action.payload };
    }
    case "configurableui:set_toolprompt": {
      return { ...state, toolPrompt: action.payload };
    }
    case "configurableui:set_theme": {
      return { ...state, theme: action.payload };
    }
    case "configurableui:set_widget_opacity": {
      return { ...state, widgetOpacity: action.payload };
    }
    case "configurableui:set-drag-interaction": {
      return { ...state, useDragInteraction: action.payload };
    }
    case "configurableui:set-show-widget-icon": {
      return { ...state, showWidgetIcon: action.payload };
    }
    case "configurableui:set-auto-collapse-unpinned-panels": {
      return { ...state, autoCollapseUnpinnedPanels: action.payload };
    }
    case "configurableui:set-view-overlay-display": {
      return { ...state, viewOverlayDisplay: action.payload };
    }
    case "configurableui:set-animate-tool-settings": {
      return { ...state, animateToolSettings: action.payload };
    }
    case "configurableui:set-use-tool-as-tool-settings-label": {
      return { ...state, useToolAsToolSettingsLabel: action.payload };
    }
    case "configurableui:set-toolbar-opacity": {
      return { ...state, toolbarOpacity: action.payload };
    }
  }
  return outState;
}
class StandardMessageBox extends reactExports.PureComponent {
  constructor(props) {
    super(props);
    this._handleButton = (buttonType) => {
      this._closeDialog(() => {
        if (this.props.onResult)
          this.props.onResult(buttonType);
      });
    };
    this._handleCancel = () => {
      this._closeDialog(() => {
        if (this.props.onResult)
          this.props.onResult(MessageBoxValue.Cancel);
      });
    };
    this._closeDialog = (followUp) => {
      this.setState(
        (_prevState) => ({
          opened: false
        }),
        () => {
          UiFramework.dialogs.modal.close();
          followUp();
        }
      );
    };
    this.state = {
      opened: this.props.opened
    };
  }
  render() {
    const buttonCluster = new Array();
    switch (this.props.messageBoxType) {
      case MessageBoxType.Ok:
      case MessageBoxType.LargeOk:
        buttonCluster.push({
          type: DialogButtonType.OK,
          onClick: () => {
            this._handleButton(MessageBoxValue.Ok);
          }
        });
        break;
      case MessageBoxType.OkCancel:
      case MessageBoxType.MediumAlert:
        buttonCluster.push({
          type: DialogButtonType.OK,
          onClick: () => {
            this._handleButton(MessageBoxValue.Ok);
          }
        });
        buttonCluster.push({
          type: DialogButtonType.Cancel,
          onClick: () => {
            this._handleButton(MessageBoxValue.Cancel);
          }
        });
        break;
      case MessageBoxType.YesNo:
      case MessageBoxType.YesNoCancel:
        buttonCluster.push({
          type: DialogButtonType.Yes,
          onClick: () => {
            this._handleButton(MessageBoxValue.Yes);
          }
        });
        buttonCluster.push({
          type: DialogButtonType.No,
          onClick: () => {
            this._handleButton(MessageBoxValue.No);
          }
        });
        if (MessageBoxType.YesNoCancel === this.props.messageBoxType)
          buttonCluster.push({
            type: DialogButtonType.Cancel,
            onClick: () => {
              this._handleButton(MessageBoxValue.Cancel);
            }
          });
        break;
    }
    let severity = MessageSeverity.None;
    switch (this.props.iconType) {
      case MessageBoxIconType.NoSymbol:
        severity = MessageSeverity.None;
        break;
      case MessageBoxIconType.Information:
        severity = MessageSeverity.Information;
        break;
      case MessageBoxIconType.Question:
        severity = MessageSeverity.Question;
        break;
      case MessageBoxIconType.Warning:
        severity = MessageSeverity.Warning;
        break;
      case MessageBoxIconType.Critical:
        severity = MessageSeverity.Error;
        break;
      case MessageBoxIconType.Success:
        severity = MessageSeverity.Success;
        break;
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      MessageBox,
      {
        className: this.props.className,
        style: { zIndex: DialogManagerBase.topZIndex, ...this.props.style },
        opened: this.state.opened,
        title: this.props.title,
        severity,
        buttonCluster,
        onClose: this._handleCancel,
        onEscape: this._handleCancel,
        children: this.props.children
      }
    );
  }
}
try {
  StandardMessageBox.displayName = "StandardMessageBox";
  StandardMessageBox.__docgenInfo = { "description": "StandardMessageBox React component displays a standard icon, message text and a standard button set in the lower right.", "displayName": "StandardMessageBox", "props": { "opened": { "defaultValue": null, "description": "Indicates whether the message box is open", "name": "opened", "required": true, "type": { "name": "boolean" } }, "iconType": { "defaultValue": null, "description": "The standard icon to display in the message box", "name": "iconType", "required": true, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }, { "value": "2" }, { "value": "3" }, { "value": "4" }, { "value": "5" }] } }, "title": { "defaultValue": null, "description": "Title to display in the message box", "name": "title", "required": true, "type": { "name": "string" } }, "messageBoxType": { "defaultValue": null, "description": "Controls the button set displayed", "name": "messageBoxType", "required": true, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }, { "value": "2" }, { "value": "3" }, { "value": "4" }, { "value": "5" }] } }, "onResult": { "defaultValue": null, "description": "Callback function for processing the message box result", "name": "onResult", "required": false, "type": { "name": "((result: MessageBoxValue) => void)" } }, "children": { "defaultValue": null, "description": "Content", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class PointerMessageChangedEvent extends UiEvent {
}
class PointerMessagePositionChangedEvent extends UiEvent {
}
const _PointerMessage = class _PointerMessage extends reactExports.Component {
  constructor() {
    super(...arguments);
    this.state = {
      message: "",
      isVisible: false,
      position: {
        x: 0,
        y: 0
      }
    };
    this._size = {
      height: 0,
      width: 0
    };
    this._handleSizeChanged = (size) => {
      this._size = size;
      this.updatePosition();
    };
    this._handlePointerMessageChangedEvent = (args) => {
      this._relativePosition = args.relativePosition;
      this._viewport = args.viewport;
      this._position = args.pt;
      this.setState(() => ({
        isVisible: args.isVisible,
        message: args.message,
        detailedMessage: args.detailedMessage,
        messageDetails: args.messageDetails
      }));
      this.updatePosition();
    };
    this._handlePointerMessagePositionChangedEvent = (args) => {
      this._relativePosition = args.relativePosition;
      this._position = args.pt;
      this.updatePosition();
    };
  }
  static get onPointerMessageChangedEvent() {
    return _PointerMessage._pointerMessageChangedEvent;
  }
  static showMessage(message) {
    _PointerMessage.onPointerMessageChangedEvent.emit({
      isVisible: true,
      priority: message.priority,
      message: message.briefMessage,
      detailedMessage: message.detailedMessage,
      relativePosition: message.relativePosition,
      viewport: message.viewport,
      pt: message.displayPoint,
      messageDetails: message
    });
  }
  static updateMessage(displayPoint, relativePosition) {
    _PointerMessage._onPointerMessagePositionChangedEvent.emit({
      pt: displayPoint,
      relativePosition
    });
  }
  static hideMessage() {
    _PointerMessage.onPointerMessageChangedEvent.emit({
      isVisible: false,
      priority: OutputMessagePriority.None,
      message: ""
    });
  }
  render() {
    if (!this.state.isVisible)
      return null;
    const className = classnames("uifw-pointer-message", this.props.className);
    const severity = MessageManager.getSeverity(this.state.messageDetails);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Tooltip,
      {
        className,
        onSizeChanged: this._handleSizeChanged,
        position: this.state.position,
        style: this.props.style,
        children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "uifw-pointer-message-content", children: [
          severity !== MessageSeverity.None && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "uifw-pointer-message-icon", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            Icon$1,
            {
              className: `icon ${MessageContainer.getIconClassName(
                severity
              )}`,
              iconSpec: `${MessageContainer.getIcon(severity, false)}`
            }
          ) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "uifw-pointer-message-text", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              MessageRenderer$1,
              {
                className: "uifw-pointer-message-brief",
                message: this.state.message,
                useSpan: true
              }
            ),
            this.state.detailedMessage && /* @__PURE__ */ jsxRuntimeExports.jsx(
              MessageRenderer$1,
              {
                className: "uifw-pointer-message-detailed",
                message: this.state.detailedMessage
              }
            )
          ] })
        ] })
      }
    );
  }
  componentDidMount() {
    _PointerMessage.onPointerMessageChangedEvent.addListener(
      this._handlePointerMessageChangedEvent
    );
    _PointerMessage._onPointerMessagePositionChangedEvent.addListener(
      this._handlePointerMessagePositionChangedEvent
    );
  }
  componentWillUnmount() {
    _PointerMessage.onPointerMessageChangedEvent.removeListener(
      this._handlePointerMessageChangedEvent
    );
    _PointerMessage._onPointerMessagePositionChangedEvent.removeListener(
      this._handlePointerMessagePositionChangedEvent
    );
  }
  updatePosition() {
    const adjustmentOffset = 20;
    let offset;
    switch (this._relativePosition) {
      case RelativePosition.Top:
        offset = { x: 0, y: -adjustmentOffset };
        break;
      case RelativePosition.TopRight:
        offset = { x: adjustmentOffset, y: -adjustmentOffset };
        break;
      case RelativePosition.Right:
        offset = { x: adjustmentOffset, y: 0 };
        break;
      case RelativePosition.BottomRight:
        offset = { x: adjustmentOffset, y: adjustmentOffset };
        break;
      case RelativePosition.Bottom:
        offset = { x: 0, y: adjustmentOffset };
        break;
      case RelativePosition.BottomLeft:
        offset = { x: -adjustmentOffset, y: adjustmentOffset };
        break;
      case RelativePosition.Left:
        offset = { x: -adjustmentOffset, y: 0 };
        break;
      case RelativePosition.TopLeft:
        offset = { x: -adjustmentOffset, y: -adjustmentOffset };
        break;
    }
    this.setState((prevState) => {
      if (!this._viewport)
        return null;
      if (!this._position)
        return null;
      const containerBounds = Rectangle.create(
        this._viewport.getBoundingClientRect()
      );
      const relativeBounds = Rectangle.createFromSize(this._size).offset(
        this._position
      );
      const viewportOffset = new Point().getOffsetTo(containerBounds.topLeft());
      const adjustedPosition = offsetAndContainInContainer(
        relativeBounds,
        containerBounds.getSize(),
        offset
      );
      const position = adjustedPosition.offset(viewportOffset);
      if (position.equals(prevState.position))
        return null;
      return {
        position
      };
    });
  }
};
_PointerMessage._pointerMessageChangedEvent = new PointerMessageChangedEvent();
_PointerMessage._onPointerMessagePositionChangedEvent = new PointerMessagePositionChangedEvent();
let PointerMessage = _PointerMessage;
try {
  PointerMessageChangedEvent.displayName = "PointerMessageChangedEvent";
  PointerMessageChangedEvent.__docgenInfo = { "description": "Pointer Message Changed Event emitted by the [[PointerMessage]] component", "displayName": "PointerMessageChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  PointerMessage.displayName = "PointerMessage";
  PointerMessage.__docgenInfo = { "description": "Pointer message pops up near pointer when attempting an invalid interaction.", "displayName": "PointerMessage", "props": { "message": { "defaultValue": null, "description": "Text to display", "name": "message", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  PointerMessage._pointerMessageChangedEvent.displayName = "PointerMessage._pointerMessageChangedEvent";
  PointerMessage._pointerMessageChangedEvent.__docgenInfo = { "description": "Pointer Message Changed Event emitted by the [[PointerMessage]] component", "displayName": "PointerMessage._pointerMessageChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  PointerMessage._onPointerMessagePositionChangedEvent.displayName = "PointerMessage._onPointerMessagePositionChangedEvent";
  PointerMessage._onPointerMessagePositionChangedEvent.__docgenInfo = { "description": "Pointer Message Position Changed Event emitted by the [[PointerMessage]] component", "displayName": "PointerMessage._onPointerMessagePositionChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class StatusMessageManager {
  constructor() {
    this._messages = [];
    this._messageId = 0;
  }
  initialize() {
    this._messages = [];
    this._messageId = 0;
  }
  get messages() {
    return this._messages;
  }
  add(messageDetails) {
    const id = this._messageId.toString();
    const severity = MessageManager.getSeverity(messageDetails);
    const messages = this._messages.slice();
    messages.splice(0, 0, { id, messageDetails, severity });
    this._messages = messages;
    this._messageId++;
    const stickyMessages = this._messages.filter(
      (message) => message.messageDetails.msgType === OutputMessageType.Sticky
    );
    if (stickyMessages.length > MessageManager.maxDisplayedStickyMessages) {
      const removeMessages = stickyMessages.slice(
        MessageManager.maxDisplayedStickyMessages
      );
      for (const removeMessage of removeMessages) {
        this.remove(removeMessage.id);
      }
    }
  }
  remove(id) {
    let result = false;
    const foundIndex = this._messages.findIndex(
      (message) => message.id === id
    );
    if (foundIndex >= 0) {
      const messages = this._messages.slice();
      messages.splice(foundIndex, 1);
      this._messages = messages;
      result = true;
    }
    return result;
  }
}
try {
  StatusMessageManager.displayName = "StatusMessageManager";
  StatusMessageManager.__docgenInfo = { "description": "Manager for Status messages", "displayName": "StatusMessageManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class MessageBoxCallbacks {
  constructor(onFulfilled, onRejected) {
    this.onFulfilled = onFulfilled;
    this.onRejected = onRejected;
    this.handleMessageBoxResult = (result) => {
      this.onFulfilled(result);
    };
  }
}
class MessageAddedEvent extends UiEvent {
}
class MessagesUpdatedEvent extends UiEvent {
}
class ActivityMessageUpdatedEvent extends UiEvent {
}
class ActivityMessageCancelledEvent extends UiEvent {
}
class InputFieldMessageAddedEvent extends UiEvent {
}
class InputFieldMessageRemovedEvent extends UiEvent {
}
class OpenMessageCenterEvent extends UiEvent {
}
class ToolAssistanceChangedEvent extends UiEvent {
}
class OngoingActivityMessage {
  constructor() {
    this.message = "";
    this.percentage = 0;
    this.details = new ActivityMessageDetails(
      true,
      true,
      true
    );
    this.isRestored = false;
  }
}
const _MessageManager = class _MessageManager {
  /** List of messages as NotifyMessageDetailsType. */
  static get messages() {
    return this._messages;
  }
  /** Manager of active messages. */
  static get activeMessageManager() {
    return this._activeMessageManager;
  }
  /** Clear the message list. */
  static clearMessages() {
    this._messages.splice(0);
    this._activeMessageManager.initialize();
    this._toastCloseCallbacks.forEach(({ close }) => close());
    this._toastCloseCallbacks.splice(0);
    this.onMessagesUpdatedEvent.emit({});
    this._lastMessage = void 0;
  }
  /** Update the message list. */
  static updateMessages() {
    this.onMessagesUpdatedEvent.emit({});
  }
  /** Set the maximum number of cached message. */
  static setMaxCachedMessages(max) {
    this._maxCachedMessages = max;
    this.checkMaxCachedMessages();
  }
  /** The maximum number of displayed Sticky messages. */
  static get maxDisplayedStickyMessages() {
    return this._maxDisplayedStickyMessages;
  }
  static set maxDisplayedStickyMessages(max) {
    this._maxDisplayedStickyMessages = max;
  }
  /** Output a message and/or alert to the user.
   * @param  message  Details about the message to output.
   */
  static outputMessage(message) {
    if (message.msgType === OutputMessageType.Pointer) {
      PointerMessage.showMessage(message);
    } else if (message.msgType === OutputMessageType.InputField) {
      if (message.inputField)
        _MessageManager.displayInputFieldMessage(
          message.inputField,
          message.briefMessage,
          message.detailedMessage,
          message.priority
        );
      else
        message.msgType = OutputMessageType.Sticky;
    } else if (message.msgType === OutputMessageType.Alert) {
      if (message.openAlert === OutputMessageAlert.Balloon)
        message.msgType = OutputMessageType.Sticky;
      else
        _MessageManager.showAlertMessageBox(message);
    }
    _MessageManager.addMessage(message);
  }
  /** Set the element where messages should be animated out to on exit.
   * @param  element `HTMLElement` to animate out to.
   */
  static registerAnimateOutToElement(element) {
    this._animateOutToElement = !element ? [] : [element, ...this._animateOutToElement];
  }
  /** Handles disconnected element modal frontstages, revert to last still displayed. */
  static get animateOutToElement() {
    var _a2;
    if (this._animateOutToElement.length === 0) {
      return void 0;
    }
    if ((_a2 = this._animateOutToElement[0]) == null ? void 0 : _a2.isConnected) {
      return this._animateOutToElement[0];
    }
    this._animateOutToElement.splice(0, 1);
    return this.animateOutToElement;
  }
  /** Display a message.
   * Works only with `Sticky` and `Toast` message types.
   * @param  message  Details about the message to display.
   * @param options Optionally override individual toast parameters.
   * @param settings Optionally override all toasts settings (i.e. placement or order).
   * @returns Object with reference to the message (i.e. to close it programmatically) if it was displayed.
   */
  static displayMessage(message, options, settings2) {
    if (message.msgType !== OutputMessageType.Sticky && message.msgType !== OutputMessageType.Toast) {
      return void 0;
    }
    const args = {
      message,
      options,
      settings: settings2,
      animateOutToElement: this.animateOutToElement
    };
    this.onDisplayMessage.emit(args);
    return {
      close: () => {
        var _a2;
        (_a2 = args.close) == null ? void 0 : _a2.call(args);
      }
    };
  }
  /** Output a message and/or alert to the user.
   * @param  message  Details about the message to output.
   */
  static addMessage(message) {
    if (!lodashExports.isEqual(message, this._lastMessage)) {
      this.addToMessageCenter(message);
      this._lastMessage = message;
    }
    this._activeMessageManager.add(message);
    this.refreshToastMessages();
    this.onMessageAddedEvent.emit({ message });
  }
  /**
   * Closes sticky messages that are beyond the limit,
   * opens new toast and sticky messages from the active message manager.
   */
  static refreshToastMessages() {
    this._toastCloseCallbacks = this._toastCloseCallbacks.filter((t) => {
      if (_MessageManager.activeMessageManager.messages.some(
        (msg) => t.id === msg.id
      )) {
        return true;
      }
      t.close();
      return false;
    });
    const messagesToAdd = _MessageManager.activeMessageManager.messages.filter(
      (msg) => !this._toastCloseCallbacks.find((m) => m.id === msg.id)
    );
    messagesToAdd.forEach((msg) => {
      const displayedMessage = _MessageManager.displayMessage(
        msg.messageDetails,
        {
          onRemove: () => {
            _MessageManager.activeMessageManager.remove(msg.id);
          }
        }
      );
      if (!!displayedMessage)
        this._toastCloseCallbacks.push({
          close: displayedMessage.close,
          id: msg.id
        });
    });
  }
  /** Add a message to the Message Center.
   * @param  message  Details about the message to output.
   */
  static addToMessageCenter(message) {
    this._messages.push(message);
    this.onMessagesUpdatedEvent.emit({});
    this.checkMaxCachedMessages();
  }
  /** Checks number of messages against the maximum. */
  static checkMaxCachedMessages() {
    if (this._messages.length > this._maxCachedMessages) {
      const numToErase = this._maxCachedMessages / 4;
      this._messages.splice(0, numToErase);
      this.onMessagesUpdatedEvent.emit({});
    }
  }
  /**
   * Sets details for setting up an Activity message.
   * @param details    Details for setup of ActivityMessage
   * @returns true if details is valid and can be used to display ActivityMessage
   */
  static setupActivityMessageDetails(details) {
    this._ongoingActivityMessage.details = details;
    this._ongoingActivityMessage.isRestored = details.showDialogInitially;
    return true;
  }
  /**
   * Output an activity message to the user.
   * @param message         The message text.
   * @param percentComplete The percentage of completion.
   * @return true if the message was displayed, false if the message could not be displayed.
   */
  static outputActivityMessage(message, percentComplete) {
    return _MessageManager.setupActivityMessageValues(message, percentComplete);
  }
  /**
   * Sets values on _OngoingActivityMessage to be referenced when displaying
   * an ActivityMessage.
   * @param message     Message of the process that ActivityMessage is tracking
   * @param percentage  Progress made by activity in percentage
   * @param restored    True if original ActivityMessage has been closed and
   *                    is now being restored from the status bar.
   * @returns true if details is valid and can be used to display ActivityMessage
   */
  static setupActivityMessageValues(message, percentage, restored) {
    this._ongoingActivityMessage.message = message;
    this._ongoingActivityMessage.percentage = percentage;
    this.onActivityMessageUpdatedEvent.emit({
      message,
      percentage,
      details: this._ongoingActivityMessage.details,
      restored: restored !== void 0 ? restored : this._ongoingActivityMessage.isRestored
    });
    this._ongoingActivityMessage.isRestored = false;
    return true;
  }
  /**
   * Dismisses current ActivityMessage and ends activity if canceled.
   * @param isCompleted   True if the activity was completed, false if it was canceled
   * @returns True if both ActivityMessage and activity process are ended.
   */
  static endActivityMessage(isCompleted) {
    this.endActivityProcessing(isCompleted);
    this.onActivityMessageCancelledEvent.emit({});
    return true;
  }
  /**
   * Ends processing for activity according to message definition.
   * @param isCompleted   True if the activity was completed, false if it was canceled
   */
  static endActivityProcessing(isCompleted) {
    if (isCompleted)
      this._ongoingActivityMessage.details.onActivityCompleted();
    else
      this._ongoingActivityMessage.details.onActivityCancelled();
  }
  /**
   * Displays an input field message near target element.
   * @param target  The currently focused or recently focused element to place the
   *                input field message near.
   * @param messageText  Text to display in the message.
   * @param detailedMessage   Optional detailed message text to display.
   * @param priority   Optional message priority which controls icon to display.
   */
  static displayInputFieldMessage(target, messageText, detailedMessage = "", priority = OutputMessagePriority.Error) {
    this.onInputFieldMessageAddedEvent.emit({
      target,
      messageText,
      detailedMessage,
      priority
    });
  }
  /**
   * Hides the currently displayed input field message.
   */
  static hideInputFieldMessage() {
    this.onInputFieldMessageRemovedEvent.emit({});
  }
  /**
   * Opens message center.
   */
  static openMessageCenter() {
    this.onOpenMessageCenterEvent.emit({});
  }
  /** Output a prompt to the user. A 'prompt' indicates an action the user should take to proceed. */
  static outputPrompt(prompt) {
    UiFramework.dispatchActionToStore(
      ConfigurableUiActionId.SetToolPrompt,
      prompt,
      true
    );
  }
  /** Extracts the message severity from the message details and returns the corresponding React icon.
   * @param details NotifyMessageDetailsType
   * @returns IconSpec
   */
  static getIconSpecFromDetails(details) {
    const severity = _MessageManager.getSeverity(details);
    let iconSpec = /* @__PURE__ */ jsxRuntimeExports.jsx(SvgStatusSuccess, {});
    switch (severity) {
      case MessageSeverity.Error:
      case MessageSeverity.Fatal:
        iconSpec = /* @__PURE__ */ jsxRuntimeExports.jsx(SvgStatusError, {});
        break;
      case MessageSeverity.Warning:
        iconSpec = /* @__PURE__ */ jsxRuntimeExports.jsx(SvgStatusWarning, {});
        break;
      case MessageSeverity.Information:
        iconSpec = /* @__PURE__ */ jsxRuntimeExports.jsx(SvgInfo, {});
        break;
    }
    return iconSpec;
  }
  /** Gets an icon CSS class name based on a given NotifyMessageDetailsType.
   * @public
   */
  static getIconClassName(details) {
    const severity = _MessageManager.getSeverity(details);
    const className = MessageContainer.getIconClassName(severity);
    const iconClassName = classnames("icon", "notifymessage-icon", className);
    return iconClassName;
  }
  /** Gets a MessageSeverity based on a given NotifyMessageDetailsType. */
  static getSeverity(details) {
    let severity = MessageSeverity.None;
    switch (details.priority) {
      case OutputMessagePriority.None:
        severity = MessageSeverity.Success;
        break;
      case OutputMessagePriority.Success:
        severity = MessageSeverity.Success;
        break;
      case OutputMessagePriority.Info:
        severity = MessageSeverity.Information;
        break;
      case OutputMessagePriority.Warning:
        severity = MessageSeverity.Warning;
        break;
      case OutputMessagePriority.Error:
        severity = MessageSeverity.Error;
        break;
      case OutputMessagePriority.Fatal:
        severity = MessageSeverity.Fatal;
        break;
    }
    return severity;
  }
  /** Gets a MessageBoxIconType based on a given NotifyMessageDetailsType. */
  static getIconType(details) {
    let iconType = MessageBoxIconType.NoSymbol;
    switch (details.priority) {
      case OutputMessagePriority.None:
        iconType = MessageBoxIconType.NoSymbol;
        break;
      case OutputMessagePriority.Success:
        iconType = MessageBoxIconType.Success;
        break;
      case OutputMessagePriority.Info:
        iconType = MessageBoxIconType.Information;
        break;
      case OutputMessagePriority.Warning:
        iconType = MessageBoxIconType.Warning;
        break;
      case OutputMessagePriority.Error:
        iconType = MessageBoxIconType.Critical;
        break;
      case OutputMessagePriority.Fatal:
        iconType = MessageBoxIconType.Critical;
        break;
    }
    return iconType;
  }
  /** Output a MessageBox and wait for response from the user.
   * @param mbType       The MessageBox type.
   * @param message      The message to display.
   * @param icon         The MessageBox icon type.
   * @return the response from the user.
   */
  static async openMessageBox(mbType, message, icon) {
    return new Promise(
      (onFulfilled, onRejected) => {
        const messageBoxCallbacks = new MessageBoxCallbacks(
          onFulfilled,
          onRejected
        );
        const messageElement = /* @__PURE__ */ jsxRuntimeExports.jsx(MessageRenderer$1, { message, useSpan: true });
        UiFramework.dialogs.modal.open(
          this.standardMessageBox(
            mbType,
            icon,
            UiFramework.translate("general.alert"),
            messageElement,
            messageBoxCallbacks
          )
        );
      }
    );
  }
  /** @internal */
  static showAlertMessageBox(messageDetails) {
    const iconType = this.getIconType(messageDetails);
    const content = /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(MessageRenderer$1, { message: messageDetails.briefMessage, useSpan: true }),
      messageDetails.detailedMessage && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: /* @__PURE__ */ jsxRuntimeExports.jsx(MessageRenderer$1, { message: messageDetails.detailedMessage, useSpan: true }) })
    ] });
    UiFramework.dialogs.modal.open(
      this.standardMessageBox(
        MessageBoxType.Ok,
        iconType,
        UiFramework.translate("general.alert"),
        content
      )
    );
  }
  static standardMessageBox(mbType, iconType, title, messageElement, callbacks) {
    const onResult = callbacks !== void 0 ? callbacks.handleMessageBoxResult : void 0;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      StandardMessageBox,
      {
        opened: true,
        messageBoxType: mbType,
        iconType,
        title,
        onResult,
        children: messageElement
      }
    );
  }
  /** Setup tool assistance instructions for a tool. The instructions include the main instruction, which includes the current prompt.
   * @param instructions The tool assistance instructions.
   * @public
   */
  static setToolAssistance(instructions) {
    _MessageManager.onToolAssistanceChangedEvent.emit({ instructions });
  }
  /** Show a tooltip window. Saves tooltip location for AccuSnap to test if cursor has moved far enough away to close tooltip.
   * @param htmlElement The HTMLElement that anchors the tooltip.
   * @param message     What to display inside the tooltip.
   * @param location    An optional location, relative to the origin of htmlElement, for the tooltip. If undefined, center of `htmlElement`.
   * @param options     Options that supply additional information about how the tooltip should function.
   */
  static openToolTip(htmlElement, message, location, options) {
    IModelApp.notifications.toolTipLocation.setFrom(location);
    ElementTooltip.showTooltip(htmlElement, message, location, options);
  }
  /** @internal */
  static closeAllMessages() {
    ElementTooltip.hideTooltip();
    PointerMessage.hideMessage();
    _MessageManager.clearMessages();
    _MessageManager.hideInputFieldMessage();
    _MessageManager.endActivityMessage(false);
  }
};
_MessageManager._maxCachedMessages = 500;
_MessageManager._maxDisplayedStickyMessages = 3;
_MessageManager._messages = [];
_MessageManager._ongoingActivityMessage = new OngoingActivityMessage();
_MessageManager._activeMessageManager = new StatusMessageManager();
_MessageManager._animateOutToElement = [];
_MessageManager._toastCloseCallbacks = [];
_MessageManager.onMessageAddedEvent = new MessageAddedEvent();
_MessageManager.onMessagesUpdatedEvent = new MessagesUpdatedEvent();
_MessageManager.onActivityMessageUpdatedEvent = new ActivityMessageUpdatedEvent();
_MessageManager.onActivityMessageCancelledEvent = new ActivityMessageCancelledEvent();
_MessageManager.onInputFieldMessageAddedEvent = new InputFieldMessageAddedEvent();
_MessageManager.onInputFieldMessageRemovedEvent = new InputFieldMessageRemovedEvent();
_MessageManager.onOpenMessageCenterEvent = new OpenMessageCenterEvent();
_MessageManager.onDisplayMessage = new BeUiEvent();
_MessageManager.onToolAssistanceChangedEvent = new ToolAssistanceChangedEvent();
let MessageManager = _MessageManager;
try {
  MessageAddedEvent.displayName = "MessageAddedEvent";
  MessageAddedEvent.__docgenInfo = { "description": "Message Added Event class.", "displayName": "MessageAddedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessagesUpdatedEvent.displayName = "MessagesUpdatedEvent";
  MessagesUpdatedEvent.__docgenInfo = { "description": "Messages Updated Event class.", "displayName": "MessagesUpdatedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ActivityMessageUpdatedEvent.displayName = "ActivityMessageUpdatedEvent";
  ActivityMessageUpdatedEvent.__docgenInfo = { "description": "Activity Message Added Event class.", "displayName": "ActivityMessageUpdatedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ActivityMessageCancelledEvent.displayName = "ActivityMessageCancelledEvent";
  ActivityMessageCancelledEvent.__docgenInfo = { "description": "Activity Message Cancelled Event class.", "displayName": "ActivityMessageCancelledEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InputFieldMessageAddedEvent.displayName = "InputFieldMessageAddedEvent";
  InputFieldMessageAddedEvent.__docgenInfo = { "description": "Input Field Message Added Event class", "displayName": "InputFieldMessageAddedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InputFieldMessageRemovedEvent.displayName = "InputFieldMessageRemovedEvent";
  InputFieldMessageRemovedEvent.__docgenInfo = { "description": "Input Field Message Removed Event class.", "displayName": "InputFieldMessageRemovedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  OpenMessageCenterEvent.displayName = "OpenMessageCenterEvent";
  OpenMessageCenterEvent.__docgenInfo = { "description": "Open Message Center Event class.", "displayName": "OpenMessageCenterEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ToolAssistanceChangedEvent.displayName = "ToolAssistanceChangedEvent";
  ToolAssistanceChangedEvent.__docgenInfo = { "description": "Tool Assistance Changed event class", "displayName": "ToolAssistanceChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.displayName = "MessageManager";
  MessageManager.__docgenInfo = { "description": "The MessageManager class manages messages and prompts. It is used by the [[AppNotificationManager]] class.", "displayName": "MessageManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager._ongoingActivityMessage.displayName = "MessageManager._ongoingActivityMessage";
  MessageManager._ongoingActivityMessage.__docgenInfo = { "description": "Keeps track of the current activity message, and updates whenever\nsetupActivityMessageDetails() or setupActivityMessageValues()\nis called.\nUsed to display tracked progress in ActivityMessage.", "displayName": "MessageManager._ongoingActivityMessage", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager._activeMessageManager.displayName = "MessageManager._activeMessageManager";
  MessageManager._activeMessageManager.__docgenInfo = { "description": "Manager for Status messages", "displayName": "MessageManager._activeMessageManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.onMessageAddedEvent.displayName = "MessageManager.onMessageAddedEvent";
  MessageManager.onMessageAddedEvent.__docgenInfo = { "description": "Message Added Event class.", "displayName": "MessageManager.onMessageAddedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.onMessagesUpdatedEvent.displayName = "MessageManager.onMessagesUpdatedEvent";
  MessageManager.onMessagesUpdatedEvent.__docgenInfo = { "description": "Messages Updated Event class.", "displayName": "MessageManager.onMessagesUpdatedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.onActivityMessageUpdatedEvent.displayName = "MessageManager.onActivityMessageUpdatedEvent";
  MessageManager.onActivityMessageUpdatedEvent.__docgenInfo = { "description": "Activity Message Added Event class.", "displayName": "MessageManager.onActivityMessageUpdatedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.onActivityMessageCancelledEvent.displayName = "MessageManager.onActivityMessageCancelledEvent";
  MessageManager.onActivityMessageCancelledEvent.__docgenInfo = { "description": "Activity Message Cancelled Event class.", "displayName": "MessageManager.onActivityMessageCancelledEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.onInputFieldMessageAddedEvent.displayName = "MessageManager.onInputFieldMessageAddedEvent";
  MessageManager.onInputFieldMessageAddedEvent.__docgenInfo = { "description": "Input Field Message Added Event class", "displayName": "MessageManager.onInputFieldMessageAddedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.onInputFieldMessageRemovedEvent.displayName = "MessageManager.onInputFieldMessageRemovedEvent";
  MessageManager.onInputFieldMessageRemovedEvent.__docgenInfo = { "description": "Input Field Message Removed Event class.", "displayName": "MessageManager.onInputFieldMessageRemovedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.onOpenMessageCenterEvent.displayName = "MessageManager.onOpenMessageCenterEvent";
  MessageManager.onOpenMessageCenterEvent.__docgenInfo = { "description": "Open Message Center Event class.", "displayName": "MessageManager.onOpenMessageCenterEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.onDisplayMessage.displayName = "MessageManager.onDisplayMessage";
  MessageManager.onDisplayMessage.__docgenInfo = { "description": "Specialization of BeEvent for events that take a single strongly typed argument, primarily used for UI events.", "displayName": "MessageManager.onDisplayMessage", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.onToolAssistanceChangedEvent.displayName = "MessageManager.onToolAssistanceChangedEvent";
  MessageManager.onToolAssistanceChangedEvent.__docgenInfo = { "description": "Tool Assistance Changed event class", "displayName": "MessageManager.onToolAssistanceChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.messages.displayName = "MessageManager.messages";
  MessageManager.messages.__docgenInfo = { "description": "List of messages as NotifyMessageDetailsType.", "displayName": "MessageManager.messages", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.activeMessageManager.displayName = "MessageManager.activeMessageManager";
  MessageManager.activeMessageManager.__docgenInfo = { "description": "Manager of active messages.", "displayName": "MessageManager.activeMessageManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.maxDisplayedStickyMessages.displayName = "MessageManager.maxDisplayedStickyMessages";
  MessageManager.maxDisplayedStickyMessages.__docgenInfo = { "description": "The maximum number of displayed Sticky messages.", "displayName": "MessageManager.maxDisplayedStickyMessages", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  MessageManager.animateOutToElement.displayName = "MessageManager.animateOutToElement";
  MessageManager.animateOutToElement.__docgenInfo = { "description": "Handles disconnected element modal frontstages, revert to last still displayed.", "displayName": "MessageManager.animateOutToElement", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class InputFieldMessage extends reactExports.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      message: "",
      isVisible: false,
      priority: OutputMessagePriority.None,
      showCloseButton: !!this.props.showCloseButton
    };
    this._onInputMessageClose = () => {
      this.setState({ isVisible: false });
    };
    this._handleInputFieldMessageAddedEvent = (args) => {
      this.setState({
        inputFieldElement: args.target,
        message: args.messageText,
        isVisible: true,
        priority: args.priority,
        detailedMessage: args.detailedMessage
      });
    };
    this._handleInputFieldMessageRemovedEvent = () => {
      this.setState({ isVisible: false });
    };
  }
  render() {
    const {
      isVisible,
      inputFieldElement,
      message,
      priority,
      detailedMessage,
      showCloseButton
    } = this.state;
    if (!inputFieldElement || !message) {
      return null;
    }
    let iconComponent = /* @__PURE__ */ jsxRuntimeExports.jsx(SvgInfoCircularHollow, {});
    switch (priority) {
      case OutputMessagePriority.Warning:
        iconComponent = /* @__PURE__ */ jsxRuntimeExports.jsx(SvgStatusWarning, {});
        break;
      case OutputMessagePriority.Error:
        iconComponent = /* @__PURE__ */ jsxRuntimeExports.jsx(SvgStatusError, {});
        break;
      case OutputMessagePriority.Info:
        iconComponent = /* @__PURE__ */ jsxRuntimeExports.jsx(SvgInfoCircularHollow, {});
        break;
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Popup,
      {
        isOpen: isVisible,
        position: RelativePosition.BottomLeft,
        onClose: this._onInputMessageClose,
        target: inputFieldElement,
        children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "uifw-popup-message-inputField", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-popup-message-inputField-content", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "uifw-popup-message-inputField-primary", children: [
            iconComponent && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "uifw-popup-message-icon", children: [
              " ",
              /* @__PURE__ */ jsxRuntimeExports.jsx("i", { className: classnames("icon", "core-svg-icon"), children: iconComponent }),
              " "
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "uifw-popup-message-text", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                MessageRenderer$1,
                {
                  className: "uifw-popup-message-brief",
                  message
                }
              ),
              detailedMessage && /* @__PURE__ */ jsxRuntimeExports.jsx(
                MessageRenderer$1,
                {
                  className: "uifw-popup-message-detailed",
                  message: detailedMessage
                }
              )
            ] })
          ] }) }),
          showCloseButton && // eslint-disable-next-line jsx-a11y/click-events-have-key-events
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: "uifw-popup-message-close",
              onClick: this._onInputMessageClose,
              role: "button",
              tabIndex: -1,
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgClose, {}) })
            }
          )
        ] })
      }
    );
  }
  componentDidMount() {
    MessageManager.onInputFieldMessageAddedEvent.addListener(
      this._handleInputFieldMessageAddedEvent
    );
    MessageManager.onInputFieldMessageRemovedEvent.addListener(
      this._handleInputFieldMessageRemovedEvent
    );
  }
  componentWillUnmount() {
    MessageManager.onInputFieldMessageAddedEvent.removeListener(
      this._handleInputFieldMessageAddedEvent
    );
    MessageManager.onInputFieldMessageRemovedEvent.removeListener(
      this._handleInputFieldMessageRemovedEvent
    );
  }
}
try {
  InputFieldMessage.displayName = "InputFieldMessage";
  InputFieldMessage.__docgenInfo = { "description": "InputField message pops up near pointer when attempting an invalid interaction.", "displayName": "InputFieldMessage", "props": { "showCloseButton": { "defaultValue": null, "description": "", "name": "showCloseButton", "required": false, "type": { "name": "boolean" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function PopoutWidget({
  widgetContainerId,
  widgetDef
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className: "uifw-popout-widget-filled-container",
      "data-widget-id": widgetContainerId,
      children: widgetDef.reactNode
    }
  );
}
try {
  PopoutWidget.displayName = "PopoutWidget";
  PopoutWidget.__docgenInfo = { "description": "Component used to wrap a widget for use in a child window.", "displayName": "PopoutWidget", "props": { "widgetContainerId": { "defaultValue": null, "description": "", "name": "widgetContainerId", "required": true, "type": { "name": "string" } }, "widgetDef": { "defaultValue": null, "description": "", "name": "widgetDef", "required": true, "type": { "name": "WidgetDef" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class ConfigurableCreateInfo {
  constructor(classId, uniqueId, id) {
    this.classId = classId;
    this.uniqueId = uniqueId;
    this.id = id;
  }
}
class ConfigurableBase {
  constructor(info, options) {
    this._uniqueId = info.uniqueId;
    this._classId = info.classId;
    this._name = options && options.hasOwnProperty("name") ? options.name : info.uniqueId;
    this._appDataOptions = options;
  }
  /** @internal */
  get uniqueId() {
    return this._uniqueId;
  }
  /** allow options set via appData to be seen by API calls */
  get applicationData() {
    return this._appDataOptions;
  }
  /** Gets the class Id of configurable element */
  get classId() {
    return this._classId;
  }
  /** Get internal name of configurable element. If no name is defined in configuration
   * then the name will match the UniqueId.
   */
  get name() {
    return this._name;
  }
}
var ConfigurableUiControlType = /* @__PURE__ */ ((ConfigurableUiControlType2) => {
  ConfigurableUiControlType2["Content"] = "ContentControl";
  ConfigurableUiControlType2["NavigationAid"] = "NavigationAidControl";
  ConfigurableUiControlType2["StatusBarWidget"] = "StatusBarWidgetControl";
  ConfigurableUiControlType2["ToolUiProvider"] = "ToolUiProvider";
  ConfigurableUiControlType2["Viewport"] = "ViewportContentControl";
  ConfigurableUiControlType2["Widget"] = "WidgetControl";
  return ConfigurableUiControlType2;
})(ConfigurableUiControlType || {});
class ConfigurableUiControl extends ConfigurableBase {
  /** Creates an instance of ConfigurableUiControl.
   * @param info         An object that the subclass must pass to this base class.
   * @param options      Options provided to the control
   * @note Subclasses must pass all arguments to the base class and not add themselves
   * to any container - the control is added automatically.
   * @protected
   */
  constructor(info, options) {
    super(info, options);
    this._cid = info.id;
  }
  /** @internal
   */
  initialize() {
    this.onInitialize();
  }
  /** Called to initialize the ConfigurableUiControl. */
  onInitialize() {
  }
  /** Called when Frontstage is deactivated. */
  onFrontstageDeactivated() {
  }
  /** Called when Frontstage is ready. */
  onFrontstageReady() {
  }
  /** Returns the ID of this ConfigurableUiControl.
   */
  get controlId() {
    return this._cid;
  }
  /** Returns a promise that resolves when the control is ready for usage.
   */
  get isReady() {
    return Promise.resolve();
  }
}
class NavigationAidActivatedEvent extends UiEvent {
}
class NavigationAidControl extends ConfigurableUiControl {
  constructor(info, options) {
    super(info, options);
  }
  /** The React element associated with this control */
  get reactNode() {
    return this._reactNode;
  }
  set reactNode(r) {
    this._reactNode = r;
  }
  /** Square size of navigation aid. Default size is "64px". Override to set a different size. */
  getSize() {
    return void 0;
  }
  /** Get the type of this control. */
  getType() {
    return ConfigurableUiControlType.NavigationAid;
  }
}
try {
  NavigationAidActivatedEvent.displayName = "NavigationAidActivatedEvent";
  NavigationAidActivatedEvent.__docgenInfo = { "description": "NavigationAid Activated Event class.", "displayName": "NavigationAidActivatedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  NavigationAidControl.displayName = "NavigationAidControl";
  NavigationAidControl.__docgenInfo = { "description": "The base class for Navigation Aid controls.", "displayName": "NavigationAidControl", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const _CubeNavigationAidControl = class _CubeNavigationAidControl extends NavigationAidControl {
  constructor(info, options) {
    super(info, options);
    this.reactNode = /* @__PURE__ */ jsxRuntimeExports.jsx(
      CubeNavigationAid,
      {
        iModelConnection: options.imodel,
        viewport: options.viewport
      }
    );
  }
  // istanbul ignore next
  getSize() {
    return "96px";
  }
};
_CubeNavigationAidControl.navigationAidId = "CubeNavigationAid";
let CubeNavigationAidControl = _CubeNavigationAidControl;
try {
  CubeNavigationAidControl.displayName = "CubeNavigationAidControl";
  CubeNavigationAidControl.__docgenInfo = { "description": "Navigation Aid that displays an interactive rotation cube for Spatial views that synchronizes with the rotation of the iModel Viewport", "displayName": "CubeNavigationAidControl", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const _DrawingNavigationAidControl = class _DrawingNavigationAidControl extends NavigationAidControl {
  constructor(info, options) {
    super(info, options);
    this.reactNode = /* @__PURE__ */ jsxRuntimeExports.jsx(
      DrawingNavigationAid,
      {
        iModelConnection: options.imodel,
        viewport: options.viewport
      }
    );
  }
  // istanbul ignore next
  getSize() {
    return "96px";
  }
};
_DrawingNavigationAidControl.navigationAidId = "DrawingNavigationAid";
let DrawingNavigationAidControl = _DrawingNavigationAidControl;
try {
  DrawingNavigationAidControl.displayName = "DrawingNavigationAidControl";
  DrawingNavigationAidControl.__docgenInfo = { "description": "Navigation Aid that displays an interactive mini-map for Drawing views that synchronizes with the iModel Viewport.", "displayName": "DrawingNavigationAidControl", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class ViewUtilities {
  /**
   * Extracts the BisBaseClass from a full class name.
   * @param classFullName full class name
   * @returns BisBaseClass name
   */
  static getBisBaseClass(classFullName) {
    const bisBaseClass = classFullName.substring(
      classFullName.indexOf(":") + 1
    );
    return bisBaseClass;
  }
  /**
   * Determines if given class is a spatial view.
   * @param classname Name of class to check
   */
  static isSpatial(classname) {
    return classname === SpatialViewState.className || classname === OrthographicViewState.className;
  }
  /**
   * Determines if given class is a spatial view.
   * @param classname Name of class to check
   */
  static isOrthographic(classname) {
    return classname === OrthographicViewState.className;
  }
  /**
   * Determines if given class is a drawing view.
   * @param classname Name of class to check
   */
  static isDrawing(classname) {
    return classname === DrawingViewState.className;
  }
  /**
   * Determines if given class is a sheet view.
   * @param classname Name of class to check
   */
  static isSheet(classname) {
    return classname === SheetViewState.className;
  }
  /**
   * Determines if viewport displays a Sheet view.
   * @param viewport ScreenViewport to check
   */
  static isSheetView(viewport) {
    return ViewUtilities.isSheet(
      ViewUtilities.getBisBaseClass(viewport.view.classFullName)
    );
  }
  /**
   * Determines if viewport displays a Drawing view.
   * @param viewport ScreenViewport to check
   */
  static isDrawingView(viewport) {
    return ViewUtilities.isDrawing(
      ViewUtilities.getBisBaseClass(viewport.view.classFullName)
    );
  }
  /**
   * Determines if viewport displays a Orthographic view.
   * @param viewport ScreenViewport to check
   */
  static isOrthographicView(viewport) {
    return ViewUtilities.isOrthographic(
      ViewUtilities.getBisBaseClass(viewport.view.classFullName)
    );
  }
  /**
   * Determines if viewport displays a Spatial view.
   * @param viewport ScreenViewport to check
   */
  static isSpatialView(viewport) {
    return ViewUtilities.isSpatial(
      ViewUtilities.getBisBaseClass(viewport.view.classFullName)
    );
  }
  /**
   * Determines if viewport displays a 3d view.
   * @param viewport ScreenViewport to check
   */
  static is3dView(viewport) {
    return viewport.view.is3d();
  }
  /**
   * Determines if viewport supports use of a camera.
   * @param viewport ScreenViewport to check
   */
  static viewSupportsCamera(viewport) {
    return SpatialViewState.className === ViewUtilities.getBisBaseClass(viewport.view.classFullName);
  }
}
class CardSelectedEvent extends UiEvent {
}
class SheetsModalFrontstage {
  /**
   * Creates a SheetsModalFrontstage
   * @param sheets Collection of sheets available in SheetNavigationAid
   * @param connection IModelConnection to query for sheet ViewState
   */
  constructor(sheets, connection, currentIndex) {
    this.title = UiFramework.translate("navigationAid.sheetsModalFrontstage");
    this._cards = [];
    this._searchValue = "";
    this._handleSearchValueChanged = (value) => {
      this._searchValue = value;
      UiFramework.frontstages.updateModalFrontstage();
    };
    this._connection = connection;
    this._currentIndex = currentIndex;
    this._storeSheetsAsCards(sheets);
  }
  /**
   * Gathers card info from available sheets
   * @param sheets SheetData from available sheets
   */
  _storeSheetsAsCards(sheets) {
    sheets.forEach((sheet, index) => {
      this._cards.push({
        index,
        label: sheet.name,
        iconSpec: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgDocument, {}),
        viewId: sheet.viewId,
        isActive: index === this._currentIndex
      });
    });
  }
  /** Gets set of cards */
  get content() {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      CardContainer,
      {
        cards: this._cards,
        searchValue: this._searchValue,
        connection: this._connection
      }
    );
  }
  /** Gets components to be placed in the app bar */
  get appBarRight() {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SearchBox,
      {
        placeholder: UiFramework.translate("general.search"),
        onValueChanged: this._handleSearchValueChanged,
        valueChangedDelay: 250
      }
    );
  }
}
const _CardContainer = class _CardContainer extends reactExports.Component {
  /** Get CardSelectedEvent event */
  static get onCardSelectedEvent() {
    return _CardContainer._cardSelectedEvent;
  }
  /** @internal */
  render() {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollView, { className: this.props.className, style: this.props.style, children: /* @__PURE__ */ jsxRuntimeExports.jsx(FlexWrapContainer, { children: this.props.cards.map((card, _index) => {
      let includeCard = true;
      if (this.props.searchValue) {
        includeCard = this.contains(card.label, this.props.searchValue);
      }
      if (includeCard) {
        return /* @__PURE__ */ jsxRuntimeExports.jsx(
          SheetCard,
          {
            label: card.label,
            index: card.index,
            iconSpec: card.iconSpec,
            isActive: card.isActive,
            onClick: async () => this._handleCardSelected(card)
          },
          card.label
        );
      }
      return null;
    }) }) });
  }
  /**
   * Determines if string contains a substring
   * @param valueA The string to search through
   * @param valueB The value to search for
   * @return True if valueB can be found in valueA, false otherwise
   */
  contains(valueA, valueB) {
    if (!valueA || !valueB)
      return false;
    if (valueB.length > valueA.length)
      return false;
    return valueA.toLocaleUpperCase().indexOf(valueB.toLocaleUpperCase(), 0) !== -1;
  }
  /**
   * Updates view with ViewState for selected card.
   * @param card Data about the sheet card selected.
   */
  async _handleCardSelected(card) {
    if (IModelApp.viewManager && IModelApp.viewManager.selectedView) {
      const vp = IModelApp.viewManager.selectedView;
      const viewState = await this.props.connection.views.load(card.viewId);
      vp.changeView(viewState);
    }
    card.isActive = true;
    UiFramework.frontstages.closeModalFrontstage();
    _CardContainer.onCardSelectedEvent.emit({
      id: card.viewId,
      index: card.index
    });
  }
};
_CardContainer._cardSelectedEvent = new CardSelectedEvent();
let CardContainer = _CardContainer;
class SheetCard extends reactExports.Component {
  constructor(props) {
    super(props);
    this._onClick = () => {
      this.setState({ isActive: true }, () => this.props.onClick());
    };
    this._onMouseDown = () => {
      this.setState({ isPressed: true });
    };
    this._onMouseLeave = () => {
      if (this.state.isPressed)
        this.setState({ isPressed: false });
    };
    this.state = { isActive: this.props.isActive, isPressed: false };
  }
  render() {
    const { label, index } = this.props;
    const className = classnames(
      "uifw-sheet-card",
      this.state.isActive && "is-active",
      this.state.isPressed && "is-pressed"
    );
    const iconSpec = this.props.iconSpec ? this.props.iconSpec : /* @__PURE__ */ jsxRuntimeExports.jsx(SvgPlaceholder, {});
    return (
      // eslint-disable-next-line jsx-a11y/click-events-have-key-events
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          className,
          onClick: this._onClick,
          onMouseDown: this._onMouseDown,
          onMouseLeave: this._onMouseLeave,
          role: "button",
          tabIndex: -1,
          children: [
            label,
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "sheet-image-container", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "icon", children: [
              " ",
              /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec })
            ] }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "sheet-index", children: index + 1 })
          ]
        }
      )
    );
  }
}
try {
  CardSelectedEvent.displayName = "CardSelectedEvent";
  CardSelectedEvent.__docgenInfo = { "description": "Class for CardSelectedEvent", "displayName": "CardSelectedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  SheetsModalFrontstage.displayName = "SheetsModalFrontstage";
  SheetsModalFrontstage.__docgenInfo = { "description": "Modal frontstage displaying sheet information in cards.", "displayName": "SheetsModalFrontstage", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  CardContainer.displayName = "CardContainer";
  CardContainer.__docgenInfo = { "description": "Displays cards in SheetModalFrontstage", "displayName": "CardContainer", "props": { "cards": { "defaultValue": null, "description": "", "name": "cards", "required": true, "type": { "name": "CardInfo[]" } }, "searchValue": { "defaultValue": null, "description": "", "name": "searchValue", "required": true, "type": { "name": "string" } }, "connection": { "defaultValue": null, "description": "", "name": "connection", "required": true, "type": { "name": "IModelConnection" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  CardContainer._cardSelectedEvent.displayName = "CardContainer._cardSelectedEvent";
  CardContainer._cardSelectedEvent.__docgenInfo = { "description": "Class for CardSelectedEvent", "displayName": "CardContainer._cardSelectedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  CardContainer.onCardSelectedEvent.displayName = "CardContainer.onCardSelectedEvent";
  CardContainer.onCardSelectedEvent.__docgenInfo = { "description": "Get CardSelectedEvent event", "displayName": "CardContainer.onCardSelectedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  SheetCard.displayName = "SheetCard";
  SheetCard.__docgenInfo = { "description": "Displays information about an individual sheet", "displayName": "SheetCard", "props": { "label": { "defaultValue": null, "description": "", "name": "label", "required": true, "type": { "name": "string" } }, "index": { "defaultValue": null, "description": "", "name": "index", "required": true, "type": { "name": "number" } }, "iconSpec": { "defaultValue": null, "description": "", "name": "iconSpec", "required": true, "type": { "name": "IconSpec" } }, "isActive": { "defaultValue": null, "description": "", "name": "isActive", "required": true, "type": { "name": "boolean" } }, "onClick": { "defaultValue": null, "description": "", "name": "onClick", "required": true, "type": { "name": "() => void" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const _SheetNavigationAidControl = class _SheetNavigationAidControl extends NavigationAidControl {
  constructor(info, options) {
    super(info, options);
    this.reactNode = /* @__PURE__ */ jsxRuntimeExports.jsx(SheetNavigationAid, { iModelConnection: options.imodel });
  }
  // istanbul ignore next
  getSize() {
    return "96px";
  }
};
_SheetNavigationAidControl.navigationAidId = "SheetNavigationAid";
let SheetNavigationAidControl = _SheetNavigationAidControl;
class SheetNavigationAid extends reactExports.Component {
  constructor(props) {
    super(props);
    this._isMounted = false;
    this.state = {
      index: 0,
      sheetData: []
    };
    this._handleCardSelected = (event) => {
      event && this.setState({
        index: event.index
      });
    };
    this._handleOnClickLeftArrow = () => {
      this.setState(
        (prevState) => ({
          index: prevState.index <= 0 ? prevState.sheetData.length - 1 : prevState.index - 1
        }),
        async () => this._updateView()
      );
    };
    this._handleOnClickRightArrow = () => {
      this.setState(
        (prevState) => ({
          index: (prevState.index + 1) % prevState.sheetData.length
        }),
        async () => this._updateView()
      );
    };
    this._handleSelectedViewportChanged = (args) => {
      if (args.current) {
        this._handleViewportChanged(args.current);
      }
    };
    this._handleViewIdChanged = (args) => {
      if (this._viewport === args.viewport)
        this._handleViewportChanged(args.viewport);
    };
    this._handleViewportChanged = (viewport) => {
      const className = ViewUtilities.getBisBaseClass(
        viewport.view.classFullName
      );
      if (ViewUtilities.isSheet(className)) {
        this._viewport = viewport;
        const viewId = this._viewport.view.id.toString();
        const index = this.state.sheetData.findIndex((sheetData) => {
          return viewId === sheetData.viewId;
        });
        if (index >= 0)
          this.setState({ index });
      }
    };
    this._handleOnClickSheetName = () => {
      UiFramework.frontstages.openModalFrontstage(this.modalFrontstage());
    };
    this._viewport = IModelApp.viewManager.selectedView;
  }
  /** Adds listeners when components mounts */
  async componentDidMount() {
    this._isMounted = true;
    CardContainer.onCardSelectedEvent.addListener(this._handleCardSelected);
    ViewportComponentEvents.onViewIdChangedEvent.addListener(
      this._handleViewIdChanged
    );
    IModelApp.viewManager.onSelectedViewportChanged.addListener(
      this._handleSelectedViewportChanged
    );
    const stateData = await this._setupSheets();
    if (this._isMounted)
      this.setState(stateData);
  }
  /** Removes listeners when component will unmount */
  componentWillUnmount() {
    this._isMounted = false;
    CardContainer.onCardSelectedEvent.removeListener(this._handleCardSelected);
    ViewportComponentEvents.onViewIdChangedEvent.removeListener(
      this._handleViewIdChanged
    );
    IModelApp.viewManager.onSelectedViewportChanged.removeListener(
      this._handleSelectedViewportChanged
    );
  }
  /** Queries for sheet info and sets as sheetData */
  async _setupSheets() {
    const stateData = {
      index: 0,
      sheetData: []
    };
    if (!this.props.iModelConnection || !this.props.iModelConnection.views.getViewList)
      return stateData;
    let viewId = "";
    if (this._viewport) {
      viewId = this._viewport.view.id.toString();
    }
    const sheets = await this.props.iModelConnection.views.getViewList({
      from: "BisCore.SheetViewDefinition"
    });
    sheets.forEach((viewSpec, index) => {
      stateData.sheetData.push({ name: viewSpec.name, viewId: viewSpec.id });
      if (viewSpec.id === viewId)
        stateData.index = index;
    });
    return stateData;
  }
  /** @internal */
  render() {
    const name = this.state.sheetData.length > 0 ? (
      /* istanbul ignore next */
      this.state.sheetData[this.state.index].name
    ) : "";
    const leftIndex = this.state.index === 0 ? this.state.sheetData.length - 1 : (
      /* istanbul ignore next */
      this.state.index - 1
    );
    const rightIndex = this.state.index >= this.state.sheetData.length - 1 ? 0 : (
      /* istanbul ignore next */
      this.state.index + 1
    );
    const leftTitle = this.state.sheetData[leftIndex] ? (
      /* istanbul ignore next */
      this.state.sheetData[leftIndex].name
    ) : void 0;
    const rightTitle = this.state.sheetData[rightIndex] ? (
      /* istanbul ignore next */
      this.state.sheetData[rightIndex].name
    ) : void 0;
    let content;
    if (this.state.sheetData.length > 0) {
      content = /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "sheet-title", children: UiFramework.translate("general.sheet") }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "div",
          {
            className: "sheet-name",
            title: name,
            onClick: this._handleOnClickSheetName,
            role: "button",
            tabIndex: -1,
            children: name
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "sheet-container", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            IconButton,
            {
              size: "small",
              styleType: "borderless",
              onClick: this._handleOnClickLeftArrow,
              tabIndex: -1,
              title: leftTitle,
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgChevronLeft, {})
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            this.state.index + 1,
            " ",
            UiFramework.translate("general.of"),
            this.state.sheetData.length
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            IconButton,
            {
              size: "small",
              styleType: "borderless",
              onClick: this._handleOnClickRightArrow,
              role: "button",
              tabIndex: -1,
              title: rightTitle,
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgChevronRight, {})
            }
          )
        ] })
      ] });
    } else {
      content = /* @__PURE__ */ jsxRuntimeExports.jsx(ProgressRadial, { size: "x-small", indeterminate: true });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: classnames("uifw-sheet-navigation", this.props.className),
        style: this.props.style,
        children: content
      }
    );
  }
  /** Updates view to currently set sheet */
  // istanbul ignore next
  async _updateView() {
    const viewState = await this.props.iModelConnection.views.load(
      this.state.sheetData[this.state.index].viewId
    );
    if (this._viewport)
      this._viewport.changeView(viewState);
  }
  /** Creates a new SheetsModalFrontstage */
  // istanbul ignore next
  modalFrontstage() {
    return new SheetsModalFrontstage(
      this.state.sheetData,
      this.props.iModelConnection,
      this.state.index
    );
  }
}
try {
  SheetNavigationAidControl.displayName = "SheetNavigationAidControl";
  SheetNavigationAidControl.__docgenInfo = { "description": "A Sheet Navigation Aid control.", "displayName": "SheetNavigationAidControl", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  SheetNavigationAid.displayName = "SheetNavigationAid";
  SheetNavigationAid.__docgenInfo = { "description": "A Sheet Navigation Aid.", "displayName": "SheetNavigationAid", "props": { "iModelConnection": { "defaultValue": null, "description": "", "name": "iModelConnection", "required": true, "type": { "name": "IModelConnection" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class ExpandableButton extends reactExports.PureComponent {
  render() {
    const className = classnames(
      "nz-widget-tools-button-expandable",
      this.props.className
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, style: this.props.style, children: [
      this.props.children,
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-triangle" })
    ] });
  }
}
try {
  ExpandableButton.displayName = "ExpandableButton";
  ExpandableButton.__docgenInfo = { "description": "Expandable toolbar button. Used in [[Toolbar]] component.", "displayName": "ExpandableButton", "props": { "children": { "defaultValue": null, "description": "One of toolbar buttons. I.e. [[Item]]", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class ToolbarButton extends reactExports.PureComponent {
  render() {
    const className = classnames(
      "nz-toolbar-button-button",
      this.props.className
    );
    const buttonStyle = {
      ...this.props.style
    };
    if (this.props.small) {
      let backgroundOpacity = Number(
        document.documentElement.style.getPropertyValue(
          "--buic-toolbar-opacity"
        )
      );
      let boxShadowOpacity = TOOLBAR_BOX_SHADOW_OPACITY_DEFAULT;
      let filterBlur = TOOLBAR_BACKDROP_FILTER_BLUR_DEFAULT;
      if (this.props.mouseProximity !== void 0) {
        backgroundOpacity = calculateToolbarOpacity(this.props.mouseProximity);
        boxShadowOpacity = calculateBoxShadowOpacity(this.props.mouseProximity);
        filterBlur = calculateBackdropFilterBlur(this.props.mouseProximity);
      }
      buttonStyle.backgroundColor = getToolbarBackgroundColor(backgroundOpacity);
      buttonStyle.boxShadow = getToolbarBoxShadow(boxShadowOpacity);
      buttonStyle.backdropFilter = getToolbarBackdropFilter(filterBlur);
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "button",
      {
        className,
        style: buttonStyle,
        onClick: this.props.onClick,
        title: this.props.title,
        children: [
          !this.props.small && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-gradient" }),
          this.props.children
        ]
      }
    );
  }
}
try {
  ToolbarButton.displayName = "ToolbarButton";
  ToolbarButton.__docgenInfo = { "description": "Basic toolbar button. Used in [[Toolbar]] component.", "displayName": "ToolbarButton", "props": { "children": { "defaultValue": null, "description": "Button content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "onClick": { "defaultValue": null, "description": "Function called when the button is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "small": { "defaultValue": null, "description": "Indicates whether to use a small App button", "name": "small", "required": false, "type": { "name": "boolean" } }, "mouseProximity": { "defaultValue": null, "description": "Mouse proximity to button", "name": "mouseProximity", "required": false, "type": { "name": "number" } }, "title": { "defaultValue": null, "description": "Tooltip for button", "name": "title", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class ToolbarIcon extends reactExports.PureComponent {
  render() {
    const { className, small, ...props } = this.props;
    const buttonClassName = classnames(
      "nz-toolbar-button-icon",
      small && "nz-toolbar-button-icon-small",
      className
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(ToolbarButton, { className: buttonClassName, small, ...props, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-icon", children: this.props.icon }),
      this.props.children
    ] });
  }
}
try {
  ToolbarIcon.displayName = "ToolbarIcon";
  ToolbarIcon.__docgenInfo = { "description": "Toolbar button which displays icon. Used in [[Toolbar]] component.", "displayName": "ToolbarIcon", "props": { "icon": { "defaultValue": null, "description": "Button icon.", "name": "icon", "required": false, "type": { "name": "ReactNode" } }, "small": { "defaultValue": null, "description": "Indicates whether to use a small App button", "name": "small", "required": false, "type": { "name": "boolean" } }, "mouseProximity": { "defaultValue": null, "description": "Mouse proximity to button", "name": "mouseProximity", "required": false, "type": { "name": "number" } }, "children": { "defaultValue": null, "description": "Button content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "onClick": { "defaultValue": null, "description": "Function called when the button is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "title": { "defaultValue": null, "description": "Tooltip for button", "name": "title", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const _StandardRotationNavigationAidControl = class _StandardRotationNavigationAidControl extends NavigationAidControl {
  constructor(info, options) {
    super(info, options);
    this.reactNode = /* @__PURE__ */ jsxRuntimeExports.jsx(StandardRotationNavigationAid, {});
  }
};
_StandardRotationNavigationAidControl.navigationAidId = "StandardRotationNavigationAid";
let StandardRotationNavigationAidControl = _StandardRotationNavigationAidControl;
class StandardRotationNavigationAid extends reactExports.Component {
  constructor(props) {
    super(props);
    this._handleRef = (element) => {
      this.setState(() => ({ element }));
    };
    this._handlePopupClose = () => {
      this.setState(() => ({ isExpanded: false }));
    };
    this._toggleIsExpanded = () => {
      this.setState((prevState) => ({ isExpanded: !prevState.isExpanded }));
    };
    this._handleListItemClicked = (item) => {
      const selected = item;
      this.setState(
        () => ({ isExpanded: false, selected }),
        () => ViewportComponentEvents.setStandardRotation(selected)
      );
    };
    const list = [
      {
        label: UiFramework.translate("rotations.top"),
        iconsSpec: svgCubeFacesTop
      },
      {
        label: UiFramework.translate("rotations.bottom"),
        iconsSpec: svgCubeFacesBottom
      },
      {
        label: UiFramework.translate("rotations.left"),
        iconsSpec: svgCubeFacesLeft
      },
      {
        label: UiFramework.translate("rotations.right"),
        iconsSpec: svgCubeFacesRight
      },
      {
        label: UiFramework.translate("rotations.front"),
        iconsSpec: svgCubeFacesFront
      },
      {
        label: UiFramework.translate("rotations.rear"),
        iconsSpec: svgCubeFacesRear
      },
      {
        label: UiFramework.translate("rotations.isoLeft"),
        iconsSpec: svgCubeFacesIsoLeft
      },
      {
        label: UiFramework.translate("rotations.isoRight"),
        iconsSpec: svgCubeFacesIsoRight
      }
    ];
    this.state = {
      element: null,
      isExpanded: false,
      list,
      selected: StandardViewId.Top
    };
  }
  render() {
    const className = classnames(
      "uifw-standard-rotation-navigation",
      this.props.className
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, style: this.props.style, ref: this._handleRef, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(ExpandableButton, { className: "expandable", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        ToolbarIcon,
        {
          className: "icon-button",
          icon: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: classnames("three-d-icon", "icon"), children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            Icon$1,
            {
              iconSpec: this.state.list[this.state.selected].iconsSpec
            }
          ) }),
          onClick: this._toggleIsExpanded,
          title: UiFramework.translate("standardRotationNavigationAid.title")
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Popup,
        {
          isOpen: this.state.isExpanded,
          offset: 0,
          onClose: this._handlePopupClose,
          position: RelativePosition.Bottom,
          target: this.state.element,
          children: this.getExpandedContent()
        }
      )
    ] });
  }
  getExpandedContent() {
    if (!this.state.isExpanded)
      return void 0;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Panel$1, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(Columns, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(GroupColumn, { children: this.state.list.map((item, itemIndex) => {
      return /* @__PURE__ */ jsxRuntimeExports.jsx(
        GroupTool,
        {
          label: item.label,
          icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec: item.iconsSpec }),
          isActive: this.state.selected.valueOf() === itemIndex,
          onClick: () => this._handleListItemClicked(itemIndex),
          item: ToolbarItemUtilities.createActionButton(
            "",
            0,
            "",
            "",
            () => {
            }
          )
        },
        itemIndex.toString()
      );
    }) }) }) });
  }
}
try {
  StandardRotationNavigationAidControl.displayName = "StandardRotationNavigationAidControl";
  StandardRotationNavigationAidControl.__docgenInfo = { "description": "A 3D Standard Rotation Navigation Aid control.", "displayName": "StandardRotationNavigationAidControl", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  StandardRotationNavigationAid.displayName = "StandardRotationNavigationAid";
  StandardRotationNavigationAid.__docgenInfo = { "description": "A 3D Standard Rotation Navigation Aid.", "displayName": "StandardRotationNavigationAid", "props": { "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class ActivityTracker {
  constructor() {
    this._intervalTimeout = 1e3;
    this._events = [
      "keydown",
      "wheel",
      "scroll",
      "mousedown",
      "mousemove",
      "touchstart",
      "touchmove",
      "pointerdown",
      "pointermove",
      "visibilitychange"
    ];
    this._bindEvents = () => {
      this._events.forEach((e) => {
        window.addEventListener(e, this._trackUiActivity, { capture: true });
      });
    };
    this._unbindEvents = () => {
      this._events.forEach((e) => {
        window.removeEventListener(e, this._trackUiActivity, { capture: true });
      });
    };
    this._trackUiInterval = () => {
      InternalConfigurableUiManager.onUiIntervalEvent.emit({
        idleTimeout: this._idleTimeout
      });
    };
    this._trackUiActivity = (event) => {
      InternalConfigurableUiManager.onUiActivityEvent.emit({ event });
    };
  }
  /** Initializes the time tracker and adds event listeners
   */
  initialize(props) {
    if (props) {
      this._idleTimeout = props.idleTimeout;
      if (props.intervalTimeout !== void 0)
        this._intervalTimeout = props.intervalTimeout;
    }
    this._bindEvents();
    this._intervalId = setInterval(
      this._trackUiInterval,
      this._intervalTimeout
    );
  }
  /** Terminates the time tracker and removes event listeners
   */
  terminate() {
    this._unbindEvents();
    if (this._intervalId) {
      clearInterval(this._intervalId);
      this._intervalId = void 0;
    }
  }
}
class ContentControlActivatedEvent extends UiEvent {
}
class ContentControl extends ConfigurableUiControl {
  /** Creates an instance of ContentControl.
   * @param info         An object that the subclass must pass to this base class.
   * @param options      Options provided via the applicationData in a [[ContentProps]].
   */
  constructor(info, options) {
    super(info, options);
    this._keyAdded = false;
  }
  /** Called when this ContentControl is activated */
  onActivated() {
  }
  /** Called when this ContentControl is deactivated */
  onDeactivated() {
  }
  /** Gets the type of ConfigurableUiControl, which is 'Content' in this case */
  getType() {
    return ConfigurableUiControlType.Content;
  }
  /** Returns true if this control is a Viewport control. */
  get isViewport() {
    return false;
  }
  /** Returns the ScreenViewport if isViewport is true */
  get viewport() {
    return void 0;
  }
  getKeyedReactNode() {
    if (!this._keyAdded && reactExports.isValidElement(this._reactNode)) {
      if (!this._reactNode.key) {
        const additionalProps = { key: this.uniqueId };
        this._reactNode = reactExports.cloneElement(this._reactNode, additionalProps);
      }
      this._keyAdded = true;
    }
    return this._reactNode;
  }
  getReactNode() {
    return this.getKeyedReactNode();
  }
  /** The React node associated with this control. */
  get reactNode() {
    return this.getReactNode();
  }
  set reactNode(r) {
    this._reactNode = r;
  }
  /** Get the NavigationAidControl associated with this ContentControl */
  get navigationAidControl() {
    return "";
  }
}
try {
  ContentControlActivatedEvent.displayName = "ContentControlActivatedEvent";
  ContentControlActivatedEvent.__docgenInfo = { "description": "ContentControl Activated Event class.", "displayName": "ContentControlActivatedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ContentControl.displayName = "ContentControl";
  ContentControl.__docgenInfo = { "description": "The base class for Frontstage content controls.", "displayName": "ContentControl", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function Pane(props) {
  const { children, className, split, style, size, eleRef } = props;
  const paneClasses = reactExports.useMemo(
    () => classnames("Pane", split, className),
    [split, className]
  );
  const paneStyle = reactExports.useMemo(() => {
    const baseStyle = {
      flex: 1,
      position: "relative",
      outline: "none"
    };
    if (size !== void 0) {
      if (split === "vertical") {
        baseStyle.width = size;
      } else {
        baseStyle.height = size;
        baseStyle.display = "flex";
      }
      baseStyle.flex = "none";
    }
    return {
      ...style,
      ...baseStyle
    };
  }, [size, split, style]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref: eleRef, className: paneClasses, style: paneStyle, children });
}
try {
  Pane.displayName = "Pane";
  Pane.__docgenInfo = { "description": "", "displayName": "Pane", "props": { "className": { "defaultValue": null, "description": "", "name": "className", "required": false, "type": { "name": "string" } }, "size": { "defaultValue": null, "description": "", "name": "size", "required": false, "type": { "name": "string | number" } }, "split": { "defaultValue": null, "description": "", "name": "split", "required": false, "type": { "name": "enum", "value": [{ "value": '"horizontal"' }, { "value": '"vertical"' }] } }, "style": { "defaultValue": null, "description": "", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "eleRef": { "defaultValue": null, "description": "", "name": "eleRef", "required": true, "type": { "name": "RefObject<HTMLDivElement>" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function Resizer(props) {
  const {
    className,
    onClick,
    onDoubleClick,
    onMouseDown,
    onTouchEnd,
    onTouchStart,
    split,
    style
  } = props;
  const resizerClasses = reactExports.useMemo(
    () => classnames(split, className),
    [split, className]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "span",
    {
      role: "presentation",
      className: resizerClasses,
      style,
      onMouseDown: (event) => onMouseDown(event.nativeEvent),
      onTouchStart: (event) => {
        onTouchStart(event.nativeEvent);
      },
      onTouchEnd: (event) => {
        event.preventDefault();
        onTouchEnd();
      },
      onClick: (event) => {
        if (onClick) {
          event.preventDefault();
          onClick(event.nativeEvent);
        }
      },
      onDoubleClick: (event) => {
        if (onDoubleClick) {
          event.preventDefault();
          onDoubleClick(event.nativeEvent);
        }
      },
      children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-resizer-grip" })
    }
  );
}
try {
  Resizer.displayName = "Resizer";
  Resizer.__docgenInfo = { "description": "", "displayName": "Resizer", "props": { "split": { "defaultValue": null, "description": "", "name": "split", "required": false, "type": { "name": "enum", "value": [{ "value": '"horizontal"' }, { "value": '"vertical"' }] } }, "style": { "defaultValue": null, "description": "", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "className": { "defaultValue": null, "description": "", "name": "className", "required": true, "type": { "name": "string" } }, "onMouseDown": { "defaultValue": null, "description": "", "name": "onMouseDown", "required": true, "type": { "name": "(event: MouseEvent) => void" } }, "onTouchEnd": { "defaultValue": null, "description": "", "name": "onTouchEnd", "required": true, "type": { "name": "() => void" } }, "onTouchStart": { "defaultValue": null, "description": "", "name": "onTouchStart", "required": true, "type": { "name": "(event: TouchEvent) => void" } }, "onClick": { "defaultValue": null, "description": "", "name": "onClick", "required": false, "type": { "name": "((event: MouseEvent) => void)" } }, "onDoubleClick": { "defaultValue": null, "description": "", "name": "onDoubleClick", "required": false, "type": { "name": "((event: MouseEvent) => void)" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function unFocus(ownerDoc) {
  var _a2;
  if (!ownerDoc)
    return;
  const docSelection = ownerDoc.getSelection();
  if (docSelection) {
    docSelection.empty();
  } else {
    try {
      const winSelection = (_a2 = ownerDoc.defaultView) == null ? void 0 : _a2.getSelection();
      winSelection == null ? void 0 : winSelection.removeAllRanges();
    } catch (_e) {
    }
  }
}
function getDefaultSize(defaultSize, minSize, maxSize, draggedSize) {
  if (typeof draggedSize === "number") {
    const min = typeof minSize === "number" ? minSize : 0;
    const max = typeof maxSize === "number" && maxSize >= 0 ? maxSize : Infinity;
    return Math.max(min, Math.min(max, draggedSize));
  }
  if (defaultSize !== void 0) {
    return defaultSize;
  }
  return minSize;
}
function removeNullChildren(children) {
  return reactExports.Children.toArray(children).filter((c) => c);
}
function SplitPane(props) {
  const {
    style,
    size,
    defaultSize,
    maxSize,
    children,
    paneStyle,
    pane1Style,
    pane2Style,
    className,
    paneClassName,
    pane1ClassName,
    pane2ClassName,
    onDragStarted,
    onDragFinished,
    step,
    onChange,
    onResizerClick,
    onResizerDoubleClick
  } = props;
  const allowResize = reactExports.useMemo(
    () => void 0 !== props.allowResize ? props.allowResize : true,
    [props.allowResize]
  );
  const minSize = reactExports.useMemo(
    () => void 0 !== props.minSize ? props.minSize : 50,
    [props.minSize]
  );
  const primary = reactExports.useMemo(
    () => void 0 !== props.primary ? props.primary : "first",
    [props.primary]
  );
  const split = reactExports.useMemo(
    () => void 0 !== props.split ? props.split : "vertical",
    [props.split]
  );
  const initialSize = size !== void 0 ? size : getDefaultSize(defaultSize, minSize, maxSize);
  const [position, setPosition] = reactExports.useState(0);
  const [draggedSize, setDraggedSize] = reactExports.useState();
  const [active, setActive] = reactExports.useState(false);
  const [pane1Size, setPane1Size] = reactExports.useState(
    () => primary === "first" ? initialSize : void 0
  );
  const [pane2Size, setPane2Size] = reactExports.useState(
    () => primary === "second" ? initialSize : void 0
  );
  const splitPane = reactExports.useRef(null);
  const pane1 = reactExports.useRef(null);
  const pane2 = reactExports.useRef(null);
  const notNullChildren = reactExports.useMemo(
    () => removeNullChildren(children),
    [children]
  );
  reactExports.useEffect(() => {
    primary === "first" ? setPane1Size(initialSize) : setPane2Size(initialSize);
    primary === "first" ? setPane2Size(void 0) : setPane1Size(void 0);
  }, [initialSize, primary]);
  const splitPaneStyle = reactExports.useMemo(() => {
    const directionSpecificParts = split === "vertical" ? {
      flexDirection: "row",
      left: 0,
      right: 0
    } : {
      bottom: 0,
      flexDirection: "column",
      minHeight: "100%",
      top: 0,
      width: "100%"
    };
    return {
      display: "flex",
      flex: 1,
      height: "100%",
      position: "absolute",
      outline: "none",
      overflow: "hidden",
      MozUserSelect: "text",
      WebkitUserSelect: "text",
      msUserSelect: "text",
      userSelect: "text",
      ...style,
      ...directionSpecificParts
    };
  }, [split, style]);
  const pane1DivStyle = { ...paneStyle, ...pane1Style };
  const pane2DivStyle = { ...paneStyle, ...pane2Style };
  const resizerStyle = reactExports.useMemo(
    () => props.resizerStyle ?? {},
    [props.resizerStyle]
  );
  const resizerClasses = reactExports.useMemo(
    () => classnames("Resizer", !allowResize && "disabled"),
    [allowResize]
  );
  const splitPaneClasses = reactExports.useMemo(
    () => classnames("SplitPane", className, split, !allowResize && "disabled"),
    [className, split, allowResize]
  );
  const pane1Classes = reactExports.useMemo(
    () => classnames("Pane1", paneClassName, pane1ClassName),
    [paneClassName, pane1ClassName]
  );
  const pane2Classes = reactExports.useMemo(
    () => classnames("Pane2", paneClassName, pane2ClassName),
    [paneClassName, pane2ClassName]
  );
  const initializeDrag = reactExports.useCallback(
    (x, y) => {
      var _a2;
      unFocus((_a2 = splitPane.current) == null ? void 0 : _a2.ownerDocument);
      const newPosition = split === "vertical" ? x : y;
      onDragStarted && onDragStarted();
      setActive(true);
      setPosition(newPosition);
    },
    [onDragStarted, split]
  );
  const onTouchStart = reactExports.useCallback(
    (event) => {
      if (allowResize) {
        initializeDrag(event.touches[0].clientX, event.touches[0].clientY);
      }
    },
    [allowResize, initializeDrag]
  );
  const processMove = reactExports.useCallback(
    (x, y) => {
      var _a2, _b2, _c2, _d, _e;
      unFocus((_a2 = splitPane.current) == null ? void 0 : _a2.ownerDocument);
      const isPrimaryFirst = primary === "first";
      const ref = isPrimaryFirst ? pane1.current : pane2.current;
      const ref2 = isPrimaryFirst ? pane2.current : pane1.current;
      const splitPaneDiv = splitPane.current;
      if (ref && ref2 && splitPaneDiv) {
        const node = ref;
        const node2 = ref2;
        if (node.getBoundingClientRect) {
          const width = node.getBoundingClientRect().width;
          const height = node.getBoundingClientRect().height;
          const current = split === "vertical" ? x : y;
          const oldSize = split === "vertical" ? width : height;
          let positionDelta = position - current;
          if (step) {
            if (Math.abs(positionDelta) < step) {
              return;
            }
            positionDelta = ~~(positionDelta / step) * step;
          }
          let sizeDelta = isPrimaryFirst ? positionDelta : -positionDelta;
          const pane1Order = parseInt(
            ((_c2 = (_b2 = node.ownerDocument) == null ? void 0 : _b2.defaultView) == null ? void 0 : _c2.getComputedStyle(node).order) ?? "0",
            10
          );
          const pane2Order = parseInt(
            ((_e = (_d = node2.ownerDocument) == null ? void 0 : _d.defaultView) == null ? void 0 : _e.getComputedStyle(node2).order) ?? "0",
            10
          );
          if (pane1Order > pane2Order) {
            sizeDelta = -sizeDelta;
          }
          let newMaxSize = maxSize;
          if (typeof maxSize === "number" && maxSize !== void 0 && maxSize <= 0) {
            if (split === "vertical") {
              newMaxSize = splitPaneDiv.getBoundingClientRect().width + maxSize;
            } else {
              newMaxSize = splitPaneDiv.getBoundingClientRect().height + maxSize;
            }
          }
          let newSize = oldSize - sizeDelta;
          const newPosition = position - positionDelta;
          if (typeof minSize === "number" && newSize < minSize) {
            newSize = minSize;
          } else if (typeof newMaxSize === "number" && newMaxSize !== void 0 && newSize > newMaxSize) {
            newSize = newMaxSize;
          } else {
            setPosition(newPosition);
          }
          onChange && onChange(newSize);
          setDraggedSize(newSize);
          isPrimaryFirst ? setPane1Size(newSize) : setPane2Size(newSize);
        }
      }
    },
    [maxSize, minSize, onChange, position, primary, split, step]
  );
  const onTouchMove = reactExports.useCallback(
    (event) => {
      if (!allowResize || !active)
        return;
      processMove(event.touches[0].clientX, event.touches[0].clientY);
    },
    [active, allowResize, processMove]
  );
  const onMouseMove = reactExports.useCallback(
    (event) => {
      if (!allowResize || !active)
        return;
      processMove(event.clientX, event.clientY);
    },
    [active, allowResize, processMove]
  );
  const onMouseDown = reactExports.useCallback(
    (event) => {
      if (allowResize) {
        event.preventDefault();
        initializeDrag(event.clientX, event.clientY);
      }
    },
    [allowResize, initializeDrag]
  );
  const processResizeFinished = reactExports.useCallback(() => {
    if (void 0 !== draggedSize && allowResize && active) {
      onDragFinished && onDragFinished(draggedSize);
    }
    setActive(false);
  }, [draggedSize, allowResize, active, onDragFinished]);
  const onMouseUp = reactExports.useCallback(
    (event) => {
      event.preventDefault();
      processResizeFinished();
    },
    [processResizeFinished]
  );
  reactExports.useEffect(() => {
    var _a2;
    const ownerDoc = (_a2 = splitPane.current) == null ? void 0 : _a2.ownerDocument;
    if (!ownerDoc)
      return;
    ownerDoc.addEventListener("mouseup", onMouseUp);
    ownerDoc.addEventListener("mousemove", onMouseMove);
    ownerDoc.addEventListener("touchmove", onTouchMove);
    return () => {
      ownerDoc.removeEventListener("mouseup", onMouseUp);
      ownerDoc.removeEventListener("mousemove", onMouseMove);
      ownerDoc.removeEventListener("touchmove", onTouchMove);
    };
  }, [onMouseMove, onMouseUp, onTouchMove]);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: splitPaneClasses, ref: splitPane, style: splitPaneStyle, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      Pane,
      {
        className: pane1Classes,
        eleRef: pane1,
        size: pane1Size,
        split,
        style: pane1DivStyle,
        children: notNullChildren[0]
      },
      "pane1"
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      Resizer,
      {
        className: resizerClasses,
        onClick: onResizerClick,
        onDoubleClick: onResizerDoubleClick,
        onMouseDown,
        onTouchStart,
        onTouchEnd: processResizeFinished,
        split,
        style: resizerStyle
      },
      "resizer"
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      Pane,
      {
        className: pane2Classes,
        eleRef: pane2,
        size: pane2Size,
        split,
        style: pane2DivStyle,
        children: notNullChildren[1]
      },
      "pane2"
    )
  ] });
}
try {
  SplitPane.displayName = "SplitPane";
  SplitPane.__docgenInfo = { "description": "Local TypeScript implementation of `SplitPane` from `react-split-pane` package since that\npackage is not regularly maintained.\nSee https://github.com/tomkp/react-split-pane/blob/master/LICENSE.", "displayName": "SplitPane", "props": { "allowResize": { "defaultValue": null, "description": "Pass false to disable resizing", "name": "allowResize", "required": false, "type": { "name": "boolean" } }, "children": { "defaultValue": null, "description": "The array of two react nodes, one for each pane.", "name": "children", "required": true, "type": { "name": "ReactNode[]" } }, "primary": { "defaultValue": null, "description": "Determines which pane maintains its size when browser window is resized.", "name": "primary", "required": false, "type": { "name": "enum", "value": [{ "value": '"first"' }, { "value": '"second"' }] } }, "minSize": { "defaultValue": null, "description": "", "name": "minSize", "required": false, "type": { "name": "string | number" } }, "maxSize": { "defaultValue": null, "description": "You can limit the maximal size of the 'fixed' pane using the maxSize parameter with a positive value\n(measured in pixels but state just a number). If you wrap the SplitPane into a container component\n(yes you can, just remember the container has to have the relative or absolute positioning), then you'll need to limit\nthe movement of the splitter (resizer) at the end of the SplitPane (otherwise it can be dragged outside the SplitPane\nand you don't catch it never more). For this purpose use the maxSize parameter with value 0. When dragged the splitter/resizer\nwill stop at the border of the SplitPane component and think this you'll be able to pick it again and drag it back then.\nAnd more: if you set the maxSize to negative value (e.g. -200), then the splitter stops 200px before the border\n(in other words it sets the minimal size of the 'resizable' pane in this case). This can be useful also in the\nfull-screen case of use.", "name": "maxSize", "required": false, "type": { "name": "string | number" } }, "defaultSize": { "defaultValue": null, "description": "Default initial size of primary pane", "name": "defaultSize", "required": false, "type": { "name": "string | number" } }, "size": { "defaultValue": null, "description": "Size of primary pane", "name": "size", "required": false, "type": { "name": "string | number" } }, "step": { "defaultValue": null, "description": "You can use the step prop to only allow resizing in fixed increments.", "name": "step", "required": false, "type": { "name": "number" } }, "split": { "defaultValue": null, "description": "", "name": "split", "required": false, "type": { "name": "enum", "value": [{ "value": '"horizontal"' }, { "value": '"vertical"' }] } }, "onDragStarted": { "defaultValue": null, "description": "This callback is invoked when a drag start.", "name": "onDragStarted", "required": false, "type": { "name": "(() => void)" } }, "onDragFinished": { "defaultValue": null, "description": "This callback is invoked when a drag ends.", "name": "onDragFinished", "required": false, "type": { "name": "((newSize: number) => void)" } }, "onChange": { "defaultValue": null, "description": "Callback is invoked with the current drag during a drag event.", "name": "onChange", "required": false, "type": { "name": "((newSize: number) => void)" } }, "onResizerClick": { "defaultValue": null, "description": "Callback is invoked if user clicks on Resizer.", "name": "onResizerClick", "required": false, "type": { "name": "((event: MouseEvent) => void)" } }, "onResizerDoubleClick": { "defaultValue": null, "description": "Callback is invoked if user double clicks on Resizer.", "name": "onResizerDoubleClick", "required": false, "type": { "name": "((event: MouseEvent) => void)" } }, "style": { "defaultValue": null, "description": "Styling to be applied to the main container", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "paneStyle": { "defaultValue": null, "description": "Styling to be applied to both panes", "name": "paneStyle", "required": false, "type": { "name": "CSSProperties" } }, "pane1Style": { "defaultValue": null, "description": "Styling to be applied to the first pane, with precedence over paneStyle", "name": "pane1Style", "required": false, "type": { "name": "CSSProperties" } }, "pane2Style": { "defaultValue": null, "description": "Styling to be applied to the second pane, with precedence over paneStyle", "name": "pane2Style", "required": false, "type": { "name": "CSSProperties" } }, "resizerStyle": { "defaultValue": null, "description": "Styling to be applied to the resizer bar", "name": "resizerStyle", "required": false, "type": { "name": "CSSProperties" } }, "className": { "defaultValue": null, "description": "Class name to be added to the SplitPane div", "name": "className", "required": false, "type": { "name": "string" } }, "paneClassName": { "defaultValue": null, "description": "Class name to be added to each Pane's div", "name": "paneClassName", "required": false, "type": { "name": "string" } }, "pane1ClassName": { "defaultValue": null, "description": "Class name to be added to Pane1's div", "name": "pane1ClassName", "required": false, "type": { "name": "string" } }, "pane2ClassName": { "defaultValue": null, "description": "Class name to be added to Pane2's div", "name": "pane2ClassName", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function ContentWrapper(props) {
  const { content } = props;
  const [isActive, setIsActive] = reactExports.useState(
    content === UiFramework.content.getActive()
  );
  const activeFrontstageDef = useActiveFrontstageDef();
  const contentControlKey = (contentControl) => {
    let controlId;
    if (contentControl && contentControl.key) {
      const key = contentControl.key;
      controlId = key.split("::", 1)[0];
    }
    return controlId;
  };
  const [hasMultipleContents, setHasMultipleContents] = reactExports.useState(
    () => {
      var _a2, _b2;
      return activeFrontstageDef && !!((_a2 = activeFrontstageDef.floatingContentControls) == null ? void 0 : _a2.length) || (((_b2 = activeFrontstageDef == null ? void 0 : activeFrontstageDef.contentGroup) == null ? void 0 : _b2.getContentControls().length) ?? 0) > 1;
    }
  );
  reactExports.useEffect(() => {
    setIsActive(content === UiFramework.content.getActive());
  }, [content]);
  reactExports.useEffect(() => {
    const handleActiveContentChanged = (args) => {
      var _a2, _b2;
      const contentIsIdentical = content === args.activeContent;
      if (contentIsIdentical)
        setIsActive(contentIsIdentical);
      else {
        const contentId = contentControlKey(content);
        const activeContentId = contentControlKey(args.activeContent);
        setIsActive(contentId === activeContentId);
      }
      setHasMultipleContents(
        activeFrontstageDef && !!((_a2 = activeFrontstageDef.floatingContentControls) == null ? void 0 : _a2.length) || (((_b2 = activeFrontstageDef == null ? void 0 : activeFrontstageDef.contentGroup) == null ? void 0 : _b2.getContentControls().length) ?? 0) > 1
      );
    };
    return UiFramework.content.onActiveContentChangedEvent.addListener(
      handleActiveContentChanged
    );
  }, [activeFrontstageDef, content]);
  const handleMouseDown = reactExports.useCallback(() => {
    UiFramework.content.setActive(content);
    setIsActive(true);
  }, [content]);
  reactExports.useEffect(() => {
    const onAvailableContentChanged = () => {
      var _a2, _b2;
      setHasMultipleContents(
        activeFrontstageDef && !!((_a2 = activeFrontstageDef.floatingContentControls) == null ? void 0 : _a2.length) || (((_b2 = activeFrontstageDef == null ? void 0 : activeFrontstageDef.contentGroup) == null ? void 0 : _b2.getContentControls().length) ?? 0) > 1
      );
    };
    return UiFramework.content.onAvailableContentChangedEvent.addListener(
      onAvailableContentChanged
    );
  }, [activeFrontstageDef]);
  const overlayClassName = classnames(
    "uifw-contentlayout-overlay-div",
    isActive && hasMultipleContents ? "uifw-contentlayout-overlay-active" : "uifw-contentlayout-overlay-inactive"
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: classnames("uifw-contentlayout-wrapper", props.className),
      style: props.style,
      onMouseDown: handleMouseDown,
      onMouseMove: UiFramework.visibility.handleContentMouseMove,
      role: "presentation",
      children: [
        content,
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: overlayClassName })
      ]
    }
  );
}
class SplitContainer extends reactExports.Component {
  constructor(props) {
    super(props);
    this._containerDiv = null;
    this._onSplitterChange = (size) => {
      let percentage = 0;
      if (this._containerDiv && size > 0) {
        if (this.props.orientation === Orientation.Horizontal) {
          const height = this._containerDiv.getBoundingClientRect().height;
          if (height > 0)
            percentage = size / height;
        } else {
          const width = this._containerDiv.getBoundingClientRect().width;
          if (width > 0)
            percentage = size / width;
        }
        if (this.props.onSplitterChange)
          this.props.onSplitterChange(size, percentage);
      }
    };
  }
  render() {
    const orientation = this.props.orientation === Orientation.Horizontal ? "horizontal" : "vertical";
    const defaultSize = `${(this.props.percentage * 100).toString()}%`;
    const minSizeTopLeft = this.props.minSizeTopLeft;
    const minSizeBottomRight = -this.props.minSizeBottomRight;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        ref: (e) => {
          this._containerDiv = e;
        },
        className: classnames(
          "uifw-contentlayout-full-size",
          this.props.className
        ),
        style: this.props.style,
        children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
          SplitPane,
          {
            split: orientation,
            minSize: minSizeTopLeft,
            maxSize: minSizeBottomRight,
            defaultSize,
            onChange: this._onSplitterChange,
            allowResize: this.props.resizable,
            children: [
              this.props.contentA,
              this.props.contentB
            ]
          }
        )
      }
    );
  }
}
class SingleContentContainer extends reactExports.Component {
  render() {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: classnames(
          "uifw-contentlayout-full-size",
          this.props.className
        ),
        style: this.props.style,
        "data-testid": "single-content-container",
        onMouseMove: UiFramework.visibility.handleContentMouseMove,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          ContentWrapper,
          {
            content: this.props.content,
            style: { height: "100%", position: "relative" }
          }
        )
      }
    );
  }
}
const MIN_SPLIT_SIZE = 6;
class BaseSplit {
  constructor(props) {
    this.stateId = "";
    this.isLocked = false;
    this.defaultPercentage = props.percentage;
    if (props.id)
      this.stateId = props.id;
    if (props.lock)
      this.isLocked = props.lock;
  }
}
class HorizontalSplit extends BaseSplit {
  constructor(props) {
    super(props);
    this._topIndex = -1;
    this._bottomIndex = -1;
    this._minSizeTop = MIN_SPLIT_SIZE;
    this._minSizeBottom = MIN_SPLIT_SIZE;
    this._handleSplitterChange = (_size, percentage) => {
      this._props.percentage = percentage;
    };
    this._props = props;
    if (typeof props.top === "number") {
      this._topIndex = props.top;
    } else {
      this._topSplit = ContentLayoutDef$1.createSplit(props.top);
    }
    if (typeof props.bottom === "number") {
      this._bottomIndex = props.bottom;
    } else {
      this._bottomSplit = ContentLayoutDef$1.createSplit(props.bottom);
    }
    if (props.minSizeTop)
      this._minSizeTop = Math.max(props.minSizeTop, MIN_SPLIT_SIZE);
    if (props.minSizeBottom)
      this._minSizeBottom = Math.max(props.minSizeBottom, MIN_SPLIT_SIZE);
  }
  createContentContainer(contentNodes, resizable) {
    if (this.isLocked)
      resizable = false;
    const topContent = !this._topSplit ? /* @__PURE__ */ jsxRuntimeExports.jsx(ContentWrapper, { content: contentNodes[this._topIndex] }) : this._topSplit.createContentContainer(contentNodes, resizable);
    const bottomContent = !this._bottomSplit ? /* @__PURE__ */ jsxRuntimeExports.jsx(ContentWrapper, { content: contentNodes[this._bottomIndex] }) : this._bottomSplit.createContentContainer(contentNodes, resizable);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SplitContainer,
      {
        contentA: topContent,
        contentB: bottomContent,
        orientation: Orientation.Horizontal,
        percentage: this.defaultPercentage,
        resizable,
        minSizeTopLeft: this._minSizeTop,
        minSizeBottomRight: this._minSizeBottom,
        splitterStateId: this.stateId,
        onSplitterChange: this._handleSplitterChange
      }
    );
  }
}
class VerticalSplit extends BaseSplit {
  constructor(props) {
    super(props);
    this._leftIndex = -1;
    this._rightIndex = -1;
    this._minSizeLeft = MIN_SPLIT_SIZE;
    this._minSizeRight = MIN_SPLIT_SIZE;
    this._handleSplitterChange = (_size, percentage) => {
      this._props.percentage = percentage;
    };
    this._props = props;
    if (typeof props.left === "number") {
      this._leftIndex = props.left;
    } else {
      this._leftSplit = ContentLayoutDef$1.createSplit(props.left);
    }
    if (typeof props.right === "number") {
      this._rightIndex = props.right;
    } else {
      this._rightSplit = ContentLayoutDef$1.createSplit(props.right);
    }
    if (props.minSizeLeft)
      this._minSizeLeft = Math.max(props.minSizeLeft, MIN_SPLIT_SIZE);
    if (props.minSizeRight)
      this._minSizeRight = Math.max(props.minSizeRight, MIN_SPLIT_SIZE);
  }
  createContentContainer(contentNodes, resizable) {
    if (this.isLocked)
      resizable = false;
    const leftContent = !this._leftSplit ? /* @__PURE__ */ jsxRuntimeExports.jsx(ContentWrapper, { content: contentNodes[this._leftIndex] }) : this._leftSplit.createContentContainer(contentNodes, resizable);
    const rightContent = !this._rightSplit ? /* @__PURE__ */ jsxRuntimeExports.jsx(ContentWrapper, { content: contentNodes[this._rightIndex] }) : this._rightSplit.createContentContainer(contentNodes, resizable);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SplitContainer,
      {
        contentA: leftContent,
        contentB: rightContent,
        orientation: Orientation.Vertical,
        percentage: this.defaultPercentage,
        resizable,
        minSizeTopLeft: this._minSizeLeft,
        minSizeBottomRight: this._minSizeRight,
        splitterStateId: this.stateId,
        onSplitterChange: this._handleSplitterChange
      }
    );
  }
}
let ContentLayoutDef$1 = class ContentLayoutDef {
  constructor(layoutProps) {
    this.id = "";
    this.description = "";
    this._layoutProps = layoutProps;
    this.id = layoutProps.id;
    if (layoutProps.description !== void 0)
      this.description = layoutProps.description;
  }
  get rootSplit() {
    return this._rootSplit;
  }
  /** Creates [[ContentLayoutProps]] for JSON purposes
   * @public
   */
  toJSON() {
    return this._layoutProps;
  }
  /** Fill a layout container with React nodes for each content view
   */
  fillLayoutContainer(contentNodes, resizable) {
    this._rootSplit = ContentLayoutDef.createSplit(this._layoutProps);
    if (this.rootSplit) {
      return this.rootSplit.createContentContainer(contentNodes, resizable);
    }
    if (contentNodes.length > 0)
      return /* @__PURE__ */ jsxRuntimeExports.jsx(SingleContentContainer, { content: contentNodes[0] });
    return void 0;
  }
  /** Gets the indexes of content views used in this Content Layout
   */
  getUsedContentIndexes() {
    let allContentIndexes = [];
    if (!this._layoutProps.horizontalSplit && !this._layoutProps.verticalSplit)
      allContentIndexes.push(0);
    else {
      allContentIndexes = allContentIndexes.concat(
        this.getHorizontalSplitContentIndexes(this._layoutProps.horizontalSplit)
      );
      allContentIndexes = allContentIndexes.concat(
        this.getVerticalSplitContentIndexes(this._layoutProps.verticalSplit)
      );
    }
    const uniqueContentIndexes = [...new Set(allContentIndexes)];
    return uniqueContentIndexes;
  }
  getHorizontalSplitContentIndexes(splitProps) {
    let contentIndexes = [];
    if (!splitProps)
      return contentIndexes;
    if (typeof splitProps.top === "number")
      contentIndexes.push(splitProps.top);
    else {
      contentIndexes = contentIndexes.concat(
        this.getHorizontalSplitContentIndexes(splitProps.top.horizontalSplit)
      );
      contentIndexes = contentIndexes.concat(
        this.getVerticalSplitContentIndexes(splitProps.top.verticalSplit)
      );
    }
    if (typeof splitProps.bottom === "number")
      contentIndexes.push(splitProps.bottom);
    else {
      contentIndexes = contentIndexes.concat(
        this.getHorizontalSplitContentIndexes(splitProps.bottom.horizontalSplit)
      );
      contentIndexes = contentIndexes.concat(
        this.getVerticalSplitContentIndexes(splitProps.bottom.verticalSplit)
      );
    }
    return contentIndexes;
  }
  getVerticalSplitContentIndexes(splitProps) {
    let contentIndexes = [];
    if (!splitProps)
      return contentIndexes;
    if (typeof splitProps.left === "number")
      contentIndexes.push(splitProps.left);
    else {
      contentIndexes = contentIndexes.concat(
        this.getHorizontalSplitContentIndexes(splitProps.left.horizontalSplit)
      );
      contentIndexes = contentIndexes.concat(
        this.getVerticalSplitContentIndexes(splitProps.left.verticalSplit)
      );
    }
    if (typeof splitProps.right === "number")
      contentIndexes.push(splitProps.right);
    else {
      contentIndexes = contentIndexes.concat(
        this.getHorizontalSplitContentIndexes(splitProps.right.horizontalSplit)
      );
      contentIndexes = contentIndexes.concat(
        this.getVerticalSplitContentIndexes(splitProps.right.verticalSplit)
      );
    }
    return contentIndexes;
  }
  /** @internal */
  static createSplit(fragmentDef) {
    if (fragmentDef.horizontalSplit) {
      return new HorizontalSplit(fragmentDef.horizontalSplit);
    } else if (fragmentDef.verticalSplit) {
      return new VerticalSplit(fragmentDef.verticalSplit);
    }
    return void 0;
  }
};
class ContentLayoutActivatedEvent extends UiEvent {
}
class ContentLayout extends reactExports.Component {
  constructor(props) {
    super(props);
    this._handleContentLayoutActivated = (args) => {
      const contentLayoutDef2 = args.contentLayout;
      const contentGroup2 = args.contentGroup;
      const contentNodes2 = contentGroup2.getContentNodes();
      const contentContainer2 = contentLayoutDef2.fillLayoutContainer(
        contentNodes2,
        true
      );
      this.setState({
        contentLayoutDef: args.contentLayout,
        contentContainer: contentContainer2,
        contentGroupId: contentGroup2.groupId
      });
    };
    this._onMouseDown = (_event) => {
      UiFramework.content.setMouseDown(true);
    };
    this._onMouseUp = (_event) => {
      UiFramework.content.setMouseDown(false);
    };
    const contentLayoutDef = this.props.contentLayout;
    const contentGroup = this.props.contentGroup;
    const contentNodes = contentGroup.getContentNodes();
    const contentContainer = contentLayoutDef.fillLayoutContainer(
      contentNodes,
      true
    );
    this.state = {
      contentLayoutDef: this.props.contentLayout,
      contentContainer,
      contentGroupId: this.props.contentGroup.groupId
    };
  }
  componentDidMount() {
    UiFramework.frontstages.onContentLayoutActivatedEvent.addListener(
      this._handleContentLayoutActivated
    );
  }
  componentWillUnmount() {
    UiFramework.frontstages.onContentLayoutActivatedEvent.removeListener(
      this._handleContentLayoutActivated
    );
  }
  render() {
    if (this.state.contentContainer) {
      return /* @__PURE__ */ jsxRuntimeExports.jsx(
        "div",
        {
          id: "uifw-contentlayout-div",
          className: this.props.className,
          style: this.props.style,
          onMouseDown: this._onMouseDown,
          onMouseUp: this._onMouseUp,
          role: "presentation",
          children: this.state.contentContainer
        },
        `${this.state.contentGroupId}-${this.state.contentLayoutDef.id}`
      );
    }
    return null;
  }
}
try {
  ContentWrapper.displayName = "ContentWrapper";
  ContentWrapper.__docgenInfo = { "description": "ContentWrapper React component.", "displayName": "ContentWrapper", "props": { "content": { "defaultValue": null, "description": "", "name": "content", "required": true, "type": { "name": "ReactNode" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ContentLayoutDef$1.displayName = "ContentLayoutDef";
  ContentLayoutDef$1.__docgenInfo = { "description": "Content Layout Definition class.", "displayName": "ContentLayoutDef", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ContentLayoutActivatedEvent.displayName = "ContentLayoutActivatedEvent";
  ContentLayoutActivatedEvent.__docgenInfo = { "description": "Content Layout Activated Event class.", "displayName": "ContentLayoutActivatedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ContentLayout.displayName = "ContentLayout";
  ContentLayout.__docgenInfo = { "description": "Content Layout React component.", "displayName": "ContentLayout", "props": { "contentLayout": { "defaultValue": null, "description": "", "name": "contentLayout", "required": true, "type": { "name": "ContentLayoutDef" } }, "contentGroup": { "defaultValue": null, "description": "", "name": "contentGroup", "required": true, "type": { "name": "ContentGroup" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
var WidgetState = /* @__PURE__ */ ((WidgetState2) => {
  WidgetState2[WidgetState2["Open"] = 0] = "Open";
  WidgetState2[WidgetState2["Closed"] = 1] = "Closed";
  WidgetState2[WidgetState2["Hidden"] = 2] = "Hidden";
  WidgetState2[WidgetState2["Floating"] = 3] = "Floating";
  WidgetState2[WidgetState2["Unloaded"] = 4] = "Unloaded";
  return WidgetState2;
})(WidgetState || {});
var StagePanelLocation = /* @__PURE__ */ ((StagePanelLocation2) => {
  StagePanelLocation2[StagePanelLocation2["Top"] = 101] = "Top";
  StagePanelLocation2[StagePanelLocation2["Left"] = 102] = "Left";
  StagePanelLocation2[StagePanelLocation2["Right"] = 103] = "Right";
  StagePanelLocation2[StagePanelLocation2["Bottom"] = 104] = "Bottom";
  return StagePanelLocation2;
})(StagePanelLocation || {});
var SafeAreaInsets = /* @__PURE__ */ ((SafeAreaInsets2) => {
  SafeAreaInsets2[SafeAreaInsets2["Bottom"] = 1] = "Bottom";
  SafeAreaInsets2[SafeAreaInsets2["Left"] = 2] = "Left";
  SafeAreaInsets2[SafeAreaInsets2["Right"] = 4] = "Right";
  SafeAreaInsets2[SafeAreaInsets2["Top"] = 8] = "Top";
  SafeAreaInsets2[SafeAreaInsets2["All"] = 15] = "All";
  return SafeAreaInsets2;
})(SafeAreaInsets || {});
const _SafeAreaInsetsHelpers = class _SafeAreaInsetsHelpers {
  static isBottom(flags) {
    return SafeAreaInsets.Bottom.valueOf() === (flags & SafeAreaInsets.Bottom);
  }
  static isLeft(flags) {
    return SafeAreaInsets.Left.valueOf() === (flags & SafeAreaInsets.Left);
  }
  static isRight(flags) {
    return SafeAreaInsets.Right.valueOf() === (flags & SafeAreaInsets.Right);
  }
  static isTop(flags) {
    return SafeAreaInsets.Top.valueOf() === (flags & SafeAreaInsets.Top);
  }
};
_SafeAreaInsetsHelpers.getCssClassNames = (flags) => {
  return {
    "nz-safe-area-bottom": _SafeAreaInsetsHelpers.isBottom(flags),
    "nz-safe-area-left": _SafeAreaInsetsHelpers.isLeft(flags),
    "nz-safe-area-right": _SafeAreaInsetsHelpers.isRight(flags),
    "nz-safe-area-top": _SafeAreaInsetsHelpers.isTop(flags)
  };
};
let SafeAreaInsetsHelpers = _SafeAreaInsetsHelpers;
class Footer extends reactExports.PureComponent {
  render() {
    const className = classnames(
      "nz-footer-footer",
      this.props.safeAreaInsets && SafeAreaInsetsHelpers.getCssClassNames(this.props.safeAreaInsets),
      this.props.className
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className,
        style: this.props.style,
        onMouseEnter: this.props.onMouseEnter,
        onMouseLeave: this.props.onMouseLeave,
        children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-messages", children: this.props.messages }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-indicators", children: this.props.children })
        ] })
      }
    );
  }
}
try {
  Footer.displayName = "Footer";
  Footer.__docgenInfo = { "description": "Footer component.", "displayName": "Footer", "props": { "children": { "defaultValue": null, "description": "Footer indicators, separators and status fields. I.e: [StatusBarSeparator]($appui-react), [[FooterIndicator]],\n[[MessageCenter]], [[ToolAssistance]], [[SnapMode]]", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "messages": { "defaultValue": null, "description": "Footer messages. I.e. [[Message]], [[Toast]]", "name": "messages", "required": false, "type": { "name": "ReactNode" } }, "onMouseEnter": { "defaultValue": null, "description": "Handler for mouse enter", "name": "onMouseEnter", "required": false, "type": { "name": "((event: MouseEvent<HTMLElement, MouseEvent>) => void)" } }, "onMouseLeave": { "defaultValue": null, "description": "Handler for mouse leave", "name": "onMouseLeave", "required": false, "type": { "name": "((event: MouseEvent<HTMLDivElement, MouseEvent>) => void)" } }, "safeAreaInsets": { "defaultValue": null, "description": "Describes respected safe area insets.", "name": "safeAreaInsets", "required": false, "type": { "name": "enum", "value": [{ "value": "1" }, { "value": "2" }, { "value": "4" }, { "value": "8" }, { "value": "15" }] } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const SafeAreaContext = reactExports.createContext(
  void 0
);
try {
  SafeAreaContext.displayName = "SafeAreaContext";
  SafeAreaContext.__docgenInfo = { "description": "Context used to manage safe area (feature used by devices with non-rectangular screens).\n\nIf used, this is expected to wrap [[ConfigurableUiContent]].\n\n```tsx\n<SafeAreaContext.Provider value={SafeAreaInsets.All}>\n  <ConfigurableUiContent />\n</SafeAreaContext.Provider>\n```", "displayName": "SafeAreaContext", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const FooterIndicator = reactExports.forwardRef(function FooterIndicator2(props, ref) {
  const { children, ...attributes } = props;
  const className = classnames("nz-footer-indicator", props.className);
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref, ...{ ...attributes, className }, children });
});
try {
  FooterIndicator.displayName = "FooterIndicator";
  FooterIndicator.__docgenInfo = { "description": "Indicator used in [[Footer]] component.", "displayName": "FooterIndicator", "props": { "children": { "defaultValue": null, "description": "Indicator content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "title": { "defaultValue": null, "description": "Title for the indicator", "name": "title", "required": false, "type": { "name": "string" } }, "onClick": { "defaultValue": null, "description": "Function called when the indicator is clicked", "name": "onClick", "required": false, "type": { "name": "((event: MouseEvent<Element, MouseEvent>) => void)" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const StatusBarField = reactExports.forwardRef(function StatusBarField2(props, ref) {
  const hasClickAction = !!props.onClick;
  const classNames = classnames(
    "uifw-statusbar-field",
    hasClickAction && "uifw-action",
    props.className
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    FooterIndicator,
    {
      ref,
      className: classNames,
      title: props.title,
      style: props.style,
      onClick: props.onClick,
      ...{
        role: "button",
        tabIndex: -1
      },
      children: props.children
    }
  );
});
try {
  StatusBarField.displayName = "StatusBarField";
  StatusBarField.__docgenInfo = { "description": "Field component used in [[StatusBar]] component.", "displayName": "StatusBarField", "props": { "children": { "defaultValue": null, "description": "Field content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "onClick": { "defaultValue": null, "description": "Function called when field is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "title": { "defaultValue": null, "description": "Title of a field.", "name": "title", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function StatusBarPopup(props) {
  const { className, offset, ...other } = props;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Popup,
    {
      className: classnames("nz-status-bar-popup", className),
      position: RelativePosition.Top,
      showShadow: true,
      ...other,
      showArrow: false
    }
  );
}
try {
  StatusBarPopup.displayName = "StatusBarPopup";
  StatusBarPopup.__docgenInfo = { "description": "Popup component used in [[StatusBar]] component.", "displayName": "StatusBarPopup", "props": { "showShadow": { "defaultValue": null, "description": "Show or hide the box shadow (defaults to true)", "name": "showShadow", "required": false, "type": { "name": "boolean" } }, "showArrow": { "defaultValue": null, "description": "Show or hide the arrow (defaults to false)", "name": "showArrow", "required": false, "type": { "name": "boolean" } }, "isOpen": { "defaultValue": null, "description": "Indicates whether the popup is shown or not (defaults to false)", "name": "isOpen", "required": false, "type": { "name": "boolean" } }, "position": { "defaultValue": null, "description": "Direction (relative to the target) to which the popup is expanded (defaults to Bottom)", "name": "position", "required": false, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }, { "value": "2" }, { "value": "3" }, { "value": "4" }, { "value": "5" }, { "value": "6" }, { "value": "7" }, { "value": "8" }, { "value": "9" }] } }, "top": { "defaultValue": null, "description": "Top position (absolute positioning - defaults to 0)", "name": "top", "required": false, "type": { "name": "number" } }, "left": { "defaultValue": null, "description": "Left position (absolute positioning - defaults to 0)", "name": "left", "required": false, "type": { "name": "number" } }, "onOpen": { "defaultValue": null, "description": "Function called when the popup is opened", "name": "onOpen", "required": false, "type": { "name": "(() => void)" } }, "onOutsideClick": { "defaultValue": null, "description": "Function called when user clicks outside the popup", "name": "onOutsideClick", "required": false, "type": { "name": "((e: MouseEvent) => void)" } }, "onClose": { "defaultValue": null, "description": "Function called when the popup is closed", "name": "onClose", "required": false, "type": { "name": "(() => void)" } }, "onEnter": { "defaultValue": null, "description": "Function called when the popup is closed on Enter", "name": "onEnter", "required": false, "type": { "name": "(() => void)" } }, "onWheel": { "defaultValue": null, "description": "Function called when the wheel is used", "name": "onWheel", "required": false, "type": { "name": "((e: WheelEvent) => void)" } }, "onContextMenu": { "defaultValue": null, "description": "Function called when the right mouse button is pressed", "name": "onContextMenu", "required": false, "type": { "name": "((e: MouseEvent) => void)" } }, "offset": { "defaultValue": null, "description": "Offset from the parent (defaults to 4)", "name": "offset", "required": false, "type": { "name": "number" } }, "target": { "defaultValue": null, "description": "Target element to position popup", "name": "target", "required": false, "type": { "name": "HTMLElement | null" } }, "role": { "defaultValue": null, "description": 'Role - if not specified "dialog" is used', "name": "role", "required": false, "type": { "name": "enum", "value": [{ "value": '"alert"' }, { "value": '"alertdialog"' }, { "value": '"dialog"' }] } }, "ariaLabel": { "defaultValue": null, "description": "accessibility label", "name": "ariaLabel", "required": false, "type": { "name": "string" } }, "moveFocus": { "defaultValue": null, "description": "set focus to popup - default to not set focus", "name": "moveFocus", "required": false, "type": { "name": "boolean" } }, "focusTarget": { "defaultValue": null, "description": "Element to receive focus, specified by React.RefObject or CSS selector string. If undefined and moveFocus is true then focus is moved to first focusable element.", "name": "focusTarget", "required": false, "type": { "name": "string | RefObject<HTMLElement>" } }, "isPinned": { "defaultValue": null, "description": "Indicates whether the popup is pinned.", "name": "isPinned", "required": false, "type": { "name": "boolean" } }, "animate": { "defaultValue": null, "description": "Indicates whether to use animation for open/close (defaults to true)", "name": "animate", "required": false, "type": { "name": "boolean" } }, "closeOnEnter": { "defaultValue": null, "description": "Indicates whether to close the popup when Enter is pressed (defaults to true)", "name": "closeOnEnter", "required": false, "type": { "name": "boolean" } }, "closeOnWheel": { "defaultValue": null, "description": "Indicates whether to close the popup when the wheel is used (defaults to true)", "name": "closeOnWheel", "required": false, "type": { "name": "boolean" } }, "closeOnContextMenu": { "defaultValue": null, "description": "Indicates whether to close the popup when the right mouse button is pressed (defaults to true)", "name": "closeOnContextMenu", "required": false, "type": { "name": "boolean" } }, "closeOnNestedPopupOutsideClick": { "defaultValue": null, "description": "If false outside click processing and closing are skipped if click occurs in another Popup component, default to false.", "name": "closeOnNestedPopupOutsideClick", "required": false, "type": { "name": "boolean" } }, "keepContentsMounted": { "defaultValue": null, "description": "If true the children are mounted once and unmounted when this component is unmounted. If false the\nchildren are unmounted each time the popup is closed.", "name": "keepContentsMounted", "required": false, "type": { "name": "boolean" } }, "repositionOnResize": { "defaultValue": null, "description": "If true the popup will remain open and will be repositioned when window resize events occur, default to false.\n@beta", "name": "repositionOnResize", "required": false, "type": { "name": "boolean" } }, "children": { "defaultValue": null, "description": "Content", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class StatusBar extends reactExports.Component {
  render() {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(SafeAreaContext.Consumer, { children: (safeAreaInsets) => {
      var _a2, _b2;
      return /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Footer,
        {
          className: this.props.className,
          onMouseEnter: UiFramework.visibility.handleWidgetMouseEnter,
          safeAreaInsets,
          style: this.props.style,
          children: [
            ((_b2 = (_a2 = this.props.widgetControl) == null ? void 0 : _a2.getReactNode) == null ? void 0 : _b2.call(_a2)) ?? null,
            this.props.children
          ]
        }
      );
    } });
  }
}
function StatusBarSpaceBetween(props) {
  const { className, ...divProps } = props;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Div,
    {
      ...divProps,
      mainClassName: className ? className : "uifw-statusbar-space-between"
    }
  );
}
function StatusBarLeftSection(props) {
  const { className, ...divProps } = props;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Div,
    {
      ...divProps,
      mainClassName: className ? className : "uifw-statusbar-left"
    }
  );
}
function StatusBarCenterSection(props) {
  const { className, ...divProps } = props;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Div,
    {
      ...divProps,
      mainClassName: className ? className : "uifw-statusbar-center"
    }
  );
}
function StatusBarRightSection(props) {
  const { className, ...divProps } = props;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Div,
    {
      ...divProps,
      mainClassName: className ? className : "uifw-statusbar-right"
    }
  );
}
((StatusBar2) => {
  StatusBar2.Field = StatusBarField;
  StatusBar2.Popup = StatusBarPopup;
})(StatusBar || (StatusBar = {}));
try {
  StatusBarSpaceBetween.displayName = "StatusBarSpaceBetween";
  StatusBarSpaceBetween.__docgenInfo = { "description": "StatusBar With Space Between Items React functional component", "displayName": "StatusBarSpaceBetween", "props": { "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  StatusBarLeftSection.displayName = "StatusBarLeftSection";
  StatusBarLeftSection.__docgenInfo = { "description": "StatusBar Left Section React functional component", "displayName": "StatusBarLeftSection", "props": { "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  StatusBarCenterSection.displayName = "StatusBarCenterSection";
  StatusBarCenterSection.__docgenInfo = { "description": "StatusBar Center Section React functional component", "displayName": "StatusBarCenterSection", "props": { "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  StatusBarRightSection.displayName = "StatusBarRightSection";
  StatusBarRightSection.__docgenInfo = { "description": "StatusBar Right Section React functional component", "displayName": "StatusBarRightSection", "props": { "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  StatusBar.displayName = "StatusBar";
  StatusBar.__docgenInfo = { "description": "Status Bar React component.\nComponents used in a [[StatusBar]].", "displayName": "StatusBar", "props": { "widgetControl": { "defaultValue": null, "description": "", "name": "widgetControl", "required": false, "type": { "name": "StatusBarWidgetControl" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  StatusBar.Field.displayName = "StatusBar.Field";
  StatusBar.Field.__docgenInfo = { "description": "Field of a [[StatusBar]].", "displayName": "StatusBar.Field", "props": { "children": { "defaultValue": null, "description": "Field content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "onClick": { "defaultValue": null, "description": "Function called when field is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "title": { "defaultValue": null, "description": "Title of a field.", "name": "title", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  StatusBar.Popup.displayName = "StatusBar.Popup";
  StatusBar.Popup.__docgenInfo = { "description": "Popup of a [[StatusBar]].", "displayName": "StatusBar.Popup", "props": { "showShadow": { "defaultValue": null, "description": "Show or hide the box shadow (defaults to true)", "name": "showShadow", "required": false, "type": { "name": "boolean" } }, "showArrow": { "defaultValue": null, "description": "Show or hide the arrow (defaults to false)", "name": "showArrow", "required": false, "type": { "name": "boolean" } }, "isOpen": { "defaultValue": null, "description": "Indicates whether the popup is shown or not (defaults to false)", "name": "isOpen", "required": false, "type": { "name": "boolean" } }, "position": { "defaultValue": null, "description": "Direction (relative to the target) to which the popup is expanded (defaults to Bottom)", "name": "position", "required": false, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }, { "value": "2" }, { "value": "3" }, { "value": "4" }, { "value": "5" }, { "value": "6" }, { "value": "7" }, { "value": "8" }, { "value": "9" }] } }, "top": { "defaultValue": null, "description": "Top position (absolute positioning - defaults to 0)", "name": "top", "required": false, "type": { "name": "number" } }, "left": { "defaultValue": null, "description": "Left position (absolute positioning - defaults to 0)", "name": "left", "required": false, "type": { "name": "number" } }, "onOpen": { "defaultValue": null, "description": "Function called when the popup is opened", "name": "onOpen", "required": false, "type": { "name": "(() => void)" } }, "onOutsideClick": { "defaultValue": null, "description": "Function called when user clicks outside the popup", "name": "onOutsideClick", "required": false, "type": { "name": "((e: MouseEvent) => void)" } }, "onClose": { "defaultValue": null, "description": "Function called when the popup is closed", "name": "onClose", "required": false, "type": { "name": "(() => void)" } }, "onEnter": { "defaultValue": null, "description": "Function called when the popup is closed on Enter", "name": "onEnter", "required": false, "type": { "name": "(() => void)" } }, "onWheel": { "defaultValue": null, "description": "Function called when the wheel is used", "name": "onWheel", "required": false, "type": { "name": "((e: WheelEvent) => void)" } }, "onContextMenu": { "defaultValue": null, "description": "Function called when the right mouse button is pressed", "name": "onContextMenu", "required": false, "type": { "name": "((e: MouseEvent) => void)" } }, "offset": { "defaultValue": null, "description": "Offset from the parent (defaults to 4)", "name": "offset", "required": false, "type": { "name": "number" } }, "target": { "defaultValue": null, "description": "Target element to position popup", "name": "target", "required": false, "type": { "name": "HTMLElement | null" } }, "role": { "defaultValue": null, "description": 'Role - if not specified "dialog" is used', "name": "role", "required": false, "type": { "name": "enum", "value": [{ "value": '"alert"' }, { "value": '"alertdialog"' }, { "value": '"dialog"' }] } }, "ariaLabel": { "defaultValue": null, "description": "accessibility label", "name": "ariaLabel", "required": false, "type": { "name": "string" } }, "moveFocus": { "defaultValue": null, "description": "set focus to popup - default to not set focus", "name": "moveFocus", "required": false, "type": { "name": "boolean" } }, "focusTarget": { "defaultValue": null, "description": "Element to receive focus, specified by React.RefObject or CSS selector string. If undefined and moveFocus is true then focus is moved to first focusable element.", "name": "focusTarget", "required": false, "type": { "name": "string | RefObject<HTMLElement>" } }, "isPinned": { "defaultValue": null, "description": "Indicates whether the popup is pinned.", "name": "isPinned", "required": false, "type": { "name": "boolean" } }, "animate": { "defaultValue": null, "description": "Indicates whether to use animation for open/close (defaults to true)", "name": "animate", "required": false, "type": { "name": "boolean" } }, "closeOnEnter": { "defaultValue": null, "description": "Indicates whether to close the popup when Enter is pressed (defaults to true)", "name": "closeOnEnter", "required": false, "type": { "name": "boolean" } }, "closeOnWheel": { "defaultValue": null, "description": "Indicates whether to close the popup when the wheel is used (defaults to true)", "name": "closeOnWheel", "required": false, "type": { "name": "boolean" } }, "closeOnContextMenu": { "defaultValue": null, "description": "Indicates whether to close the popup when the right mouse button is pressed (defaults to true)", "name": "closeOnContextMenu", "required": false, "type": { "name": "boolean" } }, "closeOnNestedPopupOutsideClick": { "defaultValue": null, "description": "If false outside click processing and closing are skipped if click occurs in another Popup component, default to false.", "name": "closeOnNestedPopupOutsideClick", "required": false, "type": { "name": "boolean" } }, "keepContentsMounted": { "defaultValue": null, "description": "If true the children are mounted once and unmounted when this component is unmounted. If false the\nchildren are unmounted each time the popup is closed.", "name": "keepContentsMounted", "required": false, "type": { "name": "boolean" } }, "repositionOnResize": { "defaultValue": null, "description": "If true the popup will remain open and will be repositioned when window resize events occur, default to false.\n@beta", "name": "repositionOnResize", "required": false, "type": { "name": "boolean" } }, "children": { "defaultValue": null, "description": "Content", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function StatusBarIndicator(props) {
  const hasClickAction = !!props.onClick || !!props.popup;
  const [isOpen, setIsOpen] = reactExports.useState(!!props.defaultIsOpen);
  const handleOnIndicatorClick = () => {
    var _a2;
    setIsOpen(!isOpen);
    (_a2 = props.onClick) == null ? void 0 : _a2.call(props);
  };
  const target = reactExports.useRef(null);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      StatusBar.Field,
      {
        ref: target,
        className: props.className,
        title: props.title,
        style: props.style,
        onClick: hasClickAction ? handleOnIndicatorClick : void 0,
        children: props.children
      }
    ),
    props.popup && /* @__PURE__ */ jsxRuntimeExports.jsx(
      StatusBar.Popup,
      {
        target: target.current,
        onClose: () => setIsOpen(false),
        isOpen,
        children: props.popup
      }
    )
  ] });
}
try {
  StatusBarIndicator.displayName = "StatusBarIndicator";
  StatusBarIndicator.__docgenInfo = { "description": "General-purpose [[StatusBar]] indicator.", "displayName": "StatusBarIndicator", "props": { "children": { "defaultValue": null, "description": "Indicator content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "popup": { "defaultValue": null, "description": "Content to display in a popup when indicator is clicked.", "name": "popup", "required": false, "type": { "name": "ReactNode" } }, "onClick": { "defaultValue": null, "description": "Function called when indicator is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "defaultIsOpen": { "defaultValue": null, "description": "Default state of an indicator popup.", "name": "defaultIsOpen", "required": false, "type": { "name": "boolean" } }, "title": { "defaultValue": null, "description": "Title of an indicator.", "name": "title", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
var StatusBarSection = /* @__PURE__ */ ((StatusBarSection2) => {
  StatusBarSection2[StatusBarSection2["Message"] = 0] = "Message";
  StatusBarSection2[StatusBarSection2["Left"] = 0] = "Left";
  StatusBarSection2[StatusBarSection2["Stage"] = 1] = "Stage";
  StatusBarSection2[StatusBarSection2["Center"] = 1] = "Center";
  StatusBarSection2[StatusBarSection2["Selection"] = 2] = "Selection";
  StatusBarSection2[StatusBarSection2["Right"] = 2] = "Right";
  StatusBarSection2[StatusBarSection2["Context"] = 3] = "Context";
  return StatusBarSection2;
})(StatusBarSection || {});
var StatusBarLabelSide = /* @__PURE__ */ ((StatusBarLabelSide2) => {
  StatusBarLabelSide2[StatusBarLabelSide2["Left"] = 0] = "Left";
  StatusBarLabelSide2[StatusBarLabelSide2["Right"] = 1] = "Right";
  return StatusBarLabelSide2;
})(StatusBarLabelSide || {});
function isStatusBarActionItem(item) {
  return "execute" in item;
}
function isStatusBarLabelItem(item) {
  return !isStatusBarActionItem(item) && "label" in item;
}
function isStatusBarCustomItem(item) {
  return "content" in item;
}
function StatusBarLabelIndicator(props) {
  const { className, iconSpec, label, labelSide, ...other } = props;
  const classNames = classnames(
    "uifw-statusbar-labelIndicator",
    labelSide === StatusBarLabelSide.Right && "uifw-reversed",
    className
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(StatusBarIndicator, { className: classNames, ...other, children: [
    label && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "uifw-label", children: label }),
    iconSpec && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-icon", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec }) })
  ] });
}
try {
  StatusBarLabelIndicator.displayName = "StatusBarLabelIndicator";
  StatusBarLabelIndicator.__docgenInfo = { "description": "[[StatusBar]] indicator that shows a label with an icon.", "displayName": "StatusBarLabelIndicator", "props": { "iconSpec": { "defaultValue": null, "description": "Specification of an icon.", "name": "iconSpec", "required": false, "type": { "name": "IconSpec" } }, "label": { "defaultValue": null, "description": "Indicator label.", "name": "label", "required": false, "type": { "name": "string" } }, "labelSide": { "defaultValue": null, "description": "Side to display label.", "name": "labelSide", "required": false, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }] } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "title": { "defaultValue": null, "description": "Title of an indicator.", "name": "title", "required": false, "type": { "name": "string" } }, "onClick": { "defaultValue": null, "description": "Function called when indicator is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "popup": { "defaultValue": null, "description": "Content to display in a popup when indicator is clicked.", "name": "popup", "required": false, "type": { "name": "ReactNode" } }, "defaultIsOpen": { "defaultValue": null, "description": "Default state of an indicator popup.", "name": "defaultIsOpen", "required": false, "type": { "name": "boolean" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function isProviderItem(item) {
  return "providerId" in item;
}
function Ellipsis(props) {
  const className = classnames("nz-base-ellipsis", props.className);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, style: props.style, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-dot" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-dot" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-dot" })
  ] });
}
try {
  Ellipsis.displayName = "Ellipsis";
  Ellipsis.__docgenInfo = { "description": "A component that renders three dots (common for text truncation). Used in overflow buttons.", "displayName": "Ellipsis", "props": { "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const iModelPanelHeader = {
  projects: "Projects"
};
const iModelPanel = {
  noIModels: "There are no IModels in Project {{projectName}}"
};
const keyinbrowser = {
  label: "Keyin Browser",
  keyin: "Key-in",
  args: "Arguments",
  argsTip: "'|' separated arguments",
  execute: "Execute",
  placeholder: "Enter key-in",
  incorrectArgs: "Incorrect number of key-in arguments",
  failedToRun: "Key-in failed to run",
  exceptionOccurred: "Key-in caused the following exception to occur",
  couldNotFindTool: "Could not find a tool with the given key-in",
  couldNotSaveHistory: "Could not save key-in in history"
};
const general = {
  sheet: "Sheet",
  alert: "Alert",
  settings: "Settings",
  minimize: "Minimize",
  overflow: "Overflow",
  "no-content": "No data for this content view",
  of: "of",
  search: "Search"
};
const rotations = {
  top: "Top",
  bottom: "Bottom",
  left: "Left",
  right: "Right",
  front: "Front",
  rear: "Rear",
  isoLeft: "Iso Left",
  isoRight: "Iso Right"
};
const messageCenter = {
  messages: "Messages",
  all: "All",
  errors: "Errors",
  prompt: "No messages.",
  "export": "Export"
};
const activityCenter = {
  percentComplete: "% complete",
  moreDetails: "Click or tap for more details."
};
const snapModeField = {
  snapMode: "Snap Mode",
  keypoint: "Keypoint",
  intersection: "Intersection",
  center: "Center",
  nearest: "Nearest",
  origin: "Origin",
  midpoint: "Midpoint",
  bisector: "Bisector",
  multi: "Multiple"
};
const selectionScopeField = {
  label: "Scope",
  toolTip: "Selection Scope"
};
const tests = {
  singleContent: "Single Content",
  label: "Test Label",
  subtitle: "Test Subtitle"
};
const ContentLayoutDef2 = {
  SingleContent: "Single Content",
  TwoHalvesVertical: "Two Halves Vertical",
  ThreeRightStacked: "Three Right Stacked",
  FourQuadrants: "Four Quadrants"
};
const pickerButtons = {
  showAll: "Show All",
  hideAll: "Hide All",
  invert: "Invert",
  separator: "Separator"
};
const categoriesModels = {
  categories: "Categories",
  models: "Models",
  loadingMessage: "Loading...",
  options: "Options"
};
const realityData = {
  title: "Reality Data",
  group: "Group",
  size: "Size",
  kilobytes: "KB",
  notAvailable: "N/A",
  noneAvailable: "No reality data available.",
  bingMap: "Bing Map",
  attached: "(Attached)",
  settings: "Settings...",
  options: "Options",
  hybrid: "Hybrid",
  aerial: "Aerial",
  street: "Street",
  showAll: "Show All",
  hideAll: "Hide All",
  invertDisplay: "Invert",
  bingMapSettings: "Bing Map Settings",
  elevationOffset: "Elevation Offset:"
};
const viewTypes = {
  views: "Views",
  spatialViews: "Spatial Views",
  drawings: "Drawings",
  sheets: "Sheets",
  others: "Others"
};
const buttons = {
  yes: "Yes",
  no: "No",
  clearSelection: "Clear Selection",
  openBackstageMenu: "Open Backstage Menu"
};
const navigationAid = {
  sheetsModalFrontstage: "Sheet Views"
};
const commands = {
  backToPreviousFrontstage: "Back to previous Frontstage"
};
const settings = {
  settingsStageLabel: "Settings",
  noSettingsProvidersRegistered: "No SettingsProviders have been registered.",
  noSettingsAvailable: "No Settings Available",
  "quantity-formatting": {
    label: "Units",
    subLabel: "Quantity Formatting",
    tooltip: "Define Active Units and Quantity Formats",
    formatSectionLabel: "Quantity Formats",
    setButtonLabel: "Set",
    clearButtonLabel: "Clear"
  },
  uiSettingsPage: {
    label: "UI Settings",
    tooltip: "UI Settings",
    light: "Light",
    dark: "Dark",
    lightHighContrast: "Light (High contrast)",
    darkHighContrast: "Dark (High contrast)",
    inherit: "Inherit",
    systemPreferred: "System preferred",
    themeTitle: "Theme",
    themeDescription: "Change the theme",
    autoHideTitle: "Auto-Hide UI",
    autoHideDescription: "Toggle the auto-hide of UI after inactivity",
    dragInteractionTitle: "Toolbar Group Buttons show child action item",
    dragInteractionDescription: "Requires long press to open pop-up panel. Single click executes displayed action.",
    useProximityOpacityTitle: "Change Toolbar Opacity",
    useProximityOpacityDescription: "Change the toolbar opacity as the mouse gets closer or farther away",
    snapWidgetOpacityTitle: "Snap Toolbar Opacity",
    snapWidgetOpacityDescription: "Immediately change the toolbar opacity when the mouse gets close",
    accuDrawNotificationsTitle: "Display AccuDraw Notifications",
    accuDrawNotificationsDescription: "Display toast notifications when AccuDraw status changes",
    widgetOpacityTitle: "Widget Opacity",
    widgetOpacityDescription: "Opacity when mouse is not hovering floating widgets",
    widgetIconTitle: "Show Icon on Widget Tab",
    widgetIconDescription: "If widget panel less 320 pixels wide then only icons will display on widget tab.",
    autoCollapseUnpinnedPanelsTitle: "Auto Collapse Unpinned Panels",
    autoCollapseUnpinnedPanelsDescription: "Collapse unpinned widget panels when mouse leaves panel boundary. If false, mouse down outside is required.",
    animateToolSettingsTitle: "Animate Docked Tool Settings on appear",
    animateToolSettingsDescription: "Animate tool settings when they appear in the docked bar.",
    useToolAsToolSettingsLabelTitle: "Use tool as tool settings label",
    useToolAsToolSettingsLabelDescription: "Use active tool name as the Tool Settings tab label, if false, will always be titled 'Tool Settings'.",
    toolbarOpacityTitle: "Toolbar Opacity",
    toolbarOpacityDescription: "Opacity of toolbars when mouse is not hovering."
  }
};
const tools = {
  clearSelection: "Clear Selection",
  hideSelected: "Hide Elements",
  hide: "Hide Tools",
  hideCategories: "Hide Categories",
  hideModels: "Hide Models",
  isolate: "Isolate Tools",
  isolateSelected: "Isolate Elements",
  isolateCategories: "Isolate Categories",
  isolateModels: "Isolate Models",
  emphasizeSelected: "Emphasize Selected",
  clearVisibility: "Clear Isolated, Hidden, and Emphasized Elements",
  noToolSettingsStart: "",
  noToolSettingsEnd: " does not have tool settings.",
  noToolSettingsPlaceholderName: "Active Tool",
  sectionTools: "Section Tools",
  sectionClear: "Clear Sections",
  sectionPanelLabel: "Section",
  sectionByPlane: "Plane",
  sectionByElement: "Element",
  sectionByRange: "Range",
  sectionByShape: "Shape",
  measureTools: "Measure Tools",
  sectionShowHandles: "Section Handles",
  viewZoomingTools: "Zooming Tools",
  View: {
    Fly: {
      description: "Fly in view",
      flyover: "Fly"
    },
    ToggleCamera: {
      description: "Turn on/off use of camera in active view",
      flyover: "Toggle Camera",
      turnOnFlyover: "Turn Camera On",
      turnOffFlyover: "Turn Camera Off"
    }
  },
  OpenSettings: {
    keyin: "open settings"
  },
  RestoreAllFrontstages: {
    keyin: "restore all frontstages",
    description: "Restore all Frontstages to Default Layout",
    flyover: "Restore all Stages to Default Layout"
  },
  RestoreFrontstageLayout: {
    keyin: "restore frontstage layout",
    description: "Restore Frontstage to Default Layout",
    flyover: "Restore Stage to Default Layout",
    noStageFound: "Specified stage not found."
  },
  ClearKeyinPaletteHistory: {
    keyin: "clear keyin history",
    description: "Clear keyin palette history",
    flyover: "Clear keyin palette history"
  },
  BumpToolSetting: {
    keyin: "bump toolsetting",
    description: "Bump a Tool Settings value",
    flyover: "Bump a Tool Setting"
  },
  FocusToolSettings: {
    keyin: "focus toolsettings",
    description: "Focus into the Tool Settings widget",
    flyover: "Focus into Tool Settings"
  }
};
const listTools = {
  searchViews: "Search Views...",
  listView: "List",
  thumbnailView: "Thumbnails",
  camera: "Camera",
  views: "Saved Views",
  viewAttributes: "View Attributes",
  constructions: "Constructions",
  acs: "ACS",
  grid: "Grid",
  visEdges: "Visible Edges",
  hidEdges: "Hidden Edges",
  monochrome: "Monochrome Display",
  ambientOcclusion: "Ambient Occlusion",
  shadows: "Shadows",
  backgroundMap: "Background Map"
};
const toolAssistance = {
  title: "Tool Assistance",
  promptAtCursor: "Show prompt at cursor",
  moreInfo: "Click for more information",
  mouse: "Mouse",
  touch: "Touch",
  pin: "Pin"
};
const widget = {
  labels: {
    toolSettings: "Tool Settings"
  },
  tooltips: {
    dockToolSettings: "Dock to top",
    moreWidgets: "More widgets",
    moreToolSettings: "More tool settings",
    pinPanel: "Pin widget panel",
    sendHome: "Send to panel",
    resizeGrip: "Resize widget panel",
    toolSettingsHandle: "Drag to undock",
    unpinPanel: "Unpin widget panel",
    popoutActiveTab: "Pop out active widget tab"
  },
  errorMessage: {
    unknownError: "Something went wrong...",
    widgetPopoutFail: "Widget failed to popout, ensure you allow popout for this site."
  }
};
const statusBar = {
  overflow: "More status fields"
};
const modalFrontstage = {
  backButtonTitle: "Return to previous stage"
};
const standardRotationNavigationAid = {
  title: "Standard Rotations"
};
const accuDraw = {
  dialogTitle: "AccuDraw",
  subMenu: "AccuDraw",
  lockSmart: "Smart Lock",
  lockX: "Lock X",
  lockY: "Lock Y",
  lockZ: "Lock Z",
  lockAngle: "Lock Angle",
  lockDistance: "Lock Distance",
  changeCompassMode: "Toggle Mode",
  rotateTop: "Rotate Top",
  rotateFront: "Rotate Front",
  rotateSide: "Rotate Side",
  rotateView: "Rotate View",
  setOrigin: "Reposition Origin",
  rotateCycle: "Rotate Cycle",
  rotateAxes: "Rotate Axes",
  rotateToElement: "Rotate To Element",
  defineACSByPoints: "Define ACS By Points",
  bumpToolSetting: "Bump Tool Settings",
  focusToolSetting: "Focus into Tool Settings",
  rotateSubMenu: "AccuDraw Rotate",
  rotationSet: "AccuDraw rotation mode set to {{rotationString}}",
  rotation: {
    top: "top",
    front: "front",
    side: "side",
    view: "view",
    context: "context",
    ACS: "ACS"
  },
  compassModeSet: "AccuDraw compass mode set to {{modeString}}",
  compassMode: {
    polar: "polar",
    rectangular: "rectangular"
  }
};
const presentationUnitSystem = {
  "selector-label": "Presentation Unit System",
  Metric: "Metric",
  BritishImperial: "Imperial",
  USCustomary: "US Customary",
  USSurvey: "US Survey"
};
const dialog = {
  cancel: "Cancel",
  close: "Close"
};
const UiFramework$1 = {
  iModelPanelHeader,
  iModelPanel,
  keyinbrowser,
  general,
  rotations,
  messageCenter,
  activityCenter,
  snapModeField,
  selectionScopeField,
  tests,
  ContentLayoutDef: ContentLayoutDef2,
  pickerButtons,
  categoriesModels,
  realityData,
  viewTypes,
  buttons,
  navigationAid,
  commands,
  settings,
  tools,
  listTools,
  toolAssistance,
  widget,
  statusBar,
  modalFrontstage,
  standardRotationNavigationAid,
  accuDraw,
  presentationUnitSystem,
  dialog
};
const defaults = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  ContentLayoutDef: ContentLayoutDef2,
  accuDraw,
  activityCenter,
  buttons,
  categoriesModels,
  commands,
  default: UiFramework$1,
  dialog,
  general,
  iModelPanel,
  iModelPanelHeader,
  keyinbrowser,
  listTools,
  messageCenter,
  modalFrontstage,
  navigationAid,
  pickerButtons,
  presentationUnitSystem,
  realityData,
  rotations,
  selectionScopeField,
  settings,
  snapModeField,
  standardRotationNavigationAid,
  statusBar,
  tests,
  toolAssistance,
  tools,
  viewTypes,
  widget
}, Symbol.toStringTag, { value: "Module" }));
function useTranslation() {
  const fallback = reactExports.useCallback((key) => {
    if (!UiFramework.initialized) {
      return void 0;
    }
    return UiFramework.translate(key);
  }, []);
  return usePackageTranslation({
    namespace: UiFramework.localizationNamespace,
    fallback,
    defaults
  });
}
try {
  useTranslation.displayName = "useTranslation";
  useTranslation.__docgenInfo = { "description": "Returns a translation function.", "displayName": "useTranslation", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const StatusBarOverflow = reactExports.forwardRef(function StatusBarOverflow2(props, ref) {
  const { translate } = useTranslation();
  const roRef = useResizeObserver(props.onResize);
  const refs = useRefs(roRef, ref);
  const className = classnames("uifw-statusbar-overflow", props.className);
  return (
    // eslint-disable-next-line jsx-a11y/click-events-have-key-events
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className,
        onClick: props.onClick,
        ref: refs,
        style: props.style,
        role: "button",
        tabIndex: -1,
        title: translate("statusBar.overflow"),
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(Ellipsis, {})
      }
    )
  );
});
try {
  StatusBarOverflow.displayName = "StatusBarOverflow";
  StatusBarOverflow.__docgenInfo = { "description": "Entry point to overflow status bar items of [[StatusBarComposer]] component.", "displayName": "StatusBarOverflow", "props": { "onClick": { "defaultValue": null, "description": "Function called when button is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "onResize": { "defaultValue": null, "description": "Function called when button is resized.", "name": "onResize", "required": false, "type": { "name": "((w: number) => void)" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function StatusBarOverflowPanel(props) {
  const className = classnames("uifw-statusbar-panel", props.className);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Popup,
    {
      className,
      isOpen: props.open,
      offset: 0,
      onClose: props.onClose,
      style: props.style,
      showShadow: true,
      target: props.target,
      children: props.children
    }
  );
}
try {
  StatusBarOverflowPanel.displayName = "StatusBarOverflowPanel";
  StatusBarOverflowPanel.__docgenInfo = { "description": "Displays overflow status bar items.", "displayName": "StatusBarOverflowPanel", "props": { "children": { "defaultValue": null, "description": "Panel content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "onClose": { "defaultValue": null, "description": "", "name": "onClose", "required": true, "type": { "name": "() => void" } }, "target": { "defaultValue": null, "description": "", "name": "target", "required": true, "type": { "name": "HTMLElement | undefined" } }, "open": { "defaultValue": null, "description": "", "name": "open", "required": true, "type": { "name": "boolean" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const isInstance = (args) => {
  return !Array.isArray(args);
};
class StatusBarItemsManager {
  constructor(items) {
    this._items = [];
    this.onItemsChanged = new BeEvent();
    if (items)
      this.loadItemsInternal(items, true, false);
  }
  loadItemsInternal(items, processConditions, sendItemChanged) {
    if (processConditions && items) {
      const eventIds = StatusBarItemsManager.getSyncIdsOfInterest(items);
      if (0 !== eventIds.length) {
        const { itemsUpdated, updatedItems } = this.internalRefreshAffectedItems(items, new Set(eventIds));
        if (itemsUpdated)
          items = updatedItems;
      }
    }
    this._items = items;
    if (sendItemChanged)
      this.onItemsChanged.raiseEvent({ items });
  }
  /** load items but do not fire onItemsChanged
   * @internal
   */
  loadItems(items) {
    this.loadItemsInternal(items, true, false);
  }
  /** Get an array of the StatusBar items  */
  get items() {
    return this._items;
  }
  set items(items) {
    if (items !== this._items)
      this.loadItemsInternal(items, true, true);
  }
  add(itemOrItems) {
    let itemsToAdd;
    if (isInstance(itemOrItems))
      itemsToAdd = [itemOrItems];
    else {
      itemsToAdd = itemOrItems.filter(
        (itemToAdd, index) => itemOrItems.findIndex((item) => item.id === itemToAdd.id) === index
      );
    }
    itemsToAdd = itemsToAdd.filter(
      (itemToAdd) => this._items.find((item) => item.id === itemToAdd.id) === void 0
    );
    if (itemsToAdd.length === 0)
      return;
    const items = [...this._items, ...itemsToAdd];
    this.items = items;
  }
  /** Remove StatusBar items based on id */
  remove(itemIdOrItemIds) {
    const items = this._items.filter((item) => {
      return isInstance(itemIdOrItemIds) ? item.id !== itemIdOrItemIds : !itemIdOrItemIds.find((itemId) => itemId === item.id);
    });
    this.items = items;
  }
  /** @internal */
  removeAll() {
    this._items = [];
  }
  static getSyncIdsOfInterest(items) {
    const eventIds = /* @__PURE__ */ new Set();
    items.forEach((item) => {
      for (const [, entry] of Object.entries(item)) {
        if (entry instanceof ConditionalBooleanValue) {
          entry.syncEventIds.forEach(
            (eventId) => eventIds.add(eventId.toLowerCase())
          );
        } else if (entry instanceof ConditionalStringValue) {
          entry.syncEventIds.forEach(
            (eventId) => eventIds.add(eventId.toLowerCase())
          );
        }
      }
    });
    return [...eventIds.values()];
  }
  internalRefreshAffectedItems(items, eventIds) {
    if (0 === eventIds.size)
      return { itemsUpdated: false, updatedItems: [] };
    let updateRequired = false;
    const newItems = [];
    for (const item of items) {
      const updatedItem = { ...item };
      for (const [, entry] of Object.entries(updatedItem)) {
        if (entry instanceof ConditionalBooleanValue) {
          if (ConditionalBooleanValue.refreshValue(entry, eventIds))
            updateRequired = true;
        } else if (entry instanceof ConditionalStringValue) {
          if (ConditionalStringValue.refreshValue(entry, eventIds))
            updateRequired = true;
        }
      }
      newItems.push(updatedItem);
    }
    return { itemsUpdated: updateRequired, updatedItems: newItems };
  }
  refreshAffectedItems(eventIds) {
    if (0 === eventIds.size)
      return;
    const { itemsUpdated, updatedItems } = this.internalRefreshAffectedItems(
      this.items,
      eventIds
    );
    if (itemsUpdated)
      this.loadItemsInternal(updatedItems, false, true);
  }
}
const useDefaultStatusBarItems = (manager) => {
  const [items, setItems] = reactExports.useState(manager.items);
  const isInitialMount = reactExports.useRef(true);
  reactExports.useEffect(() => {
    if (isInitialMount.current)
      isInitialMount.current = false;
    else {
      setItems(manager.items);
    }
  }, [manager]);
  reactExports.useEffect(() => {
    return manager.onItemsChanged.addListener((args) => {
      setItems(args.items);
    });
  }, [manager]);
  return items;
};
try {
  useDefaultStatusBarItems.displayName = "useDefaultStatusBarItems";
  useDefaultStatusBarItems.__docgenInfo = { "description": "Hook that returns items from [[StatusBarItemsManager]].", "displayName": "useDefaultStatusBarItems", "props": { "_items": { "defaultValue": null, "description": "", "name": "_items", "required": true, "type": { "name": "readonly StatusBarItem[]" } }, "onItemsChanged": { "defaultValue": null, "description": "Event raised when StatusBar items are changed.\n@internal", "name": "onItemsChanged", "required": true, "type": { "name": "BeEvent<(args: StatusBarItemsChangedArgs) => void>" } }, "loadItemsInternal": { "defaultValue": null, "description": "", "name": "loadItemsInternal", "required": true, "type": { "name": "(items: readonly StatusBarItem[], processConditions: boolean, sendItemChanged: boolean) => void" } }, "loadItems": { "defaultValue": null, "description": "load items but do not fire onItemsChanged\n@internal", "name": "loadItems", "required": true, "type": { "name": "(items: readonly StatusBarItem[]) => void" } }, "items": { "defaultValue": null, "description": "Get an array of the StatusBar items", "name": "items", "required": true, "type": { "name": "readonly StatusBarItem[]" } }, "add": { "defaultValue": null, "description": "", "name": "add", "required": true, "type": { "name": "(itemOrItems: StatusBarItem | readonly StatusBarItem[]) => void" } }, "remove": { "defaultValue": null, "description": "Remove StatusBar items based on id", "name": "remove", "required": true, "type": { "name": "(itemIdOrItemIds: string | readonly string[]) => void" } }, "removeAll": { "defaultValue": null, "description": "@internal", "name": "removeAll", "required": true, "type": { "name": "() => void" } }, "internalRefreshAffectedItems": { "defaultValue": null, "description": "", "name": "internalRefreshAffectedItems", "required": true, "type": { "name": "(items: readonly StatusBarItem[], eventIds: Set<string>) => { itemsUpdated: boolean; updatedItems: StatusBarItem[]; }" } }, "refreshAffectedItems": { "defaultValue": null, "description": "", "name": "refreshAffectedItems", "required": true, "type": { "name": "(eventIds: Set<string>) => void" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function useActiveStageId() {
  const [activeStageId, setActiveStageId] = reactExports.useState(
    UiFramework.frontstages.activeFrontstageId
  );
  reactExports.useEffect(() => {
    const handleFrontstageActivatedEvent = (args) => {
      setActiveStageId(args.activatedFrontstageDef.id);
    };
    UiFramework.frontstages.onFrontstageActivatedEvent.addListener(
      handleFrontstageActivatedEvent
    );
    return () => {
      UiFramework.frontstages.onFrontstageActivatedEvent.removeListener(
        handleFrontstageActivatedEvent
      );
    };
  }, []);
  return activeStageId;
}
try {
  useActiveStageId.displayName = "useActiveStageId";
  useActiveStageId.__docgenInfo = { "description": "React hook that maintains the active stage Id.", "displayName": "useActiveStageId", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
var StagePanelSection = /* @__PURE__ */ ((StagePanelSection2) => {
  StagePanelSection2[StagePanelSection2["Start"] = 0] = "Start";
  StagePanelSection2[StagePanelSection2["End"] = 1] = "End";
  return StagePanelSection2;
})(StagePanelSection || {});
const _abstract = abstract;
const AbstractUiItemsManager = _abstract.UiItemsManager;
const isAbstractStatusBarCustomItem = _abstract.isAbstractStatusBarCustomItem;
const AbstractStagePanelLocation = _abstract.StagePanelLocation;
const AbstractStagePanelSection = _abstract.StagePanelSection;
const AbstractToolbarItemUtilities = ToolbarItemUtilities;
const originalDataSymbol = Symbol("originalData");
function createAbstractUiItemsManagerAdapter() {
  if (!AbstractUiItemsManager)
    return void 0;
  return new AbstractUiItemsManagerAdapter(AbstractUiItemsManager);
}
class AbstractUiItemsManagerAdapter {
  constructor(_adaptee) {
    this._adaptee = _adaptee;
  }
  clearAllProviders() {
    return this._adaptee.clearAllProviders();
  }
  get onUiProviderRegisteredEvent() {
    if (!("emit" in this._adaptee.onUiProviderRegisteredEvent)) {
      this._adaptee.onUiProviderRegisteredEvent.emit = (args) => {
        this._adaptee.onUiProviderRegisteredEvent.raiseEvent(args);
      };
    }
    return this._adaptee.onUiProviderRegisteredEvent;
  }
  unregister(providerId) {
    return this._adaptee.unregister(providerId);
  }
  get hasRegisteredProviders() {
    return this._adaptee.hasRegisteredProviders;
  }
  get registeredProviderIds() {
    return this._adaptee.registeredProviderIds;
  }
  register(provider, overrides) {
    const abstractProvider = createUiItemsProviderToAbstractAdapter(provider);
    return this._adaptee.register(abstractProvider, overrides);
  }
  getUiItemsProvider(providerId) {
    const abstractProvider = this._adaptee.getUiItemsProvider(providerId);
    if (!abstractProvider)
      return void 0;
    const provider = createAbstractToUiItemsProviderAdapter(abstractProvider);
    return provider;
  }
  getBackstageItems() {
    const abstractItems = this._adaptee.getBackstageItems();
    const items = abstractItems.map((abstractItem) => {
      const item = fromAbstractBackstageItem(abstractItem);
      return {
        ...item,
        providerId: abstractItem.providerId ? abstractItem.providerId : ""
      };
    });
    return items;
  }
  getStatusBarItems(stageId, stageUsage) {
    const abstractItems = this._adaptee.getStatusBarItems(stageId, stageUsage);
    const items = abstractItems.map((abstractItem) => {
      const item = fromAbstractStatusBarItem(abstractItem);
      return {
        ...item,
        providerId: abstractItem.providerId ? abstractItem.providerId : ""
      };
    });
    return items;
  }
  getToolbarButtonItems(stageId, stageUsage, usage, orientation) {
    const abstractItems = this._adaptee.getToolbarButtonItems(
      stageId,
      stageUsage,
      usage,
      orientation
    );
    const items = abstractItems.map((abstractItem) => {
      const item = fromAbstractToolbarItem(abstractItem);
      return {
        ...item,
        providerId: abstractItem.providerId ?? ""
      };
    });
    return items;
  }
  getWidgets(stageId, stageUsage, location, section) {
    const abstractLocations = toAbstractStagePanelLocation(location);
    const abstractSections = section === void 0 ? [void 0] : toAbstractStagePanelSection(section);
    const combos = [];
    abstractLocations.forEach((abstractLocation) => {
      abstractSections.forEach((abstractSection) => {
        combos.push([abstractLocation, abstractSection]);
      });
    });
    let abstractWidgets = combos.reduce(
      (acc, [abstractLocation, abstractSection]) => {
        const w = this._adaptee.getWidgets(
          stageId,
          stageUsage,
          abstractLocation,
          abstractSection
        );
        acc.push(...w);
        return acc;
      },
      []
    );
    abstractWidgets = abstractWidgets.reduce(
      (acc, abstractWidget) => {
        if (-1 === acc.findIndex((w) => w.id === abstractWidget.id))
          acc.push(abstractWidget);
        return acc;
      },
      []
    );
    const widgets = abstractWidgets.map((abstractWidget) => {
      const widget2 = fromAbstractWidget(abstractWidget);
      return {
        ...widget2,
        providerId: abstractWidget.providerId ?? ""
      };
    });
    return widgets;
  }
}
function createUiItemsProviderToAbstractAdapter(adaptee) {
  const {
    id,
    onUnregister,
    provideBackstageItems,
    provideStatusBarItems,
    provideWidgets,
    ...other
  } = adaptee;
  const provider = createGetPropertyAdapter(adaptee);
  return {
    id: adaptee.id,
    onUnregister: adaptee.onUnregister,
    provideBackstageItems: () => {
      const items = provider.provideBackstageItems();
      const abstractItems = items.map((item) => toAbstractBackstageItem(item));
      return abstractItems;
    },
    provideStatusBarItems: (stageId, stageUsage, _stageAppData) => {
      const items = provider.provideStatusBarItems(stageId, stageUsage);
      const abstractItems = items.map((item) => toAbstractStatusBarItem(item));
      return abstractItems;
    },
    provideToolbarButtonItems: (stageId, stageUsage, usage, orientation, _stageAppData) => {
      const items = provider.provideToolbarItems(
        stageId,
        stageUsage,
        usage,
        orientation
      );
      const abstractItems = items.map((item) => toAbstractToolbarItem(item));
      return abstractItems;
    },
    provideWidgets: (stageId, stageUsage, abstractLocation, abstractSection, _zoneLocation, _stageAppData) => {
      const location = fromAbstractStagePanelLocation(abstractLocation);
      const section = abstractSection === void 0 ? void 0 : fromAbstractStagePanelSection(abstractSection);
      const items = provider.provideWidgets(
        stageId,
        stageUsage,
        location,
        section
      );
      const abstractWidgets = items.map((item) => toAbstractWidget(item));
      return abstractWidgets;
    },
    ...other
  };
}
function createAbstractToUiItemsProviderAdapter(adaptee) {
  const {
    id,
    onUnregister,
    provideBackstageItems,
    provideStatusBarItems,
    provideWidgets,
    ...other
  } = adaptee;
  return {
    id: adaptee.id,
    onUnregister: adaptee.onUnregister,
    provideBackstageItems: () => {
      if (!adaptee.provideBackstageItems)
        return [];
      const abstractItems = adaptee.provideBackstageItems();
      const items = abstractItems.map(
        (abstractItem) => fromAbstractBackstageItem(abstractItem)
      );
      return items;
    },
    provideStatusBarItems: (stageId, stageUsage) => {
      if (!adaptee.provideStatusBarItems)
        return [];
      const abstractItems = adaptee.provideStatusBarItems(stageId, stageUsage);
      const items = abstractItems.map(
        (abstractItem) => fromAbstractStatusBarItem(abstractItem)
      );
      return items;
    },
    provideWidgets: (stageId, stageUsage, location, section) => {
      if (!adaptee.provideWidgets)
        return [];
      const abstractLocations = toAbstractStagePanelLocation(location);
      const abstractSections = section === void 0 ? [void 0] : toAbstractStagePanelSection(section);
      const combos = [];
      abstractLocations.forEach((abstractLocation) => {
        abstractSections.forEach((abstractSection) => {
          combos.push([abstractLocation, abstractSection]);
        });
      });
      let abstractWidgets = combos.reduce(
        (acc, [abstractLocation, abstractSection]) => {
          assert(!!adaptee.provideWidgets);
          const w = adaptee.provideWidgets(
            stageId,
            stageUsage,
            abstractLocation,
            abstractSection
          );
          acc.push(...w);
          return acc;
        },
        []
      );
      abstractWidgets = abstractWidgets.reduce(
        (acc, abstractWidget) => {
          if (-1 === acc.findIndex((w) => w.id === abstractWidget.id))
            acc.push(abstractWidget);
          return acc;
        },
        []
      );
      const widgets = abstractWidgets.map(
        (abstractWidget) => fromAbstractWidget(abstractWidget)
      );
      return widgets;
    },
    provideToolbarItems: (stageId, stageUsage, usage, orientation) => {
      if (!adaptee.provideToolbarButtonItems)
        return [];
      const abstractItems = adaptee.provideToolbarButtonItems(
        stageId,
        stageUsage,
        usage,
        orientation
      );
      const items = abstractItems.map(
        (abstractItem) => fromAbstractToolbarItem(abstractItem)
      );
      return items;
    },
    ...other
  };
}
function toAbstractBackstageItem(item) {
  let abstractItem = getOriginalData(item);
  if (abstractItem)
    return abstractItem;
  const internalData = /* @__PURE__ */ new Map();
  const icon = IconHelper.getIconData(item.icon, internalData);
  abstractItem = {
    ...item,
    badgeType: item.badge,
    icon,
    internalData
  };
  setOriginalData(abstractItem, item);
  return abstractItem;
}
function toAbstractStatusBarItem(item) {
  let abstractItem = getOriginalData(item);
  if (abstractItem)
    return abstractItem;
  if (isStatusBarCustomItem(item)) {
    abstractItem = {
      ...item,
      badgeType: item.badge,
      isCustom: true,
      reactNode: item.content
    };
  } else {
    const internalData = /* @__PURE__ */ new Map();
    const icon = IconHelper.getIconData(item.icon, internalData);
    abstractItem = {
      ...item,
      badgeType: item.badge,
      icon,
      internalData
    };
  }
  setOriginalData(abstractItem, item);
  return abstractItem;
}
function toAbstractToolbarItem(item) {
  let abstractItem = getOriginalData(item);
  if (abstractItem)
    return abstractItem;
  const internalData = /* @__PURE__ */ new Map();
  const icon = IconHelper.getIconData(item.icon, internalData);
  if (isToolbarActionItem(item)) {
    abstractItem = {
      ...item,
      badgeType: item.badge,
      icon,
      internalData,
      parentToolGroupId: item.parentGroupItemId
    };
  } else if (isToolbarGroupItem(item)) {
    abstractItem = {
      ...item,
      badgeType: item.badge,
      icon,
      internalData,
      parentToolGroupId: item.parentGroupItemId,
      items: item.items.map((i) => toAbstractToolbarItem(i))
    };
  } else {
    abstractItem = {
      ...item,
      badgeType: item.badge,
      isCustom: true,
      icon,
      internalData
    };
  }
  setOriginalData(abstractItem, item);
  return abstractItem;
}
function toAbstractWidget(widget2) {
  var _a2;
  let abstractWidget = getOriginalData(widget2);
  if (abstractWidget)
    return abstractWidget;
  const allowedPanelTargets = (_a2 = widget2.allowedPanels) == null ? void 0 : _a2.map((location) => {
    const map = {
      [StagePanelLocation.Top]: "top",
      [StagePanelLocation.Left]: "left",
      [StagePanelLocation.Right]: "right",
      [StagePanelLocation.Bottom]: "bottom"
    };
    return map[location];
  });
  const internalData = /* @__PURE__ */ new Map();
  const icon = IconHelper.getIconData(widget2.icon, internalData);
  abstractWidget = {
    ...widget2,
    id: widget2.id ?? "",
    getWidgetContent: () => widget2.content,
    allowedPanelTargets,
    badgeType: widget2.badge,
    defaultFloatingPosition: typeof widget2.canFloat === "object" ? widget2.canFloat.defaultPosition : void 0,
    defaultFloatingSize: typeof widget2.canFloat === "object" ? widget2.canFloat.defaultSize : void 0,
    floatingContainerId: typeof widget2.canFloat === "object" ? widget2.canFloat.containerId : void 0,
    hideWithUiWhenFloating: typeof widget2.canFloat === "object" ? widget2.canFloat.hideWithUi : void 0,
    isFloatingStateSupported: !!widget2.canFloat,
    isFloatingStateWindowResizable: typeof widget2.canFloat === "object" ? widget2.canFloat.isResizable : void 0,
    icon,
    internalData
  };
  setOriginalData(abstractWidget, widget2);
  return abstractWidget;
}
function fromAbstractBackstageItem(abstractItem) {
  let item = getOriginalData(abstractItem);
  if (item)
    return item;
  const icon = fromAbstractIcon(abstractItem.icon, abstractItem.internalData);
  item = {
    ...abstractItem,
    badge: abstractItem.badgeType,
    icon
  };
  setOriginalData(item, abstractItem);
  return item;
}
function fromAbstractStatusBarItem(abstractItem) {
  let item = getOriginalData(abstractItem);
  if (item)
    return item;
  if (isAbstractStatusBarCustomItem(abstractItem)) {
    const content = abstractItem.reactNode;
    return {
      ...abstractItem,
      badge: abstractItem.badgeType,
      content
    };
  } else {
    const icon = fromAbstractIcon(abstractItem.icon, abstractItem.internalData);
    item = {
      ...abstractItem,
      badge: abstractItem.badgeType,
      icon
    };
  }
  setOriginalData(item, abstractItem);
  return item;
}
function fromAbstractToolbarItem(abstractItem) {
  let item = getOriginalData(abstractItem);
  if (item)
    return item;
  const icon = fromAbstractIcon(abstractItem.icon, abstractItem.internalData);
  if (AbstractToolbarItemUtilities.isCustomDefinition(abstractItem)) {
    item = {
      ...abstractItem,
      badge: abstractItem.badgeType,
      icon
    };
  } else {
    item = {
      ...abstractItem,
      badge: abstractItem.badgeType,
      parentGroupItemId: abstractItem.parentToolGroupId,
      icon
    };
  }
  setOriginalData(item, abstractItem);
  return item;
}
function fromAbstractWidget(abstractWidget) {
  var _a2;
  let widget2 = getOriginalData(abstractWidget);
  if (widget2)
    return widget2;
  const allowedPanels = (_a2 = abstractWidget.allowedPanelTargets) == null ? void 0 : _a2.map((target) => {
    const map = {
      top: StagePanelLocation.Top,
      left: StagePanelLocation.Left,
      right: StagePanelLocation.Right,
      bottom: StagePanelLocation.Bottom
    };
    return map[target];
  });
  const icon = fromAbstractIcon(
    abstractWidget.icon,
    abstractWidget.internalData
  );
  widget2 = {
    ...abstractWidget,
    id: abstractWidget.id ?? "",
    content: abstractWidget.getWidgetContent(),
    allowedPanels,
    badge: abstractWidget.badgeType,
    canFloat: abstractWidget.isFloatingStateSupported && {
      containerId: abstractWidget.floatingContainerId,
      defaultPosition: abstractWidget.defaultFloatingPosition,
      defaultSize: abstractWidget.defaultFloatingSize,
      hideWithUi: abstractWidget.hideWithUiWhenFloating,
      isResizable: abstractWidget.isFloatingStateWindowResizable
    },
    icon
  };
  setOriginalData(widget2, abstractWidget);
  return widget2;
}
function fromAbstractStagePanelLocation(location) {
  switch (location) {
    case AbstractStagePanelLocation.Left:
      return StagePanelLocation.Left;
    case AbstractStagePanelLocation.Top:
      return StagePanelLocation.Top;
    case AbstractStagePanelLocation.TopMost:
      return StagePanelLocation.Top;
    case AbstractStagePanelLocation.Bottom:
      return StagePanelLocation.Bottom;
    case AbstractStagePanelLocation.BottomMost:
      return StagePanelLocation.Bottom;
  }
  return StagePanelLocation.Right;
}
function toAbstractStagePanelLocation(location) {
  switch (location) {
    case StagePanelLocation.Left:
      return [AbstractStagePanelLocation.Left];
    case StagePanelLocation.Top:
      return [
        AbstractStagePanelLocation.Top,
        AbstractStagePanelLocation.TopMost
      ];
    case StagePanelLocation.Bottom:
      return [
        AbstractStagePanelLocation.Bottom,
        AbstractStagePanelLocation.BottomMost
      ];
  }
  return [AbstractStagePanelLocation.Right];
}
function fromAbstractStagePanelSection(section) {
  switch (section) {
    case AbstractStagePanelSection.End:
      return StagePanelSection.End;
  }
  return StagePanelSection.Start;
}
function toAbstractStagePanelSection(section) {
  switch (section) {
    case StagePanelSection.End:
      return [AbstractStagePanelSection.End];
  }
  return [AbstractStagePanelSection.Start, AbstractStagePanelSection.Middle];
}
function fromAbstractIcon(icon, internalData) {
  if (!icon)
    return void 0;
  const iconString = ConditionalStringValue.getValue(icon);
  if (!iconString)
    return void 0;
  if (iconString === IconHelper.reactIconKey && internalData)
    return internalData.get(IconHelper.reactIconKey);
  return iconString;
}
function getOriginalData(obj) {
  const originalData = obj[originalDataSymbol];
  if (!originalData)
    return void 0;
  return originalData;
}
function setOriginalData(obj, data) {
  obj[originalDataSymbol] = data;
}
function createGetPropertyAdapter(provider) {
  return {
    ...provider,
    provideBackstageItems: () => {
      var _a2, _b2;
      return ((_a2 = provider.getBackstageItems) == null ? void 0 : _a2.call(provider)) ?? ((_b2 = provider.provideBackstageItems) == null ? void 0 : _b2.call(provider)) ?? [];
    },
    provideStatusBarItems: (stageId, stageUsage) => {
      var _a2, _b2;
      return ((_a2 = provider.getStatusBarItems) == null ? void 0 : _a2.call(provider)) ?? ((_b2 = provider.provideStatusBarItems) == null ? void 0 : _b2.call(provider, stageId, stageUsage)) ?? [];
    },
    provideToolbarItems: (stageId, stageUsage, usage, orientation) => {
      var _a2;
      if (provider.getToolbarItems) {
        return provider.getToolbarItems().filter((item) => {
          var _a3;
          const layout = (_a3 = item.layouts) == null ? void 0 : _a3.standard;
          if (!layout)
            return false;
          return layout.usage === usage && layout.orientation === orientation;
        });
      }
      return ((_a2 = provider.provideToolbarItems) == null ? void 0 : _a2.call(
        provider,
        stageId,
        stageUsage,
        usage,
        orientation
      )) ?? [];
    },
    provideWidgets: (stageId, stageUsage, location, section) => {
      var _a2;
      if (provider.getWidgets) {
        return provider.getWidgets().filter((item) => {
          var _a3;
          const layout = (_a3 = item.layouts) == null ? void 0 : _a3.standard;
          if (!layout)
            return false;
          return layout.location === location && layout.section === section;
        });
      }
      return ((_a2 = provider.provideWidgets) == null ? void 0 : _a2.call(provider, stageId, stageUsage, location, section)) ?? [];
    }
  };
}
const _UiItemsManager = class _UiItemsManager {
  /** For use in unit testing
   * @internal
   */
  static useAbstractAdapter(useAbstractAdapter) {
    if (useAbstractAdapter) {
      this._abstractAdapter = createAbstractUiItemsManagerAdapter();
    } else {
      this._abstractAdapter = void 0;
    }
  }
  /** For use in unit testing
   * @internal */
  static clearAllProviders() {
    if (this._abstractAdapter)
      return this._abstractAdapter.clearAllProviders();
    _UiItemsManager._registeredUiItemsProviders.clear();
  }
  /** Event raised any time a UiProvider is registered or unregistered. */
  static get onUiProviderRegisteredEvent() {
    if (this._abstractAdapter)
      return this._abstractAdapter.onUiProviderRegisteredEvent;
    return this._onUiProviderRegisteredEvent;
  }
  /** Return number of registered UiProvider. */
  static get registeredProviderIds() {
    if (this._abstractAdapter)
      return this._abstractAdapter.registeredProviderIds;
    const ids = [..._UiItemsManager._registeredUiItemsProviders.keys()];
    return ids;
  }
  /** Return true if there is any registered UiProvider. */
  static get hasRegisteredProviders() {
    if (this._abstractAdapter)
      return this._abstractAdapter.hasRegisteredProviders;
    return this._registeredUiItemsProviders.size > 0;
  }
  /**
   * Retrieves a previously loaded UiItemsProvider.
   * @param providerId id of the UiItemsProvider to get
   */
  static getUiItemsProvider(providerId) {
    var _a2;
    if (this._abstractAdapter)
      return this._abstractAdapter.getUiItemsProvider(providerId);
    return (_a2 = _UiItemsManager._registeredUiItemsProviders.get(providerId)) == null ? void 0 : _a2.provider;
  }
  static sendRegisteredEvent(args) {
    _UiItemsManager.onUiProviderRegisteredEvent.raiseEvent(args);
  }
  /**
   * Registers a UiItemsProvider with the UiItemsManager.
   * @param uiProvider the UI items provider to register.
   * @param overrides the UI items provider to register.
   */
  static register(uiProvider, overrides) {
    if (this._abstractAdapter)
      return this._abstractAdapter.register(uiProvider, overrides);
    const providerId = (overrides == null ? void 0 : overrides.providerId) ?? uiProvider.id;
    if (_UiItemsManager.getUiItemsProvider(providerId)) {
      Logger.logInfo(
        UiFramework.loggerCategory(this),
        `UiItemsProvider (${providerId}) is already loaded`
      );
    } else {
      _UiItemsManager._registeredUiItemsProviders.set(providerId, {
        provider: uiProvider,
        overrides
      });
      Logger.logInfo(
        UiFramework.loggerCategory(this),
        `UiItemsProvider ${uiProvider.id} registered as ${providerId} `
      );
      _UiItemsManager.sendRegisteredEvent({ providerId });
    }
  }
  /** Remove a specific UiItemsProvider from the list of available providers. */
  static unregister(providerId) {
    if (this._abstractAdapter)
      return this._abstractAdapter.unregister(providerId);
    const provider = _UiItemsManager.getUiItemsProvider(providerId);
    if (!provider)
      return;
    provider.onUnregister && provider.onUnregister();
    _UiItemsManager._registeredUiItemsProviders.delete(providerId);
    Logger.logInfo(
      UiFramework.loggerCategory(this),
      `UiItemsProvider (${providerId}) unloaded`
    );
    _UiItemsManager.sendRegisteredEvent({ providerId });
  }
  static allowItemsFromProvider(entry, stageId, stageUsage) {
    const overrides = entry.overrides;
    if (void 0 !== stageId && (overrides == null ? void 0 : overrides.stageIds) && !overrides.stageIds.some((value) => value === stageId))
      return false;
    if (void 0 !== stageUsage && (overrides == null ? void 0 : overrides.stageUsages) && !overrides.stageUsages.some((value) => value === stageUsage))
      return false;
    return true;
  }
  /** Called when the application is populating a toolbar so that any registered UiItemsProvider can add tool buttons that either either execute
   * an action or specify a registered ToolId into toolbar.
   * @param stageId a string identifier of the active stage.
   * @param stageUsage the StageUsage of the active stage.
   * @param usage usage of the toolbar
   * @param orientation orientation of the toolbar
   * @returns an array of error messages. The array will be empty if the load is successful, otherwise it is a list of one or more problems.
   */
  static getToolbarButtonItems(stageId, stageUsage, usage, orientation) {
    if (this._abstractAdapter) {
      return this._abstractAdapter.getToolbarButtonItems(
        stageId,
        stageUsage,
        usage,
        orientation
      );
    }
    const items = [];
    _UiItemsManager._registeredUiItemsProviders.forEach((entry) => {
      var _a2;
      if (!this.allowItemsFromProvider(entry, stageId, stageUsage))
        return;
      const uiProvider = entry.provider;
      const providerId = ((_a2 = entry.overrides) == null ? void 0 : _a2.providerId) ?? uiProvider.id;
      const provider = createGetPropertyAdapter(uiProvider);
      const providerItems = provider.provideToolbarItems(stageId, stageUsage, usage, orientation).map((item) => ({
        ...item,
        providerId
      }));
      items.push(...providerItems);
    });
    return getUniqueItems(items);
  }
  /** Called when the application is populating the statusbar so that any registered UiItemsProvider can add status fields
   * @param stageId a string identifier of the active stage.
   * @param stageUsage the StageUsage of the active stage.
   * @returns An array of CommonStatusBarItem that will be used to create controls for the status bar.
   */
  static getStatusBarItems(stageId, stageUsage) {
    if (this._abstractAdapter) {
      return this._abstractAdapter.getStatusBarItems(stageId, stageUsage);
    }
    const items = [];
    _UiItemsManager._registeredUiItemsProviders.forEach((entry) => {
      var _a2;
      if (!this.allowItemsFromProvider(entry, stageId, stageUsage))
        return;
      const uiProvider = entry.provider;
      const providerId = ((_a2 = entry.overrides) == null ? void 0 : _a2.providerId) ?? uiProvider.id;
      const provider = createGetPropertyAdapter(uiProvider);
      const providerItems = provider.provideStatusBarItems(stageId, stageUsage).map((item) => ({
        ...item,
        providerId
      }));
      items.push(...providerItems);
    });
    return getUniqueItems(items);
  }
  /** Called when the application is populating the statusbar so that any registered UiItemsProvider can add status fields
   * @returns An array of BackstageItem that will be used to create controls for the backstage menu.
   */
  static getBackstageItems() {
    if (this._abstractAdapter) {
      return this._abstractAdapter.getBackstageItems();
    }
    const items = [];
    _UiItemsManager._registeredUiItemsProviders.forEach((entry) => {
      var _a2;
      const uiProvider = entry.provider;
      const providerId = ((_a2 = entry.overrides) == null ? void 0 : _a2.providerId) ?? uiProvider.id;
      const provider = createGetPropertyAdapter(uiProvider);
      const providerItems = provider.provideBackstageItems().map((item) => ({
        ...item,
        providerId
      }));
      items.push(...providerItems);
    });
    return getUniqueItems(items);
  }
  /** Called when the application is populating the Stage Panels so that any registered UiItemsProvider can add widgets
   * @param stageId a string identifier of the active stage.
   * @param stageUsage the StageUsage of the active stage.
   * @param location the location within the stage.
   * @param section the section within location.
   * @returns An array of widgets.
   */
  static getWidgets(stageId, stageUsage, location, section) {
    const items = [];
    if (this._abstractAdapter) {
      const abstractWidgets = this._abstractAdapter.getWidgets(
        stageId,
        stageUsage,
        location,
        section
      );
      items.push(...abstractWidgets);
    }
    _UiItemsManager._registeredUiItemsProviders.forEach((entry) => {
      var _a2;
      const uiProvider = entry.provider;
      const providerId = ((_a2 = entry.overrides) == null ? void 0 : _a2.providerId) ?? uiProvider.id;
      if (!this.allowItemsFromProvider(entry, stageId, stageUsage))
        return;
      const provider = createGetPropertyAdapter(uiProvider);
      const providerItems = provider.provideWidgets(stageId, stageUsage, location, section).map((item) => ({
        ...item,
        providerId
      }));
      items.push(...providerItems);
    });
    return getUniqueItems(items);
  }
};
_UiItemsManager._registeredUiItemsProviders = /* @__PURE__ */ new Map();
_UiItemsManager._onUiProviderRegisteredEvent = new BeUiEvent();
_UiItemsManager._abstractAdapter = createAbstractUiItemsManagerAdapter();
let UiItemsManager = _UiItemsManager;
function getUniqueItems(items) {
  const uniqueItems = [];
  items.forEach((item) => {
    if (uniqueItems.find((existingItem) => item.id === existingItem.id))
      return;
    uniqueItems.push(item);
  });
  return uniqueItems;
}
function useAvailableUiItemsProviders() {
  const [uiItemsProviderIds, setUiItemsProviderIds] = reactExports.useState(
    UiItemsManager.registeredProviderIds
  );
  reactExports.useEffect(() => {
    return UiItemsManager.onUiProviderRegisteredEvent.addListener(() => {
      setUiItemsProviderIds(UiItemsManager.registeredProviderIds);
    });
  }, []);
  return uiItemsProviderIds;
}
try {
  useAvailableUiItemsProviders.displayName = "useAvailableUiItemsProviders";
  useAvailableUiItemsProviders.__docgenInfo = { "description": "React hook that maintains the number of available UiItemsProviders. This allows components to use it to refresh when a\nUiItemsProviders is added or removed allowing the ui component to be re-rendered.", "displayName": "useAvailableUiItemsProviders", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const useUiItemsProviderStatusBarItems = (manager) => {
  const uiItemProviderIds = useAvailableUiItemsProviders();
  const stageId = useActiveStageId();
  const [items, setItems] = reactExports.useState(manager.items);
  const providersRef = reactExports.useRef("");
  const currentStageRef = reactExports.useRef("");
  reactExports.useEffect(() => {
    const uiProviders = uiItemProviderIds.join("-");
    if (providersRef.current !== uiProviders || currentStageRef.current !== stageId) {
      currentStageRef.current = stageId;
      const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
      if (frontstageDef) {
        providersRef.current = uiProviders;
        const statusBarItems = UiItemsManager.getStatusBarItems(
          stageId,
          frontstageDef.usage
        );
        manager.loadItems(statusBarItems);
        setItems(manager.items);
      }
    }
  }, [manager, uiItemProviderIds, stageId]);
  reactExports.useEffect(() => {
    return manager.onItemsChanged.addListener((args) => {
      setItems(args.items);
    });
  }, [manager]);
  return items;
};
try {
  useUiItemsProviderStatusBarItems.displayName = "useUiItemsProviderStatusBarItems";
  useUiItemsProviderStatusBarItems.__docgenInfo = { "description": "Hook that returns items from [[StatusBarItemsManager]].", "displayName": "useUiItemsProviderStatusBarItems", "props": { "_items": { "defaultValue": null, "description": "", "name": "_items", "required": true, "type": { "name": "readonly StatusBarItem[]" } }, "onItemsChanged": { "defaultValue": null, "description": "Event raised when StatusBar items are changed.\n@internal", "name": "onItemsChanged", "required": true, "type": { "name": "BeEvent<(args: StatusBarItemsChangedArgs) => void>" } }, "loadItemsInternal": { "defaultValue": null, "description": "", "name": "loadItemsInternal", "required": true, "type": { "name": "(items: readonly StatusBarItem[], processConditions: boolean, sendItemChanged: boolean) => void" } }, "loadItems": { "defaultValue": null, "description": "load items but do not fire onItemsChanged\n@internal", "name": "loadItems", "required": true, "type": { "name": "(items: readonly StatusBarItem[]) => void" } }, "items": { "defaultValue": null, "description": "Get an array of the StatusBar items", "name": "items", "required": true, "type": { "name": "readonly StatusBarItem[]" } }, "add": { "defaultValue": null, "description": "", "name": "add", "required": true, "type": { "name": "(itemOrItems: StatusBarItem | readonly StatusBarItem[]) => void" } }, "remove": { "defaultValue": null, "description": "Remove StatusBar items based on id", "name": "remove", "required": true, "type": { "name": "(itemIdOrItemIds: string | readonly string[]) => void" } }, "removeAll": { "defaultValue": null, "description": "@internal", "name": "removeAll", "required": true, "type": { "name": "() => void" } }, "internalRefreshAffectedItems": { "defaultValue": null, "description": "", "name": "internalRefreshAffectedItems", "required": true, "type": { "name": "(items: readonly StatusBarItem[], eventIds: Set<string>) => { itemsUpdated: boolean; updatedItems: StatusBarItem[]; }" } }, "refreshAffectedItems": { "defaultValue": null, "description": "", "name": "refreshAffectedItems", "required": true, "type": { "name": "(eventIds: Set<string>) => void" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function getCombinedSectionItemPriority(item) {
  let sectionValue = 0;
  if (item.section === StatusBarSection.Center)
    sectionValue = 1e5;
  else if (item.section === StatusBarSection.Context)
    sectionValue = 2e5;
  else if (item.section === StatusBarSection.Right)
    sectionValue = 3e5;
  return sectionValue + item.itemPriority;
}
const DockedStatusBarEntryContext = reactExports.createContext(null);
DockedStatusBarEntryContext.displayName = "nz:DockedStatusBarEntryContext";
function useStatusBarEntry() {
  return reactExports.useContext(DockedStatusBarEntryContext);
}
function DockedStatusBarItem(props) {
  const { onResize } = useStatusBarEntry();
  const ref = useResizeObserver(onResize);
  const className = classnames(
    "uifw-statusbar-item-container",
    props.className
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      "data-item-id": props.itemId,
      "data-item-type": "status-bar-item",
      "data-item-location": props.section,
      "data-item-priority": props.itemPriority,
      "data-item-provider-id": props.providerId,
      className,
      ref,
      style: props.style,
      children: props.children
    }
  );
}
function DockedStatusBarEntry({
  children,
  entryKey,
  getOnResize
}) {
  const onResize = reactExports.useMemo(
    () => getOnResize(entryKey),
    [getOnResize, entryKey]
  );
  const entry = reactExports.useMemo(
    () => ({
      isOverflown: false,
      onResize
    }),
    [onResize]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(DockedStatusBarEntryContext.Provider, { value: entry, children });
}
function useStatusBarItemSyncEffect(itemsManager, syncIdsOfInterest) {
  reactExports.useEffect(() => {
    const handleSyncUiEvent = (args) => {
      if (0 === syncIdsOfInterest.length)
        return;
      if (syncIdsOfInterest.some(
        (value) => args.eventIds.has(value.toLowerCase())
      )) {
        itemsManager.refreshAffectedItems(args.eventIds);
      }
    };
    SyncUiEventDispatcher.onSyncUiEvent.addListener(handleSyncUiEvent);
    return () => {
      SyncUiEventDispatcher.onSyncUiEvent.removeListener(handleSyncUiEvent);
    };
  }, [itemsManager, itemsManager.items, syncIdsOfInterest]);
}
function StatusBarLabelItemComponent(props) {
  const label = ConditionalStringValue.getValue(props.label);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(StatusBarLabelIndicator, { iconSpec: props.icon, label });
}
function StatusBarActionItemComponent(props) {
  const title = ConditionalStringValue.getValue(props.tooltip);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    StatusBarLabelIndicator,
    {
      title,
      onClick: props.execute,
      iconSpec: props.icon
    }
  );
}
function combineItems(stageItems, addonItems) {
  const items = [];
  if (stageItems.length) {
    stageItems.forEach((srcItem) => {
      if (-1 === items.findIndex((item) => item.id === srcItem.id)) {
        items.push(srcItem);
      }
    });
  }
  if (addonItems.length) {
    addonItems.forEach((srcItem) => {
      if (-1 === items.findIndex((item) => item.id === srcItem.id)) {
        items.push(srcItem);
      }
    });
  }
  return items;
}
function verifiedMapEntries(map) {
  for (const [, val] of map) {
    if (val === void 0)
      return void 0;
  }
  return map;
}
function getItemToPlaceInOverflow(width, docked, overflowWidth) {
  let settingsWidth = 0;
  let i = 0;
  for (; i < docked.length; i++) {
    const w = docked[i][1];
    const newSettingsWidth = settingsWidth + w;
    if (newSettingsWidth > width) {
      settingsWidth += overflowWidth;
      break;
    }
    settingsWidth = newSettingsWidth;
  }
  let j = i;
  for (; j > 0; j--) {
    if (settingsWidth <= width)
      break;
    const w = docked[j][1];
    settingsWidth -= w;
  }
  const overflowItems = new Array();
  for (i = j; i < docked.length; i++) {
    overflowItems.push(docked[i][0]);
  }
  return overflowItems;
}
function isItemInOverflow(id, overflowItemIds) {
  if (!overflowItemIds || 0 === overflowItemIds.length)
    return false;
  return !!overflowItemIds.find((value) => value === id);
}
function StatusBarComposer(props) {
  const {
    className,
    style,
    items,
    mainClassName,
    leftClassName,
    centerClassName,
    rightClassName
  } = props;
  const [defaultItemsManager, setDefaultItemsManager] = reactExports.useState(
    () => new StatusBarItemsManager(items)
  );
  const [isOverflowPanelOpen, setIsOverflowPanelOpen] = reactExports.useState(false);
  const containerWidth = reactExports.useRef(void 0);
  const isInitialMount = reactExports.useRef(true);
  reactExports.useEffect(() => {
    if (isInitialMount.current)
      isInitialMount.current = false;
    else {
      setDefaultItemsManager(new StatusBarItemsManager(items));
    }
  }, [items]);
  const defaultItems = useDefaultStatusBarItems(defaultItemsManager);
  const syncIdsOfInterest = reactExports.useMemo(
    () => StatusBarItemsManager.getSyncIdsOfInterest(defaultItems),
    [defaultItems]
  );
  useStatusBarItemSyncEffect(defaultItemsManager, syncIdsOfInterest);
  const [addonItemsManager] = reactExports.useState(() => new StatusBarItemsManager());
  const addonItems = useUiItemsProviderStatusBarItems(addonItemsManager);
  const addonSyncIdsOfInterest = reactExports.useMemo(
    () => StatusBarItemsManager.getSyncIdsOfInterest(addonItems),
    [addonItems]
  );
  useStatusBarItemSyncEffect(addonItemsManager, addonSyncIdsOfInterest);
  const statusBarItems = reactExports.useMemo(
    () => combineItems(defaultItems, addonItems),
    [defaultItems, addonItems]
  );
  const entryWidths = reactExports.useRef(/* @__PURE__ */ new Map());
  const overflowWidth = reactExports.useRef(void 0);
  const [overflown, setOverflown] = reactExports.useState();
  const calculateOverflow = reactExports.useCallback(() => {
    const widths = verifiedMapEntries(entryWidths.current);
    if (containerWidth.current === void 0 || widths === void 0 || overflowWidth.current === void 0) {
      setOverflown(new Array());
      return;
    }
    const newOverflown = getItemToPlaceInOverflow(
      containerWidth.current,
      [...widths.entries()],
      overflowWidth.current
    );
    if (!eqlOverflown(overflown, newOverflown)) {
      setOverflown(newOverflown);
      if (0 === newOverflown.length && isOverflowPanelOpen)
        setIsOverflowPanelOpen(false);
    }
  }, [isOverflowPanelOpen, overflown]);
  const handleOverflowResize = reactExports.useCallback(
    (w) => {
      const calculate = overflowWidth.current !== w;
      overflowWidth.current = w;
      calculate && calculateOverflow();
    },
    [calculateOverflow]
  );
  const handleEntryResize = reactExports.useCallback(
    (key) => (w) => {
      const oldW = entryWidths.current.get(key);
      if (void 0 === oldW || Math.abs(oldW - w) > 2) {
        entryWidths.current.set(key, w);
        calculateOverflow();
      }
    },
    [calculateOverflow]
  );
  const getSectionName = (section) => {
    switch (section) {
      case StatusBarSection.Center:
      case StatusBarSection.Stage:
        return "status-bar-center";
      case StatusBarSection.Context:
        return "status-bar-right-start";
      case StatusBarSection.Right:
      case StatusBarSection.Selection:
        return "status-bar-right-end";
      case StatusBarSection.Left:
      case StatusBarSection.Message:
        return "status-bar-left";
    }
  };
  const getComponent = reactExports.useCallback(
    (item, key, itemPriority, section) => {
      const providerId = isProviderItem(item) ? item.providerId : void 0;
      return /* @__PURE__ */ jsxRuntimeExports.jsx(
        DockedStatusBarEntry,
        {
          entryKey: key,
          getOnResize: handleEntryResize,
          children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
            DockedStatusBarItem,
            {
              itemId: item.id,
              itemPriority,
              providerId,
              section: getSectionName(section),
              children: [
                isStatusBarCustomItem(item) && item.content,
                isStatusBarActionItem(item) && /* @__PURE__ */ jsxRuntimeExports.jsx(StatusBarActionItemComponent, { ...item }),
                isStatusBarLabelItem(item) && /* @__PURE__ */ jsxRuntimeExports.jsx(StatusBarLabelItemComponent, { ...item })
              ]
            },
            key
          )
        },
        key
      );
    },
    [handleEntryResize]
  );
  const getSectionItems = reactExports.useCallback(
    (section) => {
      const sectionItems = statusBarItems.filter(
        (item) => item.section.valueOf() === section.valueOf() && !isItemInOverflow(item.id, overflown) && !ConditionalBooleanValue.getValue(item.isHidden)
      ).sort((a, b) => a.itemPriority - b.itemPriority);
      return sectionItems.map((sectionItem) => /* @__PURE__ */ jsxRuntimeExports.jsx(reactExports.Fragment, { children: getComponent(
        sectionItem,
        sectionItem.id,
        sectionItem.itemPriority,
        sectionItem.section
      ) }, sectionItem.id));
    },
    [statusBarItems, overflown, getComponent]
  );
  const getOverflowItems = reactExports.useCallback(() => {
    const itemsInOverflow = statusBarItems.filter(
      (item) => isItemInOverflow(item.id, overflown) && !ConditionalBooleanValue.getValue(item.isHidden)
    ).sort(
      (a, b) => getCombinedSectionItemPriority(a) - getCombinedSectionItemPriority(b)
    ).reverse();
    return itemsInOverflow.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsx(reactExports.Fragment, { children: getComponent(item, item.id, item.itemPriority, item.section) }, item.id));
  }, [statusBarItems, overflown, getComponent]);
  const handleContainerResize = reactExports.useCallback(
    (w) => {
      if (void 0 === containerWidth.current || Math.abs(containerWidth.current - w) > 2) {
        containerWidth.current = w;
        calculateOverflow();
      }
    },
    [calculateOverflow]
  );
  const containerRef = reactExports.useRef(null);
  const targetRef = reactExports.useRef(null);
  const resizeObserverRef = useResizeObserver(handleContainerResize);
  const refs = useRefs(containerRef, resizeObserverRef);
  const onOverflowClick = reactExports.useCallback(() => {
    setIsOverflowPanelOpen((prev) => !prev);
  }, []);
  const handleOnClose = reactExports.useCallback(() => {
    setIsOverflowPanelOpen(false);
  }, []);
  const leftItems = reactExports.useMemo(
    () => getSectionItems(StatusBarSection.Left),
    [getSectionItems]
  );
  const centerItems = reactExports.useMemo(
    () => getSectionItems(StatusBarSection.Center),
    [getSectionItems]
  );
  const rightItems = reactExports.useMemo(
    () => getSectionItems(StatusBarSection.Right),
    [getSectionItems]
  );
  const contextItems = reactExports.useMemo(
    () => getSectionItems(StatusBarSection.Context),
    [getSectionItems]
  );
  const overflowItems = reactExports.useMemo(
    () => getOverflowItems(),
    [getOverflowItems]
  );
  const containerClassName = classnames("uifw-statusbar-docked", className);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className: containerClassName,
      ref: refs,
      style,
      role: "presentation",
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs(StatusBarSpaceBetween, { className: mainClassName, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(StatusBarLeftSection, { className: leftClassName, children: leftItems }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(StatusBarCenterSection, { className: centerClassName, children: [
          centerItems,
          contextItems
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(StatusBarRightSection, { className: rightClassName, children: [
          rightItems,
          (!overflown || overflown.length > 0) && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              StatusBarOverflow,
              {
                onClick: onOverflowClick,
                onResize: handleOverflowResize,
                ref: targetRef
              }
            ),
            overflowItems.length > 0 && isOverflowPanelOpen && targetRef.current && /* @__PURE__ */ jsxRuntimeExports.jsx(
              StatusBarOverflowPanel,
              {
                onClose: handleOnClose,
                open: true,
                target: targetRef.current,
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "div",
                  {
                    className: "uifw-statusbar-overflow-items-container",
                    "data-testid": "uifw-statusbar-overflow-items-container",
                    children: overflowItems.map((overflowEntry) => overflowEntry)
                  }
                )
              }
            )
          ] })
        ] })
      ] })
    }
  );
}
try {
  nz:
    DockedStatusBarEntryContext.displayName = "nz:DockedStatusBarEntryContext";
  nz:
    DockedStatusBarEntryContext.__docgenInfo = { "description": "Used in [[StatusBarComposer]] component to display a statusbar item.", "displayName": "nz:DockedStatusBarEntryContext", "props": { "children": { "defaultValue": null, "description": "Tool setting content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "itemPriority": { "defaultValue": null, "description": "", "name": "itemPriority", "required": false, "type": { "name": "number" } }, "providerId": { "defaultValue": null, "description": "", "name": "providerId", "required": false, "type": { "name": "string" } }, "section": { "defaultValue": null, "description": "", "name": "section", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class WidgetControl extends ConfigurableUiControl {
  constructor(info, options) {
    super(info, options);
  }
  /** The ReactNode associated with this control */
  get reactNode() {
    return this._reactNode;
  }
  set reactNode(r) {
    this._reactNode = r;
  }
  /** The [[WidgetDef]] associated with this control */
  get widgetDef() {
    return this._widgetDef;
  }
  set widgetDef(w) {
    this._widgetDef = w;
  }
  /** Gets the type of ConfigurableUiControl, which is 'Widget' in this case */
  getType() {
    return ConfigurableUiControlType.Widget;
  }
  /** Sets the [[WidgetState]] for this control */
  setWidgetState(state) {
    this.widgetDef.setWidgetState(state);
  }
  /** Called when widget state changes. */
  onWidgetStateChanged() {
  }
  /** Overwrite to save transient DOM state (i.e. scroll offset). */
  saveTransientState() {
  }
  /** Overwrite to restore transient DOM state.
   * @note Return true if the state is restored or the Widget will remount.
   */
  restoreTransientState() {
    return false;
  }
}
try {
  WidgetControl.displayName = "WidgetControl";
  WidgetControl.__docgenInfo = { "description": "The base class for Widget controls.", "displayName": "WidgetControl", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class StatusBarWidgetControl extends WidgetControl {
  constructor(info, options) {
    super(info, options);
  }
  /** Gets the type of ConfigurableUiControl, which is 'StatusBarWidget' in this case */
  getType() {
    return ConfigurableUiControlType.StatusBarWidget;
  }
}
try {
  StatusBarWidgetControl.displayName = "StatusBarWidgetControl";
  StatusBarWidgetControl.__docgenInfo = { "description": "Status Bar Widget Control.", "displayName": "StatusBarWidgetControl", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const _StatusBarWidgetComposerControl = class _StatusBarWidgetComposerControl extends StatusBarWidgetControl {
  constructor() {
    super(...arguments);
    this.id = _StatusBarWidgetComposerControl.controlId;
  }
  getReactNode() {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      StatusBarComposer,
      {
        items: []
      },
      UiFramework.frontstages.activeFrontstageId
    );
  }
};
_StatusBarWidgetComposerControl.controlId = "uifw:StatusBarWidgetComposerControl";
let StatusBarWidgetComposerControl = _StatusBarWidgetComposerControl;
try {
  StatusBarWidgetComposerControl.displayName = "StatusBarWidgetComposerControl";
  StatusBarWidgetComposerControl.__docgenInfo = { "description": "StatusBarWidgetComposerControl provides status bar to specified [[Frontstage]] that allows status bar items to be populated\nvia UiItemsProviders. See [[StandardStatusbarItemsProvider]] that can be used to populate this status bar with a common\nset of status fields.", "displayName": "StatusBarWidgetComposerControl", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class WidgetStateChangedEvent extends UiEvent {
}
var WidgetType = /* @__PURE__ */ ((WidgetType2) => {
  WidgetType2[WidgetType2["Tool"] = 0] = "Tool";
  WidgetType2[WidgetType2["Navigation"] = 1] = "Navigation";
  WidgetType2[WidgetType2["Rectangular"] = 2] = "Rectangular";
  WidgetType2[WidgetType2["ToolSettings"] = 3] = "ToolSettings";
  WidgetType2[WidgetType2["StatusBar"] = 4] = "StatusBar";
  return WidgetType2;
})(WidgetType || {});
const _WidgetDef = class _WidgetDef {
  constructor() {
    this._label = "";
    this._tooltip = "";
    this._defaultState = WidgetState.Closed;
    this._classId = void 0;
    this._priority = 0;
    this._stateChanged = false;
    this._widgetType = 2;
    _WidgetDef._sId++;
    this._id = `Widget-${_WidgetDef._sId}`;
  }
  get state() {
    const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    const nineZone = frontstageDef == null ? void 0 : frontstageDef.nineZoneState;
    if (!nineZone)
      return this.defaultState;
    if (!frontstageDef.findWidgetDef(this.id))
      return this.defaultState;
    return getWidgetState(this, nineZone);
  }
  get id() {
    return this._id;
  }
  get classId() {
    return this._classId;
  }
  get priority() {
    return this._priority;
  }
  get isFloatingStateSupported() {
    if (!this.initialConfig)
      return true;
    const allowedPanels = this.initialConfig.allowedPanels;
    if (allowedPanels && allowedPanels.length === 0) {
      return true;
    }
    const canFloat = this.initialConfig.canFloat;
    if (canFloat === void 0)
      return true;
    return !!canFloat;
  }
  get isFloatingStateWindowResizable() {
    var _a2;
    const canFloat = (_a2 = this.initialConfig) == null ? void 0 : _a2.canFloat;
    if (typeof canFloat === "object") {
      return canFloat.isResizable === void 0 ? true : canFloat.isResizable;
    }
    return true;
  }
  get isToolSettings() {
    return this._widgetType === 3;
  }
  get isStatusBar() {
    return this._widgetType === 4;
  }
  get stateChanged() {
    return this._stateChanged;
  }
  get applicationData() {
    return this._applicationData;
  }
  get isFloating() {
    return this.state === WidgetState.Floating;
  }
  get iconSpec() {
    return this._icon === IconHelper.reactIconKey ? IconHelper.getIconReactNode(this._icon, this._internalData) : this._icon;
  }
  set iconSpec(spec) {
    this._icon = this._internalData ? IconHelper.getIconData(spec, this._internalData) : spec;
  }
  get badgeType() {
    return this._badge;
  }
  get initialConfig() {
    return this._initialConfig;
  }
  get widgetType() {
    return this._widgetType;
  }
  set widgetType(type) {
    this._widgetType = type;
  }
  /** @internal */
  get defaultFloatingPosition() {
    return this._defaultFloatingPosition;
  }
  set defaultFloatingPosition(position) {
    this._defaultFloatingPosition = position;
  }
  /** @internal */
  get defaultFloatingSize() {
    return this._defaultFloatingSize;
  }
  set defaultFloatingSize(size) {
    this._defaultFloatingSize = size;
  }
  /** @internal */
  get defaultState() {
    return this._defaultState;
  }
  /** @internal */
  static create(config, type = 2) {
    const def = new _WidgetDef();
    def.initializeFromConfig(config, type);
    return def;
  }
  initializeFromConfig(config, type) {
    this._widgetType = type;
    this._initialConfig = config;
    this._id = config.id;
    if (config.label)
      this._label = config.label;
    else if (config.labelKey)
      this._label = IModelApp.localization.getLocalizedString(config.labelKey);
    else if (type === 3)
      this._label = "Tool Settings";
    this.setCanPopout(config.canPopout);
    const canFloat = config.canFloat;
    if (typeof canFloat === "object") {
      this.setFloatingContainerId(canFloat.containerId);
      this.defaultFloatingPosition = canFloat.defaultPosition;
      this._hideWithUiWhenFloating = !!canFloat.hideWithUi;
      this._defaultFloatingSize = canFloat.defaultSize;
    }
    if ("allowedPanels" in config) {
      this.allowedPanelTargets = config.allowedPanels;
    } else if (type === 3)
      this.allowedPanelTargets = [
        StagePanelLocation.Bottom,
        StagePanelLocation.Left,
        StagePanelLocation.Right
      ];
    if (config.priority !== void 0)
      this._priority = config.priority;
    if (config.tooltip)
      this.setTooltip(config.tooltip);
    else if (config.tooltipKey)
      this._tooltip = IModelApp.localization.getLocalizedString(
        config.tooltipKey
      );
    if (config.defaultState !== void 0) {
      this._defaultState = config.defaultState;
    }
    if (config.allowedPanels && config.allowedPanels.length === 0 && config.defaultState === WidgetState.Open) {
      this._defaultState = WidgetState.Floating;
    }
    this._widgetReactNode = config.content;
    this._icon = config.icon;
    if (config.icon !== void 0 && this._icon === void 0)
      this._icon = config.icon;
    if (config.badge !== void 0)
      this._badge = config.badge;
    this._preferredPanelSize = config.preferredPanelSize;
  }
  /** @alpha */
  get preferredPanelSize() {
    return this._preferredPanelSize;
  }
  /** Get the label string */
  get label() {
    return PropsHelper.getStringFromSpec(this._label);
  }
  /** Set the label.
   * @param labelSpec A string or a function to get the string.
   */
  setLabel(labelSpec) {
    this._label = labelSpec;
    const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    if (!frontstageDef)
      return;
    const label = PropsHelper.getStringFromSpec(labelSpec);
    frontstageDef.dispatch({
      type: "WIDGET_TAB_SET_LABEL",
      id: this.id,
      label
    });
  }
  /** Get the tooltip string */
  get tooltip() {
    return PropsHelper.getStringFromSpec(this._tooltip);
  }
  /** Set the tooltip.
   * @param v A string or a function to get the string.
   */
  setTooltip(v) {
    this._tooltip = v;
  }
  get widgetControl() {
    return this._widgetControl;
  }
  getWidgetControl(type) {
    if (!this._widgetControl && this.classId) {
      let usedClassId = "";
      if (typeof this.classId === "string") {
        if (this.classId)
          this._widgetControl = UiFramework.controls.create(
            this.classId,
            this.id,
            this.applicationData
          );
        usedClassId = this.classId;
      } else {
        const info = new ConfigurableCreateInfo(
          this.classId.name,
          this.id,
          this.id
        );
        usedClassId = this.classId.name;
        this._widgetControl = new this.classId(
          info,
          this.applicationData
        );
      }
      if (this._widgetControl) {
        if (this._widgetControl.getType() !== type) {
          throw new UiError(
            UiFramework.loggerCategory(this),
            `getWidgetControl: '${usedClassId}' is NOT a ${type}; it is a ${this._widgetControl.getType()}`
          );
        }
        this._widgetControl.widgetDef = this;
        this._widgetControl.initialize();
      }
    }
    if (!this._widgetControl && this.isStatusBar) {
      const info = new ConfigurableCreateInfo(
        "StatusBarWidgetComposerControl",
        StatusBarWidgetComposerControl.controlId,
        StatusBarWidgetComposerControl.controlId
      );
      this._widgetControl = new StatusBarWidgetComposerControl(info, void 0);
      this._widgetControl.widgetDef = this;
      this._widgetControl.initialize();
    }
    return this._widgetControl;
  }
  get reactNode() {
    if (!this._widgetReactNode) {
      const widgetControl = this.getWidgetControl(
        ConfigurableUiControlType.Widget
      );
      if (widgetControl && widgetControl.reactNode)
        this._widgetReactNode = widgetControl.reactNode;
    }
    return this._widgetReactNode;
  }
  set reactNode(node) {
    this._widgetReactNode = node;
  }
  setWidgetState(newState) {
    const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    const nineZone = frontstageDef == null ? void 0 : frontstageDef.nineZoneState;
    if (!nineZone || this.isStatusBar)
      return;
    if (!frontstageDef.findWidgetDef(this.id))
      return;
    switch (newState) {
      case WidgetState.Closed: {
        frontstageDef.dispatch({
          type: "WIDGET_TAB_CLOSE",
          id: this.id
        });
        break;
      }
      case WidgetState.Floating: {
        frontstageDef.dispatch({
          type: "WIDGET_TAB_FLOAT",
          id: this.id,
          position: this._defaultFloatingPosition
        });
        break;
      }
      case WidgetState.Hidden: {
        frontstageDef.dispatch({
          type: "WIDGET_TAB_HIDE",
          id: this.id
        });
        break;
      }
      case WidgetState.Open: {
        frontstageDef.dispatch({
          type: "WIDGET_TAB_OPEN",
          id: this.id
        });
        break;
      }
      case WidgetState.Unloaded: {
        frontstageDef.dispatch({
          type: "WIDGET_TAB_UNLOAD",
          id: this.id
        });
        break;
      }
    }
  }
  /** @internal */
  handleWidgetStateChanged(newState) {
    this._stateChanged = true;
    UiFramework.frontstages.onWidgetStateChangedEvent.emit({
      widgetDef: this,
      widgetState: newState
    });
    this.onWidgetStateChanged();
  }
  setCanPopout(value) {
    this._canPopout = value;
  }
  get canPopout() {
    return this._canPopout;
  }
  setFloatingContainerId(value) {
    this._floatingContainerId = value;
  }
  get floatingContainerId() {
    return this._floatingContainerId;
  }
  canOpen() {
    return this.isFloating || this.isActive;
  }
  get isVisible() {
    return WidgetState.Hidden !== this.state && WidgetState.Unloaded !== this.state;
  }
  get activeState() {
    return this.state;
  }
  get isActive() {
    return WidgetState.Open === this.activeState;
  }
  set hideWithUiWhenFloating(hide) {
    this._hideWithUiWhenFloating = !!hide;
  }
  get hideWithUiWhenFloating() {
    return !!this._hideWithUiWhenFloating;
  }
  get allowedPanelTargets() {
    return this._allowedPanelTargets;
  }
  set allowedPanelTargets(targets) {
    this._allowedPanelTargets = targets;
  }
  onWidgetStateChanged() {
    this.widgetControl && this.widgetControl.onWidgetStateChanged();
  }
  /** Overwrite to save transient DOM state (i.e. scroll offset). */
  saveTransientState() {
    this.widgetControl && this.widgetControl.saveTransientState();
    this._saveTransientState && this._saveTransientState();
  }
  /** Overwrite to restore transient DOM state.
   * @note Return true if the state is restored or the Widget will remount.
   */
  restoreTransientState() {
    let result = true;
    if (this.widgetControl || this._restoreTransientState) {
      let result1 = false, result2 = false;
      if (this.widgetControl)
        result1 = this.widgetControl.restoreTransientState();
      if (this._restoreTransientState)
        result2 = this._restoreTransientState();
      result = !(result1 || result2);
    }
    return result;
  }
  /** Opens the widget and makes it visible to the user.
   * @note Opens the stage panel if needed.
   * @note Brings the floating widget to the front.
   * @note Brings the window of the popout widget to the front (if allowed by the browser).
   * @public
   */
  show() {
    const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    const nineZone = frontstageDef == null ? void 0 : frontstageDef.nineZoneState;
    if (!nineZone)
      return;
    if (!frontstageDef.findWidgetDef(this.id))
      return;
    const tabLocation = getTabLocation(nineZone, this.id);
    if (tabLocation && isPopoutTabLocation(tabLocation)) {
      const testWindow = UiFramework.childWindows.find(
        tabLocation.popoutWidgetId
      );
      testWindow == null ? void 0 : testWindow.window.focus();
    }
    frontstageDef.dispatch({
      type: "WIDGET_TAB_SHOW",
      id: this.id
    });
  }
  /** Opens the widget and expands it to fill full size of the stage panel.
   * @public
   */
  expand() {
    const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    const nineZone = frontstageDef == null ? void 0 : frontstageDef.nineZoneState;
    if (!nineZone)
      return;
    if (!frontstageDef.findWidgetDef(this.id))
      return;
    frontstageDef.dispatch({
      type: "WIDGET_TAB_EXPAND",
      id: this.id
    });
  }
};
_WidgetDef._sId = 0;
let WidgetDef = _WidgetDef;
function getWidgetState(widgetDef, nineZone) {
  var _a2, _b2, _c2;
  const tab2 = nineZone.tabs[widgetDef.id];
  if (tab2 && tab2.unloaded) {
    return WidgetState.Unloaded;
  }
  if (((_a2 = nineZone.draggedTab) == null ? void 0 : _a2.tabId) === widgetDef.id) {
    return WidgetState.Closed;
  }
  const toolSettingsTabId = (_b2 = nineZone.toolSettings) == null ? void 0 : _b2.tabId;
  if (toolSettingsTabId === widgetDef.id && ((_c2 = nineZone.toolSettings) == null ? void 0 : _c2.type) === "docked") {
    return nineZone.toolSettings.hidden ? WidgetState.Hidden : WidgetState.Open;
  }
  const location = getTabLocation(nineZone, widgetDef.id);
  if (!location) {
    return WidgetState.Hidden;
  }
  if (isFloatingTabLocation(location)) {
    return WidgetState.Floating;
  }
  if (isPanelTabLocation(location)) {
    const panel = nineZone.panels[location.side];
    if (panel.collapsed || void 0 === panel.size || 0 === panel.size)
      return WidgetState.Closed;
  }
  const widget2 = nineZone.widgets[location.widgetId];
  if (widget2.minimized || widgetDef.id !== widget2.activeTabId)
    return WidgetState.Closed;
  return WidgetState.Open;
}
try {
  WidgetStateChangedEvent.displayName = "WidgetStateChangedEvent";
  WidgetStateChangedEvent.__docgenInfo = { "description": "Widget State Changed Event class.", "displayName": "WidgetStateChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  WidgetType.displayName = "WidgetType";
  WidgetType.__docgenInfo = { "description": "Widget type enum.", "displayName": "WidgetType", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  WidgetDef.displayName = "WidgetDef";
  WidgetDef.__docgenInfo = { "description": "A Widget Definition in the 9-Zone Layout system.", "displayName": "WidgetDef", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class WidgetHost {
  /** Constructor for WidgetHost.
   */
  constructor() {
    this._widgetDefs = new Array();
    this._sortedWidgetDefs = [];
  }
  /** Adds a WidgetDef to the list of Widgets.
   * @param widgetDef  Definition of the Widget to add
   */
  addWidgetDef(widgetDef) {
    this._widgetDefs.push(widgetDef);
    this.sortWidgetDefs();
  }
  /** Gets the list of Widgets. */
  get widgetDefs() {
    return this._sortedWidgetDefs;
  }
  /** Gets the number of Widgets. */
  get widgetCount() {
    return this.widgetDefs.length;
  }
  /** If there is only one Widget in the Panel, gets the single WidgetDef.
   * @returns The single WidgetDef if there is only one Widget; otherwise, undefined is returned.
   */
  getSingleWidgetDef() {
    if (this.widgetCount === 1) {
      return this.widgetDefs[0];
    }
    return void 0;
  }
  /** Finds a WidgetDef with a given Id.
   * @param id  Id of the WidgetDef to find
   * @returns The WidgetDef if found; otherwise, undefined is returned.
   */
  findWidgetDef(id) {
    return this.widgetDefs.find((widgetDef) => widgetDef.id === id);
  }
  /** Updates the WidgetHost with dynamic widgets
   * @internal
   */
  updateDynamicWidgetDefs(stageId, stageUsage, location, section, allStageWidgetDefs) {
    const uniqueWidgets = this._widgetDefs.filter((widgetDef) => {
      return !allStageWidgetDefs.find((wDef) => wDef.id === widgetDef.id);
    });
    allStageWidgetDefs.push(...uniqueWidgets);
    let dynamicWidgetDefs;
    if (section !== void 0) {
      dynamicWidgetDefs = UiFramework.widgetManager.getWidgetDefs(
        stageId,
        stageUsage,
        location,
        section
      ) ?? [];
    }
    const uniqueDynamicWidgetDefs = dynamicWidgetDefs == null ? void 0 : dynamicWidgetDefs.filter((widgetDef) => {
      return !allStageWidgetDefs.find((wDef) => wDef.id === widgetDef.id);
    });
    if (uniqueDynamicWidgetDefs) {
      allStageWidgetDefs.push(...uniqueDynamicWidgetDefs);
      this._dynamicWidgetDefs = [...uniqueDynamicWidgetDefs];
    }
    this.sortWidgetDefs();
  }
  /** Sorts all widgets */
  sortWidgetDefs() {
    let sortedWidgetDefs = [];
    if (this._dynamicWidgetDefs)
      sortedWidgetDefs = this._widgetDefs.concat(this._dynamicWidgetDefs);
    else
      sortedWidgetDefs = this._widgetDefs.slice();
    sortedWidgetDefs.sort((a, b) => a.priority - b.priority);
    this._sortedWidgetDefs = sortedWidgetDefs;
  }
}
try {
  WidgetHost.displayName = "WidgetHost";
  WidgetHost.__docgenInfo = { "description": "A WidgetHost represents a definition that hosts one or most Widgets in a Frontstage.", "displayName": "WidgetHost", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
var StagePanelState = /* @__PURE__ */ ((StagePanelState2) => {
  StagePanelState2[StagePanelState2["Off"] = 0] = "Off";
  StagePanelState2[StagePanelState2["Minimized"] = 1] = "Minimized";
  StagePanelState2[StagePanelState2["Open"] = 2] = "Open";
  StagePanelState2[StagePanelState2["Popup"] = 3] = "Popup";
  return StagePanelState2;
})(StagePanelState || {});
try {
  StagePanelState.displayName = "StagePanelState";
  StagePanelState.__docgenInfo = { "description": "Enum for StagePanel state.", "displayName": "StagePanelState", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class PanelStateChangedEvent extends UiEvent {
}
class PanelSizeChangedEvent extends UiEvent {
}
class StagePanelDef extends WidgetHost {
  /** Constructor for PanelDef. */
  constructor() {
    super();
    this._location = StagePanelLocation.Left;
    this._start = new StagePanelSectionDef();
    this._end = new StagePanelSectionDef();
  }
  /** @internal */
  static create(config, location) {
    const def = new StagePanelDef();
    def.initializeFromConfig(config, location);
    return def;
  }
  /** @internal */
  initializeFromConfig(config, location) {
    var _a2, _b2;
    this._location = location;
    this._initialConfig = config;
    this._start.initializeFromConfig((_a2 = config == null ? void 0 : config.sections) == null ? void 0 : _a2.start);
    this._end.initializeFromConfig((_b2 = config == null ? void 0 : config.sections) == null ? void 0 : _b2.end);
  }
  /** @internal */
  get initialConfig() {
    return this._initialConfig;
  }
  /** Current size of the panel.
   * @deprecated in 4.12.x. Use {@link StagePanelDef.sizeSpec} instead.
   */
  get size() {
    var _a2;
    const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    const state = frontstageDef == null ? void 0 : frontstageDef.nineZoneState;
    if (!state)
      return (_a2 = this.initialConfig) == null ? void 0 : _a2.size;
    const side = toPanelSide(this.location);
    const panel = state.panels[side];
    return panel.size;
  }
  set size(size) {
    const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    if (!frontstageDef)
      return;
    const side = toPanelSide(this.location);
    frontstageDef.dispatch({
      type: "PANEL_SET_SIZE",
      side,
      size
    });
  }
  /** Current size of the panel */
  get sizeSpec() {
    var _a2;
    const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    const state = frontstageDef == null ? void 0 : frontstageDef.nineZoneState;
    if (!state)
      return (_a2 = this.initialConfig) == null ? void 0 : _a2.sizeSpec;
    const side = toPanelSide(this.location);
    const panel = state.panels[side];
    return panel.size;
  }
  set sizeSpec(size) {
    const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    if (!frontstageDef)
      return;
    const side = toPanelSide(this.location);
    frontstageDef.dispatch({
      type: "PANEL_SET_SIZE",
      side,
      size
    });
  }
  /** @internal */
  handleSizeChanged(size) {
    InternalFrontstageManager.onPanelSizeChangedEvent.emit({
      panelDef: this,
      size
    });
  }
  /** Indicates whether the panel is resizable. */
  get resizable() {
    return this.defaultResizable;
  }
  /** Indicates whether the panel is pinned. */
  get pinned() {
    const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    const state = frontstageDef == null ? void 0 : frontstageDef.nineZoneState;
    if (!state)
      return this.defaultPinned;
    const side = toPanelSide(this.location);
    const panel = state.panels[side];
    return panel.pinned;
  }
  set pinned(pinned) {
    const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    if (!frontstageDef)
      return;
    const side = toPanelSide(this.location);
    frontstageDef.dispatch({
      type: "PANEL_SET_PINNED",
      side,
      pinned
    });
  }
  /** @internal */
  handlePinnedChanged(pinned) {
    UiFramework.frontstages.onPanelPinnedChangedEvent.emit({
      panelDef: this,
      pinned
    });
  }
  /** Location of panel. */
  get location() {
    return this._location;
  }
  /** Panel state. Defaults to PanelState.Open. */
  get panelState() {
    const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    const state = frontstageDef == null ? void 0 : frontstageDef.nineZoneState;
    if (!state)
      return this.defaultState;
    const side = toPanelSide(this.location);
    return getPanelState(state, side);
  }
  set panelState(panelState) {
    const frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    if (!frontstageDef)
      return;
    const side = toPanelSide(this.location);
    const collapsed = panelState === StagePanelState.Open ? false : true;
    frontstageDef.dispatch({
      type: "PANEL_SET_COLLAPSED",
      collapsed,
      side
    });
  }
  /** @internal */
  handlePanelStateChanged(panelState) {
    UiFramework.frontstages.onPanelStateChangedEvent.emit({
      panelDef: this,
      panelState
    });
  }
  /** @internal */
  get defaultState() {
    var _a2;
    const defaultState = (_a2 = this._initialConfig) == null ? void 0 : _a2.defaultState;
    return defaultState ?? StagePanelState.Open;
  }
  /** @internal */
  get defaultPinned() {
    var _a2;
    return ((_a2 = this._initialConfig) == null ? void 0 : _a2.pinned) ?? true;
  }
  /** @internal */
  get defaultResizable() {
    var _a2;
    return ((_a2 = this._initialConfig) == null ? void 0 : _a2.resizable) ?? true;
  }
  /** Gets the list of Widgets. */
  get widgetDefs() {
    return [
      ...super.widgetDefs,
      ...this._start.widgetDefs,
      ...this._end.widgetDefs
    ];
  }
  /** @internal */
  updateDynamicWidgetDefs(stageId, stageUsage, location, _section, allStageWidgetDefs) {
    this._start.updateDynamicWidgetDefs(
      stageId,
      stageUsage,
      location,
      StagePanelSection.Start,
      allStageWidgetDefs
    );
    this._end.updateDynamicWidgetDefs(
      stageId,
      stageUsage,
      location,
      StagePanelSection.End,
      allStageWidgetDefs
    );
  }
  /** @internal */
  getPanelSectionDef(section) {
    switch (section) {
      case StagePanelSection.Start: {
        return this._start;
      }
      case StagePanelSection.End: {
        return this._end;
      }
    }
  }
}
class StagePanelSectionDef extends WidgetHost {
  /** @internal */
  initializeFromConfig(config) {
    config == null ? void 0 : config.forEach((widgetConfig) => {
      const widgetDef = WidgetDef.create(widgetConfig);
      this.addWidgetDef(widgetDef);
    });
  }
}
function toPanelSide(location) {
  switch (location) {
    case StagePanelLocation.Bottom:
      return "bottom";
    case StagePanelLocation.Left:
      return "left";
    case StagePanelLocation.Right:
      return "right";
    case StagePanelLocation.Top:
      return "top";
  }
}
fn(
  (nineZone, side, pinned) => {
    const panel = nineZone.panels[side];
    panel.pinned = pinned;
  }
);
function getPanelState(state, side) {
  const panel = state.panels[side];
  return panel.collapsed ? StagePanelState.Minimized : StagePanelState.Open;
}
try {
  toPanelSide.displayName = "toPanelSide";
  toPanelSide.__docgenInfo = { "description": "", "displayName": "toPanelSide", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  PanelStateChangedEvent.displayName = "PanelStateChangedEvent";
  PanelStateChangedEvent.__docgenInfo = { "description": "Panel state changed event class.", "displayName": "PanelStateChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  StagePanelDef.displayName = "StagePanelDef";
  StagePanelDef.__docgenInfo = { "description": "A StagePanelDef represents each Stage Panel within a Frontstage.", "displayName": "StagePanelDef", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class ToolInformation {
  constructor(toolId) {
    this.toolId = toolId;
  }
  /** Get the ToolUiProvider registered for this tool */
  get toolUiProvider() {
    if (!this._toolUiProvider) {
      let provider;
      if (UiFramework.controls.isRegistered(this.toolId)) {
        provider = UiFramework.controls.create(
          this.toolId,
          this.toolId
        );
      } else {
        if (UiFramework.toolSettings.useDefaultToolSettingsProvider && this.toolId === UiFramework.toolSettings.toolIdForToolSettings)
          provider = UiFramework.controls.create(
            "DefaultToolSettings",
            this.toolId
          );
      }
      if (provider) {
        if (provider.getType() !== ConfigurableUiControlType.ToolUiProvider) {
          throw new UiError(
            UiFramework.loggerCategory(this),
            `toolUiProvider: toolId '${this.toolId}' is registered to a control that is NOT a ToolUiProvider`
          );
        }
        this._toolUiProvider = provider;
      }
    }
    return this._toolUiProvider;
  }
}
class FrontstageActivatedEvent extends UiEvent {
}
class FrontstageDeactivatedEvent extends UiEvent {
}
class FrontstageReadyEvent extends UiEvent {
}
class ModalFrontstageChangedEvent extends UiEvent {
}
class ModalFrontstageRequestedCloseEvent extends UiEvent {
}
class ModalFrontstageClosedEvent extends UiEvent {
}
class ToolActivatedEvent extends UiEvent {
}
class ToolIconChangedEvent extends UiEvent {
}
const _InternalFrontstageManager = class _InternalFrontstageManager {
  /** This should only be called within InternalFrontstageManager and its tests.
   *  @internal
   */
  static ensureToolInformationIsSet(toolId) {
    if (!_InternalFrontstageManager._toolInformationMap.get(toolId))
      _InternalFrontstageManager._toolInformationMap.set(
        toolId,
        new ToolInformation(toolId)
      );
  }
  // pass on SyncToolSettingsPropertiesEvent from ToolAdmin so they are treated as DialogItemSync events
  static handleSyncToolSettingsPropertiesEvent(args) {
    _InternalFrontstageManager.activeToolSettingsProvider && _InternalFrontstageManager.activeToolSettingsProvider.syncToolSettingsProperties(
      args
    );
  }
  // pass on ReloadToolSettingsEvent from ToolAdmin so they are treated by UiProviders
  static handleReloadToolSettingsEvent() {
    if (_InternalFrontstageManager.activeToolSettingsProvider) {
      _InternalFrontstageManager.activeToolSettingsProvider.reloadPropertiesFromTool();
    }
  }
  /** Initializes the InternalFrontstageManager
   * @internal
   */
  static initialize() {
    if (this._initialized)
      return;
    if (IModelApp && IModelApp.toolAdmin) {
      IModelApp.toolAdmin.activeToolChanged.addListener(
        (tool, _start) => {
          UiFramework.toolSettings.clearToolSettingsData();
          if (tool instanceof InteractiveTool)
            UiFramework.toolSettings.initializeDataForTool(tool);
          _InternalFrontstageManager.ensureToolInformationIsSet(tool.toolId);
          _InternalFrontstageManager.setActiveTool(tool);
        }
      );
      UiFramework.toolSettings.onSyncToolSettingsProperties.addListener(
        _InternalFrontstageManager.handleSyncToolSettingsPropertiesEvent
      );
      UiFramework.toolSettings.onReloadToolSettingsProperties.addListener(
        _InternalFrontstageManager.handleReloadToolSettingsEvent
      );
    }
    if (IModelApp && IModelApp.viewManager) {
      IModelApp.viewManager.onSelectedViewportChanged.addListener(
        _InternalFrontstageManager._handleSelectedViewportChanged
      );
    }
    this._initialized = true;
  }
  /** @internal */
  static get isInitialized() {
    return _InternalFrontstageManager._initialized;
  }
  static set isInitialized(v) {
    _InternalFrontstageManager._initialized = v;
  }
  /** Returns true if Frontstage is loading its controls. If false the Frontstage content and controls have been created. */
  static get isLoading() {
    return _InternalFrontstageManager._isLoading;
  }
  /** @internal */
  static get nineZoneSize() {
    return _InternalFrontstageManager._nineZoneSize;
  }
  static set nineZoneSize(size) {
    _InternalFrontstageManager._nineZoneSize = size;
  }
  /** @internal */
  static get frontstageDefs() {
    return this._frontstageDefs;
  }
  /** Clears the Frontstage map.
   */
  static clearFrontstageDefs() {
    _InternalFrontstageManager._frontstageDefs.clear();
    _InternalFrontstageManager._activeFrontstageDef = void 0;
  }
  /** Clears the Frontstage Providers and the defs that may have been created from them.
   */
  static clearFrontstageProviders() {
    _InternalFrontstageManager._frontstageProviders.clear();
    _InternalFrontstageManager.clearFrontstageDefs();
  }
  static getFrontstageKey(frontstageId) {
    return frontstageId;
  }
  /** @internal */
  static clearFrontstageDefsForIModelId(iModelId) {
    if (!iModelId)
      return;
    const keysToRemove = [];
    _InternalFrontstageManager._frontstageDefs.forEach(
      (_, key) => {
        if (key.startsWith(`[${iModelId}]`))
          keysToRemove.push(key);
      }
    );
    keysToRemove.forEach((keyValue) => {
      _InternalFrontstageManager._frontstageDefs.delete(keyValue);
    });
  }
  /** Add a Frontstage via a [[FrontstageProvider]].
   * @param frontstageProvider  FrontstageProvider representing the Frontstage to add
   */
  static addFrontstageProvider(frontstageProvider) {
    const key = _InternalFrontstageManager.getFrontstageKey(
      frontstageProvider.id
    );
    key && _InternalFrontstageManager._frontstageDefs.delete(key);
    _InternalFrontstageManager._frontstageProviders.set(
      frontstageProvider.id,
      frontstageProvider
    );
  }
  /** Find a loaded Frontstage with a given id. If the id is not provided, the active Frontstage is returned.
   * If the FrontstageDef has not been cached by FrontstageDef then it will not be found. See async function
   * `getFrontstageDef` to get a FrontstageDef.
   * @param id  Id of the Frontstage to find
   * @returns  FrontstageDef with a given id if found, or undefined if not found.
   */
  static findFrontstageDef(id) {
    const key = _InternalFrontstageManager.getFrontstageKey(id);
    const frontstageDef = _InternalFrontstageManager._frontstageDefs.get(key);
    if (frontstageDef instanceof FrontstageDef)
      return frontstageDef;
    return void 0;
  }
  static findFrontstageProvider(id) {
    return id ? _InternalFrontstageManager._frontstageProviders.get(id) : void 0;
  }
  /** Find a loaded Frontstage with a given id. If the id is not provided, the active Frontstage is returned. If
   * no cached FrontstageDef is found but a FrontstageProvider is registered a FrontstageDef will be created, cached, and
   * returned.
   * @param id  Id of the Frontstage to find
   * @returns  FrontstageDef with a given id if found, or undefined if not found.
   */
  static async getFrontstageDef(id) {
    if (!id)
      return _InternalFrontstageManager.activeFrontstageDef;
    let frontstageDef = _InternalFrontstageManager.findFrontstageDef(id);
    if (frontstageDef)
      return frontstageDef;
    if (id) {
      const frontstageProvider = _InternalFrontstageManager.findFrontstageProvider(id);
      if (frontstageProvider) {
        frontstageDef = await FrontstageDef.create(frontstageProvider);
        if (frontstageDef) {
          const key = _InternalFrontstageManager.getFrontstageKey(
            frontstageDef.id
          );
          _InternalFrontstageManager._frontstageDefs.set(key, frontstageDef);
        }
        return frontstageDef;
      }
    }
    return void 0;
  }
  /** Gets the active FrontstageDef. If a Frontstage is not active, undefined is returned.
   * @return  Active FrontstageDef, or undefined if one is not active.
   */
  static get activeFrontstageDef() {
    return _InternalFrontstageManager._activeFrontstageDef;
  }
  /** Gets the Id of the active FrontstageDef. If a Frontstage is not active, blank is returned.
   * @return  Id of the active FrontstageDef, or blank if one is not active.
   */
  static get activeFrontstageId() {
    const activeFrontstage = _InternalFrontstageManager._activeFrontstageDef;
    return activeFrontstage ? activeFrontstage.id : "";
  }
  static hasFrontstage(frontstageId) {
    if (_InternalFrontstageManager.findFrontstageDef(frontstageId))
      return true;
    if (_InternalFrontstageManager.findFrontstageProvider(frontstageId))
      return true;
    return false;
  }
  /** Sets the active FrontstageDef give the stageId.
   * @param  frontstageId  Id of the Frontstage to set active.
   * @returns A Promise that is fulfilled when the [[Frontstage]] is ready.
   */
  static async setActiveFrontstage(frontstageId) {
    const frontstageDef = await _InternalFrontstageManager.getFrontstageDef(
      frontstageId
    );
    if (!frontstageDef) {
      Logger.logError(
        UiFramework.loggerCategory(this),
        `setActiveFrontstage: Could not load a FrontstageDef with id of '${frontstageId}'`
      );
      return;
    }
    return _InternalFrontstageManager.setActiveFrontstageDef(frontstageDef);
  }
  /** Sets the active FrontstageDef.
   * @param  frontstageDef  FrontstageDef to set active.
   * @returns A Promise that is fulfilled when the [[FrontstageDef]] is ready.
   */
  static async setActiveFrontstageDef(frontstageDef) {
    if (_InternalFrontstageManager._activeFrontstageDef === frontstageDef)
      return;
    _InternalFrontstageManager._isLoading = true;
    const deactivatedFrontstageDef = _InternalFrontstageManager._activeFrontstageDef;
    if (deactivatedFrontstageDef) {
      await deactivatedFrontstageDef.onDeactivated();
      const timeTracker = deactivatedFrontstageDef.timeTracker;
      _InternalFrontstageManager.onFrontstageDeactivatedEvent.emit({
        deactivatedFrontstageDef,
        activatedFrontstageDef: frontstageDef,
        totalTime: timeTracker.getTotalTimeSeconds(),
        engagementTime: timeTracker.getEngagementTimeSeconds(),
        idleTime: timeTracker.getIdleTimeSeconds()
      });
    }
    _InternalFrontstageManager._activeFrontstageDef = frontstageDef;
    if (frontstageDef) {
      await frontstageDef.onActivated();
      _InternalFrontstageManager.onFrontstageActivatedEvent.emit({
        activatedFrontstageDef: frontstageDef,
        deactivatedFrontstageDef
      });
      await frontstageDef.waitUntilReady();
      _InternalFrontstageManager._isLoading = false;
      frontstageDef.onFrontstageReady();
      _InternalFrontstageManager.onFrontstageReadyEvent.emit({ frontstageDef });
      UiFramework.visibility.handleFrontstageReady();
      await frontstageDef.setActiveContent();
    }
    _InternalFrontstageManager._isLoading = false;
  }
  /** Deactivates the active FrontstageDef.
   */
  static async deactivateFrontstageDef() {
    await this.setActiveFrontstageDef(void 0);
  }
  /** Gets the Id of the active tool. If a tool is not active, blank is returned.
   * @return  Id of the active tool, or blank if one is not active.
   */
  static get activeToolId() {
    return _InternalFrontstageManager._activeToolId;
  }
  /** Sets the active tool id */
  static setActiveToolId(toolId) {
    _InternalFrontstageManager._activeToolId = toolId;
    const toolSettingsProvider = _InternalFrontstageManager.activeToolSettingsProvider;
    if (toolSettingsProvider)
      toolSettingsProvider.initialize();
    _InternalFrontstageManager.onToolActivatedEvent.emit({ toolId });
  }
  /** Sets the active tool */
  static setActiveTool(tool) {
    _InternalFrontstageManager.setActiveToolId(tool.toolId);
    _InternalFrontstageManager.onToolIconChangedEvent.emit({
      iconSpec: tool.iconSpec
    });
  }
  /** Gets the active tool's [[ToolInformation]] */
  static get activeToolInformation() {
    return _InternalFrontstageManager._toolInformationMap.get(
      _InternalFrontstageManager.activeToolId
    );
  }
  /** Gets the Tool Setting React node of the active tool.
   * @return  Tool Setting React node of the active tool, or undefined if there is no active tool or Tool Settings for the active tool.
   * @internal
   */
  static get activeToolSettingsProvider() {
    const activeToolInformation = _InternalFrontstageManager.activeToolInformation;
    return activeToolInformation == null ? void 0 : activeToolInformation.toolUiProvider;
  }
  /** Sets the active layout, content group and active content.
   * @param contentLayoutDef  Content layout to make active
   * @param contentGroup  Content Group to make active
   */
  static async setActiveLayout(contentLayoutDef, contentGroup) {
    const activeFrontstageDef = _InternalFrontstageManager.activeFrontstageDef;
    if (activeFrontstageDef) {
      _InternalFrontstageManager._isLoading = false;
      activeFrontstageDef.setContentLayoutAndGroup(
        contentLayoutDef,
        contentGroup
      );
      _InternalFrontstageManager.onContentLayoutActivatedEvent.emit({
        contentLayout: contentLayoutDef,
        contentGroup
      });
      await activeFrontstageDef.waitUntilReady();
      _InternalFrontstageManager._isLoading = false;
      await activeFrontstageDef.setActiveContent();
    }
  }
  /** Sets the active layout, content group and active content.
   * @param contentGroup  Content Group to make active
   */
  static async setActiveContentGroup(contentGroup) {
    const contentLayoutDef = UiFramework.content.layouts.getForGroup(contentGroup);
    await _InternalFrontstageManager.setActiveLayout(
      contentLayoutDef,
      contentGroup
    );
  }
  /** Opens a modal Frontstage. Modal Frontstages can be stacked.
   * @param modalFrontstage  Information about the modal Frontstage
   */
  static openModalFrontstage(modalFrontstage2) {
    _InternalFrontstageManager.pushModalFrontstage(modalFrontstage2);
  }
  static pushModalFrontstage(modalFrontstage2) {
    const timeTracker = new TimeTracker();
    timeTracker.startTiming();
    const frontstageItem = {
      modalFrontstage: modalFrontstage2,
      timeTracker
    };
    _InternalFrontstageManager._modalFrontstages.push(frontstageItem);
    _InternalFrontstageManager.emitModalFrontstageChangedEvent();
  }
  /** Closes the top-most modal Frontstage.
   */
  static closeModalFrontstage() {
    if (_InternalFrontstageManager._modalFrontstages.length > 0) {
      const topMostStageItem = _InternalFrontstageManager._modalFrontstages[_InternalFrontstageManager._modalFrontstages.length - 1];
      if (topMostStageItem.modalFrontstage.notifyCloseRequest)
        _InternalFrontstageManager.onCloseModalFrontstageRequestedEvent.emit({
          modalFrontstage: topMostStageItem.modalFrontstage,
          stageCloseFunc: _InternalFrontstageManager.popModalFrontstage
        });
      else
        _InternalFrontstageManager.popModalFrontstage();
    }
  }
  static popModalFrontstage() {
    const frontstageItem = _InternalFrontstageManager._modalFrontstages.pop();
    if (frontstageItem) {
      const modalFrontstage2 = frontstageItem.modalFrontstage;
      const timeTracker = frontstageItem.timeTracker;
      timeTracker.stopTiming();
      _InternalFrontstageManager.onModalFrontstageClosedEvent.emit({
        modalFrontstage: modalFrontstage2,
        totalTime: timeTracker.getTotalTimeSeconds(),
        engagementTime: timeTracker.getEngagementTimeSeconds(),
        idleTime: timeTracker.getIdleTimeSeconds()
      });
    }
    _InternalFrontstageManager.emitModalFrontstageChangedEvent();
    UiFramework.visibility.handleFrontstageReady();
  }
  static emitModalFrontstageChangedEvent() {
    _InternalFrontstageManager.onModalFrontstageChangedEvent.emit({
      modalFrontstageCount: _InternalFrontstageManager.modalFrontstageCount
    });
  }
  /** Updates the top-most modal Frontstage.
   */
  static updateModalFrontstage() {
    _InternalFrontstageManager.emitModalFrontstageChangedEvent();
  }
  /** Gets the top-most modal Frontstage.
   * @returns Top-most modal Frontstage, or undefined if there is none.
   */
  static get activeModalFrontstage() {
    if (_InternalFrontstageManager._modalFrontstages.length > 0) {
      const frontstageItem = _InternalFrontstageManager._modalFrontstages[_InternalFrontstageManager._modalFrontstages.length - 1];
      const modalFrontstage2 = frontstageItem.modalFrontstage;
      return modalFrontstage2;
    } else {
      return void 0;
    }
  }
  /** Gets the number of modal Frontstages.
   * @returns Modal Frontstage count
   */
  static get modalFrontstageCount() {
    return _InternalFrontstageManager._modalFrontstages.length;
  }
  /** Sets the active Navigation Aid via its Id.
   * @param navigationAidId  Id of the Navigation Aid to set as active
   * @param iModelConnection IModelConnection to query for view data
   */
  static setActiveNavigationAid(navigationAidId, iModelConnection) {
    _InternalFrontstageManager.onNavigationAidActivatedEvent.emit({
      navigationAidId,
      iModelConnection
    });
  }
  /** Sets the state of the widget with the given id
   * @param widgetId  Id of the Widget for which to set the state
   * @param state     New state of the widget
   * @returns true if the widget state was set successfully, or false if not.
   */
  static setWidgetState(widgetId, state) {
    const widgetDef = _InternalFrontstageManager.findWidget(widgetId);
    if (widgetDef) {
      widgetDef.setWidgetState(state);
      return true;
    } else {
      Logger.logError(
        UiFramework.loggerCategory(this),
        `setWidgetState: Could not find Widget with id of '${widgetId}'`
      );
    }
    return false;
  }
  /** Finds a widget with the given id in the active frontstage
   * @param widgetId  Id of the Widget to find
   * @returns The WidgetDef with the given id, or undefined if not found.
   */
  static findWidget(widgetId) {
    const activeFrontstageDef = _InternalFrontstageManager.activeFrontstageDef;
    if (activeFrontstageDef)
      return activeFrontstageDef.findWidgetDef(widgetId);
    return void 0;
  }
  /** Opens a nested Frontstage. Nested Frontstages can be stacked.
   * @param nestedFrontstage  Information about the nested Frontstage
   */
  static async openNestedFrontstage(nestedFrontstage) {
    if (_InternalFrontstageManager.nestedFrontstageCount === 0)
      _InternalFrontstageManager._activePrimaryFrontstageDef = _InternalFrontstageManager._activeFrontstageDef;
    _InternalFrontstageManager.pushNestedFrontstage(nestedFrontstage);
    await _InternalFrontstageManager.setActiveFrontstageDef(nestedFrontstage);
  }
  static pushNestedFrontstage(nestedFrontstage) {
    _InternalFrontstageManager._nestedFrontstages.push(nestedFrontstage);
  }
  /** Closes the top-most nested Frontstage.
   */
  static async closeNestedFrontstage() {
    _InternalFrontstageManager.popNestedFrontstage();
    if (_InternalFrontstageManager.nestedFrontstageCount > 0) {
      await _InternalFrontstageManager.setActiveFrontstageDef(
        _InternalFrontstageManager.activeNestedFrontstage
      );
    } else {
      await _InternalFrontstageManager.setActiveFrontstageDef(
        _InternalFrontstageManager._activePrimaryFrontstageDef
      );
      _InternalFrontstageManager._activePrimaryFrontstageDef = void 0;
    }
  }
  static popNestedFrontstage() {
    _InternalFrontstageManager._nestedFrontstages.pop();
  }
  /** Gets the top-most nested Frontstage.
   * @returns Top-most nested Frontstage, or undefined if there is none.
   */
  static get activeNestedFrontstage() {
    if (_InternalFrontstageManager._nestedFrontstages.length > 0)
      return _InternalFrontstageManager._nestedFrontstages[_InternalFrontstageManager._nestedFrontstages.length - 1];
    return void 0;
  }
  /** Gets the number of nested Frontstages.
   * @returns Nested Frontstage count
   */
  static get nestedFrontstageCount() {
    return _InternalFrontstageManager._nestedFrontstages.length;
  }
};
_InternalFrontstageManager._initialized = false;
_InternalFrontstageManager._isLoading = false;
_InternalFrontstageManager._activeToolId = "";
_InternalFrontstageManager._frontstageDefs = /* @__PURE__ */ new Map();
_InternalFrontstageManager._modalFrontstages = new Array();
_InternalFrontstageManager._frontstageProviders = /* @__PURE__ */ new Map();
_InternalFrontstageManager._nineZoneSize = void 0;
_InternalFrontstageManager._nestedFrontstages = new Array();
_InternalFrontstageManager._toolInformationMap = /* @__PURE__ */ new Map();
_InternalFrontstageManager._handleSelectedViewportChanged = (args) => {
  if (args.current && _InternalFrontstageManager.activeFrontstageDef && !_InternalFrontstageManager.isLoading) {
    _InternalFrontstageManager.activeFrontstageDef.setActiveViewFromViewport(
      args.current
    );
  }
};
_InternalFrontstageManager.onFrontstageDeactivatedEvent = new FrontstageDeactivatedEvent();
_InternalFrontstageManager.onFrontstageActivatedEvent = new FrontstageActivatedEvent();
_InternalFrontstageManager.onFrontstageReadyEvent = new FrontstageReadyEvent();
_InternalFrontstageManager.onModalFrontstageChangedEvent = new ModalFrontstageChangedEvent();
_InternalFrontstageManager.onModalFrontstageClosedEvent = new ModalFrontstageClosedEvent();
_InternalFrontstageManager.onCloseModalFrontstageRequestedEvent = new ModalFrontstageRequestedCloseEvent();
_InternalFrontstageManager.onToolActivatedEvent = new ToolActivatedEvent();
_InternalFrontstageManager.onToolSettingsReloadEvent = new UiEvent();
_InternalFrontstageManager.onToolPanelOpenedEvent = new UiEvent();
_InternalFrontstageManager.onToolIconChangedEvent = new ToolIconChangedEvent();
_InternalFrontstageManager.onContentLayoutActivatedEvent = new ContentLayoutActivatedEvent();
_InternalFrontstageManager.onContentControlActivatedEvent = new ContentControlActivatedEvent();
_InternalFrontstageManager.onNavigationAidActivatedEvent = new NavigationAidActivatedEvent();
_InternalFrontstageManager.onWidgetStateChangedEvent = new WidgetStateChangedEvent();
_InternalFrontstageManager.onWidgetDefsUpdatedEvent = new UiEvent();
_InternalFrontstageManager.onFrontstageNineZoneStateChangedEvent = // eslint-disable-next-line deprecation/deprecation
new UiEvent();
_InternalFrontstageManager.onFrontstageRestoreLayoutEvent = // eslint-disable-next-line deprecation/deprecation
new UiEvent();
_InternalFrontstageManager.onFrontstageWidgetsChangedEvent = // eslint-disable-next-line deprecation/deprecation
new UiEvent();
_InternalFrontstageManager.onPanelStateChangedEvent = new PanelStateChangedEvent();
_InternalFrontstageManager.onPanelPinnedChangedEvent = // eslint-disable-next-line deprecation/deprecation
new UiEvent();
_InternalFrontstageManager.onPanelSizeChangedEvent = new PanelSizeChangedEvent();
let InternalFrontstageManager = _InternalFrontstageManager;
class SyncToolSettingsPropertiesEvent extends UiEvent {
}
const _InternalToolSettingsManager = class _InternalToolSettingsManager {
  // istanbul ignore next
  static syncToolSettingsProperties(toolId, syncProperties) {
    _InternalToolSettingsManager.onSyncToolSettingsProperties.emit({
      toolId,
      syncProperties
    });
  }
  // istanbul ignore next
  static reloadToolSettingsProperties() {
    _InternalToolSettingsManager.onReloadToolSettingsProperties.emit();
  }
  static dispatchSyncUiEvent(syncEventId, useImmediateDispatch) {
    if (useImmediateDispatch)
      SyncUiEventDispatcher.dispatchImmediateSyncUiEvent(syncEventId);
    else
      SyncUiEventDispatcher.dispatchSyncUiEvent(syncEventId);
  }
  /** Initializes the ToolSettingsManager
   * @internal
   */
  static initialize() {
    if (IModelApp && IModelApp.toolAdmin) {
      IModelApp.toolAdmin.toolSettingsChangeHandler = _InternalToolSettingsManager.syncToolSettingsProperties;
      IModelApp.toolAdmin.reloadToolSettingsHandler = _InternalToolSettingsManager.reloadToolSettingsProperties;
      IModelApp.toolAdmin.toolSyncUiEventDispatcher = _InternalToolSettingsManager.dispatchSyncUiEvent;
    }
  }
  /** clear cached Tool Settings properties. */
  static clearToolSettingsData() {
    _InternalToolSettingsManager.useDefaultToolSettingsProvider = false;
    _InternalToolSettingsManager._activeToolLabel = "";
    _InternalToolSettingsManager._activeToolDescription = "";
    _InternalToolSettingsManager._toolIdForToolSettings = "";
  }
  /** Cache Tool Settings properties */
  static initializeToolSettingsData(toolSettingsProperties, toolId, toolLabel, toolDescription) {
    _InternalToolSettingsManager.clearToolSettingsData();
    if (toolLabel)
      _InternalToolSettingsManager._activeToolLabel = toolLabel;
    if (toolDescription)
      _InternalToolSettingsManager._activeToolDescription = toolDescription;
    if (toolSettingsProperties && toolSettingsProperties.length > 0) {
      if (toolId)
        _InternalToolSettingsManager._toolIdForToolSettings = toolId;
      _InternalToolSettingsManager._useDefaultToolSettingsProvider = true;
      return true;
    }
    return false;
  }
  /** Set of data used in Tool Settings for the specified tool. The tool specified should be the "active" tool.
   */
  static initializeDataForTool(tool) {
    _InternalToolSettingsManager.initializeToolSettingsData(
      tool.supplyToolSettingsProperties(),
      tool.toolId,
      tool.flyover,
      tool.description
    );
  }
  /** Returns the toolSettings properties that can be used to populate the tool settings widget. */
  static get toolSettingsProperties() {
    if (IModelApp.toolAdmin && IModelApp.toolAdmin.activeTool && IModelApp.toolAdmin.activeTool.toolId === _InternalToolSettingsManager._toolIdForToolSettings) {
      const properties = IModelApp.toolAdmin.activeTool.supplyToolSettingsProperties();
      if (properties)
        return properties;
    }
    return [];
  }
  /** Returns true if the Tool Settings are to be auto populated from the toolSettingsProperties.
   * The setter is chiefly for testing.
   */
  static get useDefaultToolSettingsProvider() {
    return _InternalToolSettingsManager._useDefaultToolSettingsProvider;
  }
  static set useDefaultToolSettingsProvider(useDefaultToolSettings) {
    _InternalToolSettingsManager._useDefaultToolSettingsProvider = useDefaultToolSettings;
  }
  /** The name of the active tool. This is typically the flyover text specified for the tool. */
  static get activeToolLabel() {
    return _InternalToolSettingsManager._activeToolLabel;
  }
  static set activeToolLabel(label) {
    _InternalToolSettingsManager._activeToolLabel = label;
  }
  /** Returns the description of the active tool. */
  static get activeToolDescription() {
    return _InternalToolSettingsManager._activeToolDescription;
  }
  /** Gets the Id of the active tool. If a tool is not active, blank is returned.
   * @return  Id of the active tool, or blank if one is not active.
   */
  static get toolIdForToolSettings() {
    return _InternalToolSettingsManager._toolIdForToolSettings;
  }
  static focusIntoToolSettings() {
    let divElement = document.querySelector("div.nz-toolSettings-docked");
    if (divElement) {
      if (focusIntoContainer(divElement))
        return true;
    }
    divElement = document.querySelector(
      "div.uifw-tool-settings-grid-container"
    );
    if (divElement) {
      if (focusIntoContainer(divElement))
        return true;
    }
    return false;
  }
};
_InternalToolSettingsManager._useDefaultToolSettingsProvider = false;
_InternalToolSettingsManager._toolIdForToolSettings = "";
_InternalToolSettingsManager._activeToolLabel = "";
_InternalToolSettingsManager._activeToolDescription = "";
_InternalToolSettingsManager.onSyncToolSettingsProperties = new SyncToolSettingsPropertiesEvent();
_InternalToolSettingsManager.onReloadToolSettingsProperties = new UiEvent();
let InternalToolSettingsManager = _InternalToolSettingsManager;
class MouseDownChangedEvent extends UiEvent {
}
class ActiveContentChangedEvent extends UiEvent {
}
class ContentDialogChangedEvent extends DialogChangedEvent {
}
const CONTENT_DIALOG_ZINDEX_DEFAULT = 2e3;
const _InternalContentDialogManager = class _InternalContentDialogManager {
  /** Get the array of modeless dialogs */
  static get dialogs() {
    return _InternalContentDialogManager.dialogManager.dialogs;
  }
  /** Initialize the modeless dialog manager.
   * @internal
   */
  static initialize() {
    _InternalContentDialogManager._topZIndex = _InternalContentDialogManager.getDialogZIndexDefault();
  }
  static getDialogZIndexDefault() {
    const variable = "--uicore-z-index-view-content-dialog";
    const value = getCssVariableAsNumber(variable);
    if (!isNaN(value))
      return value;
    Logger.logError(
      UiFramework.loggerCategory(this),
      `'${variable}' CSS variable not found`
    );
    return CONTENT_DIALOG_ZINDEX_DEFAULT;
  }
  /** Open a modeless dialog
   * @param dialog The Dialog to open
   * @param id The id of the Dialog to open
   */
  static open(dialog2, id, parentDocument = document) {
    const dialogInfo = _InternalContentDialogManager._dialogMap.get(id);
    if (dialogInfo) {
      const message = `Dialog with id of '${id}' already opened`;
      Logger.logInfo(
        UiFramework.loggerCategory(this),
        `openDialog: ${message}`
      );
      IModelApp.notifications.outputMessage(
        new NotifyMessageDetails(
          OutputMessagePriority.Info,
          message,
          void 0,
          OutputMessageType.Toast
        )
      );
      return;
    }
    _InternalContentDialogManager._dialogMap.set(id, {
      reactNode: dialog2,
      zIndex: ++_InternalContentDialogManager._topZIndex,
      parentDocument
    });
    _InternalContentDialogManager._idArray.push(id);
    _InternalContentDialogManager.dialogManager.openDialog(
      dialog2,
      id,
      parentDocument
    );
  }
  /** Close a modeless dialog
   * @param id The id of the Dialog to close.
   */
  static close(id) {
    const dialogInfo = _InternalContentDialogManager._dialogMap.get(id);
    if (dialogInfo) {
      _InternalContentDialogManager.dialogManager.removeDialog(
        dialogInfo.reactNode
      );
      _InternalContentDialogManager._dialogMap.delete(id);
      const index = _InternalContentDialogManager._idArray.indexOf(id);
      if (index >= 0)
        _InternalContentDialogManager._idArray.splice(index, 1);
      if (_InternalContentDialogManager.active === void 0)
        _InternalContentDialogManager._topZIndex = _InternalContentDialogManager.getDialogZIndexDefault();
      this.update();
    } else {
      Logger.logError(
        UiFramework.loggerCategory(this),
        `closeDialog: Could not find dialog with id of '${id}'`
      );
    }
  }
  /** @internal */
  static closeAll() {
    _InternalContentDialogManager.dialogManager.closeAll();
    _InternalContentDialogManager._dialogMap.clear();
    _InternalContentDialogManager._idArray = [];
    _InternalContentDialogManager._topZIndex = _InternalContentDialogManager.getDialogZIndexDefault();
  }
  /** Update the dialogs */
  static update() {
    _InternalContentDialogManager.dialogManager.update();
  }
  /** Get the active modeless dialog */
  static get active() {
    if (_InternalContentDialogManager._idArray.length > 0) {
      const id = _InternalContentDialogManager._idArray[_InternalContentDialogManager._idArray.length - 1];
      const dialogInfo = _InternalContentDialogManager._dialogMap.get(id);
      if (dialogInfo)
        return dialogInfo.reactNode;
    }
    return void 0;
  }
  /** Get the count of modeless dialogs */
  static get count() {
    return _InternalContentDialogManager.dialogManager.dialogCount;
  }
  /** Handle a pointer down event on a modeless dialog */
  static handlePointerDownEvent(_event, id, updateFunc) {
    const dialogInfo = _InternalContentDialogManager._dialogMap.get(id);
    if (dialogInfo && dialogInfo.reactNode !== _InternalContentDialogManager.active) {
      dialogInfo.zIndex = ++_InternalContentDialogManager._topZIndex;
      _InternalContentDialogManager._idArray.splice(
        _InternalContentDialogManager._idArray.indexOf(id),
        1
      );
      _InternalContentDialogManager._idArray.push(id);
      updateFunc();
      this.update();
    }
  }
  /** Get the z-index for a modeless dialog */
  static getZIndex(id) {
    let zIndex = _InternalContentDialogManager.getDialogZIndexDefault();
    const dialogInfo = _InternalContentDialogManager._dialogMap.get(id);
    if (dialogInfo)
      zIndex = dialogInfo.zIndex;
    return zIndex;
  }
  static getInfo(id) {
    return _InternalContentDialogManager._dialogMap.get(id);
  }
};
_InternalContentDialogManager.onContentDialogChangedEvent = new ContentDialogChangedEvent();
_InternalContentDialogManager.dialogManager = new DialogManagerBase(
  _InternalContentDialogManager.onContentDialogChangedEvent
);
_InternalContentDialogManager._dialogMap = /* @__PURE__ */ new Map();
_InternalContentDialogManager._idArray = new Array();
_InternalContentDialogManager._topZIndex = CONTENT_DIALOG_ZINDEX_DEFAULT;
let InternalContentDialogManager = _InternalContentDialogManager;
try {
  InternalContentDialogManager.displayName = "InternalContentDialogManager";
  InternalContentDialogManager.__docgenInfo = { "description": "Content Dialog Manager class displays and manages multiple modeless dialogs", "displayName": "InternalContentDialogManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalContentDialogManager.onContentDialogChangedEvent.displayName = "InternalContentDialogManager.onContentDialogChangedEvent";
  InternalContentDialogManager.onContentDialogChangedEvent.__docgenInfo = { "description": "Content Dialog Changed Event class.", "displayName": "InternalContentDialogManager.onContentDialogChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalContentDialogManager.dialogManager.displayName = "InternalContentDialogManager.dialogManager";
  InternalContentDialogManager.dialogManager.__docgenInfo = { "description": "Dialog Manager class.", "displayName": "InternalContentDialogManager.dialogManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalContentDialogManager.dialogs.displayName = "InternalContentDialogManager.dialogs";
  InternalContentDialogManager.dialogs.__docgenInfo = { "description": "Get the array of modeless dialogs", "displayName": "InternalContentDialogManager.dialogs", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalContentDialogManager.active.displayName = "InternalContentDialogManager.active";
  InternalContentDialogManager.active.__docgenInfo = { "description": "Get the active modeless dialog", "displayName": "InternalContentDialogManager.active", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  InternalContentDialogManager.count.displayName = "InternalContentDialogManager.count";
  InternalContentDialogManager.count.__docgenInfo = { "description": "Get the count of modeless dialogs", "displayName": "InternalContentDialogManager.count", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
var FunctionKey = /* @__PURE__ */ ((FunctionKey2) => {
  FunctionKey2["F1"] = "F1";
  FunctionKey2["F2"] = "F2";
  FunctionKey2["F3"] = "F3";
  FunctionKey2["F4"] = "F4";
  FunctionKey2["F5"] = "F5";
  FunctionKey2["F6"] = "F6";
  FunctionKey2["F7"] = "F7";
  FunctionKey2["F8"] = "F8";
  FunctionKey2["F9"] = "F9";
  FunctionKey2["F10"] = "F10";
  FunctionKey2["F11"] = "F11";
  FunctionKey2["F12"] = "F12";
  return FunctionKey2;
})(FunctionKey || {});
var SpecialKey = /* @__PURE__ */ ((SpecialKey2) => {
  SpecialKey2["Home"] = "Home";
  SpecialKey2["End"] = "End";
  SpecialKey2["PageUp"] = "PageUp";
  SpecialKey2["PageDown"] = "PageDown";
  SpecialKey2["Escape"] = "Escape";
  SpecialKey2["Delete"] = "Delete";
  SpecialKey2["Insert"] = "Insert";
  SpecialKey2["Tab"] = "Tab";
  SpecialKey2["ArrowLeft"] = "ArrowLeft";
  SpecialKey2["ArrowRight"] = "ArrowRight";
  SpecialKey2["ArrowUp"] = "ArrowUp";
  SpecialKey2["ArrowDown"] = "ArrowDown";
  SpecialKey2["Enter"] = "Enter";
  SpecialKey2["Return"] = "Enter";
  SpecialKey2["Space"] = " ";
  SpecialKey2["Backspace"] = "Backspace";
  SpecialKey2["Clear"] = "Clear";
  SpecialKey2["Divide"] = "Divide";
  SpecialKey2["Multiply"] = "Multiply";
  SpecialKey2["Subtract"] = "Subtract";
  SpecialKey2["Add"] = "Add";
  SpecialKey2["Decimal"] = "Decimal";
  return SpecialKey2;
})(SpecialKey || {});
class KeyboardShortcut extends ItemDefBase {
  constructor(props) {
    super(props);
    this._isAltKeyRequired = false;
    this._isCtrlKeyRequired = false;
    this._isShiftKeyRequired = false;
    this._isFunctionKey = false;
    this._isSpecialKey = false;
    this._key = props.key;
    if (this._key in FunctionKey)
      this._isFunctionKey = true;
    if (this._key in SpecialKey)
      this._isSpecialKey = true;
    this._shortcuts = new KeyboardShortcutContainer();
    if (props.item) {
      this._item = props.item;
      if (!this.iconSpec)
        this.iconSpec = this._item.iconSpec;
      if (!this.label)
        this.setLabel(this._item.label);
      if (!this.tooltip)
        this.setTooltip(this._item.tooltip);
      if (this.isDisabled === void 0)
        this.isDisabled = this._item.isDisabled;
      if (this.isHidden === void 0)
        this.isHidden = this._item.isHidden;
    } else if (props.shortcuts) {
      props.shortcuts.forEach((childProps) => {
        const shortcut = new KeyboardShortcut(childProps);
        this._shortcuts.registerKey(shortcut.keyMapKey, shortcut);
      });
    } else {
      throw new UiError(
        UiFramework.loggerCategory(this),
        `Either 'item' or 'shortcuts' must be specified for '${props.key}' key.`
      );
    }
    if (props.isAltKeyRequired !== void 0)
      this._isAltKeyRequired = props.isAltKeyRequired;
    if (props.isCtrlKeyRequired !== void 0)
      this._isCtrlKeyRequired = props.isCtrlKeyRequired;
    if (props.isShiftKeyRequired !== void 0)
      this._isShiftKeyRequired = props.isShiftKeyRequired;
  }
  /** Returns the id for this shortcut */
  get id() {
    return this.keyMapKey;
  }
  /** Returns the shortcut container */
  get shortcutContainer() {
    return this._shortcuts;
  }
  /** Finds a shortcut with a given key in the shortcut's container */
  getShortcut(mapKey) {
    return this._shortcuts.findKey(mapKey);
  }
  /** Returns the shortcut's key map key used as the id */
  get keyMapKey() {
    const keyMapKey = KeyboardShortcutContainer.generateKeyMapKey(
      this.key,
      this._isAltKeyRequired,
      this._isCtrlKeyRequired,
      this._isShiftKeyRequired
    );
    return keyMapKey;
  }
  /** Returns the [[ActionButtonItemDef]] associated with this shortcut */
  get item() {
    return this._item;
  }
  /** Called when the [[ActionButtonItemDef]] associated with this shortcut is invoked */
  itemPicked() {
    if (this._shortcuts.areKeyboardShortcutsAvailable()) {
      this._shortcuts.showShortcutsMenu();
    } else {
      setTimeout(() => {
        if (this._item)
          this._item.execute();
      });
    }
  }
  /** Gets the keyboard key */
  get key() {
    return this._key;
  }
  /** Gets whether the Alt key required. */
  get isAltKeyRequired() {
    return this._isAltKeyRequired;
  }
  /** Gets whether the Ctrl key required. */
  get isCtrlKeyRequired() {
    return this._isCtrlKeyRequired;
  }
  /** Gets whether the Shift key required. */
  get isShiftKeyRequired() {
    return this._isShiftKeyRequired;
  }
  /** Gets whether this is a Function key. */
  get isFunctionKey() {
    return this._isFunctionKey;
  }
  /** Gets whether this is a Special key. */
  get isSpecialKey() {
    return this._isSpecialKey;
  }
}
class KeyboardShortcutContainer {
  constructor() {
    this._keyMap = /* @__PURE__ */ new Map();
    this._keyArray = new Array();
  }
  /** Registers a Keyboard Shortcut associated with a given key in the managed list */
  registerKey(keyMapKey, inShortcut) {
    let shortcut;
    if ((shortcut = this.findKey(keyMapKey)) === void 0) {
      shortcut = inShortcut;
      this._keyArray.push(shortcut);
    } else {
      const index = this._keyArray.findIndex((value) => {
        return value.keyMapKey === keyMapKey;
      });
      if (index >= 0) {
        shortcut = inShortcut;
        this._keyArray[index] = shortcut;
      }
    }
    if (shortcut)
      this._keyMap.set(keyMapKey, shortcut);
    return shortcut;
  }
  /** Finds a Keyboard Shortcut associated with a given key */
  findKey(keyMapKey) {
    return this._keyMap.get(keyMapKey);
  }
  /** Determines if any Keyboard Shortcuts are available in this container */
  areKeyboardShortcutsAvailable() {
    return this._keyMap.size !== 0;
  }
  /** Empties any Keyboard Shortcuts from this container */
  emptyData() {
    this._keyMap.clear();
    this._keyArray.length = 0;
  }
  getAvailableKeyboardShortcuts() {
    return this._keyArray.slice();
  }
  /** Generates a key used for storing and finding the Keyboard Shortcuts in this container */
  static generateKeyMapKey(keyboardKey, isAltKeyRequired, isCtrlKeyRequired, isShiftKeyRequired) {
    let keyMapKey = keyboardKey;
    if (isAltKeyRequired)
      keyMapKey = `Alt+${keyMapKey}`;
    if (isShiftKeyRequired)
      keyMapKey = `Shift+${keyMapKey}`;
    if (isCtrlKeyRequired)
      keyMapKey = `Ctrl+${keyMapKey}`;
    return keyMapKey;
  }
  /** Displays a menu for the Keyboard Shortcuts in this container */
  showShortcutsMenu() {
    const offset = 8;
    KeyboardShortcutMenu.onKeyboardShortcutMenuEvent.emit({
      menuVisible: true,
      menuX: UiFramework.keyboardShortcuts.cursorX - offset,
      menuY: UiFramework.keyboardShortcuts.cursorY - offset,
      shortcuts: this.getAvailableKeyboardShortcuts()
    });
  }
}
const _InternalKeyboardShortcutManager = class _InternalKeyboardShortcutManager {
  /** Initialize the Keyboard Shortcut manager
   * @internal
   */
  static initialize() {
    SyncUiEventDispatcher.onSyncUiEvent.addListener(
      _InternalKeyboardShortcutManager._handleSyncUiEvent
    );
  }
  /** Loads Keyboard Shortcuts into the managed list */
  static loadShortcuts(shortcutList) {
    shortcutList.forEach((shortcutProps) => {
      this.loadShortcut(shortcutProps);
    });
  }
  /** Loads a Keyboard Shortcut into the managed list */
  static loadShortcut(shortcutProps) {
    const shortcut = new KeyboardShortcut(shortcutProps);
    this._shortcuts.registerKey(shortcut.keyMapKey, shortcut);
  }
  /** Processes a keystroke and invokes a matching Keyboard Shortcut */
  static processKey(keyboardKey, isAltKeyPressed = false, isCtrlKeyPressed = false, isShiftKeyPressed = false) {
    const keyMapKey = KeyboardShortcutContainer.generateKeyMapKey(
      keyboardKey,
      isAltKeyPressed,
      isCtrlKeyPressed,
      isShiftKeyPressed
    );
    const shortcut = this.getShortcut(keyMapKey);
    if (shortcut) {
      shortcut.itemPicked();
      return true;
    }
    return false;
  }
  /** Returns the managed list of Keyboard Shortcuts */
  static get shortcutContainer() {
    return this._shortcuts;
  }
  /** Returns a Keyboard Shortcut from the managed lists */
  static getShortcut(keyMapKey) {
    return this._shortcuts.findKey(keyMapKey);
  }
  /** Determines if focus is set to Home */
  static get isFocusOnHome() {
    const element = document.activeElement;
    return element && element === document.body;
  }
  /** Sets focus to Home */
  static setFocusToHome() {
    const element = document.activeElement;
    if (element && element !== document.body) {
      element.blur();
      document.body.focus();
    }
  }
  /** Displays the Keyboard Shortcuts menu at the cursor */
  static displayMenu() {
    if (this._shortcuts.areKeyboardShortcutsAvailable()) {
      this._shortcuts.showShortcutsMenu();
    }
  }
  /** Closes the Keyboard Shortcuts menu */
  static closeMenu() {
    KeyboardShortcutMenu.onKeyboardShortcutMenuEvent.emit({
      menuVisible: false,
      menuX: 0,
      menuY: 0,
      shortcuts: void 0
    });
  }
  /** Returns the cursor X position, which is mouseEvent.pageX. */
  static get cursorX() {
    return CursorInformation.cursorX;
  }
  /** Returns the cursor Y position, which is mouseEvent.pageY. */
  static get cursorY() {
    return CursorInformation.cursorY;
  }
};
_InternalKeyboardShortcutManager._shortcuts = new KeyboardShortcutContainer();
_InternalKeyboardShortcutManager._handleSyncUiEvent = (args) => {
  const updateBooleanValue = (booleanValue) => {
    if (SyncUiEventDispatcher.hasEventOfInterest(
      args.eventIds,
      booleanValue.syncEventIds
    ))
      booleanValue.refresh();
  };
  const handleForSyncIds = (shortcut) => {
    if (shortcut.isDisabled instanceof ConditionalBooleanValue)
      updateBooleanValue(shortcut.isDisabled);
    if (shortcut.isHidden instanceof ConditionalBooleanValue)
      updateBooleanValue(shortcut.isHidden);
  };
  _InternalKeyboardShortcutManager._traverseShortcuts(
    _InternalKeyboardShortcutManager._shortcuts.getAvailableKeyboardShortcuts(),
    handleForSyncIds
  );
};
_InternalKeyboardShortcutManager._traverseShortcuts = (shortcuts, callback) => {
  shortcuts.forEach((shortcut) => {
    callback(shortcut);
    if (shortcut.shortcutContainer.areKeyboardShortcutsAvailable()) {
      const childShortcuts = shortcut.shortcutContainer.getAvailableKeyboardShortcuts();
      _InternalKeyboardShortcutManager._traverseShortcuts(
        childShortcuts,
        callback
      );
    }
  });
};
let InternalKeyboardShortcutManager = _InternalKeyboardShortcutManager;
class UiActivityEvent extends BeUiEvent {
}
class UiIntervalEvent extends BeUiEvent {
}
const _InternalConfigurableUiManager = class _InternalConfigurableUiManager {
  /** Initializes the InternalConfigurableUiManager and registers core controls.
   * @internal
   */
  static initialize() {
    if (this._initialized)
      return;
    _InternalConfigurableUiManager.register(
      StandardRotationNavigationAidControl.navigationAidId,
      StandardRotationNavigationAidControl
    );
    _InternalConfigurableUiManager.register(
      SheetNavigationAidControl.navigationAidId,
      SheetNavigationAidControl
    );
    _InternalConfigurableUiManager.register(
      DrawingNavigationAidControl.navigationAidId,
      DrawingNavigationAidControl
    );
    _InternalConfigurableUiManager.register(
      CubeNavigationAidControl.navigationAidId,
      CubeNavigationAidControl
    );
    SyncUiEventDispatcher.initialize();
    InternalFrontstageManager.initialize();
    InternalToolSettingsManager.initialize();
    InternalModelessDialogManager.initialize();
    InternalContentDialogManager.initialize();
    InternalKeyboardShortcutManager.initialize();
    this._initialized = true;
  }
  /** Registers a control implementing the [[ConfigurableUiElement]] interface.
   * These controls can be a
   * [[ContentControl]],
   * [[NavigationAidControl]],
   * [[StatusBarWidgetControl]],
   * [[WidgetControl]] or
   * [ToolUiProvider]($appui-react).
   * @param classId the class id of the control to register
   * @param constructor the constructor of the control to register
   */
  static register(classId, constructor) {
    if (this._registeredControls.get(classId) !== void 0) {
      throw new UiError(
        UiFramework.loggerCategory(this),
        `registerControl: classId '${classId}' already registered`
      );
    }
    this._registeredControls.set(classId, constructor);
  }
  /** Determines if a control has been registered based on its classId.
   * @param classId   the class id of the control to test
   * @returns  true if the control is registered or false if not
   */
  static isRegistered(classId) {
    const constructor = this._registeredControls.get(classId);
    return constructor !== void 0;
  }
  /** Determines if a control has been registered.
   * @internal
   */
  static getConstructorClassId(constructor) {
    for (const [key, value] of this._registeredControls.entries()) {
      if (value === constructor)
        return key;
    }
    return void 0;
  }
  /** Unregisters a control that has been registered.
   * @param classId   the class id of the control to unregister
   */
  static unregister(classId) {
    const constructor = this._registeredControls.get(classId);
    if (constructor)
      this._registeredControls.delete(classId);
  }
  /** Creates a control registered by calling registerControl.
   * @param classId   the class id of the control to create
   * @param uniqueId  a unique id for the control
   * @param options   options passed to the constructor of the control
   * @param controlId controlId which may not be unique across all control instances.
   * @returns  the created control
   */
  static create(classId, uniqueId, options, controlId) {
    const info = new ConfigurableCreateInfo(
      classId,
      uniqueId,
      controlId ?? uniqueId
    );
    const constructor = this._registeredControls.get(info.classId);
    if (!constructor) {
      throw new UiError(
        UiFramework.loggerCategory(this),
        `createControl: classId '${classId}' not registered`
      );
    }
    const control = new constructor(info, options);
    return control;
  }
  /** Gets the HTML wrapper element for Configurable UI */
  static getWrapperElement() {
    const wrapper = document.getElementById("uifw-configurableui-wrapper");
    const htmlElement = wrapper;
    return htmlElement;
  }
  /** Assists in the transition to context wrapper exported from Configurable UI */
  static getWrapperDocument() {
    const wrapper = document.getElementById("uifw-configurableui-wrapper");
    return (wrapper == null ? void 0 : wrapper.ownerDocument) ?? document;
  }
  /** Closes all UI popups currently open */
  static closeUi() {
    MessageManager.closeAllMessages();
    InternalModelessDialogManager.closeAll();
    InternalModalDialogManager.closeAll();
    InternalContentDialogManager.closeAll();
    UiFramework.keyboardShortcuts.closeMenu();
    UiFramework.closeCursorMenu();
    PopupManager.clearPopups();
  }
};
_InternalConfigurableUiManager._registeredControls = /* @__PURE__ */ new Map();
_InternalConfigurableUiManager._initialized = false;
_InternalConfigurableUiManager.activityTracker = new ActivityTracker();
_InternalConfigurableUiManager.onUiActivityEvent = new UiActivityEvent();
_InternalConfigurableUiManager.onUiIntervalEvent = new UiIntervalEvent();
let InternalConfigurableUiManager = _InternalConfigurableUiManager;
class TimeTracker {
  constructor() {
    this._firstActiveTimestamp = 0;
    this._idleStartTimestamp = 0;
    this._lastActiveTimestamp = 0;
    this._totalIdleTime = 0;
    this._idleCount = 0;
    this._idleTimeout = 5e3;
    this._totalTime = 0;
    this._idleTime = 0;
    this._idleTimeCounter = (args) => {
      const idleTimeout = args.idleTimeout ?? this._idleTimeout;
      if (this._lastActiveTimestamp > 0 && this._idleStartTimestamp === 0 && Date.now() - this._lastActiveTimestamp >= idleTimeout) {
        this._idleStartTimestamp = Date.now();
        this._idleCount++;
      }
    };
    this._trackActivity = (_args) => {
      this._lastActiveTimestamp = Date.now();
      if (this._idleStartTimestamp > 0) {
        const lastIdleTime = this._lastActiveTimestamp - this._idleStartTimestamp;
        this._totalIdleTime += lastIdleTime;
        this._idleStartTimestamp = 0;
      }
    };
  }
  /** Starts time tracking
   */
  startTiming() {
    this._totalIdleTime = 0;
    this._idleStartTimestamp = 0;
    this._idleCount = 0;
    this._totalTime = 0;
    this._idleTime = 0;
    this._firstActiveTimestamp = Date.now();
    this._lastActiveTimestamp = this._firstActiveTimestamp;
    InternalConfigurableUiManager.onUiIntervalEvent.addListener(
      this._idleTimeCounter
    );
    InternalConfigurableUiManager.onUiActivityEvent.addListener(
      this._trackActivity
    );
  }
  /** Stops time tracking
   */
  stopTiming() {
    InternalConfigurableUiManager.onUiIntervalEvent.removeListener(
      this._idleTimeCounter
    );
    InternalConfigurableUiManager.onUiActivityEvent.removeListener(
      this._trackActivity
    );
    this._totalTime = Date.now() - this._firstActiveTimestamp;
    this._idleTime = this._totalIdleTime + this._idleCount * this._idleTimeout;
  }
  /** Gets engagement time in seconds
   */
  getEngagementTimeSeconds() {
    return (this._totalTime - this._idleTime) / 1e3;
  }
  /** Gets total time in seconds
   */
  getTotalTimeSeconds() {
    return this._totalTime / 1e3;
  }
  /** Gets idle time in seconds
   */
  getIdleTimeSeconds() {
    return this._idleTime / 1e3;
  }
}
class ContentGroupProvider {
  /** Allow provider to update any data stored in ContentGroupProps. Typically this may
   * be to remove applicationData entries.
   */
  prepareToSaveProps(contentGroupProps) {
    return contentGroupProps;
  }
  /** Allow provider to update any stored ContentGroupProps be it is to be used to create ContentGroup and layouts.
   * Typically this may be to add applicationData to content entries.
   */
  applyUpdatesToSavedProps(contentGroupProps) {
    return contentGroupProps;
  }
  /** Allow provider to save any content group data before the stage deactivated. */
  async onFrontstageDeactivated() {
  }
}
const _ContentGroup = class _ContentGroup {
  constructor(contentGroupProps) {
    this._contentControls = /* @__PURE__ */ new Map();
    this._contentSetMap = /* @__PURE__ */ new Map();
    this.layout = { ...contentGroupProps.layout };
    this.propsId = contentGroupProps.id;
    this.groupId = `[${contentGroupProps.id}-${_ContentGroup._sId++}]`;
    this.contentPropsList = contentGroupProps.contents;
  }
  get id() {
    return this.groupId;
  }
  /** Gets a [[ContentControl]] from the Content Group based on its [[ContentProps]]. */
  getContentControl(contentProps, _index) {
    const id = `${contentProps.id}::${this.groupId}`;
    let contentControl;
    if (!this._contentControls.get(contentProps.id)) {
      let usedClassId = "";
      if (typeof contentProps.classId === "string") {
        if (!this._contentControls.get(contentProps.id) && UiFramework.controls.isRegistered(contentProps.classId)) {
          contentControl = UiFramework.controls.create(
            contentProps.classId,
            id,
            contentProps.applicationData,
            contentProps.id
          );
          usedClassId = contentProps.classId;
        }
      } else {
        const info = new ConfigurableCreateInfo(
          contentProps.classId.name,
          id,
          contentProps.id
        );
        contentControl = new contentProps.classId(
          info,
          contentProps.applicationData
        );
        usedClassId = contentProps.classId.name;
      }
      if (contentControl) {
        if (contentControl.getType() !== ConfigurableUiControlType.Content && contentControl.getType() !== ConfigurableUiControlType.Viewport) {
          throw new UiError(
            UiFramework.loggerCategory(this),
            `getContentControl error: '${usedClassId}' is NOT a ContentControl or ViewportContentControl`
          );
        }
        contentControl.initialize();
        this._contentControls.set(contentProps.id, contentControl);
      }
    }
    return this._contentControls.get(contentProps.id);
  }
  /** Gets a [[ContentControl]] from the Content Group with a given ID. */
  getContentControlById(id) {
    return this._contentControls.get(id);
  }
  /** Gets the React nodes representing the Content Views in this Content Group. */
  getContentNodes() {
    const contentNodes = new Array();
    this._contentSetMap.clear();
    this.contentPropsList.forEach(
      (contentProps, index) => {
        const control = this.getContentControl(contentProps, index);
        if (control) {
          contentNodes.push(control.reactNode);
          this._contentSetMap.set(control.controlId, control);
        }
      }
    );
    return contentNodes;
  }
  /** Gets the [[ContentControl]] associated with a given React node representing a Content View. */
  getControlFromElement(node) {
    if (this._contentSetMap.size === 0)
      this.getContentNodes();
    if (node && node.key) {
      const key = node.key;
      const controlId = key.split("::", 1)[0];
      return this._contentSetMap.get(controlId);
    }
    return void 0;
  }
  /** Refreshes the React nodes representing the Content Views in this Content Group.. */
  refreshContentNodes() {
    this._contentSetMap.clear();
  }
  /** Gets an array of the content controls representing the Content Views. */
  getContentControls() {
    const contentControls = new Array();
    this.contentPropsList.forEach(
      (contentProps, index) => {
        const control = this.getContentControl(contentProps, index);
        if (control) {
          contentControls.push(control);
        }
      }
    );
    return contentControls;
  }
  /** Called when Frontstage is deactivated. */
  onFrontstageDeactivated() {
    this.clearContentControls();
  }
  /** Called when Frontstage is ready. */
  onFrontstageReady() {
  }
  /** Clears the map of content controls. */
  clearContentControls() {
    this._contentControls.clear();
  }
  /** Creates [[ContentGroupProps]] for JSON purposes
   * @public
   */
  toJSON(contentCallback) {
    const contentGroupProps = {
      id: this.propsId,
      layout: this.layout,
      contents: this.contentPropsList
    };
    contentGroupProps.contents.forEach(
      (content, index) => {
        if (typeof content.classId !== "string") {
          const classId = InternalConfigurableUiManager.getConstructorClassId(
            content.classId
          );
          if (classId !== void 0)
            content.classId = classId;
          else
            throw new UiError(
              // eslint-disable-line deprecation/deprecation
              UiFramework.loggerCategory(this),
              `toJSON: ContentControl at index ${index} is NOT registered with a string id`
            );
          if (contentCallback)
            contentCallback(content);
        }
      }
    );
    return contentGroupProps;
  }
  /** Gets Viewports from Viewport Content Controls
   * @internal
   */
  getViewports() {
    const contentControls = this.getContentControls();
    const viewports = new Array();
    contentControls.forEach((control) => {
      if (control.isViewport && control.viewport) {
        viewports.push(control.viewport);
      } else {
        viewports.push(void 0);
      }
    });
    return viewports;
  }
};
_ContentGroup._sId = 0;
let ContentGroup = _ContentGroup;
try {
  ContentGroupProvider.displayName = "ContentGroupProvider";
  ContentGroupProvider.__docgenInfo = { "description": "Abstract class that can be implemented and specified by frontstage to dynamically construct\ncontent group just prior to activating the frontstage.", "displayName": "ContentGroupProvider", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ContentGroup.displayName = "ContentGroup";
  ContentGroup.__docgenInfo = { "description": "ContentGroup class. Content Groups define content displayed in content views that are laid out using a [[ContentLayout]].", "displayName": "ContentGroup", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function addDockedToolSettings(state, tabId, hidden = false) {
  if (state.toolSettings)
    throw new UiError(category, "Tool settings already exist");
  if (!(tabId in state.tabs))
    throw new UiError(category, "Tab does not exist", void 0, () => ({
      tabId
    }));
  const location = getTabLocation(state, tabId);
  if (location)
    throw new UiError(
      category,
      "Tab is already in a widget",
      void 0,
      () => ({ tabId, widgetId: location.widgetId })
    );
  return fn(state, (stateDraft) => {
    stateDraft.toolSettings = {
      tabId,
      type: "docked",
      hidden
    };
  });
}
function NineZoneStateReducer(state, action) {
  var _a2, _b2, _c2, _d;
  switch (action.type) {
    case "RESIZE": {
      state = fn(state, (draft) => {
        setSizeProps(draft.size, action.size);
      });
      const nzBounds = Rectangle.createFromSize(action.size);
      for (const id of state.floatingWidgets.allIds) {
        const floatingWidget2 = state.floatingWidgets.byId[id];
        const bounds = Rectangle.create(floatingWidget2.bounds);
        const containedBounds = bounds.containIn(nzBounds);
        state = updateFloatingWidgetState(state, id, {
          bounds: containedBounds.toProps()
        });
      }
      for (const side of panelSides) {
        state = updatePanelState(state, side, (draft) => {
          const size = getPanelSize$1(
            draft.size,
            side,
            draft.minSize,
            draft.maxSize,
            state.size
          );
          draft.size = size;
        });
      }
      return state;
    }
    case "PANEL_TOGGLE_COLLAPSED": {
      return updatePanelState(state, action.side, (draft) => {
        draft.collapsed = !draft.collapsed;
      });
    }
    case "PANEL_SET_COLLAPSED": {
      return updatePanelState(state, action.side, (draft) => {
        draft.collapsed = action.collapsed;
      });
    }
    case "PANEL_SET_SIZE": {
      const { side, size: preferredSize } = action;
      return updatePanelState(state, side, (draft) => {
        const size = getPanelSize$1(
          preferredSize,
          draft.side,
          draft.minSize,
          draft.maxSize,
          state.size
        );
        draft.size = size;
      });
    }
    case "PANEL_SET_MIN_SIZE": {
      return updatePanelState(state, action.side, (draft) => {
        const size = getPanelSize$1(
          draft.size,
          draft.side,
          action.minSize,
          draft.maxSize,
          state.size
        );
        const minSizeInPixels = getPanelPixelSizeFromSpec(
          draft.side,
          state.size,
          action.minSize
        );
        draft.minSize = minSizeInPixels;
        draft.size = size;
      });
    }
    case "PANEL_SET_MAX_SIZE": {
      return updatePanelState(state, action.side, (draft) => {
        const size = getPanelSize$1(
          draft.size,
          draft.side,
          draft.minSize,
          action.maxSize,
          state.size
        );
        draft.maxSize = action.maxSize;
        draft.size = size;
      });
    }
    case "PANEL_SET_RESIZABLE": {
      return updatePanelState(state, action.side, (draft) => {
        draft.resizable = action.resizable;
      });
    }
    case "PANEL_SET_SPLITTER_VALUE": {
      const splitterPercent = Math.min(Math.max(action.percent, 0), 100);
      return updatePanelState(state, action.side, (draft) => {
        draft.splitterPercent = splitterPercent;
      });
    }
    case "PANEL_TOGGLE_SPAN": {
      const { side } = action;
      return updatePanelState(state, side, (draft) => {
        draft.span = !draft.span;
      });
    }
    case "PANEL_SET_PINNED": {
      return updatePanelState(state, action.side, (draft) => {
        draft.pinned = action.pinned;
      });
    }
    case "PANEL_TOGGLE_PINNED": {
      return updatePanelState(state, action.side, (draft) => {
        draft.pinned = !draft.pinned;
      });
    }
    case "PANEL_INITIALIZE": {
      return updatePanelState(state, action.side, (draft) => {
        const size = getPanelSize$1(
          action.size,
          draft.side,
          draft.minSize,
          draft.maxSize,
          state.size
        );
        draft.size = size;
      });
    }
    case "PANEL_WIDGET_DRAG_START": {
      const { side, newFloatingWidgetId } = action;
      const panel = state.panels[side];
      const widgetIndex = panel.widgets.indexOf(action.id);
      const widget2 = getWidgetState$1(state, action.id);
      state = removePanelWidget(state, action.id);
      return addFloatingWidget(
        state,
        newFloatingWidgetId,
        widget2.tabs,
        {
          bounds: Rectangle.create(action.bounds).toProps(),
          userSized: action.userSized,
          id: action.newFloatingWidgetId,
          home: {
            side: action.side,
            widgetId: "",
            widgetIndex
          }
        },
        {
          ...widget2,
          minimized: false
        }
      );
    }
    case "WIDGET_DRAG": {
      const { floatingWidgetId, dragBy } = action;
      const floatingWidget2 = state.floatingWidgets.byId[floatingWidgetId];
      const newBounds = Rectangle.create(floatingWidget2.bounds).offset(dragBy);
      return updateFloatingWidgetState(state, floatingWidgetId, {
        bounds: newBounds.toProps()
      });
    }
    case "WIDGET_DRAG_END": {
      const { target, floatingWidgetId } = action;
      const floatingWidget2 = state.floatingWidgets.byId[floatingWidgetId];
      const draggedWidget = getWidgetState$1(state, floatingWidgetId);
      if (isWindowDropTargetState(target)) {
        const nzBounds = Rectangle.createFromSize(state.size);
        let newBounds = Rectangle.create(floatingWidget2.bounds);
        if (draggedWidget.minimized) {
          const containedBounds = newBounds.setHeight(35).containIn(nzBounds);
          newBounds = newBounds.setPosition(containedBounds.topLeft());
        } else {
          newBounds = newBounds.containIn(nzBounds);
        }
        state = updateFloatingWidgetState(state, floatingWidgetId, {
          bounds: newBounds.toProps()
        });
        return floatingWidgetBringToFront(state, floatingWidgetId);
      }
      state = removeFloatingWidget(state, floatingWidgetId);
      if (isTabDropTargetState(target)) {
        state = updateHomeOfToolSettingsWidget(
          state,
          target.widgetId,
          floatingWidget2.home
        );
        const targetWidget = getWidgetState$1(state, target.widgetId);
        const tabs = [...targetWidget.tabs];
        tabs.splice(target.tabIndex, 0, ...draggedWidget.tabs);
        state = updateWidgetState(state, target.widgetId, {
          tabs
        });
      } else if (isSectionDropTargetState(target)) {
        state = updatePanelState(state, target.side, (draft) => {
          draft.widgets.splice(target.sectionIndex, 0, target.newWidgetId);
          draft.collapsed = false;
        });
        state = addWidgetState(
          state,
          target.newWidgetId,
          draggedWidget.tabs,
          draggedWidget
        );
      } else if (isWidgetDropTargetState(target)) {
        state = updateHomeOfToolSettingsWidget(
          state,
          target.widgetId,
          floatingWidget2.home
        );
        const widget2 = getWidgetLocation(state, target.widgetId);
        if (widget2 && isPanelWidgetLocation(widget2)) {
          state = updatePanelState(state, widget2.side, (draft) => {
            draft.collapsed = false;
          });
        }
        const targetWidget = getWidgetState$1(state, target.widgetId);
        const tabs = [...targetWidget.tabs];
        tabs.splice(targetWidget.tabs.length, 0, ...draggedWidget.tabs);
        state = updateWidgetState(state, target.widgetId, {
          tabs
        });
      } else {
        const panelSectionId = getWidgetPanelSectionId(target.side, 0);
        state = updatePanelState(state, target.side, (draft) => {
          draft.widgets = [panelSectionId];
          draft.collapsed = false;
        });
        state = addWidgetState(state, panelSectionId, draggedWidget.tabs, {
          ...draggedWidget,
          minimized: false
        });
      }
      return state;
    }
    case "FLOATING_WIDGET_RESIZE": {
      const { resizeBy } = action;
      const widget2 = getWidgetState$1(state, action.id);
      const floatingWidget2 = state.floatingWidgets.byId[action.id];
      if (!isToolSettingsFloatingWidget(state, action.id)) {
        state = updateFloatingWidgetState(state, action.id, {
          userSized: true
        });
      }
      const minWidth2 = 200;
      const minHeight2 = 120;
      let newBounds = Rectangle.create(floatingWidget2.bounds);
      const maxLeft = newBounds.right - minWidth2;
      const maxTop = newBounds.bottom - minHeight2;
      newBounds = newBounds.inset(-resizeBy.left, -resizeBy.top, 0, 0);
      const left = Math.min(maxLeft, newBounds.left);
      const top = Math.min(maxTop, newBounds.top);
      newBounds = new Rectangle(left, top, newBounds.right, newBounds.bottom);
      const minRight = newBounds.left + minWidth2;
      const minBottom = newBounds.top + minHeight2;
      newBounds = newBounds.inset(0, 0, -resizeBy.right, -resizeBy.bottom);
      const right = Math.max(minRight, newBounds.right);
      const bottom = Math.max(minBottom, newBounds.bottom);
      newBounds = new Rectangle(left, top, right, bottom);
      state = updateFloatingWidgetState(state, action.id, {
        bounds: newBounds
      });
      const tab2 = state.tabs[widget2.activeTabId];
      if (tab2.isFloatingWidgetResizable) {
        const size = newBounds.getSize();
        state = updateTabState(state, widget2.activeTabId, (draft) => {
          initSizeProps(draft, "preferredFloatingWidgetSize", size);
          draft.userSized = true;
        });
      }
      return state;
    }
    case "FLOATING_WIDGET_SET_BOUNDS": {
      const nzBounds = Rectangle.createFromSize(state.size);
      const bounds = Rectangle.create(action.bounds).containIn(nzBounds);
      return updateFloatingWidgetState(state, action.id, {
        bounds: bounds.toProps()
      });
    }
    case "FLOATING_WIDGET_CLEAR_USER_SIZED": {
      return fn(state, (draft) => {
        const floatingWidget2 = draft.floatingWidgets.byId[action.id];
        floatingWidget2.userSized = false;
        const widget2 = getWidgetState$1(draft, action.id);
        const tab2 = draft.tabs[widget2.activeTabId];
        tab2.userSized = false;
      });
    }
    case "FLOATING_WIDGET_SET_USER_SIZED": {
      return fn(state, (draft) => {
        const floatingWidget2 = draft.floatingWidgets.byId[action.id];
        floatingWidget2.userSized = action.userSized;
      });
    }
    case "FLOATING_WIDGET_BRING_TO_FRONT": {
      return floatingWidgetBringToFront(state, action.id);
    }
    case "FLOATING_WIDGET_SEND_BACK": {
      const widget2 = getWidgetState$1(state, action.id);
      const sendBackHomeState = getSendBackHomeState(state, action.id);
      if (sendBackHomeState.widgetId) {
        const destinationWidget = state.widgets[sendBackHomeState.widgetId];
        const tabs = [...destinationWidget.tabs, ...widget2.tabs];
        state = updateWidgetState(state, destinationWidget.id, {
          tabs
        });
        return removeWidget(state, widget2.id);
      }
      const sectionIndex = sendBackHomeState.sectionIndex ?? 0;
      const home = state.floatingWidgets.byId[action.id].home;
      const destinationWidgetId = home.widgetId ? home.widgetId : getWidgetPanelSectionId(home.side, sectionIndex);
      state = removeWidget(state, widget2.id);
      return insertPanelWidget(
        state,
        sendBackHomeState.side,
        destinationWidgetId,
        widget2.tabs,
        sectionIndex
      );
    }
    case "POPOUT_WIDGET_SEND_BACK": {
      const popoutWidget = state.popoutWidgets.byId[action.id];
      const widget2 = getWidgetState$1(state, action.id);
      const home = popoutWidget.home;
      state = removeWidget(state, widget2.id);
      let existingWidget;
      if (home.widgetId in state.widgets) {
        existingWidget = state.widgets[home.widgetId];
      } else if (isPanelWidgetRestoreState(home)) {
        const panel = state.panels[home.side];
        const panelSectionId = getWidgetPanelSectionId(
          home.side,
          home.widgetIndex
        );
        existingWidget = state.widgets[panelSectionId];
        if (!existingWidget && panel.widgets.length >= panel.maxWidgetCount) {
          const sectionIndex = Math.min(
            panel.maxWidgetCount - 1,
            home.widgetIndex
          );
          const sectionId = panel.widgets[sectionIndex];
          existingWidget = state.widgets[sectionId];
        }
      }
      if (existingWidget) {
        const tabs = [...existingWidget.tabs, ...widget2.tabs];
        return updateWidgetState(state, existingWidget.id, {
          tabs
        });
      }
      if (isPanelWidgetRestoreState(home)) {
        const sectionId = home.widgetId ? home.widgetId : getWidgetPanelSectionId(home.side, home.widgetIndex);
        return insertPanelWidget(
          state,
          home.side,
          sectionId,
          [...widget2.tabs],
          home.widgetIndex
        );
      }
      return addFloatingWidget(
        state,
        home.widgetId,
        widget2.tabs,
        home.floatingWidget
      );
    }
    case "WIDGET_TAB_CLICK": {
      const { id, widgetId } = action;
      state = setWidgetActiveTabId(state, widgetId, id);
      return updateWidgetState(state, widgetId, {
        minimized: false
      });
    }
    case "WIDGET_TAB_DOUBLE_CLICK": {
      if (action.floatingWidgetId === void 0)
        return state;
      const widget2 = getWidgetState$1(state, action.widgetId);
      const active = action.id === widget2.activeTabId;
      if (!active)
        return setWidgetActiveTabId(state, widget2.id, action.id);
      return updateWidgetState(state, widget2.id, {
        minimized: !widget2.minimized
      });
    }
    case "WIDGET_TAB_DRAG_START": {
      const tabId = action.id;
      let home;
      if (action.floatingWidgetId) {
        const floatingWidget2 = state.floatingWidgets.byId[action.floatingWidgetId];
        home = floatingWidget2.home;
      } else {
        assert(!!action.side);
        const panel = state.panels[action.side];
        const widgetIndex = panel.widgets.indexOf(action.widgetId);
        home = {
          side: action.side,
          widgetId: action.widgetId,
          widgetIndex
        };
      }
      state = fn(state, (draft) => {
        draft.draggedTab = createDraggedTabState(tabId, {
          position: Point.create(action.position).toProps(),
          home
        });
      });
      return removeTabFromWidget(state, tabId);
    }
    case "WIDGET_TAB_DRAG": {
      return fn(state, (draft) => {
        const draggedTab = draft.draggedTab;
        const position = Point.create(draggedTab.position).offset(
          action.dragBy
        );
        setPointProps(draggedTab.position, position);
      });
    }
    case "WIDGET_TAB_DRAG_END": {
      assert(!!state.draggedTab);
      const target = action.target;
      if (isTabDropTargetState(target)) {
        state = updateHomeOfToolSettingsWidget(
          state,
          target.widgetId,
          state.draggedTab.home
        );
        const targetWidget = getWidgetState$1(state, target.widgetId);
        const tabIndex = target.tabIndex;
        const tabs = [...targetWidget.tabs];
        tabs.splice(tabIndex, 0, action.id);
        state = updateWidgetState(state, targetWidget.id, {
          tabs
        });
      } else if (isPanelDropTargetState(target)) {
        state = updatePanelState(state, target.side, (draft) => {
          draft.widgets.push(target.newWidgetId);
          draft.collapsed = false;
        });
        state = addWidgetState(state, target.newWidgetId, [action.id]);
      } else if (isSectionDropTargetState(target)) {
        state = updatePanelState(state, target.side, (draft) => {
          draft.widgets.splice(target.sectionIndex, 0, target.newWidgetId);
          draft.collapsed = false;
        });
        state = addWidgetState(state, target.newWidgetId, [action.id]);
      } else if (isWidgetDropTargetState(target)) {
        updateHomeOfToolSettingsWidget(
          state,
          target.widgetId,
          state.draggedTab.home
        );
        const widget2 = getWidgetLocation(state, target.widgetId);
        if (widget2 && isPanelWidgetLocation(widget2)) {
          state = updatePanelState(state, widget2.side, (draft) => {
            draft.collapsed = false;
          });
        }
        const targetWidget = getWidgetState$1(state, target.widgetId);
        const tabIndex = targetWidget.tabs.length;
        const tabs = [...targetWidget.tabs];
        tabs.splice(tabIndex, 0, action.id);
        state = updateWidgetState(state, targetWidget.id, {
          tabs
        });
      } else {
        const tab2 = state.tabs[state.draggedTab.tabId];
        const nzBounds = Rectangle.createFromSize(state.size);
        const bounds = Rectangle.createFromSize(
          tab2.preferredFloatingWidgetSize || target.size
        ).offset(state.draggedTab.position);
        const containedBounds = bounds.containIn(nzBounds);
        const userSized = tab2.userSized || tab2.isFloatingWidgetResizable && /* istanbul ignore next */
        !!tab2.preferredFloatingWidgetSize;
        state = addFloatingWidget(
          state,
          target.newFloatingWidgetId,
          [action.id],
          {
            bounds: containedBounds,
            home: state.draggedTab.home,
            userSized
          }
        );
      }
      return fn(state, (draft) => {
        draft.draggedTab = void 0;
      });
    }
    case "WIDGET_TAB_POPOUT": {
      const { id, position, size } = action;
      const location = getTabLocation(state, id);
      if (location && isPopoutTabLocation(location))
        return state;
      const savedTab = state.savedTabs.byId[id];
      const tab2 = state.tabs[id];
      let contentHeight = 800;
      let contentWidth = 600;
      if (tab2.preferredFloatingWidgetSize) {
        contentWidth = tab2.preferredFloatingWidgetSize.width;
        contentHeight = tab2.preferredFloatingWidgetSize.height;
      } else {
        const popoutContentContainer = document.getElementById(
          `content-container:${id}`
        );
        if (popoutContentContainer !== null) {
          contentWidth = popoutContentContainer.offsetWidth + 20;
          contentHeight = popoutContentContainer.offsetHeight + 20;
        }
      }
      let preferredBounds = (savedTab == null ? void 0 : savedTab.popoutBounds) ? Rectangle.create(savedTab.popoutBounds) : Rectangle.createFromSize({
        height: contentHeight,
        width: contentWidth
      });
      if (size)
        preferredBounds = preferredBounds.setSize(size);
      if (position)
        preferredBounds = preferredBounds.setPosition(position);
      const popoutWidgetId = getUniqueId();
      let home;
      if (location && isPanelTabLocation(location)) {
        const panel = state.panels[location.side];
        const widgetIndex = panel.widgets.indexOf(location.widgetId);
        home = {
          side: location.side,
          widgetId: location.widgetId,
          widgetIndex
        };
      } else if (location && isFloatingTabLocation(location)) {
        const floatingWidget2 = state.floatingWidgets.byId[location.floatingWidgetId];
        home = {
          widgetId: floatingWidget2.id,
          floatingWidget: floatingWidget2
        };
      }
      state = removeTabFromWidget(state, id);
      return addPopoutWidget(state, popoutWidgetId, [id], {
        bounds: preferredBounds.toProps(),
        home
      });
    }
    case "WIDGET_TAB_HIDE": {
      return hideTab(state, action.id);
    }
    case "WIDGET_TAB_REMOVE": {
      state = hideTab(state, action.id);
      return removeTab(state, action.id);
    }
    case "WIDGET_TAB_SET_LABEL": {
      return updateTabState(state, action.id, (draft) => {
        draft.label = action.label;
      });
    }
    case "WIDGET_TAB_OPEN": {
      return openWidgetTab(state, action.id);
    }
    case "WIDGET_TAB_CLOSE": {
      const { id } = action;
      if (((_a2 = state.toolSettings) == null ? void 0 : _a2.tabId) === id && state.toolSettings.type === "docked") {
        return state;
      }
      let location;
      [state, location] = unhideTab(state, id);
      const widget2 = state.widgets[location.widgetId];
      if (isFloatingTabLocation(location) && id === widget2.activeTabId) {
        state = updateWidgetState(state, widget2.id, {
          minimized: true
        });
      }
      return state;
    }
    case "WIDGET_TAB_FLOAT": {
      const { id, position, size } = action;
      let location;
      [state, location] = unhideTab(state, id);
      if (isFloatingTabLocation(location))
        return state;
      const tab2 = state.tabs[id];
      const preferredSize = size ?? tab2.preferredFloatingWidgetSize ?? { height: 400, width: 400 };
      const preferredPosition = position ?? { x: 50, y: 100 };
      const preferredBounds = Rectangle.createFromSize(preferredSize).offset(preferredPosition);
      const nzBounds = Rectangle.createFromSize(state.size);
      const containedBounds = preferredBounds.containIn(nzBounds);
      const userSized = tab2.userSized || tab2.isFloatingWidgetResizable && /* istanbul ignore next */
      !!tab2.preferredFloatingWidgetSize;
      if (isPanelTabLocation(location)) {
        const panel = state.panels[location.side];
        const widgetIndex = panel.widgets.indexOf(location.widgetId);
        state = updateTabState(state, id, (draft) => {
          initSizeProps(draft, "preferredFloatingWidgetSize", preferredSize);
        });
        state = removeTabFromWidget(state, id);
        state = addFloatingWidget(state, id, [id], {
          bounds: containedBounds,
          home: {
            side: location.side,
            widgetId: location.widgetId,
            widgetIndex
          },
          userSized
        });
      } else {
        const popoutWidgetId = location.popoutWidgetId;
        const popoutWidget = state.popoutWidgets.byId[popoutWidgetId];
        const widget2 = state.widgets[popoutWidgetId];
        state = removePopoutWidget(state, popoutWidgetId);
        const bounds = popoutWidget.bounds;
        const home = popoutWidget.home;
        state = addFloatingWidget(
          state,
          popoutWidgetId,
          [id],
          {
            bounds,
            home: isPanelWidgetRestoreState(home) ? home : home.floatingWidget.home
          },
          widget2
        );
      }
      const isToolSettings = ((_b2 = state.toolSettings) == null ? void 0 : _b2.tabId) === id;
      if (isToolSettings) {
        state = fn(state, (draft) => {
          assert(!!draft.toolSettings);
          draft.toolSettings.type = "widget";
        });
      }
      return state;
    }
    case "WIDGET_TAB_SET_POPOUT_BOUNDS": {
      return updateSavedTabState(state, action.id, (draft) => {
        initRectangleProps(draft, "popoutBounds", action.bounds);
      });
    }
    case "WIDGET_TAB_SHOW": {
      return showWidgetTab(state, action.id);
    }
    case "WIDGET_TAB_UNLOAD": {
      state = hideTab(state, action.id);
      return updateTabState(state, action.id, (draft) => {
        draft.unloaded = true;
      });
    }
    case "WIDGET_TAB_EXPAND": {
      state = showWidgetTab(state, action.id);
      const location = getTabLocation(state, action.id);
      if (location && isPanelTabLocation(location)) {
        state = updatePanelState(state, location.side, (draft) => {
          draft.splitterPercent = draft.widgets.findIndex((wId) => wId === location.widgetId) === 0 ? 100 : 0;
        });
      }
      return state;
    }
    case "WIDGET_TAB_UPDATE": {
      return updateTabState(state, action.id, (draft) => {
        for (const [key, val] of Object.entries(action.overrides)) {
          draft[key] = val;
        }
      });
    }
    case "TOOL_SETTINGS_DRAG_START": {
      if (!state.toolSettings)
        return state;
      if (state.toolSettings.type === "widget")
        return state;
      const { newFloatingWidgetId } = action;
      const tabId = state.toolSettings.tabId;
      const tab2 = state.tabs[tabId];
      const size = tab2.preferredFloatingWidgetSize || {
        height: 200,
        width: 300
      };
      state = addFloatingWidget(state, newFloatingWidgetId, [tabId], {
        bounds: Rectangle.createFromSize(size).toProps()
      });
      return fn(state, (draft) => {
        assert(!!draft.toolSettings);
        draft.toolSettings.type = "widget";
      });
    }
    case "TOOL_SETTINGS_DOCK": {
      if (!state.toolSettings)
        return state;
      if (state.toolSettings.type === "docked")
        return state;
      state = removeTabFromWidget(state, state.toolSettings.tabId);
      return fn(state, (draft) => {
        assert(!!draft.toolSettings);
        draft.toolSettings.type = "docked";
      });
    }
    case "WIDGET_DEF_ADD": {
      state = addTab(state, action.id, action.overrides);
      const savedTab = state.savedTabs.byId[action.id];
      if (savedTab) {
        return addRemovedTab(state, action.id);
      }
      if (action.location === "floating") {
        if (action.floatingWidget.id in state.widgets) {
          return addTabToWidget(state, action.id, action.floatingWidget.id);
        }
        const size = ((_c2 = action.overrides) == null ? void 0 : _c2.preferredFloatingWidgetSize) ?? {
          height: 200,
          width: 300
        };
        const position = action.floatingWidget.preferredPosition ?? {
          x: (state.size.width - size.width) / 2,
          y: (state.size.height - size.height) / 2
        };
        const nzBounds = Rectangle.createFromSize(state.size);
        const bounds = Rectangle.createFromSize(size).offset(position);
        const containedBounds = bounds.containIn(nzBounds);
        const userSized = !!((_d = action.overrides) == null ? void 0 : _d.preferredFloatingWidgetSize);
        return addFloatingWidget(state, action.floatingWidget.id, [action.id], {
          bounds: containedBounds.toProps(),
          home: {
            side: action.panelSection.side,
            widgetId: action.panelSection.id,
            widgetIndex: 0
          },
          userSized
        });
      }
      const side = action.panelSection.side;
      if (action.panelSection.id in state.widgets) {
        return addTabToWidget(state, action.id, action.panelSection.id);
      }
      const panel = state.panels[side];
      if (panel.widgets.length < panel.maxWidgetCount) {
        return addPanelWidget(state, side, action.panelSection.id, [action.id]);
      }
      const sectionIndex = Math.min(
        action.panelSection.index,
        panel.widgets.length - 1
      );
      const existingSectionId = panel.widgets[sectionIndex];
      return addTabToWidget(state, action.id, existingSectionId);
    }
    case "WIDGET_DEF_ADD_TOOL_SETTINGS": {
      state = addTab(state, action.id, action.overrides);
      return addDockedToolSettings(state, action.id);
    }
  }
  return state;
}
function openWidgetTab(state, id) {
  var _a2;
  if (((_a2 = state.toolSettings) == null ? void 0 : _a2.tabId) === id && state.toolSettings.type === "docked") {
    return fn(state, (draft) => {
      assert(draft.toolSettings.type === "docked");
      draft.toolSettings.hidden = false;
    });
  }
  let location;
  [state, location] = unhideTab(state, id);
  return fn(state, (draft) => {
    const widget2 = draft.widgets[location.widgetId];
    widget2.minimized = false;
    widget2.activeTabId = id;
    if (isPanelTabLocation(location)) {
      const panel = draft.panels[location.side];
      panel.collapsed = false;
      if (void 0 === panel.size || 0 === panel.size) {
        panel.size = panel.minSize ?? 200;
      }
    }
  });
}
function showWidgetTab(state, id) {
  state = openWidgetTab(state, id);
  const location = getTabLocation(state, id);
  if (!location)
    return state;
  state = updateWidgetState(state, location.widgetId, {
    activeTabId: id,
    minimized: false
  });
  if (isPanelTabLocation(location)) {
    state = updatePanelState(state, location.side, (draft) => {
      draft.collapsed = false;
    });
  }
  if (isFloatingTabLocation(location)) {
    state = floatingWidgetBringToFront(state, location.floatingWidgetId);
  }
  return state;
}
function unhideTab(state, id) {
  let location = getTabLocation(state, id);
  if (!location) {
    state = addRemovedTab(state, id);
    location = getTabLocation(state, id);
  }
  state = updateTabState(state, id, (draft) => {
    draft.unloaded = false;
  });
  return [state, location];
}
function hideTab(state, id) {
  state = fn(state, (draft) => {
    if (!draft.toolSettings)
      return;
    const isToolSettings = draft.toolSettings.tabId === id;
    if (isToolSettings && draft.toolSettings.type === "docked") {
      draft.toolSettings.hidden = true;
    }
  });
  state = updateTabState(state, id, (draft) => {
    draft.unloaded = false;
  });
  const location = getTabLocation(state, id);
  if (!location)
    return state;
  const widgetId = location.widgetId;
  const tabIndex = state.widgets[widgetId].tabs.indexOf(id);
  if (isFloatingTabLocation(location)) {
    const floatingWidget2 = state.floatingWidgets.byId[widgetId];
    state = updateSavedTabState(state, id, (draft) => {
      draft.home = {
        widgetId,
        tabIndex,
        floatingWidget: floatingWidget2
      };
    });
  } else if (isPanelTabLocation(location)) {
    const side = location.side;
    const widgetIndex = state.panels[side].widgets.indexOf(widgetId);
    state = updateSavedTabState(state, id, (draft) => {
      draft.home = {
        widgetId,
        side,
        widgetIndex,
        tabIndex
      };
    });
  }
  return removeTabFromWidget(state, id);
}
var StageUsage = /* @__PURE__ */ ((StageUsage2) => {
  StageUsage2["Private"] = "Private";
  StageUsage2["General"] = "General";
  StageUsage2["Redline"] = "Redline";
  StageUsage2["ViewOnly"] = "ViewOnly";
  StageUsage2["Edit"] = "Edit";
  StageUsage2["Settings"] = "Settings";
  return StageUsage2;
})(StageUsage || {});
class FrontstageDef {
  constructor() {
    this._id = "";
    this._isStageClosing = false;
    this._isReady = false;
    this._isApplicationClosing = false;
    this._version = 0;
    this._timeTracker = new TimeTracker();
    this._batching = false;
  }
  get id() {
    return this._id;
  }
  get usage() {
    return this._usage !== void 0 ? this._usage : StageUsage.General;
  }
  get version() {
    return this._version;
  }
  get contentGroupProvider() {
    return this._contentGroupProvider;
  }
  get floatingContentControls() {
    return this._floatingContentControls;
  }
  get toolSettings() {
    return this._toolSettings;
  }
  get statusBar() {
    return this._statusBar;
  }
  get contentManipulation() {
    return this._contentManipulation;
  }
  get viewNavigation() {
    return this._viewNavigation;
  }
  get topPanel() {
    return this._topPanel;
  }
  get leftPanel() {
    return this._leftPanel;
  }
  get rightPanel() {
    return this._rightPanel;
  }
  get bottomPanel() {
    return this._bottomPanel;
  }
  get contentLayoutDef() {
    return this._contentLayoutDef;
  }
  get contentGroup() {
    return this._contentGroup;
  }
  get frontstageProvider() {
    return this._frontstageProvider;
  }
  toStagePanelLocation(side) {
    switch (side) {
      case "bottom":
        return StagePanelLocation.Bottom;
      case "left":
        return StagePanelLocation.Left;
      case "right":
        return StagePanelLocation.Right;
      case "top":
        return StagePanelLocation.Top;
    }
  }
  populateStateMaps(nineZone) {
    const panelMap = /* @__PURE__ */ new Map();
    const widgetMap = /* @__PURE__ */ new Map();
    for (const panelSide of panelSides) {
      const panel = nineZone.panels[panelSide];
      const location = this.toStagePanelLocation(panelSide);
      const panelDef = this.getStagePanelDef(location);
      if (!panelDef)
        continue;
      const panelState = panel.collapsed ? StagePanelState.Minimized : StagePanelState.Open;
      panelMap.set(panelDef, panelState);
    }
    for (const widgetDef of this.widgetDefs) {
      const widgetState = getWidgetState(widgetDef, nineZone);
      widgetMap.set(widgetDef, widgetState);
    }
    return { panelMap, widgetMap };
  }
  triggerStateChangeEvents(oldState) {
    const newState = this.nineZoneState;
    if (!newState)
      return;
    if (this._isStageClosing || this._isApplicationClosing)
      return;
    const { panelMap, widgetMap } = this.populateStateMaps(oldState);
    const { panelMap: newPanelMap, widgetMap: newWidgetMap } = this.populateStateMaps(newState);
    newWidgetMap.forEach((newWidgetState, widgetDef) => {
      const originalState = widgetMap.get(widgetDef);
      if (originalState === newWidgetState)
        return;
      widgetDef.handleWidgetStateChanged(newWidgetState);
    });
    newPanelMap.forEach((newPanelState, panelDef) => {
      const originalState = panelMap.get(panelDef);
      if (originalState === newPanelState)
        return;
      panelDef.handlePanelStateChanged(newPanelState);
    });
    for (const panelSide of panelSides) {
      const panel = newState.panels[panelSide];
      const oldPanel = oldState.panels[panelSide];
      const location = this.toStagePanelLocation(panelSide);
      const panelDef = this.getStagePanelDef(location);
      if (!panelDef)
        continue;
      if (panel.size !== oldPanel.size) {
        panelDef.handleSizeChanged(panel.size);
      }
      if (panel.pinned !== oldPanel.pinned) {
        panelDef.handlePinnedChanged(panel.pinned);
      }
    }
  }
  handlePopouts(oldState) {
    const newState = this.nineZoneState;
    const oldPopouts = (oldState == null ? void 0 : oldState.popoutWidgets.allIds) || [];
    const newPopouts = (newState == null ? void 0 : newState.popoutWidgets.allIds) || [];
    const popoutsToClose = oldPopouts.filter((p) => !newPopouts.includes(p));
    const popoutsToOpen = newPopouts.filter((p) => !oldPopouts.includes(p));
    for (const popoutId of popoutsToClose) {
      UiFramework.childWindows.close(popoutId, true);
    }
    for (const popoutId of popoutsToOpen) {
      const result = this.openPopoutWidgetContainer(popoutId, oldState);
      if (!result)
        return result;
    }
    return true;
  }
  /** @internal */
  get nineZoneState() {
    return this._nineZoneState;
  }
  set nineZoneState(state) {
    if (this._nineZoneState === state)
      return;
    const oldState = this._nineZoneState;
    this._nineZoneState = state;
    if (this._batching)
      return;
    const popoutResult = this.handlePopouts(oldState);
    if (oldState) {
      this.triggerStateChangeEvents(oldState);
    }
    const isClosing = this._isStageClosing || this._isApplicationClosing;
    if (isClosing && !this.isReady)
      return;
    if (popoutResult === false) {
      IModelApp.notifications.outputMessage(
        new NotifyMessageDetails(
          OutputMessagePriority.Error,
          UiFramework.translate("widget.errorMessage.widgetPopoutFail"),
          void 0,
          OutputMessageType.Toast
        )
      );
      return;
    }
    InternalFrontstageManager.onFrontstageNineZoneStateChangedEvent.emit({
      frontstageDef: this,
      state
    });
  }
  /** @internal */
  get dispatch() {
    return this._dispatch ?? (this._dispatch = (action) => {
      const state = this.nineZoneState;
      if (!state)
        return;
      this.nineZoneState = NineZoneStateReducer(state, action);
    });
  }
  set dispatch(dispatch) {
    this._dispatch = dispatch;
  }
  /** Dispatch multiple actions inside `fn`, but trigger events once.
   * @internal
   */
  batch(fn2) {
    const initialState2 = this._nineZoneState;
    this._batching = true;
    fn2();
    this._batching = false;
    const updatedState = this._nineZoneState;
    this._nineZoneState = initialState2;
    this.nineZoneState = updatedState;
  }
  /** @internal */
  get timeTracker() {
    return this._timeTracker;
  }
  /** Created a [[FrontstageDef]] and initialize it */
  static async create(provider) {
    const def = new FrontstageDef();
    def._frontstageProvider = provider;
    const config = provider.frontstageConfig();
    await def.initializeFromConfig(config);
    return def;
  }
  /** Handles when the Frontstage becomes activated */
  async _onActivated() {
  }
  /** Handles when the Frontstage becomes activated */
  async onActivated() {
    this.updateWidgetDefs();
    const provider = this._contentGroupProvider;
    if (provider && this._initialConfig) {
      this._contentGroup = await provider.contentGroup(this._initialConfig);
    }
    if (!this._contentGroup)
      throw new UiError(
        UiFramework.loggerCategory(this),
        `onActivated: Content Group not defined`
      );
    this._contentLayoutDef = UiFramework.content.layouts.getForGroup(
      this._contentGroup
    );
    UiFramework.frontstages.onContentLayoutActivatedEvent.emit({
      contentLayout: this._contentLayoutDef,
      contentGroup: this._contentGroup
    });
    this._timeTracker.startTiming();
    await this._onActivated();
  }
  /** Handles when the Frontstage becomes inactive */
  async _onDeactivated() {
  }
  /** Handles when the Frontstage becomes inactive */
  async onDeactivated() {
    for (const control of this._widgetControls) {
      control.onFrontstageDeactivated();
    }
    for (const control of this.contentControls) {
      control.onFrontstageDeactivated();
    }
    if (this.contentGroup)
      this.contentGroup.onFrontstageDeactivated();
    if (this.contentGroupProvider)
      await this.contentGroupProvider.onFrontstageDeactivated();
    this._timeTracker.stopTiming();
    this._isStageClosing = true;
    UiFramework.childWindows.closeAll();
    if (this._floatingContentControls) {
      InternalContentDialogManager.closeAll();
      this._floatingContentControls = void 0;
    }
    if (this._toolAdminDefaultToolId) {
      IModelApp.toolAdmin.defaultToolId = this._toolAdminDefaultToolId;
      this._toolAdminDefaultToolId = void 0;
    }
    await this._onDeactivated();
    this._isStageClosing = false;
  }
  /** @internal */
  setIsApplicationClosing(value) {
    this._isApplicationClosing = value;
  }
  /** Returns once the contained widgets and content controls are ready to use */
  async waitUntilReady() {
    this._isReady = false;
    const controlReadyPromises = new Array();
    this._widgetControls.forEach((control) => {
      controlReadyPromises.push(control.isReady);
    });
    if (this.contentLayoutDef) {
      const usedContentIndexes = this.contentLayoutDef.getUsedContentIndexes();
      this.contentControls.forEach((control, index) => {
        if (usedContentIndexes.includes(index))
          controlReadyPromises.push(control.isReady);
      });
    }
    await Promise.all(controlReadyPromises);
    this._isReady = true;
  }
  /** Handles when the Frontstage becomes active */
  _onFrontstageReady() {
  }
  /** Handles when the Frontstage becomes active */
  onFrontstageReady() {
    for (const control of this._widgetControls) {
      control.onFrontstageReady();
    }
    for (const control of this.contentControls) {
      control.onFrontstageReady();
    }
    if (this.contentGroup)
      this.contentGroup.onFrontstageReady();
    if (IModelApp.toolAdmin && IModelApp.viewManager && this._initialConfig) {
      const defaultTool = this._initialConfig.defaultTool;
      if (defaultTool) {
        this._toolAdminDefaultToolId = IModelApp.toolAdmin.defaultToolId;
        IModelApp.toolAdmin.defaultToolId = defaultTool;
        void IModelApp.tools.run(defaultTool);
      } else {
        void IModelApp.toolAdmin.startDefaultTool();
      }
    }
    this._onFrontstageReady();
  }
  /** Sets the Content Layout and Content Group */
  setContentLayoutAndGroup(contentLayoutDef, contentGroup) {
    this._contentLayoutDef = contentLayoutDef;
    this._contentGroup = contentGroup;
  }
  /** Sets the active view content control to the default or first */
  async setActiveContent() {
    let contentControl;
    let activated = false;
    if (!contentControl && this.contentControls.length >= 0) {
      contentControl = this.contentControls[0];
    }
    if (contentControl) {
      UiFramework.content.setActive(contentControl.reactNode, true);
      if (contentControl.viewport) {
        const status = await IModelApp.viewManager.setSelectedView(
          contentControl.viewport
        );
        activated = status === BentleyStatus.SUCCESS;
      }
    }
    return activated;
  }
  /** Sets the active view content control */
  setActiveView(newContent, oldContent) {
    if (oldContent)
      oldContent.onDeactivated();
    newContent.onActivated();
    UiFramework.frontstages.onContentControlActivatedEvent.emit({
      activeContentControl: newContent,
      oldContentControl: oldContent
    });
  }
  /** Sets the active view content control based on the selected viewport. */
  setActiveViewFromViewport(viewport) {
    const contentControl = this.contentControls.find(
      (control) => control.viewport === viewport
    );
    if (contentControl) {
      UiFramework.content.setActive(contentControl.reactNode, true);
      return true;
    }
    return false;
  }
  /** Gets a [[StagePanelDef]] based on a given panel location
   * @beta
   */
  getStagePanelDef(location) {
    let panelDef;
    switch (location) {
      case StagePanelLocation.Top:
        panelDef = this.topPanel;
        break;
      case StagePanelLocation.Left:
        panelDef = this.leftPanel;
        break;
      case StagePanelLocation.Right:
        panelDef = this.rightPanel;
        break;
      case StagePanelLocation.Bottom:
        panelDef = this.bottomPanel;
        break;
    }
    return panelDef;
  }
  /** Gets a list of [[StagePanelDef]]s
   * @beta
   */
  get panelDefs() {
    const locations = [
      StagePanelLocation.Left,
      StagePanelLocation.Right,
      StagePanelLocation.Top,
      StagePanelLocation.Bottom
    ];
    const panels = [];
    locations.forEach((location) => {
      const panel = this.getStagePanelDef(location);
      if (panel)
        panels.push(panel);
    });
    return panels;
  }
  /** Finds a [[WidgetDef]] based on a given id */
  findWidgetDef(id) {
    for (const widgetDef of this.widgetDefs) {
      if (widgetDef.id === id)
        return widgetDef;
    }
    return void 0;
  }
  /** Gets the list of [[WidgetControl]]s */
  get _widgetControls() {
    const widgetControls = new Array();
    this.panelDefs.forEach((panelDef) => {
      panelDef.widgetDefs.forEach((widgetDef) => {
        const widgetControl = widgetDef.widgetControl;
        if (widgetControl)
          widgetControls.push(widgetControl);
      });
    });
    return widgetControls;
  }
  addFloatingContentControl(contentControl) {
    if (!contentControl)
      return;
    if (!this._floatingContentControls)
      this._floatingContentControls = new Array();
    this._floatingContentControls.push(contentControl);
    UiFramework.content.onAvailableContentChangedEvent.emit({
      contentId: contentControl.uniqueId
    });
  }
  dropFloatingContentControl(contentControl) {
    if (!contentControl || !this._floatingContentControls)
      return;
    const index = this._floatingContentControls.indexOf(contentControl);
    if (index > -1) {
      this._floatingContentControls.splice(index, 1);
      UiFramework.content.onAvailableContentChangedEvent.emit({
        contentId: contentControl.uniqueId
      });
    }
  }
  /** Gets the list of [[ContentControl]]s */
  get contentControls() {
    const contentControls = new Array();
    if (this.contentGroup) {
      contentControls.push(...this.contentGroup.getContentControls());
    }
    if (this._floatingContentControls) {
      contentControls.push(...this._floatingContentControls);
    }
    return contentControls;
  }
  /** Initializes a FrontstageDef from FrontstageConfig.
   * @internal
   */
  async initializeFromConfig(config) {
    this._id = config.id;
    this._initialConfig = config;
    if (config.contentGroup instanceof ContentGroupProvider) {
      this._contentGroupProvider = config.contentGroup;
    } else {
      this._contentGroup = config.contentGroup;
    }
    this._usage = config.usage;
    this._version = config.version;
    this._toolSettings = createWidgetDef(
      config.toolSettings,
      WidgetType.ToolSettings
    );
    this._statusBar = createWidgetDef(config.statusBar, WidgetType.StatusBar);
    this._contentManipulation = createWidgetDef(
      config.contentManipulation,
      WidgetType.Tool
    );
    this._viewNavigation = createWidgetDef(
      config.viewNavigation,
      WidgetType.Navigation
    );
    this._topPanel = createStagePanelDef(config, StagePanelLocation.Top);
    this._leftPanel = createStagePanelDef(config, StagePanelLocation.Left);
    this._rightPanel = createStagePanelDef(config, StagePanelLocation.Right);
    this._bottomPanel = createStagePanelDef(config, StagePanelLocation.Bottom);
  }
  /** @internal */
  updateWidgetDefs() {
    const allStageWidgetDefs = [];
    this.panelDefs.forEach((stagePanelDef) => {
      stagePanelDef.updateDynamicWidgetDefs(
        this.id,
        this.usage,
        stagePanelDef.location,
        void 0,
        allStageWidgetDefs
      );
    });
    InternalFrontstageManager.onFrontstageWidgetsChangedEvent.emit({
      frontstageDef: this
    });
  }
  /** Restores frontstage layout to initial configuration.
   * @beta
   */
  restoreLayout() {
    var _a2, _b2, _c2;
    for (const panelDef of this.panelDefs) {
      panelDef.size = (_a2 = panelDef.initialConfig) == null ? void 0 : _a2.size;
      panelDef.sizeSpec = (_b2 = panelDef.initialConfig) == null ? void 0 : _b2.sizeSpec;
      panelDef.panelState = panelDef.defaultState;
      panelDef.pinned = ((_c2 = panelDef.initialConfig) == null ? void 0 : _c2.pinned) ?? true;
    }
    for (const widgetDef of this.widgetDefs) {
      widgetDef.setWidgetState(widgetDef.defaultState);
    }
    InternalFrontstageManager.onFrontstageRestoreLayoutEvent.emit({
      frontstageDef: this
    });
  }
  isPopoutWidget(widgetId) {
    if (this.nineZoneState) {
      const location = getTabLocation(this.nineZoneState, widgetId);
      if (location)
        return isPopoutTabLocation(location);
    }
    return false;
  }
  isFloatingWidget(widgetId) {
    if (this.nineZoneState) {
      const location = getTabLocation(this.nineZoneState, widgetId);
      if (location)
        return isFloatingTabLocation(location);
    }
    return false;
  }
  /** Create a new floating panel that contains the widget specified by its Id.
   * @param widgetId case sensitive Widget Id
   * @param position Position of top left corner of floating panel in pixels. If undefined {x:50, y:100} is used.
   * @param size defines the width and height of the floating panel. If undefined and widget has been floated before
   * the previous size is used, else {height:400, width:400} is used.
   * @beta
   */
  floatWidget(widgetId, position, size) {
    const state = this.nineZoneState;
    if (!state)
      return;
    const widgetDef = this.findWidgetDef(widgetId);
    if (!widgetDef)
      return;
    this.dispatch({
      type: "WIDGET_TAB_FLOAT",
      id: widgetId,
      position,
      size
    });
  }
  /** Check widget and panel state to determine whether the widget is currently displayed
   * @param widgetId case-sensitive Widget Id
   * @public
   */
  isWidgetDisplayed(widgetId) {
    if (!this.nineZoneState)
      return false;
    const tabLocation = getTabLocation(this.nineZoneState, widgetId);
    if (!tabLocation)
      return false;
    if (isPanelTabLocation(tabLocation)) {
      const panel = this.nineZoneState.panels[tabLocation.side];
      const widgetDef = this.findWidgetDef(widgetId);
      const isVisible = !!widgetDef && widgetDef.state === WidgetState.Open && !panel.collapsed;
      return isVisible;
    }
    return true;
  }
  /** Opens window for specified PopoutWidget container. Used to reopen popout when running in Electron.
   * @internal
   */
  openPopoutWidgetContainer(widgetContainerId, oldState) {
    const state = this.nineZoneState;
    if (!state)
      return false;
    const location = getWidgetLocation(state, widgetContainerId);
    if (!location)
      return false;
    if (!isPopoutWidgetLocation(location))
      return false;
    const widget2 = state.widgets[widgetContainerId];
    if (widget2.tabs.length !== 1)
      return false;
    const tabId = widget2.tabs[0];
    const widgetDef = this.findWidgetDef(tabId);
    if (!widgetDef)
      return false;
    const popoutContent = /* @__PURE__ */ jsxRuntimeExports.jsx(
      PopoutWidget,
      {
        widgetContainerId,
        widgetDef
      }
    );
    const popoutWidget = state.popoutWidgets.byId[location.popoutWidgetId];
    const bounds = Rectangle.create(popoutWidget.bounds);
    const position = {
      width: bounds.getWidth(),
      height: bounds.getHeight(),
      left: bounds.left,
      top: bounds.top
    };
    const result = UiFramework.childWindows.open(
      widgetContainerId,
      widgetDef.label,
      popoutContent,
      position,
      UiFramework.useDefaultPopoutUrl
    );
    if (!result && oldState) {
      this.nineZoneState = oldState;
      return false;
    }
    return true;
  }
  /** Create a new popout/child window that contains the widget specified by its Id.
   * @param widgetId case sensitive Widget Id
   * @param position Position of top left corner of floating panel in pixels. If undefined {x:0, y:0} is used.
   * @param size defines the width and height of the floating panel. If undefined and widget has been floated before
   * the previous size is used, else {height:800, width:600} is used.
   * @beta
   */
  popoutWidget(widgetId, position, size) {
    const state = this.nineZoneState;
    if (!state)
      return;
    const widgetDef = this.findWidgetDef(widgetId);
    if (!widgetDef)
      return;
    this.dispatch({
      type: "WIDGET_TAB_POPOUT",
      id: widgetId,
      position,
      size
    });
  }
  get isStageClosing() {
    return this._isStageClosing;
  }
  get isApplicationClosing() {
    return this._isApplicationClosing;
  }
  get isReady() {
    return this._isReady;
  }
  /** @internal */
  saveChildWindowSizeAndPosition(childWindowId, childWindow) {
    const state = this.nineZoneState;
    if (!state)
      return;
    const location = getWidgetLocation(state, childWindowId);
    if (!location || !isPopoutWidgetLocation(location))
      return;
    const widget2 = state.widgets[location.popoutWidgetId];
    const tabId = widget2.tabs[0];
    const widgetDef = this.findWidgetDef(tabId);
    if (!widgetDef)
      return;
    let width = childWindow.shouldUseOuterSized ? childWindow.outerWidth : childWindow.innerWidth;
    let height = childWindow.shouldUseOuterSized ? childWindow.outerHeight : childWindow.innerHeight;
    if (childWindow.deltaHeight) {
      height = height + childWindow.deltaHeight;
      if (height < 1)
        height = 100;
    }
    if (childWindow.deltaWidth) {
      width = width + childWindow.deltaWidth;
      if (width < 1)
        width = 100;
    }
    const bounds = Rectangle.createFromSize({ width, height }).offset({
      x: childWindow.screenX,
      y: childWindow.screenY
    });
    this.dispatch({
      type: "WIDGET_TAB_SET_POPOUT_BOUNDS",
      id: tabId,
      bounds
    });
  }
  /** @internal */
  dockWidgetContainerByContainerId(widgetContainerId) {
    const state = this.nineZoneState;
    if (!state)
      return;
    const location = getWidgetLocation(state, widgetContainerId);
    if (!location)
      return;
    if (isFloatingWidgetLocation(location)) {
      this.dispatch({
        type: "FLOATING_WIDGET_SEND_BACK",
        id: location.floatingWidgetId
      });
      return;
    }
    if (isPopoutWidgetLocation(location)) {
      this.dispatch({
        type: "POPOUT_WIDGET_SEND_BACK",
        id: location.popoutWidgetId
      });
      return;
    }
  }
  /** Finds the container with the specified widget and re-docks all widgets
   * back to the panel zone location that was used when the floating container
   * was generated.
   * @param widgetId  case sensitive Widget Id.
   * @beta
   */
  dockWidgetContainer(widgetId) {
    const state = this.nineZoneState;
    if (!state)
      return;
    const location = getTabLocation(state, widgetId);
    if (!location)
      return;
    this.dockWidgetContainerByContainerId(location.widgetId);
  }
  setFloatingWidgetContainerBounds(floatingWidgetId, bounds) {
    if (!this.nineZoneState || !(floatingWidgetId in this.nineZoneState.floatingWidgets.byId))
      return false;
    this.dispatch({
      type: "FLOATING_WIDGET_SET_BOUNDS",
      id: floatingWidgetId,
      bounds
    });
    this.dispatch({
      type: "FLOATING_WIDGET_SET_USER_SIZED",
      id: floatingWidgetId,
      userSized: true
    });
    return true;
  }
  getFloatingWidgetContainerIds() {
    if (!this.nineZoneState)
      return [];
    return [...this.nineZoneState.floatingWidgets.allIds];
  }
  getFloatingWidgetContainerIdByWidgetId(widgetId) {
    if (!this.nineZoneState)
      return void 0;
    const location = getTabLocation(this.nineZoneState, widgetId);
    if (location && isFloatingTabLocation(location)) {
      return location.floatingWidgetId;
    }
    return void 0;
  }
  getFloatingWidgetContainerBounds(floatingWidgetId) {
    if (!floatingWidgetId)
      return void 0;
    if (this.nineZoneState && floatingWidgetId in this.nineZoneState.floatingWidgets.byId) {
      const foundWidget = document.querySelector(
        `div.nz-widget-floatingWidget[data-widget-id='${floatingWidgetId}']`
      );
      if (foundWidget) {
        const domRect = foundWidget.getBoundingClientRect();
        return {
          left: domRect.left,
          right: domRect.right,
          top: domRect.top,
          bottom: domRect.bottom
        };
      }
      return this.nineZoneState.floatingWidgets.byId[floatingWidgetId].bounds;
    }
    return void 0;
  }
  *_widgetDefs() {
    for (const panelDef of this.panelDefs) {
      for (const widgetDef of panelDef.widgetDefs) {
        yield widgetDef;
      }
    }
    if (this.toolSettings)
      yield this.toolSettings;
    if (this.statusBar)
      yield this.statusBar;
    return void 0;
  }
  /** Iterable of all widget definitions in a frontstage.
   * @internal
   */
  get widgetDefs() {
    const defs = this._widgetDefs();
    return {
      [Symbol.iterator]() {
        return defs;
      }
    };
  }
}
function createWidgetDef(config, type) {
  if (!config)
    return void 0;
  const widgetDef = WidgetDef.create(config, type);
  return widgetDef;
}
function createStagePanelDef(frontstageConfig, location) {
  const config = getStagePanel(location, frontstageConfig);
  const panelDef = new StagePanelDef();
  panelDef.initializeFromConfig(config, location);
  return panelDef;
}
function getStagePanel(location, config) {
  switch (location) {
    case StagePanelLocation.Top:
      return config.topPanel;
    case StagePanelLocation.Left:
      return config.leftPanel;
    case StagePanelLocation.Right:
      return config.rightPanel;
    case StagePanelLocation.Bottom:
      return config.bottomPanel;
  }
}
const useActiveFrontstageId = () => {
  const def = useActiveFrontstageDef();
  const id = reactExports.useMemo(
    () => def ? (
      /* istanbul ignore next */
      def.id
    ) : "",
    [def]
  );
  return id;
};
function useActiveFrontstageDef() {
  const [def, setDef] = reactExports.useState(
    UiFramework.frontstages.activeFrontstageDef
  );
  reactExports.useEffect(() => {
    return UiFramework.frontstages.onFrontstageActivatedEvent.addListener(
      (args) => {
        setDef(args.activatedFrontstageDef);
      }
    );
  }, []);
  return def;
}
function useSpecificWidgetDef(widgetId) {
  const frontstageDef = useActiveFrontstageDef();
  const [widgetDef, setWidgetDef] = reactExports.useState(
    () => frontstageDef == null ? void 0 : frontstageDef.findWidgetDef(widgetId)
  );
  reactExports.useEffect(() => {
    setWidgetDef(frontstageDef == null ? void 0 : frontstageDef.findWidgetDef(widgetId));
    return InternalFrontstageManager.onFrontstageWidgetsChangedEvent.addListener(
      (args) => {
        if (args.frontstageDef !== frontstageDef)
          return;
        setWidgetDef(frontstageDef.findWidgetDef(widgetId));
      }
    );
  }, [frontstageDef, widgetId]);
  return widgetDef;
}
try {
  useActiveFrontstageDef.displayName = "useActiveFrontstageDef";
  useActiveFrontstageDef.__docgenInfo = { "description": "Hook to return the active Frontstage def.", "displayName": "useActiveFrontstageDef", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useSpecificWidgetDef.displayName = "useSpecificWidgetDef";
  useSpecificWidgetDef.__docgenInfo = { "description": "Hook that returns the widgetDef for a specific widgetId within the active frontstage.", "displayName": "useSpecificWidgetDef", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  FrontstageDef.displayName = "FrontstageDef";
  FrontstageDef.__docgenInfo = { "description": "FrontstageDef class provides an API for a Frontstage.", "displayName": "FrontstageDef", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useActiveFrontstageId.displayName = "useActiveFrontstageId";
  useActiveFrontstageId.__docgenInfo = { "description": "Hook that returns active frontstage id.", "displayName": "useActiveFrontstageId", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function onEscapeSetFocusToHome(e) {
  if (e.key === Key_enum.Key.Escape.valueOf()) {
    UiFramework.keyboardShortcuts.setFocusToHome();
  }
}
function useEscapeSetFocusToHome() {
  return reactExports.useCallback(onEscapeSetFocusToHome, []);
}
try {
  onEscapeSetFocusToHome.displayName = "onEscapeSetFocusToHome";
  onEscapeSetFocusToHome.__docgenInfo = { "description": "Keyboard Event handler to set focus to Home on Escape key", "displayName": "onEscapeSetFocusToHome", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useEscapeSetFocusToHome.displayName = "useEscapeSetFocusToHome";
  useEscapeSetFocusToHome.__docgenInfo = { "description": "useCallback Hook for Keyboard Event handler to set focus to Home on Escape key", "displayName": "useEscapeSetFocusToHome", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function useUiVisibility() {
  const [uiIsVisible, setUiIsVisible] = reactExports.useState(UiFramework.getIsUiVisible());
  reactExports.useEffect(() => {
    const handleUiVisibilityChanged = (args) => setUiIsVisible(args.visible);
    UiFramework.onUiVisibilityChanged.addListener(handleUiVisibilityChanged);
    return () => {
      UiFramework.onUiVisibilityChanged.removeListener(
        handleUiVisibilityChanged
      );
    };
  }, []);
  return uiIsVisible;
}
const activateDroppedTab = (reducer) => (state, action) => {
  if (action.type === "WIDGET_TAB_DRAG_END") {
    const draggedTabId = action.id;
    state = reducer(state, action);
    const widget2 = getWidgetByTabId(state, draggedTabId);
    state = setWidgetActiveTabId(state, widget2.id, draggedTabId);
  } else if (action.type === "WIDGET_DRAG_END") {
    const draggedWidget = getWidgetState$1(state, action.floatingWidgetId);
    state = reducer(state, action);
    const widget2 = getWidgetByTabId(state, draggedWidget.activeTabId);
    state = setWidgetActiveTabId(state, widget2.id, draggedWidget.activeTabId);
  } else {
    state = reducer(state, action);
  }
  return state;
};
function getWidgetByTabId(state, draggedWidgetTabId) {
  const location = getTabLocation(state, draggedWidgetTabId);
  const widget2 = state.widgets[location.widgetId];
  return widget2;
}
function FloatingTab() {
  const { id, position } = useLayout((state) => {
    const draggedTab = state.draggedTab;
    return {
      id: draggedTab == null ? void 0 : draggedTab.tabId,
      position: draggedTab == null ? void 0 : draggedTab.position
    };
  }, true);
  const { iconSpec, label } = useLayout((state) => {
    var _a2;
    const tabId = (_a2 = state.draggedTab) == null ? void 0 : _a2.tabId;
    const tab2 = tabId ? state.tabs[tabId] : void 0;
    return {
      iconSpec: tab2 == null ? void 0 : tab2.iconSpec,
      label: tab2 == null ? void 0 : tab2.label
    };
  }, true);
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const onDrag = reactExports.useCallback(
    (dragBy) => {
      id && dispatch({
        type: "WIDGET_TAB_DRAG",
        dragBy
      });
    },
    [dispatch, id]
  );
  const onDragEnd = reactExports.useCallback(
    (target) => {
      id && dispatch({
        type: "WIDGET_TAB_DRAG_END",
        id,
        target
      });
    },
    [dispatch, id]
  );
  useDragTab({
    tabId: id || "",
    onDrag,
    onDragEnd
  });
  const showWidgetIcon = reactExports.useContext(ShowWidgetIconContext);
  const style = position && {
    transform: `translate(${position.x}px, ${position.y}px)`
  };
  const className = classnames(
    "nz-widget-floatingTab",
    !position && "nz-hidden"
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, style, children: [
    showWidgetIcon && iconSpec && /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: label })
  ] });
}
try {
  FloatingTab.displayName = "FloatingTab";
  FloatingTab.__docgenInfo = { "description": "Component that displays a floating tab.", "displayName": "FloatingTab", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function FloatingWidgets() {
  const ids = useLayout((state) => state.floatingWidgets.allIds);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    ids.map((id) => {
      return /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingWidgetProvider, { id }, id);
    }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingTab, {})
  ] });
}
try {
  FloatingWidgets.displayName = "FloatingWidgets";
  FloatingWidgets.__docgenInfo = { "description": "This component renders all floating widgets.", "displayName": "FloatingWidgets", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function useUiStateStorageHandler() {
  return reactExports.useContext(UiStateStorageContext);
}
const UiStateStorageContext = reactExports.createContext(
  new LocalStateStorage()
);
UiStateStorageContext.displayName = "uifw:UiStateStorageContext";
function UiStateStorageHandler(props) {
  const [stateStorage, setStateStorage] = reactExports.useState(
    UiFramework.getUiStateStorage()
  );
  reactExports.useEffect(() => {
    const handleSyncUiEvent = (args) => {
      if (SyncUiEventDispatcher.hasEventOfInterest(args.eventIds, [
        SyncUiEventId.UiStateStorageChanged
      ]))
        setStateStorage(UiFramework.getUiStateStorage());
    };
    return SyncUiEventDispatcher.onSyncUiEvent.addListener(handleSyncUiEvent);
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    UiStateStorageContext.Provider,
    {
      children: props.children,
      value: stateStorage
    }
  );
}
try {
  uifw:
    UiStateStorageContext.displayName = "uifw:UiStateStorageContext";
  uifw:
    UiStateStorageContext.__docgenInfo = { "description": "Allows to provide a custom [[UiStateStorage]] implementation to persist UI settings\nthrough [[UiFramework.setUiStateStorage]].\n\nThis component should wrap the [[ConfigurableUiContent]].\n\n```tsx\n<UiStateStorageHandler>\n  <ConfigurableUiContent />\n</UiStateStorageHandler>\n```", "displayName": "uifw:UiStateStorageContext", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function useTransientState(onSave, onRestore) {
  const tabId = reactExports.useContext(TabIdContext);
  return useTabTransientState(tabId, onSave, onRestore);
}
try {
  useTransientState.displayName = "useTransientState";
  useTransientState.__docgenInfo = { "description": "Hook that allows to save and restore transient DOM state (i.e. scroll offset) of a widget.", "displayName": "useTransientState", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function ScrollableWidgetContent(props) {
  const scrollPosition = reactExports.useRef(new Point());
  const ref = reactExports.useRef(null);
  const onSave = reactExports.useCallback(() => {
    if (!ref.current)
      return;
    scrollPosition.current = new Point(
      ref.current.scrollLeft,
      ref.current.scrollTop
    );
  }, []);
  const onRestore = reactExports.useCallback(() => {
    if (!ref.current)
      return;
    ref.current.scrollLeft = scrollPosition.current.x;
    ref.current.scrollTop = scrollPosition.current.y;
  }, []);
  useTransientState(onSave, onRestore);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      "data-item-id": props.itemId,
      "data-item-provider-id": props.providerId,
      "data-item-type": "widget-content",
      className: "nz-widget-content",
      ref,
      children: props.children
    }
  );
}
try {
  ScrollableWidgetContent.displayName = "ScrollableWidgetContent";
  ScrollableWidgetContent.__docgenInfo = { "description": "Component that enables widget content scrolling.", "displayName": "ScrollableWidgetContent", "props": { "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "providerId": { "defaultValue": null, "description": "", "name": "providerId", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetFallback() {
  const { translate } = useTranslation();
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { role: "alert", style: { position: "relative", minHeight: 400 }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    NonIdealState,
    {
      svg: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgError, {}),
      heading: translate("widget.errorMessage.unknownError")
    }
  ) });
}
function WidgetContent() {
  const widget2 = useWidgetDef();
  const itemId = (widget2 == null ? void 0 : widget2.id) ?? (widget2 == null ? void 0 : widget2.label) ?? "unknown";
  const onSave = reactExports.useCallback(() => {
    widget2 == null ? void 0 : widget2.saveTransientState();
  }, [widget2]);
  const onRestore = reactExports.useCallback(() => {
    widget2 == null ? void 0 : widget2.restoreTransientState();
  }, [widget2]);
  useTransientState(onSave, onRestore);
  const providerId = (widget2 == null ? void 0 : widget2.initialConfig) && isProviderItem(widget2 == null ? void 0 : widget2.initialConfig) ? widget2 == null ? void 0 : widget2.initialConfig.providerId : void 0;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollableWidgetContent, { itemId, providerId, children: /* @__PURE__ */ jsxRuntimeExports.jsx($44d7e150ebc754d2$export$e926676385687eaf, { FallbackComponent: WidgetFallback, children: widget2 == null ? void 0 : widget2.reactNode }) });
}
function useWidgetDef() {
  const tabId = reactExports.useContext(TabIdContext);
  const frontstage = useActiveFrontstageDef();
  const [widgetDef, setWidgetDef] = reactExports.useState(
    () => frontstage == null ? void 0 : frontstage.findWidgetDef(tabId)
  );
  reactExports.useEffect(() => {
    return InternalFrontstageManager.onFrontstageNineZoneStateChangedEvent.addListener(
      (args) => {
        if (args.frontstageDef !== frontstage || !frontstage || frontstage.isStageClosing || frontstage.isApplicationClosing)
          return;
        setWidgetDef(frontstage.findWidgetDef(tabId));
      }
    );
  }, [frontstage, tabId]);
  reactExports.useEffect(() => {
    return UiItemsManager.onUiProviderRegisteredEvent.addListener(() => {
      setWidgetDef(frontstage == null ? void 0 : frontstage.findWidgetDef(tabId));
    });
  }, [frontstage, tabId]);
  return widgetDef;
}
function FloatingWidget() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    FloatingWidget$1,
    {
      onMouseEnter: UiFramework.visibility.handleWidgetMouseEnter
    }
  );
}
function WidgetPanelsFrontstageContent() {
  const frontstageDef = useActiveFrontstageDef();
  if (!frontstageDef || !frontstageDef.contentLayoutDef || !frontstageDef.contentGroup)
    return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ContentLayout,
    {
      contentLayout: frontstageDef.contentLayoutDef,
      contentGroup: frontstageDef.contentGroup
    }
  );
}
class BackButton extends reactExports.PureComponent {
  render() {
    const { className, ...props } = this.props;
    const buttonClassName = classnames("nz-toolbar-button-back", className);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(ToolbarIcon, { className: buttonClassName, ...props });
  }
}
try {
  BackButton.displayName = "BackButton";
  BackButton.__docgenInfo = { "description": "Back button which displays icon. Used in [[Toolbar]] component.", "displayName": "BackButton", "props": { "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "title": { "defaultValue": null, "description": "Tooltip for button", "name": "title", "required": false, "type": { "name": "string" } }, "onClick": { "defaultValue": null, "description": "Function called when the button is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "small": { "defaultValue": null, "description": "Indicates whether to use a small App button", "name": "small", "required": false, "type": { "name": "boolean" } }, "icon": { "defaultValue": null, "description": "Button icon.", "name": "icon", "required": false, "type": { "name": "ReactNode" } }, "mouseProximity": { "defaultValue": null, "description": "Mouse proximity to button", "name": "mouseProximity", "required": false, "type": { "name": "number" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class ModalFrontstage extends reactExports.Component {
  constructor(props) {
    super(props);
    this._onGoBack = () => {
      if (this.props.navigateBack)
        this.props.navigateBack();
      this.props.closeModal();
    };
  }
  render() {
    const classNames = classnames(
      "uifw-modal-frontstage",
      this.props.isOpen && "uifw-modal-open",
      this.props.className
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: classNames, style: this.props.style, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "uifw-modal-app-bar", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            BackButton,
            {
              className: "nz-toolbar-button-app",
              onClick: this._onGoBack,
              icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgProgressBackwardCircular, {}) }),
              title: UiFramework.translate("modalFrontstage.backButtonTitle")
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { variant: "headline", className: "uifw-headline", children: this.props.title }),
          this.props.appBarRight && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "uifw-modal-app-bar-right", children: this.props.appBarRight })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-modal-stage-content", children: this.props.children })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-modal-frontstage-overlay" })
    ] });
  }
}
try {
  ModalFrontstage.displayName = "ModalFrontstage";
  ModalFrontstage.__docgenInfo = { "description": "ModalFrontstage React component", "displayName": "ModalFrontstage", "props": { "title": { "defaultValue": null, "description": "Title displayed at the top of the modal Frontstage", "name": "title", "required": true, "type": { "name": "string" } }, "isOpen": { "defaultValue": null, "description": "Indicates whether the modal Frontstage is open", "name": "isOpen", "required": false, "type": { "name": "boolean" } }, "navigateBack": { "defaultValue": null, "description": "Callback for navigating back from the modal Frontstage. This is normally connected to Redux.", "name": "navigateBack", "required": false, "type": { "name": "(() => any)" } }, "closeModal": { "defaultValue": null, "description": "Callback for closing the modal Frontstage. This is normally connected to Redux.", "name": "closeModal", "required": true, "type": { "name": "() => any" } }, "appBarRight": { "defaultValue": null, "description": "An optional React node displayed in the upper right of the modal Frontstage.", "name": "appBarRight", "required": false, "type": { "name": "ReactNode" } }, "children": { "defaultValue": null, "description": "Content", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function useActiveModalFrontstageInfo() {
  const [activeModalFrontstageInfo, setActiveModalFrontstageInfo] = reactExports.useState(UiFramework.frontstages.activeModalFrontstage);
  reactExports.useEffect(() => {
    const handleModalFrontstageChangedEvent = (args) => {
      setActiveModalFrontstageInfo(
        args.modalFrontstageCount === 0 ? void 0 : UiFramework.frontstages.activeModalFrontstage
      );
    };
    UiFramework.frontstages.onModalFrontstageChangedEvent.addListener(
      handleModalFrontstageChangedEvent
    );
    return () => {
      UiFramework.frontstages.onModalFrontstageChangedEvent.removeListener(
        handleModalFrontstageChangedEvent
      );
    };
  }, [setActiveModalFrontstageInfo]);
  return activeModalFrontstageInfo;
}
function ModalFrontstageComposer({
  stageInfo
}) {
  const handleCloseModal = reactExports.useCallback(
    /* istanbul ignore next */
    () => UiFramework.frontstages.closeModalFrontstage(),
    []
  );
  if (!stageInfo)
    return null;
  const { title, content, appBarRight } = stageInfo;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ModalFrontstage,
    {
      isOpen: true,
      title,
      closeModal: handleCloseModal,
      appBarRight,
      children: content
    }
  );
}
try {
  ModalFrontstageComposer.displayName = "ModalFrontstageComposer";
  ModalFrontstageComposer.__docgenInfo = { "description": "", "displayName": "ModalFrontstageComposer", "props": { "stageInfo": { "defaultValue": null, "description": "", "name": "stageInfo", "required": true, "type": { "name": "ModalFrontstageInfo | undefined" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetPanelsStatusBar(props) {
  const frontstageDef = useActiveFrontstageDef();
  const widgetDef = frontstageDef == null ? void 0 : frontstageDef.statusBar;
  if (!widgetDef)
    return null;
  const content = widgetDef.reactNode;
  const widgetControl = content === void 0 ? widgetDef.getWidgetControl(
    ConfigurableUiControlType.StatusBarWidget
  ) : void 0;
  const className = classnames("uifw-widgetPanels-statusBar", props.className);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    StatusBar,
    {
      className,
      style: props.style,
      widgetControl,
      children: widgetDef.reactNode
    }
  );
}
try {
  WidgetPanelsStatusBar.displayName = "WidgetPanelsStatusBar";
  WidgetPanelsStatusBar.__docgenInfo = { "description": "", "displayName": "WidgetPanelsStatusBar", "props": { "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetPanelsTab() {
  const widgetDef = useWidgetDef();
  const badgeType = widgetDef == null ? void 0 : widgetDef.badgeType;
  const badgeClassName = getBadgeClassName(badgeType);
  const badge = reactExports.useMemo(
    () => BadgeUtilities.getComponentForBadgeType(badgeType),
    [badgeType]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetTab, { className: badgeClassName, badge });
}
function getBadgeClassName(badgeType) {
  if (badgeType === BadgeType.New)
    return "uifw-badge-new";
  else if (badgeType === BadgeType.TechnicalPreview)
    return "uifw-badge-tp";
  return void 0;
}
try {
  getBadgeClassName.displayName = "getBadgeClassName";
  getBadgeClassName.__docgenInfo = { "description": "", "displayName": "getBadgeClassName", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetPanelsToolbars() {
  var _a2, _b2;
  const frontstageDef = useActiveFrontstageDef();
  const tools2 = (_a2 = frontstageDef == null ? void 0 : frontstageDef.contentManipulation) == null ? void 0 : _a2.reactNode;
  const navigation = (_b2 = frontstageDef == null ? void 0 : frontstageDef.viewNavigation) == null ? void 0 : _b2.reactNode;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "uifw-widgetPanels-toolbars", children: [
    tools2,
    navigation
  ] });
}
function DockedToolSetting(props) {
  const { onResize } = useToolSettingsEntry();
  const ref = useResizeObserver(onResize);
  const className = classnames("nz-toolSettings-setting", props.className);
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className, ref, style: props.style, children: props.children });
}
try {
  DockedToolSetting.displayName = "DockedToolSetting";
  DockedToolSetting.__docgenInfo = { "description": "Used in a [[DockedToolSettings]] component to display a setting.", "displayName": "DockedToolSetting", "props": { "children": { "defaultValue": null, "description": "Tool setting content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function useActiveToolId() {
  const [toolId, setToolId] = reactExports.useState(
    UiFramework.frontstages.activeToolId
  );
  reactExports.useEffect(() => {
    return UiFramework.frontstages.onToolActivatedEvent.addListener((args) => {
      setToolId(args.toolId);
    });
  }, []);
  return toolId;
}
function EmptyToolSettingsLabel({ toolId }) {
  const { translate } = useTranslation();
  const tool = IModelApp.tools.find(toolId);
  const toolName = tool == null ? void 0 : tool.flyover;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "uif-toolsetting-label-docked-horizontal-empty", children: [
    translate("tools.noToolSettingsStart"),
    toolName ? toolName : translate("tools.noToolSettingsPlaceholderName"),
    translate("tools.noToolSettingsEnd")
  ] });
}
function TsLabel({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uif-toolsetting-label-docked-horizontal", children });
}
function useShouldRenderDockedToolSettings() {
  return useLayout((state) => {
    const toolSettings = state.toolSettings;
    if (!toolSettings)
      return false;
    if (toolSettings.type === "widget")
      return false;
    return !toolSettings.hidden;
  });
}
function WidgetPanelsToolSettings() {
  if (!useShouldRenderDockedToolSettings())
    return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ToolSettingsDockedContent, {});
}
function ToolSettingsDockedContent() {
  var _a2;
  const activeToolId = useActiveToolId();
  const toolSettingEntries = useHorizontalToolSettingEntries();
  const forceRefreshKey = useRefreshKey(toolSettingEntries);
  const emptySettings = reactExports.useMemo(
    () => [
      {
        editorNode: /* @__PURE__ */ jsxRuntimeExports.jsx("div", {}),
        labelNode: /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyToolSettingsLabel, { toolId: activeToolId })
      }
    ],
    [activeToolId]
  );
  const entries = !toolSettingEntries || toolSettingEntries.length === 0 ? emptySettings : toolSettingEntries;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    DockedToolSettings,
    {
      itemId: ((_a2 = InternalFrontstageManager.activeToolSettingsProvider) == null ? void 0 : _a2.uniqueId) ?? "none",
      children: entries && entries.map((entry, index) => /* @__PURE__ */ jsxRuntimeExports.jsxs(DockedToolSetting, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(TsLabel, { children: entry.labelNode }),
        entry.editorNode
      ] }, index))
    },
    forceRefreshKey
  );
}
function useHorizontalToolSettingEntries() {
  var _a2;
  reactExports.useEffect(() => {
    var _a3, _b2;
    (_b2 = (_a3 = UiFramework.frontstages.activeToolInformation) == null ? void 0 : _a3.toolUiProvider) == null ? void 0 : _b2.reloadPropertiesFromTool();
  }, []);
  const [settings2, setSettings] = reactExports.useState(
    (_a2 = InternalFrontstageManager.activeToolSettingsProvider) == null ? void 0 : _a2.horizontalToolSettingNodes
  );
  reactExports.useEffect(() => {
    return UiFramework.frontstages.onToolActivatedEvent.addListener(() => {
      var _a3;
      const nodes = (_a3 = InternalFrontstageManager.activeToolSettingsProvider) == null ? void 0 : _a3.horizontalToolSettingNodes;
      setSettings(nodes);
    });
  }, []);
  reactExports.useEffect(() => {
    return UiFramework.frontstages.onToolSettingsReloadEvent.addListener(() => {
      var _a3;
      const nodes = (_a3 = InternalFrontstageManager.activeToolSettingsProvider) == null ? void 0 : _a3.horizontalToolSettingNodes;
      setSettings(nodes);
    });
  }, []);
  return settings2;
}
function ToolSettingsGrid({ settings: settings2 }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-standard-toolsettings-two-column-grid", children: settings2 && settings2.map((setting, index) => {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(reactExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "uifw-standard-toolsettings-label-entry", children: setting.labelNode }),
      setting.editorNode
    ] }, index);
  }) });
}
function useToolSettingsNode() {
  var _a2;
  reactExports.useEffect(() => {
    var _a3, _b2;
    (_b2 = (_a3 = UiFramework.frontstages.activeToolInformation) == null ? void 0 : _a3.toolUiProvider) == null ? void 0 : _b2.reloadPropertiesFromTool();
  }, []);
  const [settings2, setSettings] = reactExports.useState(
    (_a2 = InternalFrontstageManager.activeToolSettingsProvider) == null ? void 0 : _a2.toolSettingsNode
  );
  reactExports.useEffect(() => {
    return UiFramework.frontstages.onToolActivatedEvent.addListener(() => {
      var _a3;
      const nodes = (_a3 = InternalFrontstageManager.activeToolSettingsProvider) == null ? void 0 : _a3.toolSettingsNode;
      setSettings(nodes);
    });
  }, [setSettings]);
  reactExports.useEffect(() => {
    return UiFramework.frontstages.onToolSettingsReloadEvent.addListener(() => {
      var _a3;
      const nodes = (_a3 = InternalFrontstageManager.activeToolSettingsProvider) == null ? void 0 : _a3.toolSettingsNode;
      setSettings(nodes);
    });
  }, [setSettings]);
  return settings2;
}
function ToolSettingsContent() {
  const toolSettingsType = useLayout((state) => {
    var _a2;
    return (_a2 = state.toolSettings) == null ? void 0 : _a2.type;
  });
  if (!toolSettingsType || toolSettingsType === "docked")
    return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ToolSettingsWidgetContent, {});
}
function ToolSettingsWidgetContent() {
  var _a2;
  const floatingToolSettingsContainerRef = reactExports.useRef(null);
  const node = useToolSettingsNode();
  const activeToolId = useActiveToolId();
  const forceRefreshKey = useRefreshKey(node);
  const providerId = ((_a2 = InternalFrontstageManager.activeToolSettingsProvider) == null ? void 0 : _a2.uniqueId) ?? "none";
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      "data-toolsettings-provider": providerId,
      className: "uifw-floating-toolsettings-container",
      ref: floatingToolSettingsContainerRef,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollableWidgetContent, { children: node ?? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyToolSettingsLabel, { toolId: activeToolId }) })
    },
    forceRefreshKey
  );
}
function useRefreshKey(toolSettingNodes) {
  const [forceRefreshKey, setForceRefreshKey] = reactExports.useState(Date.now());
  reactExports.useEffect(() => {
    setForceRefreshKey(Date.now());
  }, [toolSettingNodes]);
  return forceRefreshKey;
}
try {
  useShouldRenderDockedToolSettings.displayName = "useShouldRenderDockedToolSettings";
  useShouldRenderDockedToolSettings.__docgenInfo = { "description": "Hook that returns true if the tool settings should be rendered docked.", "displayName": "useShouldRenderDockedToolSettings", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ToolSettingsGrid.displayName = "ToolSettingsGrid";
  ToolSettingsGrid.__docgenInfo = { "description": "Component that arranges an array of ToolSettingsEntry items into a two column grid layout.\nThe left column is considered the label column, the right column is considered the property\neditor column.", "displayName": "ToolSettingsGrid", "props": { "settings": { "defaultValue": null, "description": "", "name": "settings", "required": false, "type": { "name": "ToolSettingsEntry[]" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function useContentAlwaysMaxSize() {
  const { contentAlwaysMaxSize } = usePreviewFeatures();
  return {
    "uifw-preview-contentAlwaysMaxSize": contentAlwaysMaxSize
  };
}
function useHorizontalPanelAlignment(side) {
  const { enabled, alignments } = reactExports.useContext(HorizontalPanelAlignContext);
  return enabled ? {
    attributes: {
      "data-preview-horizontal-panel-align-top": alignments.top,
      "data-preview-horizontal-panel-align-bottom": alignments.bottom
    },
    classNames: {
      [`uifw-preview-horizontalPanelAlignment_${side}`]: true
    }
  } : {};
}
try {
  useHorizontalPanelAlignment.displayName = "useHorizontalPanelAlignment";
  useHorizontalPanelAlignment.__docgenInfo = { "description": "", "displayName": "useHorizontalPanelAlignment", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function usePanelsAutoCollapse() {
  const layoutStore = useLayoutStore$1();
  const panelsRef = reactExports.useRef(layoutStore.getState().panels);
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  const autoCollapseUnpinnedPanels = reactExports.useContext(
    AutoCollapseUnpinnedPanelsContext
  );
  reactExports.useEffect(
    () => layoutStore.subscribe((state) => {
      panelsRef.current = state.panels;
    }),
    [layoutStore]
  );
  const collapsePanels = reactExports.useCallback(() => {
    for (const side of panelSides) {
      const panel = panelsRef.current[side];
      if (panel.collapsed || panel.pinned)
        continue;
      dispatch({
        type: "PANEL_SET_COLLAPSED",
        collapsed: true,
        side: panel.side
      });
    }
  }, [dispatch]);
  const mouseDownRef = useRefEffect(
    (instance) => {
      if (!instance)
        return;
      instance.addEventListener("mousedown", collapsePanels, true);
      return () => {
        instance.removeEventListener("mousedown", collapsePanels, true);
      };
    },
    [collapsePanels]
  );
  const mouseEnterRef = useRefEffect(
    (instance) => {
      if (!instance || !autoCollapseUnpinnedPanels)
        return;
      instance.addEventListener("mouseenter", collapsePanels, true);
      return () => {
        instance.removeEventListener("mouseenter", collapsePanels, true);
      };
    },
    [collapsePanels, autoCollapseUnpinnedPanels]
  );
  const ref = useRefs(mouseDownRef, mouseEnterRef);
  return ref;
}
function StandardLayout(props) {
  const pinned = usePinnedPanels();
  const appContentRef = usePanelsAutoCollapse();
  const className = classnames("nz-standardLayout", pinned, props.className);
  const contentAlwaysMaxSize = useContentAlwaysMaxSize();
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, style: props.style, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: classnames("nz-appContent", contentAlwaysMaxSize),
        ref: appContentRef,
        children: props.children
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-centerContent", children: props.centerContent }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Panel, { side: "left", children: props.leftPanel }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Panel, { side: "right", children: props.rightPanel }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Panel, { side: "top", children: props.topPanel }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Panel, { side: "bottom", children: props.bottomPanel }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-toolSettings", children: props.toolSettings }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-statusBar", children: props.statusBar })
  ] });
}
function Panel({ children, side }) {
  const maximizedWidget = useMaximizedPanelLayout(side);
  const horizontalAlignment = useHorizontalPanelAlignment(side);
  const className = classnames(
    `nz-standardLayout_${side}Panel`,
    maximizedWidget,
    horizontalAlignment.classNames
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className, ...horizontalAlignment.attributes, children });
}
function usePinnedPanels() {
  const leftPinned = useLayout((state) => state.panels.left.pinned);
  const rightPinned = useLayout((state) => state.panels.right.pinned);
  const topPinned = useLayout((state) => state.panels.top.pinned);
  const bottomPinned = useLayout((state) => state.panels.bottom.pinned);
  return {
    "nz-pinned-left": leftPinned,
    "nz-pinned-right": rightPinned,
    "nz-pinned-top": topPinned,
    "nz-pinned-bottom": bottomPinned
  };
}
try {
  StandardLayout.displayName = "StandardLayout";
  StandardLayout.__docgenInfo = { "description": "Component that displays widgets in a standard layout.", "displayName": "StandardLayout", "props": { "children": { "defaultValue": null, "description": "Main content area of the application (i.e. viewport) that will change bounds based on panel pinned state.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "centerContent": { "defaultValue": null, "description": "Component that displays center content (i.e. toolbars). Content is always bound by widget panels.", "name": "centerContent", "required": false, "type": { "name": "ReactNode" } }, "toolSettings": { "defaultValue": null, "description": "", "name": "toolSettings", "required": false, "type": { "name": "ReactNode" } }, "statusBar": { "defaultValue": null, "description": "", "name": "statusBar", "required": false, "type": { "name": "ReactNode" } }, "topPanel": { "defaultValue": null, "description": "", "name": "topPanel", "required": false, "type": { "name": "ReactNode" } }, "leftPanel": { "defaultValue": null, "description": "", "name": "leftPanel", "required": false, "type": { "name": "ReactNode" } }, "rightPanel": { "defaultValue": null, "description": "", "name": "rightPanel", "required": false, "type": { "name": "ReactNode" } }, "bottomPanel": { "defaultValue": null, "description": "", "name": "bottomPanel", "required": false, "type": { "name": "ReactNode" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetPanelExpander({ side }) {
  const timer = reactExports.useRef(new Timer(200));
  const lastPosition = reactExports.useRef(new Point());
  const dispatch = reactExports.useContext(NineZoneDispatchContext);
  reactExports.useEffect(() => {
    timer.current.setOnExecute(() => {
      dispatch({
        side,
        collapsed: false,
        type: "PANEL_SET_COLLAPSED"
      });
    });
  }, [side, dispatch]);
  const className = classnames("nz-widgetPanels-expander", `nz-${side}`);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className,
      onMouseOverCapture: (e) => {
        lastPosition.current = new Point(e.clientX, e.clientY);
        timer.current.start();
      },
      onMouseOutCapture: () => {
        timer.current.stop();
      },
      onMouseMoveCapture: (e) => {
        const newPosition = new Point(e.clientX, e.clientY);
        if (lastPosition.current.getDistanceTo(newPosition) > 5) {
          timer.current.start();
          lastPosition.current = newPosition;
        }
      }
    }
  );
}
function WidgetPanelExpanders() {
  const expanders = useLayout((state) => {
    const panels = state.panels;
    return panelSides.filter((side) => {
      const panel = panels[side];
      return !panel.pinned && panel.collapsed;
    });
  }, true);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(jsxRuntimeExports.Fragment, { children: expanders.map((side) => {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelExpander, { side }, side);
  }) });
}
try {
  WidgetPanelExpander.displayName = "WidgetPanelExpander";
  WidgetPanelExpander.__docgenInfo = { "description": "Component that is used to auto-expand unpinned widget panel.", "displayName": "WidgetPanelExpander", "props": { "side": { "defaultValue": null, "description": "", "name": "side", "required": true, "type": { "name": "enum", "value": [{ "value": '"top"' }, { "value": '"left"' }, { "value": '"right"' }, { "value": '"bottom"' }] } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function WidgetPanelsFrontstageComponent() {
  const activeModalFrontstageInfo = useActiveModalFrontstageInfo();
  const uiIsVisible = useUiVisibility();
  const previewFeatures = usePreviewFeatures();
  useCursor();
  return /* @__PURE__ */ jsxRuntimeExports.jsx(MaximizedWidgetProvider, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    PreviewHorizontalPanelAlignFeatureProvider,
    {
      enabled: previewFeatures.horizontalPanelAlignment,
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs(ToolbarPopupAutoHideContext.Provider, { value: !uiIsVisible, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(ModalFrontstageComposer, { stageInfo: activeModalFrontstageInfo }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          StandardLayout,
          {
            centerContent: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelsToolbars, {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelExpanders, {})
            ] }),
            toolSettings: /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelsToolSettings, {}),
            statusBar: /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelsStatusBar, {}),
            topPanel: /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelProvider, { side: "top" }),
            leftPanel: /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelProvider, { side: "left" }),
            rightPanel: /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelProvider, { side: "right" }),
            bottomPanel: /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelProvider, { side: "bottom" }),
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelsFrontstageContent, {})
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetContentRenderers, {}),
        /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingWidgets, {})
      ] })
    }
  ) });
}
const widgetContent = /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetContent, {});
const toolSettingsContent = /* @__PURE__ */ jsxRuntimeExports.jsx(ToolSettingsContent, {});
const widgetPanelsFrontstage = /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelsFrontstageComponent, {});
function useLayoutStore(frontstageDef) {
  const layoutStore = reactExports.useMemo(
    () => createLayoutStore(frontstageDef == null ? void 0 : frontstageDef.nineZoneState),
    [frontstageDef]
  );
  reactExports.useEffect(() => {
    return InternalFrontstageManager.onFrontstageNineZoneStateChangedEvent.addListener(
      (args) => {
        if (args.frontstageDef !== frontstageDef)
          return;
        reactDomExports.unstable_batchedUpdates(() => {
          layoutStore.setState(args.state || defaultNineZone);
        });
      }
    );
  }, [frontstageDef, layoutStore]);
  return layoutStore;
}
function useUpdateNineZoneSize(frontstageDef) {
  reactExports.useEffect(() => {
    const size = InternalFrontstageManager.nineZoneSize;
    if (!size)
      return;
    frontstageDef.dispatch({
      type: "RESIZE",
      size: {
        height: size.height,
        width: size.width
      }
    });
  }, [frontstageDef]);
}
const log = (reducer) => (state, action) => {
  Logger.logTrace("appui-react:reducer", `action ${action.type}`, () => ({
    action,
    state
  }));
  return reducer(state, action);
};
function useNineZoneDispatch(frontstageDef) {
  const features = usePreviewFeatures();
  const reducer = reactExports.useMemo(() => {
    let nineZoneStateReducer = log(NineZoneStateReducer);
    nineZoneStateReducer = features.activateDroppedTab ? activateDroppedTab(nineZoneStateReducer) : nineZoneStateReducer;
    return nineZoneStateReducer;
  }, [features.activateDroppedTab]);
  return reactExports.useCallback(
    (action) => {
      if (action.type === "RESIZE") {
        InternalFrontstageManager.nineZoneSize = Size.create(action.size);
      }
      const state = frontstageDef.nineZoneState;
      if (!state)
        return;
      frontstageDef.nineZoneState = reducer(state, action);
    },
    [frontstageDef, reducer]
  );
}
function WidgetPanelsFrontstage() {
  const frontstageDef = useActiveFrontstageDef();
  reactExports.useEffect(() => {
    const triggerWidowCloseProcessing = () => {
      frontstageDef && frontstageDef.setIsApplicationClosing(true);
    };
    window.addEventListener("unload", triggerWidowCloseProcessing);
    return () => {
      window.removeEventListener("unload", triggerWidowCloseProcessing);
    };
  }, [frontstageDef]);
  if (!frontstageDef)
    return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ActiveFrontstageDefProvider, { frontstageDef });
}
const defaultNineZone = createNineZoneState();
const tabElement = /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelsTab, {});
const floatingWidgetElement = /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingWidget, {});
function ActiveFrontstageDefProvider({
  frontstageDef
}) {
  const dispatch = useNineZoneDispatch(frontstageDef);
  frontstageDef.dispatch = dispatch;
  const layout = useLayoutStore(frontstageDef);
  useUpdateNineZoneSize(frontstageDef);
  useSavedFrontstageState(frontstageDef);
  useSaveFrontstageSettings(frontstageDef, layout);
  useItemsManager(frontstageDef);
  const labels = useLabels();
  const uiIsVisible = useUiVisibility();
  const showWidgetIcon = useSelector((state) => {
    const frameworkState = state[UiFramework.frameworkStateKey];
    return !!frameworkState.configurableUiState.showWidgetIcon;
  });
  const autoCollapseUnpinnedPanels = useSelector(
    (state) => {
      const frameworkState = state[UiFramework.frameworkStateKey];
      return !!frameworkState.configurableUiState.autoCollapseUnpinnedPanels;
    }
  );
  const animateToolSettings = useSelector((state) => {
    const frameworkState = state[UiFramework.frameworkStateKey];
    return !!frameworkState.configurableUiState.animateToolSettings;
  });
  const useToolAsToolSettingsLabel = useSelector(
    (state) => {
      const frameworkState = state[UiFramework.frameworkStateKey];
      return !!frameworkState.configurableUiState.useToolAsToolSettingsLabel;
    }
  );
  useFrontstageManager(frontstageDef, useToolAsToolSettingsLabel);
  const handleKeyDown = useEscapeSetFocusToHome();
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className: "uifw-widgetPanels-frontstage",
      onKeyDown: handleKeyDown,
      role: "presentation",
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        NineZone,
        {
          dispatch,
          labels,
          layout,
          tab: tabElement,
          floatingWidget: floatingWidgetElement,
          showWidgetIcon,
          autoCollapseUnpinnedPanels,
          toolSettingsContent,
          widgetContent,
          animateDockedToolSettings: animateToolSettings,
          uiIsVisible,
          children: widgetPanelsFrontstage
        }
      )
    }
  );
}
function useLabels() {
  const { translate } = useTranslation();
  return {
    dockToolSettingsTitle: translate("widget.tooltips.dockToolSettings"),
    moreWidgetsTitle: translate("widget.tooltips.moreWidgets"),
    moreToolSettingsTitle: translate("widget.tooltips.moreToolSettings"),
    pinPanelTitle: translate("widget.tooltips.pinPanel"),
    resizeGripTitle: translate("widget.tooltips.resizeGrip"),
    sendWidgetHomeTitle: translate("widget.tooltips.sendHome"),
    toolSettingsHandleTitle: translate("widget.tooltips.toolSettingsHandle"),
    unpinPanelTitle: translate("widget.tooltips.unpinPanel"),
    popoutActiveTab: translate("widget.tooltips.popoutActiveTab")
  };
}
function toTabArgs(widget2) {
  var _a2;
  const label = getWidgetLabel(widget2.label);
  const args = {
    allowedPanelTargets: (_a2 = widget2.allowedPanelTargets) == null ? void 0 : _a2.map(
      (location) => toPanelSide(location)
    ),
    hideWithUiWhenFloating: !!widget2.hideWithUiWhenFloating,
    canPopout: widget2.canPopout,
    iconSpec: widget2.iconSpec,
    isFloatingWidgetResizable: widget2.isFloatingStateWindowResizable,
    label,
    preferredFloatingWidgetSize: widget2.defaultFloatingSize,
    preferredPanelWidgetSize: widget2.preferredPanelSize
  };
  return args;
}
function addWidgetDef(frontstageDef, widgetDef, location, section) {
  const side = toPanelSide(location);
  const isFloatingWidget = widgetDef.isFloatingStateSupported && widgetDef.defaultState === WidgetState.Floating;
  const floatingWidgetId = widgetDef.floatingContainerId ? widgetDef.floatingContainerId : getUniqueId();
  const userSized = !!widgetDef.defaultFloatingSize;
  frontstageDef.dispatch({
    type: "WIDGET_DEF_ADD",
    id: widgetDef.id,
    overrides: {
      ...toTabArgs(widgetDef),
      userSized
    },
    location: isFloatingWidget ? "floating" : "panel",
    floatingWidget: {
      id: floatingWidgetId,
      preferredPosition: widgetDef.defaultFloatingPosition
    },
    panelSection: {
      id: getWidgetPanelSectionId(side, section),
      side,
      index: section
    }
  });
  hideWidgetDef(frontstageDef, widgetDef);
}
function addPanelSectionWidgetDefs(frontstageDef, location, section) {
  const panelDef = frontstageDef.getStagePanelDef(location);
  if (!panelDef)
    return;
  const sectionDef = panelDef.getPanelSectionDef(section);
  const widgetDefs = determineNewWidgets(frontstageDef, sectionDef.widgetDefs);
  for (const widgetDef of widgetDefs) {
    addWidgetDef(frontstageDef, widgetDef, location, section);
  }
}
function processPopoutWidgets(frontstageDef) {
  if (ProcessDetector.isElectronAppFrontend) {
    return;
  }
  assert(!!frontstageDef.nineZoneState);
  const state = frontstageDef.nineZoneState;
  for (const id of state.popoutWidgets.allIds) {
    frontstageDef.dispatch({
      type: "POPOUT_WIDGET_SEND_BACK",
      id
    });
  }
}
function addFrontstageWidgetDefs(frontstageDef) {
  [
    StagePanelLocation.Left,
    StagePanelLocation.Right,
    StagePanelLocation.Top,
    StagePanelLocation.Bottom
  ].forEach(
    (location) => [StagePanelSection.Start, StagePanelSection.End].forEach(
      (section) => addPanelSectionWidgetDefs(frontstageDef, location, section)
    )
  );
  const toolSettingsWidgetDefs = determineNewWidgets(
    frontstageDef,
    frontstageDef.toolSettings ? [frontstageDef.toolSettings] : void 0
  );
  for (const toolSettingsWidgetDef of toolSettingsWidgetDefs) {
    frontstageDef.dispatch({
      type: "WIDGET_DEF_ADD_TOOL_SETTINGS",
      id: toolSettingsWidgetDef.id,
      overrides: toTabArgs(toolSettingsWidgetDef)
    });
  }
}
function hideWidgetDefs(frontstageDef) {
  assert(!!frontstageDef.nineZoneState);
  const state = frontstageDef.nineZoneState;
  const tabs = Object.values(state.tabs);
  for (const tab2 of tabs) {
    const widgetDef = frontstageDef.findWidgetDef(tab2.id);
    if (!widgetDef)
      continue;
    hideWidgetDef(frontstageDef, widgetDef);
  }
}
function hideWidgetDef(frontstageDef, widgetDef) {
  if (widgetDef.defaultState === WidgetState.Hidden) {
    frontstageDef.dispatch({
      type: "WIDGET_TAB_HIDE",
      id: widgetDef.id
    });
  }
  if (widgetDef.defaultState === WidgetState.Unloaded) {
    frontstageDef.dispatch({
      type: "WIDGET_TAB_UNLOAD",
      id: widgetDef.id
    });
  }
}
function removeMissingWidgets(frontstageDef) {
  var _a2;
  const state = frontstageDef.nineZoneState;
  if (!state)
    return;
  const toolSettingsTabId = (_a2 = state.toolSettings) == null ? void 0 : _a2.tabId;
  for (const [, tab2] of Object.entries(state.tabs)) {
    if (tab2.id === toolSettingsTabId)
      continue;
    const widgetDef = frontstageDef.findWidgetDef(tab2.id);
    if (widgetDef)
      continue;
    frontstageDef.dispatch({
      type: "WIDGET_TAB_REMOVE",
      id: tab2.id
    });
  }
}
function getWidgetLabel(label) {
  return label === "" ? "Widget" : label;
}
function isFrontstageStateSettingResult(settingsResult) {
  if (settingsResult.status === UiStateStorageStatus.Success)
    return true;
  return false;
}
function initializePanel(frontstageDef, location) {
  var _a2, _b2, _c2, _d, _e, _f;
  const side = toPanelSide(location);
  const panelDef = frontstageDef.getStagePanelDef(location);
  if (!panelDef)
    return;
  const size = ((_a2 = panelDef.initialConfig) == null ? void 0 : _a2.sizeSpec) ?? ((_b2 = panelDef.initialConfig) == null ? void 0 : _b2.size);
  size !== void 0 && frontstageDef.dispatch({
    type: "PANEL_SET_SIZE",
    side,
    size
  });
  frontstageDef.dispatch({
    type: "PANEL_SET_PINNED",
    side,
    pinned: panelDef.defaultPinned
  });
  frontstageDef.dispatch({
    type: "PANEL_SET_RESIZABLE",
    side,
    resizable: panelDef.defaultResizable
  });
  const minSize = ((_c2 = panelDef.initialConfig) == null ? void 0 : _c2.minSizeSpec) ?? ((_d = panelDef.initialConfig) == null ? void 0 : _d.minSize);
  minSize !== void 0 && frontstageDef.dispatch({
    type: "PANEL_SET_MIN_SIZE",
    side,
    minSize
  });
  const maxSize = ((_e = panelDef.initialConfig) == null ? void 0 : _e.maxSizeSpec) ?? ((_f = panelDef.initialConfig) == null ? void 0 : _f.maxSize);
  maxSize !== void 0 && frontstageDef.dispatch({
    type: "PANEL_SET_MAX_SIZE",
    side,
    maxSize
  });
  const collapsed = panelDef.defaultState !== StagePanelState.Open;
  frontstageDef.dispatch({
    type: "PANEL_SET_COLLAPSED",
    side,
    collapsed
  });
}
const stateVersion = 17;
function initializeNineZoneState(frontstageDef) {
  frontstageDef.batch(() => {
    frontstageDef.nineZoneState = defaultNineZone;
    const size = InternalFrontstageManager.nineZoneSize;
    size && frontstageDef.dispatch({
      type: "RESIZE",
      size
    });
    addFrontstageWidgetDefs(frontstageDef);
    initializePanel(frontstageDef, StagePanelLocation.Left);
    initializePanel(frontstageDef, StagePanelLocation.Right);
    initializePanel(frontstageDef, StagePanelLocation.Top);
    initializePanel(frontstageDef, StagePanelLocation.Bottom);
  });
}
function restoreNineZoneState(frontstageDef, packed) {
  frontstageDef.batch(() => {
    frontstageDef.nineZoneState = packed;
    for (const [, tab2] of Object.entries(packed.tabs)) {
      const widgetDef = frontstageDef.findWidgetDef(tab2.id);
      if (!widgetDef) {
        Logger.logInfo(
          UiFramework.loggerCategory(restoreNineZoneState),
          "WidgetDef is not found for saved tab.",
          {
            frontstageId: frontstageDef.id,
            tabId: tab2.id
          }
        );
        frontstageDef.dispatch({ type: "WIDGET_TAB_REMOVE", id: tab2.id });
        continue;
      }
      frontstageDef.dispatch({
        type: "WIDGET_TAB_UPDATE",
        id: tab2.id,
        overrides: toTabArgs(widgetDef)
      });
    }
    frontstageDef.nineZoneState = fn(
      frontstageDef.nineZoneState,
      (draft) => {
        const oldLeftMiddleIndex = packed.panels.left.widgets.findIndex(
          (value) => value === "leftMiddle"
        );
        if (-1 !== oldLeftMiddleIndex) {
          draft.panels.left.widgets = packed.panels.left.widgets.filter(
            (value) => value !== "leftMiddle"
          );
          if ("leftEnd" in draft.widgets) {
            draft.widgets.leftMiddle.tabs.forEach(
              (tab2) => draft.widgets.leftEnd.tabs.push(tab2)
            );
          } else {
            draft.widgets.leftEnd = { ...draft.widgets.leftMiddle };
            delete draft.widgets.leftMiddle;
          }
        }
        const oldRightMiddleIndex = packed.panels.right.widgets.findIndex(
          (value) => value === "rightMiddle"
        );
        if (-1 !== oldRightMiddleIndex) {
          draft.panels.right.widgets = packed.panels.right.widgets.filter(
            (value) => value !== "rightMiddle"
          );
          if ("rightEnd" in draft.widgets) {
            draft.widgets.rightMiddle.tabs.forEach(
              (tab2) => draft.widgets.rightEnd.tabs.push(tab2)
            );
          } else {
            draft.widgets.rightEnd = { ...draft.widgets.rightMiddle };
            delete draft.widgets.rightMiddle;
          }
        }
        return;
      }
    );
    if (InternalFrontstageManager.nineZoneSize && (0 !== InternalFrontstageManager.nineZoneSize.height || 0 !== InternalFrontstageManager.nineZoneSize.width)) {
      frontstageDef.dispatch({
        type: "RESIZE",
        size: {
          height: InternalFrontstageManager.nineZoneSize.height,
          width: InternalFrontstageManager.nineZoneSize.width
        }
      });
    }
    addFrontstageWidgetDefs(frontstageDef);
    hideWidgetDefs(frontstageDef);
    processPopoutWidgets(frontstageDef);
  });
}
function packNineZoneState(state) {
  let packed = state;
  packed = fn(packed, (draft) => {
    for (const [, tab2] of Object.entries(draft.tabs)) {
      tab2.label = "";
      delete tab2.iconSpec;
    }
  });
  return packed;
}
function useSavedFrontstageState(frontstageDef) {
  const uiStateStorage = useUiStateStorageHandler();
  const uiStateStorageRef = reactExports.useRef(uiStateStorage);
  const isMountedRef = reactExports.useRef(false);
  reactExports.useEffect(() => {
    isMountedRef.current = true;
    return () => {
      isMountedRef.current = false;
    };
  }, []);
  reactExports.useEffect(() => {
    uiStateStorageRef.current = uiStateStorage;
  }, [uiStateStorage]);
  reactExports.useEffect(() => {
    async function fetchFrontstageState() {
      if (frontstageDef.nineZoneState) {
        processPopoutWidgets(frontstageDef);
        return;
      }
      const id = frontstageDef.id;
      const version = frontstageDef.version;
      const settingResult = await uiStateStorageRef.current.getSetting(
        FRONTSTAGE_SETTINGS_NAMESPACE,
        getFrontstageStateSettingName(id)
      );
      if (!isMountedRef.current)
        return;
      if (isFrontstageStateSettingResult(settingResult)) {
        const setting = settingResult.setting;
        if (setting.version === version && setting.stateVersion === stateVersion) {
          restoreNineZoneState(frontstageDef, setting.nineZone);
          return;
        }
      }
      initializeNineZoneState(frontstageDef);
    }
    void fetchFrontstageState();
  }, [frontstageDef]);
}
function useSaveFrontstageSettings(frontstageDef, store) {
  const uiSettingsStorage = useUiStateStorageHandler();
  const pendingSave = reactExports.useRef(() => {
  });
  const saveSetting = reactExports.useMemo(() => {
    const debounced = debounce(
      async (frontstage, state) => {
        const id = frontstage.id;
        const setting = {
          id,
          version: frontstage.version,
          stateVersion,
          nineZone: packNineZoneState(state)
        };
        await uiSettingsStorage.saveSetting(
          FRONTSTAGE_SETTINGS_NAMESPACE,
          getFrontstageStateSettingName(id),
          setting
        );
      },
      1e3
    );
    const save = (frontstage, state) => {
      if (state.draggedTab)
        return;
      debounced(frontstage, state);
    };
    save.cancel = debounced.cancel;
    pendingSave.current = debounced.immediate;
    return save;
  }, [uiSettingsStorage]);
  reactExports.useEffect(() => {
    return () => {
      pendingSave.current();
    };
  }, [frontstageDef]);
  reactExports.useEffect(() => {
    return () => {
      saveSetting.cancel();
    };
  }, [saveSetting]);
  reactExports.useEffect(() => {
    saveSetting(frontstageDef, store.getState());
  }, [saveSetting, frontstageDef, store]);
  reactExports.useEffect(() => {
    return store.subscribe(() => {
      saveSetting(frontstageDef, store.getState());
    });
  }, [saveSetting, frontstageDef, store]);
}
const FRONTSTAGE_SETTINGS_NAMESPACE = "uifw-frontstageSettings";
function getFrontstageStateSettingName(frontstageId) {
  return `frontstageState[${frontstageId}]`;
}
function debounce(func, duration) {
  let timeout;
  let handler;
  const debounced = (...args) => {
    handler = () => {
      timeout = void 0;
      return func(...args);
    };
    window.clearTimeout(timeout);
    timeout = window.setTimeout(handler, duration);
  };
  debounced.cancel = () => {
    window.clearTimeout(timeout);
    timeout = void 0;
  };
  debounced.immediate = () => {
    if (timeout === void 0)
      return;
    debounced.cancel();
    handler == null ? void 0 : handler();
  };
  return debounced;
}
function useFrontstageManager(frontstageDef, useToolAsToolSettingsLabel) {
  const { translate } = useTranslation();
  const uiSettingsStorage = useUiStateStorageHandler();
  reactExports.useEffect(() => {
    return InternalFrontstageManager.onFrontstageRestoreLayoutEvent.addListener(
      (args) => {
        void uiSettingsStorage.deleteSetting(
          FRONTSTAGE_SETTINGS_NAMESPACE,
          getFrontstageStateSettingName(args.frontstageDef.id)
        );
        if (frontstageDef.id === args.frontstageDef.id) {
          initializeNineZoneState(frontstageDef);
          return;
        }
        args.frontstageDef.nineZoneState = void 0;
      }
    );
  }, [uiSettingsStorage, frontstageDef]);
  const defaultLabel = translate("widget.labels.toolSettings");
  reactExports.useEffect(() => {
    const updateLabel = () => {
      var _a2, _b2;
      const state = frontstageDef.nineZoneState;
      if (!state)
        return;
      const toolSettingsTabId = (_a2 = state.toolSettings) == null ? void 0 : _a2.tabId;
      if (!toolSettingsTabId)
        return;
      const toolId = UiFramework.frontstages.activeToolId;
      const label = useToolAsToolSettingsLabel ? ((_b2 = IModelApp.tools.find(toolId)) == null ? void 0 : _b2.flyover) || defaultLabel : defaultLabel;
      frontstageDef.dispatch({
        type: "WIDGET_TAB_SET_LABEL",
        id: toolSettingsTabId,
        label
      });
    };
    updateLabel();
    const removeListenerFuncs = [];
    if (useToolAsToolSettingsLabel) {
      removeListenerFuncs.push(
        UiFramework.frontstages.onFrontstageReadyEvent.addListener(updateLabel),
        InternalFrontstageManager.onFrontstageRestoreLayoutEvent.addListener(
          updateLabel
        ),
        UiFramework.frontstages.onToolActivatedEvent.addListener(updateLabel),
        UiFramework.frontstages.onToolSettingsReloadEvent.addListener(
          updateLabel
        )
      );
    }
    return () => {
      removeListenerFuncs.forEach((removeListener) => removeListener());
    };
  }, [frontstageDef, useToolAsToolSettingsLabel, defaultLabel]);
}
function refreshNineZoneState(frontstageDef) {
  frontstageDef.updateWidgetDefs();
  frontstageDef.batch(() => {
    addFrontstageWidgetDefs(frontstageDef);
    removeMissingWidgets(frontstageDef);
  });
}
function useItemsManager(frontstageDef) {
  reactExports.useEffect(() => {
    if (!frontstageDef.nineZoneState)
      return;
    refreshNineZoneState(frontstageDef);
  }, [frontstageDef]);
  reactExports.useEffect(() => {
    return UiItemsManager.onUiProviderRegisteredEvent.addListener(() => {
      refreshNineZoneState(frontstageDef);
    });
  }, [frontstageDef]);
}
function determineNewWidgets(frontstageDef, defs) {
  const state = frontstageDef.nineZoneState;
  defs = defs || [];
  const uniqueDefs = defs.filter((def, index, array) => {
    return index === array.findIndex((def1) => def.id === def1.id);
  });
  return uniqueDefs.filter((def) => !(def.id in state.tabs));
}
try {
  useLayoutStore.displayName = "useLayoutStore";
  useLayoutStore.__docgenInfo = { "description": "", "displayName": "useLayoutStore", "props": { "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "usage": { "defaultValue": null, "description": "", "name": "usage", "required": true, "type": { "name": "string" } }, "version": { "defaultValue": null, "description": "", "name": "version", "required": true, "type": { "name": "number" } }, "contentGroupProvider": { "defaultValue": null, "description": "", "name": "contentGroupProvider", "required": true, "type": { "name": "ContentGroupProvider | undefined" } }, "floatingContentControls": { "defaultValue": null, "description": "", "name": "floatingContentControls", "required": true, "type": { "name": "ContentControl[] | undefined" } }, "toolSettings": { "defaultValue": null, "description": "", "name": "toolSettings", "required": true, "type": { "name": "WidgetDef | undefined" } }, "statusBar": { "defaultValue": null, "description": "", "name": "statusBar", "required": true, "type": { "name": "WidgetDef | undefined" } }, "contentManipulation": { "defaultValue": null, "description": "", "name": "contentManipulation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "viewNavigation": { "defaultValue": null, "description": "", "name": "viewNavigation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "topPanel": { "defaultValue": null, "description": "", "name": "topPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "leftPanel": { "defaultValue": null, "description": "", "name": "leftPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "rightPanel": { "defaultValue": null, "description": "", "name": "rightPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "bottomPanel": { "defaultValue": null, "description": "", "name": "bottomPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "contentLayoutDef": { "defaultValue": null, "description": "", "name": "contentLayoutDef", "required": true, "type": { "name": "ContentLayoutDef | undefined" } }, "contentGroup": { "defaultValue": null, "description": "", "name": "contentGroup", "required": true, "type": { "name": "ContentGroup | undefined" } }, "frontstageProvider": { "defaultValue": null, "description": "", "name": "frontstageProvider", "required": true, "type": { "name": "FrontstageProvider | undefined" } }, "nineZoneState": { "defaultValue": null, "description": "@internal", "name": "nineZoneState", "required": true, "type": { "name": "NineZoneState | undefined" } }, "dispatch": { "defaultValue": null, "description": "@internal", "name": "dispatch", "required": true, "type": { "name": "NineZoneDispatch" } }, "batch": { "defaultValue": null, "description": "Dispatch multiple actions inside `fn`, but trigger events once.\n@internal", "name": "batch", "required": true, "type": { "name": "(fn: () => void) => void" } }, "timeTracker": { "defaultValue": null, "description": "@internal", "name": "timeTracker", "required": true, "type": { "name": "TimeTracker" } }, "onActivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes activated", "name": "onActivated", "required": true, "type": { "name": "() => Promise<void>" } }, "onDeactivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes inactive", "name": "onDeactivated", "required": true, "type": { "name": "() => Promise<void>" } }, "setIsApplicationClosing": { "defaultValue": null, "description": "@internal", "name": "setIsApplicationClosing", "required": true, "type": { "name": "(value: boolean) => void" } }, "waitUntilReady": { "defaultValue": null, "description": "Returns once the contained widgets and content controls are ready to use", "name": "waitUntilReady", "required": true, "type": { "name": "() => Promise<void>" } }, "onFrontstageReady": { "defaultValue": null, "description": "Handles when the Frontstage becomes active", "name": "onFrontstageReady", "required": true, "type": { "name": "() => void" } }, "setContentLayoutAndGroup": { "defaultValue": null, "description": "Sets the Content Layout and Content Group", "name": "setContentLayoutAndGroup", "required": true, "type": { "name": "(contentLayoutDef: ContentLayoutDef, contentGroup: ContentGroup) => void" } }, "setActiveContent": { "defaultValue": null, "description": "Sets the active view content control to the default or first", "name": "setActiveContent", "required": true, "type": { "name": "() => Promise<boolean>" } }, "setActiveView": { "defaultValue": null, "description": "Sets the active view content control", "name": "setActiveView", "required": true, "type": { "name": "(newContent: ContentControl, oldContent?: ContentControl | undefined) => void" } }, "setActiveViewFromViewport": { "defaultValue": null, "description": "Sets the active view content control based on the selected viewport.", "name": "setActiveViewFromViewport", "required": true, "type": { "name": "(viewport: ScreenViewport) => boolean" } }, "getStagePanelDef": { "defaultValue": null, "description": "Gets a [[StagePanelDef]] based on a given panel location\n@beta", "name": "getStagePanelDef", "required": true, "type": { "name": "(location: StagePanelLocation) => StagePanelDef | undefined" } }, "panelDefs": { "defaultValue": null, "description": "Gets a list of [[StagePanelDef]]s\n@beta", "name": "panelDefs", "required": true, "type": { "name": "StagePanelDef[]" } }, "findWidgetDef": { "defaultValue": null, "description": "Finds a [[WidgetDef]] based on a given id", "name": "findWidgetDef", "required": true, "type": { "name": "(id: string) => WidgetDef | undefined" } }, "addFloatingContentControl": { "defaultValue": null, "description": "", "name": "addFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "dropFloatingContentControl": { "defaultValue": null, "description": "", "name": "dropFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "contentControls": { "defaultValue": null, "description": "Gets the list of [[ContentControl]]s", "name": "contentControls", "required": true, "type": { "name": "ContentControl[]" } }, "initializeFromConfig": { "defaultValue": null, "description": "Initializes a FrontstageDef from FrontstageConfig.\n@internal", "name": "initializeFromConfig", "required": true, "type": { "name": "(config: FrontstageConfig) => Promise<void>" } }, "updateWidgetDefs": { "defaultValue": null, "description": "@internal", "name": "updateWidgetDefs", "required": true, "type": { "name": "() => void" } }, "restoreLayout": { "defaultValue": null, "description": "Restores frontstage layout to initial configuration.\n@beta", "name": "restoreLayout", "required": true, "type": { "name": "() => void" } }, "isPopoutWidget": { "defaultValue": null, "description": "", "name": "isPopoutWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "isFloatingWidget": { "defaultValue": null, "description": "", "name": "isFloatingWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "floatWidget": { "defaultValue": null, "description": "Create a new floating panel that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:50, y:100} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:400, width:400} is used.\n@beta", "name": "floatWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isWidgetDisplayed": { "defaultValue": null, "description": "Check widget and panel state to determine whether the widget is currently displayed\n@param widgetId case-sensitive Widget Id\n@public", "name": "isWidgetDisplayed", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "openPopoutWidgetContainer": { "defaultValue": null, "description": "Opens window for specified PopoutWidget container. Used to reopen popout when running in Electron.\n@internal", "name": "openPopoutWidgetContainer", "required": true, "type": { "name": "(widgetContainerId: string, oldState: NineZoneState | undefined) => boolean" } }, "popoutWidget": { "defaultValue": null, "description": "Create a new popout/child window that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:0, y:0} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:800, width:600} is used.\n@beta", "name": "popoutWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isStageClosing": { "defaultValue": null, "description": "", "name": "isStageClosing", "required": true, "type": { "name": "boolean" } }, "isApplicationClosing": { "defaultValue": null, "description": "", "name": "isApplicationClosing", "required": true, "type": { "name": "boolean" } }, "isReady": { "defaultValue": null, "description": "", "name": "isReady", "required": true, "type": { "name": "boolean" } }, "saveChildWindowSizeAndPosition": { "defaultValue": null, "description": "@internal", "name": "saveChildWindowSizeAndPosition", "required": true, "type": { "name": "(childWindowId: string, childWindow: ChildWindow) => void" } }, "dockWidgetContainerByContainerId": { "defaultValue": null, "description": "@internal", "name": "dockWidgetContainerByContainerId", "required": true, "type": { "name": "(widgetContainerId: string) => void" } }, "dockWidgetContainer": { "defaultValue": null, "description": "Finds the container with the specified widget and re-docks all widgets\nback to the panel zone location that was used when the floating container\nwas generated.\n@param widgetId case sensitive Widget Id.\n@beta", "name": "dockWidgetContainer", "required": true, "type": { "name": "(widgetId: string) => void" } }, "setFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "setFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string, bounds: RectangleProps) => boolean" } }, "getFloatingWidgetContainerIds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIds", "required": true, "type": { "name": "() => string[]" } }, "getFloatingWidgetContainerIdByWidgetId": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIdByWidgetId", "required": true, "type": { "name": "(widgetId: string) => string | undefined" } }, "getFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string | undefined) => RectangleProps | undefined" } }, "widgetDefs": { "defaultValue": null, "description": "Iterable of all widget definitions in a frontstage.\n@internal", "name": "widgetDefs", "required": true, "type": { "name": "{ [Symbol.iterator](): Iterator<WidgetDef, any, undefined>; }" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useUpdateNineZoneSize.displayName = "useUpdateNineZoneSize";
  useUpdateNineZoneSize.__docgenInfo = { "description": "Update in-memory NineZoneState of newly activated frontstage with up to date size.", "displayName": "useUpdateNineZoneSize", "props": { "_id": { "defaultValue": null, "description": "", "name": "_id", "required": true, "type": { "name": "string" } }, "_initialConfig": { "defaultValue": null, "description": "", "name": "_initialConfig", "required": false, "type": { "name": "FrontstageConfig" } }, "_isStageClosing": { "defaultValue": null, "description": "", "name": "_isStageClosing", "required": true, "type": { "name": "boolean" } }, "_isReady": { "defaultValue": null, "description": "", "name": "_isReady", "required": true, "type": { "name": "boolean" } }, "_isApplicationClosing": { "defaultValue": null, "description": "", "name": "_isApplicationClosing", "required": true, "type": { "name": "boolean" } }, "_usage": { "defaultValue": null, "description": "", "name": "_usage", "required": false, "type": { "name": "string" } }, "_version": { "defaultValue": null, "description": "", "name": "_version", "required": true, "type": { "name": "number" } }, "_toolSettings": { "defaultValue": null, "description": "", "name": "_toolSettings", "required": false, "type": { "name": "WidgetDef" } }, "_statusBar": { "defaultValue": null, "description": "", "name": "_statusBar", "required": false, "type": { "name": "WidgetDef" } }, "_contentManipulation": { "defaultValue": null, "description": "", "name": "_contentManipulation", "required": false, "type": { "name": "WidgetDef" } }, "_viewNavigation": { "defaultValue": null, "description": "", "name": "_viewNavigation", "required": false, "type": { "name": "WidgetDef" } }, "_topPanel": { "defaultValue": null, "description": "", "name": "_topPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_leftPanel": { "defaultValue": null, "description": "", "name": "_leftPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_rightPanel": { "defaultValue": null, "description": "", "name": "_rightPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_bottomPanel": { "defaultValue": null, "description": "", "name": "_bottomPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_contentLayoutDef": { "defaultValue": null, "description": "", "name": "_contentLayoutDef", "required": false, "type": { "name": "ContentLayoutDef" } }, "_contentGroup": { "defaultValue": null, "description": "", "name": "_contentGroup", "required": false, "type": { "name": "ContentGroup" } }, "_frontstageProvider": { "defaultValue": null, "description": "", "name": "_frontstageProvider", "required": false, "type": { "name": "FrontstageProvider" } }, "_timeTracker": { "defaultValue": null, "description": "", "name": "_timeTracker", "required": true, "type": { "name": "TimeTracker" } }, "_nineZoneState": { "defaultValue": null, "description": "", "name": "_nineZoneState", "required": false, "type": { "name": "NineZoneState" } }, "_contentGroupProvider": { "defaultValue": null, "description": "", "name": "_contentGroupProvider", "required": false, "type": { "name": "ContentGroupProvider" } }, "_floatingContentControls": { "defaultValue": null, "description": "", "name": "_floatingContentControls", "required": false, "type": { "name": "ContentControl[]" } }, "_toolAdminDefaultToolId": { "defaultValue": null, "description": "", "name": "_toolAdminDefaultToolId", "required": false, "type": { "name": "string" } }, "_dispatch": { "defaultValue": null, "description": "", "name": "_dispatch", "required": false, "type": { "name": "NineZoneDispatch" } }, "_batching": { "defaultValue": null, "description": "", "name": "_batching", "required": true, "type": { "name": "boolean" } }, "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "usage": { "defaultValue": null, "description": "", "name": "usage", "required": true, "type": { "name": "string" } }, "version": { "defaultValue": null, "description": "", "name": "version", "required": true, "type": { "name": "number" } }, "contentGroupProvider": { "defaultValue": null, "description": "", "name": "contentGroupProvider", "required": true, "type": { "name": "ContentGroupProvider | undefined" } }, "floatingContentControls": { "defaultValue": null, "description": "", "name": "floatingContentControls", "required": true, "type": { "name": "ContentControl[] | undefined" } }, "toolSettings": { "defaultValue": null, "description": "", "name": "toolSettings", "required": true, "type": { "name": "WidgetDef | undefined" } }, "statusBar": { "defaultValue": null, "description": "", "name": "statusBar", "required": true, "type": { "name": "WidgetDef | undefined" } }, "contentManipulation": { "defaultValue": null, "description": "", "name": "contentManipulation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "viewNavigation": { "defaultValue": null, "description": "", "name": "viewNavigation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "topPanel": { "defaultValue": null, "description": "", "name": "topPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "leftPanel": { "defaultValue": null, "description": "", "name": "leftPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "rightPanel": { "defaultValue": null, "description": "", "name": "rightPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "bottomPanel": { "defaultValue": null, "description": "", "name": "bottomPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "contentLayoutDef": { "defaultValue": null, "description": "", "name": "contentLayoutDef", "required": true, "type": { "name": "ContentLayoutDef | undefined" } }, "contentGroup": { "defaultValue": null, "description": "", "name": "contentGroup", "required": true, "type": { "name": "ContentGroup | undefined" } }, "frontstageProvider": { "defaultValue": null, "description": "", "name": "frontstageProvider", "required": true, "type": { "name": "FrontstageProvider | undefined" } }, "toStagePanelLocation": { "defaultValue": null, "description": "", "name": "toStagePanelLocation", "required": true, "type": { "name": "(side: PanelSide) => StagePanelLocation" } }, "populateStateMaps": { "defaultValue": null, "description": "", "name": "populateStateMaps", "required": true, "type": { "name": "(nineZone: NineZoneState) => { panelMap: Map<StagePanelDef, StagePanelState>; widgetMap: Map<WidgetDef, WidgetState>; }" } }, "triggerStateChangeEvents": { "defaultValue": null, "description": "", "name": "triggerStateChangeEvents", "required": true, "type": { "name": "(oldState: NineZoneState) => void" } }, "handlePopouts": { "defaultValue": null, "description": "", "name": "handlePopouts", "required": true, "type": { "name": "(oldState: NineZoneState | undefined) => boolean" } }, "nineZoneState": { "defaultValue": null, "description": "@internal", "name": "nineZoneState", "required": true, "type": { "name": "NineZoneState | undefined" } }, "dispatch": { "defaultValue": null, "description": "@internal", "name": "dispatch", "required": true, "type": { "name": "NineZoneDispatch" } }, "batch": { "defaultValue": null, "description": "Dispatch multiple actions inside `fn`, but trigger events once.\n@internal", "name": "batch", "required": true, "type": { "name": "(fn: () => void) => void" } }, "timeTracker": { "defaultValue": null, "description": "@internal", "name": "timeTracker", "required": true, "type": { "name": "TimeTracker" } }, "_onActivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes activated", "name": "_onActivated", "required": true, "type": { "name": "() => Promise<void>" } }, "onActivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes activated", "name": "onActivated", "required": true, "type": { "name": "() => Promise<void>" } }, "_onDeactivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes inactive", "name": "_onDeactivated", "required": true, "type": { "name": "() => Promise<void>" } }, "onDeactivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes inactive", "name": "onDeactivated", "required": true, "type": { "name": "() => Promise<void>" } }, "setIsApplicationClosing": { "defaultValue": null, "description": "@internal", "name": "setIsApplicationClosing", "required": true, "type": { "name": "(value: boolean) => void" } }, "waitUntilReady": { "defaultValue": null, "description": "Returns once the contained widgets and content controls are ready to use", "name": "waitUntilReady", "required": true, "type": { "name": "() => Promise<void>" } }, "_onFrontstageReady": { "defaultValue": null, "description": "Handles when the Frontstage becomes active", "name": "_onFrontstageReady", "required": true, "type": { "name": "() => void" } }, "onFrontstageReady": { "defaultValue": null, "description": "Handles when the Frontstage becomes active", "name": "onFrontstageReady", "required": true, "type": { "name": "() => void" } }, "setContentLayoutAndGroup": { "defaultValue": null, "description": "Sets the Content Layout and Content Group", "name": "setContentLayoutAndGroup", "required": true, "type": { "name": "(contentLayoutDef: ContentLayoutDef, contentGroup: ContentGroup) => void" } }, "setActiveContent": { "defaultValue": null, "description": "Sets the active view content control to the default or first", "name": "setActiveContent", "required": true, "type": { "name": "() => Promise<boolean>" } }, "setActiveView": { "defaultValue": null, "description": "Sets the active view content control", "name": "setActiveView", "required": true, "type": { "name": "(newContent: ContentControl, oldContent?: ContentControl | undefined) => void" } }, "setActiveViewFromViewport": { "defaultValue": null, "description": "Sets the active view content control based on the selected viewport.", "name": "setActiveViewFromViewport", "required": true, "type": { "name": "(viewport: ScreenViewport) => boolean" } }, "getStagePanelDef": { "defaultValue": null, "description": "Gets a [[StagePanelDef]] based on a given panel location\n@beta", "name": "getStagePanelDef", "required": true, "type": { "name": "(location: StagePanelLocation) => StagePanelDef | undefined" } }, "panelDefs": { "defaultValue": null, "description": "Gets a list of [[StagePanelDef]]s\n@beta", "name": "panelDefs", "required": true, "type": { "name": "StagePanelDef[]" } }, "findWidgetDef": { "defaultValue": null, "description": "Finds a [[WidgetDef]] based on a given id", "name": "findWidgetDef", "required": true, "type": { "name": "(id: string) => WidgetDef | undefined" } }, "_widgetControls": { "defaultValue": null, "description": "Gets the list of [[WidgetControl]]s", "name": "_widgetControls", "required": true, "type": { "name": "WidgetControl[]" } }, "addFloatingContentControl": { "defaultValue": null, "description": "", "name": "addFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "dropFloatingContentControl": { "defaultValue": null, "description": "", "name": "dropFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "contentControls": { "defaultValue": null, "description": "Gets the list of [[ContentControl]]s", "name": "contentControls", "required": true, "type": { "name": "ContentControl[]" } }, "initializeFromConfig": { "defaultValue": null, "description": "Initializes a FrontstageDef from FrontstageConfig.\n@internal", "name": "initializeFromConfig", "required": true, "type": { "name": "(config: FrontstageConfig) => Promise<void>" } }, "updateWidgetDefs": { "defaultValue": null, "description": "@internal", "name": "updateWidgetDefs", "required": true, "type": { "name": "() => void" } }, "restoreLayout": { "defaultValue": null, "description": "Restores frontstage layout to initial configuration.\n@beta", "name": "restoreLayout", "required": true, "type": { "name": "() => void" } }, "isPopoutWidget": { "defaultValue": null, "description": "", "name": "isPopoutWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "isFloatingWidget": { "defaultValue": null, "description": "", "name": "isFloatingWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "floatWidget": { "defaultValue": null, "description": "Create a new floating panel that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:50, y:100} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:400, width:400} is used.\n@beta", "name": "floatWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isWidgetDisplayed": { "defaultValue": null, "description": "Check widget and panel state to determine whether the widget is currently displayed\n@param widgetId case-sensitive Widget Id\n@public", "name": "isWidgetDisplayed", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "openPopoutWidgetContainer": { "defaultValue": null, "description": "Opens window for specified PopoutWidget container. Used to reopen popout when running in Electron.\n@internal", "name": "openPopoutWidgetContainer", "required": true, "type": { "name": "(widgetContainerId: string, oldState: NineZoneState | undefined) => boolean" } }, "popoutWidget": { "defaultValue": null, "description": "Create a new popout/child window that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:0, y:0} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:800, width:600} is used.\n@beta", "name": "popoutWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isStageClosing": { "defaultValue": null, "description": "", "name": "isStageClosing", "required": true, "type": { "name": "boolean" } }, "isApplicationClosing": { "defaultValue": null, "description": "", "name": "isApplicationClosing", "required": true, "type": { "name": "boolean" } }, "isReady": { "defaultValue": null, "description": "", "name": "isReady", "required": true, "type": { "name": "boolean" } }, "saveChildWindowSizeAndPosition": { "defaultValue": null, "description": "@internal", "name": "saveChildWindowSizeAndPosition", "required": true, "type": { "name": "(childWindowId: string, childWindow: ChildWindow) => void" } }, "dockWidgetContainerByContainerId": { "defaultValue": null, "description": "@internal", "name": "dockWidgetContainerByContainerId", "required": true, "type": { "name": "(widgetContainerId: string) => void" } }, "dockWidgetContainer": { "defaultValue": null, "description": "Finds the container with the specified widget and re-docks all widgets\nback to the panel zone location that was used when the floating container\nwas generated.\n@param widgetId case sensitive Widget Id.\n@beta", "name": "dockWidgetContainer", "required": true, "type": { "name": "(widgetId: string) => void" } }, "setFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "setFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string, bounds: RectangleProps) => boolean" } }, "getFloatingWidgetContainerIds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIds", "required": true, "type": { "name": "() => string[]" } }, "getFloatingWidgetContainerIdByWidgetId": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIdByWidgetId", "required": true, "type": { "name": "(widgetId: string) => string | undefined" } }, "getFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string | undefined) => RectangleProps | undefined" } }, "_widgetDefs": { "defaultValue": null, "description": "", "name": "_widgetDefs", "required": true, "type": { "name": "() => Iterator<WidgetDef, any, undefined>" } }, "widgetDefs": { "defaultValue": null, "description": "Iterable of all widget definitions in a frontstage.\n@internal", "name": "widgetDefs", "required": true, "type": { "name": "{ [Symbol.iterator](): Iterator<WidgetDef, any, undefined>; }" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useNineZoneDispatch.displayName = "useNineZoneDispatch";
  useNineZoneDispatch.__docgenInfo = { "description": "", "displayName": "useNineZoneDispatch", "props": { "_id": { "defaultValue": null, "description": "", "name": "_id", "required": true, "type": { "name": "string" } }, "_initialConfig": { "defaultValue": null, "description": "", "name": "_initialConfig", "required": false, "type": { "name": "FrontstageConfig" } }, "_isStageClosing": { "defaultValue": null, "description": "", "name": "_isStageClosing", "required": true, "type": { "name": "boolean" } }, "_isReady": { "defaultValue": null, "description": "", "name": "_isReady", "required": true, "type": { "name": "boolean" } }, "_isApplicationClosing": { "defaultValue": null, "description": "", "name": "_isApplicationClosing", "required": true, "type": { "name": "boolean" } }, "_usage": { "defaultValue": null, "description": "", "name": "_usage", "required": false, "type": { "name": "string" } }, "_version": { "defaultValue": null, "description": "", "name": "_version", "required": true, "type": { "name": "number" } }, "_toolSettings": { "defaultValue": null, "description": "", "name": "_toolSettings", "required": false, "type": { "name": "WidgetDef" } }, "_statusBar": { "defaultValue": null, "description": "", "name": "_statusBar", "required": false, "type": { "name": "WidgetDef" } }, "_contentManipulation": { "defaultValue": null, "description": "", "name": "_contentManipulation", "required": false, "type": { "name": "WidgetDef" } }, "_viewNavigation": { "defaultValue": null, "description": "", "name": "_viewNavigation", "required": false, "type": { "name": "WidgetDef" } }, "_topPanel": { "defaultValue": null, "description": "", "name": "_topPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_leftPanel": { "defaultValue": null, "description": "", "name": "_leftPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_rightPanel": { "defaultValue": null, "description": "", "name": "_rightPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_bottomPanel": { "defaultValue": null, "description": "", "name": "_bottomPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_contentLayoutDef": { "defaultValue": null, "description": "", "name": "_contentLayoutDef", "required": false, "type": { "name": "ContentLayoutDef" } }, "_contentGroup": { "defaultValue": null, "description": "", "name": "_contentGroup", "required": false, "type": { "name": "ContentGroup" } }, "_frontstageProvider": { "defaultValue": null, "description": "", "name": "_frontstageProvider", "required": false, "type": { "name": "FrontstageProvider" } }, "_timeTracker": { "defaultValue": null, "description": "", "name": "_timeTracker", "required": true, "type": { "name": "TimeTracker" } }, "_nineZoneState": { "defaultValue": null, "description": "", "name": "_nineZoneState", "required": false, "type": { "name": "NineZoneState" } }, "_contentGroupProvider": { "defaultValue": null, "description": "", "name": "_contentGroupProvider", "required": false, "type": { "name": "ContentGroupProvider" } }, "_floatingContentControls": { "defaultValue": null, "description": "", "name": "_floatingContentControls", "required": false, "type": { "name": "ContentControl[]" } }, "_toolAdminDefaultToolId": { "defaultValue": null, "description": "", "name": "_toolAdminDefaultToolId", "required": false, "type": { "name": "string" } }, "_dispatch": { "defaultValue": null, "description": "", "name": "_dispatch", "required": false, "type": { "name": "NineZoneDispatch" } }, "_batching": { "defaultValue": null, "description": "", "name": "_batching", "required": true, "type": { "name": "boolean" } }, "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "usage": { "defaultValue": null, "description": "", "name": "usage", "required": true, "type": { "name": "string" } }, "version": { "defaultValue": null, "description": "", "name": "version", "required": true, "type": { "name": "number" } }, "contentGroupProvider": { "defaultValue": null, "description": "", "name": "contentGroupProvider", "required": true, "type": { "name": "ContentGroupProvider | undefined" } }, "floatingContentControls": { "defaultValue": null, "description": "", "name": "floatingContentControls", "required": true, "type": { "name": "ContentControl[] | undefined" } }, "toolSettings": { "defaultValue": null, "description": "", "name": "toolSettings", "required": true, "type": { "name": "WidgetDef | undefined" } }, "statusBar": { "defaultValue": null, "description": "", "name": "statusBar", "required": true, "type": { "name": "WidgetDef | undefined" } }, "contentManipulation": { "defaultValue": null, "description": "", "name": "contentManipulation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "viewNavigation": { "defaultValue": null, "description": "", "name": "viewNavigation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "topPanel": { "defaultValue": null, "description": "", "name": "topPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "leftPanel": { "defaultValue": null, "description": "", "name": "leftPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "rightPanel": { "defaultValue": null, "description": "", "name": "rightPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "bottomPanel": { "defaultValue": null, "description": "", "name": "bottomPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "contentLayoutDef": { "defaultValue": null, "description": "", "name": "contentLayoutDef", "required": true, "type": { "name": "ContentLayoutDef | undefined" } }, "contentGroup": { "defaultValue": null, "description": "", "name": "contentGroup", "required": true, "type": { "name": "ContentGroup | undefined" } }, "frontstageProvider": { "defaultValue": null, "description": "", "name": "frontstageProvider", "required": true, "type": { "name": "FrontstageProvider | undefined" } }, "toStagePanelLocation": { "defaultValue": null, "description": "", "name": "toStagePanelLocation", "required": true, "type": { "name": "(side: PanelSide) => StagePanelLocation" } }, "populateStateMaps": { "defaultValue": null, "description": "", "name": "populateStateMaps", "required": true, "type": { "name": "(nineZone: NineZoneState) => { panelMap: Map<StagePanelDef, StagePanelState>; widgetMap: Map<WidgetDef, WidgetState>; }" } }, "triggerStateChangeEvents": { "defaultValue": null, "description": "", "name": "triggerStateChangeEvents", "required": true, "type": { "name": "(oldState: NineZoneState) => void" } }, "handlePopouts": { "defaultValue": null, "description": "", "name": "handlePopouts", "required": true, "type": { "name": "(oldState: NineZoneState | undefined) => boolean" } }, "nineZoneState": { "defaultValue": null, "description": "@internal", "name": "nineZoneState", "required": true, "type": { "name": "NineZoneState | undefined" } }, "dispatch": { "defaultValue": null, "description": "@internal", "name": "dispatch", "required": true, "type": { "name": "NineZoneDispatch" } }, "batch": { "defaultValue": null, "description": "Dispatch multiple actions inside `fn`, but trigger events once.\n@internal", "name": "batch", "required": true, "type": { "name": "(fn: () => void) => void" } }, "timeTracker": { "defaultValue": null, "description": "@internal", "name": "timeTracker", "required": true, "type": { "name": "TimeTracker" } }, "_onActivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes activated", "name": "_onActivated", "required": true, "type": { "name": "() => Promise<void>" } }, "onActivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes activated", "name": "onActivated", "required": true, "type": { "name": "() => Promise<void>" } }, "_onDeactivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes inactive", "name": "_onDeactivated", "required": true, "type": { "name": "() => Promise<void>" } }, "onDeactivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes inactive", "name": "onDeactivated", "required": true, "type": { "name": "() => Promise<void>" } }, "setIsApplicationClosing": { "defaultValue": null, "description": "@internal", "name": "setIsApplicationClosing", "required": true, "type": { "name": "(value: boolean) => void" } }, "waitUntilReady": { "defaultValue": null, "description": "Returns once the contained widgets and content controls are ready to use", "name": "waitUntilReady", "required": true, "type": { "name": "() => Promise<void>" } }, "_onFrontstageReady": { "defaultValue": null, "description": "Handles when the Frontstage becomes active", "name": "_onFrontstageReady", "required": true, "type": { "name": "() => void" } }, "onFrontstageReady": { "defaultValue": null, "description": "Handles when the Frontstage becomes active", "name": "onFrontstageReady", "required": true, "type": { "name": "() => void" } }, "setContentLayoutAndGroup": { "defaultValue": null, "description": "Sets the Content Layout and Content Group", "name": "setContentLayoutAndGroup", "required": true, "type": { "name": "(contentLayoutDef: ContentLayoutDef, contentGroup: ContentGroup) => void" } }, "setActiveContent": { "defaultValue": null, "description": "Sets the active view content control to the default or first", "name": "setActiveContent", "required": true, "type": { "name": "() => Promise<boolean>" } }, "setActiveView": { "defaultValue": null, "description": "Sets the active view content control", "name": "setActiveView", "required": true, "type": { "name": "(newContent: ContentControl, oldContent?: ContentControl | undefined) => void" } }, "setActiveViewFromViewport": { "defaultValue": null, "description": "Sets the active view content control based on the selected viewport.", "name": "setActiveViewFromViewport", "required": true, "type": { "name": "(viewport: ScreenViewport) => boolean" } }, "getStagePanelDef": { "defaultValue": null, "description": "Gets a [[StagePanelDef]] based on a given panel location\n@beta", "name": "getStagePanelDef", "required": true, "type": { "name": "(location: StagePanelLocation) => StagePanelDef | undefined" } }, "panelDefs": { "defaultValue": null, "description": "Gets a list of [[StagePanelDef]]s\n@beta", "name": "panelDefs", "required": true, "type": { "name": "StagePanelDef[]" } }, "findWidgetDef": { "defaultValue": null, "description": "Finds a [[WidgetDef]] based on a given id", "name": "findWidgetDef", "required": true, "type": { "name": "(id: string) => WidgetDef | undefined" } }, "_widgetControls": { "defaultValue": null, "description": "Gets the list of [[WidgetControl]]s", "name": "_widgetControls", "required": true, "type": { "name": "WidgetControl[]" } }, "addFloatingContentControl": { "defaultValue": null, "description": "", "name": "addFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "dropFloatingContentControl": { "defaultValue": null, "description": "", "name": "dropFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "contentControls": { "defaultValue": null, "description": "Gets the list of [[ContentControl]]s", "name": "contentControls", "required": true, "type": { "name": "ContentControl[]" } }, "initializeFromConfig": { "defaultValue": null, "description": "Initializes a FrontstageDef from FrontstageConfig.\n@internal", "name": "initializeFromConfig", "required": true, "type": { "name": "(config: FrontstageConfig) => Promise<void>" } }, "updateWidgetDefs": { "defaultValue": null, "description": "@internal", "name": "updateWidgetDefs", "required": true, "type": { "name": "() => void" } }, "restoreLayout": { "defaultValue": null, "description": "Restores frontstage layout to initial configuration.\n@beta", "name": "restoreLayout", "required": true, "type": { "name": "() => void" } }, "isPopoutWidget": { "defaultValue": null, "description": "", "name": "isPopoutWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "isFloatingWidget": { "defaultValue": null, "description": "", "name": "isFloatingWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "floatWidget": { "defaultValue": null, "description": "Create a new floating panel that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:50, y:100} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:400, width:400} is used.\n@beta", "name": "floatWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isWidgetDisplayed": { "defaultValue": null, "description": "Check widget and panel state to determine whether the widget is currently displayed\n@param widgetId case-sensitive Widget Id\n@public", "name": "isWidgetDisplayed", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "openPopoutWidgetContainer": { "defaultValue": null, "description": "Opens window for specified PopoutWidget container. Used to reopen popout when running in Electron.\n@internal", "name": "openPopoutWidgetContainer", "required": true, "type": { "name": "(widgetContainerId: string, oldState: NineZoneState | undefined) => boolean" } }, "popoutWidget": { "defaultValue": null, "description": "Create a new popout/child window that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:0, y:0} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:800, width:600} is used.\n@beta", "name": "popoutWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isStageClosing": { "defaultValue": null, "description": "", "name": "isStageClosing", "required": true, "type": { "name": "boolean" } }, "isApplicationClosing": { "defaultValue": null, "description": "", "name": "isApplicationClosing", "required": true, "type": { "name": "boolean" } }, "isReady": { "defaultValue": null, "description": "", "name": "isReady", "required": true, "type": { "name": "boolean" } }, "saveChildWindowSizeAndPosition": { "defaultValue": null, "description": "@internal", "name": "saveChildWindowSizeAndPosition", "required": true, "type": { "name": "(childWindowId: string, childWindow: ChildWindow) => void" } }, "dockWidgetContainerByContainerId": { "defaultValue": null, "description": "@internal", "name": "dockWidgetContainerByContainerId", "required": true, "type": { "name": "(widgetContainerId: string) => void" } }, "dockWidgetContainer": { "defaultValue": null, "description": "Finds the container with the specified widget and re-docks all widgets\nback to the panel zone location that was used when the floating container\nwas generated.\n@param widgetId case sensitive Widget Id.\n@beta", "name": "dockWidgetContainer", "required": true, "type": { "name": "(widgetId: string) => void" } }, "setFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "setFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string, bounds: RectangleProps) => boolean" } }, "getFloatingWidgetContainerIds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIds", "required": true, "type": { "name": "() => string[]" } }, "getFloatingWidgetContainerIdByWidgetId": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIdByWidgetId", "required": true, "type": { "name": "(widgetId: string) => string | undefined" } }, "getFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string | undefined) => RectangleProps | undefined" } }, "_widgetDefs": { "defaultValue": null, "description": "", "name": "_widgetDefs", "required": true, "type": { "name": "() => Iterator<WidgetDef, any, undefined>" } }, "widgetDefs": { "defaultValue": null, "description": "Iterable of all widget definitions in a frontstage.\n@internal", "name": "widgetDefs", "required": true, "type": { "name": "{ [Symbol.iterator](): Iterator<WidgetDef, any, undefined>; }" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  ActiveFrontstageDefProvider.displayName = "ActiveFrontstageDefProvider";
  ActiveFrontstageDefProvider.__docgenInfo = { "description": "", "displayName": "ActiveFrontstageDefProvider", "props": { "frontstageDef": { "defaultValue": null, "description": "", "name": "frontstageDef", "required": true, "type": { "name": "FrontstageDef" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  addPanelSectionWidgetDefs.displayName = "addPanelSectionWidgetDefs";
  addPanelSectionWidgetDefs.__docgenInfo = { "description": "Adds missing widgets to the layout.", "displayName": "addPanelSectionWidgetDefs", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  addFrontstageWidgetDefs.displayName = "addFrontstageWidgetDefs";
  addFrontstageWidgetDefs.__docgenInfo = { "description": "Adds frontstageDef widgets that are missing in NineZoneState.", "displayName": "addFrontstageWidgetDefs", "props": { "_id": { "defaultValue": null, "description": "", "name": "_id", "required": true, "type": { "name": "string" } }, "_initialConfig": { "defaultValue": null, "description": "", "name": "_initialConfig", "required": false, "type": { "name": "FrontstageConfig" } }, "_isStageClosing": { "defaultValue": null, "description": "", "name": "_isStageClosing", "required": true, "type": { "name": "boolean" } }, "_isReady": { "defaultValue": null, "description": "", "name": "_isReady", "required": true, "type": { "name": "boolean" } }, "_isApplicationClosing": { "defaultValue": null, "description": "", "name": "_isApplicationClosing", "required": true, "type": { "name": "boolean" } }, "_usage": { "defaultValue": null, "description": "", "name": "_usage", "required": false, "type": { "name": "string" } }, "_version": { "defaultValue": null, "description": "", "name": "_version", "required": true, "type": { "name": "number" } }, "_toolSettings": { "defaultValue": null, "description": "", "name": "_toolSettings", "required": false, "type": { "name": "WidgetDef" } }, "_statusBar": { "defaultValue": null, "description": "", "name": "_statusBar", "required": false, "type": { "name": "WidgetDef" } }, "_contentManipulation": { "defaultValue": null, "description": "", "name": "_contentManipulation", "required": false, "type": { "name": "WidgetDef" } }, "_viewNavigation": { "defaultValue": null, "description": "", "name": "_viewNavigation", "required": false, "type": { "name": "WidgetDef" } }, "_topPanel": { "defaultValue": null, "description": "", "name": "_topPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_leftPanel": { "defaultValue": null, "description": "", "name": "_leftPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_rightPanel": { "defaultValue": null, "description": "", "name": "_rightPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_bottomPanel": { "defaultValue": null, "description": "", "name": "_bottomPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_contentLayoutDef": { "defaultValue": null, "description": "", "name": "_contentLayoutDef", "required": false, "type": { "name": "ContentLayoutDef" } }, "_contentGroup": { "defaultValue": null, "description": "", "name": "_contentGroup", "required": false, "type": { "name": "ContentGroup" } }, "_frontstageProvider": { "defaultValue": null, "description": "", "name": "_frontstageProvider", "required": false, "type": { "name": "FrontstageProvider" } }, "_timeTracker": { "defaultValue": null, "description": "", "name": "_timeTracker", "required": true, "type": { "name": "TimeTracker" } }, "_nineZoneState": { "defaultValue": null, "description": "", "name": "_nineZoneState", "required": false, "type": { "name": "NineZoneState" } }, "_contentGroupProvider": { "defaultValue": null, "description": "", "name": "_contentGroupProvider", "required": false, "type": { "name": "ContentGroupProvider" } }, "_floatingContentControls": { "defaultValue": null, "description": "", "name": "_floatingContentControls", "required": false, "type": { "name": "ContentControl[]" } }, "_toolAdminDefaultToolId": { "defaultValue": null, "description": "", "name": "_toolAdminDefaultToolId", "required": false, "type": { "name": "string" } }, "_dispatch": { "defaultValue": null, "description": "", "name": "_dispatch", "required": false, "type": { "name": "NineZoneDispatch" } }, "_batching": { "defaultValue": null, "description": "", "name": "_batching", "required": true, "type": { "name": "boolean" } }, "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "usage": { "defaultValue": null, "description": "", "name": "usage", "required": true, "type": { "name": "string" } }, "version": { "defaultValue": null, "description": "", "name": "version", "required": true, "type": { "name": "number" } }, "contentGroupProvider": { "defaultValue": null, "description": "", "name": "contentGroupProvider", "required": true, "type": { "name": "ContentGroupProvider | undefined" } }, "floatingContentControls": { "defaultValue": null, "description": "", "name": "floatingContentControls", "required": true, "type": { "name": "ContentControl[] | undefined" } }, "toolSettings": { "defaultValue": null, "description": "", "name": "toolSettings", "required": true, "type": { "name": "WidgetDef | undefined" } }, "statusBar": { "defaultValue": null, "description": "", "name": "statusBar", "required": true, "type": { "name": "WidgetDef | undefined" } }, "contentManipulation": { "defaultValue": null, "description": "", "name": "contentManipulation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "viewNavigation": { "defaultValue": null, "description": "", "name": "viewNavigation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "topPanel": { "defaultValue": null, "description": "", "name": "topPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "leftPanel": { "defaultValue": null, "description": "", "name": "leftPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "rightPanel": { "defaultValue": null, "description": "", "name": "rightPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "bottomPanel": { "defaultValue": null, "description": "", "name": "bottomPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "contentLayoutDef": { "defaultValue": null, "description": "", "name": "contentLayoutDef", "required": true, "type": { "name": "ContentLayoutDef | undefined" } }, "contentGroup": { "defaultValue": null, "description": "", "name": "contentGroup", "required": true, "type": { "name": "ContentGroup | undefined" } }, "frontstageProvider": { "defaultValue": null, "description": "", "name": "frontstageProvider", "required": true, "type": { "name": "FrontstageProvider | undefined" } }, "toStagePanelLocation": { "defaultValue": null, "description": "", "name": "toStagePanelLocation", "required": true, "type": { "name": "(side: PanelSide) => StagePanelLocation" } }, "populateStateMaps": { "defaultValue": null, "description": "", "name": "populateStateMaps", "required": true, "type": { "name": "(nineZone: NineZoneState) => { panelMap: Map<StagePanelDef, StagePanelState>; widgetMap: Map<WidgetDef, WidgetState>; }" } }, "triggerStateChangeEvents": { "defaultValue": null, "description": "", "name": "triggerStateChangeEvents", "required": true, "type": { "name": "(oldState: NineZoneState) => void" } }, "handlePopouts": { "defaultValue": null, "description": "", "name": "handlePopouts", "required": true, "type": { "name": "(oldState: NineZoneState | undefined) => boolean" } }, "nineZoneState": { "defaultValue": null, "description": "@internal", "name": "nineZoneState", "required": true, "type": { "name": "NineZoneState | undefined" } }, "dispatch": { "defaultValue": null, "description": "@internal", "name": "dispatch", "required": true, "type": { "name": "NineZoneDispatch" } }, "batch": { "defaultValue": null, "description": "Dispatch multiple actions inside `fn`, but trigger events once.\n@internal", "name": "batch", "required": true, "type": { "name": "(fn: () => void) => void" } }, "timeTracker": { "defaultValue": null, "description": "@internal", "name": "timeTracker", "required": true, "type": { "name": "TimeTracker" } }, "_onActivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes activated", "name": "_onActivated", "required": true, "type": { "name": "() => Promise<void>" } }, "onActivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes activated", "name": "onActivated", "required": true, "type": { "name": "() => Promise<void>" } }, "_onDeactivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes inactive", "name": "_onDeactivated", "required": true, "type": { "name": "() => Promise<void>" } }, "onDeactivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes inactive", "name": "onDeactivated", "required": true, "type": { "name": "() => Promise<void>" } }, "setIsApplicationClosing": { "defaultValue": null, "description": "@internal", "name": "setIsApplicationClosing", "required": true, "type": { "name": "(value: boolean) => void" } }, "waitUntilReady": { "defaultValue": null, "description": "Returns once the contained widgets and content controls are ready to use", "name": "waitUntilReady", "required": true, "type": { "name": "() => Promise<void>" } }, "_onFrontstageReady": { "defaultValue": null, "description": "Handles when the Frontstage becomes active", "name": "_onFrontstageReady", "required": true, "type": { "name": "() => void" } }, "onFrontstageReady": { "defaultValue": null, "description": "Handles when the Frontstage becomes active", "name": "onFrontstageReady", "required": true, "type": { "name": "() => void" } }, "setContentLayoutAndGroup": { "defaultValue": null, "description": "Sets the Content Layout and Content Group", "name": "setContentLayoutAndGroup", "required": true, "type": { "name": "(contentLayoutDef: ContentLayoutDef, contentGroup: ContentGroup) => void" } }, "setActiveContent": { "defaultValue": null, "description": "Sets the active view content control to the default or first", "name": "setActiveContent", "required": true, "type": { "name": "() => Promise<boolean>" } }, "setActiveView": { "defaultValue": null, "description": "Sets the active view content control", "name": "setActiveView", "required": true, "type": { "name": "(newContent: ContentControl, oldContent?: ContentControl | undefined) => void" } }, "setActiveViewFromViewport": { "defaultValue": null, "description": "Sets the active view content control based on the selected viewport.", "name": "setActiveViewFromViewport", "required": true, "type": { "name": "(viewport: ScreenViewport) => boolean" } }, "getStagePanelDef": { "defaultValue": null, "description": "Gets a [[StagePanelDef]] based on a given panel location\n@beta", "name": "getStagePanelDef", "required": true, "type": { "name": "(location: StagePanelLocation) => StagePanelDef | undefined" } }, "panelDefs": { "defaultValue": null, "description": "Gets a list of [[StagePanelDef]]s\n@beta", "name": "panelDefs", "required": true, "type": { "name": "StagePanelDef[]" } }, "findWidgetDef": { "defaultValue": null, "description": "Finds a [[WidgetDef]] based on a given id", "name": "findWidgetDef", "required": true, "type": { "name": "(id: string) => WidgetDef | undefined" } }, "_widgetControls": { "defaultValue": null, "description": "Gets the list of [[WidgetControl]]s", "name": "_widgetControls", "required": true, "type": { "name": "WidgetControl[]" } }, "addFloatingContentControl": { "defaultValue": null, "description": "", "name": "addFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "dropFloatingContentControl": { "defaultValue": null, "description": "", "name": "dropFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "contentControls": { "defaultValue": null, "description": "Gets the list of [[ContentControl]]s", "name": "contentControls", "required": true, "type": { "name": "ContentControl[]" } }, "initializeFromConfig": { "defaultValue": null, "description": "Initializes a FrontstageDef from FrontstageConfig.\n@internal", "name": "initializeFromConfig", "required": true, "type": { "name": "(config: FrontstageConfig) => Promise<void>" } }, "updateWidgetDefs": { "defaultValue": null, "description": "@internal", "name": "updateWidgetDefs", "required": true, "type": { "name": "() => void" } }, "restoreLayout": { "defaultValue": null, "description": "Restores frontstage layout to initial configuration.\n@beta", "name": "restoreLayout", "required": true, "type": { "name": "() => void" } }, "isPopoutWidget": { "defaultValue": null, "description": "", "name": "isPopoutWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "isFloatingWidget": { "defaultValue": null, "description": "", "name": "isFloatingWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "floatWidget": { "defaultValue": null, "description": "Create a new floating panel that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:50, y:100} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:400, width:400} is used.\n@beta", "name": "floatWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isWidgetDisplayed": { "defaultValue": null, "description": "Check widget and panel state to determine whether the widget is currently displayed\n@param widgetId case-sensitive Widget Id\n@public", "name": "isWidgetDisplayed", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "openPopoutWidgetContainer": { "defaultValue": null, "description": "Opens window for specified PopoutWidget container. Used to reopen popout when running in Electron.\n@internal", "name": "openPopoutWidgetContainer", "required": true, "type": { "name": "(widgetContainerId: string, oldState: NineZoneState | undefined) => boolean" } }, "popoutWidget": { "defaultValue": null, "description": "Create a new popout/child window that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:0, y:0} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:800, width:600} is used.\n@beta", "name": "popoutWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isStageClosing": { "defaultValue": null, "description": "", "name": "isStageClosing", "required": true, "type": { "name": "boolean" } }, "isApplicationClosing": { "defaultValue": null, "description": "", "name": "isApplicationClosing", "required": true, "type": { "name": "boolean" } }, "isReady": { "defaultValue": null, "description": "", "name": "isReady", "required": true, "type": { "name": "boolean" } }, "saveChildWindowSizeAndPosition": { "defaultValue": null, "description": "@internal", "name": "saveChildWindowSizeAndPosition", "required": true, "type": { "name": "(childWindowId: string, childWindow: ChildWindow) => void" } }, "dockWidgetContainerByContainerId": { "defaultValue": null, "description": "@internal", "name": "dockWidgetContainerByContainerId", "required": true, "type": { "name": "(widgetContainerId: string) => void" } }, "dockWidgetContainer": { "defaultValue": null, "description": "Finds the container with the specified widget and re-docks all widgets\nback to the panel zone location that was used when the floating container\nwas generated.\n@param widgetId case sensitive Widget Id.\n@beta", "name": "dockWidgetContainer", "required": true, "type": { "name": "(widgetId: string) => void" } }, "setFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "setFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string, bounds: RectangleProps) => boolean" } }, "getFloatingWidgetContainerIds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIds", "required": true, "type": { "name": "() => string[]" } }, "getFloatingWidgetContainerIdByWidgetId": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIdByWidgetId", "required": true, "type": { "name": "(widgetId: string) => string | undefined" } }, "getFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string | undefined) => RectangleProps | undefined" } }, "_widgetDefs": { "defaultValue": null, "description": "", "name": "_widgetDefs", "required": true, "type": { "name": "() => Iterator<WidgetDef, any, undefined>" } }, "widgetDefs": { "defaultValue": null, "description": "Iterable of all widget definitions in a frontstage.\n@internal", "name": "widgetDefs", "required": true, "type": { "name": "{ [Symbol.iterator](): Iterator<WidgetDef, any, undefined>; }" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  isFrontstageStateSettingResult.displayName = "isFrontstageStateSettingResult";
  isFrontstageStateSettingResult.__docgenInfo = { "description": "", "displayName": "isFrontstageStateSettingResult", "props": { "status": { "defaultValue": null, "description": "", "name": "status", "required": true, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }, { "value": "2" }, { "value": "3" }, { "value": "4" }] } }, "setting": { "defaultValue": null, "description": "", "name": "setting", "required": false, "type": { "name": "any" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  initializeNineZoneState.displayName = "initializeNineZoneState";
  initializeNineZoneState.__docgenInfo = { "description": "", "displayName": "initializeNineZoneState", "props": { "_id": { "defaultValue": null, "description": "", "name": "_id", "required": true, "type": { "name": "string" } }, "_initialConfig": { "defaultValue": null, "description": "", "name": "_initialConfig", "required": false, "type": { "name": "FrontstageConfig" } }, "_isStageClosing": { "defaultValue": null, "description": "", "name": "_isStageClosing", "required": true, "type": { "name": "boolean" } }, "_isReady": { "defaultValue": null, "description": "", "name": "_isReady", "required": true, "type": { "name": "boolean" } }, "_isApplicationClosing": { "defaultValue": null, "description": "", "name": "_isApplicationClosing", "required": true, "type": { "name": "boolean" } }, "_usage": { "defaultValue": null, "description": "", "name": "_usage", "required": false, "type": { "name": "string" } }, "_version": { "defaultValue": null, "description": "", "name": "_version", "required": true, "type": { "name": "number" } }, "_toolSettings": { "defaultValue": null, "description": "", "name": "_toolSettings", "required": false, "type": { "name": "WidgetDef" } }, "_statusBar": { "defaultValue": null, "description": "", "name": "_statusBar", "required": false, "type": { "name": "WidgetDef" } }, "_contentManipulation": { "defaultValue": null, "description": "", "name": "_contentManipulation", "required": false, "type": { "name": "WidgetDef" } }, "_viewNavigation": { "defaultValue": null, "description": "", "name": "_viewNavigation", "required": false, "type": { "name": "WidgetDef" } }, "_topPanel": { "defaultValue": null, "description": "", "name": "_topPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_leftPanel": { "defaultValue": null, "description": "", "name": "_leftPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_rightPanel": { "defaultValue": null, "description": "", "name": "_rightPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_bottomPanel": { "defaultValue": null, "description": "", "name": "_bottomPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_contentLayoutDef": { "defaultValue": null, "description": "", "name": "_contentLayoutDef", "required": false, "type": { "name": "ContentLayoutDef" } }, "_contentGroup": { "defaultValue": null, "description": "", "name": "_contentGroup", "required": false, "type": { "name": "ContentGroup" } }, "_frontstageProvider": { "defaultValue": null, "description": "", "name": "_frontstageProvider", "required": false, "type": { "name": "FrontstageProvider" } }, "_timeTracker": { "defaultValue": null, "description": "", "name": "_timeTracker", "required": true, "type": { "name": "TimeTracker" } }, "_nineZoneState": { "defaultValue": null, "description": "", "name": "_nineZoneState", "required": false, "type": { "name": "NineZoneState" } }, "_contentGroupProvider": { "defaultValue": null, "description": "", "name": "_contentGroupProvider", "required": false, "type": { "name": "ContentGroupProvider" } }, "_floatingContentControls": { "defaultValue": null, "description": "", "name": "_floatingContentControls", "required": false, "type": { "name": "ContentControl[]" } }, "_toolAdminDefaultToolId": { "defaultValue": null, "description": "", "name": "_toolAdminDefaultToolId", "required": false, "type": { "name": "string" } }, "_dispatch": { "defaultValue": null, "description": "", "name": "_dispatch", "required": false, "type": { "name": "NineZoneDispatch" } }, "_batching": { "defaultValue": null, "description": "", "name": "_batching", "required": true, "type": { "name": "boolean" } }, "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "usage": { "defaultValue": null, "description": "", "name": "usage", "required": true, "type": { "name": "string" } }, "version": { "defaultValue": null, "description": "", "name": "version", "required": true, "type": { "name": "number" } }, "contentGroupProvider": { "defaultValue": null, "description": "", "name": "contentGroupProvider", "required": true, "type": { "name": "ContentGroupProvider | undefined" } }, "floatingContentControls": { "defaultValue": null, "description": "", "name": "floatingContentControls", "required": true, "type": { "name": "ContentControl[] | undefined" } }, "toolSettings": { "defaultValue": null, "description": "", "name": "toolSettings", "required": true, "type": { "name": "WidgetDef | undefined" } }, "statusBar": { "defaultValue": null, "description": "", "name": "statusBar", "required": true, "type": { "name": "WidgetDef | undefined" } }, "contentManipulation": { "defaultValue": null, "description": "", "name": "contentManipulation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "viewNavigation": { "defaultValue": null, "description": "", "name": "viewNavigation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "topPanel": { "defaultValue": null, "description": "", "name": "topPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "leftPanel": { "defaultValue": null, "description": "", "name": "leftPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "rightPanel": { "defaultValue": null, "description": "", "name": "rightPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "bottomPanel": { "defaultValue": null, "description": "", "name": "bottomPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "contentLayoutDef": { "defaultValue": null, "description": "", "name": "contentLayoutDef", "required": true, "type": { "name": "ContentLayoutDef | undefined" } }, "contentGroup": { "defaultValue": null, "description": "", "name": "contentGroup", "required": true, "type": { "name": "ContentGroup | undefined" } }, "frontstageProvider": { "defaultValue": null, "description": "", "name": "frontstageProvider", "required": true, "type": { "name": "FrontstageProvider | undefined" } }, "toStagePanelLocation": { "defaultValue": null, "description": "", "name": "toStagePanelLocation", "required": true, "type": { "name": "(side: PanelSide) => StagePanelLocation" } }, "populateStateMaps": { "defaultValue": null, "description": "", "name": "populateStateMaps", "required": true, "type": { "name": "(nineZone: NineZoneState) => { panelMap: Map<StagePanelDef, StagePanelState>; widgetMap: Map<WidgetDef, WidgetState>; }" } }, "triggerStateChangeEvents": { "defaultValue": null, "description": "", "name": "triggerStateChangeEvents", "required": true, "type": { "name": "(oldState: NineZoneState) => void" } }, "handlePopouts": { "defaultValue": null, "description": "", "name": "handlePopouts", "required": true, "type": { "name": "(oldState: NineZoneState | undefined) => boolean" } }, "nineZoneState": { "defaultValue": null, "description": "@internal", "name": "nineZoneState", "required": true, "type": { "name": "NineZoneState | undefined" } }, "dispatch": { "defaultValue": null, "description": "@internal", "name": "dispatch", "required": true, "type": { "name": "NineZoneDispatch" } }, "batch": { "defaultValue": null, "description": "Dispatch multiple actions inside `fn`, but trigger events once.\n@internal", "name": "batch", "required": true, "type": { "name": "(fn: () => void) => void" } }, "timeTracker": { "defaultValue": null, "description": "@internal", "name": "timeTracker", "required": true, "type": { "name": "TimeTracker" } }, "_onActivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes activated", "name": "_onActivated", "required": true, "type": { "name": "() => Promise<void>" } }, "onActivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes activated", "name": "onActivated", "required": true, "type": { "name": "() => Promise<void>" } }, "_onDeactivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes inactive", "name": "_onDeactivated", "required": true, "type": { "name": "() => Promise<void>" } }, "onDeactivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes inactive", "name": "onDeactivated", "required": true, "type": { "name": "() => Promise<void>" } }, "setIsApplicationClosing": { "defaultValue": null, "description": "@internal", "name": "setIsApplicationClosing", "required": true, "type": { "name": "(value: boolean) => void" } }, "waitUntilReady": { "defaultValue": null, "description": "Returns once the contained widgets and content controls are ready to use", "name": "waitUntilReady", "required": true, "type": { "name": "() => Promise<void>" } }, "_onFrontstageReady": { "defaultValue": null, "description": "Handles when the Frontstage becomes active", "name": "_onFrontstageReady", "required": true, "type": { "name": "() => void" } }, "onFrontstageReady": { "defaultValue": null, "description": "Handles when the Frontstage becomes active", "name": "onFrontstageReady", "required": true, "type": { "name": "() => void" } }, "setContentLayoutAndGroup": { "defaultValue": null, "description": "Sets the Content Layout and Content Group", "name": "setContentLayoutAndGroup", "required": true, "type": { "name": "(contentLayoutDef: ContentLayoutDef, contentGroup: ContentGroup) => void" } }, "setActiveContent": { "defaultValue": null, "description": "Sets the active view content control to the default or first", "name": "setActiveContent", "required": true, "type": { "name": "() => Promise<boolean>" } }, "setActiveView": { "defaultValue": null, "description": "Sets the active view content control", "name": "setActiveView", "required": true, "type": { "name": "(newContent: ContentControl, oldContent?: ContentControl | undefined) => void" } }, "setActiveViewFromViewport": { "defaultValue": null, "description": "Sets the active view content control based on the selected viewport.", "name": "setActiveViewFromViewport", "required": true, "type": { "name": "(viewport: ScreenViewport) => boolean" } }, "getStagePanelDef": { "defaultValue": null, "description": "Gets a [[StagePanelDef]] based on a given panel location\n@beta", "name": "getStagePanelDef", "required": true, "type": { "name": "(location: StagePanelLocation) => StagePanelDef | undefined" } }, "panelDefs": { "defaultValue": null, "description": "Gets a list of [[StagePanelDef]]s\n@beta", "name": "panelDefs", "required": true, "type": { "name": "StagePanelDef[]" } }, "findWidgetDef": { "defaultValue": null, "description": "Finds a [[WidgetDef]] based on a given id", "name": "findWidgetDef", "required": true, "type": { "name": "(id: string) => WidgetDef | undefined" } }, "_widgetControls": { "defaultValue": null, "description": "Gets the list of [[WidgetControl]]s", "name": "_widgetControls", "required": true, "type": { "name": "WidgetControl[]" } }, "addFloatingContentControl": { "defaultValue": null, "description": "", "name": "addFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "dropFloatingContentControl": { "defaultValue": null, "description": "", "name": "dropFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "contentControls": { "defaultValue": null, "description": "Gets the list of [[ContentControl]]s", "name": "contentControls", "required": true, "type": { "name": "ContentControl[]" } }, "initializeFromConfig": { "defaultValue": null, "description": "Initializes a FrontstageDef from FrontstageConfig.\n@internal", "name": "initializeFromConfig", "required": true, "type": { "name": "(config: FrontstageConfig) => Promise<void>" } }, "updateWidgetDefs": { "defaultValue": null, "description": "@internal", "name": "updateWidgetDefs", "required": true, "type": { "name": "() => void" } }, "restoreLayout": { "defaultValue": null, "description": "Restores frontstage layout to initial configuration.\n@beta", "name": "restoreLayout", "required": true, "type": { "name": "() => void" } }, "isPopoutWidget": { "defaultValue": null, "description": "", "name": "isPopoutWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "isFloatingWidget": { "defaultValue": null, "description": "", "name": "isFloatingWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "floatWidget": { "defaultValue": null, "description": "Create a new floating panel that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:50, y:100} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:400, width:400} is used.\n@beta", "name": "floatWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isWidgetDisplayed": { "defaultValue": null, "description": "Check widget and panel state to determine whether the widget is currently displayed\n@param widgetId case-sensitive Widget Id\n@public", "name": "isWidgetDisplayed", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "openPopoutWidgetContainer": { "defaultValue": null, "description": "Opens window for specified PopoutWidget container. Used to reopen popout when running in Electron.\n@internal", "name": "openPopoutWidgetContainer", "required": true, "type": { "name": "(widgetContainerId: string, oldState: NineZoneState | undefined) => boolean" } }, "popoutWidget": { "defaultValue": null, "description": "Create a new popout/child window that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:0, y:0} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:800, width:600} is used.\n@beta", "name": "popoutWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isStageClosing": { "defaultValue": null, "description": "", "name": "isStageClosing", "required": true, "type": { "name": "boolean" } }, "isApplicationClosing": { "defaultValue": null, "description": "", "name": "isApplicationClosing", "required": true, "type": { "name": "boolean" } }, "isReady": { "defaultValue": null, "description": "", "name": "isReady", "required": true, "type": { "name": "boolean" } }, "saveChildWindowSizeAndPosition": { "defaultValue": null, "description": "@internal", "name": "saveChildWindowSizeAndPosition", "required": true, "type": { "name": "(childWindowId: string, childWindow: ChildWindow) => void" } }, "dockWidgetContainerByContainerId": { "defaultValue": null, "description": "@internal", "name": "dockWidgetContainerByContainerId", "required": true, "type": { "name": "(widgetContainerId: string) => void" } }, "dockWidgetContainer": { "defaultValue": null, "description": "Finds the container with the specified widget and re-docks all widgets\nback to the panel zone location that was used when the floating container\nwas generated.\n@param widgetId case sensitive Widget Id.\n@beta", "name": "dockWidgetContainer", "required": true, "type": { "name": "(widgetId: string) => void" } }, "setFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "setFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string, bounds: RectangleProps) => boolean" } }, "getFloatingWidgetContainerIds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIds", "required": true, "type": { "name": "() => string[]" } }, "getFloatingWidgetContainerIdByWidgetId": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIdByWidgetId", "required": true, "type": { "name": "(widgetId: string) => string | undefined" } }, "getFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string | undefined) => RectangleProps | undefined" } }, "_widgetDefs": { "defaultValue": null, "description": "", "name": "_widgetDefs", "required": true, "type": { "name": "() => Iterator<WidgetDef, any, undefined>" } }, "widgetDefs": { "defaultValue": null, "description": "Iterable of all widget definitions in a frontstage.\n@internal", "name": "widgetDefs", "required": true, "type": { "name": "{ [Symbol.iterator](): Iterator<WidgetDef, any, undefined>; }" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  restoreNineZoneState.displayName = "restoreNineZoneState";
  restoreNineZoneState.__docgenInfo = { "description": "Converts from saved NineZoneState to NineZoneState.", "displayName": "restoreNineZoneState", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  packNineZoneState.displayName = "packNineZoneState";
  packNineZoneState.__docgenInfo = { "description": "Prepares NineZoneState to be saved.", "displayName": "packNineZoneState", "props": { "draggedTab": { "defaultValue": null, "description": "", "name": "draggedTab", "required": true, "type": { "name": "DraggedTabState | undefined" } }, "floatingWidgets": { "defaultValue": null, "description": "", "name": "floatingWidgets", "required": true, "type": { "name": "FloatingWidgetsState" } }, "popoutWidgets": { "defaultValue": null, "description": "", "name": "popoutWidgets", "required": true, "type": { "name": "PopoutWidgetsState" } }, "panels": { "defaultValue": null, "description": "", "name": "panels", "required": true, "type": { "name": "PanelsState" } }, "tabs": { "defaultValue": null, "description": "", "name": "tabs", "required": true, "type": { "name": "TabsState" } }, "savedTabs": { "defaultValue": null, "description": "", "name": "savedTabs", "required": true, "type": { "name": "SavedTabsState" } }, "toolSettings": { "defaultValue": null, "description": "", "name": "toolSettings", "required": true, "type": { "name": "ToolSettingsState | undefined" } }, "widgets": { "defaultValue": null, "description": "", "name": "widgets", "required": true, "type": { "name": "WidgetsState" } }, "size": { "defaultValue": null, "description": "", "name": "size", "required": true, "type": { "name": "SizeProps" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useSavedFrontstageState.displayName = "useSavedFrontstageState";
  useSavedFrontstageState.__docgenInfo = { "description": "", "displayName": "useSavedFrontstageState", "props": { "_id": { "defaultValue": null, "description": "", "name": "_id", "required": true, "type": { "name": "string" } }, "_initialConfig": { "defaultValue": null, "description": "", "name": "_initialConfig", "required": false, "type": { "name": "FrontstageConfig" } }, "_isStageClosing": { "defaultValue": null, "description": "", "name": "_isStageClosing", "required": true, "type": { "name": "boolean" } }, "_isReady": { "defaultValue": null, "description": "", "name": "_isReady", "required": true, "type": { "name": "boolean" } }, "_isApplicationClosing": { "defaultValue": null, "description": "", "name": "_isApplicationClosing", "required": true, "type": { "name": "boolean" } }, "_usage": { "defaultValue": null, "description": "", "name": "_usage", "required": false, "type": { "name": "string" } }, "_version": { "defaultValue": null, "description": "", "name": "_version", "required": true, "type": { "name": "number" } }, "_toolSettings": { "defaultValue": null, "description": "", "name": "_toolSettings", "required": false, "type": { "name": "WidgetDef" } }, "_statusBar": { "defaultValue": null, "description": "", "name": "_statusBar", "required": false, "type": { "name": "WidgetDef" } }, "_contentManipulation": { "defaultValue": null, "description": "", "name": "_contentManipulation", "required": false, "type": { "name": "WidgetDef" } }, "_viewNavigation": { "defaultValue": null, "description": "", "name": "_viewNavigation", "required": false, "type": { "name": "WidgetDef" } }, "_topPanel": { "defaultValue": null, "description": "", "name": "_topPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_leftPanel": { "defaultValue": null, "description": "", "name": "_leftPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_rightPanel": { "defaultValue": null, "description": "", "name": "_rightPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_bottomPanel": { "defaultValue": null, "description": "", "name": "_bottomPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_contentLayoutDef": { "defaultValue": null, "description": "", "name": "_contentLayoutDef", "required": false, "type": { "name": "ContentLayoutDef" } }, "_contentGroup": { "defaultValue": null, "description": "", "name": "_contentGroup", "required": false, "type": { "name": "ContentGroup" } }, "_frontstageProvider": { "defaultValue": null, "description": "", "name": "_frontstageProvider", "required": false, "type": { "name": "FrontstageProvider" } }, "_timeTracker": { "defaultValue": null, "description": "", "name": "_timeTracker", "required": true, "type": { "name": "TimeTracker" } }, "_nineZoneState": { "defaultValue": null, "description": "", "name": "_nineZoneState", "required": false, "type": { "name": "NineZoneState" } }, "_contentGroupProvider": { "defaultValue": null, "description": "", "name": "_contentGroupProvider", "required": false, "type": { "name": "ContentGroupProvider" } }, "_floatingContentControls": { "defaultValue": null, "description": "", "name": "_floatingContentControls", "required": false, "type": { "name": "ContentControl[]" } }, "_toolAdminDefaultToolId": { "defaultValue": null, "description": "", "name": "_toolAdminDefaultToolId", "required": false, "type": { "name": "string" } }, "_dispatch": { "defaultValue": null, "description": "", "name": "_dispatch", "required": false, "type": { "name": "NineZoneDispatch" } }, "_batching": { "defaultValue": null, "description": "", "name": "_batching", "required": true, "type": { "name": "boolean" } }, "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "usage": { "defaultValue": null, "description": "", "name": "usage", "required": true, "type": { "name": "string" } }, "version": { "defaultValue": null, "description": "", "name": "version", "required": true, "type": { "name": "number" } }, "contentGroupProvider": { "defaultValue": null, "description": "", "name": "contentGroupProvider", "required": true, "type": { "name": "ContentGroupProvider | undefined" } }, "floatingContentControls": { "defaultValue": null, "description": "", "name": "floatingContentControls", "required": true, "type": { "name": "ContentControl[] | undefined" } }, "toolSettings": { "defaultValue": null, "description": "", "name": "toolSettings", "required": true, "type": { "name": "WidgetDef | undefined" } }, "statusBar": { "defaultValue": null, "description": "", "name": "statusBar", "required": true, "type": { "name": "WidgetDef | undefined" } }, "contentManipulation": { "defaultValue": null, "description": "", "name": "contentManipulation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "viewNavigation": { "defaultValue": null, "description": "", "name": "viewNavigation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "topPanel": { "defaultValue": null, "description": "", "name": "topPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "leftPanel": { "defaultValue": null, "description": "", "name": "leftPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "rightPanel": { "defaultValue": null, "description": "", "name": "rightPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "bottomPanel": { "defaultValue": null, "description": "", "name": "bottomPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "contentLayoutDef": { "defaultValue": null, "description": "", "name": "contentLayoutDef", "required": true, "type": { "name": "ContentLayoutDef | undefined" } }, "contentGroup": { "defaultValue": null, "description": "", "name": "contentGroup", "required": true, "type": { "name": "ContentGroup | undefined" } }, "frontstageProvider": { "defaultValue": null, "description": "", "name": "frontstageProvider", "required": true, "type": { "name": "FrontstageProvider | undefined" } }, "toStagePanelLocation": { "defaultValue": null, "description": "", "name": "toStagePanelLocation", "required": true, "type": { "name": "(side: PanelSide) => StagePanelLocation" } }, "populateStateMaps": { "defaultValue": null, "description": "", "name": "populateStateMaps", "required": true, "type": { "name": "(nineZone: NineZoneState) => { panelMap: Map<StagePanelDef, StagePanelState>; widgetMap: Map<WidgetDef, WidgetState>; }" } }, "triggerStateChangeEvents": { "defaultValue": null, "description": "", "name": "triggerStateChangeEvents", "required": true, "type": { "name": "(oldState: NineZoneState) => void" } }, "handlePopouts": { "defaultValue": null, "description": "", "name": "handlePopouts", "required": true, "type": { "name": "(oldState: NineZoneState | undefined) => boolean" } }, "nineZoneState": { "defaultValue": null, "description": "@internal", "name": "nineZoneState", "required": true, "type": { "name": "NineZoneState | undefined" } }, "dispatch": { "defaultValue": null, "description": "@internal", "name": "dispatch", "required": true, "type": { "name": "NineZoneDispatch" } }, "batch": { "defaultValue": null, "description": "Dispatch multiple actions inside `fn`, but trigger events once.\n@internal", "name": "batch", "required": true, "type": { "name": "(fn: () => void) => void" } }, "timeTracker": { "defaultValue": null, "description": "@internal", "name": "timeTracker", "required": true, "type": { "name": "TimeTracker" } }, "_onActivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes activated", "name": "_onActivated", "required": true, "type": { "name": "() => Promise<void>" } }, "onActivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes activated", "name": "onActivated", "required": true, "type": { "name": "() => Promise<void>" } }, "_onDeactivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes inactive", "name": "_onDeactivated", "required": true, "type": { "name": "() => Promise<void>" } }, "onDeactivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes inactive", "name": "onDeactivated", "required": true, "type": { "name": "() => Promise<void>" } }, "setIsApplicationClosing": { "defaultValue": null, "description": "@internal", "name": "setIsApplicationClosing", "required": true, "type": { "name": "(value: boolean) => void" } }, "waitUntilReady": { "defaultValue": null, "description": "Returns once the contained widgets and content controls are ready to use", "name": "waitUntilReady", "required": true, "type": { "name": "() => Promise<void>" } }, "_onFrontstageReady": { "defaultValue": null, "description": "Handles when the Frontstage becomes active", "name": "_onFrontstageReady", "required": true, "type": { "name": "() => void" } }, "onFrontstageReady": { "defaultValue": null, "description": "Handles when the Frontstage becomes active", "name": "onFrontstageReady", "required": true, "type": { "name": "() => void" } }, "setContentLayoutAndGroup": { "defaultValue": null, "description": "Sets the Content Layout and Content Group", "name": "setContentLayoutAndGroup", "required": true, "type": { "name": "(contentLayoutDef: ContentLayoutDef, contentGroup: ContentGroup) => void" } }, "setActiveContent": { "defaultValue": null, "description": "Sets the active view content control to the default or first", "name": "setActiveContent", "required": true, "type": { "name": "() => Promise<boolean>" } }, "setActiveView": { "defaultValue": null, "description": "Sets the active view content control", "name": "setActiveView", "required": true, "type": { "name": "(newContent: ContentControl, oldContent?: ContentControl | undefined) => void" } }, "setActiveViewFromViewport": { "defaultValue": null, "description": "Sets the active view content control based on the selected viewport.", "name": "setActiveViewFromViewport", "required": true, "type": { "name": "(viewport: ScreenViewport) => boolean" } }, "getStagePanelDef": { "defaultValue": null, "description": "Gets a [[StagePanelDef]] based on a given panel location\n@beta", "name": "getStagePanelDef", "required": true, "type": { "name": "(location: StagePanelLocation) => StagePanelDef | undefined" } }, "panelDefs": { "defaultValue": null, "description": "Gets a list of [[StagePanelDef]]s\n@beta", "name": "panelDefs", "required": true, "type": { "name": "StagePanelDef[]" } }, "findWidgetDef": { "defaultValue": null, "description": "Finds a [[WidgetDef]] based on a given id", "name": "findWidgetDef", "required": true, "type": { "name": "(id: string) => WidgetDef | undefined" } }, "_widgetControls": { "defaultValue": null, "description": "Gets the list of [[WidgetControl]]s", "name": "_widgetControls", "required": true, "type": { "name": "WidgetControl[]" } }, "addFloatingContentControl": { "defaultValue": null, "description": "", "name": "addFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "dropFloatingContentControl": { "defaultValue": null, "description": "", "name": "dropFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "contentControls": { "defaultValue": null, "description": "Gets the list of [[ContentControl]]s", "name": "contentControls", "required": true, "type": { "name": "ContentControl[]" } }, "initializeFromConfig": { "defaultValue": null, "description": "Initializes a FrontstageDef from FrontstageConfig.\n@internal", "name": "initializeFromConfig", "required": true, "type": { "name": "(config: FrontstageConfig) => Promise<void>" } }, "updateWidgetDefs": { "defaultValue": null, "description": "@internal", "name": "updateWidgetDefs", "required": true, "type": { "name": "() => void" } }, "restoreLayout": { "defaultValue": null, "description": "Restores frontstage layout to initial configuration.\n@beta", "name": "restoreLayout", "required": true, "type": { "name": "() => void" } }, "isPopoutWidget": { "defaultValue": null, "description": "", "name": "isPopoutWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "isFloatingWidget": { "defaultValue": null, "description": "", "name": "isFloatingWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "floatWidget": { "defaultValue": null, "description": "Create a new floating panel that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:50, y:100} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:400, width:400} is used.\n@beta", "name": "floatWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isWidgetDisplayed": { "defaultValue": null, "description": "Check widget and panel state to determine whether the widget is currently displayed\n@param widgetId case-sensitive Widget Id\n@public", "name": "isWidgetDisplayed", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "openPopoutWidgetContainer": { "defaultValue": null, "description": "Opens window for specified PopoutWidget container. Used to reopen popout when running in Electron.\n@internal", "name": "openPopoutWidgetContainer", "required": true, "type": { "name": "(widgetContainerId: string, oldState: NineZoneState | undefined) => boolean" } }, "popoutWidget": { "defaultValue": null, "description": "Create a new popout/child window that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:0, y:0} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:800, width:600} is used.\n@beta", "name": "popoutWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isStageClosing": { "defaultValue": null, "description": "", "name": "isStageClosing", "required": true, "type": { "name": "boolean" } }, "isApplicationClosing": { "defaultValue": null, "description": "", "name": "isApplicationClosing", "required": true, "type": { "name": "boolean" } }, "isReady": { "defaultValue": null, "description": "", "name": "isReady", "required": true, "type": { "name": "boolean" } }, "saveChildWindowSizeAndPosition": { "defaultValue": null, "description": "@internal", "name": "saveChildWindowSizeAndPosition", "required": true, "type": { "name": "(childWindowId: string, childWindow: ChildWindow) => void" } }, "dockWidgetContainerByContainerId": { "defaultValue": null, "description": "@internal", "name": "dockWidgetContainerByContainerId", "required": true, "type": { "name": "(widgetContainerId: string) => void" } }, "dockWidgetContainer": { "defaultValue": null, "description": "Finds the container with the specified widget and re-docks all widgets\nback to the panel zone location that was used when the floating container\nwas generated.\n@param widgetId case sensitive Widget Id.\n@beta", "name": "dockWidgetContainer", "required": true, "type": { "name": "(widgetId: string) => void" } }, "setFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "setFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string, bounds: RectangleProps) => boolean" } }, "getFloatingWidgetContainerIds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIds", "required": true, "type": { "name": "() => string[]" } }, "getFloatingWidgetContainerIdByWidgetId": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIdByWidgetId", "required": true, "type": { "name": "(widgetId: string) => string | undefined" } }, "getFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string | undefined) => RectangleProps | undefined" } }, "_widgetDefs": { "defaultValue": null, "description": "", "name": "_widgetDefs", "required": true, "type": { "name": "() => Iterator<WidgetDef, any, undefined>" } }, "widgetDefs": { "defaultValue": null, "description": "Iterable of all widget definitions in a frontstage.\n@internal", "name": "widgetDefs", "required": true, "type": { "name": "{ [Symbol.iterator](): Iterator<WidgetDef, any, undefined>; }" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  getFrontstageStateSettingName.displayName = "getFrontstageStateSettingName";
  getFrontstageStateSettingName.__docgenInfo = { "description": "", "displayName": "getFrontstageStateSettingName", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  useItemsManager.displayName = "useItemsManager";
  useItemsManager.__docgenInfo = { "description": "", "displayName": "useItemsManager", "props": { "_id": { "defaultValue": null, "description": "", "name": "_id", "required": true, "type": { "name": "string" } }, "_initialConfig": { "defaultValue": null, "description": "", "name": "_initialConfig", "required": false, "type": { "name": "FrontstageConfig" } }, "_isStageClosing": { "defaultValue": null, "description": "", "name": "_isStageClosing", "required": true, "type": { "name": "boolean" } }, "_isReady": { "defaultValue": null, "description": "", "name": "_isReady", "required": true, "type": { "name": "boolean" } }, "_isApplicationClosing": { "defaultValue": null, "description": "", "name": "_isApplicationClosing", "required": true, "type": { "name": "boolean" } }, "_usage": { "defaultValue": null, "description": "", "name": "_usage", "required": false, "type": { "name": "string" } }, "_version": { "defaultValue": null, "description": "", "name": "_version", "required": true, "type": { "name": "number" } }, "_toolSettings": { "defaultValue": null, "description": "", "name": "_toolSettings", "required": false, "type": { "name": "WidgetDef" } }, "_statusBar": { "defaultValue": null, "description": "", "name": "_statusBar", "required": false, "type": { "name": "WidgetDef" } }, "_contentManipulation": { "defaultValue": null, "description": "", "name": "_contentManipulation", "required": false, "type": { "name": "WidgetDef" } }, "_viewNavigation": { "defaultValue": null, "description": "", "name": "_viewNavigation", "required": false, "type": { "name": "WidgetDef" } }, "_topPanel": { "defaultValue": null, "description": "", "name": "_topPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_leftPanel": { "defaultValue": null, "description": "", "name": "_leftPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_rightPanel": { "defaultValue": null, "description": "", "name": "_rightPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_bottomPanel": { "defaultValue": null, "description": "", "name": "_bottomPanel", "required": false, "type": { "name": "StagePanelDef" } }, "_contentLayoutDef": { "defaultValue": null, "description": "", "name": "_contentLayoutDef", "required": false, "type": { "name": "ContentLayoutDef" } }, "_contentGroup": { "defaultValue": null, "description": "", "name": "_contentGroup", "required": false, "type": { "name": "ContentGroup" } }, "_frontstageProvider": { "defaultValue": null, "description": "", "name": "_frontstageProvider", "required": false, "type": { "name": "FrontstageProvider" } }, "_timeTracker": { "defaultValue": null, "description": "", "name": "_timeTracker", "required": true, "type": { "name": "TimeTracker" } }, "_nineZoneState": { "defaultValue": null, "description": "", "name": "_nineZoneState", "required": false, "type": { "name": "NineZoneState" } }, "_contentGroupProvider": { "defaultValue": null, "description": "", "name": "_contentGroupProvider", "required": false, "type": { "name": "ContentGroupProvider" } }, "_floatingContentControls": { "defaultValue": null, "description": "", "name": "_floatingContentControls", "required": false, "type": { "name": "ContentControl[]" } }, "_toolAdminDefaultToolId": { "defaultValue": null, "description": "", "name": "_toolAdminDefaultToolId", "required": false, "type": { "name": "string" } }, "_dispatch": { "defaultValue": null, "description": "", "name": "_dispatch", "required": false, "type": { "name": "NineZoneDispatch" } }, "_batching": { "defaultValue": null, "description": "", "name": "_batching", "required": true, "type": { "name": "boolean" } }, "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "usage": { "defaultValue": null, "description": "", "name": "usage", "required": true, "type": { "name": "string" } }, "version": { "defaultValue": null, "description": "", "name": "version", "required": true, "type": { "name": "number" } }, "contentGroupProvider": { "defaultValue": null, "description": "", "name": "contentGroupProvider", "required": true, "type": { "name": "ContentGroupProvider | undefined" } }, "floatingContentControls": { "defaultValue": null, "description": "", "name": "floatingContentControls", "required": true, "type": { "name": "ContentControl[] | undefined" } }, "toolSettings": { "defaultValue": null, "description": "", "name": "toolSettings", "required": true, "type": { "name": "WidgetDef | undefined" } }, "statusBar": { "defaultValue": null, "description": "", "name": "statusBar", "required": true, "type": { "name": "WidgetDef | undefined" } }, "contentManipulation": { "defaultValue": null, "description": "", "name": "contentManipulation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "viewNavigation": { "defaultValue": null, "description": "", "name": "viewNavigation", "required": true, "type": { "name": "WidgetDef | undefined" } }, "topPanel": { "defaultValue": null, "description": "", "name": "topPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "leftPanel": { "defaultValue": null, "description": "", "name": "leftPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "rightPanel": { "defaultValue": null, "description": "", "name": "rightPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "bottomPanel": { "defaultValue": null, "description": "", "name": "bottomPanel", "required": true, "type": { "name": "StagePanelDef | undefined" } }, "contentLayoutDef": { "defaultValue": null, "description": "", "name": "contentLayoutDef", "required": true, "type": { "name": "ContentLayoutDef | undefined" } }, "contentGroup": { "defaultValue": null, "description": "", "name": "contentGroup", "required": true, "type": { "name": "ContentGroup | undefined" } }, "frontstageProvider": { "defaultValue": null, "description": "", "name": "frontstageProvider", "required": true, "type": { "name": "FrontstageProvider | undefined" } }, "toStagePanelLocation": { "defaultValue": null, "description": "", "name": "toStagePanelLocation", "required": true, "type": { "name": "(side: PanelSide) => StagePanelLocation" } }, "populateStateMaps": { "defaultValue": null, "description": "", "name": "populateStateMaps", "required": true, "type": { "name": "(nineZone: NineZoneState) => { panelMap: Map<StagePanelDef, StagePanelState>; widgetMap: Map<WidgetDef, WidgetState>; }" } }, "triggerStateChangeEvents": { "defaultValue": null, "description": "", "name": "triggerStateChangeEvents", "required": true, "type": { "name": "(oldState: NineZoneState) => void" } }, "handlePopouts": { "defaultValue": null, "description": "", "name": "handlePopouts", "required": true, "type": { "name": "(oldState: NineZoneState | undefined) => boolean" } }, "nineZoneState": { "defaultValue": null, "description": "@internal", "name": "nineZoneState", "required": true, "type": { "name": "NineZoneState | undefined" } }, "dispatch": { "defaultValue": null, "description": "@internal", "name": "dispatch", "required": true, "type": { "name": "NineZoneDispatch" } }, "batch": { "defaultValue": null, "description": "Dispatch multiple actions inside `fn`, but trigger events once.\n@internal", "name": "batch", "required": true, "type": { "name": "(fn: () => void) => void" } }, "timeTracker": { "defaultValue": null, "description": "@internal", "name": "timeTracker", "required": true, "type": { "name": "TimeTracker" } }, "_onActivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes activated", "name": "_onActivated", "required": true, "type": { "name": "() => Promise<void>" } }, "onActivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes activated", "name": "onActivated", "required": true, "type": { "name": "() => Promise<void>" } }, "_onDeactivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes inactive", "name": "_onDeactivated", "required": true, "type": { "name": "() => Promise<void>" } }, "onDeactivated": { "defaultValue": null, "description": "Handles when the Frontstage becomes inactive", "name": "onDeactivated", "required": true, "type": { "name": "() => Promise<void>" } }, "setIsApplicationClosing": { "defaultValue": null, "description": "@internal", "name": "setIsApplicationClosing", "required": true, "type": { "name": "(value: boolean) => void" } }, "waitUntilReady": { "defaultValue": null, "description": "Returns once the contained widgets and content controls are ready to use", "name": "waitUntilReady", "required": true, "type": { "name": "() => Promise<void>" } }, "_onFrontstageReady": { "defaultValue": null, "description": "Handles when the Frontstage becomes active", "name": "_onFrontstageReady", "required": true, "type": { "name": "() => void" } }, "onFrontstageReady": { "defaultValue": null, "description": "Handles when the Frontstage becomes active", "name": "onFrontstageReady", "required": true, "type": { "name": "() => void" } }, "setContentLayoutAndGroup": { "defaultValue": null, "description": "Sets the Content Layout and Content Group", "name": "setContentLayoutAndGroup", "required": true, "type": { "name": "(contentLayoutDef: ContentLayoutDef, contentGroup: ContentGroup) => void" } }, "setActiveContent": { "defaultValue": null, "description": "Sets the active view content control to the default or first", "name": "setActiveContent", "required": true, "type": { "name": "() => Promise<boolean>" } }, "setActiveView": { "defaultValue": null, "description": "Sets the active view content control", "name": "setActiveView", "required": true, "type": { "name": "(newContent: ContentControl, oldContent?: ContentControl | undefined) => void" } }, "setActiveViewFromViewport": { "defaultValue": null, "description": "Sets the active view content control based on the selected viewport.", "name": "setActiveViewFromViewport", "required": true, "type": { "name": "(viewport: ScreenViewport) => boolean" } }, "getStagePanelDef": { "defaultValue": null, "description": "Gets a [[StagePanelDef]] based on a given panel location\n@beta", "name": "getStagePanelDef", "required": true, "type": { "name": "(location: StagePanelLocation) => StagePanelDef | undefined" } }, "panelDefs": { "defaultValue": null, "description": "Gets a list of [[StagePanelDef]]s\n@beta", "name": "panelDefs", "required": true, "type": { "name": "StagePanelDef[]" } }, "findWidgetDef": { "defaultValue": null, "description": "Finds a [[WidgetDef]] based on a given id", "name": "findWidgetDef", "required": true, "type": { "name": "(id: string) => WidgetDef | undefined" } }, "_widgetControls": { "defaultValue": null, "description": "Gets the list of [[WidgetControl]]s", "name": "_widgetControls", "required": true, "type": { "name": "WidgetControl[]" } }, "addFloatingContentControl": { "defaultValue": null, "description": "", "name": "addFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "dropFloatingContentControl": { "defaultValue": null, "description": "", "name": "dropFloatingContentControl", "required": true, "type": { "name": "(contentControl?: ContentControl | undefined) => void" } }, "contentControls": { "defaultValue": null, "description": "Gets the list of [[ContentControl]]s", "name": "contentControls", "required": true, "type": { "name": "ContentControl[]" } }, "initializeFromConfig": { "defaultValue": null, "description": "Initializes a FrontstageDef from FrontstageConfig.\n@internal", "name": "initializeFromConfig", "required": true, "type": { "name": "(config: FrontstageConfig) => Promise<void>" } }, "updateWidgetDefs": { "defaultValue": null, "description": "@internal", "name": "updateWidgetDefs", "required": true, "type": { "name": "() => void" } }, "restoreLayout": { "defaultValue": null, "description": "Restores frontstage layout to initial configuration.\n@beta", "name": "restoreLayout", "required": true, "type": { "name": "() => void" } }, "isPopoutWidget": { "defaultValue": null, "description": "", "name": "isPopoutWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "isFloatingWidget": { "defaultValue": null, "description": "", "name": "isFloatingWidget", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "floatWidget": { "defaultValue": null, "description": "Create a new floating panel that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:50, y:100} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:400, width:400} is used.\n@beta", "name": "floatWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isWidgetDisplayed": { "defaultValue": null, "description": "Check widget and panel state to determine whether the widget is currently displayed\n@param widgetId case-sensitive Widget Id\n@public", "name": "isWidgetDisplayed", "required": true, "type": { "name": "(widgetId: string) => boolean" } }, "openPopoutWidgetContainer": { "defaultValue": null, "description": "Opens window for specified PopoutWidget container. Used to reopen popout when running in Electron.\n@internal", "name": "openPopoutWidgetContainer", "required": true, "type": { "name": "(widgetContainerId: string, oldState: NineZoneState | undefined) => boolean" } }, "popoutWidget": { "defaultValue": null, "description": "Create a new popout/child window that contains the widget specified by its Id.\n@param widgetId case sensitive Widget Id\n@param position Position of top left corner of floating panel in pixels. If undefined {x:0, y:0} is used.\n@param size defines the width and height of the floating panel. If undefined and widget has been floated before\nthe previous size is used, else {height:800, width:600} is used.\n@beta", "name": "popoutWidget", "required": true, "type": { "name": "(widgetId: string, position?: Readonly<WritableXAndY> | undefined, size?: SizeProps | undefined) => void" } }, "isStageClosing": { "defaultValue": null, "description": "", "name": "isStageClosing", "required": true, "type": { "name": "boolean" } }, "isApplicationClosing": { "defaultValue": null, "description": "", "name": "isApplicationClosing", "required": true, "type": { "name": "boolean" } }, "isReady": { "defaultValue": null, "description": "", "name": "isReady", "required": true, "type": { "name": "boolean" } }, "saveChildWindowSizeAndPosition": { "defaultValue": null, "description": "@internal", "name": "saveChildWindowSizeAndPosition", "required": true, "type": { "name": "(childWindowId: string, childWindow: ChildWindow) => void" } }, "dockWidgetContainerByContainerId": { "defaultValue": null, "description": "@internal", "name": "dockWidgetContainerByContainerId", "required": true, "type": { "name": "(widgetContainerId: string) => void" } }, "dockWidgetContainer": { "defaultValue": null, "description": "Finds the container with the specified widget and re-docks all widgets\nback to the panel zone location that was used when the floating container\nwas generated.\n@param widgetId case sensitive Widget Id.\n@beta", "name": "dockWidgetContainer", "required": true, "type": { "name": "(widgetId: string) => void" } }, "setFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "setFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string, bounds: RectangleProps) => boolean" } }, "getFloatingWidgetContainerIds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIds", "required": true, "type": { "name": "() => string[]" } }, "getFloatingWidgetContainerIdByWidgetId": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerIdByWidgetId", "required": true, "type": { "name": "(widgetId: string) => string | undefined" } }, "getFloatingWidgetContainerBounds": { "defaultValue": null, "description": "", "name": "getFloatingWidgetContainerBounds", "required": true, "type": { "name": "(floatingWidgetId: string | undefined) => RectangleProps | undefined" } }, "_widgetDefs": { "defaultValue": null, "description": "", "name": "_widgetDefs", "required": true, "type": { "name": "() => Iterator<WidgetDef, any, undefined>" } }, "widgetDefs": { "defaultValue": null, "description": "Iterable of all widget definitions in a frontstage.\n@internal", "name": "widgetDefs", "required": true, "type": { "name": "{ [Symbol.iterator](): Iterator<WidgetDef, any, undefined>; }" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class ContentDialogRenderer extends reactExports.PureComponent {
  constructor(props) {
    super(props);
  }
  render() {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      DialogRendererBase,
      {
        ...this.props,
        dialogManager: InternalContentDialogManager.dialogManager
      }
    );
  }
}
try {
  ContentDialogRenderer.displayName = "ContentDialogRenderer";
  ContentDialogRenderer.__docgenInfo = { "description": "ContentDialogRenderer React component renders modeless dialogs.", "displayName": "ContentDialogRenderer", "props": { "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function useActivityMessage({
  activityMessageInfo,
  dismissActivityMessage,
  cancelActivityMessage
}) {
  const toaster = useToaster();
  const { translate } = useTranslation();
  const recentToast = reactExports.useRef();
  reactExports.useEffect(() => {
    toaster.setSettings({ placement: "bottom" });
  }, [toaster]);
  reactExports.useEffect(() => {
    var _a2, _b2;
    if (activityMessageInfo == null ? void 0 : activityMessageInfo.restored) {
      recentToast.current = toaster.informational(
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          ActivityMessageContent,
          {
            initialActivityMessageInfo: activityMessageInfo
          }
        ),
        {
          onRemove: dismissActivityMessage,
          type: "persisting",
          link: ((_a2 = activityMessageInfo == null ? void 0 : activityMessageInfo.details) == null ? void 0 : _a2.supportsCancellation) && cancelActivityMessage ? {
            title: translate("dialog.cancel"),
            onClick: cancelActivityMessage
          } : void 0
        }
      );
    }
    if (!activityMessageInfo) {
      (_b2 = recentToast.current) == null ? void 0 : _b2.close();
    }
  }, [
    activityMessageInfo,
    cancelActivityMessage,
    dismissActivityMessage,
    toaster,
    translate
  ]);
}
function MessageRenderer() {
  const [activityMessageInfo, setActivityMessageInfo] = reactExports.useState();
  reactExports.useEffect(() => {
    return MessageManager.onActivityMessageUpdatedEvent.addListener((args) => {
      setActivityMessageInfo(args);
    });
  }, []);
  reactExports.useEffect(() => {
    return MessageManager.onActivityMessageCancelledEvent.addListener(() => {
      setActivityMessageInfo(void 0);
    });
  }, []);
  const cancelActivityMessage = reactExports.useCallback(() => {
    MessageManager.endActivityMessage(false);
  }, []);
  useActivityMessage({ activityMessageInfo, cancelActivityMessage });
  useDisplayMessage();
  return null;
}
function ActivityMessageContent({
  initialActivityMessageInfo
}) {
  var _a2, _b2;
  const { translate } = useTranslation();
  const percentCompleteLabel = translate("activityCenter.percentComplete");
  const [activityMessageInfo, setActivityMessageInfo] = reactExports.useState(
    initialActivityMessageInfo
  );
  reactExports.useEffect(() => {
    const handleActivityMessageUpdatedEvent = (args) => {
      setActivityMessageInfo(args);
    };
    return MessageManager.onActivityMessageUpdatedEvent.addListener(
      handleActivityMessageUpdatedEvent
    );
  }, []);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    activityMessageInfo.message && /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(jsxRuntimeExports.Fragment, { children: activityMessageInfo.message.reactNode || activityMessageInfo.message }) }),
    !!((_a2 = activityMessageInfo.details) == null ? void 0 : _a2.showPercentInMessage) && /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { variant: "small", children: `${activityMessageInfo.percentage} ${percentCompleteLabel}` }),
    ((_b2 = activityMessageInfo.details) == null ? void 0 : _b2.showProgressBar) && /* @__PURE__ */ jsxRuntimeExports.jsx(ProgressLinear, { value: activityMessageInfo == null ? void 0 : activityMessageInfo.percentage })
  ] });
}
function useDisplayMessage() {
  const toaster = useToaster();
  reactExports.useEffect(() => {
    return MessageManager.onDisplayMessage.addListener((args) => {
      const { message, options, settings: settings2, animateOutToElement } = args;
      const toastOptions = {
        hasCloseButton: true,
        duration: message.displayTime.milliseconds,
        type: message.msgType === OutputMessageType.Sticky ? "persisting" : "temporary",
        animateOutTo: animateOutToElement,
        ...options
      };
      toaster.setSettings({
        placement: "bottom",
        order: "ascending",
        ...settings2
      });
      const content = /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(MessageRenderer$1, { message: message.briefMessage }),
        message.detailedMessage && /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { variant: "small", children: /* @__PURE__ */ jsxRuntimeExports.jsx(MessageRenderer$1, { message: message.detailedMessage }) })
      ] });
      let toast;
      switch (message.priority) {
        case OutputMessagePriority.Warning: {
          toast = toaster.warning(content, toastOptions);
          break;
        }
        case OutputMessagePriority.Info: {
          toast = toaster.informational(content, toastOptions);
          break;
        }
        case OutputMessagePriority.Error:
        case OutputMessagePriority.Fatal: {
          toast = toaster.negative(content, toastOptions);
          break;
        }
        default:
          toast = toaster.positive(content, toastOptions);
      }
      args.close = toast.close;
    });
  }, [toaster]);
}
try {
  MessageRenderer.displayName = "MessageRenderer";
  MessageRenderer.__docgenInfo = { "description": "Component that renders activity, toast and sticky messages dispatched from message manager.", "displayName": "MessageRenderer", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const WrapperContext = reactExports.createContext(document.body);
function ConfigurableUiContent(props) {
  const [mainElement, setMainElement] = reactExports.useState(
    null
  );
  const [portalContainer, setPortalContainer] = reactExports.useState();
  reactExports.useEffect(() => {
    UiFramework.keyboardShortcuts.setFocusToHome();
  }, []);
  reactExports.useEffect(() => {
    InternalConfigurableUiManager.activityTracker.initialize({
      idleTimeout: props.idleTimeout,
      intervalTimeout: props.intervalTimeout
    });
    return () => {
      InternalConfigurableUiManager.activityTracker.terminate();
    };
  }, [props.idleTimeout, props.intervalTimeout]);
  const handleMouseMove = reactExports.useCallback((e) => {
    const point = new Point(e.pageX, e.pageY);
    CursorInformation.handleMouseMove(point);
  }, []);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "main",
    {
      role: "main",
      id: "uifw-configurableui-wrapper",
      className: props.className,
      style: props.style,
      onMouseMove: handleMouseMove,
      ref: setMainElement,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(WrapperContext.Provider, { value: mainElement, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
          ThemeProvider,
          {
            style: { height: "100%" },
            portalContainer,
            children: [
              props.appBackstage,
              /* @__PURE__ */ jsxRuntimeExports.jsx(WidgetPanelsFrontstage, {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx(ContentDialogRenderer, {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx(ModelessDialogRenderer, {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx(ModalDialogRenderer, {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx(ElementTooltip, {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx(PointerMessage, {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx(KeyboardShortcutMenu, {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx(InputFieldMessage, {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx(CursorPopupMenu, {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx(CursorPopupRenderer, {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx(PopupRenderer, {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx(MessageRenderer, {})
            ]
          }
        ) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "div",
          {
            className: "uifw-configurableui-portalContainer",
            ref: (instance) => setPortalContainer(instance ?? void 0)
          }
        )
      ]
    }
  );
}
try {
  ConfigurableUiContent.displayName = "ConfigurableUiContent";
  ConfigurableUiContent.__docgenInfo = { "description": "The ConfigurableUiContent component is the component the pages specified using ConfigurableUi", "displayName": "ConfigurableUiContent", "props": { "appBackstage": { "defaultValue": null, "description": "React node of the Backstage", "name": "appBackstage", "required": false, "type": { "name": "ReactNode" } }, "idleTimeout": { "defaultValue": null, "description": "@internal", "name": "idleTimeout", "required": false, "type": { "name": "number" } }, "intervalTimeout": { "defaultValue": null, "description": "@internal", "name": "intervalTimeout", "required": false, "type": { "name": "number" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  WrapperContext.displayName = "WrapperContext";
  WrapperContext.__docgenInfo = { "description": "Allows children to get the bounds or other properties of the wrapper element.", "displayName": "WrapperContext", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const _CardPopup = class _CardPopup extends reactExports.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      size: new Size(-1, -1)
    };
    this._onSizeKnown = (newSize) => {
      if (!this.state.size.equals(newSize))
        this.setState({ size: Size.create(newSize) });
    };
    this._handleKeyDown = (event) => {
      switch (event.key) {
        case Key_enum.Key.Escape.valueOf():
          this._cancel();
          break;
      }
    };
  }
  _cancel() {
    if (this.props.onCancel) {
      this.props.onCancel();
    }
  }
  render() {
    let point = PopupManager.getPopupPosition(
      this.props.el ?? this.context,
      this.props.pt,
      new Point(),
      this.state.size
    );
    const popupRect = CursorPopup.getPopupRect(
      point,
      this.props.offset,
      this.state.size,
      this.props.placement ?? mapToPlacement(this.props.relativePosition)
    );
    point = new Point(popupRect.left, popupRect.top);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      PositionPopup,
      {
        className: classnames("uifw-no-border", "uifw-card"),
        point,
        onSizeKnown: this._onSizeKnown,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          DivWithOutsideClick,
          {
            onOutsideClick: this.props.onCancel,
            onKeyDown: this._handleKeyDown,
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(FocusTrap, { active: true, returnFocusOnDeactivate: true, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Card,
              {
                content: this.props.content,
                title: this.props.title,
                items: this.props.items,
                onItemExecuted: this.props.onItemExecuted
              }
            ) })
          }
        )
      },
      this.props.id
    );
  }
};
_CardPopup.contextType = WrapperContext;
let CardPopup = _CardPopup;
function Card(props) {
  let titleNode;
  if (props.title) {
    if (typeof props.title === "string")
      titleNode = /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { variant: "leading", children: props.title });
    else {
      const propertyValueRendererManager = PropertyValueRendererManager.defaultManager;
      const titleValue = propertyValueRendererManager.render(props.title);
      titleNode = /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { variant: "leading", children: titleValue });
    }
  }
  const content = isReactContent(props.content) ? props.content.reactNode : /* @__PURE__ */ jsxRuntimeExports.jsx(MessageRenderer$1, { message: props.content });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "uifw-card-content", children: [
      props.title && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        titleNode,
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-card-gap" })
      ] }),
      content
    ] }),
    props.items && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-card-separator" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        ToolbarWithOverflow,
        {
          expandsTo: Direction.Bottom,
          panelAlignment: ToolbarPanelAlignment.Start,
          items: props.items,
          useDragInteraction: true,
          toolbarOpacitySetting: ToolbarOpacitySetting.Transparent,
          onItemExecuted: props.onItemExecuted
        }
      )
    ] })
  ] });
}
try {
  Card.displayName = "Card";
  Card.__docgenInfo = { "description": "Card component", "displayName": "Card", "props": { "content": { "defaultValue": null, "description": "", "name": "content", "required": true, "type": { "name": "PopupContentType" } }, "title": { "defaultValue": null, "description": "", "name": "title", "required": true, "type": { "name": "string | PropertyRecord | undefined" } }, "items": { "defaultValue": null, "description": "", "name": "items", "required": false, "type": { "name": "CommonToolbarItem[] | ToolbarItem[]" } }, "onItemExecuted": { "defaultValue": null, "description": "", "name": "onItemExecuted", "required": true, "type": { "name": "(item: any) => void" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  CardPopup.displayName = "CardPopup";
  CardPopup.__docgenInfo = { "description": "Popup component for Input Editor", "displayName": "CardPopup", "props": { "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "offset": { "defaultValue": null, "description": "", "name": "offset", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "pt": { "defaultValue": null, "description": "", "name": "pt", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "content": { "defaultValue": null, "description": "", "name": "content", "required": true, "type": { "name": "PopupContentType" } }, "title": { "defaultValue": null, "description": "", "name": "title", "required": true, "type": { "name": "string | PropertyRecord | undefined" } }, "items": { "defaultValue": null, "description": "", "name": "items", "required": true, "type": { "name": "CommonToolbarItem[] | ToolbarItem[] | undefined" } }, "orientation": { "defaultValue": null, "description": "", "name": "orientation", "required": true, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }] } }, "onCancel": { "defaultValue": null, "description": "", "name": "onCancel", "required": true, "type": { "name": "() => void" } }, "onItemExecuted": { "defaultValue": null, "description": "", "name": "onItemExecuted", "required": true, "type": { "name": "(item: any) => void" } }, "el": { "defaultValue": null, "description": "", "name": "el", "required": false, "type": { "name": "HTMLElement" } }, "placement": { "defaultValue": null, "description": "", "name": "placement", "required": false, "type": { "name": "enum", "value": [{ "value": '"top"' }, { "value": '"left"' }, { "value": '"right"' }, { "value": '"bottom"' }, { "value": '"top-start"' }, { "value": '"top-end"' }, { "value": '"left-start"' }, { "value": '"left-end"' }, { "value": '"right-start"' }, { "value": '"right-end"' }, { "value": '"bottom-start"' }, { "value": '"bottom-end"' }] } }, "relativePosition": { "defaultValue": null, "description": "", "name": "relativePosition", "required": false, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }, { "value": "2" }, { "value": "3" }, { "value": "4" }, { "value": "5" }, { "value": "6" }, { "value": "7" }, { "value": "8" }, { "value": "9" }] } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class HTMLElementPopup extends reactExports.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      size: new Size(-1, -1)
    };
    this._onSizeKnown = (newSize) => {
      if (!this.state.size.equals(newSize))
        this.setState({ size: Size.create(newSize) });
    };
  }
  render() {
    let point = PopupManager.getPopupPosition(
      this.props.el,
      this.props.pt,
      new Point(),
      this.state.size
    );
    const popupRect = CursorPopup.getPopupRect(
      point,
      this.props.offset,
      this.state.size,
      this.props.relativePosition
    );
    point = new Point(popupRect.left, popupRect.top);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      PositionPopup,
      {
        className: "uifw-no-border",
        point,
        onSizeKnown: this._onSizeKnown,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(DivWithOutsideClick, { onOutsideClick: this.props.onCancel, children: /* @__PURE__ */ jsxRuntimeExports.jsx(MessageRenderer$1, { message: this.props.element }) })
      },
      this.props.id
    );
  }
}
try {
  HTMLElementPopup.displayName = "HTMLElementPopup";
  HTMLElementPopup.__docgenInfo = { "description": "Popup component for HTMLElement", "displayName": "HTMLElementPopup", "props": { "element": { "defaultValue": null, "description": "", "name": "element", "required": true, "type": { "name": "HTMLElement" } }, "relativePosition": { "defaultValue": null, "description": "", "name": "relativePosition", "required": true, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }, { "value": "2" }, { "value": "3" }, { "value": "4" }, { "value": "5" }, { "value": "6" }, { "value": "7" }, { "value": "8" }, { "value": "9" }] } }, "orientation": { "defaultValue": null, "description": "", "name": "orientation", "required": true, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }] } }, "onCancel": { "defaultValue": null, "description": "", "name": "onCancel", "required": true, "type": { "name": "OnCancelFunc" } }, "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "el": { "defaultValue": null, "description": "", "name": "el", "required": true, "type": { "name": "HTMLElement" } }, "pt": { "defaultValue": null, "description": "", "name": "pt", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "offset": { "defaultValue": null, "description": "", "name": "offset", "required": true, "type": { "name": "Readonly<WritableXAndY>" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class InputEditorCommitHandler {
  constructor(onCommit) {
    this.onCommit = onCommit;
    this.handleCommit = (args) => {
      let newValue = 0;
      if (args.newValue.valueFormat === PropertyValueFormat.Primitive && args.newValue.value !== void 0) {
        newValue = args.newValue.value;
      }
      this.onCommit(newValue);
    };
  }
}
class InputEditorPopup extends reactExports.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      size: new Size(-1, -1)
    };
    this._onSizeKnown = (newSize) => {
      if (!this.state.size.equals(newSize))
        this.setState({ size: Size.create(newSize) });
    };
  }
  /** @internal */
  render() {
    const point = PopupManager.getPopupPosition(
      this.props.el,
      this.props.pt,
      this.props.offset,
      this.state.size
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      PositionPopup,
      {
        point,
        onSizeKnown: this._onSizeKnown,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(DivWithOutsideClick, { onOutsideClick: this.props.onCancel, children: /* @__PURE__ */ jsxRuntimeExports.jsx(PositionPopupContent, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          EditorContainer,
          {
            propertyRecord: this.props.record,
            onCommit: this.props.commitHandler.handleCommit,
            onCancel: this.props.onCancel,
            setFocus: true
          }
        ) }) })
      },
      this.props.id
    );
  }
}
try {
  InputEditorPopup.displayName = "InputEditorPopup";
  InputEditorPopup.__docgenInfo = { "description": "Popup component for Input Editor", "displayName": "InputEditorPopup", "props": { "record": { "defaultValue": null, "description": "", "name": "record", "required": true, "type": { "name": "PropertyRecord" } }, "onCancel": { "defaultValue": null, "description": "", "name": "onCancel", "required": true, "type": { "name": "() => void" } }, "commitHandler": { "defaultValue": null, "description": "", "name": "commitHandler", "required": true, "type": { "name": "InputEditorCommitHandler" } }, "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "el": { "defaultValue": null, "description": "", "name": "el", "required": true, "type": { "name": "HTMLElement" } }, "pt": { "defaultValue": null, "description": "", "name": "pt", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "offset": { "defaultValue": null, "description": "", "name": "offset", "required": true, "type": { "name": "Readonly<WritableXAndY>" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const _ClearKeyinPaletteHistoryTool = class _ClearKeyinPaletteHistoryTool extends Tool {
  // istanbul ignore next
  static get minArgs() {
    return 0;
  }
  // istanbul ignore next
  static get maxArgs() {
    return 0;
  }
  async run() {
    clearKeyinPaletteHistory();
    return true;
  }
};
_ClearKeyinPaletteHistoryTool.toolId = "ClearKeyinPaletteHistory";
_ClearKeyinPaletteHistoryTool.iconSpec = svgRemove;
let ClearKeyinPaletteHistoryTool = _ClearKeyinPaletteHistoryTool;
const keyinPaletteTools = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  ClearKeyinPaletteHistoryTool
}, Symbol.toStringTag, { value: "Module" }));
function matchesWords(word, target, contiguous = false) {
  if (!target || target.length === 0) {
    return null;
  }
  let result = null;
  let i = 0;
  word = word.toLowerCase();
  target = target.toLowerCase();
  while (i < target.length && (result = matchesWordsInternal(word, target, 0, i, contiguous)) === null) {
    i = nextWord(target, i + 1);
  }
  return result;
}
function matchesWordsInternal(word, target, i, j, contiguous) {
  if (i === word.length) {
    return [];
  } else if (j === target.length) {
    return null;
  } else if (!charactersMatch(word.charCodeAt(i), target.charCodeAt(j))) {
    return null;
  } else {
    let result = null;
    let nextWordIndex = j + 1;
    result = matchesWordsInternal(word, target, i + 1, j + 1, contiguous);
    if (!contiguous) {
      while (!result && (nextWordIndex = nextWord(target, nextWordIndex)) < target.length) {
        result = matchesWordsInternal(
          word,
          target,
          i + 1,
          nextWordIndex,
          contiguous
        );
        nextWordIndex++;
      }
    }
    return result === null ? null : join({ start: j, end: j + 1 }, result);
  }
}
function nextWord(word, start) {
  for (let i = start; i < word.length; i++) {
    if (isWordSeparator(word.charCodeAt(i)) || i > 0 && isWordSeparator(word.charCodeAt(i - 1))) {
      return i;
    }
  }
  return word.length;
}
function isWhitespace(code) {
  return code === 32 .valueOf() || code === 9 .valueOf() || code === 10 .valueOf() || code === 13 .valueOf();
}
const wordSeparators = /* @__PURE__ */ new Set();
"`~!@#$%^&*()-=+[{]}\\|;:'\",.<>/?".split("").forEach((s) => wordSeparators.add(s.charCodeAt(0)));
function isWordSeparator(code) {
  return isWhitespace(code) || wordSeparators.has(code);
}
function charactersMatch(codeA, codeB) {
  return codeA === codeB || isWordSeparator(codeA) && isWordSeparator(codeB);
}
function join(head, tail) {
  if (tail.length === 0) {
    tail = [head];
  } else if (head.end === tail[0].start) {
    tail[0].start = head.start;
  } else {
    tail.unshift(head);
  }
  return tail;
}
const KEYIN_PALETTE_NAMESPACE = "KeyinPalettePanel";
const KEYIN_HISTORY_KEY = "historyArray";
function clearKeyinPaletteHistory() {
  const uiSettingsStorage = UiFramework.getUiStateStorage();
  if (uiSettingsStorage) {
    void uiSettingsStorage.deleteSetting(
      KEYIN_PALETTE_NAMESPACE,
      KEYIN_HISTORY_KEY
    );
  }
}
function KeyinPalettePanel({
  keyins,
  onKeyinExecuted,
  historyLength: allowedHistoryLength = 6
}) {
  const { translate } = useTranslation();
  const [currentKeyin, setCurrentKeyin] = reactExports.useState("");
  const inputRef = reactExports.useRef(null);
  const keyinSeparator = "--#--";
  const [historyKeyins, setHistoryKeyins] = reactExports.useState([]);
  const uiSettingsStorage = useUiStateStorageHandler();
  reactExports.useEffect(() => {
    async function fetchState() {
      const settingsResult = await uiSettingsStorage.getSetting(
        KEYIN_PALETTE_NAMESPACE,
        KEYIN_HISTORY_KEY
      );
      if (UiStateStorageStatus.Success === settingsResult.status) {
        const filteredHistory = settingsResult.setting.filter(
          (keyin) => {
            const result = IModelApp.tools.parseKeyin(keyin);
            return result.ok;
          }
        );
        setHistoryKeyins(filteredHistory);
      } else {
        setHistoryKeyins([]);
      }
    }
    void fetchState();
  }, [uiSettingsStorage]);
  const storeHistoryKeyins = reactExports.useCallback(
    async (value) => {
      const result = await uiSettingsStorage.saveSetting(
        KEYIN_PALETTE_NAMESPACE,
        KEYIN_HISTORY_KEY,
        value
      );
      if (result.status !== UiStateStorageStatus.Success) {
        const errorDetails = new NotifyMessageDetails(
          OutputMessagePriority.Error,
          translate("keyinbrowser.couldNotSaveHistory")
        );
        IModelApp.notifications.outputMessage(errorDetails);
      }
    },
    [translate, uiSettingsStorage]
  );
  const allKeyins = reactExports.useMemo(() => {
    const availableKeyins = [];
    historyKeyins.forEach((value) => {
      availableKeyins.push({ value, isHistory: true });
    });
    availableKeyins.push(
      ...keyins.sort(
        (a, b) => a.value.toLowerCase().localeCompare(b.value.toLowerCase())
      )
    );
    return availableKeyins;
  }, [historyKeyins, keyins]);
  const submitKeyin = reactExports.useCallback(
    async (value) => {
      let detailedMessage;
      let message;
      try {
        switch (await IModelApp.tools.parseAndRun(value)) {
          case ParseAndRunResult.ToolNotFound:
            message = `translate("keyinbrowser.couldNotFindTool")} ${value}`;
            break;
          case ParseAndRunResult.BadArgumentCount:
            message = translate("keyinbrowser.incorrectArgs");
            break;
          case ParseAndRunResult.FailedToRun:
            message = translate("keyinbrowser.failedToRun");
            break;
        }
      } catch (ex) {
        {
          message = translate("keyinbrowser.exceptionOccurred");
          detailedMessage = `${translate(
            "keyinbrowser.exceptionOccurred"
          )}: ${ex}`;
        }
      }
      if (void 0 !== message) {
        const errorDetails = new NotifyMessageDetails(
          OutputMessagePriority.Error,
          message,
          detailedMessage,
          OutputMessageType.Sticky
        );
        IModelApp.notifications.outputMessage(errorDetails);
      } else {
        if (value.length < 400 && value !== ClearKeyinPaletteHistoryTool.keyin && value !== ClearKeyinPaletteHistoryTool.englishKeyin) {
          const newHistoryEntries = [value];
          for (const entry of historyKeyins) {
            if (entry !== value) {
              newHistoryEntries.push(entry);
              if (newHistoryEntries.length >= allowedHistoryLength)
                break;
            }
          }
          await storeHistoryKeyins(newHistoryEntries);
        }
        if (onKeyinExecuted)
          onKeyinExecuted(value);
      }
    },
    [
      translate,
      onKeyinExecuted,
      storeHistoryKeyins,
      historyKeyins,
      allowedHistoryLength
    ]
  );
  const selectKeyin = reactExports.useCallback(() => {
    if (inputRef.current) {
      inputRef.current.focus();
      inputRef.current.setSelectionRange(
        inputRef.current.value.length,
        inputRef.current.value.length
      );
    }
  }, [inputRef]);
  const updateKeyin = reactExports.useCallback(
    (value) => {
      setCurrentKeyin(value);
      selectKeyin();
    },
    [selectKeyin]
  );
  const getKeyinFromListboxValue = (value) => {
    if (value) {
      const indexSeparator = value.search(keyinSeparator);
      if (indexSeparator)
        return value.substring(0, indexSeparator);
    }
    return void 0;
  };
  const handleKeypressOnKeyinsList = reactExports.useCallback(
    async (event) => {
      var _a2, _b2;
      const key = event.key;
      if (key === Key_enum.Key.Enter.valueOf()) {
        event.preventDefault();
        const keyinToSend = getKeyinFromListboxValue(
          (_b2 = (_a2 = event.currentTarget) == null ? void 0 : _a2.dataset) == null ? void 0 : _b2.focusvalue
        );
        if (keyinToSend) {
          if (event.ctrlKey)
            updateKeyin(keyinToSend);
          else
            await submitKeyin(keyinToSend);
        }
      }
    },
    [submitKeyin, updateKeyin]
  );
  const onListboxValueChange = reactExports.useCallback(
    async (value, isControlOrCommandPressed) => {
      const keyin = getKeyinFromListboxValue(value);
      if (keyin) {
        if (isControlOrCommandPressed)
          updateKeyin(keyin);
        else
          await submitKeyin(keyin);
      }
    },
    [submitKeyin, updateKeyin]
  );
  const onInputValueChange = reactExports.useCallback(
    (event) => {
      setCurrentKeyin(event.target.value);
    },
    []
  );
  const filteredKeyins = reactExports.useMemo(() => {
    const filteredHistory = [];
    if (void 0 === currentKeyin || 0 === currentKeyin.length) {
      return allKeyins;
    } else {
      const newKeyinSet = [];
      allKeyins.forEach((value) => {
        if (value.value.length >= currentKeyin.length) {
          const matches = matchesWords(
            currentKeyin,
            value.value,
            currentKeyin.length > 60
          );
          if (matches && matches.length) {
            if (value.isHistory) {
              filteredHistory.push(value);
              newKeyinSet.push({ ...value, matches });
            } else {
              if (-1 === filteredHistory.findIndex(
                (historyEntry) => historyEntry.value === value.value
              ))
                newKeyinSet.push({ ...value, matches });
            }
          }
        }
      });
      return newKeyinSet;
    }
  }, [allKeyins, currentKeyin]);
  const onInputValueKeyDown = reactExports.useCallback(
    async (event) => {
      if (Key_enum.Key.Enter.valueOf() === event.key) {
        event.preventDefault();
        event.stopPropagation();
        if (1 === filteredKeyins.length) {
          if (event.ctrlKey)
            updateKeyin(filteredKeyins[0].value);
          else
            await submitKeyin(filteredKeyins[0].value);
        } else {
          if (currentKeyin)
            await submitKeyin(currentKeyin);
        }
      } else if (Key_enum.Key.Tab.valueOf() === event.key && 1 === filteredKeyins.length) {
        event.preventDefault();
        event.stopPropagation();
        updateKeyin(filteredKeyins[0].value);
      } else {
        if (event.key === Key_enum.Key.ArrowDown.valueOf() && filteredKeyins.length > 0) {
          event.preventDefault();
          event.stopPropagation();
          if (inputRef.current) {
            const nextElement = inputRef.current.nextElementSibling;
            nextElement && nextElement.focus();
          }
        }
      }
    },
    [filteredKeyins, submitKeyin, currentKeyin, updateKeyin]
  );
  const lastHistoryIndex = filteredKeyins.findIndex((entry) => true !== entry.isHistory) - 1;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "uifw-command-palette-panel", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      Input,
      {
        ref: inputRef,
        type: "text",
        onKeyDown: onInputValueKeyDown,
        className: "uifw-command-palette-input",
        "data-testid": "command-palette-input",
        onChange: onInputValueChange,
        placeholder: translate("keyinbrowser.placeholder"),
        value: currentKeyin,
        size: "small"
      }
    ),
    filteredKeyins.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(
      Listbox,
      {
        id: "uifw-command-sources",
        className: "map-manager-source-list",
        onKeyDown: handleKeypressOnKeyinsList,
        onListboxValueChange,
        children: filteredKeyins.map((entry, index) => {
          const value = `${entry.value}${keyinSeparator}${entry.isHistory ? "history" : "registry"}`;
          const itemClass = `uifw-command-palette-value-entry${index === lastHistoryIndex ? " uifw-history-bottom-border" : ""}`;
          return /* @__PURE__ */ jsxRuntimeExports.jsx(
            ListboxItem,
            {
              className: itemClass,
              value,
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(FilteredText, { value: entry.value, matches: entry.matches })
            },
            `${entry.value}-${index}`
          );
        })
      }
    )
  ] });
}
try {
  KeyinPalettePanel.displayName = "KeyinPalettePanel";
  KeyinPalettePanel.__docgenInfo = { "description": "", "displayName": "KeyinPalettePanel", "props": { "keyins": { "defaultValue": null, "description": "", "name": "keyins", "required": true, "type": { "name": "KeyinEntry[]" } }, "onKeyinExecuted": { "defaultValue": null, "description": "", "name": "onKeyinExecuted", "required": false, "type": { "name": "OnItemExecutedFunc" } }, "historyLength": { "defaultValue": null, "description": "", "name": "historyLength", "required": false, "type": { "name": "number" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function KeyinPalettePopup({
  // eslint-disable-next-line deprecation/deprecation
  el,
  id,
  keyins,
  onCancel,
  onItemExecuted,
  anchorEl
}) {
  const _el = anchorEl ?? el;
  const [popupSize, setPopupSize] = reactExports.useState(new Size(-1, -1));
  const onSizeKnown = reactExports.useCallback(
    (newSize) => {
      if (!popupSize.equals(newSize))
        setPopupSize(Size.create(newSize));
    },
    [popupSize]
  );
  const cancel = reactExports.useCallback(() => {
    onCancel && onCancel();
  }, [onCancel]);
  const handleKeyDown = reactExports.useCallback(
    (event) => {
      switch (event.key) {
        case Key_enum.Key.Escape.valueOf():
          cancel();
          event.preventDefault();
          break;
      }
    },
    [cancel]
  );
  const xMid = _el.getBoundingClientRect().left + _el.getBoundingClientRect().width / 2;
  let point = new Point(xMid, _el.getBoundingClientRect().top);
  if (popupSize.width > 0)
    point = point.offsetX(popupSize.width / -2);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    PositionPopup,
    {
      className: "uifw-command-palette-popup-container",
      point,
      onSizeKnown,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(DivWithOutsideClick, { onOutsideClick: onCancel, onKeyDown: handleKeyDown, children: /* @__PURE__ */ jsxRuntimeExports.jsx(FocusTrap, { active: true, returnFocusOnDeactivate: true, children: /* @__PURE__ */ jsxRuntimeExports.jsx(KeyinPalettePanel, { keyins, onKeyinExecuted: onItemExecuted }) }) })
    },
    id
  );
}
try {
  KeyinPalettePopup.displayName = "KeyinPalettePopup";
  KeyinPalettePopup.__docgenInfo = { "description": "Keyin Palette Popup Component", "displayName": "KeyinPalettePopup", "props": { "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "el": { "defaultValue": null, "description": "@deprecated in 4.11.x. Please use the optional `anchorEl` property moving forward.", "name": "el", "required": true, "type": { "name": "HTMLElement" } }, "keyins": { "defaultValue": null, "description": "", "name": "keyins", "required": true, "type": { "name": "KeyinEntry[]" } }, "anchorEl": { "defaultValue": null, "description": "", "name": "anchorEl", "required": false, "type": { "name": "HTMLElement" } }, "onCancel": { "defaultValue": null, "description": "", "name": "onCancel", "required": false, "type": { "name": "OnCancelFunc" } }, "onItemExecuted": { "defaultValue": null, "description": "", "name": "onItemExecuted", "required": false, "type": { "name": "OnItemExecutedFunc" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const _ToolbarPopup = class _ToolbarPopup extends reactExports.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      size: new Size(-1, -1)
    };
    this._onSizeKnown = (newSize) => {
      if (!this.state.size.equals(newSize))
        this.setState({ size: Size.create(newSize) });
    };
    this._handleKeyDown = (event) => {
      switch (event.key) {
        case Key_enum.Key.Escape.valueOf():
          this._cancel();
          break;
      }
    };
  }
  _cancel() {
    if (this.props.onCancel) {
      this.props.onCancel();
    }
  }
  render() {
    let point = PopupManager.getPopupPosition(
      this.props.el ?? this.context,
      this.props.pt,
      new Point(),
      this.state.size
    );
    const popupRect = CursorPopup.getPopupRect(
      point,
      this.props.offset,
      this.state.size,
      this.props.placement ?? mapToPlacement(this.props.relativePosition)
    );
    point = new Point(popupRect.left, popupRect.top);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      PositionPopup,
      {
        className: "uifw-no-border",
        point,
        onSizeKnown: this._onSizeKnown,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          DivWithOutsideClick,
          {
            onOutsideClick: this.props.onCancel,
            onKeyDown: this._handleKeyDown,
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(FocusTrap, { active: true, returnFocusOnDeactivate: true, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Toolbar,
              {
                expandsTo: Direction.Bottom,
                panelAlignment: ToolbarPanelAlignment.Start,
                items: this.props.items,
                useDragInteraction: true,
                toolbarOpacitySetting: ToolbarOpacitySetting.Defaults,
                onItemExecuted: this.props.onItemExecuted
              }
            ) })
          }
        )
      },
      this.props.id
    );
  }
};
_ToolbarPopup.contextType = WrapperContext;
let ToolbarPopup = _ToolbarPopup;
try {
  ToolbarPopup.displayName = "ToolbarPopup";
  ToolbarPopup.__docgenInfo = { "description": "Popup component for Toolbar", "displayName": "ToolbarPopup", "props": { "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "offset": { "defaultValue": null, "description": "", "name": "offset", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "pt": { "defaultValue": null, "description": "", "name": "pt", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "items": { "defaultValue": null, "description": "", "name": "items", "required": true, "type": { "name": "ToolbarItem[]" } }, "orientation": { "defaultValue": null, "description": "", "name": "orientation", "required": true, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }] } }, "onCancel": { "defaultValue": null, "description": "", "name": "onCancel", "required": true, "type": { "name": "() => void" } }, "onItemExecuted": { "defaultValue": null, "description": "", "name": "onItemExecuted", "required": true, "type": { "name": "(item: any) => void" } }, "el": { "defaultValue": null, "description": "", "name": "el", "required": false, "type": { "name": "HTMLElement" } }, "placement": { "defaultValue": null, "description": "", "name": "placement", "required": false, "type": { "name": "enum", "value": [{ "value": '"top"' }, { "value": '"left"' }, { "value": '"right"' }, { "value": '"bottom"' }, { "value": '"top-start"' }, { "value": '"top-end"' }, { "value": '"left-start"' }, { "value": '"left-end"' }, { "value": '"right-start"' }, { "value": '"right-end"' }, { "value": '"bottom-start"' }, { "value": '"bottom-end"' }] } }, "relativePosition": { "defaultValue": null, "description": "", "name": "relativePosition", "required": false, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }, { "value": "2" }, { "value": "3" }, { "value": "4" }, { "value": "5" }, { "value": "6" }, { "value": "7" }, { "value": "8" }, { "value": "9" }] } } } };
} catch (__react_docgen_typescript_loader_error) {
}
function EditorLabel({
  uiDataProvider,
  item,
  isLeftmostRecord
}) {
  const [isDisabled, setIsDisabled] = reactExports.useState(!!item.isDisabled);
  const displayLabel = reactExports.useMemo(() => {
    return item.property.displayLabel ? item.property.displayLabel : (
      /* istanbul ignore next */
      item.property.name
    );
  }, [item]);
  const propertyId = reactExports.useMemo(
    () => `dialogItemProperty-${item.property.name}`,
    [item]
  );
  reactExports.useEffect(() => {
    const handleSync = (args) => {
      const mySyncItem = args.properties.find(
        (syncItem) => syncItem.propertyName === item.property.name
      );
      if (mySyncItem) {
        setIsDisabled(!!mySyncItem.isDisabled);
      }
    };
    uiDataProvider.onSyncPropertiesChangeEvent.addListener(handleSync);
    return () => {
      uiDataProvider.onSyncPropertiesChangeEvent.removeListener(handleSync);
    };
  }, [uiDataProvider, item]);
  const className = classnames(
    "uifw-default-label",
    isDisabled && "uifw-label-disabled",
    !!isLeftmostRecord && "uifw-default-narrow-only-display",
    !isLeftmostRecord && "uifw-default-inline-label"
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className, htmlFor: propertyId, children: [
    displayLabel,
    ":"
  ] });
}
function PropertyEditor({
  uiDataProvider,
  initialItem,
  isLock,
  setFocus
}) {
  const getLatestRecordValue = reactExports.useCallback(() => {
    let newRecord = UiLayoutDataProvider.getPropertyRecord(initialItem);
    const foundItem = isLock ? uiDataProvider.items.find(
      (item) => {
        var _a2;
        return ((_a2 = item.lockProperty) == null ? void 0 : _a2.property.name) === initialItem.property.name;
      }
    ) : uiDataProvider.items.find(
      (item) => item.property.name === initialItem.property.name
    );
    if (foundItem) {
      if (isLock) {
        newRecord = newRecord.copyWithNewValue({
          value: foundItem.lockProperty.value.value,
          valueFormat: PropertyValueFormat.Primitive
        });
        newRecord.isDisabled = foundItem.lockProperty.isDisabled;
      } else {
        newRecord = newRecord.copyWithNewValue({
          value: foundItem.value.value,
          valueFormat: PropertyValueFormat.Primitive
        });
        newRecord.isDisabled = foundItem.isDisabled;
      }
    }
    return newRecord;
  }, [initialItem, isLock, uiDataProvider.items]);
  const currentRecord = getLatestRecordValue();
  const [propertyRecord, setPropertyRecord] = reactExports.useState(currentRecord);
  reactExports.useEffect(() => {
    const handleSync = (args) => {
      const mySyncItem = args.properties.find(
        (syncItem) => syncItem.propertyName === initialItem.property.name
      );
      if (mySyncItem) {
        const newPropertyValue = propertyRecord.copyWithNewValue(
          {
            value: mySyncItem.value.value,
            valueFormat: PropertyValueFormat.Primitive,
            displayValue: mySyncItem.value.displayValue
          },
          mySyncItem.property
        );
        if (mySyncItem.property) {
          if (mySyncItem.property.name === mySyncItem.propertyName) {
            newPropertyValue.isDisabled = mySyncItem.isDisabled;
            setPropertyRecord(newPropertyValue);
          } else {
            Logger.logError(
              "PropertyEditor",
              `Error trying to replace propertyName=${mySyncItem.propertyName} with property named ${mySyncItem.property.name}`
            );
          }
        } else {
          newPropertyValue.isDisabled = mySyncItem.isDisabled;
          setPropertyRecord(newPropertyValue);
        }
      }
    };
    uiDataProvider.onSyncPropertiesChangeEvent.addListener(handleSync);
    return () => {
      uiDataProvider.onSyncPropertiesChangeEvent.removeListener(handleSync);
    };
  }, [uiDataProvider, propertyRecord, initialItem.property.name]);
  const className = reactExports.useMemo(
    () => isLock ? "uifw-default-property-lock" : "uifw-default-editor",
    [isLock]
  );
  const handleCommit = reactExports.useCallback(
    (commit) => {
      assert(
        commit.newValue.valueFormat === PropertyValueFormat.Primitive && commit.propertyRecord.value.valueFormat === PropertyValueFormat.Primitive
      );
      const newPropertyValue = propertyRecord.copyWithNewValue(commit.newValue);
      const syncItem = {
        value: commit.newValue,
        propertyName: initialItem.property.name,
        isDisabled: newPropertyValue.isDisabled
      };
      uiDataProvider.applyUiPropertyChange(syncItem);
      uiDataProvider.reloadDialogItems(true);
    },
    [initialItem.property.name, propertyRecord, uiDataProvider]
  );
  const handleCancel = () => {
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    EditorContainer,
    {
      propertyRecord,
      setFocus,
      onCommit: handleCommit,
      onCancel: handleCancel
    },
    initialItem.property.name
  ) }, initialItem.property.name);
}
class ComponentGenerator {
  constructor(_uiDataProvider) {
    this._uiDataProvider = _uiDataProvider;
  }
  get uiDataProvider() {
    return this._uiDataProvider;
  }
  getEditor(item, isLock = false, setFocus = false) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      PropertyEditor,
      {
        uiDataProvider: this.uiDataProvider,
        initialItem: item,
        isLock,
        setFocus
      },
      item.property.name
    );
  }
  generateRowWithButtonGroupEditors(row, rowIndex) {
    if (1 === row.items.length) {
      return /* @__PURE__ */ jsxRuntimeExports.jsx(
        "div",
        {
          className: "uifw-default-inline-editor-group uifw-default-center-across-width",
          children: this.getEditor(row.items[0])
        },
        row.items[0].property.name
      );
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: "uifw-default-inline-editor-group uifw-default-center-across-width",
        children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-default-inline-editor-group", children: row.items.map((item) => this.getEditor(item)) })
      },
      rowIndex
    );
  }
  generateEntryWithButtonGroupEditors(row, rowIndex) {
    if (1 === row.items.length) {
      return {
        labelNode: "",
        editorNode: /* @__PURE__ */ jsxRuntimeExports.jsx(
          "div",
          {
            className: "uifw-default-inline-editor-group uifw-default-center-across-width",
            children: this.getEditor(row.items[0])
          },
          row.items[0].property.name
        )
      };
    }
    return {
      labelNode: "",
      editorNode: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "div",
        {
          className: "uifw-default-inline-editor-group uifw-default-center-across-width",
          children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-default-inline-editor-group", children: row.items.map((item) => this.getEditor(item)) })
        },
        rowIndex
      )
    };
  }
  getEditorLabel(item, isLeftmostRecord = false) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      EditorLabel,
      {
        uiDataProvider: this.uiDataProvider,
        item,
        isLeftmostRecord
      }
    );
  }
  getLeftLockAndLabel(rowItem, multiplePropertiesOnRow) {
    const record = UiLayoutDataProvider.getPropertyRecord(rowItem);
    const lockEditor = UiLayoutDataProvider.hasAssociatedLockProperty(rowItem) ? this.getEditor(rowItem.lockProperty, true) : null;
    const label = UiLayoutDataProvider.editorWantsLabel(rowItem) ? this.getEditorLabel(rowItem) : null;
    const classNames = multiplePropertiesOnRow ? "uifw-default-lock-and-label uifw-default-wide-only-display" : "uifw-default-lock-and-label";
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: classNames, children: [
      lockEditor,
      label
    ] }, `lock-${record.property.name}`);
  }
  getInlineLabelAndEditor(item, isLeftmostRecord) {
    const label = UiLayoutDataProvider.editorWantsLabel(item) ? this.getEditorLabel(item, isLeftmostRecord) : null;
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "uifw-default-inline-label-and-editor",
        children: [
          label,
          this.getEditor(item)
        ]
      },
      item.property.name
    );
  }
  getRowWithMultipleEditors(row) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-default-inline-editor-group", children: row.items.map(
      (item, index) => this.getInlineLabelAndEditor(item, 0 === index)
    ) });
  }
  getDivForRow(row) {
    if (1 === row.items.length)
      return this.getEditor(row.items[0]);
    return this.getRowWithMultipleEditors(row);
  }
  getRow(row, rowIndex) {
    if (UiLayoutDataProvider.onlyContainButtonGroupEditors(row)) {
      return this.generateRowWithButtonGroupEditors(row, rowIndex);
    } else {
      return /* @__PURE__ */ jsxRuntimeExports.jsxs(reactExports.Fragment, { children: [
        this.getLeftLockAndLabel(row.items[0], row.items.length > 1),
        this.getDivForRow(row)
      ] }, rowIndex);
    }
  }
  getToolSettingsEntry(row, rowIndex) {
    if (UiLayoutDataProvider.onlyContainButtonGroupEditors(row)) {
      return this.generateEntryWithButtonGroupEditors(row, rowIndex);
    } else {
      return {
        labelNode: this.getLeftLockAndLabel(row.items[0], row.items.length > 1),
        editorNode: this.getDivForRow(row)
      };
    }
  }
  /** ToolSettingsEntries are used by the tool settings bar. */
  getToolSettingsEntries() {
    return this.uiDataProvider.rows.map(
      (row, index) => this.getToolSettingsEntry(row, index)
    );
  }
}
try {
  ComponentGenerator.displayName = "ComponentGenerator";
  ComponentGenerator.__docgenInfo = { "description": "Utility methods to generate react ui from DialogRow specs", "displayName": "ComponentGenerator", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const ToolSettingsContentContext = reactExports.createContext({
  availableContentWidth: 0
});
function ToolSettingsGridContainer({
  componentGenerator
}) {
  const { availableContentWidth } = reactExports.useContext(
    ToolSettingsContentContext
  );
  const layoutMode = toLayoutMode(availableContentWidth);
  const className = classnames(
    "uifw-tool-settings-grid-container",
    // istanbul ignore next
    1 === layoutMode && "uifw-default-narrow"
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    DialogGridContainer,
    {
      componentGenerator,
      containerClassName: className
    }
  );
}
function DialogGridContainer({
  componentGenerator,
  containerClassName
}) {
  const className = classnames("uifw-default-container", containerClassName);
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-default-resizer-parent", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className, children: componentGenerator.uiDataProvider.rows.map(
    (row, index) => componentGenerator.getRow(row, index)
  ) }) });
}
function DefaultDialogGridContainer({
  componentGenerator,
  isToolSettings
}) {
  return !!isToolSettings ? /* @__PURE__ */ jsxRuntimeExports.jsx(ToolSettingsGridContainer, { componentGenerator }) : (
    /* istanbul ignore next */
    /* @__PURE__ */ jsxRuntimeExports.jsx(DialogGridContainer, { componentGenerator })
  );
}
const toLayoutMode = (width) => {
  return width < 250 && width > 0 ? (
    /* istanbul ignore next */
    1
  ) : 0;
};
try {
  ToolSettingsGridContainer.displayName = "ToolSettingsGridContainer";
  ToolSettingsGridContainer.__docgenInfo = { "description": "Component to provide grid of property editors", "displayName": "ToolSettingsGridContainer", "props": { "componentGenerator": { "defaultValue": null, "description": "", "name": "componentGenerator", "required": true, "type": { "name": "ComponentGenerator" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  DialogGridContainer.displayName = "DialogGridContainer";
  DialogGridContainer.__docgenInfo = { "description": "", "displayName": "DialogGridContainer", "props": { "componentGenerator": { "defaultValue": null, "description": "", "name": "componentGenerator", "required": true, "type": { "name": "ComponentGenerator" } }, "containerClassName": { "defaultValue": null, "description": "", "name": "containerClassName", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  DefaultDialogGridContainer.displayName = "DefaultDialogGridContainer";
  DefaultDialogGridContainer.__docgenInfo = { "description": "DefaultDialogGridContainer populates a React node with the items specified by the UiLayoutDataProvider", "displayName": "DefaultDialogGridContainer", "props": { "componentGenerator": { "defaultValue": null, "description": "", "name": "componentGenerator", "required": true, "type": { "name": "ComponentGenerator" } }, "isToolSettings": { "defaultValue": null, "description": "", "name": "isToolSettings", "required": false, "type": { "name": "boolean" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class ToolSettingsPopup extends reactExports.PureComponent {
  constructor(props) {
    super(props);
    this.state = {
      size: new Size(-1, -1)
    };
    this._onSizeKnown = (newSize) => {
      if (!this.state.size.equals(newSize))
        this.setState({ size: Size.create(newSize) });
    };
    this._handleKeyDown = (e) => {
      switch (e.key) {
        case Key_enum.Key.Escape.valueOf():
          if (this.props.onCancel)
            this.props.onCancel();
          break;
      }
    };
  }
  render() {
    const componentGenerator = new ComponentGenerator(this.props.dataProvider);
    let point = PopupManager.getPopupPosition(
      this.props.el,
      this.props.pt,
      new Point(),
      this.state.size
    );
    const popupRect = CursorPopup.getPopupRect(
      point,
      this.props.offset,
      this.state.size,
      this.props.relativePosition
    );
    point = new Point(popupRect.left, popupRect.top);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      PositionPopup,
      {
        point,
        onSizeKnown: this._onSizeKnown,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          DivWithOutsideClick,
          {
            onOutsideClick: this.props.onCancel,
            onKeyDown: this._handleKeyDown,
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(PositionPopupContent, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(FocusTrap, { active: true, returnFocusOnDeactivate: true, children: componentGenerator && /* @__PURE__ */ jsxRuntimeExports.jsx(DialogGridContainer, { componentGenerator }) }) })
          }
        )
      },
      this.props.id
    );
  }
}
try {
  ToolSettingsPopup.displayName = "ToolSettingsPopup";
  ToolSettingsPopup.__docgenInfo = { "description": "Popup component for Tool Settings", "displayName": "ToolSettingsPopup", "props": { "dataProvider": { "defaultValue": null, "description": "", "name": "dataProvider", "required": true, "type": { "name": "DialogLayoutDataProvider" } }, "relativePosition": { "defaultValue": null, "description": "", "name": "relativePosition", "required": true, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }, { "value": "2" }, { "value": "3" }, { "value": "4" }, { "value": "5" }, { "value": "6" }, { "value": "7" }, { "value": "8" }, { "value": "9" }] } }, "orientation": { "defaultValue": null, "description": "", "name": "orientation", "required": true, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }] } }, "onCancel": { "defaultValue": null, "description": "", "name": "onCancel", "required": true, "type": { "name": "OnCancelFunc" } }, "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "el": { "defaultValue": null, "description": "", "name": "el", "required": true, "type": { "name": "HTMLElement" } }, "pt": { "defaultValue": null, "description": "", "name": "pt", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "offset": { "defaultValue": null, "description": "", "name": "offset", "required": true, "type": { "name": "Readonly<WritableXAndY>" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const ComponentPopup = ({
  pt,
  children,
  offset,
  placement,
  id,
  onCancel,
  anchor
}) => {
  const wrapper = reactExports.useContext(WrapperContext);
  const [size, setSize] = reactExports.useState(new Size(-1, -1));
  reactExports.useEffect(() => {
    const newSize = Size.create(size);
    if (!size.equals(newSize)) {
      setSize(newSize);
    }
  }, [size]);
  let point = PopupManager.getPopupPosition(
    anchor ?? wrapper,
    pt,
    new Point(),
    size
  );
  const popupRect = CursorPopup.getPopupRect(point, offset, size, placement);
  point = new Point(popupRect.left, popupRect.top);
  const handleSizeKnown = (newSize) => {
    if (!size.equals(newSize))
      setSize(Size.create(newSize));
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    PositionPopup,
    {
      className: "uifw-no-border",
      point,
      onSizeKnown: handleSizeKnown,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(DivWithOutsideClick, { onOutsideClick: onCancel, children })
    },
    id
  );
};
try {
  ComponentPopup.displayName = "ComponentPopup";
  ComponentPopup.__docgenInfo = { "description": "Displays a React Component inside a popup. The user-facing API is the {@link PopupManager} and should be used instead of this component.", "displayName": "ComponentPopup", "props": { "placement": { "defaultValue": null, "description": "", "name": "placement", "required": true, "type": { "name": "enum", "value": [{ "value": '"top"' }, { "value": '"left"' }, { "value": '"right"' }, { "value": '"bottom"' }, { "value": '"top-start"' }, { "value": '"top-end"' }, { "value": '"left-start"' }, { "value": '"left-end"' }, { "value": '"right-start"' }, { "value": '"right-end"' }, { "value": '"bottom-start"' }, { "value": '"bottom-end"' }] } }, "orientation": { "defaultValue": null, "description": "", "name": "orientation", "required": true, "type": { "name": "enum", "value": [{ "value": "0" }, { "value": "1" }] } }, "onCancel": { "defaultValue": null, "description": "", "name": "onCancel", "required": true, "type": { "name": "() => void" } }, "anchor": { "defaultValue": null, "description": "", "name": "anchor", "required": false, "type": { "name": "HTMLElement" } }, "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "offset": { "defaultValue": null, "description": "", "name": "offset", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "pt": { "defaultValue": null, "description": "", "name": "pt", "required": true, "type": { "name": "Readonly<WritableXAndY>" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class PopupsChangedEvent extends UiEvent {
}
const isReactContent = (content) => {
  return content.reactNode !== void 0;
};
const _PopupManager = class _PopupManager {
  static get popupCount() {
    return this._popups.length;
  }
  static get popups() {
    return this._popups;
  }
  static set popups(popups) {
    if (this._popups === popups)
      return;
    this._popups = popups;
    this.onPopupsChangedEvent.emit({ popups });
  }
  /** @internal */
  static clearPopups() {
    this.popups = [];
  }
  static addPopup(popupInfo) {
    const popups = [..._PopupManager._popups, popupInfo];
    this.popups = popups;
  }
  static updatePopup(popupInfo, itemIndex) {
    if (itemIndex < 0)
      return;
    const popups = [
      ..._PopupManager._popups.slice(0, itemIndex),
      popupInfo,
      ..._PopupManager._popups.slice(itemIndex + 1)
    ];
    this.popups = popups;
  }
  static addOrUpdatePopup(popupInfo) {
    const itemIndex = _PopupManager._popups.findIndex(
      (info) => info.id === popupInfo.id
    );
    if (itemIndex >= 0)
      _PopupManager.updatePopup(popupInfo, itemIndex);
    else
      _PopupManager.addPopup(popupInfo);
  }
  static removePopup(id) {
    const index = _PopupManager._popups.findIndex(
      (info) => id === info.id
    );
    let result = true;
    if (index >= 0) {
      const popups = _PopupManager._popups.filter((info) => {
        return info.id !== id;
      });
      this.popups = popups;
    } else {
      Logger.logError(
        UiFramework.loggerCategory(this),
        `removePopup: Could not find popup with id of '${id}'`
      );
      result = false;
    }
    return result;
  }
  static get defaultOffset() {
    return _PopupManager._defaultOffset;
  }
  static set defaultOffset(offset) {
    _PopupManager._defaultOffset = offset;
  }
  static showInputEditor(el, pt, value, propertyDescription, onCommit, onCancel) {
    const primitiveValue = {
      value,
      valueFormat: PropertyValueFormat.Primitive
    };
    const record = new PropertyRecord(primitiveValue, propertyDescription);
    const commitHandler = new InputEditorCommitHandler(onCommit);
    const id = _PopupManager._editorId;
    const component = /* @__PURE__ */ jsxRuntimeExports.jsx(
      InputEditorPopup,
      {
        id,
        el,
        pt,
        offset: _PopupManager.defaultOffset,
        record,
        onCancel,
        commitHandler
      }
    );
    const popupInfo = {
      id,
      pt,
      component,
      parentDocument: el.ownerDocument,
      parent: el.ownerDocument
    };
    _PopupManager.addOrUpdatePopup(popupInfo);
    return true;
  }
  static hideInputEditor() {
    return _PopupManager.removePopup(_PopupManager._editorId);
  }
  static showKeyinPalette(keyins, el, onItemExecuted, onCancel) {
    const id = _PopupManager._keyPalettePopupId;
    const cancelFn = onCancel ?? (() => _PopupManager.hideKeyinPalette());
    const component = /* @__PURE__ */ jsxRuntimeExports.jsx(
      KeyinPalettePopup,
      {
        keyins,
        id,
        el,
        onItemExecuted,
        onCancel: cancelFn
      }
    );
    const pt = { x: 0, y: 0 };
    const popupInfo = {
      id,
      pt,
      component,
      parentDocument: el == null ? void 0 : el.ownerDocument
    };
    _PopupManager.addOrUpdatePopup(popupInfo);
    return true;
  }
  static hideKeyinPalette() {
    return _PopupManager.removePopup(_PopupManager._keyPalettePopupId);
  }
  // @deprecated in 4.11.x. Use {@link PopupManager.displayToolbar} instead.
  static showToolbar(toolbarProps, el, pt, offset, onItemExecuted, onCancel, relativePosition) {
    _PopupManager.displayToolbar(toolbarProps.items, {
      anchor: el,
      location: pt,
      offset,
      onCancel,
      placement: mapToPlacement(relativePosition),
      onItemExecuted
    });
    return true;
  }
  static displayToolbar(items, options) {
    const id = options.id ?? _PopupManager._toolbarId;
    const { anchor, location, offset, onCancel, placement, onItemExecuted } = options;
    const component = /* @__PURE__ */ jsxRuntimeExports.jsx(
      ToolbarPopup,
      {
        id,
        el: anchor,
        pt: location,
        offset,
        items,
        placement,
        orientation: Orientation.Horizontal,
        onCancel,
        onItemExecuted
      }
    );
    const popupInfo = {
      id,
      pt: location,
      component,
      parentDocument: (anchor == null ? void 0 : anchor.ownerDocument) ?? InternalConfigurableUiManager.getWrapperDocument(),
      parent: anchor == null ? void 0 : anchor.ownerDocument
    };
    _PopupManager.addOrUpdatePopup(popupInfo);
    return true;
  }
  static hideToolbar() {
    return _PopupManager.removePopup(_PopupManager._toolbarId);
  }
  // @deprecated in 4.11.x. Use {@link PopupManager.showComponent} instead.
  static showHTMLElement(displayElement, el, pt, offset, onCancel, relativePosition) {
    const id = _PopupManager._htmlElementId;
    const component = (
      // eslint-disable-next-line deprecation/deprecation
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        HTMLElementPopup,
        {
          id,
          el,
          pt,
          offset,
          element: displayElement,
          relativePosition,
          orientation: Orientation.Horizontal,
          onCancel
        }
      )
    );
    const popupInfo = {
      id,
      pt,
      component,
      parentDocument: el.ownerDocument
    };
    _PopupManager.addOrUpdatePopup(popupInfo);
    return true;
  }
  // @deprecated in 4.11.x. Use {@link PopupManager.hideComponent} instead.
  static hideHTMLElement() {
    return _PopupManager.removePopup(_PopupManager._htmlElementId);
  }
  /**
   * Displays a React component as a popup.
   * @param displayElement The React component to display.
   * @param options for displaying the component.
   */
  static showComponent(displayElement, options) {
    const { onCancel, location, offset, placement, anchor, id } = options;
    const _id = _PopupManager._htmlElementId;
    const component = /* @__PURE__ */ jsxRuntimeExports.jsx(
      ComponentPopup,
      {
        id: id ?? _id,
        anchor: anchor ?? void 0,
        pt: location,
        offset,
        placement,
        orientation: Orientation.Horizontal,
        onCancel,
        children: displayElement
      }
    );
    const popupInfo = {
      id: id ?? _id,
      pt: location,
      component,
      parentDocument: (anchor == null ? void 0 : anchor.ownerDocument) ?? InternalConfigurableUiManager.getWrapperDocument()
    };
    _PopupManager.addOrUpdatePopup(popupInfo);
    return true;
  }
  static hideComponent(id) {
    return _PopupManager.removePopup(id ?? _PopupManager._htmlElementId);
  }
  // @deprecated in 4.11.x. Use {@link PopupManager.displayCard} instead.
  static showCard(content, title, toolbarProps, el, pt, offset, onItemExecuted, onCancel, relativePosition) {
    const id = _PopupManager._cardId;
    const placement = mapToPlacement(relativePosition);
    const component = /* @__PURE__ */ jsxRuntimeExports.jsx(
      CardPopup,
      {
        id,
        el,
        pt,
        offset,
        content,
        title,
        items: toolbarProps ? toolbarProps.items : (
          /* istanbul ignore next */
          void 0
        ),
        placement,
        orientation: Orientation.Horizontal,
        onCancel,
        onItemExecuted
      }
    );
    const popupInfo = {
      id,
      pt,
      component,
      parentDocument: el.ownerDocument,
      parent: el.ownerDocument
    };
    _PopupManager.addOrUpdatePopup(popupInfo);
    return true;
  }
  static displayCard(content, options) {
    const id = options.id ?? _PopupManager._cardId;
    const {
      onCancel,
      location,
      placement,
      offset,
      anchor,
      title,
      toolbarProps,
      onItemExecuted
    } = options;
    const component = /* @__PURE__ */ jsxRuntimeExports.jsx(
      CardPopup,
      {
        id,
        el: anchor,
        pt: location,
        offset,
        content: { reactNode: content },
        title,
        items: toolbarProps == null ? void 0 : toolbarProps.items,
        placement,
        orientation: Orientation.Horizontal,
        onCancel,
        onItemExecuted
      }
    );
    const popupInfo = {
      id,
      pt: location,
      component,
      parentDocument: (anchor == null ? void 0 : anchor.ownerDocument) ?? InternalConfigurableUiManager.getWrapperDocument(),
      parent: anchor == null ? void 0 : anchor.ownerDocument
    };
    _PopupManager.addOrUpdatePopup(popupInfo);
    return true;
  }
  static hideCard(id) {
    const index = _PopupManager._popups.findIndex(
      (info) => id ?? _PopupManager._cardId === info.id
    );
    if (index >= 0)
      return _PopupManager.removePopup(_PopupManager._cardId);
    return false;
  }
  static openToolSettings(dataProvider, el, pt, offset, onCancel, relativePosition) {
    const id = _PopupManager._toolSettingsId;
    const component = /* @__PURE__ */ jsxRuntimeExports.jsx(
      ToolSettingsPopup,
      {
        id,
        el,
        pt,
        offset,
        dataProvider,
        relativePosition,
        orientation: Orientation.Horizontal,
        onCancel
      }
    );
    const popupInfo = {
      id,
      pt,
      component,
      parentDocument: el.ownerDocument
    };
    _PopupManager.addOrUpdatePopup(popupInfo);
    return true;
  }
  static closeToolSettings() {
    return _PopupManager.removePopup(_PopupManager._toolSettingsId);
  }
  static getPopupPosition(el, pt, offset, size) {
    const containerBounds = Rectangle.create(el.getBoundingClientRect());
    const relativeBounds = Rectangle.createFromSize(size).offset(pt);
    const adjustedPosition = offsetAndContainInContainer(
      relativeBounds,
      containerBounds.getSize(),
      offset
    );
    const position = adjustedPosition.offset(containerBounds.topLeft());
    return position;
  }
};
_PopupManager._popups = [];
_PopupManager._editorId = "InputEditor";
_PopupManager._toolbarId = "Toolbar";
_PopupManager._htmlElementId = "HTMLElement";
_PopupManager._cardId = "Card";
_PopupManager._toolSettingsId = "ToolSettings";
_PopupManager._keyPalettePopupId = "KeyinPalette";
_PopupManager._defaultOffset = { x: 8, y: 8 };
_PopupManager.onPopupsChangedEvent = new PopupsChangedEvent();
let PopupManager = _PopupManager;
const _PopupRenderer = class _PopupRenderer extends reactExports.Component {
  constructor() {
    super(...arguments);
    this.state = {
      parentDocument: null,
      popups: PopupManager.popups
    };
    this._handleRefSet = (popupDiv) => {
      this.setState({ parentDocument: (popupDiv == null ? void 0 : popupDiv.ownerDocument) ?? null });
    };
    this._handlePopupsChangedEvent = (args) => {
      this.setState({ popups: args.popups });
    };
  }
  componentDidMount() {
    Logger.logInfo("PopupManager", `mounting PopupManager`);
    PopupManager.onPopupsChangedEvent.addListener(
      this._handlePopupsChangedEvent
    );
  }
  componentWillUnmount() {
    Logger.logInfo("PopupManager", `un-mounting PopupManager`);
    PopupManager.onPopupsChangedEvent.removeListener(
      this._handlePopupsChangedEvent
    );
  }
  render() {
    if (PopupManager.popupCount <= 0)
      return null;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: "appui-react-popup-render-container",
        ref: this._handleRefSet,
        children: this.state.parentDocument && this.state.popups.filter(
          (info) => (info.parent ?? // eslint-disable-next-line deprecation/deprecation
          info.parentDocument ?? this.context.ownerDocument) === this.state.parentDocument
        ).map((popupInfo) => {
          return /* @__PURE__ */ jsxRuntimeExports.jsx(reactExports.Fragment, { children: popupInfo.component }, popupInfo.id);
        })
      }
    );
  }
};
_PopupRenderer.contextType = WrapperContext;
let PopupRenderer = _PopupRenderer;
try {
  PopupsChangedEvent.displayName = "PopupsChangedEvent";
  PopupsChangedEvent.__docgenInfo = { "description": "Popups Changed Event class.", "displayName": "PopupsChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  isReactContent.displayName = "isReactContent";
  isReactContent.__docgenInfo = { "description": "ReactContent type guard.", "displayName": "isReactContent", "props": { "reactNode": { "defaultValue": null, "description": "", "name": "reactNode", "required": true, "type": { "name": "ReactNode" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  PopupManager.displayName = "PopupManager";
  PopupManager.__docgenInfo = { "description": "Popup Manager class", "displayName": "PopupManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  PopupManager.onPopupsChangedEvent.displayName = "PopupManager.onPopupsChangedEvent";
  PopupManager.onPopupsChangedEvent.__docgenInfo = { "description": "Popups Changed Event class.", "displayName": "PopupManager.onPopupsChangedEvent", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
try {
  PopupRenderer.displayName = "PopupRenderer";
  PopupRenderer.__docgenInfo = { "description": "Popup Renderer", "displayName": "PopupRenderer", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function copyStyles(targetDoc, sourceDoc = document) {
  const stylesheets = sourceDoc.adoptedStyleSheets ? Array.from([...sourceDoc.styleSheets, ...sourceDoc.adoptedStyleSheets]) : Array.from(sourceDoc.styleSheets);
  stylesheets.forEach((stylesheet) => {
    const css = stylesheet;
    if (stylesheet.href) {
      const newStyleElement = targetDoc.createElement("link");
      newStyleElement.rel = "stylesheet";
      newStyleElement.href = stylesheet.href;
      targetDoc.head.appendChild(newStyleElement);
    } else {
      if (css && css.cssRules && css.cssRules.length > 0) {
        const newStyleElement = targetDoc.createElement("style");
        Array.from(css.cssRules).forEach((rule) => {
          newStyleElement.appendChild(targetDoc.createTextNode(rule.cssText));
        });
        targetDoc.head.appendChild(newStyleElement);
      }
    }
  });
  const svgSymbolParent = sourceDoc.getElementById("__SVG_SPRITE_NODE__");
  if (svgSymbolParent) {
    targetDoc.body.appendChild(svgSymbolParent.cloneNode(true));
  }
}
const childHtml = `<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <style>
    html,
    body {
      height: 100%;
      width: 100%;
      margin: 0;
      overflow: hidden;
    }
    #root {
      height: 100%;
    }
  </style>
</head>
<body>
  <noscript>You need to enable JavaScript to run this app.</noscript>
  <div id="root"></div>
</body>
</html>`;
class InternalChildWindowManager {
  constructor() {
    this._openChildWindows = [];
    this._roots = {};
    this.close = (childWindowId, processWindowClose = true) => {
      const windowIndex = this.openChildWindows.findIndex(
        (openWindow) => openWindow.childWindowId === childWindowId
      );
      if (-1 === windowIndex)
        return false;
      const childWindow = this.openChildWindows[windowIndex];
      this.openChildWindows.splice(windowIndex, 1);
      if (processWindowClose) {
        childWindow.window.close();
      } else {
        const frontStageDef = UiFramework.frontstages.activeFrontstageDef;
        frontStageDef && frontStageDef.dockWidgetContainerByContainerId(childWindowId);
      }
      return true;
    };
  }
  get openChildWindows() {
    return this._openChildWindows;
  }
  /**
   * When using React 18, the `createRoot` function must be provided in order to render Popout content with React 18.
   * Do not call if using React 17 or before.
   *
   * Note: The type of the function is intentionally simplified here.
   *
   * @param createRootFn Function imported from `import { createRoot } from "react-dom/client";`
   * @beta Will be removed once the transition to React 18 is complete.
   */
  // istanbul ignore next: Result of this assignment is only visible in `open`, which is not tested.
  useCreateRoot(createRootFn) {
    this._createRoot = createRootFn;
  }
  /**
   * Abstracts ReactDOM.render to use either the _createRoot method or the default ReactDOM.render.
   * @param element Element to render.
   * @param container Container to render to.
   */
  // istanbul ignore next: Used in `open` which is not tested.
  render(element, container) {
    if (this._createRoot) {
      const childWindowId = UiFramework.childWindows.findId(
        container.ownerDocument.defaultView
      );
      if (childWindowId) {
        this._roots[childWindowId] = this._createRoot(container);
        this._roots[childWindowId].render(element);
      }
    } else {
      reactDomExports.render(element, container);
    }
  }
  /**
   * Returns the OpenChildWindowInfo for the related id.
   * @param childWindowId Id of the window to retrieve.
   * @returns undefined if not found.
   */
  find(childWindowId) {
    if (void 0 === childWindowId)
      return void 0;
    return this.openChildWindows.find(
      (openWindow) => openWindow.childWindowId === childWindowId
    );
  }
  /**
   * Return the childWindowId of the provided window.
   * @param contentWindow Window element to identify
   * @returns undefined if not found
   */
  findId(contentWindow) {
    if (!contentWindow)
      return void 0;
    const childWindow = this.openChildWindows.find(
      (openWindow) => openWindow.window === contentWindow
    );
    return childWindow == null ? void 0 : childWindow.childWindowId;
  }
  // istanbul ignore next: Used in `open` which is not tested.
  renderChildWindowContents(childWindow, childWindowId, content, title) {
    childWindow.document.title = title;
    if (childWindow.expectedHeight && childWindow.expectedWidth) {
      childWindow.deltaWidth = childWindow.expectedWidth - (childWindow.shouldUseOuterSized ? childWindow.outerWidth : childWindow.innerWidth);
      childWindow.deltaHeight = childWindow.expectedHeight - (childWindow.shouldUseOuterSized ? childWindow.outerHeight : childWindow.innerHeight);
    }
    const reactConnectionDiv = childWindow.document.getElementById("root");
    if (reactConnectionDiv && content && reactExports.isValidElement(content)) {
      this._openChildWindows.push({
        childWindowId,
        window: childWindow,
        parentWindow: window
      });
      let element;
      if (content.props.widgetDef) {
        const tabId = content.props.widgetDef.id;
        element = /* @__PURE__ */ jsxRuntimeExports.jsx(Provider, { store: UiFramework.store, children: /* @__PURE__ */ jsxRuntimeExports.jsx(TabIdContext.Provider, { value: tabId, children: /* @__PURE__ */ jsxRuntimeExports.jsx(UiStateStorageHandler, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(ThemeManager, { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "uifw-child-window-container-host", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(PopupRenderer, {}),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ModalDialogRenderer, {}),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ModelessDialogRenderer, {}),
          /* @__PURE__ */ jsxRuntimeExports.jsx(CursorPopupMenu, {}),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-child-window-container nz-widget-widget", children: content })
        ] }) }) }) }) });
      } else {
        element = /* @__PURE__ */ jsxRuntimeExports.jsx(Provider, { store: UiFramework.store, children: /* @__PURE__ */ jsxRuntimeExports.jsx(UiStateStorageHandler, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(ThemeManager, { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "uifw-child-window-container-host", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(PopupRenderer, {}),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ModalDialogRenderer, {}),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ModelessDialogRenderer, {}),
          /* @__PURE__ */ jsxRuntimeExports.jsx(CursorPopupMenu, {}),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-child-window-container nz-widget-widget", children: content })
        ] }) }) }) });
      }
      setTimeout(() => {
        copyStyles(childWindow.document);
        setTimeout(() => {
          this.render(element, reactConnectionDiv);
        });
      });
      childWindow.addEventListener("pagehide", () => {
        const frontStageDef = UiFramework.frontstages.activeFrontstageDef;
        if (!frontStageDef)
          return;
        frontStageDef.saveChildWindowSizeAndPosition(
          childWindowId,
          childWindow
        );
        this.close(childWindowId, false);
        if (this._roots[childWindowId]) {
          this._roots[childWindowId].unmount();
        } else
          reactDomExports.unmountComponentAtNode(reactConnectionDiv);
      });
    }
  }
  /** Close all child/pop-out windows. This typically is called when the frontstage is changed. */
  closeAll() {
    this.openChildWindows.forEach(
      (openChildWindow) => openChildWindow.window.close()
    );
    this._openChildWindows = [];
  }
  // istanbul ignore next: Used in `open` which is not tested.
  adjustWidowLocation(location) {
    const outLocation = { ...location };
    if (0 === location.top && 0 === location.left) {
      outLocation.left = window.outerWidth / 2 + window.screenX - location.width / 2;
      outLocation.top = window.outerHeight / 2 + window.screenY - location.height / 2;
    }
    return outLocation;
  }
  /**
   * Open a new child window.
   * @param childWindowId Id to assign to the newly created window.
   * @param title Title to display on the window.
   * @param content ReactNode to be rendered in the window.
   * @param location Position and size information
   * @param useDefaultPopoutUrl use "/iTwinPopup.html" as the window Url, "" otherwise.
   * @returns true if the window is opened successfully.
   */
  // istanbul ignore next: Would require mocking window.open to return a window object.
  open(childWindowId, title, content, location, useDefaultPopoutUrl) {
    if (this.openChildWindows.findIndex(
      (openWindow) => openWindow.childWindowId === childWindowId
    ) >= 0) {
      return false;
    }
    location = this.adjustWidowLocation(location);
    const url = useDefaultPopoutUrl ? "/iTwinPopup.html" : "";
    const childWindow = window.open(
      url,
      "",
      `width=${location.width},height=${location.height},left=${location.left},top=${location.top},menubar=no,resizable=yes,scrollbars=no,status=no,location=no`
    );
    if (!childWindow)
      return false;
    childWindow.expectedHeight = location.height;
    childWindow.expectedWidth = location.width;
    childWindow.shouldUseOuterSized = navigator.userAgent.toLowerCase().indexOf("edg/") > -1;
    if (0 === url.length) {
      childWindow.document.write(childHtml);
      this.renderChildWindowContents(
        childWindow,
        childWindowId,
        content,
        title
      );
    } else {
      childWindow.addEventListener(
        "load",
        () => {
          this.renderChildWindowContents(
            childWindow,
            childWindowId,
            content,
            title
          );
        },
        false
      );
    }
    window.addEventListener(
      "unload",
      () => {
        const frontStageDef = UiFramework.frontstages.activeFrontstageDef;
        if (frontStageDef) {
          this.close(childWindowId, true);
        }
      },
      false
    );
    return true;
  }
}
try {
  InternalChildWindowManager.displayName = "InternalChildWindowManager";
  InternalChildWindowManager.__docgenInfo = { "description": "Supports opening a child browser window from the main application window. The child window is managed by the main application\nand is running in the same security context. The application must deliver the html file iTwinPopup.html along side its index.html.\nSee also: [Child Window Manager]($docs/learning/ui/appui-react/ChildWindows.md)", "displayName": "InternalChildWindowManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const FrameworkReducer = combineReducers({
  configurableUiState: ConfigurableUiReducer,
  sessionState: SessionStateReducer
});
class ReducerRegistry {
  /** ReducerRegistry constructor that initializes an empty reducer map to be populated by called to registerReducer. */
  constructor() {
    this._reducers = {};
  }
  /** Call to register a reducer and its name. */
  registerReducer(name, reducer) {
    if (this._reducers[name]) {
      throw new UiError(
        UiFramework.loggerCategory(this),
        `Redux Reducer with matching name of '${name}' is already registered`
      );
    }
    this._reducers = { ...this._reducers, [name]: reducer };
    if (this._onReducerListChanged) {
      this._onReducerListChanged(this._reducers);
    }
  }
  /** Supports a single listener which should be the Redux store, see [[StoreManager]].
   * @internal
   */
  setChangeListener(listener) {
    this._onReducerListChanged = listener;
  }
  /** Returns map of registered Reducers. */
  getReducers() {
    return this._reducers;
  }
  /** Clear reducers only required for unit testing
   * @internal
   */
  clearReducers() {
    this._reducers = {};
    if (this._onReducerListChanged) {
      this._onReducerListChanged(this._reducers);
    }
  }
}
const ReducerRegistryInstance = new ReducerRegistry();
const _StateManager = class _StateManager {
  /**
   * StateManager construct.
   * @param defaultReducers Default set of Reducers used by the current application. If this object does not contain the [[FrameworkReducer]] it will automatically be added.
   */
  constructor(defaultReducers) {
    if (defaultReducers) {
      this._defaultReducers = defaultReducers;
      const keys = Object.keys(defaultReducers);
      if (-1 === keys.findIndex((key) => key === "frameworkState"))
        this._defaultReducers = {
          ...defaultReducers,
          frameworkState: FrameworkReducer
        };
    } else {
      this._defaultReducers = { frameworkState: FrameworkReducer };
    }
    const dynamicallyRegisteredReducers = ReducerRegistryInstance.getReducers();
    let allReducers = this.combineDynamicAndDefaultReducers(
      dynamicallyRegisteredReducers,
      this._defaultReducers
    );
    this._store = createStore$1(
      allReducers,
      window.__REDUX_DEVTOOLS_EXTENSION__ && window.__REDUX_DEVTOOLS_EXTENSION__()
    );
    ReducerRegistryInstance.setChangeListener((newDynamicReducers) => {
      allReducers = this.combineDynamicAndDefaultReducers(
        newDynamicReducers,
        this._defaultReducers
      );
      this._store.replaceReducer(allReducers);
    });
    _StateManager._singletonStore = this._store;
  }
  /** Method to test if the StateManager has been initialized.
   * @param suppressErrorLog If true and the StateManager is not initialized a log message will be output.
   */
  static isInitialized(suppressErrorLog = false) {
    if (!_StateManager._singletonStore) {
      if (!suppressErrorLog)
        Logger.logError(
          _StateManager._LOG_CATEGORY,
          "Accessing store before it is created."
        );
      return false;
    } else {
      return true;
    }
  }
  /** Return the current Redux store. Useful when using connect method to connect a Ui Component to the store. */
  static get store() {
    if (_StateManager.isInitialized()) {
      return _StateManager._singletonStore;
    } else {
      throw new UiError(
        _StateManager._LOG_CATEGORY,
        `Redux Store has not been initialized`
      );
    }
  }
  /** Return the current state from the Redux store. */
  static get state() {
    if (_StateManager.isInitialized()) {
      return _StateManager._singletonStore.getState();
    } else {
      return void 0;
    }
  }
  combineDynamicAndDefaultReducers(dynamicallyRegisteredReducers, defaultReducers) {
    const allReducers = {
      ...defaultReducers,
      ...dynamicallyRegisteredReducers
    };
    return combineReducers$1(allReducers);
  }
  /** To be used only in unit test
   * @internal
   */
  static clearStore() {
    _StateManager._singletonStore = void 0;
  }
};
_StateManager._LOG_CATEGORY = "StateManager";
let StateManager = _StateManager;
class ModelOverrideProvider {
  constructor(modelIds, defaultAppearance) {
    this.modelIds = modelIds;
    this.defaultAppearance = defaultAppearance;
  }
  addFeatureOverrides(overrides, _viewport) {
    overrides.setDefaultOverrides(this.defaultAppearance, true);
    const appearance = FeatureAppearance.fromJSON({});
    this.modelIds.forEach((modelId) => {
      overrides.override({ modelId, appearance, onConflict: "replace" });
    });
  }
}
class SubCategoryOverrideProvider {
  constructor(subCategoryIds, defaultAppearance) {
    this.subCategoryIds = subCategoryIds;
    this.defaultAppearance = defaultAppearance;
  }
  addFeatureOverrides(overrides, _viewport) {
    overrides.setDefaultOverrides(this.defaultAppearance, true);
    const appearance = FeatureAppearance.fromJSON({});
    this.subCategoryIds.forEach((subCategoryId) => {
      overrides.override({ subCategoryId, appearance, onConflict: "replace" });
    });
  }
}
const _HideIsolateEmphasizeActionHandler = class _HideIsolateEmphasizeActionHandler {
  /** String Id sent to allow UI to refresh its display state.  */
  static get hideIsolateEmphasizeUiSyncId() {
    return "selection-context-emphasize-elements-changed";
  }
};
_HideIsolateEmphasizeActionHandler.emphasizeElementsChanged = new BeEvent();
let HideIsolateEmphasizeActionHandler = _HideIsolateEmphasizeActionHandler;
const _HideIsolateEmphasizeManager = class _HideIsolateEmphasizeManager extends HideIsolateEmphasizeActionHandler {
  /** Returns true if there is only a category hilited. */
  static categorySelected(vp) {
    const subcategories = vp.iModel.hilited.subcategories;
    return subcategories.size === 1;
  }
  /** Returns true if only a model is hilited. */
  static modelSelected(vp) {
    const models = vp.iModel.hilited.models;
    return models.size === 1;
  }
  /**
   * Hide the hilited categories.
   * @param vp Viewport to affect
   */
  static hideSelectedCategory(vp) {
    const ids = vp.iModel.hilited.subcategories.toId64Set();
    vp.changeCategoryDisplay(ids, false);
  }
  /** Get sub categories that relate to the category Id */
  static async _getSubCategories(iModelConnection, categoryIds) {
    const allSubcats = [];
    const request = iModelConnection.subcategories.load(categoryIds);
    if (request)
      await request.promise;
    for (const categoryId of categoryIds) {
      const subcats = iModelConnection.subcategories.getSubCategories(categoryId);
      if (subcats)
        allSubcats.push(...subcats);
    }
    return allSubcats;
  }
  /**
   * Emphasize hilited categories.
   * @param vp Viewport to affect
   */
  static async emphasizeSelectedCategory(vp) {
    const ids = vp.iModel.hilited.subcategories.toId64Array();
    if (ids.length === 0)
      return;
    const defaultAppearance = EmphasizeElements.getOrCreate(vp).createDefaultAppearance();
    EmphasizeElements.clear(vp);
    const subcats = await _HideIsolateEmphasizeManager._getSubCategories(
      vp.iModel,
      ids
    );
    vp.addFeatureOverrideProvider(
      new SubCategoryOverrideProvider(subcats, defaultAppearance)
    );
  }
  /**
   * Hide the selected model
   * @param vp Viewport to affect
   */
  static hideSelectedModel(vp) {
    const ids = vp.iModel.hilited.models.toId64Array();
    if (ids.length === 0)
      return;
    vp.changeModelDisplay(ids, false);
  }
  /**
   * Isolate the selected model
   * @param vp Viewport to affect
   */
  static emphasizeSelectedModel(vp) {
    const ids = vp.iModel.hilited.models.toId64Array();
    if (ids.length === 0)
      return;
    const defaultAppearance = EmphasizeElements.getOrCreate(vp).createDefaultAppearance();
    EmphasizeElements.clear(vp);
    vp.addFeatureOverrideProvider(
      new ModelOverrideProvider(ids, defaultAppearance)
    );
  }
  /**
   * Isolate the selected elements
   * @param vp Viewport to affect
   */
  static isolateSelected(vp) {
    EmphasizeElements.getOrCreate(vp).isolateSelectedElements(vp, true, false);
  }
  /**
   * Hide the selected elements
   * @param vp Viewport to affect
   */
  static hideSelected(vp) {
    EmphasizeElements.getOrCreate(vp).hideSelectedElements(vp, false, false);
  }
  /**
   * Clear Hidden,Isolated, or Emphasized elements in specified view
   * @param vp Viewport to affect
   *
   */
  static clearEmphasize(vp) {
    if (vp) {
      EmphasizeElements.getOrCreate(vp).clearEmphasizedElements(vp);
      EmphasizeElements.clear(vp);
    }
  }
  /**
   * Emphasize the selected elements from either presentation layer's logical selection or selected graphics
   * @param vp Viewport to affect
   * @param emphasisSilhouette defaults to true
   */
  static async emphasizeSelected(vp, emphasisSilhouette = true) {
    if (_HideIsolateEmphasizeManager.categorySelected(vp)) {
      await _HideIsolateEmphasizeManager.emphasizeSelectedCategory(vp);
      return;
    } else if (_HideIsolateEmphasizeManager.modelSelected(vp)) {
      _HideIsolateEmphasizeManager.emphasizeSelectedModel(vp);
      return;
    }
    const ee = EmphasizeElements.getOrCreate(vp);
    ee.wantEmphasis = emphasisSilhouette;
    ee.emphasizeSelectedElements(vp, void 0, true, false);
    vp.isFadeOutActive = true;
  }
  /**
   * Isolate hilited models.
   * @param vp Viewport to affect
   */
  static async isolateSelectedModel(vp) {
    const ids = vp.iModel.hilited.models.toId64Array();
    if (ids.length === 0)
      return;
    await vp.replaceViewedModels(ids);
  }
  /**
   * Isolate hilited categories.
   * @param vp Viewport to affect
   */
  static isolateSelectedCategory(vp) {
    const ids = vp.iModel.hilited.subcategories.toId64Set();
    const categoriesToDrop = [];
    vp.view.categorySelector.categories.forEach((categoryId) => {
      if (!ids.has(categoryId))
        categoriesToDrop.push(categoryId);
    });
    vp.changeCategoryDisplay(categoriesToDrop, false);
    vp.changeCategoryDisplay(ids, true);
  }
  static async getSelectionSetElementModels(iModel) {
    const props = await iModel.elements.getProps(iModel.selectionSet.elements);
    const modelIds = /* @__PURE__ */ new Set();
    for (const prop of props)
      if (prop.model)
        modelIds.add(prop.model);
    return modelIds;
  }
  static async getSelectionSetElementCategories(iModel) {
    const props = await iModel.elements.getProps(
      iModel.selectionSet.elements
    );
    const categoryIds = /* @__PURE__ */ new Set();
    for (const prop of props)
      if (prop.category)
        categoryIds.add(prop.category);
    return categoryIds;
  }
  /**
   * Isolate either based on Presentation selection, if defined, else the selected graphic elements
   * @param vp Viewport to affect
   */
  static async isolateCommand(vp) {
    if (_HideIsolateEmphasizeManager.categorySelected(vp)) {
      _HideIsolateEmphasizeManager.isolateSelectedCategory(vp);
      return;
    } else if (_HideIsolateEmphasizeManager.modelSelected(vp)) {
      await _HideIsolateEmphasizeManager.isolateSelectedModel(vp);
      return;
    }
  }
  /**
   * Isolate model from selected elements
   * @param vp Viewport to affect
   */
  static async isolateSelectedElementsModel(vp) {
    const modelsToKeep = new Set(
      await _HideIsolateEmphasizeManager.getSelectionSetElementModels(vp.iModel)
    );
    if (vp.view.isSpatialView()) {
      const modelsToTurnOff = [...vp.view.modelSelector.models].filter(
        (modelId) => !modelsToKeep.has(modelId)
      );
      this.updateModelOverride(vp, modelsToTurnOff);
    }
    await vp.replaceViewedModels(modelsToKeep);
  }
  /**
   * Isolate the selected category found in SelectionSet elements
   * @param vp Viewport to affect
   */
  static async isolateSelectedElementsCategory(vp) {
    const categoriesToKeep = new Set(
      await _HideIsolateEmphasizeManager.getSelectionSetElementCategories(
        vp.iModel
      )
    );
    const categoriesToTurnOff = [...vp.view.categorySelector.categories].filter(
      (categoryId) => !categoriesToKeep.has(categoryId)
    );
    vp.changeCategoryDisplay(categoriesToTurnOff, false);
    vp.changeCategoryDisplay(categoriesToKeep, true);
    this.updateCategoryOverride(vp, categoriesToTurnOff);
  }
  /**
   * Hide either based on Presentation selection, if defined, else the selected graphic elements
   * @param vp Viewport to affect
   */
  static async hideCommand(vp) {
    if (_HideIsolateEmphasizeManager.categorySelected(vp)) {
      _HideIsolateEmphasizeManager.hideSelectedCategory(vp);
      return;
    } else if (_HideIsolateEmphasizeManager.modelSelected(vp)) {
      _HideIsolateEmphasizeManager.hideSelectedModel(vp);
      return;
    }
    EmphasizeElements.getOrCreate(vp).hideSelectedElements(vp, false, false);
  }
  /**
   * Hide the models defined by the elements in the current SelectionSet
   * @param vp Viewport to affect
   */
  static async hideSelectedElementsModel(vp) {
    const modelIds = await _HideIsolateEmphasizeManager.getSelectionSetElementModels(vp.iModel);
    vp.changeModelDisplay(modelIds, false);
    this.updateModelOverride(vp, [...modelIds]);
  }
  /**
   * Clear (restore) the previously hidden/isolated models hidden by hideSelectedElementsModel
   * @param vp Viewport to affect
   */
  static clearOverrideModels(vp) {
    const ids = this._overrideModelIds.get(vp);
    if (ids) {
      vp.changeModelDisplay([...ids], true);
      this.clearModelOverride(vp);
    }
  }
  /**
   * Determine if models are hidden by hideSelectedElementsModel or isolateSelectedElementsModel
   * @param vp Viewport to affect
   */
  static isOverrideModels(vp) {
    const ids = this._overrideModelIds.get(vp);
    return ids ? [...ids].length > 0 : false;
  }
  /**
   * Determine if categories are hidden by hideSelectedElementsCategory or isolateSelectedElementsCategory
   * @param vp Viewport to affect
   */
  static isOverrideCategories(vp) {
    const ids = this._overrideCategoryIds.get(vp);
    return ids ? [...ids].length > 0 : false;
  }
  /**
   * Hide the categories defined by the elements in the current SelectionSet
   * @param vp Viewport to affect
   */
  static async hideSelectedElementsCategory(vp) {
    const categoryIds = await _HideIsolateEmphasizeManager.getSelectionSetElementCategories(
      vp.iModel
    );
    vp.changeCategoryDisplay(categoryIds, false);
    this.updateCategoryOverride(vp, [...categoryIds]);
  }
  /**
   * Clear (restore) the previously hidden categories hidden by hideSelectedElementsCategory
   * @param vp Viewport to affect
   */
  static clearOverrideCategories(vp) {
    const ids = this._overrideCategoryIds.get(vp);
    if (ids) {
      vp.changeCategoryDisplay([...ids], true);
      this.clearCategoryOverride(vp);
    }
  }
  /** Checks to see if any featureOverrideProviders are active */
  areFeatureOverridesActive(vp) {
    const emphasizeElementsProvider = vp.findFeatureOverrideProviderOfType(
      EmphasizeElements
    );
    if (void 0 !== emphasizeElementsProvider && emphasizeElementsProvider.isActive(vp))
      return true;
    else if (vp.neverDrawn && vp.neverDrawn.size > 0)
      return true;
    const modelOverrideProvider = vp.findFeatureOverrideProviderOfType(
      ModelOverrideProvider
    );
    if (void 0 !== modelOverrideProvider && modelOverrideProvider.modelIds.length > 0)
      return true;
    const subCategoryOverrideProvider = vp.findFeatureOverrideProviderOfType(
      SubCategoryOverrideProvider
    );
    if (void 0 !== subCategoryOverrideProvider && subCategoryOverrideProvider.subCategoryIds.length > 0)
      return true;
    if (_HideIsolateEmphasizeManager.isOverrideCategories(vp))
      return true;
    if (_HideIsolateEmphasizeManager.isOverrideModels(vp))
      return true;
    return false;
  }
  /**
   * Function that is run when `IsolateSelectedElementsModel` tool button is pressed
   */
  async processIsolateSelectedElementsModel() {
    const vp = IModelApp.viewManager.selectedView;
    if (!vp)
      return;
    await _HideIsolateEmphasizeManager.isolateSelectedElementsModel(vp);
    HideIsolateEmphasizeActionHandler.emphasizeElementsChanged.raiseEvent({
      viewport: vp,
      action: "IsolateSelectedModels"
      /* IsolateSelectedModels */
    });
    SyncUiEventDispatcher.dispatchSyncUiEvent(
      HideIsolateEmphasizeActionHandler.hideIsolateEmphasizeUiSyncId
    );
  }
  /**
   * Function that is run when `IsolateSelectedElementsCategory` tool button is pressed
   */
  async processIsolateSelectedElementsCategory() {
    const vp = IModelApp.viewManager.selectedView;
    if (!vp)
      return;
    await _HideIsolateEmphasizeManager.isolateSelectedElementsCategory(vp);
    HideIsolateEmphasizeActionHandler.emphasizeElementsChanged.raiseEvent({
      viewport: vp,
      action: "IsolateSelectedCategories"
      /* IsolateSelectedCategories */
    });
    SyncUiEventDispatcher.dispatchSyncUiEvent(
      HideIsolateEmphasizeActionHandler.hideIsolateEmphasizeUiSyncId
    );
  }
  /**
   * Function that is run when `IsolateSelected` tool button is pressed
   */
  async processIsolateSelected(clearSelection = true) {
    const vp = IModelApp.viewManager.selectedView;
    if (!vp)
      return;
    _HideIsolateEmphasizeManager.isolateSelected(vp);
    HideIsolateEmphasizeActionHandler.emphasizeElementsChanged.raiseEvent({
      viewport: vp,
      action: "IsolateSelectedElements"
      /* IsolateSelectedElements */
    });
    SyncUiEventDispatcher.dispatchSyncUiEvent(
      HideIsolateEmphasizeActionHandler.hideIsolateEmphasizeUiSyncId
    );
    if (!clearSelection)
      return;
    const selection = vp.view.iModel.selectionSet;
    if (selection.isActive)
      selection.emptyAll();
  }
  /**
   * Function that is run when `HideSelectedElementsModel` tool button is pressed
   */
  async processHideSelectedElementsModel() {
    const vp = IModelApp.viewManager.selectedView;
    if (!vp)
      return;
    await _HideIsolateEmphasizeManager.hideSelectedElementsModel(vp);
    HideIsolateEmphasizeActionHandler.emphasizeElementsChanged.raiseEvent({
      viewport: vp,
      action: "HideSelectedModels"
      /* HideSelectedModels */
    });
    SyncUiEventDispatcher.dispatchSyncUiEvent(
      HideIsolateEmphasizeActionHandler.hideIsolateEmphasizeUiSyncId
    );
  }
  /**
   * Function that is run when `HideSelectedElementsCategory` tool button is pressed
   */
  async processHideSelectedElementsCategory() {
    const vp = IModelApp.viewManager.selectedView;
    if (!vp)
      return;
    await _HideIsolateEmphasizeManager.hideSelectedElementsCategory(vp);
    HideIsolateEmphasizeActionHandler.emphasizeElementsChanged.raiseEvent({
      viewport: vp,
      action: "HideSelectedCategories"
      /* HideSelectedCategories */
    });
    SyncUiEventDispatcher.dispatchSyncUiEvent(
      HideIsolateEmphasizeActionHandler.hideIsolateEmphasizeUiSyncId
    );
  }
  /**
   * Function that is run when `HideSelected` tool button is pressed
   */
  async processHideSelected() {
    const vp = IModelApp.viewManager.selectedView;
    if (!vp)
      return;
    _HideIsolateEmphasizeManager.hideSelected(vp);
    HideIsolateEmphasizeActionHandler.emphasizeElementsChanged.raiseEvent({
      viewport: vp,
      action: "HideSelectedElements"
      /* HideSelectedElements */
    });
    SyncUiEventDispatcher.dispatchSyncUiEvent(
      HideIsolateEmphasizeActionHandler.hideIsolateEmphasizeUiSyncId
    );
    const selection = vp.view.iModel.selectionSet;
    if (selection.isActive)
      selection.emptyAll();
  }
  /**
   * Function that is run when `EmphasizeSelected` tool button is pressed
   */
  async processEmphasizeSelected() {
    const vp = IModelApp.viewManager.selectedView;
    if (!vp)
      return;
    await _HideIsolateEmphasizeManager.emphasizeSelected(vp);
    HideIsolateEmphasizeActionHandler.emphasizeElementsChanged.raiseEvent({
      viewport: vp,
      action: "EmphasizeSelectedElements"
      /* EmphasizeSelectedElements */
    });
    SyncUiEventDispatcher.dispatchSyncUiEvent(
      HideIsolateEmphasizeActionHandler.hideIsolateEmphasizeUiSyncId
    );
    const selection = vp.view.iModel.selectionSet;
    if (selection.isActive)
      selection.emptyAll();
  }
  /**
   * Function that is run when `ClearEmphasize` tool button is pressed
   */
  async processClearEmphasize() {
    const vp = IModelApp.viewManager.selectedView;
    if (!vp)
      return;
    _HideIsolateEmphasizeManager.clearEmphasize(vp);
    HideIsolateEmphasizeActionHandler.emphasizeElementsChanged.raiseEvent({
      viewport: vp,
      action: "ClearHiddenIsolatedEmphasized"
      /* ClearHiddenIsolatedEmphasized */
    });
    SyncUiEventDispatcher.dispatchSyncUiEvent(
      HideIsolateEmphasizeActionHandler.hideIsolateEmphasizeUiSyncId
    );
  }
  /**
   * Function that is run when `ClearOverrideModels` tool button is pressed
   */
  async processClearOverrideModels() {
    const vp = IModelApp.viewManager.selectedView;
    if (!vp)
      return;
    _HideIsolateEmphasizeManager.clearOverrideModels(vp);
    HideIsolateEmphasizeActionHandler.emphasizeElementsChanged.raiseEvent({
      viewport: vp,
      action: "ClearOverrideModels"
      /* ClearOverrideModels */
    });
    SyncUiEventDispatcher.dispatchSyncUiEvent(
      HideIsolateEmphasizeActionHandler.hideIsolateEmphasizeUiSyncId
    );
  }
  /**
   * Function that is run when `ClearOverrideCategories` tool button is pressed
   */
  async processClearOverrideCategories() {
    const vp = IModelApp.viewManager.selectedView;
    if (!vp)
      return;
    _HideIsolateEmphasizeManager.clearOverrideCategories(vp);
    HideIsolateEmphasizeActionHandler.emphasizeElementsChanged.raiseEvent({
      viewport: vp,
      action: "ClearOverrideCategories"
      /* ClearOverrideCategories */
    });
    SyncUiEventDispatcher.dispatchSyncUiEvent(
      HideIsolateEmphasizeActionHandler.hideIsolateEmphasizeUiSyncId
    );
  }
  /**
   * Add category ids to the category override cache (hidden or isolated categories)
   */
  static updateCategoryOverride(vp, ids) {
    const prevIds = this._overrideCategoryIds.get(vp);
    const newIds = [...prevIds || [], ...ids];
    this._overrideCategoryIds.set(vp, new Set(newIds));
  }
  /**
   * Add model ids to the model override cache (hidden or isolated models)
   */
  static updateModelOverride(vp, ids) {
    const prevIds = this._overrideModelIds.get(vp);
    const newIds = [...prevIds || [], ...ids];
    this._overrideModelIds.set(vp, new Set(newIds));
  }
  /**
   * Return the list of category overrides (hidden or isolated categories)
   */
  static getCategoryOverrides(vp) {
    return this._overrideCategoryIds.get(vp);
  }
  /**
   * Return the list of model overrides (hidden or isolated models)
   */
  static getModelOverrides(vp) {
    return this._overrideModelIds.get(vp);
  }
  /**
   * Clear the category override cache (hidden or isolated categories)
   */
  static clearCategoryOverride(vp) {
    this._overrideCategoryIds.delete(vp);
  }
  /**
   * Clear the model override cache (hidden or isolated models)
   */
  static clearModelOverride(vp) {
    this._overrideModelIds.delete(vp);
  }
};
_HideIsolateEmphasizeManager._overrideCategoryIds = /* @__PURE__ */ new Map();
_HideIsolateEmphasizeManager._overrideModelIds = /* @__PURE__ */ new Map();
let HideIsolateEmphasizeManager = _HideIsolateEmphasizeManager;
var BackstageItemUtilities;
((BackstageItemUtilities2) => {
  function createStageLauncher(frontstageId, groupPriority, itemPriority, label, subtitle, icon, overrides) {
    return {
      groupPriority,
      icon,
      id: frontstageId,
      itemPriority,
      label,
      stageId: frontstageId,
      subtitle,
      ...overrides
    };
  }
  BackstageItemUtilities2.createStageLauncher = createStageLauncher;
  function createActionItem(itemId, groupPriority, itemPriority, execute, label, subtitle, icon, overrides) {
    return {
      execute,
      groupPriority,
      icon,
      id: itemId,
      itemPriority,
      label,
      subtitle,
      ...overrides
    };
  }
  BackstageItemUtilities2.createActionItem = createActionItem;
})(BackstageItemUtilities || (BackstageItemUtilities = {}));
function ModalSettingsStage({
  initialSettingsTabId
}) {
  var _a2, _b2;
  const { translate } = useTranslation();
  const id = ((_a2 = UiFramework.frontstages.activeFrontstageDef) == null ? void 0 : _a2.id) ?? "none";
  const stageUsage = ((_b2 = UiFramework.frontstages.activeFrontstageDef) == null ? void 0 : _b2.usage) ?? StageUsage.General;
  const tabEntries = UiFramework.settingsManager.getSettingEntries(
    id,
    stageUsage
  );
  const currentSettingsTab = reactExports.useCallback(() => {
    const categoryToFind = initialSettingsTabId ? initialSettingsTabId.toLowerCase() : tabEntries[0].tabId.toLowerCase();
    let foundTab = tabEntries.find(
      (entry) => entry.tabId.toLowerCase() === categoryToFind
    );
    if (!foundTab)
      foundTab = tabEntries.find(
        (entry) => entry.label.toLowerCase() === categoryToFind
      );
    return foundTab;
  }, [initialSettingsTabId, tabEntries]);
  reactExports.useEffect(() => {
    const handleFrontstageCloseRequested = ({
      modalFrontstage: modalFrontstage2,
      stageCloseFunc
    }) => {
      if (modalFrontstage2 instanceof SettingsModalFrontstage && stageCloseFunc) {
        UiFramework.settingsManager.closeSettingsContainer(stageCloseFunc);
      }
    };
    return UiFramework.frontstages.onCloseModalFrontstageRequestedEvent.addListener(
      handleFrontstageCloseRequested
    );
  }, [tabEntries]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-settings-container", children: tabEntries.length ? /* @__PURE__ */ jsxRuntimeExports.jsx(
    SettingsContainer,
    {
      tabs: tabEntries,
      currentSettingsTab: currentSettingsTab(),
      settingsManager: UiFramework.settingsManager
    }
  ) : /* @__PURE__ */ jsxRuntimeExports.jsx(Centered, { children: translate("settings.noSettingsAvailable") }) });
}
const _SettingsModalFrontstage = class _SettingsModalFrontstage {
  constructor(initialSettingsTabId) {
    this.initialSettingsTabId = initialSettingsTabId;
    this.title = UiFramework.translate("settings.settingsStageLabel");
    this.notifyCloseRequest = true;
  }
  get content() {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(ModalSettingsStage, { initialSettingsTabId: this.initialSettingsTabId });
  }
  static getBackstageActionItem(groupPriority, itemPriority) {
    return BackstageItemUtilities.createActionItem(
      _SettingsModalFrontstage.id,
      groupPriority,
      itemPriority,
      () => UiFramework.frontstages.openModalFrontstage(
        new _SettingsModalFrontstage()
      ),
      UiFramework.translate("settings.settingsStageLabel"),
      void 0,
      svgSettings,
      { isHidden: _SettingsModalFrontstage.noSettingsAvailable() }
    );
  }
  static showSettingsStage(initialSettingsTab) {
    if (UiFramework.settingsManager.providers.length) {
      if (UiFramework.frontstages.activeModalFrontstage && UiFramework.frontstages.activeModalFrontstage instanceof _SettingsModalFrontstage) {
        if (initialSettingsTab) {
          UiFramework.settingsManager.activateSettingsTab(initialSettingsTab);
          return;
        }
      }
      UiFramework.frontstages.openModalFrontstage(
        new _SettingsModalFrontstage(initialSettingsTab)
      );
    } else {
      const detailedMessage = UiFramework.translate(
        "settings.noSettingsProvidersRegistered"
      );
      const info = new NotifyMessageDetails(
        OutputMessagePriority.Info,
        UiFramework.translate("settings.noSettingsAvailable"),
        detailedMessage,
        OutputMessageType.Toast
      );
      IModelApp.notifications.outputMessage(info);
      Logger.logInfo(UiFramework.loggerCategory(this), detailedMessage);
    }
  }
};
_SettingsModalFrontstage.id = "appui-react.modalSettingsStage";
_SettingsModalFrontstage.noSettingsAvailable = () => new ConditionalBooleanValue(
  () => 0 === UiFramework.settingsManager.providers.length,
  [SyncUiEventId.SettingsProvidersChanged]
);
let SettingsModalFrontstage = _SettingsModalFrontstage;
try {
  SettingsModalFrontstage.displayName = "SettingsModalFrontstage";
  SettingsModalFrontstage.__docgenInfo = { "description": "Modal frontstage displaying and editing settings from registered settings providers. See [SettingsManager]($core-react)\nand [SettingsContainer]($core-react).", "displayName": "SettingsModalFrontstage", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
const _OpenSettingsTool = class _OpenSettingsTool extends Tool {
  // istanbul ignore next
  static get minArgs() {
    return 0;
  }
  // istanbul ignore next
  static get maxArgs() {
    return 1;
  }
  async run(settingCategory) {
    SettingsModalFrontstage.showSettingsStage(settingCategory);
    return true;
  }
  async parseAndRun(...args) {
    return this.run(args[0]);
  }
};
_OpenSettingsTool.toolId = "OpenSettings";
_OpenSettingsTool.iconSpec = svgSettings;
let OpenSettingsTool = _OpenSettingsTool;
const openSettingTools = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  OpenSettingsTool
}, Symbol.toStringTag, { value: "Module" }));
const _RestoreFrontstageLayoutTool = class _RestoreFrontstageLayoutTool extends Tool {
  // istanbul ignore next
  static get minArgs() {
    return 0;
  }
  // istanbul ignore next
  static get maxArgs() {
    return 1;
  }
  async run(frontstageId) {
    let frontstageDef;
    if (frontstageId) {
      frontstageDef = await UiFramework.frontstages.getFrontstageDef(
        frontstageId
      );
    } else {
      frontstageDef = UiFramework.frontstages.activeFrontstageDef;
    }
    if (frontstageDef)
      frontstageDef.restoreLayout();
    else
      IModelApp.notifications.outputMessage(
        new NotifyMessageDetails(
          OutputMessagePriority.Info,
          UiFramework.translate("tools.RestoreFrontstageLayout.noStageFound")
        )
      );
    return true;
  }
  async parseAndRun(...args) {
    return this.run(args[0]);
  }
};
_RestoreFrontstageLayoutTool.toolId = "RestoreFrontstageLayout";
_RestoreFrontstageLayoutTool.iconSpec = svgViewLayouts;
let RestoreFrontstageLayoutTool = _RestoreFrontstageLayoutTool;
const _RestoreAllFrontstagesTool = class _RestoreAllFrontstagesTool extends Tool {
  async run() {
    const frontstages = InternalFrontstageManager.frontstageDefs;
    for (const [, frontstage] of frontstages) {
      frontstage.restoreLayout();
    }
    return true;
  }
};
_RestoreAllFrontstagesTool.toolId = "RestoreAllFrontstages";
_RestoreAllFrontstagesTool.iconSpec = svgViewLayouts;
let RestoreAllFrontstagesTool = _RestoreAllFrontstagesTool;
const restoreLayoutTools = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  RestoreAllFrontstagesTool,
  RestoreFrontstageLayoutTool
}, Symbol.toStringTag, { value: "Module" }));
const _FocusToolSettings = class _FocusToolSettings extends Tool {
  async run() {
    return UiFramework.toolSettings.focusIntoToolSettings();
  }
};
_FocusToolSettings.toolId = "FocusToolSettings";
let FocusToolSettings = _FocusToolSettings;
const _BumpToolSetting = class _BumpToolSetting extends Tool {
  // istanbul ignore next
  static get maxArgs() {
    return 1;
  }
  async run(settingIndexStr) {
    let settingIndex;
    if (settingIndexStr) {
      settingIndex = parseInt(settingIndexStr, 10);
      if (isNaN(settingIndex))
        return false;
    }
    IModelApp.toolAdmin.bumpToolSetting(settingIndex);
    return true;
  }
  async parseAndRun(...args) {
    return this.run(args[0]);
  }
};
_BumpToolSetting.toolId = "BumpToolSetting";
let BumpToolSetting = _BumpToolSetting;
const toolSettingTools = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  BumpToolSetting,
  FocusToolSettings
}, Symbol.toStringTag, { value: "Module" }));
const _UiShowHideSettingsProvider = class _UiShowHideSettingsProvider {
  constructor() {
    this.providerId = "UiShowHideSettingsProvider";
  }
  static initialize() {
    UiFramework.registerUserSettingsProvider(new _UiShowHideSettingsProvider());
  }
  async loadUserSettings(storage) {
    let result = await storage.getSetting(
      _UiShowHideSettingsProvider._settingsNamespace,
      _UiShowHideSettingsProvider._autoHideUiKey
    );
    if (result.status === UiStateStorageStatus.Success)
      InternalUiShowHideManager.setAutoHideUi(result.setting);
    result = await storage.getSetting(
      _UiShowHideSettingsProvider._settingsNamespace,
      _UiShowHideSettingsProvider._useProximityOpacityKey
    );
    if (result.status === UiStateStorageStatus.Success)
      InternalUiShowHideManager.setUseProximityOpacity(result.setting);
    result = await storage.getSetting(
      _UiShowHideSettingsProvider._settingsNamespace,
      _UiShowHideSettingsProvider._snapWidgetOpacityKey
    );
    if (result.status === UiStateStorageStatus.Success)
      InternalUiShowHideManager.setSnapWidgetOpacity(result.setting);
  }
  static async storeAutoHideUi(v, storage) {
    void (storage ?? UiFramework.getUiStateStorage()).saveSetting(
      this._settingsNamespace,
      this._autoHideUiKey,
      v
    );
  }
  static async storeUseProximityOpacity(v, storage) {
    void (storage ?? UiFramework.getUiStateStorage()).saveSetting(
      this._settingsNamespace,
      this._useProximityOpacityKey,
      v
    );
  }
  static async storeSnapWidgetOpacity(v, storage) {
    void (storage ?? UiFramework.getUiStateStorage()).saveSetting(
      this._settingsNamespace,
      this._snapWidgetOpacityKey,
      v
    );
  }
};
_UiShowHideSettingsProvider._settingsNamespace = "AppUiSettings";
_UiShowHideSettingsProvider._autoHideUiKey = "AutoHideUi";
_UiShowHideSettingsProvider._useProximityOpacityKey = "UseProximityOpacity";
_UiShowHideSettingsProvider._snapWidgetOpacityKey = "SnapWidgetOpacity";
let UiShowHideSettingsProvider = _UiShowHideSettingsProvider;
const INACTIVITY_TIME_DEFAULT = 3500;
const _InternalUiShowHideManager = class _InternalUiShowHideManager {
  /** Determines if the Ui is visible */
  static get isUiVisible() {
    return _InternalUiShowHideManager._isUiVisible;
  }
  static set isUiVisible(visible) {
    _InternalUiShowHideManager._isUiVisible = visible;
  }
  /** @public */
  static setAutoHideUi(value) {
    _InternalUiShowHideManager._autoHideUi = value;
  }
  /** @internal */
  static setUseProximityOpacity(value) {
    _InternalUiShowHideManager._useProximityOpacity = value;
  }
  /** @internal */
  static setSnapWidgetOpacity(value) {
    _InternalUiShowHideManager._snapWidgetOpacity = value;
  }
  /** Determines whether the `auto-hide Ui` feature is on. Defaults to false.
   * When true, the Ui automatically hides after a few seconds of inactivity.
   */
  static get autoHideUi() {
    return _InternalUiShowHideManager._autoHideUi;
  }
  static set autoHideUi(autoHide) {
    void UiShowHideSettingsProvider.storeAutoHideUi(autoHide);
    _InternalUiShowHideManager._autoHideUi = autoHide;
    SyncUiEventDispatcher.dispatchImmediateSyncUiEvent(
      SyncUiEventId.ShowHideManagerSettingChange
    );
  }
  /** Determines whether the widget panels are shown and hidden. Defaults to false. */
  static get showHidePanels() {
    return _InternalUiShowHideManager._showHidePanels;
  }
  static set showHidePanels(showHide) {
    _InternalUiShowHideManager._showHidePanels = showHide;
    UiFramework.onUiVisibilityChanged.emit({
      visible: UiFramework.getIsUiVisible()
    });
  }
  /** Determines whether the status bar is shown and hidden. Defaults to false. */
  static get showHideFooter() {
    return _InternalUiShowHideManager._showHideFooter;
  }
  static set showHideFooter(showHide) {
    _InternalUiShowHideManager._showHideFooter = showHide;
    UiFramework.onUiVisibilityChanged.emit({
      visible: UiFramework.getIsUiVisible()
    });
  }
  /** Determines the amount of inactivity time before the Ui is hidden. Defaults to 3.5 seconds. */
  static get inactivityTime() {
    return _InternalUiShowHideManager._inactivityTime;
  }
  static set inactivityTime(time) {
    _InternalUiShowHideManager._inactivityTime = time;
  }
  /** Determines whether the proximity of the mouse should alter the opacity of a toolbar. Defaults to true. */
  static get useProximityOpacity() {
    return _InternalUiShowHideManager._useProximityOpacity;
  }
  static set useProximityOpacity(value) {
    _InternalUiShowHideManager._useProximityOpacity = value;
    void UiShowHideSettingsProvider.storeUseProximityOpacity(value);
    SyncUiEventDispatcher.dispatchImmediateSyncUiEvent(
      SyncUiEventId.ShowHideManagerSettingChange
    );
    UiFramework.onUiVisibilityChanged.emit({
      visible: UiFramework.getIsUiVisible()
    });
  }
  /** Determines whether the opacity of a toolbar should snap. Defaults to false. */
  static get snapWidgetOpacity() {
    return _InternalUiShowHideManager._snapWidgetOpacity;
  }
  static set snapWidgetOpacity(value) {
    _InternalUiShowHideManager._snapWidgetOpacity = value;
    void UiShowHideSettingsProvider.storeSnapWidgetOpacity(value);
    SyncUiEventDispatcher.dispatchImmediateSyncUiEvent(
      SyncUiEventId.ShowHideManagerSettingChange
    );
    UiFramework.onUiVisibilityChanged.emit({
      visible: UiFramework.getIsUiVisible()
    });
  }
  /** Handler for when a Frontstage is ready */
  static handleFrontstageReady() {
    if (!_InternalUiShowHideManager._autoHideUi)
      return;
    _InternalUiShowHideManager.showUiAndResetTimer();
  }
  /** Handler for when the mouse moves over the content area */
  static handleContentMouseMove(_event) {
    if (!_InternalUiShowHideManager._autoHideUi)
      return;
    _InternalUiShowHideManager.showUiAndResetTimer();
  }
  /** Handler for when the mouse enters a widget */
  static handleWidgetMouseEnter(_event) {
    if (!_InternalUiShowHideManager._autoHideUi)
      return;
    _InternalUiShowHideManager.showUiAndCancelTimer();
  }
  /** Shows the Ui and resets the inactivity timer */
  static showUiAndResetTimer() {
    window.clearTimeout(this._showUiTimeout);
    this._showUiTimeout = window.setTimeout(() => {
      _InternalUiShowHideManager.showUi();
      _InternalUiShowHideManager.resetTimer();
    });
  }
  /** Shows the Ui and cancels the inactivity timer */
  static showUiAndCancelTimer() {
    window.clearTimeout(this._showUiTimeout);
    this._showUiTimeout = window.setTimeout(() => {
      _InternalUiShowHideManager.showUi();
      _InternalUiShowHideManager.cancelTimer();
    });
  }
  static cancelTimer() {
    window.clearTimeout(_InternalUiShowHideManager._timeout);
  }
  static resetTimer() {
    _InternalUiShowHideManager.cancelTimer();
    _InternalUiShowHideManager._timeout = window.setTimeout(
      _InternalUiShowHideManager.hideUi,
      _InternalUiShowHideManager._inactivityTime
    );
  }
  static showUi() {
    UiFramework.setIsUiVisible(true);
  }
  static hideUi() {
    UiFramework.setIsUiVisible(false);
  }
  /** @internal */
  static terminate() {
    this.cancelTimer();
    window.clearTimeout(this._showUiTimeout);
    this._showUiTimeout = void 0;
    this._isUiVisible = true;
    this._autoHideUi = true;
    this._showHidePanels = false;
    this._showHideFooter = false;
    this._inactivityTime = INACTIVITY_TIME_DEFAULT;
    this._useProximityOpacity = false;
    this._snapWidgetOpacity = false;
  }
};
_InternalUiShowHideManager._isUiVisible = true;
_InternalUiShowHideManager._autoHideUi = true;
_InternalUiShowHideManager._showHidePanels = false;
_InternalUiShowHideManager._showHideFooter = false;
_InternalUiShowHideManager._inactivityTime = INACTIVITY_TIME_DEFAULT;
_InternalUiShowHideManager._useProximityOpacity = false;
_InternalUiShowHideManager._snapWidgetOpacity = false;
let InternalUiShowHideManager = _InternalUiShowHideManager;
function isGenerated(value) {
  return value.startsWith("Widget-");
}
function createStableWidgetDef(widgetDef, stableId) {
  return new Proxy(widgetDef, {
    get(target, name, receiver) {
      const idName = "id";
      const value = Reflect.get(target, name, receiver);
      if (name === idName && isGenerated(value)) {
        return stableId;
      }
      return value;
    }
  });
}
try {
  createStableWidgetDef.displayName = "createStableWidgetDef";
  createStableWidgetDef.__docgenInfo = { "description": "Proxy will return `stableId` if target [[WidgetDef]] id is auto-generated.", "displayName": "createStableWidgetDef", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
class WidgetsChangedEvent extends BeUiEvent {
}
function getWidgetManagerStableWidgetId(stageUsage, location, section, index) {
  return `uifw-wm-${stageUsage || ""}-${StagePanelLocation[location]}-${StagePanelSection[section]}-${index}`;
}
class WidgetManager {
  constructor() {
    this._widgets = [];
    this.onWidgetsChanged = new WidgetsChangedEvent();
  }
  /** @internal */
  get widgetCount() {
    return this._widgets.length;
  }
  /** @internal */
  get widgets() {
    return this._widgets;
  }
  set widgets(w) {
    this._widgets = w;
    this.onWidgetsChanged.emit({ items: w });
  }
  /** Adds a WidgetDef for use in a Frontstage.
   * @note Added `widgetDef` must return unique id to correctly save/restore App layout.
   * Semi-stable id is generated when auto-generated `widgetDef` id is detected,
   * but correctness of such id depends on `addWidgetDef` call order and widget location.
   */
  addWidgetDef(widgetDef, stageId, stageUsage, location, section) {
    if (stageId === void 0 && stageUsage === void 0) {
      Logger.logError(
        UiFramework.loggerCategory(this),
        `addWidgetDef: stageId or stageUsage param must be specified`
      );
      return false;
    }
    section = section !== void 0 ? section : StagePanelSection.Start;
    const index = this._widgets.reduce((acc, info) => {
      if (info.stageId === stageId && info.stageUsage === stageUsage && info.location === location && info.section === section)
        return acc + 1;
      return acc;
    }, 0);
    const stableId = getWidgetManagerStableWidgetId(
      stageUsage,
      location,
      section,
      index
    );
    const stableWidget = createStableWidgetDef(widgetDef, stableId);
    const newWidget = {
      widgetDef: stableWidget,
      stageId,
      stageUsage,
      location,
      section
    };
    const oldWidgets = this._widgets.filter(
      (info) => info.widgetDef.id !== newWidget.widgetDef.id
    );
    const updatedWidgets = [...oldWidgets, newWidget];
    this.widgets = updatedWidgets;
    return true;
  }
  /** Removes a WidgetDef.
   */
  removeWidgetDef(widgetId) {
    let result = false;
    const updatedWidgets = this._widgets.filter(
      (info) => info.widgetDef.id !== widgetId
    );
    if (updatedWidgets.length !== this._widgets.length) {
      this.widgets = updatedWidgets;
      result = true;
    }
    return result;
  }
  /** Gets WidgetDefs for a Frontstage location.
   */
  getWidgetDefs(stageId, stageUsage, location, section) {
    const definedSection = section === void 0 ? StagePanelSection.Start : section;
    const widgetInfos = this._widgets.filter((info) => {
      return (!info.stageId || info.stageId === stageId) && (!info.stageUsage || info.stageUsage === stageUsage) && info.location === location && info.section === definedSection;
    });
    const widgetDefs = widgetInfos.map((info) => info.widgetDef);
    const widgets = UiItemsManager.getWidgets(
      stageId,
      stageUsage,
      location,
      definedSection
    );
    widgets.forEach((abstractProps) => {
      const widgetDef = WidgetDef.create(abstractProps);
      widgetDefs.push(widgetDef);
    });
    return widgetDefs.length > 0 ? widgetDefs : void 0;
  }
}
const _InternalContentLayoutManager = class _InternalContentLayoutManager {
  /** build a layout key that is unique for group layout combination */
  static getKey(props) {
    return `${props.contentGroupId}-${props.layoutId}`;
  }
  /** Return a LayoutDef that is specific to a content group.
   * @returns the [[ContentLayoutDef]] if found, or undefined otherwise
   */
  static getForGroup(contentGroupProps, overrideContentLayout) {
    const layoutId = (overrideContentLayout == null ? void 0 : overrideContentLayout.id) ?? contentGroupProps.layout.id;
    const layoutKey = this.getKey({
      contentGroupId: contentGroupProps.id,
      layoutId
    });
    if (!overrideContentLayout && _InternalContentLayoutManager._layoutDefs.has(layoutKey)) {
      return _InternalContentLayoutManager._layoutDefs.get(layoutKey);
    }
    const newContentLayoutProps = {
      ...contentGroupProps.layout,
      ...overrideContentLayout
    };
    const newLayoutDef = new ContentLayoutDef$1(newContentLayoutProps);
    this.add(layoutKey, newLayoutDef);
    return newLayoutDef;
  }
  /** Finds a Content Layout with a given id.
   * @param layoutKey  group specific layout id, see `getLayoutKey`
   * @returns the [[ContentLayoutDef]] if found, or undefined otherwise
   */
  static find(layoutKey) {
    return _InternalContentLayoutManager._layoutDefs.get(layoutKey);
  }
  /** Adds a Content Layout.
   * @param layoutId  the id of the Content Layout to add
   * @param layoutDef  the Content Layout definition to add
   */
  static add(layoutId, layoutDef) {
    _InternalContentLayoutManager._layoutDefs.set(layoutId, layoutDef);
  }
  /** Gets the active Content Layout */
  static get activeLayout() {
    let layoutDef;
    const activeFrontstageDef = UiFramework.frontstages.activeFrontstageDef;
    if (activeFrontstageDef)
      layoutDef = activeFrontstageDef.contentLayoutDef;
    return layoutDef;
  }
  /** Gets the active Content Group */
  static get activeContentGroup() {
    let contentGroup;
    const activeFrontstageDef = UiFramework.frontstages.activeFrontstageDef;
    if (activeFrontstageDef)
      contentGroup = activeFrontstageDef.contentGroup;
    return contentGroup;
  }
  /** Sets the active Content Layout, Content Group and Content Control.
   * @param contentLayoutDef  Content layout to make active
   * @param contentGroup  Content Group to make active
   */
  static async setActive(contentLayoutDef, contentGroup) {
    await InternalFrontstageManager.setActiveLayout(
      contentLayoutDef,
      contentGroup
    );
  }
  /** Sets the active Content Group.
   * @param contentGroup  Content Group to make active
   */
  static async setActiveContentGroup(contentGroup) {
    await InternalFrontstageManager.setActiveContentGroup(contentGroup);
  }
  /** Refreshes the active layout and content group.
   */
  static refreshActive() {
    const activeFrontstageDef = UiFramework.frontstages.activeFrontstageDef;
    if (activeFrontstageDef && activeFrontstageDef.contentLayoutDef && activeFrontstageDef.contentGroup) {
      UiFramework.frontstages.onContentLayoutActivatedEvent.emit({
        contentLayout: activeFrontstageDef.contentLayoutDef,
        contentGroup: activeFrontstageDef.contentGroup
      });
      activeFrontstageDef.contentGroup.refreshContentNodes();
    }
  }
};
_InternalContentLayoutManager._layoutDefs = /* @__PURE__ */ new Map();
let InternalContentLayoutManager = _InternalContentLayoutManager;
const _InternalContentViewManager = class _InternalContentViewManager {
  /** Determines if the mouse is down in a content view */
  static get isMouseDown() {
    return this._mouseDown;
  }
  /** Sets the mouse down status for a content view */
  static setMouseDown(mouseDown) {
    this._mouseDown = mouseDown;
    this.onMouseDownChangedEvent.emit({ mouseDown });
  }
  /** Gets the active content as a React.ReactNode. */
  static getActive() {
    return this._activeContent;
  }
  static getControlFromElement(content, activeContentGroup, floatingControls, logIfNotFound = false) {
    if (floatingControls == null ? void 0 : floatingControls.length) {
      let control = floatingControls.find(
        (contentControl) => contentControl.reactNode === content
      );
      if (control)
        return control;
      let controlId;
      if (content && content.key) {
        const key = content.key;
        controlId = key.split("::", 1)[0];
      }
      floatingControls.forEach((contentControl) => {
        const node = contentControl.reactNode;
        const key = node == null ? void 0 : node.key;
        const nodeId = key && key.split("::", 1)[0];
        if (nodeId === controlId)
          control = contentControl;
      });
      if (control)
        return control;
    }
    if (activeContentGroup) {
      const activeContentControl = activeContentGroup.getControlFromElement(content);
      if (activeContentControl)
        return activeContentControl;
    }
    if (logIfNotFound)
      Logger.logError(
        UiFramework.loggerCategory(this),
        `getControlFromElement: no control found for element`
      );
    return void 0;
  }
  /** Return the active ContentControl. */
  static getActiveContentControl() {
    let activeContentControl;
    const activeFrontstageDef = UiFramework.frontstages.activeFrontstageDef;
    if (this._activeContent && activeFrontstageDef) {
      const activeContentGroup = activeFrontstageDef.contentGroup;
      activeContentControl = this.getControlFromElement(
        this._activeContent,
        activeContentGroup,
        activeFrontstageDef.floatingContentControls
      );
    }
    return activeContentControl;
  }
  static addFloatingContentControl(contentControl) {
    const activeFrontstageDef = UiFramework.frontstages.activeFrontstageDef;
    if (activeFrontstageDef && contentControl) {
      activeFrontstageDef.addFloatingContentControl(contentControl);
    }
  }
  static dropFloatingContentControl(contentControl) {
    const activeFrontstageDef = UiFramework.frontstages.activeFrontstageDef;
    if (activeFrontstageDef && contentControl)
      activeFrontstageDef.dropFloatingContentControl(contentControl);
  }
  /** Sets the active [[ContentControl]] */
  static setActive(activeContent, forceEventProcessing = false) {
    if (this._activeContent !== activeContent || forceEventProcessing) {
      const oldContent = this._activeContent;
      this._activeContent = activeContent;
      const activeFrontstageDef = UiFramework.frontstages.activeFrontstageDef;
      if (activeFrontstageDef) {
        const activeContentGroup = activeFrontstageDef.contentGroup;
        const oldContentControl = this.getControlFromElement(
          oldContent,
          activeContentGroup,
          activeFrontstageDef.floatingContentControls
        );
        const activeContentControl = this.getControlFromElement(
          activeContent,
          activeContentGroup,
          activeFrontstageDef.floatingContentControls,
          true
        );
        if (activeContentControl) {
          const doSetActiveView = forceEventProcessing || !activeContentControl.viewport || /* istanbul ignore next */
          oldContentControl && /* istanbul ignore next */
          !oldContentControl.viewport;
          if (doSetActiveView) {
            activeFrontstageDef.setActiveView(
              activeContentControl,
              oldContentControl
            );
            this.onActiveContentChangedEvent.emit({
              activeContent,
              oldContent
            });
          } else {
            if (activeContentControl.viewport && activeContentControl.viewport !== IModelApp.viewManager.selectedView) {
              void IModelApp.viewManager.setSelectedView(
                activeContentControl.viewport
              );
            }
          }
        }
      }
    }
  }
  /** Refreshes the active [[ContentControl]] */
  static refreshActive(activeContent) {
    this.layouts.refreshActive();
    this.setActive(activeContent, true);
  }
  /**
   * Determines if content displays a Sheet view.
   * @param content ContentControl to check
   */
  static isContentSheetView(content) {
    if (!content || !content.viewport)
      return false;
    return ViewUtilities.isSheetView(content.viewport);
  }
  /**
   * Determines if content displays a Drawing view.
   * @param content ContentControl to check
   */
  static isContentDrawingView(content) {
    if (!content || !content.viewport)
      return false;
    return ViewUtilities.isDrawingView(content.viewport);
  }
  /**
   * Determines if content displays a Spatial view.
   * @param content ContentControl to check
   */
  static isContentSpatialView(content) {
    if (!content || !content.viewport)
      return false;
    return ViewUtilities.isSpatialView(content.viewport);
  }
  /**
   * Determines if content displays a Orthographic view.
   * @param content ContentControl to check
   */
  static isContentOrthographicView(content) {
    if (!content || !content.viewport)
      return false;
    return ViewUtilities.isOrthographicView(content.viewport);
  }
  /**
   * Determines if content displays a 3d view.
   * @param content ContentControl to check
   */
  static isContent3dView(content) {
    if (!content || !content.viewport)
      return false;
    return ViewUtilities.is3dView(content.viewport);
  }
  /**
   * Determines if viewport supports use of a camera.
   * @param content ContentControl to check
   */
  static contentSupportsCamera(content) {
    if (!content || !content.viewport)
      return false;
    return ViewUtilities.viewSupportsCamera(content.viewport);
  }
  /**
   * Manage content layouts.
   * @beta
   */
  static get layouts() {
    return InternalContentLayoutManager;
  }
  /**
   * Manage dialogs displaying managed content.
   * @beta
   */
  static get dialogs() {
    return InternalContentDialogManager;
  }
};
_InternalContentViewManager._mouseDown = false;
_InternalContentViewManager.onMouseDownChangedEvent = new MouseDownChangedEvent();
_InternalContentViewManager.onActiveContentChangedEvent = new ActiveContentChangedEvent();
_InternalContentViewManager.onAvailableContentChangedEvent = new UiEvent();
let InternalContentViewManager = _InternalContentViewManager;
var CalculatorOperator = /* @__PURE__ */ ((CalculatorOperator2) => {
  CalculatorOperator2[CalculatorOperator2["None"] = 0] = "None";
  CalculatorOperator2[CalculatorOperator2["Clear"] = 1] = "Clear";
  CalculatorOperator2[CalculatorOperator2["ClearAll"] = 2] = "ClearAll";
  CalculatorOperator2[CalculatorOperator2["Backspace"] = 3] = "Backspace";
  CalculatorOperator2[CalculatorOperator2["Add"] = 4] = "Add";
  CalculatorOperator2[CalculatorOperator2["Subtract"] = 5] = "Subtract";
  CalculatorOperator2[CalculatorOperator2["Multiply"] = 6] = "Multiply";
  CalculatorOperator2[CalculatorOperator2["Divide"] = 7] = "Divide";
  CalculatorOperator2[CalculatorOperator2["NegPos"] = 8] = "NegPos";
  CalculatorOperator2[CalculatorOperator2["Decimal"] = 9] = "Decimal";
  CalculatorOperator2[CalculatorOperator2["Equals"] = 10] = "Equals";
  return CalculatorOperator2;
})(CalculatorOperator || {});
class CalculatorEngine {
  constructor() {
    this._displayValue = "0";
    this._previousKeyType = 0;
    this._firstValue = "";
    this._modValue = "";
    this._createResultString = (operator, displayedNum) => {
      let resultString = displayedNum;
      if (this._isMathOperator(operator)) {
        const firstValue = this._firstValue;
        const savedOperator = this._operator;
        resultString = firstValue && savedOperator && this._previousKeyType !== 1 && this._previousKeyType !== 2 ? this._calculate(firstValue, savedOperator, displayedNum) : displayedNum;
      } else {
        switch (operator) {
          case 1:
            resultString = "0";
            break;
          case 2:
            resultString = "0";
            break;
          case 3:
            resultString = resultString.slice(0, -1);
            if (resultString.length === 0)
              resultString = "0";
            break;
          case 9:
            if (!resultString.includes("."))
              resultString = `${displayedNum}.`;
            else if (this._previousKeyType === 1 || this._previousKeyType === 2)
              resultString = "0.";
            break;
          case 8:
            if (displayedNum.length > 0 && displayedNum[0] === "-")
              resultString = displayedNum.substring(1);
            else
              resultString = `-${displayedNum}`;
            break;
          case 10: {
            const firstValue = this._firstValue;
            const savedOperator = this._operator;
            const modValue = this._modValue;
            if (firstValue && savedOperator) {
              resultString = this._previousKeyType === 2 ? this._calculate(displayedNum, savedOperator, modValue) : this._calculate(firstValue, savedOperator, displayedNum);
            } else {
              resultString = displayedNum;
            }
            break;
          }
        }
      }
      return resultString;
    };
    this._isMathOperator = (operator) => {
      switch (operator) {
        case 4:
        case 5:
        case 6:
        case 7:
          return true;
      }
      return false;
    };
    this._calculate = (n1Str, operator, n2Str) => {
      let result = 0;
      const n1 = parseFloat(n1Str);
      const n2 = parseFloat(n2Str);
      switch (operator) {
        case 4:
          result = n1 + n2;
          break;
        case 5:
          result = n1 - n2;
          break;
        case 6:
          result = n1 * n2;
          break;
        case 7:
          const displayValue = n2;
          if (displayValue !== 0)
            result = n1 / displayValue;
          else
            result = 0;
          break;
      }
      return result.toString();
    };
    this._updateCalculatorState = (operator, resultString, displayedNum) => {
      let keyType = 0;
      if (this._isMathOperator(operator)) {
        if (this._firstValue && this._operator && this._previousKeyType === 0) {
          this._firstValue = resultString;
        } else {
          this._firstValue = displayedNum;
        }
        this._operator = operator;
        keyType = 1;
      } else {
        switch (operator) {
          case 2:
            this.clearAll();
            break;
          case 10: {
            let secondValue = displayedNum;
            if (this._firstValue) {
              if (this._previousKeyType === 2) {
                secondValue = this._modValue;
              }
            }
            this._modValue = secondValue;
            keyType = 2;
          }
        }
      }
      this._previousKeyType = keyType;
    };
    this.clearAll();
  }
  clearAll() {
    this._displayValue = "0";
    this._previousKeyType = 0;
    this._firstValue = "";
    this._modValue = "";
    this._operator = void 0;
  }
  get result() {
    return parseFloat(this._displayValue);
  }
  get displayValue() {
    return this._displayValue;
  }
  processValue(value) {
    let resultString;
    if (this._displayValue === "0" || this._previousKeyType === 1 || this._previousKeyType === 2) {
      resultString = value;
    } else {
      resultString = this._displayValue + value;
    }
    this._displayValue = resultString;
    this._previousKeyType = 0;
    return resultString;
  }
  processOperator(operator) {
    const displayedNum = this._displayValue;
    const resultString = this._createResultString(operator, displayedNum);
    this._displayValue = resultString;
    this._updateCalculatorState(operator, resultString, displayedNum);
    return resultString;
  }
}
class SquareButton extends reactExports.PureComponent {
  render() {
    const { className, style, ...buttonProps } = this.props;
    const buttonClassNames = classnames("uifw-square-button", className);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Button,
      {
        ...buttonProps,
        size: "small",
        className: buttonClassNames,
        style
      }
    );
  }
}
try {
  SquareButton.displayName = "SquareButton";
  SquareButton.__docgenInfo = { "description": "", "displayName": "SquareButton", "props": { "as": { "defaultValue": null, "description": "", "name": "as", "required": false, "type": { "name": "enum", "value": [{ "value": '"button"' }] } }, "htmlDisabled": { "defaultValue": null, "description": "Built-in html `disabled` attribute", "name": "htmlDisabled", "required": false, "type": { "name": "boolean" } }, "startIcon": { "defaultValue": null, "description": "Icon shown before the main button content.", "name": "startIcon", "required": false, "type": { "name": "Element" } }, "endIcon": { "defaultValue": null, "description": "Icon shown after the main button content.", "name": "endIcon", "required": false, "type": { "name": "Element" } }, "labelProps": { "defaultValue": null, "description": "Passes props to the button label.", "name": "labelProps", "required": false, "type": { "name": "DetailedHTMLProps<HTMLAttributes<HTMLSpanElement>, HTMLSpanElement>" } }, "startIconProps": { "defaultValue": null, "description": "Passes props to the start icon.", "name": "startIconProps", "required": false, "type": { "name": "DetailedHTMLProps<HTMLAttributes<HTMLSpanElement>, HTMLSpanElement>" } }, "endIconProps": { "defaultValue": null, "description": "Passes props to the end icon.", "name": "endIconProps", "required": false, "type": { "name": "DetailedHTMLProps<HTMLAttributes<HTMLSpanElement>, HTMLSpanElement>" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const backspaceIcon = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3c!--%20Generator:%20Adobe%20Illustrator%2023.0.3,%20SVG%20Export%20Plug-In%20.%20SVG%20Version:%206.00%20Build%200)%20--%3e%3csvg%20version='1.1'%20id='Calque_1'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%20x='0px'%20y='0px'%20viewBox='0%200%2016%2016'%20style='enable-background:new%200%200%2016%2016;'%20xml:space='preserve'%3e%3cpath%20d='M5,2L0,7.5L5,13h11V2H5z%20M12.21,8.79c0.39,0.39,0.39,1.03,0,1.42c-0.19,0.19-0.44,0.29-0.71,0.29%20c-0.27,0-0.52-0.1-0.71-0.29L9.5,8.91l-1.29,1.3c-0.38,0.37-1.04,0.37-1.42,0c-0.39-0.39-0.39-1.03,0-1.42l1.3-1.29l-1.3-1.29%20c-0.39-0.39-0.39-1.03,0-1.42c0.39-0.39,1.03-0.39,1.42,0l1.29,1.3l1.29-1.3c0.39-0.39,1.03-0.39,1.42,0c0.39,0.39,0.39,1.03,0,1.42%20l-1.3,1.29L12.21,8.79z'/%3e%3c/svg%3e";
const _Calculator = class _Calculator extends reactExports.PureComponent {
  constructor(props) {
    super(props);
    this._mainDiv = reactExports.createRef();
    this._equalsClicked = false;
    this._onValueButtonClick = (keyChar) => {
      const displayValue2 = this.props.engine.processValue(keyChar);
      this._mainDivFocus();
      this.setState({ displayValue: displayValue2 });
    };
    this._onOperatorButtonClick = (operator) => {
      if (operator === CalculatorOperator.Clear && this._equalsClicked)
        operator = CalculatorOperator.ClearAll;
      const displayValue2 = this.props.engine.processOperator(operator);
      this._mainDivFocus();
      this.setState({ displayValue: displayValue2 });
      if (operator === CalculatorOperator.Equals)
        this._equalsClicked = true;
      else if (operator === CalculatorOperator.ClearAll)
        this._equalsClicked = false;
    };
    this._handleOk = (_event) => {
      this._ok();
    };
    this._handleCancel = (_event) => {
      this._cancel();
    };
    this._handleKeyDown = (event) => {
      switch (event.key) {
        case "0":
        case "1":
        case "2":
        case "3":
        case "4":
        case "5":
        case "6":
        case "7":
        case "8":
        case "9":
          this._onValueButtonClick(event.key);
          break;
        case "a":
        case "A":
          this._onOperatorButtonClick(CalculatorOperator.ClearAll);
          break;
        case "c":
        case "C":
        case Key_enum.Key.Clear.valueOf():
          this._onOperatorButtonClick(CalculatorOperator.Clear);
          break;
        case Key_enum.Key.Escape.valueOf():
          this._cancel();
          break;
        case Key_enum.Key.Backspace.valueOf():
          this._onOperatorButtonClick(CalculatorOperator.Backspace);
          break;
        case "/":
        case Key_enum.Key.Divide.valueOf():
          this._onOperatorButtonClick(CalculatorOperator.Divide);
          break;
        case "*":
        case Key_enum.Key.Multiply.valueOf():
          this._onOperatorButtonClick(CalculatorOperator.Multiply);
          break;
        case "-":
        case Key_enum.Key.Subtract.valueOf():
          this._onOperatorButtonClick(CalculatorOperator.Subtract);
          break;
        case "+":
        case Key_enum.Key.Add.valueOf():
          this._onOperatorButtonClick(CalculatorOperator.Add);
          break;
        case ".":
        case Key_enum.Key.Decimal.valueOf():
          this._onOperatorButtonClick(CalculatorOperator.Decimal);
          break;
        case "=":
          this._onOperatorButtonClick(CalculatorOperator.Equals);
          break;
        case Key_enum.Key.Enter.valueOf():
          if (!this._equalsClicked)
            this._onOperatorButtonClick(CalculatorOperator.Equals);
          this._ok();
          break;
      }
    };
    let displayValue = "0";
    if (this.props.initialValue)
      displayValue = this.props.engine.processValue(
        this.props.initialValue.toString()
      );
    this.state = {
      displayValue
    };
  }
  _ok() {
    if (this.props.onOk) {
      this.props.onOk(this.props.engine.result);
    }
    this._clearAll();
  }
  _cancel() {
    if (this.props.onCancel) {
      this.props.onCancel();
    }
    this._clearAll();
  }
  _clearAll() {
    this.props.engine.clearAll();
    this._equalsClicked = false;
  }
  componentDidMount() {
    this._mainDivFocus();
  }
  _mainDivFocus() {
    if (this._mainDiv.current)
      this._mainDiv.current.focus();
  }
  render() {
    const { className, resultIcon, onOk, onCancel, initialValue, ...props } = this.props;
    const classNames = classnames("uifw-calculator", className);
    const topSection = resultIcon ? /* @__PURE__ */ jsxRuntimeExports.jsx(
      IconInput,
      {
        containerClassName: "uifw-calculator-top-input",
        value: this.state.displayValue,
        readOnly: true,
        icon: resultIcon
      }
    ) : /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { value: this.state.displayValue, readOnly: true, size: "small" });
    return (
      // The event handler is only being used to capture bubbled events
      // eslint-disable-next-line jsx-a11y/no-static-element-interactions
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          ...props,
          ref: this._mainDiv,
          tabIndex: -1,
          className: classNames,
          onKeyDown: this._handleKeyDown,
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-calculator-top", children: topSection }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              CalculatorKeyPad,
              {
                onValueClick: this._onValueButtonClick,
                onOperatorClick: this._onOperatorButtonClick
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "uifw-calculator-bottom-buttons", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  className: classnames(
                    "uifw-calculator-large-button",
                    "uifw-calculator-ok-button"
                  ),
                  styleType: "cta",
                  onClick: this._handleOk,
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgCheckmark, {}) })
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  className: classnames(
                    "uifw-calculator-large-button",
                    "uifw-calculator-cancel-button"
                  ),
                  onClick: this._handleCancel,
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgRemove, {}) })
                }
              )
            ] })
          ]
        }
      )
    );
  }
};
_Calculator.defaultProps = {
  engine: new CalculatorEngine()
};
let Calculator = _Calculator;
class CalculatorKeyPad extends reactExports.PureComponent {
  render() {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: classnames("uifw-calculator-button-grid"), children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        OperatorButton,
        {
          operator: CalculatorOperator.ClearAll,
          onClick: this.props.onOperatorClick,
          children: "AC"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        OperatorButton,
        {
          operator: CalculatorOperator.Clear,
          onClick: this.props.onOperatorClick,
          children: "C"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        OperatorButton,
        {
          operator: CalculatorOperator.Backspace,
          onClick: this.props.onOperatorClick,
          children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "uifw-calculator-button-svg", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec: backspaceIcon }) })
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        OperatorButton,
        {
          operator: CalculatorOperator.Divide,
          onClick: this.props.onOperatorClick,
          className: "uifw-calculator-large-font",
          children: "÷"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ValueButton, { keyChar: "7", onClick: this.props.onValueClick }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ValueButton, { keyChar: "8", onClick: this.props.onValueClick }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ValueButton, { keyChar: "9", onClick: this.props.onValueClick }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        OperatorButton,
        {
          operator: CalculatorOperator.Multiply,
          onClick: this.props.onOperatorClick,
          className: "uifw-calculator-large-font",
          children: "×"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ValueButton, { keyChar: "4", onClick: this.props.onValueClick }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ValueButton, { keyChar: "5", onClick: this.props.onValueClick }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ValueButton, { keyChar: "6", onClick: this.props.onValueClick }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        OperatorButton,
        {
          operator: CalculatorOperator.Subtract,
          onClick: this.props.onOperatorClick,
          className: "uifw-calculator-large-font",
          children: "−"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ValueButton, { keyChar: "1", onClick: this.props.onValueClick }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ValueButton, { keyChar: "2", onClick: this.props.onValueClick }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ValueButton, { keyChar: "3", onClick: this.props.onValueClick }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        OperatorButton,
        {
          operator: CalculatorOperator.Add,
          onClick: this.props.onOperatorClick,
          className: "uifw-calculator-large-font",
          children: "+"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ValueButton, { keyChar: "0", onClick: this.props.onValueClick }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        OperatorButton,
        {
          operator: CalculatorOperator.NegPos,
          onClick: this.props.onOperatorClick,
          className: "uifw-calculator-large-font",
          children: "±"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        OperatorButton,
        {
          operator: CalculatorOperator.Decimal,
          onClick: this.props.onOperatorClick,
          className: "uifw-calculator-large-font",
          children: "."
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        OperatorButton,
        {
          operator: CalculatorOperator.Equals,
          onClick: this.props.onOperatorClick,
          className: "uifw-calculator-large-font",
          children: "="
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", {})
    ] });
  }
}
class ValueButton extends reactExports.PureComponent {
  constructor() {
    super(...arguments);
    this._handleOnClick = (_event) => {
      this.props.onClick(this.props.keyChar);
    };
  }
  render() {
    const { className, keyChar, onClick, ...props } = this.props;
    const itemClassNames = classnames("uifw-calculator-item", className);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SquareButton,
      {
        ...props,
        className: itemClassNames,
        onClick: this._handleOnClick,
        children: keyChar
      }
    );
  }
}
class OperatorButton extends reactExports.PureComponent {
  constructor() {
    super(...arguments);
    this._handleOnClick = (_event) => {
      this.props.onClick(this.props.operator);
    };
  }
  render() {
    const { className, children, operator, onClick, ...props } = this.props;
    const itemClassNames = classnames("uifw-calculator-item", className);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SquareButton,
      {
        ...props,
        className: itemClassNames,
        onClick: this._handleOnClick,
        children
      }
    );
  }
}
try {
  Calculator.displayName = "Calculator";
  Calculator.__docgenInfo = { "description": "", "displayName": "Calculator", "props": { "initialValue": { "defaultValue": null, "description": "Initial value", "name": "initialValue", "required": false, "type": { "name": "number" } }, "resultIcon": { "defaultValue": null, "description": "Icon to display beside the calculated result", "name": "resultIcon", "required": false, "type": { "name": "ReactNode" } }, "onOk": { "defaultValue": null, "description": "A function to be run when the OK button is clicked", "name": "onOk", "required": false, "type": { "name": "OnNumberCommitFunc" } }, "onCancel": { "defaultValue": null, "description": "A function to be run when the Cancel button is clicked", "name": "onCancel", "required": false, "type": { "name": "OnCancelFunc" } }, "engine": { "defaultValue": { value: "new CalculatorEngine()" }, "description": "@internal Calculator state machine.", "name": "engine", "required": false, "type": { "name": "CalculatorEngine" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class CalculatorPopup extends reactExports.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      size: new Size(-1, -1)
    };
    this._onSizeKnown = (newSize) => {
      if (!this.state.size.equals(newSize))
        this.setState({ size: Size.create(newSize) });
    };
  }
  /** @internal */
  render() {
    const point = PopupManager.getPopupPosition(
      this.props.el,
      this.props.pt,
      this.props.offset,
      this.state.size
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      PositionPopup,
      {
        point,
        className: "uifw-calculator-host",
        onSizeKnown: this._onSizeKnown,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(DivWithOutsideClick, { onOutsideClick: this.props.onCancel, children: /* @__PURE__ */ jsxRuntimeExports.jsx(PositionPopupContent, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Calculator,
          {
            initialValue: this.props.initialValue,
            resultIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec: this.props.resultIcon }),
            onOk: this.props.onOk,
            onCancel: this.props.onCancel
          }
        ) }) })
      },
      this.props.id
    );
  }
}
try {
  CalculatorPopup.displayName = "CalculatorPopup";
  CalculatorPopup.__docgenInfo = { "description": "Popup component for Calculator", "displayName": "CalculatorPopup", "props": { "initialValue": { "defaultValue": null, "description": "", "name": "initialValue", "required": true, "type": { "name": "number" } }, "resultIcon": { "defaultValue": null, "description": "", "name": "resultIcon", "required": true, "type": { "name": "string" } }, "onOk": { "defaultValue": null, "description": "", "name": "onOk", "required": true, "type": { "name": "OnNumberCommitFunc" } }, "onCancel": { "defaultValue": null, "description": "", "name": "onCancel", "required": true, "type": { "name": "OnCancelFunc" } }, "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "el": { "defaultValue": null, "description": "", "name": "el", "required": true, "type": { "name": "HTMLElement" } }, "pt": { "defaultValue": null, "description": "", "name": "pt", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "offset": { "defaultValue": null, "description": "", "name": "offset", "required": true, "type": { "name": "Readonly<WritableXAndY>" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class MenuButton extends reactExports.PureComponent {
  constructor(props) {
    super(props);
    this._menu = null;
    this._open = () => {
      this.setState({ expanded: true }, () => {
        if (this._menu)
          this._menu.focus();
      });
    };
    this._handleClick = (event) => {
      if (this.state.expanded) {
        event.stopPropagation();
        this.setState({ expanded: false });
      } else {
        this._open();
      }
    };
    this._handleClose = () => {
      this.setState({ expanded: false });
    };
    this.state = {
      expanded: false
    };
  }
  render() {
    const { point, className, style, onSizeKnown, ...buttonProps } = this.props;
    const divStyle = {
      top: point.y,
      left: point.x
    };
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "uifw-menu-button",
        style: divStyle,
        ref: (e) => this.setDivRef(e),
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            SquareButton,
            {
              ...buttonProps,
              className,
              style,
              onClick: this._handleClick,
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec: /* @__PURE__ */ jsxRuntimeExports.jsx(SvgMore, {}) })
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            ContextMenu,
            {
              ref: (el) => {
                this._menu = el;
              },
              selectedIndex: 0,
              onSelect: this._handleClose,
              onOutsideClick: this._handleClose,
              onEsc: this._handleClose,
              opened: this.state.expanded,
              autoflip: false,
              children: this.props.children
            }
          )
        ]
      }
    );
  }
  setDivRef(div) {
    if (div) {
      const rect = div.getBoundingClientRect();
      const size = new Size(rect.width, rect.height);
      if (this.props.onSizeKnown)
        this.props.onSizeKnown(size);
    }
  }
}
try {
  MenuButton.displayName = "MenuButton";
  MenuButton.__docgenInfo = { "description": "", "displayName": "MenuButton", "props": { "point": { "defaultValue": null, "description": "Center point", "name": "point", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "onSizeKnown": { "defaultValue": null, "description": "Function called when size is known.", "name": "onSizeKnown", "required": false, "type": { "name": "((size: SizeProps) => void)" } }, "as": { "defaultValue": null, "description": "", "name": "as", "required": false, "type": { "name": "enum", "value": [{ "value": '"button"' }] } }, "htmlDisabled": { "defaultValue": null, "description": "Built-in html `disabled` attribute", "name": "htmlDisabled", "required": false, "type": { "name": "boolean" } }, "startIcon": { "defaultValue": null, "description": "Icon shown before the main button content.", "name": "startIcon", "required": false, "type": { "name": "Element" } }, "endIcon": { "defaultValue": null, "description": "Icon shown after the main button content.", "name": "endIcon", "required": false, "type": { "name": "Element" } }, "labelProps": { "defaultValue": null, "description": "Passes props to the button label.", "name": "labelProps", "required": false, "type": { "name": "DetailedHTMLProps<HTMLAttributes<HTMLSpanElement>, HTMLSpanElement>" } }, "startIconProps": { "defaultValue": null, "description": "Passes props to the start icon.", "name": "startIconProps", "required": false, "type": { "name": "DetailedHTMLProps<HTMLAttributes<HTMLSpanElement>, HTMLSpanElement>" } }, "endIconProps": { "defaultValue": null, "description": "Passes props to the end icon.", "name": "endIconProps", "required": false, "type": { "name": "DetailedHTMLProps<HTMLAttributes<HTMLSpanElement>, HTMLSpanElement>" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class MenuButtonPopup extends reactExports.PureComponent {
  constructor() {
    super(...arguments);
    this.state = {
      size: new Size(-1, -1)
    };
    this._onSizeKnown = (newSize) => {
      if (!this.state.size.equals(newSize))
        this.setState({ size: Size.create(newSize) });
    };
  }
  /** @internal */
  render() {
    const point = PopupManager.getPopupPosition(
      this.props.el,
      this.props.pt,
      this.props.offset,
      this.state.size
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      MenuButton,
      {
        point,
        onSizeKnown: this._onSizeKnown,
        children: this.props.content
      },
      this.props.id
    );
  }
}
try {
  MenuButtonPopup.displayName = "MenuButtonPopup";
  MenuButtonPopup.__docgenInfo = { "description": "Popup component for Menu Buttons", "displayName": "MenuButtonPopup", "props": { "content": { "defaultValue": null, "description": "", "name": "content", "required": true, "type": { "name": "ReactNode" } }, "id": { "defaultValue": null, "description": "", "name": "id", "required": true, "type": { "name": "string" } }, "el": { "defaultValue": null, "description": "", "name": "el", "required": true, "type": { "name": "HTMLElement" } }, "pt": { "defaultValue": null, "description": "", "name": "pt", "required": true, "type": { "name": "Readonly<WritableXAndY>" } }, "offset": { "defaultValue": null, "description": "", "name": "offset", "required": true, "type": { "name": "Readonly<WritableXAndY>" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const angleIcon = "data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2016%2016'%3e%3ctitle%3eangle%3c/title%3e%3cpath%20d='M16,10H14a4.9509,4.9509,0,0,0-1.2325-3.2723L14.23,5.2655l-3.4651-.8,2.0773-2.1153-.7139-.7012L0,14H15.9971V13H14.125ZM2.3838,13,9.8974,5.3488l.7967,3.4522,1.3621-1.362A3.95363,3.95363,0,0,1,13,10H11l1.875,3Z'/%3e%3c/svg%3e";
const lengthIcon = "data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2016%2016'%3e%3ctitle%3edistance%3c/title%3e%3crect%20y='11'%20width='16'%20height='1'/%3e%3cpolygon%20points='15%204%2015%205.875%2012%204%2012%206%204%206%204%204%201%205.875%201%204%200%204%200%209%201%209%201%207.125%204%209%204%207%2012%207%2012%209%2015%207.125%2015%209%2016%209%2016%204%2015%204'/%3e%3c/svg%3e";
const heightIcon = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3c!--%20Generator:%20Adobe%20Illustrator%2023.0.3,%20SVG%20Export%20Plug-In%20.%20SVG%20Version:%206.00%20Build%200)%20--%3e%3csvg%20version='1.1'%20id='Calque_1'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:xlink='http://www.w3.org/1999/xlink'%20x='0px'%20y='0px'%20viewBox='0%200%2016%2016'%20style='enable-background:new%200%200%2016%2016;'%20xml:space='preserve'%3e%3ctitle%3edistance%3c/title%3e%3cg%3e%3cpolygon%20points='16,15%2014.125,15%2016,12%2014,12%2014,4%2016,4%2014.125,1%2016,1%2016,0%2011,0%2011,1%2012.875,1%2011,4%2013,4%2013,12%2011,12%2012.875,15%200,15%200,16%2016,16%20'/%3e%3c/g%3e%3c/svg%3e";
const _AccuDrawPopupManager = class _AccuDrawPopupManager {
  static showMenuButton(id, el, pt, menuItemsProps) {
    const menuItems = MenuItemHelpers.createMenuItems(menuItemsProps);
    const menuContent = MenuItemHelpers.createMenuItemNodes(menuItems);
    const component = /* @__PURE__ */ jsxRuntimeExports.jsx(
      MenuButtonPopup,
      {
        id,
        el,
        pt,
        offset: PopupManager.defaultOffset,
        content: menuContent
      }
    );
    const popupInfo = {
      id,
      pt,
      component,
      parentDocument: el.ownerDocument,
      parent: el.ownerDocument
    };
    PopupManager.addOrUpdatePopup(popupInfo);
    return true;
  }
  static hideMenuButton(id) {
    return PopupManager.removePopup(id);
  }
  static showCalculator(el, pt, initialValue, resultIcon, onOk, onCancel) {
    const id = _AccuDrawPopupManager._calculatorId;
    const component = /* @__PURE__ */ jsxRuntimeExports.jsx(
      CalculatorPopup,
      {
        id,
        el,
        pt,
        offset: PopupManager.defaultOffset,
        initialValue,
        resultIcon,
        onOk,
        onCancel
      }
    );
    const popupInfo = {
      id,
      pt,
      component,
      parentDocument: el.ownerDocument,
      parent: el.ownerDocument
    };
    PopupManager.addOrUpdatePopup(popupInfo);
    return true;
  }
  static hideCalculator() {
    return PopupManager.removePopup(_AccuDrawPopupManager._calculatorId);
  }
  static showAngleEditor(el, pt, value, onCommit, onCancel) {
    const propertyDescription = new AngleDescription(
      void 0,
      void 0,
      angleIcon
    );
    return PopupManager.showInputEditor(
      el,
      pt,
      value,
      propertyDescription,
      onCommit,
      onCancel
    );
  }
  // @internal
  static showDimensionEditor(dimension, el, pt, value, onCommit, onCancel) {
    if (dimension === "height") {
      return _AccuDrawPopupManager.showHeightEditor(
        el,
        pt,
        value,
        onCommit,
        onCancel
      );
    }
    return _AccuDrawPopupManager.showLengthEditor(
      el,
      pt,
      value,
      onCommit,
      onCancel
    );
  }
  static showLengthEditor(el, pt, value, onCommit, onCancel) {
    const propertyDescription = new LengthDescription(
      void 0,
      void 0,
      lengthIcon
    );
    return PopupManager.showInputEditor(
      el,
      pt,
      value,
      propertyDescription,
      onCommit,
      onCancel
    );
  }
  static showHeightEditor(el, pt, value, onCommit, onCancel) {
    const propertyDescription = new LengthDescription(
      void 0,
      void 0,
      heightIcon
    );
    return PopupManager.showInputEditor(
      el,
      pt,
      value,
      propertyDescription,
      onCommit,
      onCancel
    );
  }
};
_AccuDrawPopupManager._calculatorId = "Calculator";
let AccuDrawPopupManager = _AccuDrawPopupManager;
try {
  AccuDrawPopupManager.displayName = "AccuDrawPopupManager";
  AccuDrawPopupManager.__docgenInfo = { "description": "AccuDraw Popup Manager class", "displayName": "AccuDrawPopupManager", "props": {} };
} catch (__react_docgen_typescript_loader_error) {
}
function UiDataProvidedDialog({
  uiDataProvider,
  id,
  isModal,
  ...dialogProps
}) {
  const dialogId = reactExports.useRef(id ? id : getUniqueId());
  const dialogIsModal = reactExports.useRef(isModal);
  const onOK = reactExports.useRef();
  const onCancel = reactExports.useRef();
  const closeDialog = () => {
    if (dialogIsModal.current)
      UiFramework.dialogs.modal.close();
    else
      UiFramework.dialogs.modeless.close(dialogId.current);
  };
  const handleOk = reactExports.useCallback(() => {
    onOK.current && onOK.current();
    closeDialog();
  }, []);
  const handleCancel = reactExports.useCallback(() => {
    onCancel.current && onCancel.current();
    closeDialog();
  }, []);
  const generateButtonCluster = reactExports.useCallback(
    (buttons2) => {
      if (buttons2) {
        for (const button of buttons2) {
          if (DialogButtonType.Cancel === button.type) {
            onCancel.current = button.onClick;
            button.onClick = handleCancel;
            continue;
          }
          if (DialogButtonType.OK === button.type) {
            onOK.current = button.onClick;
            button.onClick = handleOk;
            continue;
          }
        }
      }
      return buttons2;
    },
    [handleCancel, handleOk]
  );
  const [buttonCluster, setButtonCluster] = reactExports.useState(
    () => generateButtonCluster(uiDataProvider.supplyButtonData())
  );
  reactExports.useEffect(() => {
    const handleReloaded = () => {
      setButtonCluster(
        generateButtonCluster(uiDataProvider.supplyButtonData())
      );
    };
    uiDataProvider.onItemsReloadedEvent.addListener(handleReloaded);
    return () => {
      uiDataProvider.onItemsReloadedEvent.removeListener(handleReloaded);
    };
  }, [generateButtonCluster, uiDataProvider]);
  reactExports.useEffect(() => {
    const handleButtonReloaded = () => {
      setButtonCluster(
        generateButtonCluster(uiDataProvider.supplyButtonData())
      );
    };
    uiDataProvider.onButtonsReloadedEvent.addListener(handleButtonReloaded);
    return () => {
      uiDataProvider.onButtonsReloadedEvent.removeListener(
        handleButtonReloaded
      );
    };
  }, [generateButtonCluster, uiDataProvider]);
  const handleClose = reactExports.useCallback(() => closeDialog(), []);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Dialog$1,
    {
      ...dialogProps,
      opened: true,
      modal: isModal,
      buttonCluster,
      onClose: handleClose,
      onEscape: handleClose,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        DefaultDialogGridContainer,
        {
          componentGenerator: new ComponentGenerator(uiDataProvider)
        }
      )
    }
  );
}
try {
  UiDataProvidedDialog.displayName = "UiDataProvidedDialog";
  UiDataProvidedDialog.__docgenInfo = { "description": "Component to show dialog populated from properties supplied via uiDataProvider", "displayName": "UiDataProvidedDialog", "props": { "title": { "defaultValue": null, "description": "Dialog title", "name": "title", "required": true, "type": { "name": "string" } }, "uiDataProvider": { "defaultValue": null, "description": "Provider that provides and lays out DialogItems", "name": "uiDataProvider", "required": true, "type": { "name": "DialogLayoutDataProvider" } }, "isModal": { "defaultValue": null, "description": "Indicates if Dialog is Modal", "name": "isModal", "required": true, "type": { "name": "boolean" } }, "id": { "defaultValue": null, "description": "Id used to specify dialog.", "name": "id", "required": false, "type": { "name": "string" } }, "resizable": { "defaultValue": null, "description": "Indicates whether the user can resize dialog with cursor. Default: false", "name": "resizable", "required": false, "type": { "name": "boolean" } }, "movable": { "defaultValue": null, "description": "Indicates whether the user can move dialog with cursor. Default: false", "name": "movable", "required": false, "type": { "name": "boolean" } }, "width": { "defaultValue": null, "description": 'Initial width of dialog. Displayed in px if value is a number; otherwise, displayed in specified CSS unit. Default: "50%"', "name": "width", "required": false, "type": { "name": "string | number" } }, "height": { "defaultValue": null, "description": "Initial height of dialog. Displayed in px if value is a number; otherwise, displayed in specified CSS unit.", "name": "height", "required": false, "type": { "name": "string | number" } }, "minWidth": { "defaultValue": null, "description": "Minimum width that the dialog may be resized to. Displayed in px if value is a number; otherwise, displayed in specified CSS unit. Default: 300px", "name": "minWidth", "required": false, "type": { "name": "string | number" } }, "minHeight": { "defaultValue": null, "description": "Minimum height that the dialog may be resized to. Displayed in px if value is a number; otherwise, displayed in specified CSS unit. Default: 100px", "name": "minHeight", "required": false, "type": { "name": "string | number" } }, "maxWidth": { "defaultValue": null, "description": "Maximum width that the dialog may be resized to. Displayed in px if value is a number; otherwise, displayed in specified CSS unit.", "name": "maxWidth", "required": false, "type": { "name": "string | number" } }, "maxHeight": { "defaultValue": null, "description": "Maximum height that the dialog may be resized to. Displayed in px if value is a number; otherwise, displayed in specified CSS unit.", "name": "maxHeight", "required": false, "type": { "name": "string | number" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class UiVisibilityChangedEvent extends UiEvent {
}
const _UiFramework = class _UiFramework {
  /**
   * Operation on the backstage component.
   * @public
   */
  static get backstage() {
    if (!_UiFramework._backstageManager)
      throw new UiError(
        _UiFramework.loggerCategory(this),
        _UiFramework._complaint
      );
    return _UiFramework._backstageManager;
  }
  /**
   * Manage access to the child windows.
   * @public
   */
  static get childWindows() {
    return this._childWindowManager;
  }
  /**
   * Manage registered controls
   * @public
   */
  static get controls() {
    return InternalConfigurableUiManager;
  }
  /**
   * Manage access to frontstages and related helper methods.
   * @public
   */
  static get frontstages() {
    return InternalFrontstageManager;
  }
  /**
   * Manage access and behavior of the tool settings.
   * @public
   */
  static get toolSettings() {
    return InternalToolSettingsManager;
  }
  /**
   * Manage content presented by the frontstages.
   * @public
   */
  static get content() {
    return InternalContentViewManager;
  }
  /**
   * Manage displayed dialogs.
   * @public
   */
  static get dialogs() {
    return {
      modal: InternalModalDialogManager,
      modeless: InternalModelessDialogManager
    };
  }
  /**
   * Manages global keyboard shortcuts
   *
   * Note: This only manages the list of available shortcuts registered with it. It does not listens to the actual
   * keyboard events. In order for these shortcuts to be called upon a keyboard event, the application can
   * override the `IModelApp.toolAdmin` and assign it [[FrameworkToolAdmin]] or create an event listener
   * and call `UiFramework.keyboardShortcuts.processKey`.
   * @public
   */
  static get keyboardShortcuts() {
    return InternalKeyboardShortcutManager;
  }
  /**
   * Manages UI visibility (Show/Hide)
   * @public
   */
  static get visibility() {
    return InternalUiShowHideManager;
  }
  /** Registers class that will be informed when the UserSettingsStorage location has been set or changed. This allows
   * classes to load any previously saved settings from the new storage location. Common storage locations are the browser's
   * local storage, or the iTwin Product Settings cloud storage available via the SettingsAdmin see `IModelApp.settingsAdmin`.
   * @public
   */
  static registerUserSettingsProvider(entry) {
    if (this._uiSettingsProviderRegistry.has(entry.providerId))
      return false;
    this._uiSettingsProviderRegistry.set(entry.providerId, entry);
    return true;
  }
  /**
   * Called by the application to initialize the UiFramework. Also initializes UIIModelComponents, UiComponents, UiCore.
   * @param store The single Redux store created by the host application. If this is `undefined` then it is assumed that the [[StateManager]] is being used to provide the Redux store.
   * @param frameworkStateKey The name of the key used by the app when adding the UiFramework state into the Redux store. If not defined "frameworkState" is assumed. This value is ignored if [[StateManager]] is being used. The StateManager use "frameworkState".
   */
  static async initialize(store, frameworkStateKey) {
    var _a2;
    if (_UiFramework._initialized) {
      Logger.logInfo(
        _UiFramework.loggerCategory(_UiFramework),
        `UiFramework.initialize already called`
      );
      return;
    }
    if (void 0 === store && !StateManager.isInitialized(true))
      new StateManager();
    _UiFramework._store = store;
    if (frameworkStateKey && store)
      _UiFramework._frameworkStateKeyInStore = frameworkStateKey;
    const frameworkNamespace = (_a2 = IModelApp.localization) == null ? void 0 : _a2.registerNamespace(
      _UiFramework.localizationNamespace
    );
    [
      restoreLayoutTools,
      keyinPaletteTools,
      openSettingTools,
      toolSettingTools
    ].forEach(
      (tool) => IModelApp.tools.registerModule(tool, this.localizationNamespace)
    );
    _UiFramework._backstageManager = new BackstageManager();
    _UiFramework._hideIsolateEmphasizeActionHandler = new HideIsolateEmphasizeManager();
    _UiFramework._widgetManager = new WidgetManager();
    await UiIModelComponents.initialize();
    _UiFramework.settingsManager.onSettingsProvidersChanged.addListener(() => {
      SyncUiEventDispatcher.dispatchSyncUiEvent(
        SyncUiEventId.SettingsProvidersChanged
      );
    });
    UiAdmin.messagePresenter = IModelApp.notifications;
    _UiFramework._initialized = true;
    UiShowHideSettingsProvider.initialize();
    InternalConfigurableUiManager.initialize();
    return frameworkNamespace;
  }
  /** Un-registers the UiFramework internationalization service namespace */
  static terminate() {
    var _a2;
    _UiFramework._store = void 0;
    _UiFramework._frameworkStateKeyInStore = "frameworkState";
    if (StateManager.isInitialized(true))
      StateManager.clearStore();
    (_a2 = IModelApp.localization) == null ? void 0 : _a2.unregisterNamespace(
      _UiFramework.localizationNamespace
    );
    _UiFramework._backstageManager = void 0;
    _UiFramework._widgetManager = void 0;
    _UiFramework._hideIsolateEmphasizeActionHandler = void 0;
    _UiFramework._settingsManager = void 0;
    UiIModelComponents.terminate();
    InternalUiShowHideManager.terminate();
    _UiFramework._initialized = false;
  }
  /** Determines if UiFramework has been initialized */
  static get initialized() {
    return _UiFramework._initialized;
  }
  /** Property that returns the SettingManager used by AppUI-based applications.
   * @public
   */
  static get settingsManager() {
    if (void 0 === _UiFramework._settingsManager)
      _UiFramework._settingsManager = new SettingsManager();
    return _UiFramework._settingsManager;
  }
  /** @public */
  static get frameworkStateKey() {
    return _UiFramework._frameworkStateKeyInStore;
  }
  /** The UiFramework state maintained by Redux
   * @public
   */
  static get frameworkState() {
    try {
      return _UiFramework.store.getState()[_UiFramework.frameworkStateKey];
    } catch (_e) {
      return void 0;
    }
  }
  /** The Redux store */
  static get store() {
    if (_UiFramework._store)
      return _UiFramework._store;
    if (!StateManager.isInitialized(true))
      throw new UiError(
        _UiFramework.loggerCategory(this),
        `Error trying to access redux store before either store or StateManager has been initialized.`
      );
    return StateManager.store;
  }
  /** The internationalization service namespace. */
  static get localizationNamespace() {
    return "UiFramework";
  }
  /** @public */
  static get hideIsolateEmphasizeActionHandler() {
    if (!_UiFramework._hideIsolateEmphasizeActionHandler)
      throw new UiError(
        _UiFramework.loggerCategory(this),
        _UiFramework._complaint
      );
    return _UiFramework._hideIsolateEmphasizeActionHandler;
  }
  /** @public */
  static setHideIsolateEmphasizeActionHandler(handler) {
    if (handler)
      _UiFramework._hideIsolateEmphasizeActionHandler = handler;
    else
      _UiFramework._hideIsolateEmphasizeActionHandler = new HideIsolateEmphasizeManager();
  }
  /** @alpha */
  static get widgetManager() {
    if (!_UiFramework._widgetManager)
      throw new UiError(
        _UiFramework.loggerCategory(this),
        _UiFramework._complaint
      );
    return _UiFramework._widgetManager;
  }
  /** Calls localization.getLocalizedString with the "UiFramework" namespace. Do NOT include the namespace in the key.
   * @internal
   */
  static translate(key, options) {
    return IModelApp.localization.getLocalizedString(
      `${_UiFramework.localizationNamespace}:${key}`,
      options
    );
  }
  /** @internal */
  static get packageName() {
    return "appui-react";
  }
  /** @internal */
  static loggerCategory(obj) {
    const className = getObjectClassName(obj);
    const category2 = _UiFramework.packageName + (className ? `.${className}` : "");
    return category2;
  }
  static dispatchActionToStore(type, payload, immediateSync = false) {
    _UiFramework.store.dispatch({ type, payload });
    if (immediateSync)
      SyncUiEventDispatcher.dispatchImmediateSyncUiEvent(type);
    else
      SyncUiEventDispatcher.dispatchSyncUiEvent(type);
  }
  static setAccudrawSnapMode(snapMode) {
    _UiFramework.dispatchActionToStore(
      ConfigurableUiActionId.SetSnapMode,
      snapMode,
      true
    );
  }
  static getAccudrawSnapMode() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.configurableUiState.snapMode : (
      /* istanbul ignore next */
      SnapMode.NearestKeypoint
    );
  }
  /**
   * Returns the stored active selection scope id.
   */
  static getActiveSelectionScope() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.sessionState.activeSelectionScope : (
      /* istanbul ignore next */
      "element"
    );
  }
  /**
   * This method stores the active selection scope to the supplied scope id, and triggers
   * a `SessionStateActionId.SetSelectionScope` event in the `SyncUiEventDispatcher`.
   * Note: As of 4.0, this method *does not change* the active selection scope in the `Presentation.selection.scopes.activeScope` property.
   * This event should be listened to and the change should typically be applied to
   * `Presentation.selection.scopes.activeScope` property from the `@itwin/presentation-frontend` package.
   */
  static setActiveSelectionScope(selectionScopeId) {
    if (_UiFramework.frameworkState) {
      const foundIndex = _UiFramework.frameworkState.sessionState.availableSelectionScopes.findIndex(
        (selectionScope) => selectionScope.id === selectionScopeId
      );
      if (-1 !== foundIndex) {
        const scope = _UiFramework.frameworkState.sessionState.availableSelectionScopes[foundIndex];
        _UiFramework.dispatchActionToStore(
          SessionStateActionId.SetSelectionScope,
          scope.id
        );
      }
    }
  }
  /** Show a context menu at a particular location.
   * @param items Properties of the menu items to display.
   * @param location Location of the context menu, relative to the origin of anchorElement or the overall window.
   * @param anchorElement The HTMLElement that anchors the context menu. If undefined, the location is relative to the overall window.
   * @return true if the menu was displayed, false if the menu could not be displayed.
   */
  static openContextMenu(items, location, anchorElement) {
    let position = location;
    let childWindowId;
    if (anchorElement) {
      const anchorOffset = anchorElement.getBoundingClientRect();
      position = {
        x: anchorOffset.left + location.x,
        y: anchorOffset.top + location.y
      };
      childWindowId = _UiFramework.childWindows.findId(
        anchorElement.ownerDocument.defaultView
      );
    }
    position = {
      x: position.x + _UiFramework.CONTEXT_MENU_OFFSET,
      y: position.y + _UiFramework.CONTEXT_MENU_OFFSET
    };
    const cursorMenuData = {
      position,
      items,
      childWindowId
    };
    _UiFramework.openCursorMenu(cursorMenuData);
    return true;
  }
  /** @public */
  static openCursorMenu(menuData) {
    _UiFramework.dispatchActionToStore(
      SessionStateActionId.UpdateCursorMenu,
      menuData
    );
  }
  /** @public */
  static closeCursorMenu() {
    _UiFramework.dispatchActionToStore(
      SessionStateActionId.UpdateCursorMenu,
      void 0
    );
  }
  /** @public */
  static getCursorMenuData() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.sessionState.cursorMenuPayload ?? _UiFramework.frameworkState.sessionState.cursorMenuData : (
      /* istanbul ignore next */
      void 0
    );
  }
  static getActiveIModelId() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.sessionState.iModelId : (
      /* istanbul ignore next */
      ""
    );
  }
  static setActiveIModelId(iModelId) {
    _UiFramework.dispatchActionToStore(
      SessionStateActionId.SetActiveIModelId,
      iModelId
    );
  }
  static setIModelConnection(iModelConnection, immediateSync = false) {
    const oldConnection = _UiFramework.getIModelConnection();
    if (oldConnection === iModelConnection)
      return;
    InternalFrontstageManager.clearFrontstageDefsForIModelId(
      oldConnection == null ? void 0 : oldConnection.iModelId
    );
    oldConnection && SyncUiEventDispatcher.clearConnectionEvents(oldConnection);
    iModelConnection && SyncUiEventDispatcher.initializeConnectionEvents(iModelConnection);
    _UiFramework.dispatchActionToStore(
      SessionStateActionId.SetIModelConnection,
      iModelConnection,
      immediateSync
    );
    const itemsSelected = iModelConnection ? iModelConnection.selectionSet.elements.size : 0;
    _UiFramework.dispatchActionToStore(
      SessionStateActionId.SetNumItemsSelected,
      itemsSelected
    );
    _UiFramework.setActiveIModelId((iModelConnection == null ? void 0 : iModelConnection.iModelId) ?? "");
  }
  static getIModelConnection() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.sessionState.iModelConnection : (
      /* istanbul ignore next */
      void 0
    );
  }
  /** Called by iModelApp to initialize saved UI state from registered UseSettingsProviders
   * @public
   */
  static async initializeStateFromUserSettingsProviders(immediateSync = false) {
    const providerKeys = [...this._uiSettingsProviderRegistry.keys()];
    for await (const key of providerKeys) {
      await this._uiSettingsProviderRegistry.get(key).loadUserSettings(_UiFramework._uiStateStorage);
    }
    if (immediateSync)
      SyncUiEventDispatcher.dispatchImmediateSyncUiEvent(
        SyncUiEventId.UiStateStorageChanged
      );
    else
      SyncUiEventDispatcher.dispatchSyncUiEvent(
        SyncUiEventId.UiStateStorageChanged
      );
  }
  /** @public */
  static async setUiStateStorage(storage, immediateSync = false) {
    if (_UiFramework._uiStateStorage === storage)
      return;
    _UiFramework._uiStateStorage = storage;
    await this.initializeStateFromUserSettingsProviders(immediateSync);
  }
  /** The UI Settings Storage is a convenient wrapper around Local Storage to assist in caching state information across user sessions.
   * It was previously used to conflate both the state information across session and the information driven directly from user explicit action,
   * which are now handled with user preferences.
   * @public
   */
  static getUiStateStorage() {
    return _UiFramework._uiStateStorage;
  }
  static setDefaultIModelViewportControlId(iModelViewportControlId, immediateSync = false) {
    _UiFramework.dispatchActionToStore(
      SessionStateActionId.SetDefaultIModelViewportControlId,
      iModelViewportControlId,
      immediateSync
    );
  }
  static getDefaultIModelViewportControlId() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.sessionState.defaultIModelViewportControlId : (
      /* istanbul ignore next */
      void 0
    );
  }
  static setDefaultViewId(viewId, immediateSync = false) {
    _UiFramework.dispatchActionToStore(
      SessionStateActionId.SetDefaultViewId,
      viewId,
      immediateSync
    );
  }
  static getDefaultViewId() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.sessionState.defaultViewId : (
      /* istanbul ignore next */
      void 0
    );
  }
  static setDefaultViewState(viewState, immediateSync = false) {
    _UiFramework.dispatchActionToStore(
      SessionStateActionId.SetDefaultViewState,
      viewState,
      immediateSync
    );
  }
  static getDefaultViewState() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.sessionState.defaultViewState : (
      /* istanbul ignore next */
      void 0
    );
  }
  /**
   * Returns the stored list of available selection scopes. This list should be set by the application
   * by dispatching the `setAvailableSelectionScopes` action.
   * The value for this action typically come from `Presentation.selection.scopes.getSelectionScopes()`
   * method found in the `@itwin/presentation-frontend` package.
   * @public */
  static getAvailableSelectionScopes() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.sessionState.availableSelectionScopes : (
      /* istanbul ignore next */
      [{ id: "element", label: "Element" }]
    );
  }
  static getIsUiVisible() {
    return _UiFramework.visibility.isUiVisible;
  }
  static setIsUiVisible(visible) {
    if (_UiFramework.visibility.isUiVisible !== visible) {
      _UiFramework.visibility.isUiVisible = visible;
      _UiFramework.onUiVisibilityChanged.emit({ visible });
    }
  }
  /**
   * Set the theme value used by the [[ThemeManager]] component.
   */
  static setColorTheme(theme) {
    if (_UiFramework.getColorTheme() === theme)
      return;
    _UiFramework.dispatchActionToStore(
      ConfigurableUiActionId.SetTheme,
      theme,
      true
    );
  }
  static getColorTheme() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.configurableUiState.theme : (
      /* istanbul ignore next */
      SYSTEM_PREFERRED_COLOR_THEME
    );
  }
  /** UiFramework.setToolbarOpacity() sets the non-hovered opacity to the value specified.
   * @param opacity a value between 0 and 1. The default value is 0.5. IT IS NOT ADVISED TO USE A VALUE BELOW 0.2
   * @public
   */
  static setToolbarOpacity(opacity) {
    if (_UiFramework.getToolbarOpacity() === opacity)
      return;
    _UiFramework.dispatchActionToStore(
      ConfigurableUiActionId.SetToolbarOpacity,
      opacity,
      true
    );
  }
  /** UiFramework.getToolbarOpacity() returns a number between 0 and 1 that is the non-hovered opacity for toolbars.
   * @public
   */
  static getToolbarOpacity() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.configurableUiState.toolbarOpacity : (
      /* istanbul ignore next */
      TOOLBAR_OPACITY_DEFAULT
    );
  }
  static setWidgetOpacity(opacity) {
    if (_UiFramework.getWidgetOpacity() === opacity)
      return;
    _UiFramework.dispatchActionToStore(
      ConfigurableUiActionId.SetWidgetOpacity,
      opacity,
      true
    );
  }
  static getWidgetOpacity() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.configurableUiState.widgetOpacity : (
      /* istanbul ignore next */
      WIDGET_OPACITY_DEFAULT
    );
  }
  // eslint-disable-next-line @itwin/prefer-get
  static isMobile() {
    return ProcessDetector.isMobileBrowser;
  }
  static get showWidgetIcon() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.configurableUiState.showWidgetIcon : (
      /* istanbul ignore next */
      false
    );
  }
  static setShowWidgetIcon(value) {
    if (_UiFramework.showWidgetIcon === value)
      return;
    _UiFramework.dispatchActionToStore(
      ConfigurableUiActionId.SetShowWidgetIcon,
      value,
      true
    );
  }
  /** Animate Tool Settings on appear  */
  static get animateToolSettings() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.configurableUiState.animateToolSettings : (
      /* istanbul ignore next */
      false
    );
  }
  static setAnimateToolSettings(value) {
    if (_UiFramework.animateToolSettings === value)
      return;
    _UiFramework.dispatchActionToStore(
      ConfigurableUiActionId.AnimateToolSettings,
      value,
      true
    );
  }
  /** Use Tool Name As Tool Settings Widget Tab Label */
  static get useToolAsToolSettingsLabel() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.configurableUiState.useToolAsToolSettingsLabel : (
      /* istanbul ignore next */
      false
    );
  }
  static setUseToolAsToolSettingsLabel(value) {
    if (_UiFramework.useToolAsToolSettingsLabel === value)
      return;
    _UiFramework.dispatchActionToStore(
      ConfigurableUiActionId.UseToolAsToolSettingsLabel,
      value,
      true
    );
  }
  /** When `true`, panels will close as soon as the mouse leave the panel.
   * When `false` (default), panels will close on next click outside the panel.
   * @public */
  static get autoCollapseUnpinnedPanels() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.configurableUiState.autoCollapseUnpinnedPanels : (
      /* istanbul ignore next */
      false
    );
  }
  /** Method used to enable the automatic closing of an unpinned widget panel as soon as the
   * mouse leaves the widget panel. The default behavior is to require a mouse click outside
   * the panel before it is closed.
   * @public */
  static setAutoCollapseUnpinnedPanels(value) {
    if (_UiFramework.autoCollapseUnpinnedPanels === value)
      return;
    _UiFramework.dispatchActionToStore(
      ConfigurableUiActionId.AutoCollapseUnpinnedPanels,
      value,
      true
    );
  }
  static get useDragInteraction() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.configurableUiState.useDragInteraction : false;
  }
  static setUseDragInteraction(useDragInteraction) {
    _UiFramework.dispatchActionToStore(
      ConfigurableUiActionId.SetDragInteraction,
      useDragInteraction,
      true
    );
  }
  /** Returns the variable controlling whether the overlay is displayed in a Viewport
   * @public
   */
  static get viewOverlayDisplay() {
    return _UiFramework.frameworkState ? _UiFramework.frameworkState.configurableUiState.viewOverlayDisplay : (
      /* istanbul ignore next */
      true
    );
  }
  /** Set the variable that controls display of the view overlay. Applies to all viewports in the app
   * @public
   */
  static setViewOverlayDisplay(display) {
    if (_UiFramework.viewOverlayDisplay === display)
      return;
    _UiFramework.dispatchActionToStore(
      ConfigurableUiActionId.SetViewOverlayDisplay,
      display
    );
  }
  /** Determines whether a ContextMenu is open
   * @alpha
   * */
  static get isContextMenuOpen() {
    const contextMenu = document.querySelector("div.core-context-menu-opened");
    return contextMenu !== null && contextMenu !== void 0;
  }
  /** Show a Card containing content, a title and a toolbar at a particular location.
   * @param content The React component or HTMLElement of the content to display
   * @param title Title to display at the top of the card.
   * @param toolbarProps Properties of the Toolbar to display.
   * @param location Location of the Card, relative to the origin of anchorElement or the window.
   * @param offset Offset of the Card from the location.
   * @param onItemExecuted Function invoked after a Toolbar item is executed
   * @param onCancel Function invoked when the Escape key is pressed or a click occurs outside the Card
   * @param placement {@link Placement} relative to the given location. Defaults to top-end.
   * @param anchorElement The HTMLElement that anchors the Card. If undefined, the location is relative to the overall window.
   * @return true if the Card was displayed, false if the Card could not be displayed.
   * @internal
   */
  static showCard(content, title, toolbarProps, location, offset, onItemExecuted, onCancel, placement = "top-end", anchorElement) {
    const anchor = this.resolveHtmlElement(anchorElement);
    return PopupManager.displayCard(
      { reactNode: content },
      {
        title,
        toolbarProps,
        location,
        offset,
        onItemExecuted,
        onCancel,
        placement,
        anchor
      }
    );
  }
  /**
   * Hides a Card displayed with {@link UiFramework.showCard}
   * @internal
   */
  static hideCard() {
    return PopupManager.hideCard();
  }
  /** Opens a Tool Settings Ui popup at a particular location.
   * @param dataProvider The DialogLayoutDataProvider for the tool settings popup dialog.
   * @param location Location of the tool settings, relative to the origin of anchorElement or the window
   * @param offset Offset of the tool settings from the location
   * @param onCancel Function invoked when the Escape key is pressed or a click occurs outside the tool settings
   * @param placement {@link Placement} relative to the given location. Defaults to top-end.
   * @param anchorElement The HTMLElement that anchors the tool settings. If undefined, the location is relative to the overall window.
   * @return true if the tool settings were displayed, false if the tool settings could not be displayed.
   * @internal
   */
  static openToolSettingsPopup(dataProvider, location, offset, onCancel, placement = "top-end", anchorElement) {
    const el = this.resolveHtmlElement(anchorElement);
    const relativePosition = mapToRelativePosition(placement);
    return PopupManager.openToolSettings(
      dataProvider,
      el,
      location,
      offset,
      onCancel,
      relativePosition
    );
  }
  /**
   * Closes the Tool Settings Ui popup.
   * @internal
   */
  static closeToolSettingsPopup() {
    return PopupManager.closeToolSettings();
  }
  /** Show a Toolbar at a particular location.
   * @param toolbarProps Properties of the Toolbar to display.
   * @param location Location of the Toolbar, relative to the origin of anchorElement or the overall window.
   * @param offset Offset of the Toolbar from the location.
   * @param onItemExecuted Function invoked after a Toolbar item is executed
   * @param onCancel Function invoked when the Escape key is pressed or a click occurs outside the Toolbar
   * @param placement {@link Placement} relative to the given location. Defaults to top-end.
   * @param anchorElement The HTMLElement that anchors the Toolbar. If undefined, the location is relative to the overall window.
   * @return true if the Toolbar was displayed, false if the Toolbar could not be displayed.
   */
  static showToolbar(toolbarProps, location, offset, onItemExecuted, onCancel, placement = "top-end", anchorElement) {
    const anchor = _UiFramework.resolveHtmlElement(anchorElement);
    return PopupManager.displayToolbar(toolbarProps.items, {
      anchor,
      location,
      offset,
      onItemExecuted,
      onCancel,
      placement
    });
  }
  /** Hides a toolbar displayed via {@link UiFramework.showToolbar} */
  static hideToolbar() {
    return PopupManager.hideToolbar();
  }
  /** Show a menu button at a particular location. A menu button opens a context menu.
   * @param id Id of the menu button. Multiple menu buttons may be displayed.
   * @param menuItemsProps Properties of the menu items to display.
   * @param location Location of the context menu, relative to the origin of anchorElement or the window.
   * @param anchorElement The HTMLElement that anchors the context menu. If undefined, the location is relative to the overall window.
   * @return true if the menu was displayed, false if the menu could not be displayed.
   */
  static showMenuButton(id, menuItemsProps, location, anchorElement) {
    const el = this.resolveHtmlElement(anchorElement);
    return AccuDrawPopupManager.showMenuButton(
      id,
      el,
      location,
      menuItemsProps
    );
  }
  /** Hides a menu button.
   * @param id Id of the menu button. Multiple menu buttons may be displayed.
   * @return true if the menu was hidden, false if the menu could not be hidden.
   */
  static hideMenuButton(id) {
    return AccuDrawPopupManager.hideMenuButton(id);
  }
  /** Show a calculator at a particular location.
   * @param initialValue Value initially displayed in the calculator.
   * @param resultIcon Icon displayed to the left of the value.
   * @param location Location of the calculator, relative to the origin of anchorElement or the window.
   * @param onOk Function called when the OK button or the Enter key is pressed.
   * @param onCancel Function called when the Cancel button or the Escape key  is pressed.
   * @param anchorElement The HTMLElement that anchors the context menu. If undefined, the location is relative to the overall window.
   * @return true if the menu was displayed, false if the menu could not be displayed.
   */
  static showCalculator(initialValue, resultIcon, location, onOk, onCancel, anchorElement) {
    const el = this.resolveHtmlElement(anchorElement);
    return AccuDrawPopupManager.showCalculator(
      el,
      location,
      initialValue,
      resultIcon,
      onOk,
      onCancel
    );
  }
  /** Hides the calculator. */
  static hideCalculator() {
    return AccuDrawPopupManager.hideCalculator();
  }
  /** Show a React Element at a particular location.
   * @param component The ReactElement to display
   * @param options - Optional {@link: ShowComponentParams}
   */
  static showComponent(...params) {
    const options = params[1] || {};
    const component = params[0];
    const { anchorRef, id } = options;
    let { location, offset, onCancel, placement } = options;
    if (!location)
      location = { x: 0, y: 0 };
    if (!offset)
      offset = { x: 0, y: 0 };
    if (!placement)
      placement = "top-end";
    if (!onCancel)
      onCancel = () => _UiFramework.hideComponent(id);
    return PopupManager.showComponent(component, {
      location,
      offset,
      onCancel,
      placement,
      anchor: (anchorRef == null ? void 0 : anchorRef.current) ?? void 0,
      id
    });
  }
  /**
   * Hides the Component previously shown with {@link UiFramework.showComponent}
   */
  static hideComponent(id) {
    return PopupManager.hideComponent(id);
  }
  /** Show an input editor for an angle value at a particular location.
   * @param initialValue Value initially displayed in the editor.
   * @param location Location of the editor, relative to the origin of anchorElement or the window.
   * @param onCommit Function called when the OK button or the Enter key is pressed.
   * @param onCancel Function called when the Cancel button or the Escape key  is pressed.
   * @param anchorElement The HTMLElement that anchors the context menu. If undefined, the location is relative to the overall window.
   * @return true if the editor was displayed, false if the editor could not be displayed.
   */
  static showAngleEditor(initialValue, location, onCommit, onCancel, anchorElement) {
    const el = this.resolveHtmlElement(anchorElement);
    return AccuDrawPopupManager.showAngleEditor(
      el,
      location,
      initialValue,
      onCommit,
      onCancel
    );
  }
  /** Show an input editor for a length value at a particular location.
   * @param ShowInputEditorOptions Options detailed below for the input editor
   * @param ShowInputEditorOptions.anchorElement The HTMLElement that anchors the context menu. If undefined, the location is relative to the overall window.
   * @param ShowInputEditorOptions.propertyDescription Description of the primitive value property
   * @param ShowInputEditorOptions.initialValue Value initially displayed in the editor.
   * @param ShowInputEditorOptions.location Location of the editor, relative to the origin of anchorElement or the window.
   * @param ShowInputEditorOptions.onCommit Function called when the OK button or the Enter key is pressed.
   * @param ShowInputEditorOptions.onCancel Function called when the Cancel button or the Escape key is pressed.
   * @return true if the editor was displayed, false if the editor could not be displayed.
   * @internal
   */
  static showInputEditor({
    anchorElement,
    initialValue,
    location,
    onCancel,
    onCommit,
    propertyDescription
  }) {
    const el = this.resolveHtmlElement(anchorElement);
    PopupManager.showInputEditor(
      el,
      location,
      initialValue,
      propertyDescription,
      onCommit,
      onCancel
    );
    return true;
  }
  /** Show an input editor for a length value at a particular location.
   * @param dimension Dimension determining which editor to display.
   * @param initialValue Value initially displayed in the editor.
   * @param location Location of the editor, relative to the origin of anchorElement or the window.
   * @param onCommit Function called when the OK button or the Enter key is pressed.
   * @param onCancel Function called when the Cancel button or the Escape key  is pressed.
   * @param anchorElement The HTMLElement that anchors the context menu. If undefined, the location is relative to the overall window.
   * @return true if the editor was displayed, false if the editor could not be displayed.
   */
  static showDimensionEditor(dimension, initialValue, location, onCommit, onCancel, anchorElement) {
    const el = this.resolveHtmlElement(anchorElement);
    return AccuDrawPopupManager.showDimensionEditor(
      dimension,
      el,
      location,
      initialValue,
      onCommit,
      onCancel
    );
  }
  /** Hides the input editor.
   * @internal
   */
  static hideInputEditor() {
    return PopupManager.hideInputEditor();
  }
  /** Opens a Dialog and automatically populates it using the properties defined by the UiDataProvider.
   * @param uiDataProvider The UiDataProvider for the tool settings
   * @param title Specify title for dialog.
   * @param isModal Specify if the dialog is opened as a modal or modeless.
   * @param id Id of the dialog that is used to close it.
   * @param optionalProps Optional props for Dialog construction.
   * @return true if the tool settings were displayed, false if the tool settings could not be displayed.
   * @internal
   */
  static openDialog(uiDataProvider, title, isModal, id, optionalProps) {
    const dialog2 = reactExports.createElement(UiDataProvidedDialog, {
      uiDataProvider,
      title,
      isModal,
      id,
      ...optionalProps
    });
    const modalType = isModal ? "modal" : "modeless";
    _UiFramework.dialogs[modalType].open(dialog2, id);
    return true;
  }
  /** Closes the Dialog with the specified dialogId.
   * @param dialogId Id of the dialog to close.
   * @internal
   */
  static closeDialog(dialogId) {
    const findFn = (info) => info.id === dialogId;
    if (_UiFramework.dialogs.modeless.dialogs.findIndex(findFn) !== -1) {
      _UiFramework.dialogs.modeless.close(dialogId);
      return true;
    }
    if (_UiFramework.dialogs.modal.dialogs.findIndex(findFn) !== -1) {
      _UiFramework.dialogs.modal.close();
      return true;
    }
    return false;
  }
  /** Show the Key-in Palette to display key-in from all registered Tools.
   * @param keyinEntries A list of KeyinEntry to display in the palette. These can be gathered and filtered from iModelApp.tools.getToolList()
   * @param htmlElement The HTMLElement that anchors the Popup. If undefined, the location is relative to the overall window.
   * @return true if the Command Palette was displayed, false if it could not be displayed.
   */
  static showKeyinPalette(keyinEntries, htmlElement) {
    return PopupManager.showKeyinPalette(
      keyinEntries,
      _UiFramework.resolveHtmlElement(htmlElement)
    );
  }
  /** Hides the Key-in Palette. */
  static hideKeyinPalette() {
    return PopupManager.hideKeyinPalette();
  }
  static resolveHtmlElement(htmlElement) {
    const el = htmlElement ?? _UiFramework.controls.getWrapperElement();
    return el;
  }
};
_UiFramework._initialized = false;
_UiFramework._complaint = "UiFramework not initialized";
_UiFramework._frameworkStateKeyInStore = "frameworkState";
_UiFramework._uiStateStorage = new LocalStateStorage();
_UiFramework._uiSettingsProviderRegistry = /* @__PURE__ */ new Map();
_UiFramework._childWindowManager = new InternalChildWindowManager();
_UiFramework.useDefaultPopoutUrl = false;
_UiFramework.CONTEXT_MENU_OFFSET = -8;
_UiFramework.onUiVisibilityChanged = new UiVisibilityChangedEvent();
let UiFramework = _UiFramework;
class MessageCenterDialog extends reactExports.PureComponent {
  render() {
    const className = classnames(
      "nz-footer-messageCenter-dialog",
      this.props.className
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      Dialog,
      {
        className,
        style: this.props.style,
        titleBar: /* @__PURE__ */ jsxRuntimeExports.jsx(TitleBar, { title: this.props.title, children: this.props.buttons }),
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-tabs", role: "tablist", children: this.props.tabs }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-messages", children: this.props.children }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "nz-message-prompt", children: this.props.prompt }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-gradient" })
        ]
      }
    );
  }
}
try {
  MessageCenterDialog.displayName = "MessageCenterDialog";
  MessageCenterDialog.__docgenInfo = { "description": "Message center dialog used with [[MessageCenter]] component.", "displayName": "MessageCenterDialog", "props": { "buttons": { "defaultValue": null, "description": "Title bar buttons. I.e. [[TitleBarButton]]", "name": "buttons", "required": false, "type": { "name": "ReactNode" } }, "children": { "defaultValue": null, "description": "Messages of message center. I.e. [[MessageCenterMessage]]", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "prompt": { "defaultValue": null, "description": "", "name": "prompt", "required": false, "type": { "name": "string" } }, "tabs": { "defaultValue": null, "description": "Tabs of message center. See [[MessageCenterTab]]", "name": "tabs", "required": false, "type": { "name": "ReactNode" } }, "title": { "defaultValue": null, "description": "Title of title bar.", "name": "title", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class MessageCenter extends reactExports.PureComponent {
  render() {
    const {
      children,
      className,
      indicatorRef,
      label,
      onClick,
      targetRef,
      ...props
    } = this.props;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      FooterIndicator,
      {
        className: classnames("nz-footer-messageCenter-indicator", className),
        ...props,
        children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: "nz-indicator",
            onClick,
            ref: indicatorRef,
            role: "button",
            tabIndex: -1,
            children: [
              label !== void 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "nz-label", children: label }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "nz-container", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "nz-balloon", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-arrow" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-content", children })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-target", ref: targetRef })
              ] })
            ]
          }
        )
      }
    );
  }
}
try {
  MessageCenter.displayName = "MessageCenter";
  MessageCenter.__docgenInfo = { "description": "Message center indicator used in [[Footer]] component.", "displayName": "MessageCenter", "props": { "children": { "defaultValue": null, "description": "Message center balloon content.", "name": "children", "required": false, "type": { "name": "(string & (boolean | ReactChild | ReactFragment | ReactPortal | null))" } }, "indicatorRef": { "defaultValue": null, "description": "Clickable part of the indicator.", "name": "indicatorRef", "required": false, "type": { "name": "Ref<HTMLDivElement>" } }, "label": { "defaultValue": null, "description": "Message center label.", "name": "label", "required": false, "type": { "name": "string" } }, "onClick": { "defaultValue": null, "description": "Function called when indicator is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "targetRef": { "defaultValue": null, "description": "Message center dialog target.", "name": "targetRef", "required": false, "type": { "name": "Ref<HTMLDivElement>" } }, "title": { "defaultValue": null, "description": "Title for the indicator", "name": "title", "required": false, "type": { "name": "string" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class MessageCenterMessage extends reactExports.PureComponent {
  render() {
    const className = classnames(
      "nz-footer-messageCenter-message",
      this.props.className
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className, style: this.props.style, children: [
      this.props.icon && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-icon", children: this.props.icon }),
      this.props.children && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "nz-content", children: this.props.children })
    ] });
  }
}
try {
  MessageCenterMessage.displayName = "MessageCenterMessage";
  MessageCenterMessage.__docgenInfo = { "description": "Message entry in [[MessageCenterDialog]] component.", "displayName": "MessageCenterMessage", "props": { "children": { "defaultValue": null, "description": "Message content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "icon": { "defaultValue": null, "description": "Message icon.", "name": "icon", "required": false, "type": { "name": "ReactNode" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class MessageCenterTab extends reactExports.PureComponent {
  render() {
    const className = classnames(
      "nz-footer-messageCenter-tab",
      this.props.isActive && "nz-active",
      this.props.className
    );
    return (
      // eslint-disable-next-line jsx-a11y/click-events-have-key-events
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "div",
        {
          className,
          style: this.props.style,
          onClick: this.props.onClick,
          role: "tab",
          tabIndex: -1,
          children: this.props.children
        }
      )
    );
  }
}
try {
  MessageCenterTab.displayName = "MessageCenterTab";
  MessageCenterTab.__docgenInfo = { "description": "Message center tab used in [[MessageCenterDialog]] component.", "displayName": "MessageCenterTab", "props": { "children": { "defaultValue": null, "description": "Tab content.", "name": "children", "required": false, "type": { "name": "ReactNode" } }, "isActive": { "defaultValue": null, "description": "Describes if the tab is active.", "name": "isActive", "required": false, "type": { "name": "boolean" } }, "onClick": { "defaultValue": null, "description": "Function called when the tab is clicked.", "name": "onClick", "required": false, "type": { "name": "(() => void)" } }, "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
class MessageCenterField extends reactExports.Component {
  constructor(p) {
    super(p);
    this._indicator = reactExports.createRef();
    this._handleMessagesUpdatedEvent = () => {
      if (this._unloadMessagesUpdatedHandler)
        this.setState({ messageCount: MessageManager.messages.length });
    };
    this._handleOpenMessageCenterEvent = () => {
      this.setIsOpen(true);
    };
    this._handleClose = () => {
      this.setIsOpen(false);
    };
    this._handleOutsideClick = (e) => {
      if (!this._indicator.current || !(e.target instanceof Node) || this._indicator.current.contains(e.target))
        return;
      this._handleClose();
    };
    this._handleMessageIndicatorClick = () => {
      this.setIsOpen(!this.state.isOpen);
    };
    this._changeActiveTab = (tab2) => {
      this.setState({ activeTab: tab2 });
    };
    this.state = {
      activeTab: 0,
      target: null,
      messageCount: MessageManager.messages.length,
      isOpen: false
    };
  }
  /** @internal */
  componentDidMount() {
    this._unloadMessagesUpdatedHandler = MessageManager.onMessagesUpdatedEvent.addListener(
      this._handleMessagesUpdatedEvent,
      this
    );
    this._removeOpenMessagesCenterHandler = MessageManager.onOpenMessageCenterEvent.addListener(
      this._handleOpenMessageCenterEvent,
      this
    );
    MessageManager.registerAnimateOutToElement(this._indicator.current);
  }
  /** @internal */
  componentWillUnmount() {
    if (this._unloadMessagesUpdatedHandler) {
      this._unloadMessagesUpdatedHandler();
      this._unloadMessagesUpdatedHandler = void 0;
    }
    if (this._removeOpenMessagesCenterHandler) {
      this._removeOpenMessagesCenterHandler();
      this._removeOpenMessagesCenterHandler = void 0;
    }
  }
  render() {
    const title = UiFramework.translate("messageCenter.messages");
    const tooltip = `${this.state.messageCount} ${title}`;
    const divStyle = { ...this.props.style, height: "100%" };
    const footerMessages = /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: this.props.className, style: divStyle, title: tooltip, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        MessageCenter,
        {
          indicatorRef: this._indicator,
          label: title,
          onClick: this._handleMessageIndicatorClick,
          children: this.state.messageCount.toString()
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        StatusBar.Popup,
        {
          isOpen: this.state.isOpen,
          onClose: this._handleClose,
          onOutsideClick: this._handleOutsideClick,
          target: this._indicator.current,
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            MessageCenterDialog,
            {
              prompt: UiFramework.translate("messageCenter.prompt"),
              tabs: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  MessageCenterTab,
                  {
                    isActive: this.state.activeTab === 0,
                    onClick: () => this._changeActiveTab(
                      0
                      /* AllMessages */
                    ),
                    children: UiFramework.translate("messageCenter.all")
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  MessageCenterTab,
                  {
                    isActive: this.state.activeTab === 1,
                    onClick: () => this._changeActiveTab(
                      1
                      /* Problems */
                    ),
                    children: UiFramework.translate("messageCenter.errors")
                  }
                )
              ] }),
              title,
              children: this.getMessages()
            }
          )
        }
      )
    ] });
    return footerMessages;
  }
  getMessages() {
    const messages = MessageManager.messages.slice(0).reverse();
    const tabRows = new Array();
    messages.forEach((details, index) => {
      if (this.state.activeTab === 0 || this.isProblemStatus(details.priority)) {
        const iconClassName = MessageManager.getIconClassName(details);
        const iconSpec = MessageManager.getIconSpecFromDetails(details);
        const message = details.briefMessage;
        tabRows.push(
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            MessageCenterMessage,
            {
              icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1, { iconSpec, className: iconClassName }),
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(MessageRenderer$1, { message, useSpan: true }),
                details.detailedMessage && /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { variant: "small", children: /* @__PURE__ */ jsxRuntimeExports.jsx(MessageRenderer$1, { message: details.detailedMessage }) })
              ]
            },
            index.toString()
          )
        );
      }
    });
    return tabRows;
  }
  isProblemStatus(priority) {
    if (priority === OutputMessagePriority.Error || priority === OutputMessagePriority.Fatal)
      return true;
    return false;
  }
  setIsOpen(isOpen) {
    this.setState({ isOpen });
  }
}
try {
  MessageCenterField.displayName = "MessageCenterField";
  MessageCenterField.__docgenInfo = { "description": "Message Center Field React component.", "displayName": "MessageCenterField", "props": { "style": { "defaultValue": null, "description": "Custom CSS style properties", "name": "style", "required": false, "type": { "name": "CSSProperties" } }, "itemId": { "defaultValue": null, "description": "Optional unique identifier for item. If defined it will be added to DOM Element attribute as data-item-id", "name": "itemId", "required": false, "type": { "name": "string" } }, "className": { "defaultValue": null, "description": "Custom CSS class name", "name": "className", "required": false, "type": { "name": "string" } } } };
} catch (__react_docgen_typescript_loader_error) {
}
const AlignComponent = (Story) => {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", {
    style: {
      height: "100%",
      display: "flex",
      justifyContent: "center",
      paddingBlock: "2em"
    },
    children: /* @__PURE__ */ jsxRuntimeExports.jsx(Story, {})
  });
};
const meta = {
  title: "Components/MessageCenterField",
  component: MessageCenterField,
  tags: ["autodocs"],
  decorators: [AlignComponent, InitializerDecorator, AppUiDecorator],
  args: {
    style: {
      marginTop: 150
    }
  }
};
const Basic = {};
Basic.parameters = {
  ...Basic.parameters,
  docs: {
    ...(_a = Basic.parameters) == null ? void 0 : _a.docs,
    source: {
      originalSource: "{}",
      ...(_c = (_b = Basic.parameters) == null ? void 0 : _b.docs) == null ? void 0 : _c.source
    }
  }
};
const __namedExportsOrder = ["Basic"];
export {
  Basic,
  __namedExportsOrder,
  meta as default
};
